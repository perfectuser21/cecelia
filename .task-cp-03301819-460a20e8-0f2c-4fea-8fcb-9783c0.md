---
id: task-cp-03301819-460a20e8-0f2c-4fea-8fcb-9783c0
type: task-card
branch: cp-03301819-460a20e8-0f2c-4fea-8fcb-9783c0
created: 2026-03-31
---

# Task Card: PipelineOutputPage 设计细节优化

## 需求（What & Why）
优化 PipelineOutputPage 的生成阶段展示，删除 Hero 区重复缩略图，升级阶段步骤信息密度并提供重新生成能力。

## 成功标准

- Hero 区不再有封面缩略图，页面视觉不重复
- 生成阶段每步显示 started_at / completed_at 时间戳
- 生成阶段每步及 header 均有重新生成按钮，点击调用 POST /api/brain/pipelines/:id/run
- GenerationTab 接受 pipelineId prop 并用于接口调用
- CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] PipelineOutputPage 四 Tab 整体结构保持不变
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/content/pages/PipelineOutputPage.tsx','utf8');const tabs=['summary','generation','publish','analytics'];tabs.forEach(t=>{if(!c.includes(t))throw new Error('Tab missing: '+t)});console.log('4 tabs OK')"

- [x] [ARTIFACT] Hero 区封面缩略图已从页面 DOM 中移除
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/content/pages/PipelineOutputPage.tsx','utf8');const heroSection=c.slice(c.indexOf('标题区域'),c.indexOf('Tab 导航'));if(heroSection.includes('<img')&&heroSection.includes('thumbnail'))throw new Error('Hero 区仍包含缩略图');console.log('OK')"

- [x] [BEHAVIOR] Hero 区不再显示封面缩略图
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/content/pages/PipelineOutputPage.tsx','utf8');const lines=c.split('\\n');const heroStart=lines.findIndex(l=>l.includes('标题区域'));const tabStart=lines.findIndex(l=>l.includes('Tab 导航'));const heroBlock=lines.slice(heroStart,tabStart).join('\\n');if(heroBlock.includes('thumbnail'))throw new Error('Hero 有 thumbnail');console.log('OK')"

- [x] [BEHAVIOR] 生成阶段每步显示 started_at 和 completed_at 时间戳
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/content/pages/PipelineOutputPage.tsx','utf8');if(!c.includes('started_at'))throw new Error('缺 started_at');if(!c.includes('completed_at'))throw new Error('缺 completed_at');const stageBlock=c.slice(c.indexOf('执行阶段'),c.indexOf('图片产出'));if(!stageBlock.includes('formatTime('))throw new Error('阶段区块未调用 formatTime');console.log('OK')"

- [x] [BEHAVIOR] 生成阶段每步在存在 review_issues 时显示错误详情内容
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/content/pages/PipelineOutputPage.tsx','utf8');if(!c.includes('review_issues'))throw new Error('缺 review_issues');const hasConditionalRender=c.includes('review_issues.length')||c.includes('review_issues.map')||c.includes('info.review_issues');if(!hasConditionalRender)throw new Error('review_issues 未条件渲染');console.log('OK')"

- [x] [BEHAVIOR] 生成阶段每步显示重新生成按钮，点击后向 POST /api/brain/pipelines/:id/run 发起请求
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/content/pages/PipelineOutputPage.tsx','utf8');if(!c.includes('/run'))throw new Error('缺 /run');if(!c.includes('重新生成'))throw new Error('缺重新生成按钮');console.log('OK')"

- [x] [BEHAVIOR] 生成阶段 header 区域显示整体重新生成按钮
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/content/pages/PipelineOutputPage.tsx','utf8');const execStageBlock=c.slice(c.indexOf('执行阶段'),c.indexOf('图片'));if(!execStageBlock.includes('重新生成'))throw new Error('header 缺重新生成按钮');if(!execStageBlock.includes('/run'))throw new Error('header 重新生成未绑定 /run');console.log('OK')"

- [x] [BEHAVIOR] GenerationTab 接受 pipelineId prop 并将其传入重新生成接口调用
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/content/pages/PipelineOutputPage.tsx','utf8');const funcDef=c.match(/function GenerationTab[\\s\\S]*?\\)/);if(!funcDef||!funcDef[0].includes('pipelineId'))throw new Error('GenerationTab 缺 pipelineId prop');if(!c.includes('pipelineId')&&!c.includes('/run'))throw new Error('pipelineId 未用于 /run');console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/content/pages/PipelineOutputPage.test.tsx','utf8');if(!c.includes('describe')&&!c.includes('test(')&&!c.includes('it('))throw new Error('no tests');console.log('OK')"
