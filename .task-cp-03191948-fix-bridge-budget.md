---
id: task-cp-03191948-fix-bridge-budget
type: task-card
branch: cp-03191948-fix-bridge-budget
created: 2026-03-19
---

# Task Card: Bridge 崩溃修复 + 预算算法时间加权

## 需求（What & Why）
**功能描述**: 修复 Bridge ERR_HTTP_HEADERS_SENT 崩溃 + 预算算法加入时间加权（离 reset 越近越敢用）

## 成功标准
1. [BEHAVIOR] Bridge 不再因 HTTP headers 重复发送而崩溃
2. [BEHAVIOR] 预算算法考虑离 reset 的时间距离
3. [GATE] 所有现有测试通过

## 验收条件（DoD）

- [x] [BEHAVIOR] Bridge 用 safeRespond 防止重复响应崩溃
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.cjs','utf8');if(!c.includes('safeRespond'))process.exit(1)"

- [x] [BEHAVIOR] Bridge 有 uncaughtException 兜底不崩
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.cjs','utf8');if(!c.includes('uncaughtException'))process.exit(1)"

- [x] [BEHAVIOR] 预算算法 accountRemainingPct 包含时间加权逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/token-budget-planner.js','utf8');if(!c.includes('periodFraction') || !c.includes('effectiveRemaining'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"
