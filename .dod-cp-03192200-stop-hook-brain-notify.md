# DoD: Stop Hook 超时时创建 Brain 诊断任务

## 完成条件

- [x] stop-dev.sh 超时处理中添加 curl POST 创建 Brain P0 诊断任务
- [x] curl 使用 --max-time 5 非阻塞，失败不影响主流程
- [x] 保留现有 track.sh fail 和 Brain PATCH 逻辑
- [x] hooks/stop-dev.sh 根目录副本同步
- [x] Engine 版本 bump 到 13.3.3

## Test

- manual: node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('stuck_diagnosis'))process.exit(1)"
- manual: node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('max-time 5'))process.exit(1)"
- manual: node -e "const c=require('fs').readFileSync('hooks/stop-dev.sh','utf8');if(!c.includes('stuck_diagnosis'))process.exit(1)"
