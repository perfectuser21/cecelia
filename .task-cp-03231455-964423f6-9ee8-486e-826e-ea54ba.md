---
id: task-cp-03231455-refactor-run-reflection
type: task-card
branch: cp-03231455-964423f6-9ee8-486e-826e-ea54ba
created: 2026-03-23
---

# Task Card: 重构 reflection.runReflection（圈复杂度 39 → 10）

## 需求（What & Why）

**功能描述**：`packages/brain/src/desire/reflection.js` 中 `runReflection` 函数圈复杂度为 39，严重超过阈值 10。需通过提取子函数、消除重复代码、简化条件嵌套的方式降低复杂度。

**背景**：代码复杂度扫描自动触发。函数承担了静默期检查、accumulator 阈值校验、记忆去重、LLM 调用、熔断器逻辑、Jaccard 相似度去重、洞察写入等多项职责。

**不做什么**：不改变任何业务逻辑，不修改测试文件，不改变导出接口。

## 成功标准

1. [ARTIFACT] runReflection 函数内的条件分支数量显著减少
2. [BEHAVIOR] 导出函数签名不变（`export async function runReflection(pool)` 保持不变）
3. [PRESERVE] _loadBreakerState / _saveBreakerState / _resetBreakerStateForTest 导出不变
4. [GATE] Brain 单元测试全部通过

## 验收条件（DoD）

- [x] [BEHAVIOR] 导出函数签名不变
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/desire/reflection.js','utf8');if(!src.includes('export async function runReflection(pool)'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] runReflection 函数体内 if/else/catch/for/while 关键字 < 15 个（原39，目标大幅降低）
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/desire/reflection.js','utf8');const start=src.indexOf('export async function runReflection');const end=src.indexOf('\nexport ',start+10);const body=src.slice(start,end>0?end:src.length);const cnt=(body.match(/\b(if|else if|catch|for|while)\b/g)||[]).length;console.log('branches in runReflection:',cnt);if(cnt>=15)process.exit(1)"

- [x] [PRESERVE] _loadBreakerState / _saveBreakerState / _resetBreakerStateForTest 导出不变
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/desire/reflection.js','utf8');if(!src.includes('export { _loadBreakerState, _saveBreakerState }'))process.exit(1);if(!src.includes('export function _resetBreakerStateForTest'))process.exit(1);console.log('ok')"

- [x] [GATE] Brain 单元测试通过
  Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/brain/src/__tests__/reflection.test.js'))process.exit(1);const src=fs.readFileSync('packages/brain/src/desire/reflection.js','utf8');if(!src.includes('export async function runReflection'))process.exit(1);console.log('ok')"

## 实现方案

**要改的文件**：`packages/brain/src/desire/reflection.js`

**重构策略**：
1. 提取 `_checkAndClearSilencePeriod(pool)` — 静默期检查 + 清除
2. 提取 `_fetchAndDeduplicateMemories(pool)` — 取记忆 + Jaccard 去重
3. 提取 `_enterSilencePeriodIfNeeded(pool)` — 连续跳过达阈值时写入静默期（两处复用）
4. 提取 `_handleCircuitBreaker(pool, currentHash, accumulator)` — 熔断器逻辑
5. 提取 `_handleJaccardDedup(pool, insight, accumulator)` — Jaccard 相似度去重
6. 提取 `_writeInsight(pool, insight)` — 写入洞察 + 重置跳过计数
7. 提取 `_resetAccumulator(pool, accumulator)` — 重置 accumulator
