---
id: dod-mouth-brain-connect
version: 1.0.0
created: 2026-03-04
---

# DoD：修复嘴巴↔大脑通信断层

## 验收清单

- [x] callWithHistory 解析"文字+JSON"格式正确，reply 不含原始 JSON
  Test: manual:node -e "const t='文字\n{\"reply\":\"ok\",\"thalamus_signal\":null}'; const i=t.lastIndexOf('{\"reply\"'); const p=JSON.parse(t.slice(i)); console.assert(p.reply==='ok')"

- [x] GET /api/brain/feishu/groups 返回 200 + 群列表
  Test: manual:curl -s http://localhost:5221/api/brain/feishu/groups | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.assert(Array.isArray(d))"

- [x] POST /api/brain/feishu/send 存在且参数校验正确
  Test: manual:curl -s -X POST http://localhost:5221/api/brain/feishu/send -H 'Content-Type: application/json' -d '{}' | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8')); console.assert(d.error || d.success !== undefined)"

- [x] buildManifestBlock 不含假端点 feishu/groups 示例
  Test: manual:node -e "import('/app/src/orchestrator-chat.js').then(m=>m.buildManifestBlock()).then(b=>console.assert(!b.includes('feishu/groups')))"

- [x] 新增测试覆盖 callWithHistory 的"文字+JSON"解析场景
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js
