# DoD: 疏通 Slot 分配系统

- [x] [ARTIFACT] `MAX_PHYSICAL_CAP` 从 10 改为 20，`MEM_PER_TASK_MB` 统一为 400（三个文件一致）
  Test: manual:node -e "const fs=require('fs');const p=fs.readFileSync('packages/brain/src/platform-utils.js','utf8');const e=fs.readFileSync('packages/brain/src/executor.js','utf8');const c=fs.readFileSync('packages/brain/src/capacity.js','utf8');if(!p.includes('MAX_PHYSICAL_CAP = 20'))process.exit(1);if(!e.includes('MEM_PER_TASK_MB = 400'))process.exit(1);if(!c.includes('MEM_PER_TASK_MB = 400'))process.exit(1);if(!c.includes('MAX_PHYSICAL_CAP = 20'))process.exit(1)"

- [x] [BEHAVIOR] slot-allocator Token 压力不再砍 Pool C 并发数
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/slot-allocator.js','utf8');if(c.includes('poolCAfterResources = 0'))process.exit(1)"

- [x] [BEHAVIOR] 简化 Pool 分配：去掉 Pool A/B/C 固定预留，改为动态扣减
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/slot-allocator.js','utf8');if(c.includes('CECELIA_RESERVED = 2'))process.exit(1)"

- [x] [BEHAVIOR] dispatchAllowed 基于 effectiveSlots 减去实际运行数
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/slot-allocator.js','utf8');if(c.includes('poolCBudget > autoDispatchUsed'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npx vitest run 2>&1 | tail -5"
