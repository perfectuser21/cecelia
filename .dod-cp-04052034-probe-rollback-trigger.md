# DoD: 监控闭环 — capability-probe 连续失败自动触发回滚

branch: cp-04052034-probe-rollback-trigger

## 交付物

- [x] `packages/brain/src/capability-probe.js` 新增 `checkConsecutiveFailures(probeName)` 函数
- [x] `runProbeCycle()` 在批次失败后调用连续失败检查，达阈值触发回滚
- [x] 使用 `alerting.raise('P0', ...)` 在回滚触发前/后发飞书告警
- [x] `packages/brain/src/__tests__/capability-probe-rollback.test.js` 单元测试存在

## 行为验证

- [x] [BEHAVIOR] 单次探针失败不触发回滚（只告警）
  Test: `node -e "const c=require('fs').readFileSync('packages/brain/src/capability-probe.js','utf8');if(!c.includes('ROLLBACK_CONSECUTIVE_THRESHOLD'))process.exit(1);console.log('threshold defined')"`

- [x] [BEHAVIOR] 连续失败检测函数存在并查询 cecelia_events 历史记录
  Test: `node -e "const c=require('fs').readFileSync('packages/brain/src/capability-probe.js','utf8');if(!c.includes('checkConsecutiveFailures'))process.exit(1);if(!c.includes('capability_probe'))process.exit(1);console.log('ok')"`

- [x] [BEHAVIOR] 回滚前打印触发原因日志（含探针名 + 失败次数）
  Test: `node -e "const c=require('fs').readFileSync('packages/brain/src/capability-probe.js','utf8');if(!c.includes('[Probe] 触发自动回滚'))process.exit(1);console.log('log found')"`

- [x] [BEHAVIOR] 回滚执行结果写入飞书告警（成功/失败均告警）
  Test: `node -e "const c=require('fs').readFileSync('packages/brain/src/capability-probe.js','utf8');if(!c.includes('brain-rollback'))process.exit(1);if(!c.includes(\"raise('P0'\"))process.exit(1);console.log('ok')"`

- [x] [BEHAVIOR] 测试文件存在
  Test: `node -e "require('fs').accessSync('packages/brain/src/__tests__/capability-probe-rollback.test.js');console.log('test file exists')"`
