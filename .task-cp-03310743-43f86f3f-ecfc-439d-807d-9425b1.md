# Task Card: fix(brain): executeResearch 加入 NotebookLM web search 步骤

**分支**: cp-03310743-43f86f3f-ecfc-439d-807d-9425b1
**类型**: fix
**优先级**: P0

## 目标
在 executeResearch 中，`notebooklm use` 之后、`ask` 之前加入 web search 步骤，ask 完成后清空 notebook sources。

## DoD

- [x] [ARTIFACT] content-pipeline-executors.js 中 executeResearch 包含 `notebooklm source add-research` 调用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes('source add-research'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] content-pipeline-executors.js 中 executeResearch 包含 `notebooklm research wait` 调用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes('research wait'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] executeResearch 在 ask 之前执行 source add-research 和 research wait，ask 后清空 sources
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');const addIdx=c.indexOf('source add-research');const waitIdx=c.indexOf('research wait');const askIdx=c.indexOf('notebooklm ask');if(addIdx<0||waitIdx<0||askIdx<0)process.exit(1);if(addIdx>askIdx||waitIdx>askIdx)process.exit(1);if(!c.includes('deleteSource'))process.exit(1);console.log('OK')"

## 成功标准

- executeResearch 在 ask 之前先调用 `source add-research` 添加 web research sources
- `research wait --import-all` 在 ask 之前完成，确保 sources 已导入
- ask 完成后调用 `deleteSource` 清空 notebook sources 以便 pipeline 复用
