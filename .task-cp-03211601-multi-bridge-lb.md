# Task Card: 多机 Codex Bridge 负载均衡

## 成功标准

- [x] [BEHAVIOR] BRIDGE_ACCOUNTS 环境变量正确过滤账号
  Test: manual:node -e "process.env.BRIDGE_ACCOUNTS='team3,team4';const {ACCOUNTS}=require('./packages/brain/scripts/codex-bridge/codex-account-usage.cjs');if(ACCOUNTS.length!==2||!ACCOUNTS.includes('team3'))process.exit(1)"
- [x] [ARTIFACT] codex-bridge 路径可通过环境变量配置
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/codex-bridge/codex-bridge.cjs','utf8');if(!c.includes('WORK_DIR'))process.exit(1)"
- [x] [ARTIFACT] 新增西安 M1 bridge plist
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/codex-bridge/com.perfect21.codex-bridge-m1.plist','utf8');if(!c.includes('100.103.88.66'))process.exit(1)"
- [x] [BEHAVIOR] executor.js 支持多 bridge URL 路由
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('selectBestBridge'))process.exit(1)"
