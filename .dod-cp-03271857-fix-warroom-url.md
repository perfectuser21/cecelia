# DoD: fix GTDWarRoom 绝对 URL

## D1 修复绝对 URL
- [x] [ARTIFACT] GTDWarRoom.tsx 不再包含硬编码 localhost:5221
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/gtd/pages/GTDWarRoom.tsx','utf8');if(c.includes('localhost:5221'))process.exit(1);console.log('OK')"

## D2 使用 allSettled 独立容错
- [x] [BEHAVIOR] 两个 fetch 请求独立容错，单个失败不影响另一个
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/gtd/pages/GTDWarRoom.tsx','utf8');if(!c.includes('allSettled'))process.exit(1);console.log('OK')"

## D3 使用相对路径调用 Brain API
- [x] [ARTIFACT] brain tasks API 调用改为 /api/brain/tasks 相对路径
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/gtd/pages/GTDWarRoom.tsx','utf8');if(!c.includes('/api/brain/tasks'))process.exit(1);console.log('OK')"
