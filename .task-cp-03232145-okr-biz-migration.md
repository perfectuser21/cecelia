---
id: task-cp-03232145-okr-biz-migration
type: task-card
branch: cp-03232145-okr-biz-migration
created: 2026-03-23
---

# Task Card: Migration 182 — 新 OKR 表补充运营列

## 需求（What & Why）

**功能描述**: 为新 OKR 表（visions/objectives/key_results/okr_projects/okr_scopes/okr_initiatives）补充运营列，使这些表具备与旧 goals/projects 表同等的业务字段支持。

**背景**: Migration 181 已建立 goals/projects→新表的双向同步触发器，但新表缺少 description、priority、progress、starvation_score、completed_at、last_dispatch_at 等运营列，导致 ~50 个业务文件无法迁移到新表。本 PR 补充这些列并更新触发器，解锁后续代码迁移。

**不做什么**:
- 不修改任何业务 .js 代码文件
- 不添加 embedding/notion_* 列
- 不删除旧表

## 成功标准

1. [ARTIFACT] 存在 `packages/brain/migrations/182_okr_operational_columns.sql`
2. [ARTIFACT] `packages/brain/src/selfcheck.js` 中 `EXPECTED_SCHEMA_VERSION = '182'`
3. [BEHAVIOR] 新 OKR 表各层具备正确的运营列（visions/objectives/key_results/okr_projects/okr_scopes/okr_initiatives 均有 description）
4. [BEHAVIOR] okr_initiatives 有完整运营列：priority/progress/starvation_score/completed_at/last_dispatch_at
5. [BEHAVIOR] 触发器函数 sync_goal_to_okr_tables 已更新同步新增列
6. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] `packages/brain/migrations/182_okr_operational_columns.sql` 文件存在且包含 ALTER TABLE
  Test: manual:node --input-type=module -e "import {readFileSync} from 'fs';const c=readFileSync('packages/brain/migrations/182_okr_operational_columns.sql','utf8');if(!c.includes('ALTER TABLE visions'))process.exit(1)"

- [x] [ARTIFACT] selfcheck.js EXPECTED_SCHEMA_VERSION 更新为 '182'
  Test: manual:node --input-type=module -e "import {EXPECTED_SCHEMA_VERSION} from './packages/brain/src/selfcheck.js';if(EXPECTED_SCHEMA_VERSION!=='182')process.exit(1)"

- [x] [BEHAVIOR] Migration 182 SQL 在全部 6 张新 OKR 表上添加 description 列
  Test: manual:node --input-type=module -e "import {readFileSync} from 'fs';const c=readFileSync('packages/brain/migrations/182_okr_operational_columns.sql','utf8');const t=['visions','objectives','key_results','okr_projects','okr_scopes','okr_initiatives'];const m=t.filter(x=>!c.includes('ALTER TABLE '+x));if(m.length){console.error('Missing:',m);process.exit(1)}"

- [x] [BEHAVIOR] Migration 182 SQL 包含 okr_initiatives 全部运营列
  Test: manual:node --input-type=module -e "import {readFileSync} from 'fs';const c=readFileSync('packages/brain/migrations/182_okr_operational_columns.sql','utf8');const n=['priority','progress','starvation_score','completed_at','last_dispatch_at'];const m=n.filter(k=>!c.includes(k));if(m.length){console.error('Missing:',m);process.exit(1)}"

- [x] [BEHAVIOR] DB 中 okr_initiatives 表有 priority 列（DB 不可用时跳过）
  Test: manual:node --input-type=module -e "import {execSync} from 'child_process';try{const r=execSync('psql cecelia -tAc \"SELECT column_name FROM information_schema.columns WHERE table_name=\\'okr_initiatives\\' AND column_name=\\'priority\\'\"').toString().trim();if(!r)process.exit(1)}catch(e){}"

- [x] [BEHAVIOR] Migration 182 SQL 更新了触发器函数 sync_goal_to_okr_tables
  Test: manual:node --input-type=module -e "import {readFileSync} from 'fs';const c=readFileSync('packages/brain/migrations/182_okr_operational_columns.sql','utf8');if(!c.includes('sync_goal_to_okr_tables'))process.exit(1)"
