---
id: task-cp-03270407-39a6e137-4890-4317-a2bd-d981cb
type: task-card
branch: cp-03270407-39a6e137-4890-4317-a2bd-d981cb
created: 2026-03-27
---

# Task Card: Knowledge Modules 深度知识页接入 Dashboard

## 需求（What & Why）
**功能描述**: 将西安 Codex 生成的 86 个知识页接入 Dashboard，用原生组件渲染（不用 iframe）。
**背景**: Brain task 39a6e137，PR #1620 已合并实现全部功能。
**不做什么**: 不直接嵌入 HTML iframe，不修改知识页文件本身。

## 成功标准

- /knowledge/modules 页面展示所有模块，按 brain/engine/system/workflows 分组
- 每张卡片显示：标题、摘要描述（desc）、优先级标签（P0/P1/P2）
- 点击卡片可看到来源文件列表（source_files）
- GET /api/brain/knowledge/modules 从 BACKLOG.yaml 读取并返回结构化数据
- KnowledgeHome 新增深度知识页入口，现有4个卡片保持不变

## 验收条件（DoD）

- [x] [ARTIFACT] KnowledgeModules.tsx 存在，展示分组卡片列表（已在 PR #1620 实现）
  Test: manual:node -e "require('fs').accessSync('apps/api/features/knowledge/pages/KnowledgeModules.tsx')"

- [x] [ARTIFACT] KnowledgeModuleDetail.tsx 存在，显示 source_files（已在 PR #1620 实现）
  Test: manual:node -e "require('fs').accessSync('apps/api/features/knowledge/pages/KnowledgeModuleDetail.tsx')"

- [x] [BEHAVIOR] knowledge 路由代码包含 /modules 端点定义和 GROUP_LABELS 映射（已在 PR #1620 实现）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/knowledge.js','utf8');if(!c.includes('GROUP_LABELS')||!c.includes('modules'))process.exit(1)"

- [x] [BEHAVIOR] KnowledgeModuleDetail 组件读取 source_files 并渲染（已在 PR #1620 实现）
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/pages/KnowledgeModuleDetail.tsx','utf8');if(!c.includes('source_files'))process.exit(1)"

- [x] [BEHAVIOR] KnowledgeHome 有5张卡片，第5张为深度知识页入口（本次实现）
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/pages/KnowledgeHome.tsx','utf8');if(!c.includes('modules')||!c.includes('深度知识页'))process.exit(1)"

- [x] [GATE] knowledge 路由文件存在且包含 /modules 端点（已在 PR #1620 实现）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/knowledge.js','utf8');if(!c.includes('/modules'))process.exit(1)"

## 实现方案
**已完成（PR #1620）**:
- apps/api/features/knowledge/pages/KnowledgeModules.tsx
- apps/api/features/knowledge/pages/KnowledgeModuleDetail.tsx
- apps/api/features/knowledge/index.ts
- packages/brain/src/routes/knowledge.js

**本次实现**:
- apps/api/features/knowledge/pages/KnowledgeHome.tsx（加第5张卡片：深度知识页 → /knowledge/modules）
