# Task Card: .dod.md 归属校验（branch field check）

## 任务来源
Brain 自动调度任务：知识反刍 Batch 1，step-00 加入 .dod.md 归属校验

## 任务状态
**已实现** — 该功能已在 PR #1339 中完成，branch-protect.sh v27 已包含此功能。

## PRD 摘要
在 `packages/engine/hooks/branch-protect.sh` 中增加：
进入 worktree 后，若 `.dod.md` 存在，检查其 branch 字段是否与当前分支匹配，不匹配则报警（非阻断）。

## 成功标准
- branch-protect.sh 包含 `.dod.md branch field check` 注释，说明 v27 已实现该功能
- 当 .dod.md branch 字段不匹配时，输出 `[WARN] .dod.md 归属校验` 警告，不 exit 阻断

## DoD 条目

- [x] [BEHAVIOR] branch-protect.sh 增加 .dod.md branch 字段校验
  Test: manual: node -e "const fs=require('fs');const c=fs.readFileSync('packages/engine/hooks/branch-protect.sh','utf8');if(!c.includes('.dod.md branch field check'))process.exit(1)"

- [x] [BEHAVIOR] 校验不通过时输出警告信息（WARN）
  Test: manual: node -e "const fs=require('fs');const c=fs.readFileSync('packages/engine/hooks/branch-protect.sh','utf8');if(!c.includes('[WARN] .dod.md 归属校验'))process.exit(1)"

- [x] [BEHAVIOR] 不阻断正常工作流（仅 warning，注释说明不 exit）
  Test: manual: node -e "const fs=require('fs');const c=fs.readFileSync('packages/engine/hooks/branch-protect.sh','utf8');if(!c.includes('仅警告，不阻断'))process.exit(1)"
