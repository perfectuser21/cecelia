# Task Card: /plan SKILL.md PR→层级映射修复

## 需求（What & Why）
**功能描述**: /plan SKILL.md 工作量维度把 1PR 映射到 Initiative，应该是 Task。识别流程也把"单 PR 单 repo"判为 Initiative，应该判为 Task。修复映射对齐 capacity-budget 校准表。
**不做什么**: 不改 /decomp 或 /decomp-check

## 成功标准
1. [ARTIFACT] /plan SKILL.md 工作量维度 1PR→Task
2. [BEHAVIOR] 识别流程单 PR→Task
3. [GATE] 所有现有测试通过

## 验收条件（DoD）
- [x] [ARTIFACT] 工作量维度 1PR 映射到 Task
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/plan/SKILL.md','utf8');if(c.includes('1个PR | Initiative'))process.exit(1)"
- [x] [BEHAVIOR] 识别流程单 PR→Task
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/plan/SKILL.md','utf8');if(c.includes('单 PR 单 repo → Initiative'))process.exit(1)"
- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"
