# PRD: 修复意识层断链（emotion + narrative + model fallback）

## 背景

诊断发现 Cecelia 嘴巴回复死板的根本原因：意识层数据断链。

根因：
1. thalamus 使用 gpt-5.4-mini（OpenAI），OpenAI 返回空内容 → runEmotionLayer LLM 失败 → working_memory.emotion_state 13小时未更新
2. Anthropic 账号 429 → narrative 失败 → 日记停写
3. 没有 fallback → 触发 tier3 降级 → 模板回复

## 目标

1. emotion-layer.js：thalamus 失败时 fallback 到 mouth 模型
2. server.js：narrative 写入频率从每天1次提升至每6小时1次

## 涉及文件

- packages/brain/src/emotion-layer.js
- packages/brain/server.js

## 成功标准

## 成功标准

- [ ] emotion-layer.js 包含 fallback 逻辑
- [ ] server.js 包含 narrative 6小时 cron
