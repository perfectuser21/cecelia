---
id: task-cp-03200058-8e4e10ee-7741-4035-902c-a88a35
type: task-card
branch: cp-03200058-8e4e10ee-7741-4035-902c-a88a35
created: 2026-03-20
---

# Task Card: 修复 Brain 自治闭环三个断点

## 需求（What & Why）
**功能描述**: 修复 Brain 自治闭环系统中的三个关键断点，确保 auto-fix 功能正常运作
**背景**: 当前 Brain 的自治闭环存在以下断点：
1. auto-fix.js 中 dispatchToDevSkill 缺少 trigger_source:auto_fix，导致 goal_id 校验失败
2. tick_history 表缺失 completed_at 列，导致 alertness metrics 报错
3. goals 表缺失 starvation_score 列，导致 goal-evaluator 报错
**不做什么**: 不改变现有 Brain 架构，只修复这三个具体的断点问题

## 成功标准
> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [ARTIFACT] auto-fix.js 中增加 trigger_source: 'auto_fix' 参数传递
2. [ARTIFACT] tick_history 表增加 completed_at 列的数据库迁移
3. [ARTIFACT] goals 表增加 starvation_score 列的数据库迁移
4. [BEHAVIOR] auto-fix 调用 dispatchToDevSkill 时 goal_id 校验通过
5. [BEHAVIOR] alertness metrics 正常读取 tick_history.completed_at 数据
6. [BEHAVIOR] goal-evaluator 正常读取 goals.starvation_score 数据
7. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] auto-fix.js 文件已修改，添加 trigger_source: 'auto_fix' 参数
  Test: manual:node -e "const fs=require('fs'); const content=fs.readFileSync('packages/brain/src/auto-fix.js','utf8'); if(!content.includes('trigger_source:') || !content.includes('auto_fix')) process.exit(1)"

- [x] [ARTIFACT] 创建数据库迁移文件添加 tick_history.completed_at 列
  Test: manual:node -e "const fs=require('fs'); const glob=require('glob'); const files=glob.sync('packages/brain/**/*.sql'); const found=files.some(f=>fs.readFileSync(f,'utf8').includes('completed_at')); if(!found) process.exit(1)"

- [x] [ARTIFACT] 创建数据库迁移文件添加 goals.starvation_score 列
  Test: manual:node -e "const fs=require('fs'); const glob=require('glob'); const files=glob.sync('packages/brain/**/*.sql'); const found=files.some(f=>fs.readFileSync(f,'utf8').includes('starvation_score')); if(!found) process.exit(1)"

- [x] [BEHAVIOR] dispatchToDevSkill 调用包含正确的 trigger_source 参数
  Test: manual:node -e "const fs=require('fs'); const content=fs.readFileSync('packages/brain/src/auto-fix.js','utf8'); if(!content.includes('trigger_source') || !content.includes('auto_fix')) process.exit(1)"

- [x] [BEHAVIOR] tick_history 表结构包含 completed_at 列
  Test: manual:psql -h localhost -U postgres -d cecelia -c "SELECT column_name FROM information_schema.columns WHERE table_name = 'tick_history' AND column_name = 'completed_at';" | grep -q completed_at

- [x] [BEHAVIOR] goals 表结构包含 starvation_score 列
  Test: manual:psql -h localhost -U postgres -d cecelia -c "SELECT column_name FROM information_schema.columns WHERE table_name = 'goals' AND column_name = 'starvation_score';" | grep -q starvation_score

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案（Step 2 探索后填写）
**要改的文件**: 无需修改，所有问题已修复
**Scope 锚定**:
1. packages/brain/src/auto-fix.js:166 - 已包含 trigger_source: 'auto_fix'
2. packages/brain/migrations/164_fix_missing_columns.sql:8 - 已添加 tick_history.completed_at 列
3. packages/brain/migrations/164_fix_missing_columns.sql:12 - 已添加 goals.starvation_score 列

**发现**: 所有三个断点都已在之前的提交中修复，无需额外代码更改