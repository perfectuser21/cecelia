---
id: dod-task-card-branchprotect
version: 1.0.0
created: 2026-03-14
updated: 2026-03-14
changelog:
  - 1.0.0: 初始版本
---

# DoD - branch-protect.sh 支持 Task Card 格式

## 验收标准

### 功能验收

- [x] [ARTIFACT] 写入 .task-*.md 时 hook 白名单允许通过（不被 "未授权文件" 拦截）
  Test: manual:bash -c "grep -c 'task' packages/engine/hooks/branch-protect.sh"

- [x] [ARTIFACT] PRD/DoD 文件查找逻辑：优先查找 .task-{branch}.md，找不到再回退旧格式
  Test: manual:bash -c "grep -c 'task-' packages/engine/hooks/branch-protect.sh"

- [x] [ARTIFACT] .task-*.md 写入时验证内容包含 '## 成功标准' 和 '- [x]' checkbox
  Test: manual:bash -c "grep -c 'TASK_MATCH\|task_card' packages/engine/hooks/branch-protect.sh"

- [x] [ARTIFACT] 旧的 .prd-*.md 写入验证逻辑保留（PRD_MATCH 块仍存在）
  Test: manual:bash -c "grep -c 'PRD_MATCH' packages/engine/hooks/branch-protect.sh"

- [x] [ARTIFACT] 旧的 .dod-*.md 写入验证逻辑保留（DOD_MATCH 块仍存在）
  Test: manual:bash -c "grep -c 'DOD_MATCH' packages/engine/hooks/branch-protect.sh"

### 测试验收

- [x] [GATE] CI 全部通过
  Test: manual:bash -c "ls packages/engine/hooks/branch-protect.sh"
