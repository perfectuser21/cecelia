# PRD: 删除根目录旧版 check-dod-mapping.cjs，统一用 engine 版

> 完整需求请见 Task Card: `.task-cp-03171239-remove-old-check-dod-mapping.md`

## 背景

`scripts/devgate/check-dod-mapping.cjs`（根目录，3.9KB，Mar 7）是过时副本，
`packages/engine/scripts/devgate/check-dod-mapping.cjs`（26KB，Mar 15）是真实版本。
Step 1 skill 文件调用旧版导致本地自检和 CI 行为不一致。

## 目标

1. 删除 `scripts/devgate/check-dod-mapping.cjs`（根目录旧版）
2. 更新 `.agent-knowledge/engine.md` 中的命令路径
3. 更新 `.claude/CLAUDE.md` 中的命令路径
4. 修复 `packages/engine/tests/hooks/pr-gate-phase1.test.ts` 中使用旧格式的测试 fixture

## 成功标准

- 根目录旧版文件不存在
- 所有引用已更新为 packages/engine 版本路径
- CI 全部通过
