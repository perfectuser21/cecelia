---
id: dod-provider-agnostic-engine
version: 1.0.0
created: 2026-03-13
updated: 2026-03-13
changelog:
  - 1.0.0: 初始版本
---

# DoD: Provider-Agnostic Engine — 单一入口架构

## 验收清单

- [x] `packages/engine/lib/devloop-check.sh` 文件存在
  Test: manual:bash -c "grep -c 'devloop_check' packages/engine/lib/devloop-check.sh"

- [x] devloop-check.sh 包含 devloop_check 函数
  Test: manual:bash -c "grep -c 'devloop_check()' packages/engine/lib/devloop-check.sh"

- [x] stop-dev.sh 包含 source devloop-check.sh 指令（不再内联完成判断逻辑）
  Test: manual:bash -c "grep -c 'devloop-check.sh' packages/engine/hooks/stop-dev.sh"

- [x] runners/codex/runner.sh 文件存在
  Test: manual:bash -c "grep -c 'devloop_check' packages/engine/runners/codex/runner.sh"

- [x] runners/codex/runner.sh source devloop-check.sh
  Test: manual:bash -c "grep -c 'devloop-check.sh' packages/engine/runners/codex/runner.sh"

- [x] task-router.js 包含 codex_dev → xian 路由
  Test: manual:bash -c "grep -c 'codex_dev' packages/brain/src/task-router.js"

- [x] executor.js 包含 codex_dev 派发逻辑
  Test: manual:bash -c "grep -c 'codex_dev' packages/brain/src/executor.js"

- [x] Engine 版本 bump（6 个文件同步）
  Test: manual:bash -c "grep -c 'version' packages/engine/package.json"

- [x] feature-registry.yml 新增 changelog 条目
  Test: manual:bash -c "grep -c 'provider-agnostic\|codex_dev\|devloop-check' packages/engine/features/feature-registry.yml"

- [x] LEARNINGS.md 记录关键决策
  Test: manual:bash -c "grep -c 'devloop-check\|provider-agnostic\|单一入口' docs/LEARNINGS.md"
