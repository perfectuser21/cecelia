# Task Card: fix(dashboard): DocChatPage 左栏支持用户直接编辑文档

## PRD

左栏文档当前只能只读渲染，用户无法直接修改。需要加入编辑模式。

## 成功标准

- [x] [ARTIFACT] `apps/api/features/knowledge/pages/DocChatPage.tsx` 包含编辑模式切换逻辑
  Test: manual:node -e "const f=require('fs');const c=f.readFileSync('apps/api/features/knowledge/pages/DocChatPage.tsx','utf8');if(!c.includes('editMode')||!c.includes('textarea'))process.exit(1);console.log('OK')"
- [x] [BEHAVIOR] 左栏有「编辑」按钮，点击后切换为 textarea，可以修改 markdown 原文
  Test: manual:node -e "const f=require('fs');const c=f.readFileSync('apps/api/features/knowledge/pages/DocChatPage.tsx','utf8');if(!c.includes('editMode'))process.exit(1);console.log('OK')"
- [x] [BEHAVIOR] 编辑模式有「保存」按钮，调用 PUT /api/brain/design-docs/:id 保存内容
  Test: manual:node -e "const f=require('fs');const c=f.readFileSync('apps/api/features/knowledge/pages/DocChatPage.tsx','utf8');if(!c.includes('PUT'))process.exit(1);console.log('OK')"

## DoD
左栏可在渲染/编辑模式间切换，编辑并保存后内容持久化。
