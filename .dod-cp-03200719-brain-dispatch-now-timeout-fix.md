# DoD: Brain dispatch-now 端点 + devloop-check 超时修复

## 成功标准

1. [ARTIFACT] dispatch-now 路由已注册到 execution.js
2. [BEHAVIOR] dispatch-now 端点有完整的 vitest 测试覆盖
3. [BEHAVIOR] devloop-check 超时返回 blocked 而非 done
4. [BEHAVIOR] devloop-check 超时时创建 P0 诊断任务
5. [GATE] dispatch-now.test.js 测试文件存在且覆盖核心场景

## 验收条件（DoD）

- [x] [ARTIFACT] execution.js 包含 dispatch-now 路由注册
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(!c.includes('/dispatch-now'))process.exit(1)"

- [x] [BEHAVIOR] dispatch-now 端点验证 task_id 必填
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/dispatch-now.test.js','utf8');if(!c.includes('task_id'))process.exit(1)"

- [x] [BEHAVIOR] devloop-check 超时返回 blocked 状态（不再返回 done）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(c.includes('status\":\"done') && c.includes('CI 可能卡住'))process.exit(1);if(!c.includes('status\":\"blocked'))process.exit(1)"

- [x] [BEHAVIOR] devloop-check 超时时向 Brain 注册 P0 诊断任务
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('P0') || !c.includes('ci-timeout') || !c.includes('/api/brain/tasks'))process.exit(1)"

- [x] [GATE] dispatch-now 测试文件存在且包含核心测试场景
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/dispatch-now.test.js','utf8');if(!c.includes('dispatch-now') || !c.includes('describe'))process.exit(1)"
