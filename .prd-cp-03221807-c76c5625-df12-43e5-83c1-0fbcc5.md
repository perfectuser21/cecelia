# PRD: 重构 reflection.runReflection（圈复杂度 39 → 10）

## 背景
代码复杂度扫描发现 `packages/brain/src/desire/reflection.js` 中的 `runReflection` 函数圈复杂度为 39，严重超出阈值 10，维护困难。

## 目标
将 `runReflection` 函数圈复杂度降至 10 以下，通过提取子函数实现职责分离，不改变外部行为。

## 范围
- 修改文件：`packages/brain/src/desire/reflection.js`
- 不改变测试文件
- 不改变函数导出签名

## 成功标准
- `runReflection` 函数圈复杂度 ≤ 10
- 所有现有测试通过（reflection.test.js + cortex-reflection-breaker.test.js）
- 外部行为与重构前完全一致
