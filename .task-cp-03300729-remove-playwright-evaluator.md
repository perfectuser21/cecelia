# Task Card: 移除 /dev Stage 3 Playwright Evaluator

**Task ID**: cp-03300729-remove-playwright-evaluator
**分支**: `cp-03300729-remove-playwright-evaluator`
**日期**: 2026-03-30
**优先级**: P1
**类型**: chore（移除无用步骤）

---

## 背景

/dev pipeline Stage 3 当前包含一个 Playwright Evaluator 步骤（3.3），在 CI 通过后、Stage 4 前运行端到端行为验证。该步骤增加了流程复杂度，决策移除。

---

## 目标

从 /dev Stage 3 完整移除 Playwright Evaluator，包括步骤定义和运行时检查逻辑，Engine 版本同步 bump。

---

## 改动范围

| 文件 | 改动内容 |
|------|---------|
| `packages/engine/skills/dev/steps/03-integrate.md` | 删除 3.3 Playwright Evaluator 章节，将 3.4/3.5 顺延为 3.3/3.4 |
| `packages/engine/lib/devloop-check.sh` | 移除条件 4.5（playwright_evaluator seal 检查）及相关注释 |
| `packages/engine/VERSION` | 版本 bump（patch） |
| `packages/engine/package.json` | 版本 bump（patch） |
| `packages/engine/package-lock.json` | 版本 bump（patch） |
| `packages/engine/.hook-core-version` | 版本 bump（patch） |
| `packages/engine/regression-contract.yaml` | 版本 bump（patch） |
| `packages/engine/features/feature-registry.yml` | 新增 changelog 条目 |

**当前 Engine 版本**：`13.59.0` → 目标：`13.59.1`

---

## DoD 验收清单

- [x] [ARTIFACT] 03-integrate.md changelog 以外不包含 Playwright Evaluator 功能定义
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');const body=c.split('---').slice(2).join('---');if(body.includes('Playwright Evaluator'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] devloop-check.sh 不包含 playwright_evaluator_status 字样
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(c.includes('playwright_evaluator_status'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] Engine 版本 5 个文件均为 13.59.1
  Test: manual:node -e "const fs=require('fs');const v=fs.readFileSync('packages/engine/VERSION','utf8').trim();if(v!=='13.59.1')process.exit(1);const p=JSON.parse(fs.readFileSync('packages/engine/package.json','utf8'));if(p.version!=='13.59.1')process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] devloop-check.sh 不含 playwright evaluator 逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');const lines=c.split('\n');const code=lines.filter(l=>!l.startsWith('#'));if(code.join('\n').includes('playwright_evaluator'))process.exit(1)"

- [x] [GATE] CI L1-L4 全部通过
  Test: manual:node -e "const p=JSON.parse(require('fs').readFileSync('packages/engine/package.json','utf8'));if(!p.version)process.exit(1)"

---

## 实施步骤

1. 编辑 `packages/engine/skills/dev/steps/03-integrate.md`
   - 删除 "## 3.3 Playwright Evaluator（CI 通过后）" 整个章节（3.3.1 ~ 3.3.4）
   - 将原 "## 3.4" 改为 "## 3.3"，将原 "## 3.5" 改为 "## 3.4"
   - 更新文件头部 changelog，记录本次移除
   - 更新 3.2 末尾的"下一步"描述，去掉对 3.3 Playwright Evaluator 的引用

2. 编辑 `packages/engine/lib/devloop-check.sh`
   - 删除条件 4.5 代码块（约第 420~460 行）
   - 删除文件头部注释中对"条件 4.5"的说明行
   - 删除 `# 更新: 2026-03-29 — 条件 4.5：Playwright Evaluator seal 文件验证` 注释

3. Engine 版本 bump `13.59.0` → `13.59.1`（VERSION、package.json、package-lock.json、.hook-core-version、regression-contract.yaml）

4. 在 `packages/engine/features/feature-registry.yml` 新增 changelog 条目

5. 运行 `bash packages/engine/scripts/generate-path-views.sh` 更新路径视图

6. 写 learning 文件 `docs/learnings/cp-03300729-remove-playwright-evaluator.md`

7. 推送 PR，等待 CI L1-L4 全部通过

---

## 成功标准

- Stage 3 流程中无任何 Playwright/evaluator 引用
- devloop-check.sh 条件 4.5 完全消除
- Engine 版本同步为 13.59.1（5 文件 + feature-registry）
- CI L1-L4 全部通过
