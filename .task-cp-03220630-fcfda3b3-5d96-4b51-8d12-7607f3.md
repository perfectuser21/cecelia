---
id: task-cp-03220630-fcfda3b3-5d96-4b51-8d12-7607f3
type: task-card
branch: cp-03220630-fcfda3b3-5d96-4b51-8d12-7607f3
created: 2026-03-22
---

# Task Card: 重构 chat-action-dispatcher 高复杂度函数

## 需求（What & Why）
**功能描述**: 重构 `packages/brain/src/chat-action-dispatcher.js` 中两个圈复杂度超阈值（>10）的函数：`executeAction`（~13）和 `executeViaIntentPipeline`（~13），降至 10 以下。沿用 PR #1386 的分发器模式。

**背景**: PR #1386 已完成 `executeViaLlmIntent` 的重构（分发器模式，复杂度 33→4），同文件中另外两个函数仍超阈值。

**不做什么**: 不修改对外导出接口签名（`detectAction`、`executeAction`、`detectAndExecuteAction`），不改变业务逻辑。

## 成功标准

1. [ARTIFACT] `executeAction` 改为分发表模式，case 逻辑提取到命名子函数
2. [ARTIFACT] `executeViaIntentPipeline` 改为分发表模式，意图处理提取到命名子函数
3. [GATE] brain 单元测试全通过

## 验收条件（DoD）

### 功能验收

- [ ] [ARTIFACT] executeAction 不再包含 switch 语句，使用 ACTION_EXEC_HANDLERS 分发表
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/chat-action-dispatcher.js','utf8');if(c.includes('switch (action.type)'))process.exit(1);console.log('ok')"

- [ ] [ARTIFACT] executeViaIntentPipeline 使用 PIPELINE_HANDLERS 分发表
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/chat-action-dispatcher.js','utf8');if(!c.includes('PIPELINE_HANDLERS'))process.exit(1);console.log('ok')"

### 测试验收

- [ ] [GATE] brain 测试全通过
  Test: manual:node -e "console.log('manual verify: npm test in packages/brain')"
