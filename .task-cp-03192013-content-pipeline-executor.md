---
id: task-cp-03192013-content-pipeline-executor
type: task-card
branch: cp-03192013-content-pipeline-executor
created: 2026-03-19
---

# Task Card: 内容工厂 Pipeline Executor 全链路实现

## 需求（What & Why）
**功能描述**: 为内容工厂的 4 个阶段实现 executor，让 pipeline 从"创建子任务但不执行"变为"自动执行每个阶段"：
1. content-research executor：调用 notebooklm CLI 拉取调研素材
2. content-generate executor：基于素材生成图文文案 + 长文
3. content-review executor：自动检查品牌关键词命中和禁用词
4. content-export executor：归档 + manifest.json + 在线预览页

**背景**: orchestrator 状态机已完整，但子任务创建后没有 executor 执行，全靠手动 callback。

**不做什么**:
- 不改 /share-card skill
- 不改 tick/scheduler 调度逻辑
- 不做八平台发布对接

## 成功标准
1. [ARTIFACT] content-pipeline-executors.js 包含 4 个 export function
2. [BEHAVIOR] research executor 能调用 notebooklm ask 命令
3. [BEHAVIOR] review executor 能检查品牌关键词和禁用词
4. [ARTIFACT] export executor 生成 manifest.json
5. [GATE] 不破坏 Brain 现有功能

## 验收条件（DoD）

- [ ] [ARTIFACT] content-pipeline-executors.js 存在且 export 4 个函数
  Test: manual:bash -c "grep -c 'export async function execute' packages/brain/src/content-pipeline-executors.js"

- [ ] [BEHAVIOR] research executor 包含 notebooklm 调用逻辑
  Test: manual:bash -c "grep -c 'notebooklm' packages/brain/src/content-pipeline-executors.js"

- [ ] [BEHAVIOR] review executor 包含品牌关键词检查
  Test: manual:bash -c "grep -c '能力.*系统.*一人公司\|BRAND_KEYWORDS\|BANNED_WORDS' packages/brain/src/content-pipeline-executors.js"

- [ ] [ARTIFACT] export executor 生成 manifest.json
  Test: manual:bash -c "grep -c 'manifest.json\|writeFileSync.*manifest' packages/brain/src/content-pipeline-executors.js"

- [ ] [BEHAVIOR] orchestrator 集成 executor 调用
  Test: manual:bash -c "grep -c 'executeResearch\|executeGenerate\|executeReview\|executeExport' packages/brain/src/content-pipeline-orchestrator.js"

- [ ] [GATE] Brain 服务启动正常
  Test: manual:bash -c "curl -s localhost:5221/api/brain/health | grep healthy"

## 实现方案
**要改的文件**:
- `packages/brain/src/content-pipeline-executors.js` — 新建，4 个 executor
- `packages/brain/src/content-pipeline-orchestrator.js` — 集成 executor 调用
**Scope 锚定**: 只改 brain 包，2 个文件
