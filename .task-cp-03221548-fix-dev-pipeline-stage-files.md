---
id: task-cp-03221548-fix-dev-pipeline-stage-files
type: task-card
branch: cp-03221548-fix-dev-pipeline-stage-files
created: 2026-03-22
task_id: 27581a85-d52b-4493-ad01-37b9d38bc498
---

# Task Card: 修复 /dev pipeline Stage 步骤文件 P0/P1/P2 问题

## 需求（What & Why）

**功能描述**: 修复 /dev pipeline 的 Stage 步骤文件（01-spec.md、02-code.md、03-integrate.md、04-ship.md）、verify-step.sh 和 SKILL.md 中的 P0/P1/P2 问题，提升开发工作流的正确性和可靠性。

**背景**: 现有 Stage 步骤文件存在多处错误：步骤顺序错误（垃圾清理在 npm test 之前）、编号重复（两个 2.3.5）、verify-step.sh 只跑 Engine npm test 且只验证 [BEHAVIOR] 条目、01-spec.md 无限重试无告警、04-ship.md 字段名不一致等问题。

**不做什么**: 不改 devloop-check.sh，不改 stop hooks，不加 timing 功能。

## 成功标准

1. [ARTIFACT] 02-code.md 中 2.3 章节编号正确（2.3.1 到 2.3.6），顺序正确（npm test → 垃圾清理 → CI镜像 → DoD Test → Task Card hash）
2. [ARTIFACT] verify-step.sh Gate 1 根据 changed files 跑所有相关 package 的 test（不只是 engine）
3. [ARTIFACT] verify-step.sh Gate 2 同时验证 [BEHAVIOR]、[ARTIFACT]、[GATE] 三类条目
4. [ARTIFACT] 01-spec.md 中 retry>20 时输出警告并创建 P1 任务到 Brain（不降级，仍继续重试）
5. [ARTIFACT] 04-ship.md 读 task_id 时兼容 brain_task_id 和 task_id 两种字段名
6. [ARTIFACT] 02-code.md 中的 codex-test-gen 悬空引用已删除，统一为"修复代码后重新调用 subagent"
7. [ARTIFACT] 04-ship.md 清理 TaskList 时加 owner/metadata 过滤，只清理当前 /dev 创建的 Task
8. [ARTIFACT] 00-worktree-auto.md 脚本路径优先使用 repo 内路径，再 fallback ~/.claude/
9. [ARTIFACT] verify-step.sh pipe 操作从禁止列表移除
10. [ARTIFACT] SKILL.md 澄清了"不要停顿"与"等 stop hook 放行"的区别
11. [ARTIFACT] 03-integrate.md 澄清了"禁止 check-version-sync"是指不在 PR 中改版本号，验证脚本可以跑
12. [ARTIFACT] 03-integrate.md commit 示例补充了 Task ID 字段
13. [GATE] CI 全部通过且 Engine 现有测试全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] 02-code.md 编号重复问题已修复（两个 2.3.5 → 重新编号 2.3.1-2.3.6）且顺序正确（自动化测试 → 垃圾清理 → CI 镜像）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');const matches=c.match(/### 2\.3\.\d+/g)||[];const nums=matches.map(m=>parseInt(m.replace('### 2.3.',''),10));const hasDups=nums.length!==new Set(nums).size;if(hasDups){console.error('编号重复:',nums);process.exit(1);}console.log('编号唯一:',nums.join(','))"

- [x] [ARTIFACT] 02-code.md 的 code_review_gate 重试循环中不再有 codex-test-gen 特殊分支（只在 changelog 中提到是正常的）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');const codeSection=c.split('## 2.4')[1]||'';if(codeSection.includes('codex-test-gen')){console.error('2.4 节仍含 codex-test-gen 特殊分支');process.exit(1);}console.log('2.4 节无 codex-test-gen 特殊分支')"

- [x] [ARTIFACT] 01-spec.md retry>20 时有警告逻辑（包含 '>20' 或 'retry_count > 20'）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('>20')&&!c.includes('> 20')){console.error('缺少 retry>20 警告');process.exit(1);}console.log('retry>20 警告逻辑存在')"

- [x] [ARTIFACT] 04-ship.md 兼容 brain_task_id 和 task_id 两种字段名
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/04-ship.md','utf8');if(!c.includes('brain_task_id')){console.error('缺少 brain_task_id 兼容');process.exit(1);}console.log('字段名兼容已添加')"

- [x] [ARTIFACT] 04-ship.md 清理 TaskList 时加过滤（包含 filter、owner 或 metadata 关键字）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/04-ship.md','utf8');if(!c.includes('filter')&&!c.includes('owner')&&!c.includes('metadata')){console.error('缺少 TaskList 过滤');process.exit(1);}console.log('TaskList 过滤逻辑存在')"

- [x] [ARTIFACT] 00-worktree-auto.md 脚本路径支持 fallback（包含 fallback 或 packages/engine/skills 路径）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/00-worktree-auto.md','utf8');if(!c.includes('fallback')&&!c.includes('packages/engine/skills')){console.error('缺少路径 fallback');process.exit(1);}console.log('路径 fallback 逻辑存在')"

- [x] [BEHAVIOR] verify-step.sh Gate 1 支持多包测试（根据 changed files 决定跑哪些 package 的 test）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');const hasMultiPkg=c.includes('brain_changed')||c.includes('packages/brain')||c.includes('apps_changed');if(!hasMultiPkg){console.error('Gate 1 不支持多包');process.exit(1);}console.log('Gate 1 多包逻辑存在')"

- [x] [BEHAVIOR] verify-step.sh Gate 2 支持 [ARTIFACT] 和 [GATE] 条目验证
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('ARTIFACT')||!c.includes('GATE')){console.error('Gate 2 不支持 ARTIFACT/GATE');process.exit(1);}console.log('Gate 2 多类型条目支持存在')"

- [x] [ARTIFACT] 03-integrate.md commit 示例包含 Task ID 字段（含 Task-ID 或 task_id）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('Task-ID')&&!c.includes('task_id')){console.error('缺少 Task ID');process.exit(1);}console.log('Task ID 字段存在')"

- [x] [ARTIFACT] 03-integrate.md 澄清 check-version-sync 可以跑但 PR 中不改版本
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('验证脚本')){console.error('缺少 check-version-sync 澄清');process.exit(1);}console.log('version-sync 澄清存在')"

- [x] [ARTIFACT] SKILL.md 澄清"不要停顿"与"等 stop hook 放行"区别（含 stop hook 放行相关文字）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(!c.includes('stop hook 放行')){console.error('缺少 stop hook 放行澄清');process.exit(1);}console.log('stop hook 放行澄清存在')"

- [x] [GATE] Engine 现有测试全部通过
  Test: manual:node -e "const {execSync}=require('child_process');try{execSync('npm test',{cwd:'packages/engine',stdio:'inherit'});}catch(e){process.exit(1);}"

## 实现方案

**要改的文件**:
1. `packages/engine/skills/dev/steps/02-code.md` — 修复编号重复（两个2.3.5 → 2.3.1-2.3.6）+ 调整步骤顺序（自动化测试→垃圾清理→CI镜像→DoD Test→Task Card hash）+ 删除 codex-test-gen 悬空引用
2. `packages/engine/skills/dev/steps/01-spec.md` — 添加 retry>20 警告 + P1 任务创建逻辑
3. `packages/engine/skills/dev/steps/04-ship.md` — 兼容 task_id/brain_task_id + TaskList 过滤
4. `packages/engine/skills/dev/steps/00-worktree-auto.md` — 脚本路径优先 repo 内路径再 fallback ~/.claude/
5. `packages/engine/skills/dev/steps/03-integrate.md` — 澄清 check-version-sync + 添加 Task ID 到 commit 示例
6. `packages/engine/skills/dev/SKILL.md` — 澄清"不要停顿"与"等 stop hook 放行"的区别
7. `packages/engine/hooks/verify-step.sh` — Gate 1 多包测试 + Gate 2 支持 [ARTIFACT]/[GATE]

**Scope 锚定**: packages/engine/skills/dev/ 和 packages/engine/hooks/verify-step.sh，不改 devloop-check.sh，不改 stop hooks
