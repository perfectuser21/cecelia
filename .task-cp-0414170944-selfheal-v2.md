---
id: task-cp-0414170944-selfheal-v2
type: task-card
branch: cp-0414170944-selfheal-v2
created: 2026-04-14
---

# Task Card: dev-lock 自愈放宽 + Subagent 全套回归要求

## 需求
1. 放宽 dev-lock 自愈：去掉 "HEAD 分支必须匹配" 条件，改为"dev-mode 所在目录必须是活跃 worktree"
2. 02-code.md Subagent 三角色模板加"相关目录全套回归"要求

## 成功标准
- stop-dev.sh v16.6.0 → v16.7.0，self-heal 新条件
- 新测试：main session 能自愈任何活跃 worktree 的 dev-mode
- 02-code.md 模板加 Subagent 回归范围规则
- feature-registry 14.11.0
- 6 版本文件同步

## DoD

- [ ] [BEHAVIOR] stop-dev.sh 放宽 self-heal（去掉 HEAD 分支检查，改为 worktree 目录检查）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(c.includes('_heal_branch != $_heal_cur_branch'))process.exit(1);if(!c.match(/git worktree list|_heal_in_worktrees|git -C.*rev-parse/))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] stop-dev.sh version bump v16.6.0 → v16.7.0
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('v16.7.0'))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] 新测试覆盖 main session 跨 worktree 自愈
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/tests/hooks/stop-hook-selfheal-cross-branch.test.ts','utf8');['main 仓库','跨 worktree','不同分支'].forEach(k=>{if(!c.includes(k))process.exit(1)});console.log('OK')"

- [ ] [BEHAVIOR] 02-code.md Subagent Implementer 模板含全套回归
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('相关目录全套回归'))process.exit(1);if(!c.includes('tests/hooks/'))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] 02-code.md Spec Reviewer 模板含"Implementer 是否跑全套"检查
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('是否跑了全套回归'))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] 02-code.md version bump 9.0.0 → 9.1.0
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('version: 9.1.0'))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] feature-registry 14.11.0 条目
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/feature-registry.yml','utf8');if(!c.includes('14.11.0'))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] 6 版本文件同步 14.11.0
  Test: manual:node -e "const fs=require('fs');const v='14.11.0';const files=['packages/engine/VERSION','packages/engine/.hook-core-version'];files.forEach(f=>{if(!fs.readFileSync(f,'utf8').trim().startsWith(v))process.exit(1)});if(!require('./packages/engine/package.json').version.startsWith(v))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] 无中文标点炸弹
  Test: manual:bash packages/engine/ci/scripts/check-chinese-punctuation-bombs.sh

## 实现方案
**修改**:
- `packages/engine/hooks/stop-dev.sh` (放宽 self-heal, 用 git worktree list 检查而非 HEAD 匹配)
- `packages/engine/skills/dev/steps/02-code.md` (Subagent 三角色模板加回归要求)
- `packages/engine/feature-registry.yml`
- 6 版本文件

**新建**:
- `packages/engine/tests/hooks/stop-hook-selfheal-cross-branch.test.ts`

**不改**: Enrich 层、其他 step 文件、devloop-check
