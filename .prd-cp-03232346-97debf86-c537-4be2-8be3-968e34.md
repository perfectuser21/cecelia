---
id: task-cp-03232346-97debf86-c537-4be2-8be3-968e34
type: task-card
branch: cp-03232346-97debf86-c537-4be2-8be3-968e34
created: 2026-03-24
---

# Task Card: planner-domain-routing.test.js fixture 清理

## 需求（What & Why）
**功能描述**: 修复 `planner-domain-routing.test.js` 的 test fixture，使 afterEach 显式清理新 OKR 表，并修正 createKR 的数据语义。

**背景**:
- migration 181/182 在 goals/projects 上建立了触发器，INSERT 时自动双写到新 OKR 表（key_results/okr_projects/okr_scopes/okr_initiatives）
- 当前 afterEach 只清理旧表（goals/projects），触发器虽然也会级联删除新表记录，但触发器用 `EXCEPTION WHEN OTHERS` 包裹，失败时只写 WARNING 不阻断——新表记录可能残留，导致测试间数据污染
- createKR 当前 INSERT type='area_okr'（对应 objectives 层），语义上应为 'area_kr'（对应 key_results 层）

**不做什么**: 不修改 generateArchitectureDesignTask 的业务逻辑；不修改 key_results 表结构（无 domain 列，保留 goals 作为 domain SSOT）

## 成功标准

- [ ] afterEach 显式清理 key_results/okr_projects/okr_scopes/okr_initiatives/objectives（不再依赖触发器级联）
- [ ] createKR 的 INSERT type 改为 'area_kr'（触发器正确同步到 key_results 而非 objectives）

## 验收条件（DoD）

- [ ] [ARTIFACT] createKR 使用 type='area_kr' 而非 'area_okr'
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/planner-domain-routing.test.js','utf8');if(!c.includes(\"'area_kr'\"))process.exit(1);console.log('PASS')"

- [ ] [ARTIFACT] afterEach 包含 DELETE FROM key_results
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/planner-domain-routing.test.js','utf8');if(!c.includes('key_results'))process.exit(1);console.log('PASS')"

- [ ] [ARTIFACT] afterEach 包含 DELETE FROM okr_projects/okr_scopes/okr_initiatives
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/planner-domain-routing.test.js','utf8');if(!c.includes('okr_projects')||!c.includes('okr_initiatives'))process.exit(1);console.log('PASS')"

- [ ] [PRESERVE] generateArchitectureDesignTask 现有测试全部通过
  Test: manual:node -e "console.log('verified by CI vitest run')"

- [ ] [GATE] CI 全部通过
  Test: manual:node -e "console.log('CI gate')"

## 实现方案
**要改的文件**: `packages/brain/src/__tests__/planner-domain-routing.test.js`
**Scope 锚定**:
1. createKR 函数：将 `type, 'area_okr'` 改为 `type, 'area_kr'`（第49行附近）
2. afterEach：在现有清理逻辑前补充显式删除 key_results/objectives/okr_initiatives/okr_scopes/okr_projects
