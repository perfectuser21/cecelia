# DoD: fix(brain): 修复 content-pipeline 重启误杀 + 僵尸子任务堆积

## [ARTIFACT] executor.js 已更新
- [x] `syncOrphanTasksOnStartup` 中 `run_id = null` 的任务走 requeue 路径（不 quarantine）
- Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('!runId'))process.exit(1);console.log('ok')"`

## [ARTIFACT] content-pipeline-orchestrator.js 已更新
- [x] `executeQueuedContentTasks` 执行前检查父 pipeline 状态
- [x] 父任务已 failed/cancelled 的子任务被标记 cancelled 并跳过
- Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8');if(!c.includes('parentResult'))process.exit(1);console.log('ok')"`

## [BEHAVIOR] syncOrphanTasksOnStartup 不杀无进程任务
- [x] run_id=null 的 in_progress 任务在 orphan 检测中走 requeue 路径，不标记 failed
- Test: `tests/brain/content-pipeline-orphan.test.ts`

## [BEHAVIOR] 僵尸子任务自动清理
- [x] executeQueuedContentTasks 跳过父任务已 failed 的子任务，将其标记 cancelled
- Test: `tests/brain/content-pipeline-orphan.test.ts`

## 成功标准
- Brain 重启后 content-pipeline 父任务被 requeue 而非 failed
- 父任务 failed 后其 queued 子任务自动 cancelled（不再被执行）
