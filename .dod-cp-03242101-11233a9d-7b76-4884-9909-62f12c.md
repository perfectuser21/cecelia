---
branch: cp-03242101-11233a9d-7b76-4884-9909-62f12c
type: dod
---

# DoD: P0 修复 — RCA→auto-fix 无限重试，熔断机制失效

## 验收条件

- [ ] [ARTIFACT] capability-probe.js signature 改为 `probe_${f.name}`（去掉 `_${Date.now()}`）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/capability-probe.js','utf8');if(c.includes('`probe_${f.name}_${Date.now()}`'))process.exit(1);if(!c.includes('`probe_${f.name}`'))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] dispatchToDevSkill guard 查询同时覆盖 queued/in_progress 状态
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/auto-fix.js','utf8');if(!c.includes(\"queued\") || !c.includes(\"in_progress\"))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] 同一 signature 重复调用 dispatchToDevSkill，第二次因 guard（queued/in_progress）命中返回 null
  Test: tests/brain/auto-fix.test.js

- [ ] [GATE] CI 全部通过（brain 包现有测试不回归）
  Test: tests/brain/auto-fix.test.js

- [ ] [PRESERVE] dispatchToDevSkill 在 failed_count >= MAX_AUTO_FIX_ATTEMPTS 时返回 null（熔断逻辑保留）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/auto-fix.js','utf8');if(!c.includes('MAX_AUTO_FIX_ATTEMPTS') || !c.includes('return null'))process.exit(1);console.log('OK')"

- [ ] [PRESERVE] capability-probe.js 在 shouldAutoFix 检查通过后才触发 dispatchToDevSkill
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/capability-probe.js','utf8');if(!c.includes('shouldAutoFix') || !c.includes('dispatchToDevSkill'))process.exit(1);console.log('OK')"
