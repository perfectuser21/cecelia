# Task: runner.sh v2.5.0 — 删除 API Key 注入，强制 OAuth 模式

**Branch**: cp-03241123-remove-runner-apikey
**Type**: feat(engine)

## PRD

runner.sh v2.1.0 引入了从 `~/.credentials/openai.env` 加载 OPENAI_API_KEY 并导出为
CODEX_API_KEY 的逻辑，导致 Codex 始终以 API Key 模式启动而非 ChatGPT OAuth 包月模式。

删除该代码块（原 lines 103-125），确保所有 Codex runner 使用 ChatGPT OAuth 认证。

## 成功标准

- runner.sh 不再包含 CODEX_API_KEY 加载逻辑
- runner.sh 不再读取 openai.env credentials 文件
- runner.sh 版本号已更新为 v2.5.0

## DoD

- [x] [BEHAVIOR] runner.sh 不再包含 CODEX_API_KEY 加载逻辑
  Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/runners/codex/runner.sh','utf8');if(c.includes('加载 API Key'))process.exit(1);console.log('PASS')"

- [x] [BEHAVIOR] runner.sh 不再读取 openai.env credentials 文件
  Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/runners/codex/runner.sh','utf8');if(c.includes('openai.env'))process.exit(1);console.log('PASS')"

- [x] [BEHAVIOR] runner.sh 版本号已更新为 v2.5.0
  Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/runners/codex/runner.sh','utf8');if(!c.includes('v2.5.0'))process.exit(1);console.log('PASS')"
