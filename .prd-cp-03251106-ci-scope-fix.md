# PRD: CI scope 补全 — routing-map 路径扩展 + Brain-API 集成测试

## 背景

PR #1533 已关闭（落后 main 27 commit），但以下改动尚未在 main 中：

1. `ci/routing-map.yml` — `ci-helpers` 仅列具体文件名，`ci-configuration` 仅列 2 个文件。新增 scripts 或 ci/ 文件时路由无保护。
2. `apps/api/src/__tests__/brain-api-integration.test.ts` — Brain↔Frontend API proxy 层无集成测试。

## 成功标准

- `ci-helpers` 路径改为 `scripts/**`（排除 `scripts/devgate/**`），覆盖所有 scripts/ 根文件
- `ci-configuration` 路径改为 `ci/**`，覆盖所有 ci/ 文件
- `brain-api-integration.test.ts` 覆盖 proxy 层正确性（成功/失败/timeout 场景）
- `node scripts/ci-evolution-check.mjs` 继续通过

## 不做什么

- 不修改 ci-l4-runtime.yml（workspace L4 触发器涉及新 L4 job，超出本次范围）
- 不修改 workspace routing-map 层级（维持 l1, l3）
