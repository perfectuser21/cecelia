## DoD - Stop Hook 重试上限 15→30 + 失败后通知 Brain API

### 验收清单

- [x] stop-dev.sh 重试上限从 15 改为 30
  Test: manual:bash -c "grep -c 'RETRY_COUNT -gt 30' packages/engine/hooks/stop-dev.sh"

- [x] stop-dev.sh 错误消息中所有 "15" 改为 "30"
  Test: manual:bash -c "grep -c '重试 30 次' packages/engine/hooks/stop-dev.sh"

- [x] 30 次失败后调用 Brain API（task_id 存在时）
  Test: manual:bash -c "grep -c 'PATCH.*brain/tasks' packages/engine/hooks/stop-dev.sh"

- [x] Brain API 调用失败时用 || true 忽略（不影响 stop hook 流程）
  Test: manual:bash -c "grep -c 'max-time 5.*2>/dev/null || true' packages/engine/hooks/stop-dev.sh"

- [x] Engine 版本 bump（6 个文件同步）
  Test: manual:bash -c "grep -c '12.52.0' packages/engine/VERSION"

- [x] feature-registry.yml 新增 changelog 条目（3.38.0）
  Test: manual:bash -c "grep -c '3.38.0' packages/engine/features/feature-registry.yml"

- [x] regression-contract.yaml 版本同步
  Test: manual:bash -c "grep -c '12.52.0' packages/engine/regression-contract.yaml"
