# Task Card: PR Review 阻塞门禁 + Divergence 可执行检查

## 需求（What & Why）

**功能描述**: 将 PR Review workflow 从"建议模式"升级为"阻塞模式"，同时将 divergence_count 检查从 SKILL.md 文字描述提升为 devloop-check.sh 中的可执行断言。

**背景**:
1. `.github/workflows/pr-review.yml` 使用 DeepSeek V3 进行 AI 代码审查，已加入 branch protection 必填 check，但当 review 发现🔴严重问题时 job 仍然成功退出（exit 0），不阻塞合并。
2. `divergence_count >= 1` 检查（Evaluator 至少要与主 agent 有 1 次分歧）当前只存在于 `packages/workflows/skills/spec-review/SKILL.md` 文字描述中，不是可执行代码，可被绕过。
3. 两处"软约束"需升级为硬门禁，才能真正保护代码质量和 Evaluator 独立性。

**不做什么**:
- 不改变 AI review 的 prompt 内容或审查逻辑
- 不修改 branch protection 规则本身
- 不重写 spec-review SKILL.md 的其他部分
- 不改变 devloop-check.sh 中其他现有检查项的行为

---

## 成功标准

1. [ARTIFACT] `pr-review.yml` 改为直接调用 OpenRouter API，提取 review 文本，检测到🔴时以 exit 1 结束 job
2. [BEHAVIOR] `scripts/devgate/detect-review-issues.js` 独立脚本：stdin 包含🔴时 exit 1
3. [BEHAVIOR] `devloop-check.sh` 中 `check_divergence_count` 函数：值为 0 时返回非零退出码
4. [ARTIFACT] feat PR 测试文件（pr-review-blocking.test.ts + devloop-check-divergence.test.ts）
5. [GATE] CI 全部通过

---

## 验收条件（DoD）

- [x] [PRESERVE] pr-review.yml 仍在每个 PR 创建/更新时触发，结果仍以评论形式发布到 PR
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/pr-review.yml','utf8');if(!c.includes('pull_request'))process.exit(1);if(!c.includes('pull-requests: write'))process.exit(1);console.log('OK')"

- [x] [PRESERVE] devloop-check.sh 中现有的其他检查项（seal 验证、step_1_spec/step_2_code 等）行为不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('step_1_spec'))process.exit(1);if(!c.includes('seal'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] pr-review.yml 不再使用 hustcer/deepseek-review Action，包含 OpenRouter API 调用和 detect-review-issues.js 调用
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/pr-review.yml','utf8');if(c.includes('hustcer/deepseek-review'))process.exit(1);if(!c.includes('openrouter.ai'))process.exit(1);if(!c.includes('detect-review-issues'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] scripts/devgate/detect-review-issues.js：stdin 包含🔴时 exit 1，不含时 exit 0
  Test: manual:node -e "const{spawnSync}=require('child_process');const cwd=process.cwd();const r1=spawnSync('node',['scripts/devgate/detect-review-issues.js'],{input:'审查结果：🔴 严重漏洞',encoding:'utf8',cwd});if(r1.status!==1){process.exit(1)};const r2=spawnSync('node',['scripts/devgate/detect-review-issues.js'],{input:'代码质量良好',encoding:'utf8',cwd});if(r2.status!==0){process.exit(1)};console.log('OK')"

- [x] [ARTIFACT] packages/engine/lib/devloop-check.sh 包含 check_divergence_count 可执行函数
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('check_divergence_count'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] check_divergence_count 门禁语义：0（evaluator 橡皮图章）返回非零，1（至少1次分歧）返回 0（divergence_count 是 Evaluator 独立发现的问题数，>= 1 证明 Evaluator 真正挑战了 Planner）
  Test: manual:bash -c "source packages/engine/lib/devloop-check.sh && ! check_divergence_count 0 && check_divergence_count 1"

- [x] [GATE] feat PR 测试文件存在（满足 CI L3 测试覆盖要求）
  Test: manual:node -e "if(!require('fs').existsSync('packages/engine/tests/workflows/pr-review-blocking.test.ts'))process.exit(1);if(!require('fs').existsSync('packages/engine/tests/scripts/devloop-check-divergence.test.ts'))process.exit(1);console.log('OK')"

---

## 实现方案（Stage 2 探索后填写）

**要改的文件**:
- `.github/workflows/pr-review.yml` — 重写为直接调 OpenRouter API + 调用 detect-review-issues.js
- `scripts/devgate/detect-review-issues.js` — 新建，stdin 检测🔴，exit 1/0
- `packages/engine/lib/devloop-check.sh` — 新增 `check_divergence_count()` 函数
- `packages/engine/tests/workflows/pr-review-blocking.test.ts` — 新建 feat 测试
- `packages/engine/tests/scripts/devloop-check-divergence.test.ts` — 新建 feat 测试

**Scope 锚定**: CI workflow + DevGate 脚本 + Engine 测试，不涉及 Brain 或 apps
