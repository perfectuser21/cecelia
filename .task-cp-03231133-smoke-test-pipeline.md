---
id: task-cp-03231133-smoke-test-pipeline
type: task-card
branch: cp-03231133-smoke-test-pipeline
created: 2026-03-23
---

# Task Card: smoke test: /dev pipeline 端到端冒烟验证

## 需求（What & Why）

**功能描述**: 验证 verify-step.sh seal 修复后，/dev pipeline 从 Stage 1 跑到 Stage 4 全流程正常，stop hook 不再死循环。

**背景**: PR #1415 修复了 seal key 命名不一致（P0 死循环根因）。本次任务通过实际跑一次极简改动来验证修复确实生效。

**不做什么**: 不做任何功能性改动，只加一行注释作为冒烟载体。

## 成功标准

1. [ARTIFACT] packages/engine/lib/devloop-check.sh 末尾存在 smoke-test-20260323 注释行
2. [BEHAVIOR] /dev 全流程无死循环：Stage 1→2→3→4→PR 合并→stop hook exit 0
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] devloop-check.sh 现有逻辑不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('cleanup_done'))process.exit(1)"

- [x] [ARTIFACT] devloop-check.sh 末尾有 smoke-test-20260323 注释
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('smoke-test-20260323'))process.exit(1)"

- [x] [BEHAVIOR] verify-step.sh 写入的 seal key 格式为 step_N_type_seal
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('step_1_spec_seal'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('_seal_key}'))process.exit(1)"

## 实现方案（Stage 2 探索后填写）

**要改的文件**: packages/engine/lib/devloop-check.sh（末尾加一行注释）
**Scope 锚定**: packages/engine/lib/
