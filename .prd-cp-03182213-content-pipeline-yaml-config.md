# PRD: content-pipeline 读取 YAML 配置（PR3）

## 背景
内容类型注册表 Initiative（330bce6f）PR3 — PR1 已完成 YAML 注册表，现在让 Pipeline 实际使用配置。

## 目标
修改 content-pipeline-orchestrator.js，在创建 content-generate/content-review 子任务时，
通过 getContentType() 读取 YAML 中的 generate_prompt、images.count、review_rules，而非硬编码。

## 成功标准
1. content-generate 描述使用 YAML template.generate_prompt
2. content-generate payload 含 images_count（来自 YAML）
3. content-review payload 含 review_rules（来自 YAML）
4. content_type 不存在时 pipeline 标 failed
5. CI 全部通过
