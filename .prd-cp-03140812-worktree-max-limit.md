## PRD - worktree-manage.sh 新增数量上限检查（MAX_WORKTREES=8）

**需求来源**: Brain Task cec2bf39-a978-4926-ba9d-81b84ee4b179
**功能描述**: 在 `packages/engine/skills/dev/scripts/worktree-manage.sh` 的 `cmd_create()` 函数中，创建 worktree 之前检查现有数量，超过 MAX_WORKTREES=8 时拒绝创建并给出清晰的错误提示
**涉及文件**: `packages/engine/skills/dev/scripts/worktree-manage.sh`
**非目标**: 不自动清理 worktree；不修改 GC 策略；不改变其他 cmd_* 函数

## 成功标准

1. [ARTIFACT] worktree-manage.sh 中 cmd_create() 包含 MAX_WORKTREES=8 常量定义
2. [ARTIFACT] cmd_create() 包含 existing_count 数量检查逻辑
3. [BEHAVIOR] 当现有 worktree 数量 >= 8 时，create 命令以 exit 1 退出并输出错误信息
4. [GATE] CI 全部通过

### 异常处理 & 报警规范

N/A - 无 IO 操作，纯脚本逻辑
