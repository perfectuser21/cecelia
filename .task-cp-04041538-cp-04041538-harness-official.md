---
id: task-harness-official
type: task-card
branch: cp-04041538-harness-official
created: 2026-04-04
---

# Task Card: Harness v2.0 官方1:1复刻

## 需求（What & Why）
**功能描述**: 按 Anthropic 官方 Harness 设计完整实现三层流程
**背景**: 当前 Planner 做了太多（读代码+拆Sprint），且缺少 Sprint Contract 对抗协商层
**不做什么**: 不改 /dev 标准流程、不改 code_review、不改 /decomp

## 官方流程
```
sprint_planner → PRD（不碰代码）
  ↓
sprint_contract_propose ↔ sprint_contract_review（对抗协商 → Sprint Contract）
  ↓  APPROVED
sprint_generate → sprint_evaluate（代码对抗）
  ↓  PASS
→ 下一个 Sprint 的 contract_propose（循环，直到 PRD 全部完成）
  ↓  全部完成
arch_review
```

## 验收条件（DoD）

- [x] [ARTIFACT] task-router.js 包含 sprint_planner/sprint_contract_propose/sprint_contract_review
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/task-router.js','utf8');['sprint_planner','sprint_contract_propose','sprint_contract_review'].forEach(t=>{if(!c.includes(t))process.exit(1)})"

- [x] [ARTIFACT] execution.js 包含 contract_review APPROVED/REVISION 断链
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(!c.includes('APPROVED'))process.exit(1);if(!c.includes('REVISION'))process.exit(1)"

- [x] [ARTIFACT] sprint-planner SKILL.md 存在且不包含"读代码"/"architecture"相关内容
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/sprint-planner/SKILL.md','utf8');if(c.includes('architecture_design'))process.exit(1);if(!c.includes('PRD'))process.exit(1)"

- [x] [ARTIFACT] sprint-contract-proposer 和 sprint-contract-reviewer SKILL.md 存在
  Test: manual:node -e "require('fs').accessSync('packages/workflows/skills/sprint-contract-proposer/SKILL.md');require('fs').accessSync('packages/workflows/skills/sprint-contract-reviewer/SKILL.md')"

- [x] [BEHAVIOR] migration 210 包含新 task types
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/210_sprint_contract_types.sql','utf8');if(!c.includes('sprint_planner'))process.exit(1)"

## 实现方案
**要改的文件**:
- packages/brain/src/task-router.js
- packages/brain/src/routes/execution.js
- packages/brain/src/executor.js
- packages/brain/src/token-budget-planner.js
- packages/brain/migrations/210_sprint_contract_types.sql（新建）
- packages/workflows/skills/sprint-planner/SKILL.md（新建）
- packages/workflows/skills/sprint-contract-proposer/SKILL.md（新建）
- packages/workflows/skills/sprint-contract-reviewer/SKILL.md（新建）
- packages/workflows/skills/sprint-generator/SKILL.md（改：删除合同写作）
