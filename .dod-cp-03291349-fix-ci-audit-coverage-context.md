# DoD: 修复3个P0地基缺口

- [x] [ARTIFACT] brain-l3 job 含 npm audit --audit-level=high
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l3-code.yml','utf8');if(!c.includes('npm audit --audit-level=high'))process.exit(1)"

- [x] [ARTIFACT] coverage-delta job 移除无条件 continue-on-error
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l3-code.yml','utf8');const idx=c.indexOf('coverage-delta:');const seg=c.slice(idx,idx+3000);const runIdx=seg.indexOf('Run coverage');const runSeg=seg.slice(runIdx,runIdx+300);if(runSeg.includes('continue-on-error: true'))process.exit(1)"

- [x] [BEHAVIOR] /api/brain/context 返回 success:true
  Test: manual:curl -sf localhost:5221/api/brain/context | node -e "process.stdin.resume();let d='';process.stdin.on('data',c=>d+=c);process.stdin.on('end',()=>{const r=JSON.parse(d);if(!r.success)process.exit(1)})"

- [x] [GATE] CI 全部通过
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/routes/context.js')"
