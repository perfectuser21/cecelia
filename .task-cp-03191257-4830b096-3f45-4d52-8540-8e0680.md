---
id: task-cp-03191257-4830b096-3f45-4d52-8540-8e0680
type: task-card
branch: cp-03191257-4830b096-3f45-4d52-8540-8e0680
created: 2026-03-19
---

# Task Card: 固化 CECELIA_MAX_SEATS=10 硬上限 — DEFINITION.md 并发章节更新

## 需求（What & Why）

**功能描述**: 将 `CECELIA_MAX_SEATS=10` 写入项目配置，并更新 DEFINITION.md 中已过时的并发参数（章节 7）
**背景**:
- `docker-compose.yml`、`.env.example`、`packages/brain/.env.example`、`packages/brain/.env` 已有 `CECELIA_MAX_SEATS=10`（由 cp-03181813 hotfix 补写）
- DEFINITION.md 第 7.1 节机器参数为旧值（8核/PHYSICAL_CAPACITY=12），与实际 10核/PHYSICAL_CAPACITY=10 不符
- 第 7.2 节动态限流表的"满额（12）"与实际 physical_capacity=10 不符
- Brain API 当前：physical_capacity=10, budget_cap=7, max_concurrent=7, auto_dispatch_max=5

**不做什么**: 不改 JS 代码；不移除 CECELIA_BUDGET_SLOTS=7（该值是 cp-03181813 hotfix 有意设置的运营上限）

## 成功标准

1. [ARTIFACT] `packages/brain/.env.example` 包含 `CECELIA_MAX_SEATS=10` 并有注释说明
2. [ARTIFACT] `docker-compose.yml` brain service 含 `CECELIA_MAX_SEATS=10` 及注释
3. [ARTIFACT] `DEFINITION.md` 第 7.1 节机器参数更新为 10核/PHYSICAL_CAPACITY=10，双层模型文档化
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] packages/brain/.env.example 包含 CECELIA_MAX_SEATS=10
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/.env.example','utf8');if(!c.includes('CECELIA_MAX_SEATS=10'))process.exit(1)"

- [x] [ARTIFACT] docker-compose.yml brain service 包含 CECELIA_MAX_SEATS=10 及硬上限注释
  Test: manual:node -e "const c=require('fs').readFileSync('docker-compose.yml','utf8');if(!c.includes('CECELIA_MAX_SEATS=10'))process.exit(1)"

- [x] [ARTIFACT] DEFINITION.md 第 7 节 CECELIA_MAX_SEATS 已文档化（不再是 PHYSICAL_CAPACITY=12 旧值）
  Test: manual:node -e "const c=require('fs').readFileSync('DEFINITION.md','utf8');if(c.includes('PHYSICAL_CAPACITY=12'))process.exit(1)"

- [x] [BEHAVIOR] platform-utils.js 定义 MAX_PHYSICAL_CAP=10（硬上限编码固化）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/platform-utils.js','utf8');if(!c.includes('MAX_PHYSICAL_CAP = 10'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"

## 实现方案

**要改的文件**:
- `DEFINITION.md` — 第 7.1 节机器参数（8核→10核，PHYSICAL_CAPACITY=12→10，添加双层模型说明）、第 7.2 节满额值（12→10）

**Scope 锚定**: 仅改 DEFINITION.md 文档，不改代码
