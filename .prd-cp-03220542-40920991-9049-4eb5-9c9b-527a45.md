# PRD: 重构 advanceContentPipeline（复杂度 33 → <10）

## 背景
Brain 自动调度任务。代码复杂度扫描发现 `content-pipeline-orchestrator.js` 中的 `advanceContentPipeline` 函数圈复杂度为 33（阈值 10），需重构降低。

## 功能描述
文件：`packages/brain/src/content-pipeline-orchestrator.js`（第 200 行）
函数：`advanceContentPipeline`
当前圈复杂度：33（目标：<10）

重构策略：提取子函数 + dispatch map 替代顺序 if 链。

## 成功标准

- 提取 `_loadPipelineContext` 等至少 2 个命名子函数，将主函数逻辑分发
- `advanceContentPipeline` 函数圈复杂度降至 10 以下
- 所有现有测试通过，外部行为不变
