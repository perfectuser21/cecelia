---
id: dod-cp-03070855-domain-owner-role
version: 1.1.0
created: 2026-03-07
updated: 2026-03-07
changelog:
  - 1.0.0: 初始版本
  - 1.1.0: 修复 Test 字段格式（改为 manual: 格式）
---

# DoD - feat(brain): 任务创建自动填充 domain/owner_role + 路由集成

## 验收标准

### 功能验收

- [x] Migration 134 SQL 文件存在，为 tasks/goals/projects 表添加 domain/owner_role 列
  Test: manual:node -e "const s=require('fs').readFileSync('database/migrations/134-domain-owner-role.sql','utf8');if(!s.includes('domain'))process.exit(1)"

- [x] domain-map.js 的 detectDomain() 函数对 10 个 domain 的关键词正确匹配
  Test: manual:cd packages/brain && npx vitest run src/__tests__/domain-map.test.js 2>&1 | grep -q "passed"

- [x] domain-map.js 的 getDomainOwnerRole() 函数返回正确的 owner_role
  Test: manual:cd packages/brain && npx vitest run src/__tests__/domain-map.test.js 2>&1 | grep -q "passed"

- [x] 无关键词信号时默认返回 coding domain
  Test: manual:cd packages/brain && npx vitest run src/__tests__/domain-map.test.js 2>&1 | grep -q "passed"

- [x] actions.js createTask() 创建任务时自动填充 domain 和 owner_role 字段
  Test: manual:cd packages/brain && npx vitest run src/__tests__/actions-domain.test.js 2>&1 | grep -q "passed"

- [x] actions.js createTask() 接受外部显式传入 domain/owner_role 参数并优先使用
  Test: manual:cd packages/brain && npx vitest run src/__tests__/actions-domain.test.js 2>&1 | grep -q "passed"

- [x] task-router.js getTaskLocation() 根据 domain 字段路由（domain 优先于 task_type）
  Test: manual:cd packages/brain && npx vitest run src/__tests__/task-router-domain.test.js 2>&1 | grep -q "passed"

- [x] selfcheck.js EXPECTED_SCHEMA_VERSION 更新为 '134'
  Test: manual:cd packages/brain && npx vitest run src/__tests__/selfcheck.test.js 2>&1 | grep -q "passed"

### 测试验收

- [x] npm run qa 通过（含新增测试）
  Test: manual:cd packages/brain && npx vitest run src/__tests__/domain-map.test.js src/__tests__/actions-domain.test.js src/__tests__/task-router-domain.test.js 2>&1 | grep -q "passed"
