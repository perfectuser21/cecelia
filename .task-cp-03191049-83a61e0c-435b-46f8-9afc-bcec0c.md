---
id: task-cp-03191049-83a61e0c
type: task-card
branch: cp-03191049-83a61e0c-435b-46f8-9afc-bcec0c
created: 2026-03-19
---

# Task Card: 创建 zenithjoy 9 个 Initiative — 解除 OKR 拆解冻结

## 需求（What & Why）
**功能描述**: 在 Brain PostgreSQL 中创建 9 个 Initiative（projects 表，type=initiative），分别挂载到 zenithjoy 的 3 个 KR 下，解除 OKR 派发冻结。
**背景**: zenithjoy 部门的 3 个 KR 处于 decomposing 状态（拆解任务失败多次），导致 0 个 Initiative 存在，无法派发任何 Task。Plan B：绕过 /decomp，直接用种子脚本创建 9 个 Initiative。
**不做什么**: 不修改 KR 状态机逻辑，不重构 OKR tick，只创建记录。

## 成功标准

1. [ARTIFACT] `packages/brain/scripts/seed-zenithjoy-initiatives.mjs` 脚本存在且可运行
2. [BEHAVIOR] Brain DB 中存在 9 个 status='pending' 的 zenithjoy Initiative
3. [BEHAVIOR] KR1/KR2/KR3 各有 3 个 Initiative，kr_id 正确指向对应 KR
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] 种子脚本存在：`packages/brain/scripts/seed-zenithjoy-initiatives.mjs`
  Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/brain/scripts/seed-zenithjoy-initiatives.mjs'))process.exit(1)"

- [x] [BEHAVIOR] Brain 中存在 9 个 zenithjoy Initiative（status=pending, type=initiative）
  Test: manual:node -e "const {Pool}=require('pg');const p=new Pool({user:'cecelia',database:'cecelia'});p.query(\"SELECT COUNT(*) as cnt FROM projects WHERE type='initiative' AND kr_id IN ('d947e4c7-815e-454c-a8fb-0aa79d8024fb','3e3f713f-8ecb-429d-abc1-8018d308c7b5','fedab43c-a8b8-428c-bcc1-6aad6e6210fc')\").then(r=>{if(parseInt(r.rows[0].cnt)<9){process.exit(1)}p.end()})"

- [x] [BEHAVIOR] KR1 有 3 个 Initiative
  Test: manual:node -e "const {Pool}=require('pg');const p=new Pool({user:'cecelia',database:'cecelia'});p.query(\"SELECT COUNT(*) as cnt FROM projects WHERE type='initiative' AND kr_id='d947e4c7-815e-454c-a8fb-0aa79d8024fb'\").then(r=>{if(parseInt(r.rows[0].cnt)<3){process.exit(1)}p.end()})"

- [x] [BEHAVIOR] KR2 有 3 个 Initiative
  Test: manual:node -e "const {Pool}=require('pg');const p=new Pool({user:'cecelia',database:'cecelia'});p.query(\"SELECT COUNT(*) as cnt FROM projects WHERE type='initiative' AND kr_id='3e3f713f-8ecb-429d-abc1-8018d308c7b5'\").then(r=>{if(parseInt(r.rows[0].cnt)<3){process.exit(1)}p.end()})"

- [x] [BEHAVIOR] KR3 有 3 个 Initiative
  Test: manual:node -e "const {Pool}=require('pg');const p=new Pool({user:'cecelia',database:'cecelia'});p.query(\"SELECT COUNT(*) as cnt FROM projects WHERE type='initiative' AND kr_id='fedab43c-a8b8-428c-bcc1-6aad6e6210fc'\").then(r=>{if(parseInt(r.rows[0].cnt)<3){process.exit(1)}p.end()})"

- [x] [GATE] 新增脚本语法正确（不修改现有代码，不影响已有测试）
  Test: manual:node --check packages/brain/scripts/seed-zenithjoy-initiatives.mjs

## 实现方案
**要改的文件**: 
- 新建：`packages/brain/scripts/seed-zenithjoy-initiatives.mjs`（幂等种子脚本）
**Scope 锚定**: 
- 仅创建 DB 记录，不修改任何现有文件
- KR1(d947e4c7)→3个Initiative，KR2(3e3f713f)→3个Initiative，KR3(fedab43c)→3个Initiative
- 脚本幂等：若 Initiative 已存在（按 name+kr_id 唯一键），跳过创建
