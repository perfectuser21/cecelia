# DoD: Split routes.js into sub-route files

## 完成标准

- [ ] routes.js 行数 ≤ 200
  - Test: manual: node -e "const lines=require('fs').readFileSync('packages/brain/src/routes.js','utf8').split('\n').length; if(lines>200) process.exit(1); console.log('routes.js lines:', lines)"
- [ ] routes/ 有 ≥ 9 个子文件
  - Test: manual: node -e "const fs=require('fs'); const count=fs.readdirSync('packages/brain/src/routes').filter(f=>f.endsWith('.js')).length; if(count<9) process.exit(1); console.log('route files:', count)"
- [ ] [BEHAVIOR] 路由子文件全部存在且可通过语法检查
  - Test: manual: node --check packages/brain/src/routes/shared.js && node --check packages/brain/src/routes/status.js && node --check packages/brain/src/routes/execution.js && node --check packages/brain/src/routes/ops.js && echo "syntax OK"
