# DoD: Split routes.js into sub-route files

## 完成标准

- [ ] routes.js 行数 ≤ 200
  - Test: `manual: node -e "const lines=require('fs').readFileSync('packages/brain/src/routes.js','utf8').split('\\n').length; if(lines>200) process.exit(1)"`
- [ ] routes/ 有 ≥ 9 个子文件
  - Test: `manual: node -e "const fs=require('fs'); const count=fs.readdirSync('packages/brain/src/routes').filter(f=>f.endsWith('.js')).length; if(count<9) process.exit(1)"`
- [ ] 测试全通过
  - Test: `manual: npm --prefix packages/brain test`
