# DoD: 知识感知闭环

## F1: /api/brain/context 汇总接口
- [x] `packages/brain/src/routes/context.js` 文件存在
- [x] `server.js` 注册 `/api/brain/context` 路由
- [x] 接口返回包含 okr/recent_prs/active_tasks/recent_decisions/summary_text 字段

## F2: /api/brain/okr/current 接口
- [x] `/api/brain/okr/current` 端点存在
- [x] 返回 OKR 树形结构（areas → objectives → key_results）

## F3: CLAUDE.md 工具地图
- [x] `.claude/CLAUDE.md` 包含 `Brain 知识查询工具` 章节
- [x] 章节列出 `/api/brain/context` 等可查询端点

## F4: Stop Hook session summary
- [x] `packages/engine/hooks/stop.sh` 包含 conversation-summary 调用
- [x] `POST /api/brain/conversation-summary` 端点存在

## DoD Tests
- manual: node -e "require('fs').accessSync('packages/brain/src/routes/context.js')"
- manual: node -e "const c=require('fs').readFileSync('packages/brain/server.js','utf8');if(!c.includes('/api/brain/context'))process.exit(1)"
- manual: node -e "const c=require('fs').readFileSync('.claude/CLAUDE.md','utf8');if(!c.includes('Brain 知识查询工具'))process.exit(1)"
- manual: node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop.sh','utf8');if(!c.includes('conversation-summary'))process.exit(1)"
