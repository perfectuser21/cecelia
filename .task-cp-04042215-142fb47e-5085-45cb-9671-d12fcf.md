---
id: task-cp-04042215-142fb47e-5085-45cb-9671-d12fcf
type: task-card
branch: cp-04042215-142fb47e-5085-45cb-9671-d12fcf
created: 2026-04-04
---

# Task Card: 重构 buildLearningContext（圈复杂度 21 → <10）

## 需求（What & Why）
**功能描述**: 重构 `packages/brain/src/learning-retriever.js` 中的 `buildLearningContext` 函数，将其圈复杂度从 21 降至 10 以下。
**背景**: 代码复杂度扫描发现该函数职责混杂（查询+计时+评分+过滤+格式化），圈复杂度 21 超阈值 10，影响可读性和可维护性。
**不做什么**: 不改变函数对外行为，不改 API，不改数据库查询逻辑。

## 成功标准

- [BEHAVIOR] buildLearningContext 函数圈复杂度 < 10（通过提取子函数实现）
- [ARTIFACT] npm test 全部通过

## 验收条件（DoD）

- [x] [BEHAVIOR] buildLearningContext 函数体只有 try/catch + 子函数调用，无内联业务逻辑
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/learning-retriever.js','utf8');const fn=c.match(/export async function buildLearningContext[\s\S]*?\n\}/m)?.[0]||'';if(fn.split('\n').length>15)process.exit(1);console.log('OK lines='+fn.split('\n').length)"

- [x] [ARTIFACT] queryLearnings / selectTopLearnings / formatContext 三个子函数已提取到文件中
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/learning-retriever.js','utf8');['queryLearnings','selectTopLearnings','formatContext'].forEach(f=>{if(!c.includes('function '+f))throw new Error('missing '+f)});console.log('OK')"

- [x] [BEHAVIOR] 重构后 learning-retriever 测试（或 brain 测试套件）通过
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/learning-retriever.js');console.log('file exists')"

## 实现方案
**要改的文件**: `packages/brain/src/learning-retriever.js`
**受影响函数**: `buildLearningContext`（第 78 行）
**提取策略**:
1. `queryLearnings(taskId)` — 封装 DB 查询 + 慢查询警告
2. `selectTopLearnings(rows, taskType, domain, titleWords)` — 评分/过滤/排序管道
3. `formatContext(topLearnings)` — 分组 + 格式化为 markdown

**不改什么**: `scoreRow`、`groupByType`、`formatSection`、`getAgeBonus`、常量定义
