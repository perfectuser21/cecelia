## DoD - code_review 路由到 codex-bridge

### 验收清单

- [x] executor.js 路由逻辑：task_type === 'code_review' 时路由到西安 codex-bridge，其他 task_type 不受影响
  Test: manual:bash -c "grep -q triggerCodexBridge packages/brain/src/executor.js"

- [x] task-router.js location 映射：code_review → xian，isValidLocation 和 detectRoutingFailure 支持 xian
  Test: manual:bash -c "grep -q code_review packages/brain/src/task-router.js"

- [x] 错误处理：codex-bridge 不可达时返回 failed，有 AbortSignal.timeout 超时控制
  Test: manual:bash -c "grep -q AbortSignal.timeout packages/brain/src/executor.js"

- [x] 测试更新：task-router-code-review.test.js 期望 code_review → xian，trace.test.js 包含 XIAN_MAC_MINI
  Test: manual:bash -c "grep -q xian packages/brain/src/__tests__/task-router-code-review.test.js"
