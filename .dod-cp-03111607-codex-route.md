## DoD - code_review 路由到 codex-bridge

### 验收清单

- [ ] **1. executor.js 路由逻辑**
  - task_type === 'code_review' 时 POST 到 100.86.57.69:3458/execute-review
  - 其他 task_type 走现有 cecelia-bridge 不受影响
  - **Test**: 代码中有明确的 code_review 分支判断

- [ ] **2. 请求体正确**
  - 包含 task_id, checkpoint_id, work_dir, base_branch
  - work_dir 映射到西安 Mac mini 的 ~/repos/<repo> 路径
  - **Test**: 请求体结构与 codex-bridge /execute-review 接口匹配

- [ ] **3. 错误处理**
  - codex-bridge 不可达时标记任务 failed，不影响其他任务
  - **Test**: 有 try/catch 和超时处理
