# Task Card: crystallize 执行闭环 — 补全3个断线

**Branch**: cp-03241404-crystallize-exec-wiring  
**Brain Task ID**: 81ddb0f9-8464-4db2-ad9c-e1c3cd4082a1  
**Priority**: P1

## PRD

补全 crystallize 任务类型的执行回传链路，使流水线能端到端运转。

### 缺口1：playwright-runner.sh 回传
- crystallize 子任务完成后 POST `/api/brain/execution-callback`（status=AI Done / AI Failed）
- findings 包含 script_path（forge阶段）或 verify_passed（verify阶段）

### 缺口2：execution.js pipeline 推进
- execution-callback 收到 crystallize_* 子任务完成时，调用 `advanceCrystallizeStage`
- 逻辑与 content-pipeline 的 `advanceContentPipeline` 调用块对称

### 缺口3：executor.js runner 模式
- triggerCodexBridge 对 crystallize 类型使用 runner 模式（playwright-runner.sh）
- 传入 --task-id 参数，与 codex_dev 用 runner.sh 的方式对称

## 不在范围内
- kr-progress.js bug 修复
- Dashboard Roadmap UI 迁移

## 成功标准

- playwright-runner.sh 在 AI_STATUS=0 时 POST execution-callback status=AI Done
- playwright-runner.sh 在 AI_STATUS≠0 时 POST execution-callback status=AI Failed
- execution.js 5c12 块：crystallize_* 子任务 → advanceCrystallizeStage 被调用
- executor.js：crystallize* task_type 使用 playwright-runner.sh runner 模式

## 行为快照（PRESERVE）

- [x] [PRESERVE] content-pipeline（5c11块）逻辑不变：advanceContentPipeline 仍被正确调用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(!c.includes('advanceContentPipeline'))process.exit(1);console.log('OK')"
- [x] [PRESERVE] codex_dev runner 模式不变：isCodexDev 仍使用 runner.sh + --branch + --task-id
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('isCodexDev'))process.exit(1);console.log('OK')"
- [x] [PRESERVE] playwright-runner.sh 的账号轮换逻辑（run_codex函数）不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/runners/codex/playwright-runner.sh','utf8');if(!c.includes('run_codex'))process.exit(1);console.log('OK')"

## DoD（Definition of Done）

- [x] [BEHAVIOR] playwright-runner.sh 完成后 POST execution-callback 到 Brain
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/runners/codex/playwright-runner.sh','utf8');if(!c.includes('execution-callback'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] playwright-runner.sh 成功时 status=AI Done，失败时 status=AI Failed
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/runners/codex/playwright-runner.sh','utf8');if(!c.includes('AI Done')||!c.includes('AI Failed'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] execution.js 5c12 块调用 advanceCrystallizeStage（crystallize pipeline 推进）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(!c.includes('advanceCrystallizeStage'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] execution.js 从 crystallize-orchestrator.js 导入 advanceCrystallizeStage
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(!c.includes('crystallize-orchestrator'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] executor.js triggerCodexBridge 对 crystallize* 使用 playwright-runner.sh
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('playwright-runner.sh'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] executor.js 包含 isCrystallize 标志区分 crystallize 与其他类型
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('isCrystallize'))process.exit(1);console.log('OK')"
