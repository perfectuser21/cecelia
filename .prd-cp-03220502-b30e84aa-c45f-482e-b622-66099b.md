# PRD: 重构 capability-scanner.scanCapabilities（复杂度 42 → 10 以下）

## 背景

代码复杂度扫描发现 `packages/brain/src/capability-scanner.js` 第 69 行 `scanCapabilities` 函数的圈复杂度为 42，超过阈值 10。需要重构以降低复杂度。

## 目标

- `scanCapabilities` 函数圈复杂度降至 10 以下
- 提取子函数：将复杂条件分支拆分为独立的命名函数
- 保持所有现有测试通过，功能行为不变

## 成功标准

- [ ] `scanCapabilities` 圈复杂度降至 10 以下
- [ ] 提取至少 3 个命名子函数
- [ ] npm test 全部通过
