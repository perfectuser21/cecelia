## DoD - Cecelia 嘴巴实时运行状态注入

### 验收清单

- [x] 新增 `buildRuntimeStateBlock()` 函数，从 `working_memory` 读取关键运行状态字段
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js
- [x] `buildUnifiedSystemPrompt` 调用该函数，将结果注入 prompt
  Test: manual:bash -c "grep -q 'buildRuntimeStateBlock' packages/brain/src/orchestrator-chat.js"
- [x] 注入字段包括：`last_feishu_at`、`dispatch_ramp_state`、`tick_actions_today`
  Test: manual:bash -c "grep -q 'last_feishu_at' packages/brain/src/orchestrator-chat.js"
- [x] DB 查询失败时静默降级（返回空字符串，不影响对话）
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js
- [x] 字段值格式化为可读中文描述
  Test: manual:bash -c "grep -q '飞书最近发送时间' packages/brain/src/orchestrator-chat.js"
- [x] 新增测试：`buildRuntimeStateBlock` 正常返回、DB 失败降级
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js
- [x] Brain CI 通过
  Test: manual:curl -s http://localhost:5221/api/brain/health | python3 -c "import sys,json; d=json.load(sys.stdin); sys.exit(0 if d.get('status')=='healthy' else 1)"
