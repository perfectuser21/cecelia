# DoD: fix(tick): initTickLoop 启动时调用 syncOrphanTasksOnStartup

- [x] [ARTIFACT] tick.js initTickLoop 函数包含 syncOrphanTasksOnStartup 调用
  Test: manual:node -e "const s=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!s.includes('syncOrphanTasksOnStartup()'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] 启动日志格式包含 orphans_found/requeued/rebuilt/failed 字段
  Test: manual:node -e "const s=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!s.includes('[initTickLoop] startup-sync: orphans_found='))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] executor-startup-sync.test.js 场景0 验证 initTickLoop 调用链存在
  Test: manual:node -e "const s=require('fs').readFileSync('packages/brain/src/__tests__/executor-startup-sync.test.js','utf8');if(!s.includes('initTickLoop'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] syncOrphanTasksOnStartup 在 initTickLoop 内位于 startTickLoop 之前（源码顺序验证）
  Test: manual:node -e "const s=require('fs').readFileSync('packages/brain/src/tick.js','utf8');const i=s.indexOf('async function initTickLoop');const sc=s.indexOf('syncOrphanTasksOnStartup()',i);const st=s.indexOf('startTickLoop()',i);if(!(sc>i&&sc<st))process.exit(1);console.log('OK')"
