---
id: task-cp-04131701-superpowers-engine-fusion
type: task-card
branch: cp-04131701-superpowers-engine-fusion
created: 2026-04-13
---

# Task Card: 融入 Superpowers 到 Engine /dev Pipeline

## 需求（What & Why）
**功能描述**: 将 Superpowers 插件（已安装 v5.0.7）的行为纪律模板集成到 Engine /dev 4-Stage Pipeline 的 step 文件中，让 agent 在执行 /dev 时自动引用 Superpowers 的 TDD、验证、调试等 skill。
**背景**: 我们的 step 文件只有骨架（02-code.md 仅 116 行），缺少行为指导细节。Superpowers 有完整的 TDD 铁律、Verification Gate、Systematic Debugging 等模板。两者互补：Superpowers 管行为纪律，Engine 管流程强制。
**不做什么**: 不改 devloop-check.sh 状态机逻辑，不改 Stop Hook，不改 Brain 集成。只改 step markdown 文件 + SKILL.md。

## 成功标准

## 验收条件（DoD）

- [x] [BEHAVIOR] 01-spec.md 包含零占位符规则和 Self-Review 3 步检查
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('零占位符'))process.exit(1);if(!c.includes('Self-Review'))process.exit(1)"

- [x] [BEHAVIOR] 02-code.md 引用 superpowers:test-driven-development 和 superpowers:verification-before-completion
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('superpowers:test-driven-development'))process.exit(1);if(!c.includes('superpowers:verification-before-completion'))process.exit(1)"

- [x] [BEHAVIOR] 02-code.md 包含 Systematic Debugging 引用和 3 次失败升级规则
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('superpowers:systematic-debugging'))process.exit(1);if(!c.includes('3 次'))process.exit(1)"

- [x] [BEHAVIOR] 03-integrate.md 引用 systematic-debugging 用于 CI 失败修复
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('superpowers:systematic-debugging'))process.exit(1)"

- [x] [BEHAVIOR] SKILL.md 更新流程描述包含 Superpowers 集成说明
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(!c.includes('Superpowers'))process.exit(1)"

- [x] [ARTIFACT] 所有改动的文件语法正确，无 frontmatter 破坏
  Test: manual:node -e "const fs=require('fs');['packages/engine/skills/dev/SKILL.md','packages/engine/skills/dev/steps/01-spec.md','packages/engine/skills/dev/steps/02-code.md','packages/engine/skills/dev/steps/03-integrate.md'].forEach(f=>{const c=fs.readFileSync(f,'utf8');if(!c.startsWith('---'))process.exit(1)})"

## 实现方案（必填）
**要改的文件**:
- `packages/engine/skills/dev/steps/01-spec.md` — 加零占位符规则 + Self-Review
- `packages/engine/skills/dev/steps/02-code.md` — 全面改写，加 TDD/Verification/Debugging
- `packages/engine/skills/dev/steps/03-integrate.md` — 加 systematic-debugging 引用
- `packages/engine/skills/dev/SKILL.md` — 更新流程描述
**不改什么**: devloop-check.sh、hooks/*.sh、CI workflow
