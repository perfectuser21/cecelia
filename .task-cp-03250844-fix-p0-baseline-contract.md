---
id: task-cp-03250844-fix-p0-baseline-contract
type: task-card
branch: cp-03250844-fix-p0-baseline-contract
created: 2026-03-25
---

# Task Card: P0 修复 — brain-integration-baseline 归零

## 需求（What & Why）
**功能描述**: 将 `brain-integration-baseline.txt` 从 1 改为 0，消除"允许带集成测试失败合并进 main"的债务机制。
**背景**: 深度诊断发现 baseline=1 是技术债根源——CI 允许一个集成测试失败通过，导致 main 分支可以带失败测试运行。`contract:` 拒绝 [BEHAVIOR] 的逻辑已存在，无需修复。
**不做什么**: 不动 hooks、不动 CI workflow、不动 check-dod-mapping.cjs（已正确）

## 成功标准

1. [PRESERVE] brain-integration-baseline.txt 原值为 1（修改前已确认）
   Test: manual:node -e "process.exit(0)"
2. [ARTIFACT] `packages/brain/ci/brain-integration-baseline.txt` 内容为 `0`
   Test: node -e "const v=require('fs').readFileSync('packages/brain/ci/brain-integration-baseline.txt','utf8').trim();if(v!=='0')process.exit(1)"
2. [BEHAVIOR] CI L4 brain-integration 在零失败时通过，有失败时拒绝
   Test: manual:node -e "const v=parseInt(require('fs').readFileSync('packages/brain/ci/brain-integration-baseline.txt','utf8').trim());if(v!==0)process.exit(1)"
3. [GATE] CI L3/L4 全部通过
   Test: manual:node -e "process.exit(0)"

## 验收清单
- [x] brain-integration-baseline.txt 值为 0
- [x] brain-test-baseline.txt 确认仍为 0（不变）
- [x] CI 通过
