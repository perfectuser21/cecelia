# Task: Content Pipeline 配置可视化 — Dashboard 编辑器 + Executor 注入

## PRD

### 背景
PR #1611 已合并 content_type_configs 表迁移 + CRUD API。
本 PR 完成剩余两步：
- PR3: Dashboard 编辑器（ContentTypeConfigPage）
- PR4: Executor 注入（executeCopyReview 从 DB 读配置）

### 目标
让 ZenithJoy 员工在 Dashboard 前端编辑内容 pipeline 每步的 prompt/规则/参数。

## 成功标准

1. ContentTypeConfigPage.tsx 存在，包含 PUT /api/brain/content-types/:type/config 调用
2. content feature manifest (index.ts) 注册路由 /content/config
3. core/index.ts 包含 content 入口（7 entries）
4. executors 有 loadContentTypeConfig 函数并 import pool
5. executeCopyReview 使用 brandKeywords/bannedWords 变量（从 DB 或 fallback）

## DoD

- [ ] **[BEHAVIOR]** ContentTypeConfigPage 存在
  Test: `node -e "require('fs').accessSync('frontend/src/features/core/content/pages/ContentTypeConfigPage.tsx')"`

- [ ] **[BEHAVIOR]** content/index.ts 注册 /content/config 路由
  Test: `node -e "const c=require('fs').readFileSync('frontend/src/features/core/content/index.ts','utf8');if(!c.includes('/content/config'))process.exit(1)"`

- [ ] **[BEHAVIOR]** core/index.ts 包含 content 入口
  Test: `node -e "const c=require('fs').readFileSync('frontend/src/features/core/index.ts','utf8');if(!c.includes(\"'content'\"))process.exit(1)"`

- [ ] **[BEHAVIOR]** executors import pool from db.js
  Test: `node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes(\"import pool from './db.js'\"))process.exit(1)"`

- [ ] **[BEHAVIOR]** executors 使用 brandKeywords 变量
  Test: `node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes('brandKeywords.filter'))process.exit(1)"`
