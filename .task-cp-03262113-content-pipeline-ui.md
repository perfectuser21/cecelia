# Task: Content Pipeline 配置可视化 — Dashboard 编辑器

## PRD

### 背景
PR #1611 已合并 content_type_configs 表迁移 + CRUD API。
PR #1613 已合并 executor DB 注入（所有 5 个 executor 使用 getContentType()）。
本 PR 完成前端编辑器：
- ContentTypeConfigPage：可视化编辑 pipeline 每步的 prompt/规则

### 目标
让 ZenithJoy 员工在 Dashboard 前端编辑内容 pipeline 每步的 prompt/规则/参数。

## 成功标准

1. ContentTypeConfigPage.tsx 存在，包含 PUT /api/brain/content-types/:type/config 调用
2. content feature manifest (index.ts) 注册路由 /content/config
3. core/index.ts 包含 content 入口（7 entries）
4. executors 使用 getContentType() 从 DB 读取配置（由 PR #1613 完成）

## DoD

- [x] [BEHAVIOR] ContentTypeConfigPage 存在
  Test: manual: node -e "require('fs').accessSync('frontend/src/features/core/content/pages/ContentTypeConfigPage.tsx')"

- [x] [BEHAVIOR] content/index.ts 注册 /content/config 路由
  Test: manual: node -e "const c=require('fs').readFileSync('frontend/src/features/core/content/index.ts','utf8');if(!c.includes('/content/config'))process.exit(1)"

- [x] [BEHAVIOR] core/index.ts 包含 content 入口
  Test: manual: node -e "const c=require('fs').readFileSync('frontend/src/features/core/index.ts','utf8');if(!c.includes(\"'content'\"))process.exit(1)"

- [x] [BEHAVIOR] executors 使用 getContentType 从 DB 读取配置
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes('getContentType'))process.exit(1)"

- [x] [BEHAVIOR] executor 支持 copy_rules DB 配置覆盖
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes('copy_rules'))process.exit(1)"
