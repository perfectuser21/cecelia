---
id: task-cp-03201230-fix-bridge-async
type: task-card
branch: cp-03201230-fix-bridge-async
created: 2026-03-20
---
# Task Card: Bridge execSync→exec 非阻塞

## 需求（What & Why）
**功能描述**: Bridge /trigger-cecelia 用 execSync 阻塞事件循环，LLM 调用占着时 dispatch 请求超时 → fetch failed → 熔断。改成 exec 非阻塞。

## 成功标准
1. [BEHAVIOR] Bridge 不再用 execSync 启动 cecelia-run
2. [GATE] 所有现有测试通过

## 验收条件（DoD）

- [x] [BEHAVIOR] cecelia-bridge.cjs 的 /trigger-cecelia 用 exec 而非 execSync
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.cjs','utf8');if(c.includes('execSync(cmd'))process.exit(1)"

- [x] [BEHAVIOR] mkdir 也改成 mkdirSync 而非 execSync
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.cjs','utf8');if(c.includes('execSync('))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"
