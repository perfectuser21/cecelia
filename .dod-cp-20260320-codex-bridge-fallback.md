- [x] [BEHAVIOR] Codex Bridge 返回 success:false 时，非 US_ONLY 任务降级到本机 cecelia-bridge 执行
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('codexResult.success'))process.exit(1)"
- [x] [BEHAVIOR] 降级时输出日志包含 task_type 和 error 信息
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('Codex Bridge 不可达，降级到本机执行'))process.exit(1)"
- [x] [BEHAVIOR] US_ONLY_TYPES 任务不触发降级逻辑（仍走原路径）
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('!US_ONLY_TYPES.has(task.task_type)'))process.exit(1)"
