---
id: task-cp-03241445-fix-minimax-routing-content-affinity
type: task-card
branch: cp-03241445-fix-minimax-routing-content-affinity
created: 2026-03-24
---

# Task Card: MiniMax 路由清理 + content-* EXECUTOR_AFFINITY 补全

## 需求（What & Why）

**功能描述**: 清理 token-budget-planner.js 中过时的 MiniMax 路由配置，补全 content-* 任务类型的执行器亲和性定义。

**背景**: 用户决策不再使用 MiniMax（已停用）。task-router.js 的 LOCATION_MAP 已更新到 xian/codex，但 token-budget-planner.js 的 EXECUTOR_AFFINITY 还在指向 minimax，造成路由不一致。同时 content-* 任务类型未在 EXECUTOR_AFFINITY 定义，默认走 claude（错误）。

**不做什么**: 不改 task-router.js，不改其他文件。

## 成功标准

1. [ARTIFACT] token-budget-planner.js 不再有 minimax 引用
2. [ARTIFACT] content-pipeline/research/generate/review/export 在 EXECUTOR_AFFINITY 定义为 codex
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] EXECUTOR_AFFINITY 其他任务类型（strategy_session/codex_dev 等）保持不变
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/token-budget-planner.js','utf8');if(!c.includes('strategy_session')||!c.includes('codex_dev'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] token-budget-planner.js 不再有 minimax 引用
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/token-budget-planner.js','utf8');if(c.includes(\"'minimax'\"))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] content-pipeline 已加入 EXECUTOR_AFFINITY 指向 codex
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/token-budget-planner.js','utf8');if(!c.includes('content-pipeline')||!c.includes('content-generate'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] explore/research/talk 路由指向 codex（不再是 minimax）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/token-budget-planner.js','utf8');const lines=c.split('\n');const exploreLines=lines.filter(l=>l.includes(\"'explore'\")&&l.includes(\"primary\"));if(!exploreLines.some(l=>l.includes(\"'codex'\")))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] content-pipeline/content-generate 路由指向 codex
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/token-budget-planner.js','utf8');const lines=c.split('\n');const cpLines=lines.filter(l=>l.includes(\"'content-pipeline'\")&&l.includes(\"primary\"));if(!cpLines.some(l=>l.includes(\"'codex'\")))process.exit(1);console.log('ok')"

- [x] [GATE] 现有测试不回归
  Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/brain/src/token-budget-planner.js'))process.exit(1);console.log('ok')"

## 实现方案

**文件**: `packages/brain/src/token-budget-planner.js`（第 93-99 行附近）

1. 注释从"始终走 MiniMax"改为"始终走 Codex（不消耗 Claude）"
2. 第 94-98 行 5 个任务类型：minimax → codex
3. 补充 content-* 5 个类型：primary codex, fallback null, no_downgrade true
