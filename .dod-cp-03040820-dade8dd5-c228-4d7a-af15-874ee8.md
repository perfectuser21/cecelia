## DoD - Dashboard 48h系统简报展示和端到端测试

### 验收清单

- [ ] 1. **Brain API 列表端点格式修复**
  - Test: GET /api/brain/reports 返回 `{reports, count}` 格式
  - Test: 列表中每条记录包含 id, type, created_at, title, summary 字段（从 content JSONB 提取）
  - Test: 支持 limit/type 查询参数

- [ ] 2. **Brain API 详情端点正常工作**
  - Test: GET /api/brain/reports/:id 返回完整简报（含 content）
  - Test: 不存在的 ID 返回 404

- [ ] 3. **ReportsListPage 适配正确的 API 响应格式**
  - Test: 使用 `reports` 字段（而非 `records`）渲染列表
  - Test: 使用 `count` 字段显示总数
  - Test: 空列表状态显示正确

- [ ] 4. **ReportDetailPage 正常展示内容**
  - Test: 读取 /api/brain/reports/:id 并渲染各 Section
  - Test: 404 时显示错误提示

- [ ] 5. **端到端测试：手动生成 + 查看**
  - Test: POST /api/brain/reports/generate 成功插入记录
  - Test: GET /api/brain/reports 返回新记录
  - Test: GET /api/brain/reports/:id 返回完整内容

- [ ] 6. **Brain 单元测试**
  - Test: npm run brain:test 通过

- [ ] 7. **Dashboard 构建**
  - Test: npm run dashboard:build 无错误

- [ ] 8. **CI 通过，PR 合并**
