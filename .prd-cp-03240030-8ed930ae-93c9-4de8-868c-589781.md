# PRD: OKR PR10 — 旧表迁移收尾（goals/projects 剩余引用迁移）

## 背景
PR1-PR9 完成了主路径迁移。以下文件仍读写旧表（goals/projects），需全部迁移到新表后删旧表：
- self-drive.js: UPDATE goals/projects（暂停/恢复/阶段推进）fallback
- actions.js: INSERT/UPDATE goals fallback
- project-activator.js / project-compare.js: SELECT/UPDATE FROM projects
- decision-executor.js: 读写 goals fallback
- planner.js:571: initiative 领域路由 FROM projects
- decomposition-checker.js: FROM projects

## 目标
所有业务代码读写路径切到 okr_projects/okr_initiatives/okr_scopes/key_results/objectives/visions，旧表仅保留触发器双写兼容层。

## 成功标准
- 7 个文件中旧表 fallback 全部移除
- 代码不再有 UPDATE goals/UPDATE projects/INSERT INTO goals 的业务路径（Notion sync 例外）
- 所有现有测试通过
