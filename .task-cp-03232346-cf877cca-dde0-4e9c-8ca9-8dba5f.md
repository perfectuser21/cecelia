---
id: task-cp-03232346-cf877cca
type: task-card
branch: cp-03232346-cf877cca-dde0-4e9c-8ca9-8dba5f
created: 2026-03-24
---

# Task Card: MiniMax 路由清理 + content-* EXECUTOR_AFFINITY 补全

## 需求（What & Why）

**功能描述**:
1. `token-budget-planner.js` 中 `explore/research/talk/data/dept_heartbeat` 还在用 `primary: 'minimax'`，但 `task-router.js` 已将这些类型路由到西安 Codex。需同步修改为 `primary: 'codex'`。
2. `content-pipeline/content-research/content-generate/content-review/content-export` 在 `EXECUTOR_AFFINITY` 中完全缺失定义，需补全（指向 `codex`）。

**背景**: task-router.js 和 token-budget-planner.js 的路由/执行器定义出现不一致，可能导致预算计算逻辑错误地把这些任务计入 MiniMax 配额。

**不做什么**: 不改动 task-router.js，不改动其他执行器逻辑。

## 成功标准

- [x] token-budget-planner.js 中不再有 minimax 引用
- [x] content-pipeline/content-research/content-generate/content-review/content-export 在 EXECUTOR_AFFINITY 中有 primary: 'codex' 定义
- [ ] CI 全部通过

## DoD（Definition of Done）

- [ARTIFACT] token-budget-planner.js 中无 minimax 字符串
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/token-budget-planner.js','utf8');if(c.includes('minimax'))process.exit(1);console.log('ok')"

- [ARTIFACT] content-pipeline 在 EXECUTOR_AFFINITY 中有定义
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/token-budget-planner.js','utf8');if(!c.includes(\"'content-pipeline'\"))process.exit(1);console.log('ok')"

- [ARTIFACT] content-export 在 EXECUTOR_AFFINITY 中有定义
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/token-budget-planner.js','utf8');if(!c.includes(\"'content-export'\"))process.exit(1);console.log('ok')"

- [GATE] CI 通过
  Test: manual:node -e "console.log('ci-gate')"
