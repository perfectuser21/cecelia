# DoD: L3 Brain Unit Tests 提速优化

## 验收清单

- [ ] [BEHAVIOR] ci-l3-code.yml brain-unit job 的 NODE_OPTIONS 为 `--max-old-space-size=2048`
  - Test: `node -e "const c=require('fs').readFileSync('.github/workflows/ci-l3-code.yml','utf8');if(!c.includes('max-old-space-size=2048'))process.exit(1)"`
- [ ] [BEHAVIOR] packages/brain/vitest.config.js 的 maxForks 为 2
  - Test: `node -e "const c=require('fs').readFileSync('packages/brain/vitest.config.js','utf8');if(!c.includes('maxForks: 2'))process.exit(1)"`
- [ ] [BEHAVIOR] vitest.config.js 注释更新反映新的内存计算（2 forks x 2GB）
  - Test: `node -e "const c=require('fs').readFileSync('packages/brain/vitest.config.js','utf8');if(!c.includes('2 fork'))process.exit(1)"`
