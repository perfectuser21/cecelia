# PRD: OKR 体系统一 PR1 — 数据迁移 goals/projects → okr_* 表

## 背景

Brain 任务 ID: c1ffd146-1a7e-4b21-97e9-8cfc41b83756
Migration 177 已建好 7 张新 OKR 表（visions/objectives/key_results/okr_projects/okr_scopes/okr_initiatives），
migration 178 已加 tasks.okr_initiative_id。
现需要 migration 179 将旧数据完整迁移到新表，并更新 selfcheck.js 版本号。

## 功能描述

写 migration SQL，把旧表数据迁移到新 okr_* 表：
- goals.type=vision       → visions
- goals.type=area_okr     → objectives
- goals.type=area_kr      → key_results（当前无此类型，保留映射备用）
- projects.type=project   → okr_projects
- projects.type=scope     → okr_scopes
- projects.type=initiative→ okr_initiatives

幂等可重跑（ON CONFLICT DO NOTHING），验证 count 一致。

## 成功标准

- [ ] migration 179 SQL 文件存在且含 ON CONFLICT (id) DO NOTHING
- [ ] selfcheck.js EXPECTED_SCHEMA_VERSION 更新为 '179'
- [ ] selfcheck.test.js 期望版本同步为 '179'
- [ ] CI 全部通过
