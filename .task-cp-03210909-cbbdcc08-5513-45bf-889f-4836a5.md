# Task Card — 熔断器 HALF_OPEN 探针限制

## PRD

### 背景
`circuit-breaker.js` 已有 CLOSED/OPEN/HALF_OPEN 三态结构，但 HALF_OPEN 状态下 `isAllowed()` 对所有调用返回 `true`，导致探针无数量限制。每次 tick（5秒一次）都通过熔断器检查，HALF_OPEN 状态下会同时派发多个探测任务，违反"少量探测性派发"的设计意图。

### 功能描述
在 HALF_OPEN 状态中添加探针派发计数器（`probesSent`），限制最多 1 个探针任务并发派发。探针槽通过显式调用 `recordProbeDispatched(key)` 消耗，直至 HALF_OPEN 周期结束（成功→CLOSED 或失败→OPEN）时重置。

**变更文件**：
- `packages/brain/src/circuit-breaker.js`：添加 `MAX_HALF_OPEN_PROBES`、`probesSent`、`recordProbeDispatched()`
- `packages/brain/src/tick.js`：在 `isAllowed()` 通过后调用 `recordProbeDispatched('cecelia-run')`
- `packages/brain/src/__tests__/circuit-breaker-half-open-limit.test.js`：新增单元测试

## 成功标准

- [ ] HALF_OPEN 状态下探针数量受 MAX_HALF_OPEN_PROBES（=1）限制，探针槽消耗后 isAllowed() 返回 false
- [ ] recordProbeDispatched() 调用后再次 isAllowed() 返回 false，防止多个探针并发派发
- [ ] OPEN→HALF_OPEN 新冷却期开始后探针槽自动重置，允许再次派发探针
- [ ] 所有现有 circuit-breaker 单元测试（61 项）继续通过

## 验收条件（DoD）

- [x] [BEHAVIOR] circuit-breaker.js 包含 probesSent 字段
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/circuit-breaker.js','utf8');if(!c.includes('probesSent'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] circuit-breaker.js 包含 recordProbeDispatched 函数
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/circuit-breaker.js','utf8');if(!c.includes('recordProbeDispatched'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] MAX_HALF_OPEN_PROBES 已导出且值为 1
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/circuit-breaker.js','utf8');if(!c.includes('MAX_HALF_OPEN_PROBES = 1'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] isAllowed() 在 HALF_OPEN 检查 probesSent
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/circuit-breaker.js','utf8');if(!c.includes('probesSent < MAX_HALF_OPEN_PROBES'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] tick.js 集成 recordProbeDispatched
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('recordProbeDispatched'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] 新增 half-open-limit 测试文件存在
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/circuit-breaker-half-open-limit.test.js');console.log('ok')"
