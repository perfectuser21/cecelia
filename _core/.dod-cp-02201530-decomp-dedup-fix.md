# DoD: 修复 decomp-checker dedup 窗口导致队列永久停跑

## 验收清单

- [ ] D1: `hasActiveDecompositionTaskByProject()` 新函数已添加，只查 queued/in_progress
  - Test: decomp-checker-dedup-fix.test.js > hasActiveDecompositionTaskByProject > completed 任务不阻塞补货

- [ ] D2: `ensureTaskInventory()` 使用新函数（不再用 hasExistingDecompositionTaskByProject）
  - Test: decomp-checker-dedup-fix.test.js > ensureTaskInventory > 允许在 completed decomp 后补货

- [ ] D3: 旧函数 `hasExistingDecompositionTaskByProject()` 行为不变
  - Test: 现有 decomposition-checker.test.js 全部通过（回归）

- [ ] D4: 全量测试通过（vitest run）

- [ ] D5: DevGate 通过（facts-check + version-sync）

- [ ] D6: 版本号 patch 更新（1.52.5 → 1.52.6）
