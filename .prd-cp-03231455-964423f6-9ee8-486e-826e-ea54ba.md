---
id: prd-cp-03231455-refactor-run-reflection
branch: cp-03231455-964423f6-9ee8-486e-826e-ea54ba
created: 2026-03-23
---

# PRD: 重构 reflection.runReflection（圈复杂度 39 → 10）

## 背景

代码复杂度扫描发现 `packages/brain/src/desire/reflection.js` 中的 `runReflection` 函数圈复杂度为 39，远超阈值 10。

## 功能描述

对 `runReflection` 函数进行纯重构（不改变业务逻辑），通过以下方式降低圈复杂度：
1. 提取子函数：将每个独立职责抽取为具名函数
2. 消除重复代码：两处"进入静默期"逻辑提取为共享函数 `_enterSilencePeriodIfNeeded`
3. 早返回减少嵌套

## 成功标准

- runReflection 函数体内条件分支数量 < 15（大幅降低）
- 导出函数签名 `export async function runReflection(pool)` 不变
- `_loadBreakerState`、`_saveBreakerState`、`_resetBreakerStateForTest` 导出不变
- `npm test` 全部通过
