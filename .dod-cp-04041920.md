# DoD: fix content-pipeline-executors.js execSync 阻塞事件循环

## 成功标准

- [x] `run()` 函数改为 async，内部不再使用 `execSync`
- [x] `executeResearch()` 中所有 `run(...)` 调用加 `await`
- [x] `executeExport()` 中 rsync 调用改为 async exec
- [x] 文件顶部 `import { execSync }` 已移除
- [x] 单元测试通过

---

## DoD 条目

- [x] [ARTIFACT] `content-pipeline-executors.js` 不再 import `execSync`
  - Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(c.includes('execSync'))process.exit(1);console.log('OK: no execSync')"`

- [x] [ARTIFACT] `run()` 函数是 async function
  - Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes('async function run('))process.exit(1);console.log('OK: async run')"`

- [x] [ARTIFACT] `executeResearch()` 中的 notebooklm 调用包含 `await run(`
  - Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');const matches=c.match(/await run\(/g)||[];if(matches.length<3)process.exit(1);console.log('OK: await run count='+matches.length)"`

- [x] [BEHAVIOR] Brain 单元测试通过（content-pipeline-orphan）
  - Test: `tests/packages/brain/src/__tests__/content-pipeline-orphan.test.ts`
