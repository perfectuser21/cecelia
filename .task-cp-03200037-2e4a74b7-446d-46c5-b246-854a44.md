---
id: task-cp-03200037-2e4a74b7-446d-46c5-b246-854a44
type: task-card
branch: cp-03200037-2e4a74b7-446d-46c5-b246-854a44
created: 2026-03-20
---

# Task Card: cross-ref-registry.yaml 跨文件引用声明

## 需求（What & Why）
**功能描述**: 创建 `scripts/cross-ref-registry.yaml`，声明 4 条核心跨文件引用规则，每条规则明确 SSOT 文件、消费者文件（支持 glob）、extract 命令和 check 类型。
**背景**: 当前多处关键配置（devmode 字段、task_type、skill 名称、model ID）的 SSOT 与消费者之间的依赖关系缺乏机器可读的声明，导致变更时难以追踪受影响范围。
**不做什么**: 不实现 YAML 的运行时校验逻辑，不修改现有源文件，只创建声明文件。

## 成功标准
1. [ARTIFACT] `scripts/cross-ref-registry.yaml` 文件存在且 YAML 格式合法
2. [BEHAVIOR] 4 条规则的 extract 命令在当前仓库根目录能正常执行并输出内容
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] scripts/cross-ref-registry.yaml 文件存在
  Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('scripts/cross-ref-registry.yaml'))process.exit(1);console.log('EXISTS')"

- [x] [ARTIFACT] YAML 格式合法（4 条规则完整）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('scripts/cross-ref-registry.yaml','utf8');['devmode-fields','task-types','skill-names','model-ids'].forEach(r=>{if(!c.includes(r))throw new Error('missing rule: '+r)});console.log('YAML OK')"

- [x] [BEHAVIOR] devmode-fields extract 命令可执行并输出字段名
  Test: manual:bash -c "grep -E 'step_[0-9]+_[a-z]+:|cleanup_done:|intent_expand_task_id:|cto_review_task_id:' packages/engine/lib/devloop-check.sh | grep -oE 'step_[0-9]+_[a-z]+|cleanup_done|intent_expand_task_id|cto_review_task_id' | sort -u | grep -c step_"

- [x] [BEHAVIOR] task-types extract 命令可执行并输出类型列表
  Test: manual:node --input-type=module -e "import { VALID_TASK_TYPES } from './packages/brain/src/task-router.js'; if(VALID_TASK_TYPES.length<5)process.exit(1); console.log('COUNT:'+VALID_TASK_TYPES.length)"

- [x] [BEHAVIOR] model-ids extract 命令可执行并输出模型 ID
  Test: manual:node --input-type=module -e "import { MODELS } from './packages/brain/src/model-registry.js'; if(MODELS.length<3)process.exit(1); console.log('COUNT:'+MODELS.length)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案
**要改的文件**:
- 新建：`scripts/cross-ref-registry.yaml`

**Scope 锚定**:
- 仅新建一个文件，不修改任何现有代码
- SSOT 来源：packages/brain/src/task-router.js、packages/brain/src/model-registry.js、packages/engine/lib/devloop-check.sh
