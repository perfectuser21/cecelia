---
id: dod-cp-03140812-branch-date-warn-cleanup-fix
version: 1.0.0
created: 2026-03-14
updated: 2026-03-14
changelog:
  - 1.0.0: 初始版本
---

# DoD：branch-protect 分支日期警告 + cleanup.sh step_* 验证改为非阻塞

## 验收清单

- [x] branch-protect.sh 在 cp-* 分支名匹配后提取日期，判断是否超过 2 天
  Test: manual:bash -c "grep -c 'BRANCH_DATE' packages/engine/hooks/branch-protect.sh"

- [x] 分支日期超 2 天时输出 WARN 到 stderr，不 exit（只警告）
  Test: manual:bash -c "grep -c 'WARN.*分支日期' packages/engine/hooks/branch-protect.sh"

- [x] 日期比较兼容 macOS（date -v-2d）和 Linux（date -d '2 days ago'）
  Test: manual:bash -c "grep -c 'date -v-2d' packages/engine/hooks/branch-protect.sh"

- [x] cleanup.sh step 7.6 验证：步骤未完成时改为输出 WARN，不再设置 FAILED
  Test: manual:bash -c "grep -c 'WARN.*步骤未全部完成' packages/engine/skills/dev/scripts/cleanup.sh"

- [x] cleanup.sh 验证失败时 VALIDATION_PASSED 仍设为 true（不阻塞后续 cleanup）
  Test: manual:bash -c "grep -c 'VALIDATION_PASSED=false' packages/engine/skills/dev/scripts/cleanup.sh"

- [x] Engine 版本 bump 到 12.60.0（6 个文件同步）
  Test: manual:bash -c "grep -c '12.60.0' packages/engine/VERSION"

- [x] feature-registry.yml 新增本次改动条目
  Test: manual:bash -c "grep -c 'branch-date-warn-cleanup-fix' packages/engine/features/feature-registry.yml"

- [x] PR title 包含 [CONFIG] 标签
  Test: manual:bash -c "grep -c '\[CONFIG\]' .prd-cp-03140812-branch-date-warn-cleanup-fix.md"
