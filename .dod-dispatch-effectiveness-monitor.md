---
id: dod-dispatch-effectiveness-monitor
version: 1.0.0
created: 2026-03-03
updated: 2026-03-03
changelog:
  - 1.0.0: 初始版本
---

## DoD - 任务派发效果验证与监控

### 验收清单

- [ ] **D1** - GET /api/brain/dispatch/effectiveness 端点存在并可访问
  - Test: `curl -s http://localhost:5221/api/brain/dispatch/effectiveness | jq .` 返回 200
  - 包含字段: canceled_by_type, initiative_plan_cancel_rate, avg_wait_by_priority, weight_system_stats

- [ ] **D2** - dispatch/effectiveness 返回 canceled 任务统计（按 task_type 分组）
  - Test: 响应包含 `canceled_by_type` 对象，包含各 task_type 的 canceled 数量
  - 字段格式: `{ "dev": 5, "initiative_plan": 2, ... }`

- [ ] **D3** - dispatch/effectiveness 返回 initiative_plan 失败率
  - Test: 响应包含 `initiative_plan_stats` 对象
  - 包含: total, canceled, cancel_rate 字段

- [ ] **D4** - dispatch/effectiveness 返回按 priority 分组的平均等待时长
  - Test: 响应包含 `avg_wait_by_priority` 对象
  - 包含: P0, P1, P2 的平均等待秒数（仅有数据时显示）

- [ ] **D5** - task-cleanup.js 添加 initiative_plan 保护机制
  - Test: initiative_plan 类型任务不被 stale_recurring 清理机制 cancel
  - 实现: 检查 `task_type === 'initiative_plan'` 时跳过自动清理

- [ ] **D6** - GET /api/brain/cleanup/audit 端点存在并可访问
  - Test: `curl -s http://localhost:5221/api/brain/cleanup/audit | jq .` 返回 200
  - 返回格式: `{ logs: [...], total: N }` 数组

- [ ] **D7** - cleanup/audit 返回完整审计字段
  - Test: 每条日志包含 action, task_id, task_title, task_type, reason, detail, timestamp
  - 支持查询参数: ?limit=N（默认 100）

- [ ] **D8** - 所有新增函数有对应单元测试
  - Test: `npm test` 在 packages/brain 通过
  - 新增测试文件覆盖: dispatch effectiveness, cleanup audit

- [ ] **D9** - Brain CI 通过（版本号更新到 1.163.0）
  - Test: GitHub Actions brain-ci.yml 通过
  - package.json version: 1.163.0

- [ ] **D10** - DEFINITION.md 更新（如有新 API 端点）
  - Test: `node scripts/facts-check.mjs` 通过，无 DEFINITION 不一致
