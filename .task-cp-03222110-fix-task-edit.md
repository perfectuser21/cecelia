# Task: 修复任务路由编辑：UPSERT + 全C类可编辑

**Task ID**: 62dfe49a-874c-45b1-97f9-8fbac99393fb
**Branch**: cp-03222110-fix-task-edit
**Type**: fix

## 目标

1. Backend: `updateConfig` 改为 UPSERT，PUT 任意 task_type 均可保存（不再限于已有5行）
2. Frontend: 所有 C类任务开放编辑入口（不只 5 个动态任务）

## 成功标准

- [x] [ARTIFACT] D1: task-type-config-cache.js 包含 ON CONFLICT
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/task-type-config-cache.js','utf8');if(!c.includes('ON CONFLICT'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] D2: TaskTypeConfigPage 中所有 C类任务 editable 为 true
  Test: manual: node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx','utf8');const m=c.match(/editable: true/g);if(!m||m.length<10)process.exit(1);console.log('ok',m.length)"

- [x] [BEHAVIOR] D3: DetailPanel 对所有 C类任务显示编辑表单（不检查 dynamicConfig 是否存在）
  Test: manual: node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx','utf8');if(!c.includes('isEditable'))process.exit(1);console.log('ok')"
