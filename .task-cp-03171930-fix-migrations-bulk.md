---
id: task-cp-03171930-fix-migrations-bulk
type: task-card
branch: cp-03171930-fix-migrations-bulk
created: 2026-03-17
brain-task-id: 2747b457-cba5-455c-b531-0080c2d75635
---

# Task Card: 批量修复 migration 013 和 112 不幂等

## 需求

- migration 013: `CREATE INDEX` 无 `IF NOT EXISTS`（4 条）
- migration 112: `ADD CONSTRAINT ... UNIQUE` 前无 `DROP CONSTRAINT IF EXISTS`（3 条）

## 成功标准

1. [BEHAVIOR] migration 013 和 112 可重复执行不报错
2. [ARTIFACT] 013 的 CREATE INDEX 含 IF NOT EXISTS；112 的 ADD CONSTRAINT 前有 DROP CONSTRAINT IF EXISTS
3. [GATE] CI L3 smoke.test.js 通过

## 验收条件（DoD）

- [x] [BEHAVIOR] migration 013 的 4 个 CREATE INDEX 均有 IF NOT EXISTS
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/013_cortex_analyses.sql','utf8');const n=(c.match(/CREATE INDEX IF NOT EXISTS/g)||[]).length;if(n<4)throw new Error('only '+n);console.log('ok '+n)"

- [x] [ARTIFACT] migration 112 的 ADD CONSTRAINT 前均有 DROP CONSTRAINT IF EXISTS
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/112_notion_unique_index.sql','utf8');const n=(c.match(/DROP CONSTRAINT IF EXISTS/g)||[]).length;if(n<3)throw new Error('only '+n);console.log('ok '+n)"

- [x] [GATE] 两个文件均包含幂等保护关键字
  Test: manual:node -e "const a=require('fs').readFileSync('packages/brain/migrations/013_cortex_analyses.sql','utf8');const b=require('fs').readFileSync('packages/brain/migrations/112_notion_unique_index.sql','utf8');if(!a.includes('IF NOT EXISTS')||!b.includes('DROP CONSTRAINT IF EXISTS'))throw new Error('missing idempotency');console.log('ok')"
