---
id: task-cp-03192320-area-fair-dispatch
type: task-card
branch: cp-03192320-area-fair-dispatch
created: 2026-03-19
---
# Task Card: Area 公平调度

## 需求（What & Why）
**功能描述**: 废掉 Pool A/C 混合模型，按 Area 线（cecelia/zenithjoy/investment）分配 slot。每条线有 min/max/weight，从 brain_config 读取。

## 成功标准
1. [BEHAVIOR] dispatchNextTask 按 Area 保底欠债优先派发
2. [BEHAVIOR] 参数从 brain_config 读取
3. [GATE] 所有现有测试通过

## 验收条件（DoD）

- [x] [ARTIFACT] packages/brain/src/area-scheduler.js 存在，导出 selectAreaForDispatch
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/area-scheduler.js','utf8');if(!c.includes('selectAreaForDispatch'))process.exit(1)"

- [x] [BEHAVIOR] area-scheduler 从 brain_config 读取 area_slots 配置
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/area-scheduler.js','utf8');if(!c.includes('area_slots') || !c.includes('brain_config'))process.exit(1)"

- [x] [BEHAVIOR] tick.js dispatch 调用 area-scheduler 决定业务线
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('area-scheduler') || !c.includes('selectAreaForDispatch'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"
