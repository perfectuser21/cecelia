# PRD: 执行日志+质检报告上传 Brain

## 背景
generate-report.sh 生成 JSON 报告但只存本地 .dev-runs/，Brain dev_execution_logs 表和 /api/brain/dev-logs 端点已存在但从未被调用。

## 目标
在 generate-report.sh 末尾加 curl POST 上传报告到 Brain dev-logs API。

## 成功标准
- [ ] generate-report.sh 末尾调用 curl POST 上传 JSON 报告到 Brain（DEV_TASK_ID 存在时）
