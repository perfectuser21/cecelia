# PRD: fix(engine) Stage3 drift detection + spec_review 测试层检查

## 问题
1. Stage 3 push 前没有检测代码改动是否偏离 Stage 1 Spec 范围（scope drift）
2. spec_review 只看逻辑，不检查 DoD Test 是否覆盖对应测试层（unit/integration/e2e）

## 成功标准

- devloop-check.sh Stage 3 阶段含 drift check 逻辑，实际改动文件超出 DoD 声明范围时输出 warning
- spec_review subagent prompt 增加测试层检查指令，要求每个 DoD 条目对应合适的测试类型
- Engine L3 测试全部通过
