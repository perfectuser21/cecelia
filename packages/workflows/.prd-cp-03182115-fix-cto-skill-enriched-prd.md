# PRD: cto-review 改用 git diff + enriched PRD 本地写入

## 背景

两个问题需要修复：
1. cto-review 在 push 之前触发，没有 PR，不能用 gh pr diff，要改用 git diff origin/main...HEAD
2. intent-expand 完成后 enriched PRD 在 Brain task metadata 里，coding agent 读不到，需要 devloop-check.sh 条件1.5 PASS后把它拉下来写到本地 .enriched-prd-\<branch\>.md

## 成功标准

- cto-review/SKILL.md Step 1.4 改用 git diff origin/main...HEAD，排除无关文件，限制500行
- devloop-check.sh 条件1.5：intent_expand completed 后，GET Brain task metadata，写 .enriched-prd-\<branch\>.md
- 01-taskcard.md 指示 AI 读取 .enriched-prd-\<branch\>.md 作为补全后的 PRD
