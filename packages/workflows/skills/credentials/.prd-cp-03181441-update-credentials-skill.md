# PRD: 更新 credentials SKILL.md — 1Password 优先流程

## 背景
当前 SKILL.md 指引是直接存 ~/.credentials/，导致新凭据只在本地，1Password 里缺失。

## 目标
改为 1Password SSOT 流程：1P 先存 → sync-credentials.sh 同步到本地缓存。

## 成功标准

1. SKILL.md 移除旧的直接写文件指引
2. SKILL.md 包含 1Password 优先和 sync-credentials.sh 引用
3. CI 通过
