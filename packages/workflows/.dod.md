## DoD — cto-review skill

### 实现检查

- [x] [ARTIFACT] `packages/workflows/skills/cto-review/SKILL.md` 文件存在，内容包含 skill 元数据头（name: cto-review）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/workflows/skills/cto-review/SKILL.md','utf8');if(!c.includes('name: cto-review'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] SKILL.md 包含完整的 5 步执行流程定义（Step 1 ~ Step 5）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/workflows/skills/cto-review/SKILL.md','utf8');['Step 1','Step 2','Step 3','Step 4','Step 5'].forEach(s=>{if(!c.includes(s)){console.error('缺少 '+s);process.exit(1)}});console.log('OK')"

### 行为验证

- [x] [BEHAVIOR] skill 定义了 REVIEW_RESULT: PASS 和 REVIEW_RESULT: FAIL 两种输出格式
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/workflows/skills/cto-review/SKILL.md','utf8');if(!c.includes('REVIEW_RESULT: PASS')||!c.includes('REVIEW_RESULT: FAIL'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] skill 包含审查三个维度：意图交付度、DoD 质量、架构合规
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/workflows/skills/cto-review/SKILL.md','utf8');['意图交付度','DoD 质量','架构合规'].forEach(k=>{if(!c.includes(k)){console.error('缺少 '+k);process.exit(1)}});console.log('OK')"

### Gate

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/engine && npm test 2>&1 | tail -5"
