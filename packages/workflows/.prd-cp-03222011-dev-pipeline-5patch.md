# PRD: /dev pipeline 5项补强

## 背景

审查发现 /dev pipeline 存在5个系统性漏洞，需一并修复。

## 成功标准

- Stage 3 CI失败时自动写入 .dev-incident-log.json，Stage 4 Learning能读到真实数据
- Learning格式检查从Stage 4前移到Stage 2.3（push前），零 push 延迟
- Stage 1 Task Card自检加PRD bullet格式验证（≥2条）
- Stage 3.1.3 加并行PR重叠文件风险提示
- code_review_gate 新增维度H：信息卫生检查（改动引用了已删除功能/路径时报warning）

## 修复清单

1. **03-integrate.md**：3.2.3 CI失败处理新增写入 .dev-incident-log.json 步骤
2. **02-code.md**：2.3.5 新增 check-learning.sh 前移（push前执行）
3. **01-spec.md**：Stage 1自检新增 check-prd.sh 本地运行（PRD bullet格式 ≥2条）
4. **03-integrate.md**：3.1.3 新增并行PR重叠文件检测（git log查重叠改动，输出warning）
5. **packages/workflows/skills/code-review-gate/SKILL.md**：新增维度H信息卫生（引用已删除功能/路径 = warning，同一概念多处矛盾描述 = warning）

## Scope 锚定

packages/engine/skills/dev/steps/ + packages/workflows/skills/code-review-gate/ — 纯文档修复，无代码层改动

## 不做什么

- 不修改任何 .sh 脚本代码
- 不修改 CI yml 文件
- 不修改 hooks/ 下的代码
