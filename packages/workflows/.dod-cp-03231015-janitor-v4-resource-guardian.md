# DoD: Janitor v4.0 — 全资源守护

## 验收清单

- [ ] D1: crontab 软链接 `/Users/administrator/bin/janitor.sh` 存在且指向真实文件
- [ ] D2: 有头 claude 进程（TTY=s001/s003等）不被识别为孤儿，绝对不 kill
- [ ] D3: ppid=1 + 无 Brain in_progress 任务 + 无 .dev-lock + 运行>10分钟的 claude 被识别为孤儿
- [ ] D4: 有对应 in_progress Brain 任务的无头 claude 不被 kill（双重验证保护）
- [ ] D5: janitor.sh 包含 `MEM_HIGH_THRESHOLD_SEC=300`（内存高压时阈值降低）
- [ ] D6: janitor.sh 包含 `CPU_ALERT_THRESHOLD`（CPU 高压告警逻辑）
- [ ] [BEHAVIOR] frequent 模式实际运行无报错（exit 0）

## 验收测试

### D1
`manual: node -e "const fs=require('fs');const s=fs.lstatSync('/Users/administrator/bin/janitor.sh');if(!s.isSymbolicLink())process.exit(1);"`

### D2/D3/D4
`manual: bash packages/workflows/skills/janitor/__tests__/janitor_orphan.test.sh`

### D5
`manual: node -e "const c=require('fs').readFileSync('packages/workflows/skills/janitor/janitor.sh','utf8');if(!c.includes('MEM_HIGH_THRESHOLD_SEC=300'))process.exit(1);"`

### D6
`manual: node -e "const c=require('fs').readFileSync('packages/workflows/skills/janitor/janitor.sh','utf8');if(!c.includes('CPU_ALERT_THRESHOLD'))process.exit(1);"`

### [BEHAVIOR] D7
`manual: bash packages/workflows/skills/janitor/janitor.sh --mode frequent`
