# PRD: Stage 2 强制周边一致性扫描

## 需求

在 /dev Stage 2 和 code-review-gate 中新增跨文件模块一致性检查。改动文件 A 时，必须扫描 A 所在目录的同级文件，查找与本次改动矛盾的旧描述/旧行为/旧路径/旧版本号并修复。

## 成功标准

- `02-code.md` 有 2.3.6「周边一致性扫描」步骤，改动文件后必须执行
- `code-review-gate/SKILL.md` 维度 H 有「跨文件模块一致性」blocker 检查
- 跨文件矛盾未修复时 code-review-gate 返回 FAIL
