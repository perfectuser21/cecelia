# DoD: 修复 Sprint Contract Gate

branch: cp-03310721-sprint-contract-fix

## 验收条件（DoD）

- [x] [ARTIFACT] CRG SKILL.md 包含 reviewer_model 字段
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/code-review-gate/SKILL.md','utf8');if(!c.includes('reviewer_model'))process.exit(1)"`

- [x] [ARTIFACT] CRG SKILL.md 版本为 1.7.0
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/code-review-gate/SKILL.md','utf8');if(!c.includes('v1.7.0'))process.exit(1)"`

- [x] [ARTIFACT] CRG SKILL.md 包含全零统计规则（all-zero stats）
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/code-review-gate/SKILL.md','utf8');if(!c.includes('all-zero') && !c.includes('全零'))process.exit(1)"`

- [x] [ARTIFACT] spec_review SKILL.md 包含 reviewer_model 字段
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('reviewer_model'))process.exit(1)"`

- [x] [ARTIFACT] spec_review SKILL.md 版本为 1.6.0
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('v1.6.0'))process.exit(1)"`

- [x] [ARTIFACT] spec_review SKILL.md 包含 plans.length 为0时 exit 2 的规则
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('plans.length')||!c.includes('exit 2'))process.exit(1)"`

- [x] [ARTIFACT] 01-spec.md 包含 plans.length > 0 验证
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('plans.length'))process.exit(1)"`

- [x] [ARTIFACT] Engine VERSION 文件为 13.69.0
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/engine/VERSION','utf8').trim();if(c!=='13.69.0')process.exit(1)"`

- [x] [BEHAVIOR] engine package.json 版本字段为 13.69.0
      Test: `manual:node -e "const p=JSON.parse(require('fs').readFileSync('packages/engine/package.json','utf8'));if(p.version!=='13.69.0')process.exit(1)"`

- [x] [BEHAVIOR] spec_review SKILL.md 输出 JSON 模板中 reviewer_model 字段位于顶层
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');const m=c.match(/\\{[\\s\\S]*?\\\"verdict\\\"[\\s\\S]*?\\\"reviewer_model\\\"[\\s\\S]*?\\}/);if(!m)process.exit(1)"`

- [x] [BEHAVIOR] CRG SKILL.md 零统计规则包含 info 条目说明和 reviewer_model
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/code-review-gate/SKILL.md','utf8');if(!c.includes('info') || !c.includes('exit 2') || !c.includes('reviewer_model'))process.exit(1)"`

- [x] [BEHAVIOR] feature-registry.yml 包含 v13.69.0 版本的 changelog 条目
      Test: `manual:node -e "const c=require('fs').readFileSync('packages/engine/features/feature-registry.yml','utf8');if(!c.includes('13.69.0'))process.exit(1);if(!c.includes('sprint-contract'))process.exit(1)"`
