---
id: dod-xhs-publisher-refactor
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

## DoD - 小红书发布脚本可测试性重构

### 验收清单

- [x] `publish-xiaohongshu-image.cjs` 使用本地 `utils.cjs` 的 `findImages`、`readTitle`、`readContent`、`convertToWindowsPaths`、`extractDirNames`、`escapeForJS`
  Test: manual:bash -c "grep -c 'require.*utils.cjs' packages/workflows/skills/xiaohongshu-publisher/scripts/publish-xiaohongshu-image.cjs"

- [x] `publish-xiaohongshu-image.cjs` 导出 `isLoginError` 和 `isPublishSuccess`
  Test: manual:bash -c "grep -c 'module.exports' packages/workflows/skills/xiaohongshu-publisher/scripts/publish-xiaohongshu-image.cjs"

- [x] `--dry-run` 模式支持：传入后不连 CDP，打印参数并以 exit 0 退出
  Test: manual:bash -c "grep -c 'dry-run' packages/workflows/skills/xiaohongshu-publisher/scripts/publish-xiaohongshu-image.cjs"

- [x] 测试文件新增 `isLoginError`、`isPublishSuccess` 测试 case
  Test: manual:bash -c "grep -c 'isLoginError\|isPublishSuccess' packages/workflows/skills/xiaohongshu-publisher/scripts/__tests__/publish-xiaohongshu-image.test.cjs"

- [x] 现有测试继续通过
  Test: manual:bash -c "node --test packages/workflows/skills/xiaohongshu-publisher/scripts/__tests__/utils.test.cjs 2>&1 | grep -c 'pass'"
