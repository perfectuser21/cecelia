# DoD: Dev Pipeline Codex 独立审查加固（workflows 部分）

- [x] [ARTIFACT] code-review-gate SKILL.md 不含 `gh pr diff`，改用 `git diff`
  Test: manual:node -e "const src=require('fs').readFileSync('packages/workflows/skills/code-review-gate/SKILL.md','utf8');if(src.includes('gh pr diff'))process.exit(1);if(!src.includes('git diff'))process.exit(1);console.log('ok')"
