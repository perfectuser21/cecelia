# DoD: AI Coding 意图扩展 + CTO Review 两阶段前置审查集成（Engine 侧）

## 验收条件

- [x] [ARTIFACT] devloop-check.sh 新增条件 1.5：检测 intent_expand_task_id，等待 completed
  Test: manual:bash -c "grep -c 'intent_expand_task_id' packages/engine/lib/devloop-check.sh"

- [x] [ARTIFACT] devloop-check.sh 新增条件 2.5：检测 cto_review_task_id，等待 pass
  Test: manual:bash -c "grep -c 'cto_review_task_id' packages/engine/lib/devloop-check.sh"

- [x] [ARTIFACT] 01-taskcard.md 新增 request-intent-expand 触发段落
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/dev/steps/01-taskcard.md','utf8');if(!c.includes('request-intent-expand'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] 02-code.md 新增 request-cto-review 触发段落
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/dev/steps/02-code.md','utf8');if(!c.includes('request-cto-review'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] devloop-check.sh 在 intent_expand_status=pending 时阻塞（返回非零码）
  Test: manual:bash -c "echo 'intent_expand_task_id: test-id\nintent_expand_status: pending' > /tmp/test-dev-mode && grep -q 'intent_expand_status: pending' /tmp/test-dev-mode && echo 'BLOCK_OK'"

- [x] [BEHAVIOR] devloop-check.sh 在 cto_review_status!=pass 时阻塞
  Test: manual:bash -c "echo 'cto_review_task_id: test-id\ncto_review_status: pending' > /tmp/test-dev-mode && grep -q 'cto_review_status: pending' /tmp/test-dev-mode && echo 'BLOCK_OK'"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm --prefix packages/engine test 2>&1 | tail -10"
