---
id: task-cp-03221443-cleanup-codex-refs
type: task-card
branch: cp-03221443-cleanup-codex-refs
created: 2026-03-22
---

# Task Card: 清理 pipeline 过时 Codex 引用 + 修复误导注释

## 需求（What & Why）

**功能描述**: 清理深度诊断发现的4类遗留问题：
1. devloop-check.sh：删除"3次重审"说辞（已改为无限重试），删除 runners/codex/runner.sh 失效路径引用
2. code-review-gate SKILL.md：修正"在 CI 通过后"→"在 Stage 2 push 之前"，删除"Codex Gate 3/4"
3. /dev SKILL.md（packages/engine/skills/dev/SKILL.md）：删除"等 Codex"、"查 Brain API"、"Codex 协作流程图"、"等 Codex Gate 放行" 等所有过时 Codex 引用

**背景**: PR #1334 修改了无限重试逻辑，但 devloop-check.sh 的注释仍说"3次重审耗尽"。code-review-gate SKILL.md 正文第一句仍说"CI 通过后"（实际是 push 之前）。/dev SKILL.md 中"等 Codex"/"查 Brain API"是架构改变前的旧描述，现在是同步 subagent 读 .dev-mode。

**不做什么**: 不改逻辑代码，只改注释/文档/说明文字。不改 devloop-check.sh 的功能代码。

## 成功标准

1. [ARTIFACT] devloop-check.sh 不含"3次重审"
2. [ARTIFACT] devloop-check.sh 不含"runners/codex/runner.sh"
3. [ARTIFACT] code-review-gate SKILL.md L22 不含"CI 通过后"
4. [ARTIFACT] code-review-gate SKILL.md 不含"Codex Gate 3/4"
5. [ARTIFACT] /dev SKILL.md 不含"等 Codex"
6. [ARTIFACT] /dev SKILL.md 不含"查 Brain API"（在 spec_review/code_review_gate 上下文中）
7. [GATE] 所有现有测试通过

## 验收条件（DoD）

- [x] [ARTIFACT] devloop-check.sh 不含"3次重审"
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(c.includes('3次重审'))process.exit(1)"

- [ ] [ARTIFACT] devloop-check.sh 不含"runners/codex/runner.sh"
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(c.includes('runners/codex/runner.sh'))process.exit(1)"

- [ ] [ARTIFACT] code-review-gate SKILL.md 不含"在 CI 通过后、PR 合并之前"
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/code-review-gate/SKILL.md','utf8');if(c.includes('在 CI 通过后、PR 合并之前'))process.exit(1)"

- [ ] [ARTIFACT] code-review-gate SKILL.md 不含"Codex Gate 3/4"
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/code-review-gate/SKILL.md','utf8');if(c.includes('Codex Gate 3/4'))process.exit(1)"

- [ ] [ARTIFACT] /dev SKILL.md 不含"等 Codex"
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(c.includes('等 Codex'))process.exit(1)"

- [ ] [ARTIFACT] /dev SKILL.md 不含"查 Brain API"（spec_review 上下文）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(c.includes('查 Brain API'))process.exit(1)"

- [ ] [BEHAVIOR] devloop-check.sh blocked 消息包含正确的 root cause 提示
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('root cause') && !c.includes('分析') && !c.includes('修复 Task Card'))process.exit(1)"

- [ ] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/engine && npm test 2>&1 | tail -5"

## 实现方案

**要改的文件**:
1. `packages/engine/lib/devloop-check.sh` — 删除 L7 "runners/codex/runner.sh"，L32/41/146/152/154/176/182/184 "3次重审"
2. `packages/workflows/skills/code-review-gate/SKILL.md` — L11 删除"Codex Gate 3/4"，L22 改"CI 通过后"
3. `packages/engine/skills/dev/SKILL.md` — 删除所有"等 Codex"/"查 Brain API"/"Codex 协作流程图"

**Scope 锚定**: 仅注释/说明文字，不改功能逻辑
