---
id: dod-fix-cecelia-run-git-source
version: 1.1.0
created: 2026-03-11
updated: 2026-03-11
changelog:
  - 1.1.0: 修复 Test 命令使用相对路径（CI 无绝对路径）
  - 1.0.0: 初始版本
---

# DoD: 统一 cecelia-run — Mac 适配代码回归 git 源文件

## 验收清单

- [x] `packages/brain/scripts/cecelia-run.sh` 包含 resume 支持（SESSION_UUID）
  Test: manual:bash -c "grep -c 'SESSION_UUID' packages/brain/scripts/cecelia-run.sh"

- [x] 包含 exit 143 → resume 重试逻辑（不计熔断器）
  Test: manual:bash -c "grep -c 'exit 143' packages/brain/scripts/cecelia-run.sh"

- [x] 包含 Mac root → administrator 重新执行逻辑
  Test: manual:bash -c "grep -c 'administrator' packages/brain/scripts/cecelia-run.sh"

- [x] 包含 kill_tree 函数（递归杀进程树）
  Test: manual:bash -c "grep -c 'kill_tree' packages/brain/scripts/cecelia-run.sh"

- [x] `packages/brain/deploy/com.cecelia.brain.plist` 存在且 CECELIA_RUN_PATH 指向 git 源文件
  Test: manual:bash -c "grep -c 'packages/brain/scripts/cecelia-run.sh' packages/brain/deploy/com.cecelia.brain.plist"

- [x] `packages/brain/deploy/install.sh` 存在（一键部署脚本）
  Test: manual:bash -c "grep -c 'ln -s' packages/brain/deploy/install.sh"

- [x] Stop Hook CI pending 时返回 exit 2（不再 exit 0）
  Test: manual:bash -c "grep -c 'exit 2' packages/quality/hooks/stop.sh"
