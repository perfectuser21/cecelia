---
version: 1.0.0
created: 2026-03-17
---

# Task Card: cp-03170807-ci-arch-review-fixes

## 需求

修复 CTO 架构巡检（arch-review）发现的 4 个 CI 安全漏洞：
- P0-1：L4 brain-integration-baseline.txt 无上限保护（与 Fix 4 不对称）
- P0-2：L3 gate 对 test-coverage-required / coverage-delta 使用 `= "failure"` 软判断
- P0-3：RCI Execution Gate 有 continue-on-error: true，P0 合规测试失败不阻断合并
- P1：DevGate chore/docs/test/release 标题豁免范围过宽，跳过全部 4 项检查

参考：arch-review 报告 2026-03-17

## 成功标准

1. L4 brain-integration-baseline 超过 MAX_BASELINE=10 时 CI 硬失败
2. L3 gate 对 test-coverage-required 和 coverage-delta 使用 `!= "success"`（与其他检查一致）
3. RCI Execution Gate 移除 continue-on-error，修复根因使其稳定通过
4. DevGate 豁免范围缩小：chore/docs/test/release 只跳过 RCI coverage 检查，不跳过 script existence 和 DoD mapping

## DoD 验收清单

- [x] [ARTIFACT] ci-l4-runtime.yml 添加 MAX_BASELINE=10 上限保护
  Test: manual:node -e "require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8').includes('MAX_BASELINE')||process.exit(1)"
- [x] [ARTIFACT] ci-l3-code.yml l3-passed gate 对 test-coverage-required 改为 != success 判断
  Test: manual:node -e "const s=require('fs').readFileSync('.github/workflows/ci-l3-code.yml','utf8');s.includes('test-coverage-required.result')||process.exit(1)"
- [x] [ARTIFACT] ci-l3-code.yml l3-passed gate 对 coverage-delta 改为 != success 判断
  Test: manual:node -e "const s=require('fs').readFileSync('.github/workflows/ci-l3-code.yml','utf8');s.includes('coverage-delta.result')||process.exit(1)"
- [x] [ARTIFACT] ci-l1-process.yml RCI Execution Gate 移除 continue-on-error: true
  Test: manual:node -e "const s=require('fs').readFileSync('.github/workflows/ci-l1-process.yml','utf8');if(s.includes('rci-execution-gate') && s.includes('continue-on-error: true'))process.exit(1)"
- [x] [ARTIFACT] ci-l1-process.yml DevGate checks 只对非 script/DoD-mapping 检查豁免 chore/docs/test/release
  Test: manual:node -e "require('fs').readFileSync('.github/workflows/ci-l1-process.yml','utf8').includes('Script existence check — 不豁免')||process.exit(1)"
- [x] [BEHAVIOR] 修复后 CI 文件结构有效（YAML 语法正常）
  Test: manual:node -e "['ci-l1-process.yml','ci-l3-code.yml','ci-l4-runtime.yml'].forEach(f=>{require('fs').readFileSync('.github/workflows/'+f,'utf8')})"
