---
id: dev-stage-03-integrate
version: 2.0.0
updated: 2026-04-02
changelog:
  - 2.0.0: 精简 — 只保留 push + PR + CI 监控核心流程
---

# Stage 3: Integrate — Push + CI

> push + 创建 PR + 等 CI 通过。

**Task Checkpoint**: `TaskUpdate({ taskId: "3", status: "in_progress" })`

---

## 3.1 会话恢复检测

```bash
BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD)
EXISTING_PR=$(gh pr list --head "$BRANCH_NAME" --state all --json number,url,state -q '.[0]' 2>/dev/null)
# 已合并 → 跳到 cleanup；已开 → 跳到 CI 监控
```

---

## 3.2 提交 + 推送 + 创建 PR

```bash
git add -u
git commit -m "feat: <功能描述>

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"
/usr/bin/git push -u origin HEAD

BASE_BRANCH="main"
gh pr create --base $BASE_BRANCH --title "feat: <功能描述>" --body "## Summary
- <主要改动>

## Test
- [x] 本地测试通过

## SYSTEM BEHAVIOR CHANGE
Before: N/A
After:  N/A

## UNIMPLEMENTED
- N/A

---
Generated by /dev workflow"
```

---

## 3.3 CI 监控

```bash
BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD)
sleep 30
RUN_INFO=$(gh run list --branch "$BRANCH_NAME" --limit 1 --json status,conclusion,databaseId)
```

| CI 状态 | 动作 |
|---------|------|
| queued / in_progress | 等待 |
| failure | **先本地复现** → 修复 → push |
| success | 进入 Stage 4 |

### CI 失败修复

```
① 分析原因（gh run view --log-failed）
② 本地复现（npm test / bash scripts/local-precheck.sh）
③ 修复代码
④ 本地验证通过 → push
```

---

## 完成后

```bash
BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD)
sed -i '' "s/step_3_integrate: pending/step_3_integrate: done/" ".dev-mode.${BRANCH_NAME}"
```

**Task Checkpoint**: `TaskUpdate({ taskId: "3", status: "completed" })`

**继续 → Stage 4 (Ship)** — 读取 `skills/dev/steps/04-ship.md`。
