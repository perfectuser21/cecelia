# Step 9: CI 监控

> 等待 CI 通过，**不在此处合并 PR**
> CI 通过后执行 Step 10（写 LEARNINGS → push → 合并 PR），合并在 Step 10 里完成，不回到 Step 9
> **Stop Hook 控制：CI 未通过或 PR 未合并时阻止会话结束（exit 2）**

**Task Checkpoint**: `TaskUpdate({ taskId: "9", status: "in_progress" })`

---

## 流程

```
PR 创建 → 等待 CI → CI 失败？本地复现 → 修复 → 本地全绿 → push → CI 通过 → Step 10
                          ↑                                                          ↓
                    Stop Hook exit 2                                      push LEARNINGS 到功能分支
```

---

## 9.1 检查 CI 状态

```bash
BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD)

# 等待 CI 启动
sleep 30

# 查询 CI 状态
RUN_INFO=$(gh run list --branch "$BRANCH_NAME" --limit 1 --json status,conclusion,databaseId)
CI_STATUS=$(echo "$RUN_INFO" | jq -r '.[0].status')
CI_CONCLUSION=$(echo "$RUN_INFO" | jq -r '.[0].conclusion')

echo "CI 状态: $CI_STATUS"
echo "CI 结论: $CI_CONCLUSION"
```

---

## 9.2 CI 状态处理

| CI 状态 | 动作 |
|---------|------|
| queued / in_progress | 等待，继续检查 |
| failure | **先本地复现**，修复后才 push（见 9.2b） |
| success | ⚠️ 执行 Step 10 (Learning)，不在此处合并 |

---

## 9.2b CI 失败修复（本地优先规则）⭐

> **核心规则：CI 失败后，禁止直接 push 修复。必须先本地复现，本地全绿后才 push。**

原因：
- 直接 push 修复 = 用 CI（30分钟）替代本地调试（1分钟）
- 每次无效 push 消耗 macOS runner 时间
- 本地能发现的问题不应依赖 CI 来调试

### 强制执行顺序

```
① 分析 CI 失败原因
  ↓
② 本地复现（运行对应本地检查）
  ↓
③ 修复代码
  ↓
④ 本地验证通过（才允许 push）
  ↓
⑤ push
```

### ① 分析失败原因

```bash
RUN_ID=$(echo "$RUN_INFO" | jq -r '.[0].databaseId')
FAIL_LOG=$(gh run view "$RUN_ID" --log-failed 2>&1 | head -80)
echo "$FAIL_LOG"

FAILED_CHECKS=$(gh run view "$RUN_ID" --json jobs \
    -q '[.jobs[] | select(.conclusion == "failure") | .name] | join(", ")' 2>/dev/null || echo "unknown")
echo "失败的 checks: $FAILED_CHECKS"
```

**记录 incident（分析前必须先记录）**：

```bash
INCIDENT_FILE=".dev-incident-log.json"
ENTRY=$(jq -n \
    --arg ts "$(date -u +%Y-%m-%dT%H:%M:%SZ)" \
    --arg checks "$FAILED_CHECKS" \
    --arg log "$(echo "$FAIL_LOG" | head -20)" \
    '{timestamp: $ts, step: "9-ci", type: "ci_failure", description: ("CI 失败: " + $checks), error: $log, resolution: ""}')
if [[ -f "$INCIDENT_FILE" ]]; then
    jq --argjson e "$ENTRY" '. += [$e]' "$INCIDENT_FILE" > /tmp/incident_tmp.json && mv /tmp/incident_tmp.json "$INCIDENT_FILE"
else
    jq -n --argjson e "$ENTRY" '[$e]' > "$INCIDENT_FILE"
fi
```

### ② 本地复现（按失败类型选择对应检查）

根据 `$FAILED_CHECKS` 判断失败类型，运行对应的本地检查：

| CI 失败 check | 本地复现命令 |
|---------------|-------------|
| `facts-check` | `node scripts/facts-check.mjs` |
| `manifest-sync` | `node packages/brain/scripts/generate-manifest.mjs --check` |
| `brain-test`（覆盖率/测试） | `cd packages/brain && npm test` |
| `version-check`（engine） | `cd packages/engine && npm run version-check` |
| `dod-check` | `node packages/engine/scripts/devgate/check-dod-mapping.cjs` |
| **Brain 相关（含 facts/manifest/version）** | `bash scripts/local-precheck.sh --force` |
| **通用兜底** | 先 `bash scripts/local-precheck.sh`，再跑对应 package 的 `npm test` |

```bash
# 示例：Brain 相关失败，统一用 local-precheck
if echo "$FAILED_CHECKS" | grep -qiE "facts|manifest|version"; then
    echo "→ 运行本地预检..."
    bash scripts/local-precheck.sh --force
fi

# 测试失败
if echo "$FAILED_CHECKS" | grep -qi "brain-test\|test"; then
    echo "→ 运行本地测试..."
    cd packages/brain && npm test
    cd -
fi
```

**⛔ 禁止**：本地复现步骤未执行就直接 push。

### ③ 修复代码

根据本地复现的错误信息修复代码。

### ④ 本地验证全绿（才允许 push）

```bash
# 确认本地预检通过
bash scripts/local-precheck.sh

# 确认测试通过（如果测试是失败原因）
# cd packages/brain && npm test

echo "✅ 本地验证通过，准备 push"
```

**只有所有本地检查 exit 0，才能执行 push。**

### ⑤ push

```bash
git add -A
git commit -m "fix: <具体修复内容>

Co-Authored-By: Claude Sonnet 4.6 <noreply@anthropic.com>"

git push origin HEAD
```

**更新 incident resolution**：

```bash
RESOLUTION="<实际修复内容描述>"
jq --arg res "$RESOLUTION" '
    (to_entries | reverse | map(select(.value.step == "9-ci" and .value.resolution == "")) | first).key as $idx
    | .[$idx].resolution = $res
' "$INCIDENT_FILE" > /tmp/incident_tmp.json && mv /tmp/incident_tmp.json "$INCIDENT_FILE"
```

**修复后继续等待 CI**，Stop Hook 会在会话尝试结束时检查条件。

---

## 9.3 CI 通过后 → 先执行 Step 10

**⚠️ 重要：CI 通过后不立刻合并 PR**

原因：需要在功能分支上写 LEARNINGS.md 并 push，让 PR 自动包含 LEARNINGS 变更。

```bash
echo "✅ CI 通过！"
echo "→ 执行 Step 10 (Learning)：写 LEARNINGS → push 到功能分支 → 合并 PR"
```

**继续 → Step 10 (Learning)**

---

## 9.4 Stop Hook 完成条件

| 条件 | 状态 | Stop Hook 行为 |
|------|------|---------------|
| PR 未创建 | ❌ | exit 2（继续创建 PR）|
| CI 失败 | ❌ | exit 2（继续修复）|
| CI 进行中 | ⏳ | exit 2（继续等待）|
| CI 通过但未合并 | ❌ | exit 2（继续合并）|
| **PR 已合并** | ✅ | exit 0（完成！）|

---

## 禁止行为

- ❌ CI 失败后不本地复现就直接 push 修复
- ❌ CI 失败后停止不管
- ❌ PR 创建后就结束
- ❌ 等待用户手动处理

## 正确行为

- ✅ CI 失败 → 分析原因 → **本地复现** → 修复 → 本地全绿 → push → 继续等待
- ✅ CI 通过 → 执行 Step 10 (Learning)，在 Step 10 里写 LEARNINGS + 合并 PR
- ✅ PR 合并（在 Step 10 内完成）→ 继续 Step 11

---

## 完成后

**CI 通过后立刻执行 Step 10（不标记 Step 9 完成，PR 还未合并）**

Step 9 的完成标记在 Step 10 的合并步骤之后：

```bash
BRANCH_NAME=$(git rev-parse --abbrev-ref HEAD 2>/dev/null || echo "")
DEV_MODE_FILE=".dev-mode.${BRANCH_NAME}"
[[ -f "$DEV_MODE_FILE" ]] || DEV_MODE_FILE=".dev-mode"
sed -i "s/^step_9_ci: pending/step_9_ci: done/" "$DEV_MODE_FILE"
echo "✅ Step 9 完成标记已写入 .dev-mode"
```

**Task Checkpoint**: `TaskUpdate({ taskId: "9", status: "completed" })` ← Step 10 合并后执行

**继续 → Step 10 (Learning)**

写 LEARNINGS.md → push 到功能分支 → 合并 PR（PR 自动包含 LEARNINGS）
