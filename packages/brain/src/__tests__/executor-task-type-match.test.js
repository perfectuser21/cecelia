/**
 * executor-task-type-match.test.js — PLACEHOLDER
 * checkTaskTypeMatch() 已清空逻辑（exploratory 概念移除后函数为 no-op）
 * 原有告警检测测试（exploratory/dev 关键词警告）随之失效
 */
describe('executor-task-type-match (removed)', () => {
  it('placeholder: checkTaskTypeMatch is now a no-op, type-match warning tests obsolete', () => {
    // exploratory 概念已移除，checkTaskTypeMatch 保留接口但无逻辑
  });
});
