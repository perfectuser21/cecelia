# Task Card: 重构 cecelia-routes.mapStatus（复杂度 26 → 10）

## PRD

Brain 复杂度扫描器误报 `mapStatus` CC=26（实际 CC=2）。
PR #1353 已修复 scanner 函数边界检测 bug。
本次任务：导出 `mapStatus` + 添加直接单元测试，正式验收 CC<10。

## 成功标准

- mapStatus CC < 10（当前 CC=2，已满足）
- mapStatus 已导出，可直接 import 测试
- 直接单元测试覆盖所有 STATUS_MAP 映射

## DoD

- [ ] [ARTIFACT] `cecelia-routes.js` 中 `mapStatus` 已命名导出（`export function mapStatus`）
- [ ] [BEHAVIOR] 直接 import `mapStatus`，覆盖全部 6 种状态映射 + 未知状态默认 pending
- [ ] [GATE] `packages/brain/src/__tests__/cecelia-routes.test.js` 新增"直接测试 mapStatus"分组全通过
