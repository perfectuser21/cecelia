# PRD: 重构 cecelia-routes.mapStatus（复杂度 26 → 10）

## 背景

代码复杂度扫描发现 cecelia-routes 中的 mapStatus 函数圈复杂度过高（误报 CC=26）。
经分析，当前 mapStatus 实现为 1 行代码（CC=2），已满足阈值要求。
PR #1353 已修复 ComplexityScanner 函数边界检测 bug，此任务做验收确认。

## 目标

导出 `mapStatus` 函数并添加直接单元测试，完善测试覆盖率，正式验证 CC<10 要求。

## 文件

- `packages/brain/src/cecelia-routes.js`（第 38 行，mapStatus 函数）
- `packages/brain/src/__tests__/cecelia-routes.test.js`（新增直接单元测试）

## 成功标准

- [ ] `mapStatus` 函数已导出（export function）
- [ ] 直接单元测试覆盖所有 STATUS_MAP 键
- [ ] npm test 无新增失败
