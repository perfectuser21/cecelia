---
id: dod-task-card-scripts
version: 1.0.0
created: 2026-03-14
updated: 2026-03-14
changelog:
  - 1.0.0: 初始版本
---

# DoD - 引擎脚本支持 Task Card 格式（向后兼容）

## 验收标准

### 功能验收
- [x] [ARTIFACT] check-prd.sh 优先查找 `.task-{BRANCH}.md`，找不到则 fallback 到 `.prd-{BRANCH}.md`
  Test: manual:bash -c "grep -c 'task-' packages/engine/scripts/devgate/check-prd.sh"

- [x] [ARTIFACT] check-dod-mapping.cjs 优先查找 `.task-{branch}.md`，找不到则 fallback 到 `.dod-{branch}.md`
  Test: manual:bash -c "grep -c 'taskCard' packages/engine/scripts/devgate/check-dod-mapping.cjs"

- [x] [ARTIFACT] 两种格式向后兼容，旧的 .prd-*.md / .dod-*.md 路径仍然保留
  Test: manual:bash -c "grep -c 'prd-' packages/engine/scripts/devgate/check-prd.sh"

### 测试验收
- [x] CI 全部通过
  Test: contract:C2-001
