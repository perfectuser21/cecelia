# DoD - 修复 cecelia-bridge Math.min 截断（PR #858 验证）

## 验收清单

- [x] cecelia-bridge.js 已包含 CECELIA_BRIDGE_MAX_TIMEOUT_MS 新逻辑（PR #858）
  Test: manual:bash -c "grep -c 'CECELIA_BRIDGE_MAX_TIMEOUT_MS' packages/brain/scripts/cecelia-bridge.js"

- [x] cecelia-bridge.js 不再使用旧的 BRIDGE_TIMEOUT_MS 作为截断上限
  Test: manual:bash -c "grep -c 'CECELIA_BRIDGE_MAX_TIMEOUT_MS' packages/brain/scripts/cecelia-bridge.js"

- [x] LEARNINGS.md 已记录 Math.min 截断问题
  Test: manual:bash -c "grep -c 'Math.min' docs/LEARNINGS.md"

## 变更摘要

PR #858 修复了 cecelia-bridge.js line 74 的 `Math.min(timeout, BRIDGE_TIMEOUT_MS)` 截断问题：
- 旧逻辑：所有超时被截断至 120s（BRIDGE_TIMEOUT_MS 默认值）
- 新逻辑：引入 CECELIA_BRIDGE_MAX_TIMEOUT_MS（默认 600s）作为安全上限
- Cortex Opus 300s 超时现在可正确传递，不再被 SIGTERM

## 本任务角色

本任务（d7c5e7e3）是 PR #858 合并后 Brain 的追踪任务，用于确认修复已生效。
