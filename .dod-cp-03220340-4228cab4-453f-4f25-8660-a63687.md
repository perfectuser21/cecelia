# DoD：重构 cecelia-routes 降低函数复杂度

## 验收清单

- [ ] D1: inferStepProgress 复杂度 ≤10
- [ ] D2: buildCheckpoints 复杂度 ≤10
- [ ] D3: npm test（brain）全部通过

## 详细验收

### D1: inferStepProgress 复杂度 ≤10
- Test: `manual: node -e "import('./packages/brain/src/task-generators/complexity-scanner.js').then(m=>{const s=new m.default();return s.scan()}).then(issues=>{const f=issues.filter(i=>i.module_path.includes('cecelia-routes')&&i.function_name==='inferStepProgress');if(f.length>0){console.error('FAIL: inferStepProgress still complex:',f[0].current_value);process.exit(1)}console.log('PASS: inferStepProgress ok')})"`

### D2: buildCheckpoints 复杂度 ≤10
- Test: `manual: node -e "import('./packages/brain/src/task-generators/complexity-scanner.js').then(m=>{const s=new m.default();return s.scan()}).then(issues=>{const f=issues.filter(i=>i.module_path.includes('cecelia-routes')&&i.function_name==='buildCheckpoints');if(f.length>0){console.error('FAIL: buildCheckpoints still complex:',f[0].current_value);process.exit(1)}console.log('PASS: buildCheckpoints ok')})"`

### D3: npm test 通过
- Test: `manual: node --experimental-vm-modules node_modules/.bin/vitest run packages/brain/src/__tests__/ --reporter=verbose 2>&1 | tail -5`
