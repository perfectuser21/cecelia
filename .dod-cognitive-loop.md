---
id: dod-cognitive-loop
version: 1.0.0
created: 2026-03-01
updated: 2026-03-01
changelog:
  - 1.0.0: Cecelia 认知闭环架构验收清单
---

# DoD: Cecelia 认知闭环架构

## P0：统一信号入口

- [ ] 嘴巴（orchestrator-chat.js）发 USER_MESSAGE 给丘脑，不再自己路由（另一 terminal 处理，本项标记跳过）
- [ ] 飞书（feishu handler）发 USER_MESSAGE 给丘脑（另一 terminal 处理，本项标记跳过）

## P0：Plan + 丘脑融合

- [ ] 丘脑 L1 prompt 加载 /plan Skill 内容（callThalamus 的 system prompt 包含 plan 路由规则）
- [ ] /plan 路由新增类型：`research` / `remember` / `automate` / `kr_replan`（原有 coding 保留）
- [ ] 丘脑测试：发 5 种不同意图信号，路由结果正确

## P1：brain_context 注入

- [ ] 丘脑调 L1 前，先调 `buildMemoryContext(signal)` 并将结果注入 prompt
- [ ] 日志输出 `[brain_context]` 可见，包含 emotion_summary / self_key_facts 等字段
- [ ] 不引入新模块，复用 memory-retriever.js 的 `buildMemoryContext()`

## P1：Goal Evaluator → 走丘脑

- [ ] Goal Evaluator stalled 时，发 `GOAL_STALLED` 事件给丘脑（不再直接 createInitiativePlanForStall）
- [ ] 丘脑收到 `GOAL_STALLED` 能正确路由到 `kr_replan`

## P2：Rumination → RUMINATION_RESULT 协议

- [ ] Rumination 产出结构化 `{ learnings[], self_updates[], emotion_deltas[], actions[] }`
- [ ] 发信号给丘脑而非直接写 DB
- [ ] 丘脑收到后根据 `self_updates` 写 memory_stream，根据 `actions` 创建任务

## P2：激活 L2（Opus 皮层）

- [ ] L2 触发条件定义并实现：KR停滞>14天 / 失败率>60% / 方向冲突 / 用户要求复盘
- [ ] L2 产出写入 memory_stream（type='self_model'）后返回 L1 重路由
- [ ] DB 可查 cortex_route > 0（至少在测试中触发一次）

## 全局

- [ ] DevGate 通过：facts-check.mjs + check-version-sync.sh
- [ ] Brain 版本 bump（semver patch）
- [ ] 所有现有测试通过（brain-ci.yml 绿）
- [ ] 新增关键路径的测试（丘脑路由、brain_context 注入）
