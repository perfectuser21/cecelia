## DoD - executor 派发前增加 claude --version 健康探针

### 验收清单

- [x] 添加 `runClaudeProbe()` 函数到 tick.js
  Test: manual:bash -c "grep -c 'runClaudeProbe' packages/brain/src/tick.js"

- [x] 探针超时 5s（spawn + kill on timeout）
  Test: manual:bash -c "grep -c 'PROBE_TIMEOUT_MS' packages/brain/src/tick.js"

- [x] dispatchNextTask() 开头调用探针检查
  Test: manual:bash -c "grep -c 'runClaudeProbe\|_dispatchPaused' packages/brain/src/tick.js"

- [x] 失败时设置 _dispatchPaused=true，写 cecelia_events
  Test: manual:bash -c "grep -c '_dispatchPaused' packages/brain/src/tick.js"

- [x] 探针成功后自动恢复（_dispatchPaused=false），写 recovery event
  Test: manual:bash -c "grep -c 'claude_probe_recovered' packages/brain/src/tick.js"

- [x] 单测覆盖：探针失败场景 → 暂停派发
  Test: manual:bash -c "grep -c 'dispatch-claude-probe' packages/brain/src/__tests__/dispatch-claude-probe.test.js"

- [x] 单测覆盖：探针成功后恢复派发
  Test: manual:bash -c "grep -c 'runClaudeProbe\|probe' packages/brain/src/__tests__/dispatch-claude-probe.test.js"
