# PRD: 修复状态机与CI DoD同步漏洞

branch: cp-03222314-70643605-6f31-4896-bcec-c38e84

## 背景
verify-step.sh Gate 2 跑 DoD Test 通过后未写回 [x]，导致状态机与CI不同步。

## 功能描述
1. verify-step.sh Gate 2 Test通过后自动标 [x]
2. devloop-check.sh 加对应CI的DoD完整性检查

## 成功标准

- verify-step.sh Gate 2 中包含 `_mark_dod_done` 函数，Test 通过后用 `sed -i` 写回 `[x]`
- devloop-check.sh 包含条件 2.7 DoD 完整性检查，有未勾选 DoD 条目时输出 `dod_completeness` blocked 并 return 2
