# DoD: Stop Hook worktree 扫描

## 成功标准
1. [BEHAVIOR] stop.sh 包含 worktree 扫描逻辑
2. [GATE] 语法正确

## 验收条件（DoD）
- [x] [BEHAVIOR] stop.sh 包含 worktree 扫描逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop.sh','utf8');if(!c.includes('worktree list --porcelain'))process.exit(1)"
- [x] [BEHAVIOR] 扫描到 worktree 中的 .dev-lock 时设置 _DEV_LOCK_FOUND=true
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop.sh','utf8');if(!c.includes('_DEV_LOCK_FOUND=true')&&!c.includes('break 2'))process.exit(1)"
- [x] [GATE] stop.sh 语法正确
  Test: manual:bash -c "bash -n packages/engine/hooks/stop.sh"
