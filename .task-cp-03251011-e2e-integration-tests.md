---
id: task-cp-03251011-e2e-integration-tests
type: task-card
branch: cp-03251011-e2e-integration-tests
created: 2026-03-25
brain-task-id: b26dacf3-94f4-41c5-98d4-076f21c8ab2e
---

# Task Card: E2E集成测试补全 — 记忆路由链 + Thalamus动作验证

## 需求（What & Why）

**功能描述**: 为记忆路由链和 Thalamus ACTION_WHITELIST 验证补充跨模块集成测试

**背景**:
- 现有 integration/ 目录仅 3 个测试（tick-dispatch/tick-full-loop/okr-flow），全部聚焦 tick 循环
- 记忆系统（PR #1528-1536，6 个 PR）无跨模块集成测试
- Thalamus ACTION_WHITELIST 验证仅有单元测试，无集成测试验证真实链路
- memory-retriever.js 调用 memory-router.js 的跨模块集成行为未被测试覆盖

**不做什么**:
- 不需要真实 Brain 服务（避免 CI 依赖外部服务）
- 不需要真实 PostgreSQL（mock DB pool）
- 不需要真实 OpenAI API（mock embedding）

## 成功标准

- 新增 memory-router-retriever.integration.test.js，验证 routeMemory → buildMemoryContext 跨模块联动
- 新增 thalamus-action-whitelist.integration.test.js，验证 ACTION_WHITELIST + validateDecision 验证链

## 验收条件（DoD）

- [x] [PRESERVE] tick-dispatch / tick-full-loop / okr-decomposition-flow 三个现有集成测试行为不变
  Test: manual:node -e "['packages/brain/src/__tests__/integration/tick-dispatch.integration.test.js','packages/brain/src/__tests__/integration/tick-full-loop.integration.test.js','packages/brain/src/__tests__/integration/okr-decomposition-flow.integration.test.js'].forEach(f=>require('fs').accessSync(f))"

- [x] [ARTIFACT] memory-router-retriever.integration.test.js 已创建
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/memory-router-retriever.integration.test.js','utf8');if(!c.includes('routeMemory')||!c.includes('buildMemoryContext'))process.exit(1)"

- [x] [ARTIFACT] thalamus-action-whitelist.integration.test.js 已创建
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/thalamus-action-whitelist.integration.test.js','utf8');if(!c.includes('ACTION_WHITELIST'))process.exit(1)"

- [x] [BEHAVIOR] routeMemory('你在想什么') 路由到 SELF_REFLECTION，strategy.episodic=true
  Test: manual:node --input-type=module -e "import {routeMemory} from './packages/brain/src/memory-router.js';const r=routeMemory('你在想什么','chat');if(!r.strategy||!r.strategy.episodic){process.stderr.write('FAIL: SELF_REFLECTION should have episodic=true\n');process.exit(1);}console.log('OK: intentType='+r.intentType)"

- [x] [BEHAVIOR] routeMemory('系统状态告警') 路由到 STATUS_CHECK，strategy.semantic=false
  Test: manual:node --input-type=module -e "import {routeMemory} from './packages/brain/src/memory-router.js';const r=routeMemory('系统状态告警','chat');if(r.strategy&&r.strategy.semantic){process.stderr.write('FAIL: STATUS_CHECK should have semantic=false\n');process.exit(1);}console.log('OK: intentType='+r.intentType)"

- [x] [ARTIFACT] thalamus-action-whitelist.integration.test.js 包含有效 action 测试
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/thalamus-action-whitelist.integration.test.js','utf8');if(!c.includes('valid')||!c.includes('invalid'))process.exit(1)"

- [x] [GATE] CI 通过（brain 单元测试包含新集成测试）
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/integration/memory-router-retriever.integration.test.js');require('fs').accessSync('packages/brain/src/__tests__/integration/thalamus-action-whitelist.integration.test.js');console.log('both files exist')"

## 实现方案

**要改的文件**:
- 新增 `packages/brain/src/__tests__/integration/memory-router-retriever.integration.test.js`
- 新增 `packages/brain/src/__tests__/integration/thalamus-action-whitelist.integration.test.js`

**技术设计**:
1. memory-router-retriever 测试:
   - vi.mock('../../db.js', ...) + vi.mock('../../openai-client.js', ...)
   - 真实导入 routeMemory (memory-router.js) + buildMemoryContext (memory-retriever.js)
   - 验证不同意图类型触发不同 DB 查询策略

2. thalamus-action-whitelist 测试:
   - 真实导入 ACTION_WHITELIST from thalamus.js（vi.mock 所有 DB/LLM 依赖）
   - 验证 ACTION_WHITELIST 包含标准 action 类型
   - 验证未注册 action 类型不在白名单

**Scope 锚定**: `packages/brain/src/__tests__/integration/`（仅新增文件，不改现有代码）
