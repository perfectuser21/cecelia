---
id: dod-compare-report
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD - 实现 POST /api/brain/projects/compare/report

## 验收清单

- [x] 新建 `packages/brain/src/project-compare.js`，实现对比报告逻辑
  Test: manual:bash -c "ls packages/brain/src/project-compare.js"

- [x] 在 `packages/brain/src/routes.js` 新增 `POST /api/brain/projects/compare/report` 路由
  Test: manual:bash -c "grep -c 'projects/compare/report' packages/brain/src/routes.js"

- [x] 传入 2 个有效 project_id 返回 200，含完整 JSON（generated_at, format, projects, summary）
  Test: manual:bash -c "grep -c 'generateCompareReport' packages/brain/src/project-compare.js"

- [x] `format=markdown` 时响应含 markdown 字段
  Test: manual:bash -c "grep -c 'buildMarkdown' packages/brain/src/project-compare.js"

- [x] 传入少于 2 个 project_id 返回 400
  Test: manual:bash -c "grep -c 'at least 2' packages/brain/src/project-compare.js"

- [x] 传入不存在的 project_id 返回 400
  Test: manual:bash -c "grep -c 'not found' packages/brain/src/project-compare.js"

- [x] 有单元测试文件 `packages/brain/src/__tests__/project-compare.test.js`
  Test: manual:bash -c "ls packages/brain/src/__tests__/project-compare.test.js"

- [x] 测试覆盖正常路径（返回 200 报告）和错误路径（400 验证失败）
  Test: manual:bash -c "grep -c 'it(' packages/brain/src/__tests__/project-compare.test.js | grep -E '^[3-9]|^[0-9]{2}'"

- [x] 不破坏现有 Brain CI（brain-unit 通过）
  Test: manual:bash -c "grep -c 'export async function generateCompareReport' packages/brain/src/project-compare.js"
