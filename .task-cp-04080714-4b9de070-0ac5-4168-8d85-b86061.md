---
id: task-cp-04080714-4b9de070
type: task-card
branch: cp-04080714-4b9de070-0ac5-4168-8d85-b86061
created: 2026-04-08
---

# Task Card: 数据闭环 v1 — 周报 Dashboard 集成完善

## 需求（What & Why）
**功能描述**: 周报生成器和 Dashboard 页面均已实现，但 `/reports` 页面没有导航菜单入口（不可发现），且 ReportsListPage 无"手动生成周报"按钮。
**背景**: KR 数据闭环要求每周自动周报可在 Dashboard 查看并支持手动触发。
**不做什么**: 不重写周报生成器（已完整）；不增加数据库表；不改 social-media-sync。

## 成功标准

> [ARTIFACT] `/reports` 路由在 System 导航菜单下可见
> [BEHAVIOR] 点击"手动生成周报"触发 POST /api/brain/reports/generate { type: "weekly_report" }

## 验收条件（DoD）

- [x] [BEHAVIOR] system-hub/index.ts 中 /system navItem.children 包含 /reports 条目
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/system-hub/index.ts','utf8');if(!c.includes(\"'/reports'\")||!c.includes('Reports'))process.exit(1)"

- [x] [BEHAVIOR] ReportsListPage 有"手动生成周报"按钮，触发 weekly_report 类型生成
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/reports/ReportsListPage.tsx','utf8');if(!c.includes('weekly_report'))process.exit(1)"

- [x] [ARTIFACT] ReportsListPage 页面描述更新（去掉纯"48h"字样）
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/reports/ReportsListPage.tsx','utf8');if(c.includes('48h 自动生成的系统状态简报'))process.exit(1)"

## 实现方案
**要改的文件**:
1. `apps/api/features/system-hub/index.ts` — 在 System navItem.children 加 `/reports` 条目
2. `apps/dashboard/src/pages/reports/ReportsListPage.tsx` — 加"手动生成周报"按钮 + 更新描述

**不改什么**: weekly-report-generator.js、routes/system-reports.js、ReportDetailPage.tsx
