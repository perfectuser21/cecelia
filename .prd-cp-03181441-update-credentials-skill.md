# PRD: 更新 credentials SKILL.md — 1Password 优先流程

## 背景
当前 credentials SKILL.md 指引是直接存 ~/.credentials/，导致新凭据只在本地，1Password 里缺失。需要将流程改为 1Password 优先。

## 目标
将凭据管理流程改为：1Password CS Vault（SSOT）→ sync-credentials.sh → ~/.credentials/（缓存）

## 成功标准

1. SKILL.md 移除"cat >> ~/.credentials/"旧指引
2. SKILL.md 包含"先存 1Password，再 sync"的新流程
3. CI 通过
