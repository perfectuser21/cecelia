---
id: dod-fix-projects-compare-route
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

## DoD - fix: 修复 projects/compare 路由冲突

### 验收清单

- [x] `task-projects.js` 在 `GET /:id` 之前注册 `GET /compare`（Express 路由顺序正确）
  Test: manual:bash -c "grep -n 'GET /compare\|router.get.*compare\|router.get.*:id' packages/brain/src/routes/task-projects.js | head -5"

- [x] `task-projects.js` 在 `GET /:id` 之前注册 `POST /compare/report`
  Test: manual:bash -c "grep -c 'compare/report' packages/brain/src/routes/task-projects.js"

- [x] `routes.js` 中 `GET /projects/compare` 已删除
  Test: manual:bash -c "! grep -q 'router.get.*projects/compare' packages/brain/src/routes.js"

- [x] `routes.js` 中 `POST /projects/compare/report` 已删除
  Test: manual:bash -c "! grep -q 'router.post.*projects/compare/report' packages/brain/src/routes.js"

- [x] `routes/task-projects.test.js` 新增 3 个测试：GET /compare 200、GET /compare 400（ids<2）、POST /compare/report 200
  Test: manual:bash -c "grep -c 'GET /compare\|POST /compare' packages/brain/src/__tests__/routes/task-projects.test.js"
