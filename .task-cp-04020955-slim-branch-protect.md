# Task: 精简 branch-protect.sh（只留 worktree/分支保护核心逻辑）

## 描述

branch-protect.sh 从 998 行精简到 ~200 行。只保留核心检查：当前是否在 cp-* 分支的 worktree 中？是则放行，否则拦截并提示用 /dev。删掉所有 PRD/DoD 文件存在检查（交给 CI）、Learning 检查、版本检查等。保留 verify-step 状态机和 Gate seal 防伪检查。

## 功能描述

- 保留：JSON 输入解析、Write/Edit 过滤
- 保留：.dev-mode verify-step 状态机验证（v26）
- 保留：Gate seal 防伪检查（spec_review/code_review_gate）
- 保留：全局配置目录保护（hooks/skills）
- 保留：cp-* 分支名检查 + worktree 检测 + 僵尸 worktree 检测 + .dev-mode 存在检查
- 删除：PRD/DoD 文件存在检查、内容验证、数据库检查、PRD 更新检查、Task Checkpoint 检查、monorepo 子目录保护、.dod.md branch field check
- 删除：Task Card/PRD/DoD 内容写入验证

## 成功标准

- [x] [ARTIFACT] branch-protect.sh 行数 < 250 行 Test: `manual:node -e "const l=require('fs').readFileSync('packages/engine/hooks/branch-protect.sh','utf8').split('\\n').length;if(l>250)process.exit(1);console.log('Lines:',l)"`
- [x] [BEHAVIOR] cp-* worktree 中有 .dev-mode 时放行 Test: `manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/branch-protect.sh','utf8');if(!c.includes('IS_WORKTREE'))process.exit(1);if(!c.includes('.dev-mode'))process.exit(1);console.log('OK')"`
- [x] [BEHAVIOR] main 分支写代码被拦截 Test: `manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/branch-protect.sh','utf8');if(!c.includes('cp-'))process.exit(1);if(!c.includes('exit 2'))process.exit(1);console.log('OK')"`
- [x] [BEHAVIOR] verify-step 状态机验证仍存在 Test: `manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/branch-protect.sh','utf8');if(!c.includes('verify-step'))process.exit(1);console.log('OK')"`
- [x] [ARTIFACT] Engine 版本 bump 5 文件同步 Test: `manual:bash packages/engine/scripts/check-version-sync.sh`
