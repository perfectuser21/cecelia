---
id: task-cp-03220357-3c6070e3-8a41-4eaa-96e5-b8f12a
type: task-card
branch: cp-03220357-3c6070e3-8a41-4eaa-96e5-b8f12a
created: 2026-03-22
---

# Task Card: intent-expand集成Stage1 + Learning检索

## 需求（What & Why）
**功能描述**: 在 `fetch-task-prd.sh` 中实现两个功能：(1) 调用 Brain API 查询任务关联的 KR/Initiative/OKR 上下文（intent-expand），将其注入生成的 PRD；(2) 从任务标题提取关键词，搜索 `docs/learnings/` 目录推荐相关历史 Learning，并附加到 PRD。
**背景**: Agent 执行任务时缺少"更大目标"上下文（KR/OKR/Vision 链路）和历史踩坑记录，导致实现方向偏离或重复犯错。01-spec.md 已有 1.1.5（intent-expand）和 1.3.5（Learning 检索）文档章节，但 fetch-task-prd.sh 脚本未实现这两项功能。
**不做什么**: 不改变 01-spec.md 文档、不改变 Brain 后端路由、不改变生成文件的命名规则

## 成功标准
1. [ARTIFACT] fetch-task-prd.sh 在有 goal_id 时，PRD 包含 KR/Project 上下文章节
2. [ARTIFACT] fetch-task-prd.sh 根据任务标题关键词搜索 docs/learnings/ 并附加推荐
3. [BEHAVIOR] 当任务无 goal_id/project_id 时，脚本正常运行不报错
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] fetch-task-prd.sh 包含 intent-context 函数（查 goal/project 链）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/scripts/fetch-task-prd.sh','utf8');if(!c.includes('fetch_intent_context'))process.exit(1)"

- [x] [ARTIFACT] fetch-task-prd.sh 包含 Learning 搜索逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/scripts/fetch-task-prd.sh','utf8');if(!c.includes('docs/learnings'))process.exit(1)"

- [x] [BEHAVIOR] PRD 生成函数在 goal_id 非空时写入"战略上下文"段落
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/scripts/fetch-task-prd.sh','utf8');if(!c.includes('战略上下文'))process.exit(1)"

- [ ] [GATE] CI 全部通过
  Test: manual:bash -c "npm test 2>&1; exit ${PIPESTATUS[0]}"

## 实现方案
**要改的文件**: `packages/engine/skills/dev/scripts/fetch-task-prd.sh`
**Scope 锚定**: 仅修改 fetch-task-prd.sh，新增 fetch_intent_context 函数和 search_related_learnings 函数，在 generate_prd 中调用
