# DoD - 实现 tick.js 48h 简报定时检查逻辑

## 验收清单

- [x] `packages/brain/migrations/115_reports_table.sql` 创建 reports 表
- [x] `packages/brain/src/report-scheduler.js` 实现 `checkReportSchedule()` 函数
- [ ] `packages/brain/src/tick.js` 在 executeTick() 中调用 `checkReportSchedule()`
- [ ] `packages/brain/src/cortex.js` 添加 `generateSystemBrief()` 导出函数
- [ ] tick.js 返回值包含 `report_schedule` 字段
- [ ] 错误不阻塞 tick 循环（try/catch 包裹）
- [ ] Notion 配置缺失时优雅降级（只记录 DB 不推送）
- [ ] migration 文件编号唯一（115）
