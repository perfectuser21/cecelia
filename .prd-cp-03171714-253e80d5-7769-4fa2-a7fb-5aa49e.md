# PRD - 修复 Brain L3 测试失败：startup-sync + blocks.test.js

## 背景
修复两个导致 CI L3 每天挂掉的 Brain 测试问题

## 功能描述

1. startup-sync.test.js（3个失败）：PR #1025 改了孤儿恢复逻辑（watchdog_retry_count<2 → requeue，不再 fail），但测试仍按旧行为验证 orphans_fixed=1。需要更新 3 个测试用例匹配新的 requeue 行为。

2. blocks.test.js（17个失败）：集成测试直接 fetch localhost:5221，CI 里没有真实 Brain 服务。需要标记为 integration test 跳过 CI 单元测试。

## 成功标准

- [ ] startup-sync.test.js 3个失败测试更新为匹配 requeue 行为
- [ ] blocks.test.js 标记为集成测试，CI 默认跳过
- [ ] CI L3 全绿，20个失败测试全部修复
