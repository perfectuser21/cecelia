---
id: dod-compare-report-notion-export
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD - feat: 对比报告 Notion 导出

## 验收清单

- [x] `project-compare.js` 新增 `markdownToBlocks(markdown)` 函数，将 Markdown 转换为 Notion Blocks
  Test: manual:bash -c "grep -c 'markdownToBlocks' packages/brain/src/project-compare.js"

- [x] `project-compare.js` 新增 `exportToNotion(report)` 函数，调用 Notion API 创建 Page
  Test: manual:bash -c "grep -c 'exportToNotion' packages/brain/src/project-compare.js"

- [x] `generateCompareReport` 接受 `export` 参数，当 `export=notion` 时调用 exportToNotion
  Test: manual:bash -c "grep -c 'export.*notion\\|notion.*export' packages/brain/src/project-compare.js"

- [x] 路由 `POST /api/brain/projects/compare/report` 从 request body 中传递 `export` 参数
  Test: manual:bash -c "grep -c 'export' packages/brain/src/routes.js"

- [x] Notion export 失败时降级：主响应正常返回，携带 `notion_export_error` 字段
  Test: manual:bash -c "grep -c 'notion_export_error' packages/brain/src/project-compare.js"

- [x] 新增单元测试：exportToNotion 成功路径（mock fetch）
  Test: manual:bash -c "grep -c 'exportToNotion' packages/brain/src/__tests__/project-compare.test.js"

- [x] 新增单元测试：exportToNotion 失败时主报告仍返回
  Test: manual:bash -c "grep -c 'notion_export_error' packages/brain/src/__tests__/project-compare.test.js"

- [x] 新增单元测试：NOTION_API_KEY 未配置时降级返回错误
  Test: manual:bash -c "grep -c 'NOTION_API_KEY' packages/brain/src/__tests__/project-compare.test.js"
