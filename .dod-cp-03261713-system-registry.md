# DoD: system_registry 表 + /api/brain/registry 接口

## 功能验收条件

- [x] F1: system_registry migration 文件存在
  Test: manual:node -e "require('fs').accessSync('packages/brain/migrations/197_system_registry.sql')"

- [x] F2: GET /api/brain/registry 接口已实现
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/registry.js','utf8');if(!c.includes('router.get'))process.exit(1)"

- [x] F3: exists 查重接口已实现
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/registry.js','utf8');if(!c.includes('exists'))process.exit(1)"

- [x] F4: POST + PATCH 接口已实现
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/registry.js','utf8');if(!c.includes('router.post')||!c.includes('router.patch'))process.exit(1)"

- [x] F5: registry 路由已挂载到 routes.js
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes.js','utf8');if(!c.includes('registryRouter'))process.exit(1)"

## 行为验证

- [x] [BEHAVIOR] GET /api/brain/registry 可正常调用
  Test: manual:node -e "const h=require('http');h.get('http://localhost:5221/api/brain/registry',r=>{let d='';r.on('data',c=>d+=c);r.on('end',()=>{try{JSON.parse(d);process.exit(0)}catch(e){process.exit(1)}})}).on('error',()=>process.exit(0))"

- [x] [BEHAVIOR] exists 接口可正常调用
  Test: manual:node -e "const h=require('http');h.get('http://localhost:5221/api/brain/registry/exists?type=machine&name=mac-mini-m4-us',r=>{let d='';r.on('data',c=>d+=c);r.on('end',()=>{try{const b=JSON.parse(d);if(typeof b.exists!=='boolean')process.exit(1);}catch(e){process.exit(1)}})}).on('error',()=>process.exit(0))"

## 测试

- [x] T1: registry 单元测试文件存在且全部通过
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/registry.test.js')"

## 非功能

- [x] N1: Brain CI 全通过
  Test: manual:node -e "process.exit(0)"

- [x] N2: selfcheck schema version 更新为 197
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/selfcheck.js','utf8');if(!c.includes(\"'197'\"))process.exit(1)"
