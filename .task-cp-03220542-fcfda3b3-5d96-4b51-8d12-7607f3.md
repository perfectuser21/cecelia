# Task Card: 重构 executeViaLlmIntent（圈复杂度 33 → 10 以下）

## 背景

`packages/brain/src/chat-action-dispatcher.js` 中的 `executeViaLlmIntent` 函数（第 215 行）
圈复杂度为 33，远超阈值 10。复杂度来源：
- switch 多分支（6 个 case）
- LEARN case 内部嵌套：去重检查 + URL 检测 + 异步 IIFE + try-catch
- 多处短路运算符（`||`、`&&`）被计入复杂度

## 成功标准

- `executeViaLlmIntent` 函数本体简化为纯分发器，不包含 pool.query / createTask 等业务调用
- 各 case 提取为独立命名函数（handleLlmCreateTask / handleLlmLearn / handleLlmResearch / triggerUrlResearch）
- `detectAndExecuteAction` 导出签名不变，模块无语法错误

## DoD 条目

- [x] [BEHAVIOR] 模块语法无错误，可正常解析（node --check）
  Test: manual:node --check packages/brain/src/chat-action-dispatcher.js

- [x] [BEHAVIOR] executeViaLlmIntent 函数体简化为分发器（不超过 20 行）
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/chat-action-dispatcher.js','utf8');const start=src.indexOf('async function executeViaLlmIntent');if(start<0){process.exit(1)}const fromFn=src.slice(start);let depth=0,count=0;for(const l of fromFn.split('\n')){count++;depth+=(l.match(/\{/g)||[]).length-(l.match(/\}/g)||[]).length;if(count>1&&depth<=0)break}if(count>20){console.error('函数体过长:'+count+'行');process.exit(1)}console.log('OK lines='+count)"

- [x] [BEHAVIOR] 四个子函数全部提取到文件中（通过 function 声明验证）
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/chat-action-dispatcher.js','utf8');const fns=['handleLlmCreateTask','handleLlmLearn','handleLlmResearch','triggerUrlResearch'];const missing=fns.filter(n=>!src.includes('async function '+n));if(missing.length){console.error('缺少:'+missing);process.exit(1)}console.log('OK')"

- [x] [BEHAVIOR] executeViaLlmIntent 函数体内不含业务逻辑（无 pool.query/createHash/INSERT）
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/chat-action-dispatcher.js','utf8');const s=src.indexOf('async function executeViaLlmIntent');const e=src.indexOf('\nasync function',s+10);const body=src.slice(s,e>0?e:s+2000);const biz=['pool.query','createHash','INSERT INTO','addSource','createTask'];const found=biz.filter(k=>body.includes(k));if(found.length){console.error('含业务逻辑:'+found.join(','));process.exit(1)}console.log('OK')"

- [x] [BEHAVIOR] detectAndExecuteAction 导出函数存在且参数个数为 2
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/chat-action-dispatcher.js','utf8');const m=src.match(/export\s+async\s+function\s+detectAndExecuteAction\s*\(([^)]*)\)/);if(!m){console.error('导出未找到');process.exit(1)}const params=m[1].split(',').map(s=>s.trim()).filter(Boolean);if(params.length<2){console.error('参数不足:'+params.length);process.exit(1)}console.log('OK params='+params.length)"

## [PRESERVE] 行为快照（防回归）

- [x] [PRESERVE] ACTION_PATTERNS 关键词匹配规则仍存在（CREATE_TASK/QUERY_STATUS/CREATE_LEARNING）
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/chat-action-dispatcher.js','utf8');if(!src.includes('ACTION_PATTERNS')||!src.includes('CREATE_TASK')||!src.includes('QUERY_STATUS')){process.exit(1)}console.log('OK')"

- [x] [PRESERVE] detectAction 和 executeAction 均以 export function 形式导出
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/chat-action-dispatcher.js','utf8');if(!src.includes('export function detectAction')||!src.includes('export async function executeAction')){process.exit(1)}console.log('OK')"

