---
id: prd-task-006e7c15
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本（从 Brain Task 生成）
---

# PRD: /dev Step 10 重写 — 分类写入 learnings DB

## 目标

重写 /dev Step 10 (Learning 记录)，将 Learning 内容按类型分类写入 Brain PostgreSQL learnings 表，同时保留现有 MD 文件格式兼容性。

## 背景

- Task 1 (PR #891) 已完成：migration 151 为 learnings 表添加了 `learning_type`、`source_branch`、`source_pr`、`repo` 字段
- 5 种学习类型：`trap`、`architecture_decision`、`process_improvement`、`failure_pattern`、`best_practice`
- 当前 Step 10 只生成 MD 文件，不写 DB

## 成功标准

- Step 10 完成后，learnings 表有新记录，包含 learning_type、source_branch、source_pr 字段
- 现有 docs/learnings/<branch>.md 文件仍然生成（向后兼容）
- Learning 分类由 LLM 自动判断，无需开发者手动填写
- CI L1 Learning Format Gate 继续通过

## 范围

- 修改 `~/.claude/skills/dev/steps/10-learning.md`（Step 10 脚本）
- 在 Brain 中添加 `POST /api/brain/learnings` 写入端点（或复用现有 learnings API）
- LLM 调用分类（使用 Haiku 判断类型）

## 约束

- 不修改 CI Learning Format Gate 检查逻辑
- 保持 docs/learnings/<branch>.md 生成（向后兼容 CI 检查）
- DB 写入失败不应导致 Step 10 整体失败（best-effort）
