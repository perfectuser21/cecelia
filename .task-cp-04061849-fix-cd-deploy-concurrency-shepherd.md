---
id: task-cp-04061849-fix-cd-deploy-concurrency-shepherd
type: task-card
branch: cp-04061849-fix-cd-deploy-concurrency-shepherd
created: 2026-04-06
---

# Task Card: 修复 CD 流水线 concurrency 死锁 + Brain URL + shepherd 任务闭环

## 需求（What & Why）

**功能描述**:
1. deploy.yml `deploy` job 有 job-level concurrency `deploy-production`，与 workflow-level 同名 → GitHub Actions 死锁 → 0步失败
2. `BRAIN_DEPLOY_URL` secret 未设置 → fallback `https://dev-autopilot.zenjoymedia.media` → Xray 拦截 → 404
3. shepherd.js 检测到 PR merged 后只更新 `pr_status`，不设 `task.status='completed'` → KR 进度永远不推进

**背景**: 所有今天合并的 PR 实际上都没有部署到生产。Brain 认知闭环（任务→KR 进度）完全断链。

**不做什么**: 不改 CI 流程、不动 staging 逻辑结构、不加新功能

## 成功标准

## 验收条件（DoD）

- [x] [ARTIFACT] deploy.yml `deploy` job 不含 job-level concurrency 字段
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/deploy.yml','utf8');const jobs=c.split('deploy:')[1];if(jobs.includes('concurrency') && jobs.split('concurrency')[1].includes('deploy-production') && jobs.indexOf('concurrency')<jobs.indexOf('runs-on'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] deploy.yml BRAIN_URL 默认值改为 http://38.23.47.81:5221
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/deploy.yml','utf8');if(!c.includes('38.23.47.81:5221'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] shepherd.js 在 PR merge 后调用 task status 更新为 completed
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/shepherd.js','utf8');if(!c.includes(\"status: 'completed'\") && !c.includes('status:\"completed\"') && !c.includes('status: \"completed\"'))process.exit(1);console.log('ok')"

## 实现方案

**要改的文件**:
- `.github/workflows/deploy.yml` — 移除 deploy job 的 job-level concurrency（lines 246-248），改 BRAIN_URL 默认值
- `packages/brain/src/shepherd.js` — PR merged 时额外调 task status update

**不改什么**: staging_deploy 逻辑、risk_gate 逻辑、CI 配置
