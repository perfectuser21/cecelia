---
id: dod-compare-report-notion-push
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

## DoD - feat(dashboard): 对比报告 Notion 推送端点

## 验收清单

- [x] `pushCompareReportToNotion` 函数已添加到 `packages/brain/src/project-compare.js`
  Test: manual:bash -c "grep -c 'pushCompareReportToNotion' packages/brain/src/project-compare.js"

- [x] `pushCompareReportToNotion` 已 export
  Test: manual:bash -c "grep -c 'export async function pushCompareReportToNotion' packages/brain/src/project-compare.js"

- [x] 路由 `POST /compare/report/push` 已注册到 `packages/brain/src/routes/task-projects.js`
  Test: manual:bash -c "grep -c 'compare/report/push' packages/brain/src/routes/task-projects.js"

- [x] 路由注册在 `router.get('/:id')` 之前
  Test: manual:bash -c "node -e \"const fs=require('fs');const c=fs.readFileSync('packages/brain/src/routes/task-projects.js','utf8');const a=c.indexOf('compare/report/push');const b=c.indexOf(\\\"router.get('/:id'\\\");process.exit(a>0&&a<b?0:1)\""

- [x] NOTION_API_KEY 缺失时返回 400 NOTION_CONFIG_MISSING
  Test: manual:bash -c "grep -c 'NOTION_CONFIG_MISSING' packages/brain/src/project-compare.js"

- [x] notion_parent_id 缺失时返回 400
  Test: manual:bash -c "grep -c 'NOTION_COMPARE_PARENT_ID' packages/brain/src/project-compare.js"

- [x] destination 非 notion 时返回 400
  Test: manual:bash -c "grep -c 'destination' packages/brain/src/routes/task-projects.js"

- [x] 单元测试文件 `packages/brain/src/__tests__/project-compare-push.test.js` 已创建
  Test: manual:bash -c "ls packages/brain/src/__tests__/project-compare-push.test.js"

- [x] 测试覆盖正常路径（成功推送到 Notion）
  Test: manual:bash -c "grep -c 'success.*true' packages/brain/src/__tests__/project-compare-push.test.js"

- [x] 测试覆盖 NOTION_API_KEY 缺失场景
  Test: manual:bash -c "grep -c 'NOTION_CONFIG_MISSING' packages/brain/src/__tests__/project-compare-push.test.js"

- [x] 测试覆盖 notion_parent_id 缺失场景
  Test: manual:bash -c "grep -c 'notion_parent_id' packages/brain/src/__tests__/project-compare-push.test.js"

- [x] 测试覆盖 project_ids 不足 2 个场景
  Test: manual:bash -c "grep -c 'project_ids' packages/brain/src/__tests__/project-compare-push.test.js"

- [x] 测试覆盖 destination 非 notion 场景
  Test: manual:bash -c "grep -c 'destination' packages/brain/src/__tests__/project-compare-push.test.js"

- [x] 测试覆盖 Notion API 返回错误场景
  Test: manual:bash -c "grep -c 'notion.*error\|error.*notion\|Notion API' packages/brain/src/__tests__/project-compare-push.test.js"

## 实现方案

- `pushCompareReportToNotion`：直接用 `fetch` 调用 Notion API，无需修改 notion-sync.js（notionRequest 未导出）
- Markdown → Notion blocks：按 `\n\n` 分段，每段最多 2000 字符，转换为 paragraph block
- 路由在 `/compare/report` 之后、`/:id` 之前注册
