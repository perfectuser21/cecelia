# Task Card: migration 164 补充缺失的 started_at 列

## 需求（What & Why）
**功能描述**: migration 164 补充遗漏的 started_at 列
**背景**: Brain 启动报错 column "started_at" does not exist，因为 migration 164 只添加了 completed_at

## 成功标准

1. [ARTIFACT] migration 164 同时添加 started_at 和 completed_at 两列
2. [BEHAVIOR] 使用 ADD COLUMN IF NOT EXISTS 确保幂等性
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] migration 164 包含 started_at 列
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/164_fix_missing_columns.sql','utf8');if(!c.includes('started_at'))process.exit(1)"

- [x] [BEHAVIOR] 使用 ADD COLUMN IF NOT EXISTS 确保幂等执行
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/164_fix_missing_columns.sql','utf8');if(!c.includes('ADD COLUMN IF NOT EXISTS started_at'))process.exit(1)"

- [x] [GATE] EXPECTED_SCHEMA_VERSION 保持 164 不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/selfcheck.js','utf8');const m=c.match(/EXPECTED_SCHEMA_VERSION/);if(!m)process.exit(1)"
