# PRD: 【Day5】Brain 调度器默认开启 + 状态感知任务生成

> 完整 PRD+DoD 见 `.task-cp-03272224-1ca4f206-4a4f-4225-a47f-2555dd.md`

## 功能描述

1. `self-drive.js` 读取 `.agent-knowledge/CURRENT_STATE.md`，将系统当前状态融入 LLM 分析提示词
2. tick 调度器默认状态改为 `true`（`?? false` → `?? true`），Brain 启动后无需手动 enable

## 成功标准

- Brain 启动后调度器自动开启（无需手动 enable）
- self-drive 周期读取 CURRENT_STATE.md 并将内容注入分析提示词
- LLM 分析提示词包含"系统当前状态"章节

## DoD

- [x] [BEHAVIOR] tick.js 默认 tick 状态改为 `true`
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('?? true'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] self-drive.js 包含 readCurrentState() 函数，文件缺失时返回 null
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/self-drive.js','utf8');if(!c.includes('readCurrentState'))process.exit(1);if(!c.includes('return null'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] buildAnalysisPrompt 包含"系统当前状态"章节
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/self-drive.js','utf8');if(!c.includes('系统当前状态'))process.exit(1);if(!c.includes('currentState'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] runSelfDrive() 调用 readCurrentState() 并传入 analyzeSituation()
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/self-drive.js','utf8');if(!c.includes('readCurrentState()'))process.exit(1);console.log('OK')"
