---
id: task-cp-03171041-engine-rci-cleanup
type: task-card
branch: cp-03171041-bb8d2703-2949-4e08-b4b5-2add05
created: 2026-03-17
---

# Task Card: Engine RCI 清理 + 动态 CI 守护

## 需求（What & Why）
**功能描述**:
1. 清理 `packages/engine/regression-contract.yaml` 中 35 条引用不存在文件的条目 + 3 组重复 ID（C8-001/002/003）
2. 新增动态 CI 健康守护：`check-rci-health.mjs`（RCI 条目有效性检查）+ 已知失败 expires 过期检测

**背景**: RCI 有 35 个 test 字段引用不存在的文件（陈旧路径），3 组重复 ID 会导致 CI 工具误判。没有守护规则，垃圾条目会持续积累。

**不做什么**:
- 不新增 `check-coverage-completeness.mjs`（scan-rci-coverage.cjs 已有覆盖率功能）
- 不修改 `rci-execution-gate.sh` 读取路径（当前指向 packages/quality，engine RCI 格式不同）
- 不做反向孤儿检测（scope 过大，独立任务）
- 不删除有效条目（只修复/清理垃圾条目）

## 成功标准

1. [ARTIFACT] `regression-contract.yaml` 无重复 ID
2. [ARTIFACT] `regression-contract.yaml` 中 `method: auto` 条目的 test 字段引用文件均存在（或改为 manual）
3. [ARTIFACT] `check-rci-health.mjs` 脚本存在并可正常运行
4. [BEHAVIOR] `node packages/engine/scripts/devgate/check-rci-health.mjs` 对清理后的 YAML 输出通过
5. [BEHAVIOR] known-failures.json 过期条目被 check-rci-health.mjs 报警（expires < today）
6. [GATE] CI 全部通过

## 验收条件（DoD）

- [ ] [ARTIFACT] regression-contract.yaml 无重复 ID（C8 重复已修复）
  Test: manual:bash -c "! (grep '^  - id:' packages/engine/regression-contract.yaml | awk '{print \$3}' | sort | uniq -d | grep -q .)"

- [ ] [ARTIFACT] auto 条目的 test 字段引用文件全部存在（或已改为 manual）
  Test: manual:node packages/engine/scripts/devgate/check-rci-health.mjs --check missing-files

- [ ] [ARTIFACT] check-rci-health.mjs 脚本存在
  Test: manual:ls packages/engine/scripts/devgate/check-rci-health.mjs

- [ ] [BEHAVIOR] check-rci-health.mjs 整体健康检查通过（exit 0）
  Test: manual:node packages/engine/scripts/devgate/check-rci-health.mjs

- [ ] [BEHAVIOR] known-failures.json 过期条目被检测到
  Test: manual:node packages/engine/scripts/devgate/check-rci-health.mjs --check known-failures

- [ ] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/engine && npx vitest run 2>&1 | tail -5"

## 实现方案（Step 2 探索后填写）
**要改的文件**:
- `packages/engine/regression-contract.yaml` — 修复重复 ID + 清理缺失文件引用
- `packages/engine/scripts/devgate/check-rci-health.mjs` — 新建健康检查脚本

**Scope 锚定**: packages/engine/ 内部，不涉及 brain/apps
