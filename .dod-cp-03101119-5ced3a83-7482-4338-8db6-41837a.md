# DoD - 修复 Cortex _reflectionState 持久化恢复不检查过期

## 验收清单

- [x] D1: cortex.js `_loadReflectionStateFromDB` 使用 `lastSeen` 滑动窗口检查过期（PR #791 已实现）
  Test: manual:bash -c "grep -c 'lastSeen ?? val.firstSeen' packages/brain/src/cortex.js"

- [x] D2: 过期条目（>REFLECTION_WINDOW_MS）被加入 expiredKeys，触发 DELETE 查询（PR #791 已实现）
  Test: manual:bash -c "grep -c 'expiredKeys.push' packages/brain/src/cortex.js"

- [ ] D3: 新增单测：DB 返回过期条目（lastSeen 超过 30 分钟）→ `_checkReflectionBreaker` 返回 count=1（fresh start，不加载过期状态）
  Test: manual:bash -c "grep -c 'lastSeen.*expired\\|expired.*lastSeen\\|过期.*不被加载\\|D5.*过期\\|Expired' packages/brain/src/__tests__/cortex-reflection-breaker.test.js"

- [ ] D4: 新增单测：过期条目触发 DELETE working_memory 查询
  Test: manual:bash -c "grep -c 'DELETE.*working_memory\\|expiredKey\\|expired_key' packages/brain/src/__tests__/cortex-reflection-breaker.test.js"

- [ ] D5: 新测试通过
  Test: manual:bash -c "ls packages/brain/src/__tests__/cortex-reflection-breaker.test.js && echo OK"
