---
id: task-cp-03161732-fix-skill-md-descriptions
type: task-card
branch: cp-03161732-fix-skill-md-descriptions
created: 2026-03-16
---

# Task Card: 补全 11 个 SKILL.md description 字段

## 需求（What & Why）

**功能描述**: 给 `packages/workflows/skills/` 下 11 个缺少 `description:` frontmatter 字段的 SKILL.md 补上描述，防止 `generate-skills-index.mjs` 回退到读文件前 100 字。

**背景**: arch-review P1 问题，41/51 SKILL.md 有 description，10 个没有。

**不做什么**: 不修改已有 description 内容，不改 skills-index.md，不改任何逻辑代码。

**待修改的 11 个文件**:
1. content-analyzer — 无 frontmatter
2. content-creator — 无 frontmatter
3. content-rewriter — 无 frontmatter
4. image-gen-workflow — 有 frontmatter 但无 description
5. media-scraping — 有 frontmatter 但无 description
6. nas-backup — 无 frontmatter
7. nas — 无 frontmatter
8. talk — 无 frontmatter
9. upgrade-dashboard — 无 frontmatter
10. versioning — 有 frontmatter 但无 description
11. architect — 有 frontmatter 但无 description

## 成功标准

1. [ARTIFACT] 11 个目标 SKILL.md 都包含 `description:` 字段
2. [BEHAVIOR] `generate-skills-index.mjs --dry-run` 对这 11 个 skill 输出非空描述（不是文件前 100 字降级）
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] 11 个 SKILL.md 均含 description: 字段
  Test: manual:node -e "const fs=require('fs');const skills=['content-analyzer','content-creator','content-rewriter','image-gen-workflow','media-scraping','nas-backup','nas','talk','upgrade-dashboard','versioning','architect'];const missing=skills.filter(s=>{const c=fs.readFileSync('packages/workflows/skills/'+s+'/SKILL.md','utf-8');return !c.includes('description:')});if(missing.length>0)throw new Error('missing description: '+missing.join(','))"

- [x] [BEHAVIOR] generate-skills-index.mjs --dry-run 含这 11 个 skill 的描述
  Test: manual:node -e "const r=require('child_process').execSync('node scripts/generate-skills-index.mjs --dry-run',{encoding:'utf-8'});if(!r.includes('content-analyzer')||!r.includes('architect'))throw new Error('missing in output')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案

**要改的文件**: 11 个 SKILL.md（packages/workflows/skills/{name}/SKILL.md）
**Scope 锚定**: 纯 frontmatter 文档补充，不涉及任何逻辑
