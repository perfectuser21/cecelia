# Task Card: 修复 worktree GC 误删 + Sprint Report 误导输出

**branch**: cp-03311351-fix-worktree-gc-sprint-report
**task_id**: aa1a59bb-a335-4bd6-80fa-8369586857e8
**priority**: P0

## 问题

Sprint Report 评分 7/40，根因是三个 bug：
1. worktree-gc.sh 缺少 `.dev-mode` 守卫，context 超限后 `.dev-session-active` 消失 → GC 误删活跃 worktree
2. generate-sprint-report.sh seal 缺失时输出硬编码误导文字 + 给虚假 5 分
3. stop-dev.sh `.dev-session-active` 删除点（已验证正确，无需修改）

## 成功标准

- worktree-gc.sh 对含有未完成 `.dev-mode` 的 worktree 不执行删除
- stop-dev.sh 只在 `.dev-mode` 含 `cleanup_done: true` 时才删除 `.dev-session-active`
- Sprint Report seal 文件缺失时输出「⚠️ 无法检测（Seal 文件缺失）」并评 0 分
- Engine 版本 bump：13.73.1 → 13.73.2

## 验收清单

- [x] worktree-gc.sh 增加 `.dev-mode` 文件守卫（v1.3.0）
- [x] generate-sprint-report.sh seal 缺失 → 输出「⚠️ 无法检测（Seal 文件缺失）」+ 得 0 分
- [x] stop-dev.sh 验证 cleanup_done 守卫存在（无需修改）
- [x] Engine 版本 bump：13.73.1 → 13.73.2
