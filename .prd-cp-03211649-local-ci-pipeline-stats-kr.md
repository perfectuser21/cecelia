# PRD: 本地 CI 镜像检查 + Pipeline 统计汇总 + KR 进度回写

## 背景
当前 /dev 流程存在三个改进点：
1. Stage 2 push 前缺少本地 npm test / check-learning / check-dod-mapping 检查，导致 CI 失败率较高
2. Stage 4 缺少 Pipeline 各阶段耗时统计
3. Stage 4 回调 Brain 时不携带 goal_id，无法自动更新 KR 进度

## 需求
1. 在 02-code.md 的 code_review_gate 派发前，加入本地 CI 镜像检查（npm test + check-learning + check-dod-mapping）
2. 在 04-ship.md 的执行质量自评前，加入 Pipeline 统计汇总输出
3. 在 devloop-check.sh 的 execution-callback 中附带 goal_id

## 成功标准

1. [ARTIFACT] 02-code.md 在 2.3.4 和 2.4 之间新增 2.3.5 本地 CI 镜像检查章节
2. [ARTIFACT] 04-ship.md 在 4.4 执行质量自评之前新增 4.3 Pipeline 统计章节
3. [BEHAVIOR] devloop-check.sh 的 execution-callback curl 请求包含 goal_id 字段
