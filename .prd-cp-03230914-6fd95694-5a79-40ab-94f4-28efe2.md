# PRD: 修复 KR 进度信号 — verifier FK 断裂 + 进度采集失效

branch: cp-03230914-6fd95694-5a79-40ab-94f4-28efe2

## 背景

PR #1475 将 `kr-verifier.js` 的 JOIN 从 `goals` 改为 `key_results`，但没有同步更新
`kr_verifiers.kr_id` 外键指向。造成多个 KR 长期保持 0% 进度，Dashboard 看板无法反映任务实际进展。

## 根本原因

1. 7 个 verifier 指向 archived key_results：过滤条件跳过，进度永远不更新
2. 8 个 verifier 指向 goals-only IDs：UPDATE key_results 匹配 0 行，静默失败
3. 7 个活跃 key_results 没有任何 verifier → progress = 0

## 目标

通过 migration 183 修复 FK 断裂，重新链接 verifier 到 active key_results，并立即回填进度。

## 成功标准

1. migration 183 文件存在，包含 FK 变更 + verifier 重新链接 + 进度回填
2. 迁移后 7 个活跃 key_results 各有 1 个启用的 verifier 指向它们
3. 迁移后 key_results.progress 反映 verifier 的 current_value/threshold（不再全 0）
4. CI 全部通过

## 不做什么

- 不改 kr-verifier.js 代码（PR #1475 后逻辑已正确）
- 不修复 okr_projects.kr_id = NULL 问题（下一个 PR）
- 不修改 Dashboard 前端
