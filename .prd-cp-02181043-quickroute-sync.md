## PRD - quickRoute 全量扩展后 facts-check 同步

**需求来源**: 本轮 quickRoute 扩展新增多个事件类型快速路由规则，需要统一做一次 facts-check 和文档同步。

**功能描述**:
1. 为 DEPARTMENT_REPORT、EXCEPTION_REPORT、RESOURCE_LOW、USER_COMMAND、USER_MESSAGE 新增 quickRoute 规则
2. 为 TASK_COMPLETED（有问题时）和 TICK（有异常时）补充分级路由逻辑
3. 同步更新 DEFINITION.md 中的 quickRoute 覆盖事件列表
4. 验证 facts-check.mjs 全通过
5. patch bump 版本号

**涉及文件**:
- brain/src/thalamus.js（新增 quickRoute 规则）
- DEFINITION.md（更新 quickRoute 覆盖事件描述）
- brain/package.json + brain/package-lock.json（版本 bump）
- brain/VERSION（同步版本）

**成功标准**:
- node scripts/facts-check.mjs 全通过
- bash scripts/check-version-sync.sh 全通过
- npm test 在 brain/ 目录下全通过
- DEFINITION.md 记录完整 quickRoute 覆盖事件列表

**非目标**: 不修改 ACTION_WHITELIST（action_count 维持 27）
