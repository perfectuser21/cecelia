# Task Card: L1 CI 强制前端 DoD 浏览器验证检查 — Cecelia

**Branch**: cp-04010843-l1-frontend-browser-check
**Brain Task**: 6f12828f-92ba-4d4f-b8a6-e64efa808efd
**Type**: fix
**Priority**: high

---

## PRD

### 背景

Cecelia 系统的 CI L1/L3 门禁目前只保证前端代码能编译（TypeCheck + Build），不保证功能在浏览器里真实可用。每次前端功能合并后，在浏览器手测时频繁发现 bug，导致多轮修复循环。

### 问题根源

没有强制要求：开发者在提交前必须在浏览器里验证过主流程。

### 目标

在 L1 CI 加一个新检查 job：当 PR 包含前端文件改动时，强制要求 DoD 中含浏览器验证关键词。没有则 CI 拒绝合并。

### 成功标准

## 成功标准

1. 前端 PR 缺少浏览器验证步骤时，L1 CI 以清晰错误拒绝
2. 前端 PR 含浏览器验证步骤时，检查通过
3. 非前端 PR 不受影响

---

## DoD

- [x] [ARTIFACT] `.github/workflows/ci-l1-process.yml` 包含 `frontend-browser-dod-check` job
  Test: `manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l1-process.yml','utf8');if(c.indexOf('frontend-browser-dod-check')<0)process.exit(1);console.log('OK')"`

- [x] [BEHAVIOR] 前端改动无浏览器验证关键词时，frontend-browser-dod-check job 以非零退出码退出
  Test: `manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l1-process.yml','utf8');if(c.indexOf('manual:chrome:')<0)process.exit(1);console.log('OK')"`

- [x] [BEHAVIOR] 新 job 已加入 l1-passed 的 needs 数组，前端 PR 无浏览器验证无法合并
  Test: `manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l1-process.yml','utf8');if(c.indexOf('frontend-browser-dod-check')<0)process.exit(1);console.log('OK')"`
