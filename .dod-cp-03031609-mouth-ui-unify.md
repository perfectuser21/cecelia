---
id: dod-mouth-ui-unify
version: 1.0.0
created: 2026-03-03
updated: 2026-03-03
changelog:
  - 1.0.0: 初始版本
---

# DoD：嘴巴 Agent 配置 UI 统一化

## 验收清单

- [x] **删除 MouthModeSelector**：硬编码的 2×2 网格组件已移除
  Test: manual:bash -c "! grep -q 'MouthModeSelector' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **嘴巴使用 AgentCard**：mouth 不再被 `a.id !== 'mouth'` 过滤，与其他 brain agents 统一
  Test: manual:bash -c "! grep -q 'a.id !== .mouth.' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **移除 mouth 专用 API 导入**：fetchMouthConfig, updateMouthConfig 已从 import 中移除
  Test: manual:bash -c "! grep -q 'fetchMouthConfig\|updateMouthConfig' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **TypeScript 编译通过**：0 错误
  Test: manual:bash -c "cd apps/api && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c 'error TS' | grep -q '^0$'"

- [x] **Workspace CI 通过**
  Test: manual:bash -c "gh run list --repo perfectuser21/cecelia --branch cp-03031609-mouth-ui-unify --limit 1 --json conclusion | grep -q success"
