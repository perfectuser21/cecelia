# PRD: 重启恢复集成测试套件 + 回归契约

## 背景
为 Brain 重启恢复路径建立完整的集成测试套件，覆盖关键场景，并在 regression-contract.yaml 中登记回归契约。

## 目标
- 新增 syncOrphanTasksOnStartup 集成测试：OOM 孤儿场景（场景2）和并发幂等保护（场景3）
- 在 packages/quality/regression-contract.yaml 新增 B-STARTUP-REQUEUE-001 条目

## 成功标准
- executor-startup-sync.test.js 新增场景2（OOM）和场景3（并发幂等）测试
- packages/quality/regression-contract.yaml 包含 B-STARTUP-REQUEUE-001 条目（含 expires 字段）
- CI brain-ci.yml L1+L3 通过
