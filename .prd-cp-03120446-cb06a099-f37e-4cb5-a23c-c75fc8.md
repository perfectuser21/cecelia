---
id: prd-migration-learnings-quality-score
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

# PRD — migration：historical_learnings 新增 quality_score + source_type 字段

## 背景

知识过滤器 Initiative PR1：为 learnings 表（在代码中以 historical_learnings 形式呈现）添加质量评分字段，支持未来基于质量的 learning 过滤与排序。

## 目标

- 新增 `quality_score INTEGER DEFAULT NULL`（0-100 评分）
- 新增 `source_type VARCHAR(50)`（枚举：test_run/real_insight/unknown）
- 建立 index on quality_score

## 验收标准

- migration 文件（152）正确新增两字段
- `selfcheck.js` EXPECTED_SCHEMA_VERSION 更新到 '152'
- 单测验证字段存在性（不依赖 PostgreSQL，用 grep 验证 SQL 文件）

## 成功标准

- [ ] migration 152 SQL 文件包含 quality_score 和 source_type 字段定义
- [ ] selfcheck.js EXPECTED_SCHEMA_VERSION = '152'
- [ ] 单测可以通过
