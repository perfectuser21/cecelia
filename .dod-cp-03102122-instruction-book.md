---
id: dod-instruction-book
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
  - 1.1.0: 移除 D6/D7（06-code.md 改动单独处理），修正 Test 字段格式
---

# DoD：Instruction Book 体系

## 验收清单

- [x] D1: `docs/instruction-book/README.md` 存在，描述四层文档体系和 instruction book 定位
  Test:
  manual:bash -c "grep -q 'instruction-book' docs/instruction-book/README.md"

- [x] D2: `docs/instruction-book/skills/` 目录存在
  Test:
  manual:bash -c "test -d docs/instruction-book/skills"

- [x] D3: `docs/instruction-book/features/` 目录存在
  Test:
  manual:bash -c "test -d docs/instruction-book/features"

- [x] D4: 至少 3 个 skill entry 存在（dev.md, arch-review.md, brain-register.md）
  Test:
  manual:bash -c "test -f docs/instruction-book/skills/dev.md && test -f docs/instruction-book/skills/arch-review.md && test -f docs/instruction-book/skills/brain-register.md"

- [x] D5: 至少 1 个 feature entry 存在（recurring-tasks.md）
  Test:
  manual:bash -c "test -f docs/instruction-book/features/recurring-tasks.md"

- [x] D6: instruction entry 格式包含 What / Trigger / How to use / Output 四个标准章节
  Test:
  manual:bash -c "grep -q '## What' docs/instruction-book/skills/dev.md && grep -q '## Trigger' docs/instruction-book/skills/dev.md && grep -q '## How to use' docs/instruction-book/skills/dev.md && grep -q '## Output' docs/instruction-book/skills/dev.md"
