---
id: task-cp-03211111-fix-task-type-check
type: task-card
branch: cp-03211111-fix-task-type-check
created: 2026-03-21
---

# Task Card: 修复 tasks 表 CHECK 约束 + 清理无效队列任务

## 需求（What & Why）
**功能描述**: tasks 表的 task_type CHECK 约束缺少 `pipeline_rescue` 和 `content_publish`，导致 decomp-checker Check D 创建任务时报错。同时队列里有空 description 的僵尸任务，pre-flight 拒绝派发。
**背景**: pipeline_rescue 和 content_publish 已在 executor.js/task-router.js 中注册，但 DB 约束未同步更新。
**不做什么**: 不修改业务逻辑代码，只修复 DB 约束和清理数据。

## 成功标准

1. [ARTIFACT] 新 migration 文件 168_add_pipeline_rescue_content_publish_task_types.sql 存在
2. [BEHAVIOR] CHECK 约束包含 pipeline_rescue 和 content_publish
3. [BEHAVIOR] 空 description 的 queued 任务被清理（标记为 cancelled 或删除）
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] migration 文件 168 存在且包含 DROP + ADD CONSTRAINT
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/168_add_pipeline_rescue_content_publish_task_types.sql','utf8');if(!c.includes('pipeline_rescue'))process.exit(1);if(!c.includes('content_publish'))process.exit(1)"

- [x] [BEHAVIOR] migration 保留所有现有类型并新增 pipeline_rescue 和 content_publish
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/168_add_pipeline_rescue_content_publish_task_types.sql','utf8');const types=['dev','review','scope_plan','project_plan','pipeline_rescue','content_publish'];for(const t of types){if(!c.includes(t)){console.error('缺少:'+t);process.exit(1)}}"

- [x] [BEHAVIOR] migration 包含清理空 description queued 任务的语句
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/168_add_pipeline_rescue_content_publish_task_types.sql','utf8');if(!c.includes('description'))process.exit(1);if(!c.includes('queued'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"

## 实现方案（Stage 2 探索后填写）
**要改的文件**: packages/brain/migrations/168_add_pipeline_rescue_content_publish_task_types.sql
**Scope 锚定**: 仅数据库 migration，不涉及应用代码
