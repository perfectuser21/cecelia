---
id: dod-ci-runner-routing
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

# DoD: CI Runner 路由切换至自托管

## 验收清单

- [x] L1 所有 job 使用 `[self-hosted, Linux, hk-vps]`（共 11 个）
  Test: manual:bash -c "grep -c 'self-hosted, Linux, hk-vps' .github/workflows/ci-l1-process.yml"

- [x] L2 所有 job 使用 `[self-hosted, Linux, hk-vps]`（共 5 个）
  Test: manual:bash -c "grep -c 'self-hosted, Linux, hk-vps' .github/workflows/ci-l2-consistency.yml"

- [x] L3 brain-unit 保留 `ubuntu-latest`（GitHub-hosted）
  Test: manual:bash -c "grep -A3 'Brain Unit Tests' .github/workflows/ci-l3-code.yml | grep -c 'ubuntu-latest'"

- [x] L3 其余 6 个 job 使用 `[self-hosted, Linux, hk-vps]`
  Test: manual:bash -c "grep -c 'self-hosted, Linux, hk-vps' .github/workflows/ci-l3-code.yml"

- [x] L4 brain-integration 使用 `[self-hosted, macOS, ARM64, us-mac-m4]`
  Test: manual:bash -c "grep -c 'self-hosted, macOS, ARM64, us-mac-m4' .github/workflows/ci-l4-runtime.yml"

- [x] L4 其余 job 使用 `[self-hosted, Linux, hk-vps]`
  Test: manual:bash -c "grep -c 'self-hosted, Linux, hk-vps' .github/workflows/ci-l4-runtime.yml"

- [x] 无 `macos-latest` 残留
  Test: manual:bash -c "grep -rL 'macos-latest' .github/workflows/ci-l4-runtime.yml && echo ok"
