---
id: task-cp-04111014-pipeline-to-zenithjoy
type: task-card
branch: cp-04111014-pipeline-to-zenithjoy
created: 2026-04-11
---

# Task Card: 消除跨库路径 + 新增 LLM service API + 废弃旧触发端点

## 需求（What & Why）
**功能描述**: 
1. 修复 content-pipeline-executors.js 中硬编码的 ../../../../zenithjoy/content-output 跨库路径
2. 新增 POST /api/brain/llm/call 端点，供 zenithjoy 调用 LLM
3. 旧触发端点（e2e-trigger/batch-e2e-trigger/trigger-topics）加 Deprecation 响应头

**背景**: cecelia = 管家（被动执行层），zenithjoy = 公司（业务主体）。当前架构问题：跨库路径耦合、缺 LLM HTTP API、旧端点未标记废弃。

**不做什么**: 不删除旧端点（仅加响应头）、不修改数据库 schema、不影响现有功能逻辑

## 成功标准
> [ARTIFACT] content-pipeline-executors.js 无 zenithjoy 路径字符串
> [BEHAVIOR] POST /api/brain/llm/call 可正常调用
> [BEHAVIOR] 旧触发端点响应头含 Deprecation: true

## 验收条件（DoD）

- [x] [ARTIFACT] content-pipeline-executors.js 不含 zenithjoy 跨库路径
  Test: manual:grep -n "zenithjoy" packages/brain/src/content-pipeline-executors.js && echo "FAIL" || echo "PASS"

- [x] [ARTIFACT] packages/brain/src/routes/llm-service.js 文件存在
  Test: manual:ls packages/brain/src/routes/llm-service.js

- [x] [BEHAVIOR] /api/brain/llm 路由已在 routes.js 中挂载
  Test: manual:grep -n "llm" packages/brain/src/routes.js

- [x] [BEHAVIOR] 三个旧端点均加了 Deprecation 响应头
  Test: manual:grep -n "Deprecation" packages/brain/src/routes/content-pipeline.js | wc -l

## 实现方案
**要改的文件**: 
- packages/brain/src/content-pipeline-executors.js（修复路径 + 添加 homedir import）
- packages/brain/src/routes/llm-service.js（新建）
- packages/brain/src/routes.js（挂载 llm-service 路由）
- packages/brain/src/routes/content-pipeline.js（加 Deprecation 响应头）

**受影响函数/API**: 
- OUTPUT_BASE 常量定义
- executeExport 中的 rDir 目录路径
- router.post('/trigger-topics', ...)
- router.post('/e2e-trigger', ...)
- router.post('/batch-e2e-trigger', ...)

**不改什么**: 业务逻辑、数据库查询、LLM 调用实现
