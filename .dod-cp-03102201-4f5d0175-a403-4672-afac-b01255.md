# DoD - feat(brain): 对比报告 Notion 推送端点

## 验收清单

- [x] PRD 文件存在
  Test: manual:bash -c "ls .prd-cp-03102201-4f5d0175-a403-4672-afac-b01255.md | wc -l"

- [x] `pushCompareReportToNotion` 函数在 project-compare.js 中导出
  Test: manual:bash -c "grep -c 'export async function pushCompareReportToNotion' packages/brain/src/project-compare.js"

- [x] `POST /compare/report/push` 路由在 task-projects.js 中注册
  Test: manual:bash -c "grep -c \"compare/report/push'\" packages/brain/src/routes/task-projects.js"

- [x] NOTION_API_KEY 缺失时返回 400 + code NOTION_CONFIG_MISSING
  Test: manual:bash -c "grep -c 'NOTION_CONFIG_MISSING' packages/brain/src/project-compare.js"

- [x] notion_parent_id 缺失时返回 400
  Test: manual:bash -c "grep -c 'NOTION_PARENT_MISSING\\|notion_parent_id' packages/brain/src/project-compare.js"

- [x] destination 非 notion 时路由返回 400
  Test: manual:bash -c "grep -c 'destination' packages/brain/src/routes/task-projects.js"

- [x] 测试文件 project-compare-push.test.js 存在
  Test: manual:bash -c "ls packages/brain/src/__tests__/project-compare-push.test.js | wc -l"

- [x] 测试覆盖至少 5 个用例（正常路径 + 4 个错误路径）
  Test: manual:bash -c "grep -c \"it('\" packages/brain/src/__tests__/project-compare-push.test.js"
