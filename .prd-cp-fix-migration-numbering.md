## PRD - 修复 Migration 编号冲突

**需求来源**: DevGate 代码审查发现 brain/migrations/ 有三组重复编号文件，会导致数据库初始化失败
**功能描述**:
  - 读取所有冲突文件内容，分析 SQL 依赖关系，确定正确执行顺序
  - 重命名所有冲突文件，分配唯一连续编号（062 起）
  - 修复 059_rca_pipeline.sql 和 060_brain_config.sql 中错误的表名 `schema_versions`（应为 `schema_version`）
  - 更新 brain/src/selfcheck.js 的 EXPECTED_SCHEMA_VERSION 到最新编号

**涉及文件**:
  - brain/migrations/057_create_strategies_table.sql（重命名）
  - brain/migrations/057_initiative_orchestration.sql（重命名）
  - brain/migrations/058_pattern_metrics.sql（重命名）
  - brain/migrations/058_task_runs.sql（重命名）
  - brain/migrations/059_rca_pipeline.sql（修复表名，重命名）
  - brain/migrations/060_brain_config.sql（修复表名，重命名）
  - brain/migrations/060_rollback_initiative_orchestration.sql（重命名）
  - brain/migrations/061_learnings_dedup.sql（重命名）
  - brain/src/selfcheck.js（更新 EXPECTED_SCHEMA_VERSION）

**成功标准**:
  - `node scripts/facts-check.mjs` 的 migration_conflicts 和 selfcheck_version_sync 全部 ✓
  - brain/migrations/ 编号连续无重复
  - schema_version 表名一致（无 schema_versions 复数形式）

**非目标**:
  - 不执行 migration（不跑 migrate.js）
  - 不修改 SQL 业务逻辑内容
