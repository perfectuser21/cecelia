---
id: task-cp-04021644-engine-develop-cleanup
type: task-card
branch: cp-04021644-engine-develop-cleanup
created: 2026-04-02
---

# Task Card: bash-guard develop 残留 + CI L2 重复 job 清理

## 需求（What & Why）
**功能描述**: 清理 bash-guard.sh 中 develop 分支残留引用；删除 CI L2 brain-l2 job 中与独立 brain-new-files-check job 重复的内联检查。
**背景**: CTO 架构审查发现的 P1 问题。项目分支策略已改为 cp-* → PR → main，不使用 develop。L2 CI 中 brain-l2 和 brain-new-files-check 做了完全相同的检查。
**不做什么**: 不改其他 hooks，不改 CI L1/L3/L4。

## 成功标准
1. [BEHAVIOR] bash-guard.sh DEPLOY_ALLOW_BRANCH 只含 main
2. [ARTIFACT] CI L2 brain-l2 job 无内联 brain-new-files 检查（由独立 job 覆盖）
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [BEHAVIOR] bash-guard.sh 不含 develop 引用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/bash-guard.sh','utf8');if(c.includes('develop'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] CI L2 brain-l2 无重复的 brain-new-files 内联检查
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l2-consistency.yml','utf8');const m=c.match(/Check new brain src files/g);if(m&&m.length>1)process.exit(1);console.log('OK: '+(m?m.length:0)+' 处引用')"

- [x] [BEHAVIOR] Engine 版本已 bump 且 6 文件同步
  Test: manual:node -e "const {execSync}=require('child_process');execSync('bash ci/scripts/check-version-sync.sh',{cwd:'packages/engine',stdio:'inherit'})"

- [x] [GATE] CI 全部通过
  Test: manual:npm test

## 实现方案
**要改的文件**:
- 修改: packages/engine/hooks/bash-guard.sh（第36行 DEPLOY_ALLOW_BRANCH，第397行提示）
- 修改: .github/workflows/ci-l2-consistency.yml（删除 brain-l2 job 内的第117-175行内联检查）
- Bump: packages/engine/package.json, package-lock.json, VERSION, .hook-core-version, regression-contract.yaml, features/feature-registry.yml

**受影响函数/API**: bash-guard Rule 6 HK 部署
**不改什么**: 独立的 brain-new-files-check job 保留不动
