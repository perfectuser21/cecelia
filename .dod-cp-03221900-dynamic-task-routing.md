# DoD: 动态任务类型路由配置

## D1: migration 172 创建 task_type_configs 表并种入数据

- [ ] 文件 `packages/brain/migrations/172_task_type_configs.sql` 存在且包含建表语句和种子数据
- [BEHAVIOR] Test: `manual: node -e "const c=require('fs').readFileSync('packages/brain/migrations/172_task_type_configs.sql','utf8');if(!c.includes('task_type_configs'))process.exit(1);if(!c.includes('strategy_session'))process.exit(1);if(!c.includes('suggestion_plan'))process.exit(1);console.log('ok')"`

## D2: selfcheck.js EXPECTED_SCHEMA_VERSION 更新到 '172'

- [ ] selfcheck.js 中 EXPECTED_SCHEMA_VERSION = '172'
- [BEHAVIOR] Test: `manual: node -e "const c=require('fs').readFileSync('packages/brain/src/selfcheck.js','utf8');if(!c.includes(\"'172'\"))process.exit(1);console.log('ok')"`

## D3: task-type-config-cache.js 新模块存在并导出所需函数

- [ ] `packages/brain/src/task-type-config-cache.js` 存在，导出 loadCache/getCachedLocation/refreshCache/updateConfig
- [BEHAVIOR] Test: `manual: node -e "const c=require('fs').readFileSync('packages/brain/src/task-type-config-cache.js','utf8');['loadCache','getCachedLocation','refreshCache','updateConfig'].forEach(fn=>{if(!c.includes(fn)){process.exit(1)}});console.log('ok')"`

## D4: executor.js 导入缓存模块并在路由中使用

- [ ] executor.js 导入 task-type-config-cache 并调用 getCachedLocation
- [BEHAVIOR] Test: `manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('task-type-config-cache'))process.exit(1);if(!c.includes('getCachedLocation'))process.exit(1);console.log('ok')"`

## D5: Brain API 包含 GET 和 PUT task-type-configs 端点

- [ ] cecelia-routes.js 包含 GET /task-type-configs 和 PUT /task-type-configs/:task_type
- [BEHAVIOR] Test: `manual: node -e "const c=require('fs').readFileSync('packages/brain/src/cecelia-routes.js','utf8');if(!c.includes('task-type-configs'))process.exit(1);if(!c.includes('router.get'))process.exit(1);if(!c.includes('router.put'))process.exit(1);console.log('ok')"`

## D6: 前端页面 TaskTypeConfigPage 存在并有默认导出

- [ ] `apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx` 存在且包含 export default
- [BEHAVIOR] Test: `manual: node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx','utf8');if(!c.includes('export default'))process.exit(1);if(!c.includes('TaskTypeConfigPage'))process.exit(1);console.log('ok')"`

## D7: 前端路由注册包含 /task-type-configs

- [ ] `apps/api/features/system-hub/index.ts` 包含 path: '/task-type-configs' 和 component: 'TaskTypeConfigPage'
- [BEHAVIOR] Test: `manual: node -e "const c=require('fs').readFileSync('apps/api/features/system-hub/index.ts','utf8');if(!c.includes('/task-type-configs'))process.exit(1);if(!c.includes('TaskTypeConfigPage'))process.exit(1);console.log('ok')"`

## D8: 现有 task-router 合约测试全绿

- [ ] task-router 现有测试不被破坏
- [BEHAVIOR] Test: `contract: packages/brain/src/__tests__/contracts/task-router-contract.test.js`
