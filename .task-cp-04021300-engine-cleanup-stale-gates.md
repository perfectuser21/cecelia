---
id: task-cp-04021300-engine-cleanup-stale-gates
type: task-card
branch: cp-04021300-engine-cleanup-stale-gates
created: 2026-04-02
---

# Task Card: Engine 清理 — 删除旧 Gate 检查 + 僵尸测试

## 需求（What & Why）
**功能描述**: slim-engine-heartbeat 重构后遗留的僵尸代码清理。删除 verify-step.sh 中已删功能的 gate 验证代码、bash-guard.sh 中的死规则、以及测试目录下的僵尸测试文件。
**背景**: PR #1784-#1787 已删除 Planner/Generator/Evaluator subagent、Sprint Contract Gate、LITE/FULL 路径，但 verify-step.sh step1 仍保留 ~150 行对应的 seal 文件检查；bash-guard.sh Rule 5b 检查 spec_review_status/code_review_gate_status（永远不会触发）；3 个测试文件测试已删脚本。
**不做什么**: 不改 step2/step4 验证逻辑、不改 bash-guard.sh 其他规则、不改 devloop-check.sh。

## 成功标准
1. [ARTIFACT] verify-step.sh step1 不再引用任何 .dev-gate-planner / .dev-gate-generator-sprint / .dev-gate-spec / .sprint-contract-state / .dev-gate-lite 文件
2. [ARTIFACT] bash-guard.sh 不再包含 spec_review_status 或 code_review_gate_status 字符串
3. [ARTIFACT] 3 个僵尸测试文件已删除
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] verify-step.sh step2 验证逻辑（PRESERVE/TDD/垃圾清理/版本同步/DoD执行）完整保留
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');['Gate 0a','Gate 0b','Gate 0c','Gate 0d','Gate 2'].forEach(g=>{if(!c.includes(g)){console.error('缺少',g);process.exit(1)}});console.log('step2逻辑完整')"

- [x] [ARTIFACT] verify-step.sh step1 不再检查 seal 文件（.dev-gate-planner/.dev-gate-generator-sprint/.dev-gate-spec/.sprint-contract-state/.dev-gate-lite）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');const banned=['.dev-gate-planner','.dev-gate-generator-sprint','.sprint-contract-state','divergence_count','LITE path'];const found=banned.filter(b=>c.includes(b));if(found.length>0){console.error('仍包含:',found);process.exit(1)};console.log('✅ 旧gate引用已清除')"

- [x] [ARTIFACT] bash-guard.sh 不再包含 spec_review_status 或 code_review_gate_status
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/bash-guard.sh','utf8');if(c.includes('spec_review_status')||c.includes('code_review_gate_status')){process.exit(1)};console.log('✅ Rule 5b已删除')"

- [x] [ARTIFACT] 3 个僵尸测试文件已删除
  Test: manual:node -e "const fs=require('fs');const files=['packages/engine/tests/dev/test-generate-feedback-report.sh','packages/engine/tests/dev/test-upload-feedback.sh','packages/engine/tests/validation-loop/test-anti-cheat.sh'];const existing=files.filter(f=>fs.existsSync(f));if(existing.length>0){console.error('仍存在:',existing);process.exit(1)};console.log('✅ 僵尸测试已删除')"

- [x] [GATE] CI 全部通过
  Test: manual:bash packages/engine/ci/scripts/check-version-sync.sh

## 实现方案（必填 — 探索后补充，不能全空）
**要改的文件**:
- `packages/engine/hooks/verify-step.sh`（删除 verify_step1 中 lines 151-298 的旧 gate 检查）
- `packages/engine/hooks/bash-guard.sh`（删除 Rule 5b lines 324-368）
- `packages/engine/tests/dev/test-generate-feedback-report.sh`（删除）
- `packages/engine/tests/dev/test-upload-feedback.sh`（删除）
- `packages/engine/tests/validation-loop/test-anti-cheat.sh`（删除）
- Engine 5 个版本文件（bump patch）

**受影响函数/API**: `verify_step1()` 函数、bash-guard.sh 规则链
**不改什么**: verify_step2()、verify_step4()、bash-guard.sh 其他规则（1/1b/2a/2b/2c/3/3b/5）、devloop-check.sh、branch-protect.sh
