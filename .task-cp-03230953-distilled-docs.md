# Task Card: 记忆蒸馏文档层 Layer 2

branch: cp-03230953-distilled-docs
brain_task_id: 7ec2c1fc-4058-4e27-a9b4-442eac5ed9ed

## 成功标准

1. distilled_docs 表存在，可写入 4 种 type（SOUL/SELF_MODEL/USER_PROFILE/WORLD_STATE）
2. Brain 启动时自动 seed SOUL 文档（若不存在）
3. buildMemoryContext 返回的 block 在任何模式下都包含 SOUL 内容
4. chat 模式下 block 额外包含 SELF_MODEL + USER_PROFILE + WORLD_STATE
5. selfcheck.js EXPECTED_SCHEMA_VERSION = '175'，migrations/175 存在
6. 所有现有测试继续通过（无回归）

## DoD（完成定义）

### DoD-1: Migration 175
- [ ] `packages/brain/migrations/175_distilled_docs.sql` 存在
- [ ] 包含 CREATE TABLE distilled_docs (type VARCHAR PRIMARY KEY, content TEXT, updated_at TIMESTAMPTZ, version INT, generated_by VARCHAR)
- [ ] 插入 schema_version 记录

### DoD-2: distilled-docs.js
- [ ] `packages/brain/src/distilled-docs.js` 存在
- [ ] 导出 `getDoc(type, pool)` — 从 DB 取文档，缺失返回 null
- [ ] 导出 `seedSoul(pool)` — 若 SOUL 不存在则写入默认性格文本
- [ ] 导出 `refreshSelfModel(pool)` — 从 learnings (top frequency + best_practice + cortex_insight) 生成摘要
- [ ] 导出 `refreshUserProfile(pool)` — 从 user_profile_facts + user_profiles 生成摘要
- [ ] 导出 `refreshWorldState(pool)` — 从活跃 OKR/project/initiative 生成摘要

### DoD-3: buildMemoryContext 注入
- [ ] `memory-retriever.js` 修改：在 block 最前固定注入 SOUL（预算外）
- [ ] SELF_MODEL 在所有模式下也注入（预算外）
- [ ] chat 模式：追加 USER_PROFILE + WORLD_STATE（在 profileSnippet 之后，历史上下文之前）
- [ ] logMeta 包含 distilledDocs 字段

### DoD-4: server.js 集成
- [ ] 启动时调用 `seedSoul()` 确保 SOUL 存在
- [ ] 每 24h cron 调用 `refreshWorldState()`

### DoD-5: 版本更新
- [ ] `selfcheck.js` EXPECTED_SCHEMA_VERSION = '175'
- [ ] `DEFINITION.md` schema_version 175

### DoD-6: 测试
- [ ] `packages/brain/src/__tests__/distilled-docs.test.js` 存在
- [ ] 测试覆盖 getDoc / seedSoul / refreshWorldState 基本路径
- [ ] 所有现有测试继续通过
