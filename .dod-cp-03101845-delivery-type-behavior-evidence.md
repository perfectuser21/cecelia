---
id: dod-delivery-type-behavior-evidence
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD：delivery_type 字段 + PR 行为声明机制

## 验收标准

### P0：数据库 + API

- [x] 新增 migration 140：tasks 表添加 `delivery_type VARCHAR(50) DEFAULT 'code-only'`
  Test: manual:bash -c "ls packages/brain/migrations/140_delivery_type.sql"

- [x] `actions.js` createTask() 支持接收并写入 `delivery_type` 字段
  Test: manual:bash -c "grep -q 'delivery_type' packages/brain/src/actions.js"

- [x] `selfcheck.js` EXPECTED_SCHEMA_VERSION 更新为 140
  Test: manual:bash -c "grep -q '140' packages/brain/src/selfcheck.js"

### P0：PR 行为声明模板

- [x] `/dev Step 8` PR body 模板新增 `SYSTEM BEHAVIOR CHANGE` + `UNIMPLEMENTED` 两段
  Test: manual:bash -c "grep -q 'SYSTEM BEHAVIOR CHANGE' packages/engine/skills/dev/steps/08-pr.md"

### P1：behavior-change CI 检查

- [x] 新增 `packages/engine/scripts/devgate/check-delivery-type.sh`：delivery_type=behavior-change 时检查测试文件存在
  Test: manual:bash -c "ls packages/engine/scripts/devgate/check-delivery-type.sh"

### 测试验收

- [x] Brain unit tests 通过（不涉及 delivery_type 的现有测试不破坏）
  Test: manual:bash -c "cd packages/brain && npm test -- --reporter=verbose 2>&1 | tail -5"
