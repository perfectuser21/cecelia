---
id: dod-dynamic-daily-budget
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

## DoD - feat(brain): 低峰期动态 DAILY_BUDGET

### 验收清单

- [x] `getDailyBudget()` 函数已导出
  Test: manual:bash -c "grep -c 'export function getDailyBudget' packages/brain/src/rumination.js"

- [x] 低峰期（上海时间 00:00-05:59）返回 40
  Test: manual:bash -c "grep -c 'DAILY_BUDGET \* 2' packages/brain/src/rumination.js"

- [x] 正常时段（06:00-23:59）返回 20（DAILY_BUDGET 不变）
  Test: manual:bash -c "grep -c 'getDailyBudget' packages/brain/src/rumination.js"

- [x] `hasBudget()` 使用 `getDailyBudget()` 而非常量
  Test: manual:bash -c "grep -c '_dailyCount < getDailyBudget()' packages/brain/src/rumination.js"

- [x] `getRuminationStatus()` 的 `daily_budget` 字段反映动态值
  Test: manual:bash -c "grep -c 'daily_budget: getDailyBudget()' packages/brain/src/rumination.js"

- [x] 单元测试覆盖低峰期（返回 40）
  Test: manual:bash -c "grep -c 'vi.setSystemTime\|低峰期\|off-peak\|00:00\|shanghaiHour' packages/brain/src/__tests__/rumination.test.js"

- [x] 单元测试覆盖正常时段（返回 20）
  Test: manual:bash -c "grep -c 'getDailyBudget' packages/brain/src/__tests__/rumination.test.js"
