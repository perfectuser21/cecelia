# PRD: L3 CI 性能优化 — 拉满 ubuntu-latest 规格

## 背景
ubuntu-latest 提供 16GB 内存，当前 L3 只用 2 fork x 2GB = 4GB，浪费大量资源。

## 目标
将 vitest fork 数提升到 3，堆大小从 2GB 提升到 3GB，充分利用 ubuntu-latest 硬件。

## 成功标准
- [ ] `vitest.config.js` maxForks = 3
- [ ] `ci-l3-code.yml` NODE_OPTIONS max-old-space-size = 3072
