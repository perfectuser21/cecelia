---
id: task-cp-03231530-964423f6-9ee8-486e-826e-ea54ba
type: task-card
branch: cp-03231530-964423f6-9ee8-486e-826e-ea54ba
created: 2026-03-23
---

# Task Card: 重构 runReflection 降低圈复杂度（12 → ≤ 9）

## 需求（What & Why）

**功能描述**: 对 `packages/brain/src/desire/reflection.js` 中的 `runReflection` 函数进行重构，将圈复杂度从 12 降低到 9 以下（目标：≤ 9，阈值：10）。

**背景**: PR #1432 已将复杂度从 39 降至 12，但仍高于阈值 10。需要进一步提取子函数，将 `runReflection` 主函数的决策点减少到阈值以下。

**不做什么**: 不改变函数的业务逻辑和对外接口；不改变错误处理语义（各 catch 块的降级行为保持不变）。

## 成功标准

1. [ARTIFACT] `runReflection` 函数圈复杂度 ≤ 9（由 complexity-scanner 验证）
2. [BEHAVIOR] 所有现有 reflection 测试通过（reflection.test.js + cortex-reflection-breaker.test.js）
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] `runReflection` 公共接口和返回格式不变
  Test: manual:node -e "import('/Users/administrator/perfect21/cecelia/.claude/worktrees/964423f6-9ee8-486e-826e-ea54ba/packages/brain/src/desire/reflection.js').then(m => { if(typeof m.runReflection !== 'function') process.exit(1); console.log('ok'); })"

- [x] [ARTIFACT] 提取 `_callLLMForReflection(memories, accumulator)` 子函数（包含 prompt 构建 + LLM 调用 + 错误处理）
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/desire/reflection.js','utf8'); if(!c.includes('_callLLMForReflection')) process.exit(1); console.log('ok');"

- [x] [ARTIFACT] 提取 `_persistInsightAndReset(pool, insight, accumulator)` 子函数（包含写入洞察 + 重置 accumulator + 各自的错误处理）
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/desire/reflection.js','utf8'); if(!c.includes('_persistInsightAndReset')) process.exit(1); console.log('ok');"

- [x] [BEHAVIOR] runReflection 函数圈复杂度降至 10 以下
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/desire/reflection.js','utf8'); const fn=c.match(/export async function runReflection[\s\S]*?(?=\n\/\*\*|\nexport|\n$)/)?.[0]||''; const decisions=(fn.match(/\bif\b|\bcatch\b|\bcase\b|\?\./g)||[]).length+1; console.log('CC='+decisions); if(decisions>=10) process.exit(1);"

- [x] [GATE] 所有现有测试通过
  Test: manual:npm test --prefix packages/brain -- --run 2>&1 | tail -5

## 实现方案

**要改的文件**: `packages/brain/src/desire/reflection.js`

**Scope 锚定**:
1. 新增 `_callLLMForReflection(memories, accumulator)` — 构建 prompt + 调用 callLLM + catch 返回 null
2. 新增 `_persistInsightAndReset(pool, insight, accumulator)` — 包含 _writeInsight + _resetAccumulator 各自的 try/catch
3. 修改 `runReflection` — 删除 3 个 try/catch 块，调用上述子函数
