# DoD: 补充 P0 跨集成测试：OKR 拆解端到端 + Tick Full Loop

## 产物验收（ARTIFACT）

- [ ] okr-decomposition-flow 测试文件已创建
      Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/brain/src/__tests__/integration/okr-decomposition-flow.integration.test.js'))process.exit(1);console.log('ok')"
- [ ] tick-full-loop 测试文件已创建
      Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/brain/src/__tests__/integration/tick-full-loop.integration.test.js'))process.exit(1);console.log('ok')"
- [ ] okr 测试包含 recalculate-progress 验证
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/okr-decomposition-flow.integration.test.js','utf8');if(!c.includes('recalculate'))process.exit(1);console.log('ok')"
- [ ] tick 测试包含 depends_on 依赖检查
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/tick-full-loop.integration.test.js','utf8');if(!c.includes('depends_on'))process.exit(1);console.log('ok')"

## 运行时验收（BEHAVIOR）

- [ ] [BEHAVIOR] OKR 集成测试覆盖完整 CRUD 层级链（Vision→Initiative）
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/okr-decomposition-flow.integration.test.js','utf8');if(!c.includes('okr_projects')||!c.includes('cascade')||!c.includes('afterAll'))process.exit(1);console.log('ok')"
- [ ] [BEHAVIOR] Tick Full Loop 使用真实 DB 而非 mock pool
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/tick-full-loop.integration.test.js','utf8');if(c.includes(\"vi.mock('../../db.js'\"))process.exit(1);console.log('ok')"

## 质量验收

- [ ] CI 全部通过
      Test: contract:C2-001
