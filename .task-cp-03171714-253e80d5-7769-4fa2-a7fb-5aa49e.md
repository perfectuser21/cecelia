---
id: task-cp-03171714-253e80d5-7769-4fa2-a7fb-5aa49e
type: task-card
branch: cp-03171714-253e80d5-7769-4fa2-a7fb-5aa49e
created: 2026-03-17
---

# Task Card: 修复 Brain L3 测试失败（startup-sync + blocks）

## 需求（What & Why）

**功能描述**: 修复导致 CI L3 每天挂掉的 2 类测试问题
**背景**:
- PR #1025 修改了孤儿恢复逻辑：`watchdog_retry_count < 2` 时 requeue（不再 fail），但 startup-sync.test.js 仍验证旧的 fail 行为 → 3 个测试失败
- blocks.test.js 是集成测试，直接 fetch `localhost:5221` + 连真实 PostgreSQL，CI 无服务 → 17 个测试失败

**不做什么**: 不修改 executor.js 业务逻辑，只修复测试

## 成功标准

1. [ARTIFACT] startup-sync.test.js 3 个失败用例更新为匹配 requeue 行为（orphans_found/requeued/rebuilt）
2. [ARTIFACT] blocks.test.js 标记为集成测试（skipIf 无 INTEGRATION_TESTS 环境变量时跳过）
3. [GATE] `npm test` 在 packages/brain 中全部通过（或 skip，无 failure）

## 验收条件（DoD）

- [x] [ARTIFACT] startup-sync "orphan failed" 测试改为验证 requeue（status='queued', requeued=1）
  Test: manual:bash -c "grep -c 'requeued' packages/brain/src/__tests__/startup-sync.test.js"

- [x] [ARTIFACT] startup-sync "without run_id" 测试改为验证 requeue（requeued=1）
  Test: manual:bash -c "grep -c 'requeued' packages/brain/src/__tests__/startup-sync.test.js"

- [x] [ARTIFACT] startup-sync "mixed alive and orphan" 测试改为验证 requeue
  Test: manual:bash -c "grep -c 'requeued' packages/brain/src/__tests__/startup-sync.test.js"

- [x] [ARTIFACT] blocks.test.js 加 skipIf 条件
  Test: manual:bash -c "grep -c 'skipIf\|INTEGRATION_TESTS' packages/brain/src/__tests__/blocks.test.js"

- [x] [BEHAVIOR] startup-sync 测试断言孤儿走 requeue 路径（requeued=1），不走 fail 路径
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/startup-sync.test.js','utf8');if(!c.includes('expect(result.requeued).toBe(1)'))process.exit(1)"

- [x] [GATE] blocks.test.js 已标记 skipIf 保证 CI 不直连 localhost:5221
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/blocks.test.js','utf8');if(!c.includes('skipIf(!isIntegration)'))process.exit(1)"

## 实现方案

**要改的文件**:
- `packages/brain/src/__tests__/startup-sync.test.js`（更新3个测试用例）
- `packages/brain/src/__tests__/blocks.test.js`（加集成测试标记）

**Scope 锚定**: 仅修改测试文件，不动生产代码
