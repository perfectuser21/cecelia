---
id: task-cp-03231029-fix-hooks-p0
type: task-card
branch: cp-03231029-fix-hooks-p0
created: 2026-03-23
brain_task_id: 286b14a9-d7c7-4515-85b8-9f9b746497db
---

# Task Card: fix(engine): 修复 verify-step.sh/devloop-check.sh/stop-dev.sh P0/P1 bug

## 需求（What & Why）
**功能描述**: 修复3个P0 + 5个P1 bug，这些 bug 导致 /dev pipeline 无法正常完成
**背景**: 审计发现 verify-step.sh 的 _pass() 写 seal key 名和 stop-dev.sh 期望的不一致，以及其他 macOS 兼容性问题
**不做什么**: 不改流程逻辑，不加新功能，只修 bug

## 成功标准

1. [ARTIFACT] verify-step.sh _pass() 写入的 seal key 与 stop-dev.sh _SEALED_STEPS 期望的 key 完全匹配
2. [BEHAVIOR] /dev pipeline 完成 Stage 4 后不再出现"State Machine 验签缺失"死循环
3. [GATE] CI 全部通过（engine-ci.yml）

## 验收条件（DoD）

- [x] [PRESERVE] verify-step.sh 现有 Gate 0a/0b/0c/0d/1/2 功能不变
  Test: manual:bash -c "bash packages/engine/hooks/verify-step.sh step1 2>&1 | head -5"

- [x] [ARTIFACT] verify-step.sh _pass() 写入正确 seal key：step1→step_1_spec_seal, step2→step_2_code_seal, step4→step_4_ship_seal
  Test: manual:bash -c "grep -q 'step_1_spec_seal\|step_2_code_seal\|step_4_ship_seal' packages/engine/hooks/verify-step.sh && echo OK"

- [x] [ARTIFACT] stop-dev.sh _SEALED_STEPS 与 verify-step.sh seal key 命名一致
  Test: manual:bash -c "grep '_SEALED_STEPS' packages/engine/hooks/stop-dev.sh"

- [x] [ARTIFACT] verify-step.sh 中 develop 分支检测改为直接用 main（此仓库无 develop）
  Test: manual:bash -c "! grep -q 'rev-parse.*verify.*develop' packages/engine/hooks/verify-step.sh && echo OK"

- [x] [ARTIFACT] devloop-check.sh dev_mode_file 相关 grep 前加文件存在检查
  Test: manual:bash -c "grep -q 'if.*-f.*dev_mode_file' packages/engine/lib/devloop-check.sh && echo OK"

- [x] [BEHAVIOR] verify-step.sh step2 完成后 .dev-seal 文件包含 step_2_code_seal 行
  Test: manual:bash -c "tmp=$(mktemp -d) && mkdir -p $tmp/docs/learnings && echo '# L\n\n### 根本原因\n\nx\n\n### 下次预防\n\n- [ ] y' > $tmp/docs/learnings/cp-test.md && bash packages/engine/hooks/verify-step.sh step2 cp-test $tmp 2>/dev/null; grep -q 'step_2_code_seal' $tmp/.dev-seal.cp-test && echo OK || echo SKIP"

- [x] [GATE] 所有现有测试通过
  Test: manual:npm test
