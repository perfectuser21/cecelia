## PRD - Brain 测试 Baseline 归零

**需求来源**: 测试已全部修复（失败数为 0），需要将 CI baseline 归零，恢复严格模式门禁
**功能描述**: 将两个 baseline 文件从旧值重置为 0，让 CI 门禁进入严格模式——任何测试失败都必须修复
**涉及文件**:
- `packages/brain/ci/brain-test-baseline.txt`（当前值 400 → 改为 0）
- `packages/brain/ci/brain-integration-baseline.txt`（当前值 125 → 改为 0）

## 成功标准

- `brain-test-baseline.txt` 内容为 `0`
- `brain-integration-baseline.txt` 内容为 `0`
- CI 通过（brain-ci.yml 的 L3 brain-unit 和 L4 brain-integration 均通过）

**非目标**: 不修改任何测试逻辑，不修改版本号（auto-version 自动处理）

### 异常处理 & 报警规范

N/A - 无 IO 操作，仅修改配置文件
