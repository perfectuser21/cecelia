---
id: task-cp-03161600-longform-creator-step55
type: task-card
branch: cp-03161600-longform-creator-step55
created: 2026-03-16
---

# Task Card: longform-creator Step 5.5 — 写入 zenithjoy.works

## 需求（What & Why）

**功能描述**：在 `longform-creator` skill 的 Step 5（NAS 上传）完成后，插入 Step 5.5，自动将内容元数据注册到 `zenithjoy.works` 数据库表。

**背景**：
- zenithjoy.works 表已存在（PR #64）
- 当前创作流程：创作 → NAS，但不写 DB
- 目标：创作 → NAS → DB（works 表），让 publisher 可以通过 content-id 查库后发布

**不做什么**：不改 publisher 脚本，不改 scraper，不做 UI。

## 成功标准

1. [ARTIFACT] `packages/workflows/skills/longform-creator/SKILL.md` 包含 `Step 5.5`
2. [ARTIFACT] Step 5.5 包含 `INSERT INTO zenithjoy.works` SQL
3. [ARTIFACT] Step 5.5 包含 psql 连接命令
4. [BEHAVIOR] SKILL.md 中 Step 5.5 在 Step 5 和 Step 6 之间

## 验收条件（DoD）

- [x] [ARTIFACT] SKILL.md 包含 Step 5.5 章节
  Test: manual:bash -c "grep -q 'Step 5.5' packages/workflows/skills/longform-creator/SKILL.md"

- [x] [ARTIFACT] Step 5.5 包含 INSERT INTO zenithjoy.works
  Test: manual:bash -c "grep -q 'zenithjoy.works' packages/workflows/skills/longform-creator/SKILL.md"

- [x] [ARTIFACT] Step 5.5 包含 psql 命令
  Test: manual:bash -c "grep -q 'psql' packages/workflows/skills/longform-creator/SKILL.md"

- [x] [BEHAVIOR] Step 5.5 在 Step 5 和 Step 6 之间（顺序正确）
  Test: manual:node -e "const s=require('fs').readFileSync('packages/workflows/skills/longform-creator/SKILL.md','utf8');const i55=s.indexOf('Step 5.5');const i6=s.indexOf('Step 6');if(i55<0||i6<0||i55>i6)process.exit(1)"

- [x] [GATE] 所有 ARTIFACT 验证通过
  Test: manual:node -e "const s=require('fs').readFileSync('packages/workflows/skills/longform-creator/SKILL.md','utf8');['Step 5.5','zenithjoy.works','psql'].forEach(k=>{if(!s.includes(k)){console.error('Missing:',k);process.exit(1)}})"

## 实现方案

**要改的文件**：
- `packages/workflows/skills/longform-creator/SKILL.md`（cecelia repo）

在 Step 5（上传 NAS）和 Step 6（输出摘要）之间插入 Step 5.5，内容包含 psql INSERT 命令。
