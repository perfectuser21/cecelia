---
id: task-cp-03220129-4ff1b29e-a04a-4017-ad2f-cf3902
type: task-card
branch: cp-03220129-4ff1b29e-a04a-4017-ad2f-cf3902
created: 2026-03-22
---

# Task Card: 重构 actions.createTask（圈复杂度 23 → 10）

## 需求（What & Why）

**功能描述**：`packages/brain/src/actions.js` 中的 `createTask` 函数圈复杂度为 23（阈值：10），通过提取子函数降低复杂度，保持外部接口不变。

**重构策略**：
1. 提取 `buildCommonParams(params)` — 集中所有参数默认值处理（消除 ~11 个 `||` 决策）
2. 提取 `buildInsertStatement(commonParams, opts)` — 封装有/无 domain 的两种 SQL 构建逻辑（消除 ~4 个决策）
3. `createTask` 自身保留核心流程：验证 → 去重 → 插入 → 广播

## 成功标准

1. [ARTIFACT] 提取 `buildCommonParams` 函数
2. [ARTIFACT] 提取 `buildInsertStatement` 函数
3. [BEHAVIOR] createTask 功能与重构前等价
4. [GATE] 所有测试通过

## 验收条件（DoD）

- [x] [ARTIFACT] buildCommonParams 函数存在于 actions.js
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/actions.js','utf8');if(!c.includes('function buildCommonParams'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] buildInsertStatement 函数存在于 actions.js
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/actions.js','utf8');if(!c.includes('function buildInsertStatement'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] createTask 仍然被导出
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/actions.js','utf8');if(!c.includes('export {'))process.exit(1);if(!c.includes('createTask'))process.exit(1);console.log('ok')"

- [x] [GATE] packages/brain 所有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -10"
