---
id: task-cp-03240030-8ed930ae-93c9-4de8-868c-589781
type: task-card
branch: cp-03240030-8ed930ae-93c9-4de8-868c-589781
created: 2026-03-24
---

# Task Card: OKR PR10 — 旧表引用最终清理

## 需求（What & Why）

**功能描述**：移除业务代码中对旧 `goals`/`projects` 表的直接读写引用，完成迁移收尾。

**背景**：PR1-PR9 已完成主路径迁移。剩余文件仍有旧表引用（fallback 路径 + 直接写入），需要清理这些 fallback，使所有读写路径完全切到新 OKR 表。旧表保留触发器双写兼容层不变。

**不做什么**：
- 不删除旧 goals/projects 表本身（留触发器）
- 不修改 notion-full-sync.js（已有注释说明该文件 Notion 同步特殊路径，new 表无 notion_id 列，保留旧表）
- 不修改 planner.js:1194 的 repo_path 检查（repo_path 只在旧表存在，是有效约束）

## 成功标准

1. [ARTIFACT] 7 个文件中旧表 fallback 全部移除
2. [BEHAVIOR] 代码不再有 `UPDATE goals`/`UPDATE projects`/`INSERT INTO goals` 的业务路径（Notion sync 例外）
3. [GATE] 所有现有测试通过（含 actions.test.js 更新）

## 验收条件（DoD）

- [ ] [PRESERVE] actions.js createGoal/updateGoal 新 OKR 表路径仍然正常
  Test: tests/actions.test.js createGoal/updateGoal suites

- [ ] [PRESERVE] self-drive.js pause_kr/activate_kr/adjust_priority/update_roadmap 先试新表的逻辑不变
  Test: manual:node -e "const {readFileSync:r}=require('fs');const c=r('packages/brain/src/self-drive.js','utf8');if(!c.includes('okr_projects'))process.exit(1)"

- [ ] [ARTIFACT] self-drive.js 移除 4 个旧表 fallback（UPDATE goals/projects）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/self-drive.js','utf8');if(c.includes(\"UPDATE goals SET status = 'paused'\"))process.exit(1);if(c.includes(\"UPDATE projects SET sequence_order\"))process.exit(1)"

- [ ] [ARTIFACT] actions.js 移除 createGoal fallback to goals + updateGoal fallback to goals
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/actions.js','utf8');if(c.includes('INSERT INTO goals'))process.exit(1);if(c.includes('UPDATE goals SET'))process.exit(1)"

- [ ] [ARTIFACT] project-activator.js 移除 2 个 UPDATE projects fallback
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/project-activator.js','utf8');if(c.includes('UPDATE projects SET status'))process.exit(1)"

- [ ] [ARTIFACT] project-compare.js FROM projects 改为 FROM okr_projects/UNION
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/project-compare.js','utf8');const lines=c.split('\n').filter(l=>l.includes('FROM projects')&&!l.trim().startsWith('//'));if(lines.length>0){console.error(lines.join('\n'));process.exit(1)}"

- [ ] [ARTIFACT] decision-executor.js 移除 INSERT INTO goals 和 UPDATE goals fallback
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/decision-executor.js','utf8');if(c.includes('INSERT INTO goals'))process.exit(1);if(c.includes('UPDATE goals SET status'))process.exit(1)"

- [ ] [ARTIFACT] planner.js:571 FROM projects（initiative 查询）改为 FROM okr_initiatives
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/planner.js','utf8');const m=c.match(/FROM projects i[\\s\\S]{0,200}type.*initiative/);if(m)process.exit(1)"

- [ ] [ARTIFACT] decomposition-checker.js FROM projects 改为 FROM okr_initiatives
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/decomposition-checker.js','utf8');if(c.includes('FROM projects p'))process.exit(1)"

- [ ] [GATE] npm test 通过（actions.test.js 同步更新）
  Test: manual:npm test --prefix packages/brain -- --testPathPattern=actions

## 实现方案

**要改的文件**：
1. `packages/brain/src/self-drive.js` — 移除 4 个 fallback
2. `packages/brain/src/actions.js` — 移除 createGoal/updateGoal fallback to goals
3. `packages/brain/src/project-activator.js` — 移除 2 个 UPDATE projects fallback
4. `packages/brain/src/project-compare.js` — getCompareMetrics FROM projects → okr_projects，generateCompareReport FROM projects → okr_projects UNION
5. `packages/brain/src/decision-executor.js` — 移除 INSERT INTO goals + UPDATE goals fallback
6. `packages/brain/src/planner.js` — generateArchitectureDesignTask 改用 okr_initiatives
7. `packages/brain/src/decomposition-checker.js` — checkReadyKRInitiatives 改用 okr_initiatives
8. `packages/brain/src/__tests__/actions.test.js` — 更新 createGoal orphan 测试 + updateGoal 目标不存在测试（移除 goals mock）

**不修改**：notion-full-sync.js（Notion 路径例外），initiative-closer.js（已迁移），planner.js:1194（repo_path 有效约束）

**Scope 锚定**：packages/brain/src/
