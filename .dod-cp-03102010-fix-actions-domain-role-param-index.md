---
id: dod-fix-actions-domain-role-param-index
version: 1.0.0
created: 2026-03-10
---

# DoD: 修复 actions-domain-role.test.js 参数索引

## 验收清单

### P0：测试修复

- [x] `actions-domain-role.test.js` line 66 `params[params.length - 1]` 改为 `params[params.length - 2]`（取 domain，非 delivery_type）
  Test: manual:bash -c "grep -q 'params.length - 2' packages/brain/src/__tests__/actions-domain-role.test.js"

- [x] `actions-domain-role.test.js` 所有测试通过（无 assertion 断裂）
  Test: manual:bash -c "cd packages/brain && npx vitest run src/__tests__/actions-domain-role.test.js --reporter=verbose 2>&1 | grep -E 'passed|failed'"
