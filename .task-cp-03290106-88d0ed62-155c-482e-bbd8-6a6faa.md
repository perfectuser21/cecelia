---
id: task-cp-03290106-88d0ed62
type: task-card
branch: cp-03290106-88d0ed62-155c-482e-bbd8-6a6faa
created: 2026-03-29
---

# Task Card: 激活内容生成链路（Project#17）

## 需求（What & Why）

**功能描述**: 将 Project#17（"每日≥5条可靠产量调度"）从 planning 激活为 active，链接到对应 KR，并创建每日 recurring task 触发内容生成 → 自动发布流程。

**背景**:
- OKR `4a2c8a08` (ZenithJoy 自媒体自动化运营) 有两个关键 KR：
  - `65b4142d` 内容生成 — AI每天产出≥5条内容（帖子+短文）
  - `4b4d2262` 自动发布 — 每天自动发到≥3个平台
- Project#17 (`093ea455`) 目前是 planning 状态，没有 kr_id 链接，也没有对应 recurring task
- Brain tick 已支持 content-pipeline 调度和 recurring task 触发，但缺少初始化数据

**不做什么**: 不修改 content-pipeline 或 recurring task 引擎代码，只补充 data migration

## 成功标准

1. [ARTIFACT] migration 205 SQL 文件存在
2. [BEHAVIOR] migration SQL 包含 UPDATE Project#17 为 active 且 kr_id 正确
3. [BEHAVIOR] migration SQL 包含 INSERT recurring task（每日内容生成）
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] migration 205 文件存在
  Test: manual:node -e "require('fs').accessSync('packages/brain/migrations/205_activate_content_generation_chain.sql')"

- [x] [BEHAVIOR] migration SQL 包含 UPDATE Project#17 到 active 状态，并设置 kr_id
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/205_activate_content_generation_chain.sql','utf8');if(!c.includes('093ea455-b5cb-410d-8602-43c3eef57e1c')||!c.includes('active'))process.exit(1)"

- [x] [BEHAVIOR] migration SQL 包含 INSERT recurring task，project_id 正确
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/205_activate_content_generation_chain.sql','utf8');if(!c.includes('recurring_tasks')||!c.includes('content-pipeline'))process.exit(1)"

- [x] [BEHAVIOR] migration SQL 链接两个 KR（内容生成 + 自动发布）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/205_activate_content_generation_chain.sql','utf8');if(!c.includes('65b4142d-242b-457d-abfa-c0c38037f1e9')||!c.includes('4b4d2262-b250-4e7b-8044-00d02d2925a3'))process.exit(1)"

- [x] [GATE] schema_version 记录迁移版本
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/205_activate_content_generation_chain.sql','utf8');if(!c.includes('schema_version')||!c.includes('205'))process.exit(1)"

## 实现方案

**要改的文件**:
- `packages/brain/migrations/205_activate_content_generation_chain.sql` — 新增数据 migration

**Scope 锚定**:
- Project#17 ID: `093ea455-b5cb-410d-8602-43c3eef57e1c`
- 内容生成 KR: `65b4142d-242b-457d-abfa-c0c38037f1e9`
- 自动发布 KR: `4b4d2262-b250-4e7b-8044-00d02d2925a3`
- Cron: `0 0 * * *` (UTC 00:00 = 北京时间 08:00)
- 注：schema_version 204 已被并行任务占用，本 migration 使用 205
