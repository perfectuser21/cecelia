# PRD: 修复状态机与 CI DoD 同步漏洞

branch: cp-03231500-fix-dod-state-sync
created: 2026-03-23

## 背景

verify-step.sh Gate 2 跑 DoD Test 通过后未将 `[ ]` 写回 `[x]`，导致状态机
与 CI (check-dod-mapping.cjs) 不同步——状态机放行但 CI 卡住。

## 目标

1. verify-step.sh Gate 2：每条 Test 通过后自动标 `[x]`
2. devloop-check.sh：stage 2 完成后加 DoD 完整性检查（镜像 CI）

## 成功标准

- 本地 verify-step 通过后，task card 全为 `[x]`
- devloop-check.sh 若有残留 `[ ]` 则 blocked（不允许进入 Stage 3）
- CI check-dod-mapping 不再因 `[ ]` 卡住
