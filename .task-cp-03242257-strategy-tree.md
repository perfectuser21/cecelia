---
id: task-cp-03242257-strategy-tree
type: task-card
branch: cp-03242257-strategy-tree
created: 2026-03-24
---

# Task Card: Strategy Tree — OKR全链路可视化 + 进度API

## 需求（What & Why）

**功能描述**:
1. Brain 新端点 `GET /api/brain/strategy-tree`，返回完整 OKR 层级树：Area → Objective → KR → Project → Scope → Initiative → Tasks。每层带 total_tasks / completed_tasks / progress（子层自动 rollup）。
2. 前端页面 `/knowledge/strategy-tree`（StrategyTree.tsx），可折叠树形结构，默认展开到 KR 层，每节点显示进度条+状态色（绿/黄/红），Initiative 节点展开显示 Task 列表（pr_title + learning_summary + status），Task 节点可展开显示 PRD 摘要 + PR 链接。
3. `apps/api/features/knowledge/index.ts` 新增 strategy-tree 路由和导航入口。

**背景**: OKR→KR→Project→Scope→Initiative→Task 完整链路数据已在 DB（7张表），但没有串联 API 和前端全景视图。用户每次对话都要重新解释背景，缺乏持久的项目全景视图。

**不做什么**:
- 不做编辑/修改功能（只读视图）
- 不做 areas 层级以上的处理（areas 是最顶层）

## 成功标准

1. `GET /api/brain/strategy-tree` 返回完整树结构，每节点含 completed_tasks/total_tasks/progress
2. `StrategyTree.tsx` 页面存在且能渲染层级树（Area→Initiative 折叠，默认展开到 KR）
3. Initiative 节点展开后显示 Task 列表
4. `knowledge/index.ts` 注册新路由和导航入口（label: 'Strategy Tree'）
5. Brain server.js 注册 strategy-tree 路由

## 验收条件（DoD）

- [x] [PRESERVE] knowledge/index.ts 现有 5 条 navItem 条目保持不变（dev-log/decisions/designs/diary/map）
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/index.ts','utf8');if(!c.includes('dev-log')||!c.includes('decisions')||!c.includes('designs')||!c.includes('diary'))process.exit(1);console.log('OK')"

- [x] [PRESERVE] Brain server.js 现有 /api/brain/okr 和 /api/brain/knowledge 路由不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/server.js','utf8');if(!c.includes('okr-hierarchy')||!c.includes('knowledge'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] packages/brain/src/routes/strategy-tree.js 存在
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/routes/strategy-tree.js');console.log('OK')"

- [x] [BEHAVIOR] GET /api/brain/strategy-tree 返回的数据包含 areas 字段
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/strategy-tree.js','utf8');if(!c.includes('areas'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] strategy-tree.js 对每层计算 total_tasks / completed_tasks / progress rollup
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/strategy-tree.js','utf8');if(!c.includes('total_tasks')||!c.includes('completed_tasks')||!c.includes('progress'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] apps/api/features/knowledge/pages/StrategyTree.tsx 存在
  Test: manual:node -e "require('fs').accessSync('apps/api/features/knowledge/pages/StrategyTree.tsx');console.log('OK')"

- [x] [BEHAVIOR] StrategyTree.tsx 含进度条和 Initiative Task 展开逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/pages/StrategyTree.tsx','utf8');if(!c.includes('progress')||!c.includes('Initiative'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] knowledge/index.ts 新增 strategy-tree 路由和导航入口
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/index.ts','utf8');if(!c.includes('strategy-tree')||!c.includes('StrategyTree'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] Brain server.js 注册 /api/brain/strategy-tree 路由
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/server.js','utf8');if(!c.includes('strategy-tree'))process.exit(1);console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/routes/strategy-tree.js');require('fs').accessSync('apps/api/features/knowledge/pages/StrategyTree.tsx');console.log('All artifacts OK')"

## 实现方案

**要改的文件**:
- NEW `packages/brain/src/routes/strategy-tree.js` — GET /api/brain/strategy-tree 端点
- `packages/brain/server.js` — 注册路由
- NEW `apps/api/features/knowledge/pages/StrategyTree.tsx` — 前端页面
- `apps/api/features/knowledge/index.ts` — 新增路由 + 导航入口
