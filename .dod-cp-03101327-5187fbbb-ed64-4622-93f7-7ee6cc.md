# DoD - 修复 slot-allocator detectUserSessions headless/headed 误判

## 验收标准

### 功能验收

- [x] `detectUserSessions()` 调用 `listProcessesWithPpid()` 构建父进程 args 映射
  Test: manual:bash -c "grep -c 'listProcessesWithPpid' packages/brain/src/slot-allocator.js"

- [x] 对每个 claude 进程，检查其 PPID 对应进程的 args 是否含 `CECELIA_HEADLESS=true`
  Test: manual:bash -c "grep -c 'CECELIA_HEADLESS' packages/brain/src/slot-allocator.js"

- [x] 含 `CECELIA_HEADLESS=true` 父进程的 claude 进程被归类为 headless
  Test: manual:bash -c "grep -c 'isHeadlessViaPpid' packages/brain/src/slot-allocator.js"

- [x] PPID 不在映射中时降级为原有 `-p` / `--print` 检测（graceful degradation）
  Test: manual:bash -c "grep -c 'parentArgs' packages/brain/src/slot-allocator.js"

- [x] `pidToPpid` 和 `pidToArgs` Map 从 `listProcessesWithPpid()` 返回值构建
  Test: manual:bash -c "grep -c 'pidToPpid\|pidToArgs' packages/brain/src/slot-allocator.js"

### 测试验收

- [x] 单元测试：父进程含 `CECELIA_HEADLESS=true` → 分类为 headless
  Test: manual:bash -c "grep -c 'CECELIA_HEADLESS' packages/brain/src/__tests__/slot-allocator.test.js"

- [x] 单元测试：4 个无头任务运行时 user.mode 为 absent（不误判为 team）
  Test: manual:bash -c "grep -c '4 个\|4个\|PPID' packages/brain/src/__tests__/slot-allocator.test.js"

- [x] 单元测试：父进程 PID 不在进程列表中时安全降级为 headed
  Test: manual:bash -c "grep -c 'mockReturnValueOnce' packages/brain/src/__tests__/slot-allocator.test.js"

- [x] 单元测试：`listProcessesWithPpid()` 抛出异常时降级到 `-p` 检测
  Test: manual:bash -c "grep -c 'mockImplementationOnce\|ps unavailable\|graceful' packages/brain/src/__tests__/slot-allocator.test.js"

- [x] 所有现有测试继续通过（无回归）
  Test: manual:bash -c "grep -c 'describe\b' packages/brain/src/__tests__/slot-allocator.test.js"
