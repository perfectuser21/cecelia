---
id: task-cp-04110952-cp-04110947-unify-publisher-scripts
type: task-card
branch: cp-04110952-cp-04110947-unify-publisher-scripts
created: 2026-04-11
---

# Task Card: 统一 publisher 脚本到 zenithjoy，消除 cecelia 副本

## 需求（What & Why）

**功能描述**: 删除 cecelia `packages/workflows/skills/*/scripts/` 下所有重复的 publisher 脚本，更新各平台 SKILL.md 路径指向 zenithjoy。

**背景**: 存在两套独立 publisher 实现 —— cecelia 版本（旧）和 zenithjoy 版本（新，含 Chrome 146 SSH 隧道修复、统一入口 publish-image.cjs + registry.json + keepalive）。两套互不调用，均为独立运行，导致：1）修复只同步到一边；2）上次微博发布修好后 zenithjoy 又做了一套造成混乱。

**不做什么**: 不改 zenithjoy 的脚本内容；不重写 cecelia SKILL.md 的业务逻辑；不涉及 Brain API 的 publish-jobs 路由。

## 成功标准

> [ARTIFACT] cecelia skills 下 7 个平台的 scripts/*.cjs 全部删除，SKILL.md 路径指向 zenithjoy
> [BEHAVIOR] 调用 SKILL.md 中的命令能正确找到 zenithjoy 脚本（路径可 resolve）

## 验收条件（DoD）

- [x] [ARTIFACT] cecelia 8 个平台 scripts/*.cjs 全部删除
  Test: manual:bash -c "ls /Users/administrator/worktrees/cecelia/cp-04110947-unify-publisher-scripts/packages/workflows/skills/weibo-publisher/scripts/*.cjs 2>&1 | grep -c 'No such' && echo PASS"

- [x] [ARTIFACT] cecelia 8 个平台 scripts/batch-publish-*.sh 全部删除
  Test: manual:bash -c "ls /Users/administrator/worktrees/cecelia/cp-04110947-unify-publisher-scripts/packages/workflows/skills/weibo-publisher/scripts/batch-*.sh 2>&1 | grep -c 'No such' && echo PASS"

- [x] [ARTIFACT] cecelia 各平台 SKILL.md 中无 cecelia node_modules 引用
  Test: manual:bash -c "grep -r 'cecelia/node_modules' /Users/administrator/worktrees/cecelia/cp-04110947-unify-publisher-scripts/packages/workflows/skills/ && echo FAIL || echo PASS"

- [x] [BEHAVIOR] SKILL.md 中的 NODE_PATH 指向 zenithjoy publishers/node_modules（路径存在）
  Test: manual:bash -c "ls /Users/administrator/perfect21/zenithjoy/services/creator/scripts/publishers/node_modules > /dev/null && echo PASS"

- [x] [BEHAVIOR] SKILL.md 中的脚本路径可 resolve（以 weibo 为例）
  Test: manual:bash -c "ls /Users/administrator/perfect21/zenithjoy/services/creator/scripts/publishers/weibo-publisher/publish-weibo-image.cjs && echo PASS"

## 实现方案

**改的文件**:
- `packages/workflows/skills/{8 平台}/scripts/*.cjs` — 全部删除
- `packages/workflows/skills/{8 平台}/scripts/__tests__/*.cjs` — 全部删除（测试了已删的脚本）
- `packages/workflows/skills/{8 平台}/scripts/batch-publish-*.sh` — 全部删除
- `packages/workflows/skills/{7 平台}/SKILL.md` — 更新 NODE_PATH 和脚本路径
- `packages/workflows/skills/shipinhao-publisher/` — 整目录删除（无 SKILL.md，只有脚本）

**不改什么**: zenithjoy 侧脚本内容；cecelia Brain API；N8N workflows
