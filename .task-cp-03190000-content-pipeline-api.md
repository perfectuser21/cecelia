---
id: task-cp-03190000-content-pipeline-api
type: task-card
branch: cp-03190000-content-pipeline-api
created: 2026-03-19
---

# Task Card: Brain Content Pipeline API 路由

## 需求（What & Why）
**功能描述**: 新增 `packages/brain/src/routes/content-pipeline.js`，提供 Dashboard 所需的 3 个 API 端点；在 server.js 注册路由。
**背景**: content-pipeline-orchestrator + tick 集成已就绪，但缺少 HTTP 入口，Dashboard 无法触发/查询 Pipeline。
**不做什么**: 不改 orchestrator，不改 tick，不做权限验证，不做分页以外的高级查询。

## 成功标准
> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [ARTIFACT] packages/brain/src/routes/content-pipeline.js 存在，含 3 个端点
2. [ARTIFACT] server.js 注册 /api/brain/content-types 和 /api/brain/pipelines
3. [BEHAVIOR] GET /api/brain/content-types 返回内容类型数组
4. [BEHAVIOR] POST /api/brain/pipelines 创建 content-pipeline 任务并返回 201
5. [BEHAVIOR] GET /api/brain/pipelines 返回 content-pipeline 任务列表
6. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] content-pipeline.js 文件存在
  Test: manual:node --input-type=commonjs -e "const fs=require('fs');if(!fs.existsSync('packages/brain/src/routes/content-pipeline.js'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] server.js 包含 content-pipeline 路由注册
  Test: manual:node --input-type=commonjs -e "const c=require('fs').readFileSync('packages/brain/server.js','utf8');if(!c.includes('content-pipeline'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] content-pipeline.js 包含 GET / POST 路由
  Test: manual:node --input-type=commonjs -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes(\"router.get('/'\") || !c.includes(\"router.post('/'\"))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] content-pipeline.js 包含 content-types 路由
  Test: manual:node --input-type=commonjs -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes('content-types'))process.exit(1);console.log('OK')"

- [x] [GATE] brain 路由测试通过
  Test: manual:bash -c "cd packages/brain && npm test -- --reporter=verbose 2>&1 | grep -E 'content-pipeline|PASS|FAIL' | tail -8"

## 实现方案
**要改的文件**:
- 新增 packages/brain/src/routes/content-pipeline.js
  - GET /content-types → listContentTypes() 返回数组
  - POST / → 创建 content-pipeline task（keyword+content_type必填）
  - GET / → 查询 content-pipeline tasks
- 修改 packages/brain/server.js：import + app.use 注册两个路径

**Scope 锚定**: 仅新增路由文件 + server.js 2 行改动
