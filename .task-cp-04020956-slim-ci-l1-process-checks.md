# Task Card: 删 CI L1 流程检查（只留分支命名+构建）

> Brain Task: adaaac9b-3a0e-4fb4-8314-b0ce567fd93c
> PR Plan: slim-engine-heartbeat PR4

## 背景

ci-l1-process.yml 当前有大量检查 PRD/DoD/Learning 格式的 job（流程文档，非代码质量）。
slim-engine-heartbeat 计划要精简 CI，只保留真正有价值的检查。

## 成功标准

- ci-l1-process.yml 只保留 branch-naming-check、ci-config-audit、secrets-scan 和 gate job
- 删掉所有流程文档类检查（DoD格式、Learning格式、Instruction Book、RCI、seal等）
- CI 能正常运行，gate job 正确汇总结果

## PRD

### 目标
精简 L1 CI：删除流程文档检查，只保留代码安全和基础合规检查。

### 范围
- 修改：`.github/workflows/ci-l1-process.yml`
- 不改：其他 CI 文件、Engine hooks、Brain 代码

## DoD

- [x] [ARTIFACT] ci-l1-process.yml 只保留 verify-dev-workflow + ci-config-audit + secrets-scan + l1-passed
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l1-process.yml','utf8');const jobs=['verify-dev-workflow','ci-config-audit','secrets-scan','l1-passed'];const has=jobs.every(j=>c.includes(j));const noOld=['dod-check','check-learning','engine-l1','check-prd'].every(j=>!c.includes('name: '+j.replace(/-/g,' ')));if(!has||!noOld)process.exit(1)"
- [x] [BEHAVIOR] L1 gate job 正确汇总 3 个保留 job 的结果
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l1-process.yml','utf8');if(!c.includes('verify-dev-workflow.result')||!c.includes('ci-config-audit.result')||!c.includes('secrets-scan.result'))process.exit(1)"
- [x] [ARTIFACT] 删除了 changes、detect-stage 等不再需要的 job
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l1-process.yml','utf8');const removed=['detect-stage','dod-check','cleanup-check','check-prd','check-learning','quality-meta-tests','required-paths-check','engine-l1','dev-health-check','rci-stale-refs-check','fake-dod-test-check','manual-cmd-whitelist-check','instruction-book-required','frontend-browser-dod-check'];const clean=removed.every(j=>!new RegExp('^ {2}'+j+':','m').test(c));if(!clean)process.exit(1)"
