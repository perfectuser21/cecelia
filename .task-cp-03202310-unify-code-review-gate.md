---
id: task-cp-03202310-unify-code-review-gate
type: task-card
branch: cp-03202310-unify-code-review-gate
created: 2026-03-20
---

# Task Card: 统一 code_review_gate 任务类型和字段名

## 需求（What & Why）
**功能描述**: 修复 Pipeline v2 审计发现的 code_review 任务类型不一致 bug。03-integrate.md 派发 `task_type=code_review`（旧类型），但 Brain 新注册的 Gate 是 `code_review_gate`。需要统一为 `code_review_gate`。
**背景**: Pipeline v2 重构后，Brain 侧已注册 `code_review_gate` 路由，但 Engine 侧的 03-integrate.md 仍在使用旧的 `code_review` 类型名，导致派发的任务无法被正确路由。
**不做什么**: 不修改 Brain 侧代码，只修改 Engine 侧引用。

## 成功标准
1. [ARTIFACT] 03-integrate.md 中 task_type 改为 code_review_gate，字段名统一为 code_review_gate_task_id 和 code_review_gate_status
2. [BEHAVIOR] devloop-check.sh 中 _check_codex_review 调用使用新字段名
3. [ARTIFACT] SKILL.md 流程图中 code_review 引用更新为 code_review_gate
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] 03-integrate.md 使用 code_review_gate 任务类型和字段名
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('code_review_gate_task_id'))process.exit(1)"

- [x] [BEHAVIOR] devloop-check.sh 使用新字段名 code_review_gate_task_id
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('code_review_gate_task_id'))process.exit(1)"

- [x] [ARTIFACT] SKILL.md 中 code_review 引用已更新为 code_review_gate
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(!c.includes('code_review_gate'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"
