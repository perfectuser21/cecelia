# PRD: P0 修复 — brain-integration-baseline 归零 + 暴露测试债修复

## 背景

`brain-integration-baseline.txt` 值为 1，允许 CI L4 带一个集成测试失败合并到 main。
将其归零后，暴露出两个预存的测试问题需要一并修复：

1. `okr-decomposition-flow.integration.test.js`：需要 Brain 服务在 `:5221` 运行，CI 无此服务 → 应跳过
2. `smoke.test.js`：Brain 启动超时（45s）→ 增加超时时间

## 成功标准

- `brain-integration-baseline.txt` 值为 0
- `okr-decomposition-flow.integration.test.js` 在 Brain 不可达时优雅跳过（不报错）
- `smoke.test.js` Brain 启动超时阈值足够（CI 不超时）
- CI L1-L4 全部通过
