# DoD: Scanner 内嵌能力识别规则补充

## 验收条件

- [x] [PRESERVE] 现有 BRAIN_ALWAYS_ACTIVE 能力（如 `dev-workflow`）仍标记为 `active`（优先级高于 stage 规则）
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/capability-scanner.js','utf8'); if(!c.includes('BRAIN_ALWAYS_ACTIVE')) process.exit(1);"

- [x] [PRESERVE] 现有 INFRA_DEPLOYED_CAPABILITIES 能力仍标记为 `active`（infra_deployed 路径不受影响）
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/capability-scanner.js','utf8'); if(!c.includes('INFRA_DEPLOYED_CAPABILITIES')) process.exit(1);"

- [x] [ARTIFACT] `determineStatus` 函数接受第五个参数 `stage`，stage≥3 时 island→dormant
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/capability-scanner.js','utf8'); if(!c.includes('stage >= 3')) process.exit(1);"

- [x] [BEHAVIOR] stage=3 + 无技能活动 + 无表数据的能力 → status=dormant（不是 island）
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/__tests__/capability-scanner.test.js','utf8'); if(!c.includes('stage-based dormant vs island rule')) process.exit(1);"

- [x] [BEHAVIOR] stage=1 + 无技能活动 + 无表数据的能力 → status=island（保持原有行为）
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/__tests__/capability-scanner.test.js','utf8'); if(!c.includes('should mark stage=1 capability with no activity as island')) process.exit(1);"

- [x] [GATE] 新增测试覆盖 stage-based 规则，测试文件包含 3 个新测试用例
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/__tests__/capability-scanner.test.js','utf8'); if(!c.includes('should still mark BRAIN_ALWAYS_ACTIVE stage=3 caps as active')) process.exit(1);"
