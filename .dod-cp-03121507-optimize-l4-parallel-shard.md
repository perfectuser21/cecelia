---
id: dod-optimize-l4-parallel-shard
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

# DoD: 优化 L4 CI — 并行 shard + 删除 Phase 2

## 验收清单

- [x] `brain-integration` job 改为 `strategy.matrix.shard: ["1/3", "2/3", "3/3"]` 并行
  Test: manual:bash -c "grep -c 'matrix' .github/workflows/ci-l4-runtime.yml"

- [x] 串行 `for SHARD in 1/3 2/3 3/3` 循环已删除
  Test: manual:bash -c "grep -c 'for SHARD' .github/workflows/ci-l4-runtime.yml || true; exit 0"

- [x] Phase 2 "Run Brain coverage thresholds" step 已删除
  Test: manual:bash -c "grep -c 'Run Brain coverage thresholds' .github/workflows/ci-l4-runtime.yml || true; exit 0"

- [x] baseline 容忍机制保留（`brain-integration-baseline.txt` 读取逻辑）
  Test: manual:bash -c "grep -c 'brain-integration-baseline' .github/workflows/ci-l4-runtime.yml"

- [x] GoldenPath E2E step 保留
  Test: manual:bash -c "grep -c 'GoldenPath E2E' .github/workflows/ci-l4-runtime.yml"

- [x] `l4-passed` gate job 更新为等待 matrix job（`needs: [changes, brain-integration]`）
  Test: manual:bash -c "grep -c 'brain-integration' .github/workflows/ci-l4-runtime.yml"

- [x] YAML 语法有效
  Test: manual:bash -c "python3 -c \"import yaml; yaml.safe_load(open('.github/workflows/ci-l4-runtime.yml'))\" && echo OK"
