# Task: migrate L3/L4 hk-vps jobs to ubuntu-latest

**Branch**: cp-03171416-migrate-ci-to-ubuntu
**Brain Task**: da7c4864-9e6e-4547-90cc-d016d2fab76f
**Priority**: P0

## 需求

将 CI 中所有运行在 HK VPS self-hosted runner 的 jobs 迁移到 GitHub Actions ubuntu-latest。

根本原因：HK VPS 8GB 内存被 8 个并发 vitest 进程 OOM，导致机器崩溃。

## 成功标准

- 所有 CI workflow 文件（L1/L2/L3/L4）中无 `hk-vps` runner 引用
- `ci-l4-runtime.yml` 中只有 `brain-integration` 保留 `xian-mac-m1`
- CI 正常跑通，全部 Gate Passed，无 OOM 风险

## DoD

### 所有 workflow 文件中 hk-vps 全部迁移
- [x] `ci-l3-code.yml`：6 处 `hk-vps` → `ubuntu-latest`
- [x] `ci-l4-runtime.yml`：2 处（detect-changes + gate-passed）→ `ubuntu-latest`
- [x] `ci-l1-process.yml`：2 处（DoD Verification Gate + Engine L1 Process）→ `ubuntu-latest`
- [x] `ci-l2-consistency.yml`：5 处 → `ubuntu-latest`
- [x] `brain-integration` 保留 `xian-mac-m1`（需要 PostgreSQL + Homebrew）

### [BEHAVIOR] CI 运行时行为验证
- [x] [BEHAVIOR] PR CI 在 ubuntu-latest 上成功执行 L3 Code Gate（无 hk-vps 依赖）
- [x] [BEHAVIOR] PR CI 在 ubuntu-latest 上成功执行 L4 Runtime Gate detect-changes（无 hk-vps 依赖）
- [x] [BEHAVIOR] 全仓库 workflow 文件不含 `hk-vps` runner 标签（grep 结果为 0）

### DoD Tests
- Test: manual: `grep -rc 'hk-vps' .github/workflows/ | grep -v ':0' | wc -l | tr -d ' '` → 0
- Test: manual: `grep -c 'xian-mac-m1' .github/workflows/ci-l4-runtime.yml` → 2
- Test: manual: `grep -c 'ubuntu-latest' .github/workflows/ci-l3-code.yml` → 6
