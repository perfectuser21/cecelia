---
id: task-cp-0416104022-a2-pr-body-prd-link
type: task-card
branch: cp-0416104022-a2-pr-body-prd-link
task_id: 3650166a-03e0-4bc9-a811-2fd15ae31b86
created: 2026-04-16
---

# Task Card: A2 — PR body 模板自动贴 PRD 链接

## 需求
Day 2 Epic A 第二步。A1 上线了 Dashboard PRD viewer（/tasks/:id/prd），但 PR body 里没有指向链接——用户不知道去哪看。
改 03-integrate.md 的 PR 创建模板，所有 /dev PR body 第一段自动贴两行链接。

## 实现方案
- `03-integrate.md` §3.2 的 `gh pr create --body` 模板前插 PRD 链接构造块
- 从 `.dev-mode.${BRANCH}` 读 task_id → 构造 Dashboard URL
- 从 `.task-${BRANCH}.md` 存在性 → 构造 GitHub blob URL
- 不改其他 stage

## 不做
- 不强制非 /dev skill
- 不加 CI lint
- 不迁 Task Card 路径（A3）

## DoD
- [x] [ARTIFACT] 03-integrate.md 含 PRD_GH_LINK + PRD_DASH_LINK 构造逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('PRD_GH_LINK')||!c.includes('PRD_DASH_LINK'))process.exit(1)"
- [x] [ARTIFACT] Engine 14.15.1 六文件同步
  Test: manual:bash -c "v=\$(cat packages/engine/VERSION);[[ \$v == 14.15.1 ]] && grep -q \"\\\"\$v\\\"\" packages/engine/package.json && grep -q \"^\$v\" packages/engine/.hook-core-version && grep -q \"^\$v\" packages/engine/hooks/VERSION"
- [x] [ARTIFACT] feature-registry 14.15.1 changelog
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/feature-registry.yml','utf8');if(!c.includes('14.15.1')||!c.includes('PRD'))process.exit(1)"
