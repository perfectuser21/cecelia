# PRD: /dev pipeline 7项BUG修复

## 背景

全面审查 /dev 4-Stage pipeline skill 文件后，发现7个需修复的问题，覆盖路径错误、缺失模板、门禁不完整、过时注释、分数错误等。

## 修复清单

1. **01-spec.md**: CI镜像脚本路径错误 `scripts/devgate/check-dod-mapping.cjs` → `packages/engine/scripts/devgate/check-dod-mapping.cjs`
2. **01-spec.md**: Task Card 模板缺少 `[PRESERVE]` 类型占位（Step 2.0 gate 会强制要求）
3. **02-code.md**: 2.3.2 CI镜像检查无 `exit 1`（build/precheck/version-sync 失败继续走）
4. **03-integrate.md**: 3.2 注释"CI 通过后派发 code_review"已过时（code_review 已移至 Stage 2）
5. **03-integrate.md**: 3.1.5 `git add -u` 漏掉新建的 `.task-cp-*.md` 和 `.prd-cp-*.md` 文件
6. **04-ship.md**: 4.5 执行质量评分总分写错 `X/10`（实为4维×5=20分，应为 `X/20`）
7. **04-ship.md**: 4.6 `mv .task-${BRANCH}.md .history/` 变量不一致 + `.history/` 目录未确认存在

## 范围

- 全部修改在 `packages/engine/skills/dev/steps/` 目录
- 无代码层改动（verify-step.sh/branch-protect.sh 无需修改）
- Engine 版本需 bump（6个文件同步）

## 成功标准

7项修复全部完成，Stage 1 CI镜像使用正确路径，Stage 2 2.3.2 有 exit 1，Stage 3/4 无过时信息。
