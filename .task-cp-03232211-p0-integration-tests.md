# Task Card: 补充 P0 跨集成测试

- **Task ID**: 2817b37d-af41-4762-900f-024fb3ce416f
- **Branch**: cp-03232211-p0-integration-tests
- **Priority**: P0

## 背景

审核发现 Brain 有 459 个单元测试，但跨模块集成测试严重不足。
最新 OKR 迁移（PR #1461-1464）将 goals/projects 迁移到 objectives/key_results/okr_projects 新表，
但没有端到端集成测试验证新表的 FK 完整性与层级流程。

## 目标

新增 2 个 P0 集成测试文件：

1. **OKR 拆解端到端** — 直连 Brain DB 验证新 OKR 表（objectives/key_results/okr_projects/okr_scopes/okr_initiatives）的创建链、FK 级联、层级树、KR 进度重算
2. **Tick Full Loop** — 真实 DB 环境中验证 `selectNextDispatchableTask` 优先级选择 + 依赖检查 + 状态更新

## 成功标准

- [ ] `packages/brain/src/__tests__/integration/okr-decomposition-flow.integration.test.js` 创建且 vitest 全部通过
- [ ] `packages/brain/src/__tests__/integration/tick-full-loop.integration.test.js` 创建且 vitest 全部通过
- [ ] 两个测试文件使用真实 PostgreSQL（`cecelia` 数据库），afterEach/afterAll 清理测试数据
- [ ] CI 全部通过

## DoD

### OKR 集成测试文件存在
- [ ] okr-decomposition-flow 测试文件已创建
      Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/brain/src/__tests__/integration/okr-decomposition-flow.integration.test.js'))process.exit(1);console.log('ok')"
- [ ] 文件包含 Vision→Objective→KR→Project 创建链测试
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/okr-decomposition-flow.integration.test.js','utf8');if(!c.includes('okr_projects')&&!c.includes('okr-decomposition'))process.exit(1);console.log('ok')"
- [ ] 文件包含 FK 级联删除测试
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/okr-decomposition-flow.integration.test.js','utf8');if(!c.includes('cascade')||!c.includes('DELETE'))process.exit(1);console.log('ok')"
- [ ] 文件包含 recalculate-progress 测试
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/okr-decomposition-flow.integration.test.js','utf8');if(!c.includes('recalculate'))process.exit(1);console.log('ok')"

### Tick Full Loop 测试文件存在
- [ ] tick-full-loop 测试文件已创建
      Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/brain/src/__tests__/integration/tick-full-loop.integration.test.js'))process.exit(1);console.log('ok')"
- [ ] 文件包含真实 DB 连接（不全量 mock pool）
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/tick-full-loop.integration.test.js','utf8');if(c.includes('vi.mock')&&c.includes('db.js'))process.exit(1);console.log('ok')"
- [ ] 文件包含优先级排序测试
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/tick-full-loop.integration.test.js','utf8');if(!c.includes('P0')&&!c.includes('priority'))process.exit(1);console.log('ok')"
- [ ] 文件包含依赖检查测试（depends_on）
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/tick-full-loop.integration.test.js','utf8');if(!c.includes('depends_on'))process.exit(1);console.log('ok')"

### 质量
- [ ] CI 全部通过
      Test: contract:C2-001
