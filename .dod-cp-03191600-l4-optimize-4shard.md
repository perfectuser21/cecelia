# DoD: L4 CI 优化 4 shard + 堆内存 3GB

- [x] [BEHAVIOR] matrix 配置为 4 shard（1/4, 2/4, 3/4, 4/4）
  Test: manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes('\"1/4\"'))process.exit(1)"
- [x] [BEHAVIOR] NODE_OPTIONS 堆内存从 6144 改为 3072
  Test: manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(c.includes('6144'))process.exit(1);if(!c.includes('3072'))process.exit(1)"
- [x] [BEHAVIOR] 端口计算公式使用 * 4（支持 4 shard 并发）
  Test: manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');const m=c.match(/\\* 4/g);if(!m||m.length<2)process.exit(1)"
- [x] [BEHAVIOR] GoldenPath E2E 条件为 shard '1/4'
  Test: manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes(\"'1/4'\"))process.exit(1)"
- [x] [BEHAVIOR] baseline 按 4 shard 分配
  Test: manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes('/ 4'))process.exit(1)"
