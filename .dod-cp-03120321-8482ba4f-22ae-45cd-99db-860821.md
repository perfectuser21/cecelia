## DoD - liveness 失败改 requeue

### 验收清单

- [x] executor.js probeTaskLiveness：DEAD 判定后调用 requeueTask() 而非直接 updateTaskStatus(failed)
  Test: manual:bash -c "grep -c 'requeueTask(task.id' packages/brain/src/executor.js"

- [x] requeue 时 reason 为 liveness_dead，evidence 包含 liveness 诊断信息
  Test: manual:bash -c "grep -c 'liveness_dead' packages/brain/src/executor.js"

- [x] actions 数组记录 liveness_dead_requeue（不再是 liveness_auto_fail）
  Test: manual:bash -c "grep -c 'liveness_dead_requeue' packages/brain/src/executor.js"

- [x] 超过 QUARANTINE_AFTER_KILLS 后走隔离逻辑（requeueTask 内部已有，无需额外代码）
  Test: manual:bash -c "grep -c 'QUARANTINE_AFTER_KILLS' packages/brain/src/executor.js"

- [x] 单测：DEAD 探针 → 验证任务被 requeue 而非 fail（liveness-probe.test.js 更新）
  Test: manual:bash -c "grep -c 'liveness_dead_requeue' packages/brain/src/__tests__/liveness-probe.test.js"
