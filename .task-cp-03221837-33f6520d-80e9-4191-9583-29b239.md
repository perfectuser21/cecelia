# Task Card — Pipeline Rescue: 拆分 runPerception 降低圈复杂度

## 成功标准

- `runPerception` 已被拆分为 14 个独立 async 子采集函数
- `packages/brain/src/desire/perception.js` 圈复杂度 ≤ 10
- `npm test` 全部通过

## DoD

- [x] `perception.js` 中 `runPerception` 已被拆分为 14 个独立子函数 [BEHAVIOR] Test: `manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/desire/perception.js','utf8');const matches=c.match(/async function collect[A-Za-z]+/g)||[];if(matches.length<10){console.error('子函数不足，找到'+matches.length+'个');process.exit(1)}console.log('OK: 找到'+matches.length+'个子采集函数')"`
- [x] 主函数通过 `Promise.all` 并行执行 [BEHAVIOR] Test: `manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/desire/perception.js','utf8');if(!c.includes('Promise.all')){console.error('未找到Promise.all');process.exit(1)}console.log('OK: Promise.all 存在')"`
- [x] `npm test` 通过 Test: `tests/brain/perception.test.js`
