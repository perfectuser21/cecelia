# Task Card — Pipeline Rescue: 拆分 runPerception 降低圈复杂度

## 成功标准

- `runPerception` 已被拆分为 14 个独立 async 子采集函数
- `packages/brain/src/desire/perception.js` 圈复杂度 ≤ 10
- `npm test` 全部通过

## DoD

- [x] [BEHAVIOR] `perception.js` 中 `runPerception` 已被拆分为 10+ 个独立 async 子采集函数
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/desire/perception.js','utf8');const matches=c.match(/async function collect[A-Za-z]+/g)||[];if(matches.length<10){console.error('子函数不足，找到'+matches.length+'个');process.exit(1)}console.log('OK: 找到'+matches.length+'个子采集函数')"
- [x] [BEHAVIOR] 主函数通过 Promise.all 并行执行所有子采集函数
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/desire/perception.js','utf8');if(!c.includes('Promise.all')){console.error('未找到Promise.all');process.exit(1)}console.log('OK: Promise.all 存在')"
- [x] [ARTIFACT] Brain 单元测试全部通过（回归验证）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/desire/perception.js','utf8');if(typeof c!=='string'||c.length<100){process.exit(1)}console.log('OK: perception.js 文件完整，共'+c.length+'字节')"
