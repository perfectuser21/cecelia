# PRD: 三阶段 seal 对齐检查 — Planner→Contract→Generator 强制链路

## 任务描述

在 devloop-check.sh 加三道 seal 检查：
1. Sprint Contract 开始前：.dev-gate-planner.{BRANCH} 必须存在
2. Generator 开始前：.dev-gate-spec.{BRANCH} 必须存在（已由条件 1.5 覆盖）
3. Stage 3 开始前：.dev-gate-generator.{BRANCH} 必须存在

同时在 Planner subagent 和 Generator subagent 的 step 文件里加写 seal 的指令。

缺任何一个 → exit 2 打回，不到 CI 就拦住。

## 成功标准

- devloop-check.sh 新增 .dev-gate-planner.{BRANCH} 存在性检查（Sprint Contract 前）
- devloop-check.sh 新增 .dev-gate-generator.{BRANCH} 存在性检查（Stage 3 前）
- planner-prompt.md 包含写 .dev-gate-planner seal 文件的指令
- 02-code.md Generator subagent prompt 包含写 .dev-gate-generator seal 文件的指令
