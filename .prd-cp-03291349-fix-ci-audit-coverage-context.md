# PRD: 修复3个P0地基缺口 — CI audit + coverage + /context SQL

## 背景
/repo-audit 2026-03-29 得分 72/120，发现3个CI虚假绿 + /context SQL bug

## 目标
1. CI L3 加 npm audit --audit-level=high（根目录）
2. coverage-delta job 阈值违反时真正失败（移除无条件 continue-on-error）
3. /api/brain/context 修复 SQL bug（decisions 表无 title/category 列）

## 成功标准
- L3 CI 包含 npm audit 步骤
- coverage threshold 违反时 CI 失败
- GET /api/brain/context 返回 success: true
