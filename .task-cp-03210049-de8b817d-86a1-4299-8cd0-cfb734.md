---
id: task-cp-03210049-de8b817d-86a1-4299-8cd0-cfb734
type: task-card
branch: cp-03210049-de8b817d-86a1-4299-8cd0-cfb734
created: 2026-03-21
---

# Task Card: projects 表 current_phase 字段 + migration

## 需求（What & Why）

**功能描述**：
1. 新建 migration 169：为 projects 表的 current_phase 列修正 CHECK 约束（057 migration 已添加列但含 `plan` 值，PRD 要求只允许 now/next/later/null），并根据 sequence_order 回填现有 active projects 的 current_phase
2. SelfDrive `update_roadmap` action 写入 current_phase 字段（已在 self-drive.js 中实现，无需改动）

**背景**：
- migration 057 已添加 `current_phase TEXT DEFAULT NULL`，但 CHECK 约束包含 `plan`（不符合 PRD 要求）
- 17 个 active projects 的 current_phase 全为 NULL，sequence_order 也全为 NULL
- `update_roadmap` action 已正确实现（validPhases: ['now','next','later']）

**不做什么**：不改动 self-drive.js（已正确），不改动 Dashboard 前端，不改动任务调度逻辑

## 成功标准

1. [ARTIFACT] migration 169 文件存在并可执行
2. [BEHAVIOR] 数据库中 projects_phase_check 约束只允许 now/next/later（不含 plan）
3. [BEHAVIOR] active projects 根据 sequence_order 回填 current_phase（NULL sequence_order 的不设值）
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] `packages/brain/migrations/169_projects_current_phase.sql` 文件存在
  Test: manual:node -e "require('fs').accessSync('packages/brain/migrations/169_projects_current_phase.sql')"

- [x] [BEHAVIOR] migration 执行后 CHECK 约束只含 now/next/later（无 plan）
  Test: manual:psql cecelia -c "SELECT pg_get_constraintdef(oid) FROM pg_constraint WHERE conname='projects_phase_check'" 2>/dev/null | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');if(d.includes('plan'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] schema_version 表包含版本 169
  Test: manual:psql cecelia -t -c "SELECT version FROM schema_version WHERE version='169'" 2>/dev/null | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8').trim();if(d!=='169')process.exit(1);console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -10"

## 实现方案（探索后填写）

**要改的文件**：
- `packages/brain/migrations/169_projects_current_phase.sql`（新建）

**Scope 锚定**：
- 只修改 CHECK 约束（DROP + ADD），不改列类型
- 回填逻辑：sequence_order=1 → 'now'，=2 → 'next'，>=3 → 'later'，NULL → 跳过
- migration 幂等（使用 IF EXISTS / ON CONFLICT DO NOTHING）
