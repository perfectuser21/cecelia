# Task: 重组 TaskTypeConfigPage — ABCD 类别框架

**Task ID**: 5f8b10ef-9378-4090-8ec3-80c24a60e2ce
**Branch**: cp-03222041-abcd-task-type-page
**Type**: fix

## 目标

将 TaskTypeConfigPage 按 ABCD 四类框架重组：

- **A类**：锁机器（美国M4）+ 锁模型（Claude Code），目前只有 `dev`
- **B类**：锁机器（美国M4），模型不锁（Claude Code 或 Codex 均可），需本机代码/DB上下文
- **C类**：用 Codex，不锁机器（美国M4 或 西安M4 均可），动态任务可编辑
- **D类**：香港VPS，纯脚本/轻量，无需大模型

## 成功标准

- [x] [ARTIFACT] D1: 页面包含 A类/B类/C类/D类 四个分组标签
  Test: manual: node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx','utf8');if(!c.includes('A类')||!c.includes('B类')||!c.includes('C类')||!c.includes('D类'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] D2: A类只显示 dev，且有"锁机器+锁模型"说明
  Test: manual: node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx','utf8');if(!c.includes('锁机器') || !c.includes('锁模型'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] D3: C类动态任务（suggestion_plan等）仍可编辑，B类/A类/D类固定只读
  Test: manual: node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx','utf8');if(!c.includes('suggestion_plan')||!c.includes('strategy_session'))process.exit(1);console.log('ok')"
