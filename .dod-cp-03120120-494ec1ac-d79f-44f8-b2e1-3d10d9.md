---
id: dod-cortex-insight-autocreate-task
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

## DoD - Cortex insight → action 自动闭合机制

### 验收清单

- [x] migration 150 创建，tasks 表新增 source_learning_id 列
  Test: manual:bash -c "grep -c 'source_learning_id' packages/brain/migrations/150_tasks_source_learning_id.sql"

- [x] insight-action-bridge.js 创建，包含 checkAndCreateTask 导出函数
  Test: manual:bash -c "grep -c 'export.*checkAndCreateTask' packages/brain/src/insight-action-bridge.js"

- [x] cortex.js recordLearnings 在 INSERT 后 fire-and-forget 调用 bridge
  Test: manual:bash -c "grep -c 'checkAndCreateTask' packages/brain/src/cortex.js"

- [x] 关键词检测逻辑：内容含代码修复信号才触发创建
  Test: manual:bash -c "grep -c 'CODE_FIX_SIGNALS' packages/brain/src/insight-action-bridge.js"

- [x] 去重逻辑：source_learning_id 已有 task 则不重复创建
  Test: manual:bash -c "grep -c 'source_learning_id' packages/brain/src/insight-action-bridge.js"

- [x] applied=true 标记：task 创建成功后更新 learnings 表
  Test: manual:bash -c "grep -c 'applied = true' packages/brain/src/insight-action-bridge.js"

- [x] selfcheck.js EXPECTED_SCHEMA_VERSION 更新为 '150'
  Test: manual:bash -c "grep -c \"'150'\" packages/brain/src/selfcheck.js"

- [x] DEFINITION.md schema_version 两处更新为 150
  Test: manual:bash -c "grep -c 'Schema 版本: 150' DEFINITION.md"

- [x] insight-action-bridge.test.js 覆盖主要链路（有信号创建task / 无信号跳过 / 去重跳过）
  Test: manual:bash -c "grep -c 'describe\|it(' packages/brain/src/__tests__/insight-action-bridge.test.js"
