---
id: task-cp-03161231-remove-multi-feature-section
type: task-card
branch: cp-03161231-remove-multi-feature-section
created: 2026-03-16
---

# Task Card: 删除 /dev SKILL.md 中废弃的"多 Feature 支持"章节

## 需求（What & Why）
**功能描述**: 从 `packages/engine/skills/dev/SKILL.md` 中删除已废弃的 `## 多 Feature 支持（可选）` 章节，并在删除位置添加一行说明注释。

**背景**: 多 PR 编排功能已移到 Initiative 层（/architect skill）。/dev 层不再负责多 feature 状态机和 `/dev continue` 命令。保留此章节会造成误导，开发者可能错误地以为 /dev 支持 multi-feature 状态机。

**不做什么**:
- 不修改 `## 完成度检查` 及其后的内容
- 不修改其他 steps/*.md 文件
- 不更改 Engine 的任何逻辑代码

## 成功标准

1. [ARTIFACT] `packages/engine/skills/dev/SKILL.md` 中 `## 多 Feature 支持（可选）` 章节已删除（第 465-539 行）
2. [ARTIFACT] 删除位置有注释行 `> 多 PR 编排在 Initiative 层，参考 /architect skill。`
3. [ARTIFACT] Engine 版本已 bump（7 个文件同步更新）
4. [ARTIFACT] `packages/engine/features/feature-registry.yml` 新增 changelog 条目
5. [GATE] CI 全部通过

## 验收条件（DoD）

- [ ] [ARTIFACT] SKILL.md 中不含 `## 多 Feature 支持（可选）` 标题
  Test: manual:! grep -q '## 多 Feature 支持' packages/engine/skills/dev/SKILL.md

- [ ] [ARTIFACT] SKILL.md 中不含 `/dev continue` 命令描述
  Test: manual:! grep -q '/dev continue' packages/engine/skills/dev/SKILL.md

- [ ] [ARTIFACT] SKILL.md 中不含 `multi-feature-state.local.md` 引用
  Test: manual:! grep -q 'multi-feature-state' packages/engine/skills/dev/SKILL.md

- [ ] [ARTIFACT] 删除位置存在指向 /architect 的注释
  Test: manual:grep -q '/architect skill' packages/engine/skills/dev/SKILL.md

- [ ] [ARTIFACT] Engine 版本 bump 已覆盖 7 个文件
  Test: manual:grep -c 'version' packages/engine/package.json

- [ ] [ARTIFACT] feature-registry.yml 包含本次变更的 changelog 条目
  Test: manual:grep -q 'remove-multi-feature' packages/engine/features/feature-registry.yml

- [ ] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/engine && node scripts/devgate/check-dod-mapping.cjs 2>&1 | tail -5"

## 实现方案（Step 2 探索后填写）
**要改的文件**:
- `packages/engine/skills/dev/SKILL.md`（删除第 465-539 行，加注释）
- `packages/engine/package.json`（版本 bump）
- `packages/engine/package-lock.json`（版本 bump）
- `packages/engine/features/feature-registry.yml`（changelog 条目）
- 其他版本同步文件（待探索确认）

**Scope 锚定**: 仅 Engine 包，不涉及 Brain 或 Workspace
