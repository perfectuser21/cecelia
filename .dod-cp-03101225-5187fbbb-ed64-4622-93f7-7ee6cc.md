---
id: dod-fix-slot-allocator-headless-detection
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD - 修复 slot-allocator detectUserSessions headless 误判

## 验收清单

- [x] slot-allocator.js 导入 listProcessesWithPpid 并新增 buildHeadlessParentPidSet 辅助函数
  Test: manual:bash -c "grep -c 'listProcessesWithPpid' packages/brain/src/slot-allocator.js"

- [x] detectUserSessions() 通过 PPID 检测 CECELIA_HEADLESS=true，将此类进程归类为 headless
  Test: manual:bash -c "grep -c 'headlessParentPids' packages/brain/src/slot-allocator.js"

- [x] 新增单元测试：PPID 检测路径（≥4 个测试用例）
  Test: manual:bash -c "grep -c 'PPID\|CECELIA_HEADLESS' packages/brain/src/__tests__/slot-allocator.test.js"

- [x] 现有 detectUserSessions 测试不受影响（不新增 headed 误判）
  Test: manual:bash -c "grep -c 'describe.*detectUserSessions' packages/brain/src/__tests__/slot-allocator.test.js"

## 成功标准

- headless 任务进程（父进程含 CECELIA_HEADLESS=true）不再被计入 user.used
- 4 个无头任务运行时：user.mode = "absent"（无 headed session 时）
- taskPool.available 恢复正常，dispatchAllowed = true
