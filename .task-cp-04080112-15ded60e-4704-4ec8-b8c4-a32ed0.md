---
id: task-cp-04080112-15ded60e-4704-4ec8-b8c4-a32ed0
type: task-card
branch: cp-04080112-15ded60e-4704-4ec8-b8c4-a32ed0
created: 2026-04-08
---

# Task Card: KR进度采集链路修复 — current_value 回填 & resetAllKrProgress 漏写修复

## 需求（What & Why）
**功能描述**：
1. `resetAllKrProgress()` 与 PR #2017 前的 `runAllVerifiers()` 有同款 bug：只写 `progress`，不写 `key_results.current_value`
2. 无 API 可触发 `resetAllKrProgress` 做 current_value 全量回填（Brain 重启后/紧急修复场景缺失）
3. 验证全量 KR 进度可信度，确认 0% 是真实完成度还是采集失败

**背景**：PR #2017 修复了 `runAllVerifiers` 的 current_value 写入，但 `resetAllKrProgress` 仍遗漏；Brain 01:03 启动使用旧代码，current_value 全为 0 需要立即回填工具。

**不做什么**：不改 verifier SQL 查询逻辑、不改 progress 计算公式、不重启 Brain

## 调查结论
- `progress` 字段：正确（Cecelia KR1=100%, KR2=91%, KR3=87%；ZenithJoy KR1/KR2=100%）
- `current_value` 字段：全为 0（Brain 用旧代码，未写 current_value）；KR5 Dashboard=30（旧值）
- KR3 小程序/KR4 geo SEO：verifier 从未运行（last_checked=NULL），今日首次运行后 = 25%（真实值）
- 机制本身工作正常，缺修复工具

## 成功标准

- [x] [ARTIFACT] `resetAllKrProgress` 修复完成，包含 `current_value = $2` 写入
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/kr-verifier.js','utf8');if(!c.includes('current_value = \$2') || c.indexOf('current_value = \$2') === c.lastIndexOf('current_value = \$2'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] POST /api/brain/okr/backfill-current-values 端点可用，回填所有 KR current_value
  Test: manual:curl -s -X POST localhost:5221/api/brain/okr/backfill-current-values

## 实现方案
**要改的文件**：
- `packages/brain/src/kr-verifier.js` — `resetAllKrProgress()` UPDATE 语句补 `current_value = $2`
- `packages/brain/src/routes/okr-hierarchy.js` — 新增 `POST /backfill-current-values` 端点

**受影响函数/API**：`resetAllKrProgress()`、okr-hierarchy 路由

**不改什么**：`runAllVerifiers()`（已修）、verifier SQL、progress 计算
