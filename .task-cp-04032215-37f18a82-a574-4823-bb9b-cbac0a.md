---
id: task-cp-04032215-37f18a82-a574-4823-bb9b-cbac0a
type: task-card
branch: cp-04032215-37f18a82-a574-4823-bb9b-cbac0a
created: 2026-04-04
---

# Task Card: 重构 memory-retriever.formatItem（复杂度 21 → 10）

## 需求（What & Why）
**功能描述**: 重构 `packages/brain/src/memory-retriever.js` 第393行的 `formatItem` 函数，将圈复杂度从21降至10以下。
**背景**: Brain 代码质量扫描发现 formatItem 因大量 `||` 链和嵌套条件导致复杂度过高，维护困难。
**不做什么**: 不改变函数的外部行为和返回格式，不修改测试文件。

## 成功标准
> [BEHAVIOR] formatItem 行为不变，重构后复杂度降至10以下

## 验收条件（DoD）

- [x] [BEHAVIOR] formatItem 重构后行为与原始完全一致（三种 depth 层级输出正确）
  Test: tests:packages/brain/src/__tests__/memory-retriever.test.js

- [x] [BEHAVIOR] 提取了 buildExtras 和 getPreviewBase 两个辅助函数降低主函数复杂度
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/memory-retriever.js','utf8');if(!c.includes('function _buildExtras'))process.exit(1);if(!c.includes('function _getPreviewBase'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] 修改仅限于 formatItem 函数及其新增的辅助函数
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/memory-retriever.js')"

## 实现方案
**要改的文件**: `packages/brain/src/memory-retriever.js`（第393-429行）
**受影响函数**: `formatItem` → 拆分为 `_buildExtras` + `_getPreviewBase` + 简化的 `formatItem`
**不改什么**: 函数返回格式、测试文件、导出接口
