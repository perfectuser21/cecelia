---
id: task-cp-03241743-27864011-7e54-424d-9f66-441d90
type: task-card
branch: cp-03241743-27864011-7e54-424d-9f66-441d90
created: 2026-03-25
---

# Task Card: 记忆系统 PR8 — 接入 runConversationConsolidator 到 tick

## 需求（What & Why）

**功能描述**: 在 tick.js 中以每 5 分钟频率调用 `runConversationConsolidator()`，使 Brain 在用户对话空闲 30 分钟后自动压缩对话内容写入 `memory_stream`。

**背景**: `conversation-consolidator.js` 已实现完整逻辑（检查空闲条件 → LLM 压缩 → 写入 memory_stream → 防重机制），但尚未接入 tick 调度，因此不会自动运行。

**不做什么**:
- 不修改 `conversation-consolidator.js` 的逻辑
- 不改变 30 分钟空闲阈值（那是 consolidator 内部参数）
- 不添加新的数据库表或 schema

## 成功标准

- tick.js 顶部已 import `runConversationConsolidator` from `./conversation-consolidator.js`
- tick.js 存在 `_lastConversationConsolidatorTime` 状态变量（初始值 0）
- tick.js 存在 `CONVERSATION_CONSOLIDATOR_INTERVAL_MS`（5 分钟，遵循环境变量覆盖模式）
- `executeTick()` 中按 5 分钟间隔调用 `runConversationConsolidator()`，fire-and-forget，错误不阻断
- CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] tick.js 其他所有现有 import 和功能正常，无副作用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('runPipelinePatrol'))process.exit(1);if(!c.includes('runConversationConsolidator'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] tick.js 顶部 import `runConversationConsolidator` from `./conversation-consolidator.js`
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('runConversationConsolidator'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] `_lastConversationConsolidatorTime` 变量存在于 tick.js loop state 区域
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('_lastConversationConsolidatorTime'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] `CONVERSATION_CONSOLIDATOR_INTERVAL_MS` 常量存在（5分钟）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('CONVERSATION_CONSOLIDATOR_INTERVAL_MS'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] executeTick 中存在调用 runConversationConsolidator() 的代码块（含间隔检查和 catch 块）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('runConversationConsolidator()'))process.exit(1);if(!c.includes('_lastConversationConsolidatorTime = Date.now()'))process.exit(1);if(!c.includes('conversation-consolidator'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] tick.js 中 runConversationConsolidator 以 fire-and-forget + catch 错误不阻断模式调用（.catch 错误处理存在）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('runConversationConsolidator().catch'))process.exit(1);console.log('OK')"

- [x] [GATE] tick.js 语法无错（node --check 通过）
  Test: manual:node --check packages/brain/src/tick.js

## 实现方案

**要改的文件**: `packages/brain/src/tick.js`

**Scope 锚定**:
1. 在 import 区域（第 44 行 `runPipelinePatrol` 附近）添加 import
2. 在 loop state 变量区域（第 122 行 `_lastPipelinePatrolTime` 附近）添加 `_lastConversationConsolidatorTime = 0`
3. 在常量区域（第 125 行 `PIPELINE_PATROL_INTERVAL_MS` 附近）添加 `CONVERSATION_CONSOLIDATOR_INTERVAL_MS`
4. 在 `executeTick()` 中 pipeline patrol 代码块之后（约 1551 行）添加调用块
