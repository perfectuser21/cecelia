# DoD: Brain 每日自检反思

## 验收清单

- [ ] daily-reflection-scheduler.js 存在
  - manual: node -e "require('fs').accessSync('packages/brain/src/daily-reflection-scheduler.js')"
- [ ] 导出 triggerDailyReflection 函数
  - manual: node -e "const m=require('fs').readFileSync('packages/brain/src/daily-reflection-scheduler.js','utf8');if(!m.includes('export async function triggerDailyReflection'))process.exit(1)"
- [ ] tick.js 中注册了调用
  - manual: node -e "const m=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!m.includes('triggerDailyReflection'))process.exit(1)"
- [ ] 触发窗口为 04:00 UTC（与 contract-scan 03:00 错开）
  - manual: node -e "const m=require('fs').readFileSync('packages/brain/src/daily-reflection-scheduler.js','utf8');if(!m.includes('DAILY_REFLECTION_HOUR_UTC = 4'))process.exit(1)"
- [ ] 有去重机制（同一天不重复触发）
  - manual: node -e "const m=require('fs').readFileSync('packages/brain/src/daily-reflection-scheduler.js','utf8');if(!m.includes('hasTodayReflection'))process.exit(1)"
- [ ] 分析 dev_execution_logs 最近 7 天数据
  - manual: node -e "const m=require('fs').readFileSync('packages/brain/src/daily-reflection-scheduler.js','utf8');if(!m.includes('dev_execution_logs'))process.exit(1)"
- [ ] 查询未应用的 learnings
  - manual: node -e "const m=require('fs').readFileSync('packages/brain/src/daily-reflection-scheduler.js','utf8');if(!m.includes('getUnappliedLearnings'))process.exit(1)"
- [ ] 反复问题阈值 >= 3 时创建 self_fix 任务
  - manual: node -e "const m=require('fs').readFileSync('packages/brain/src/daily-reflection-scheduler.js','utf8');if(!m.includes('RECURRING_THRESHOLD = 3'))process.exit(1)"
- [ ] [BEHAVIOR] isInReflectionWindow 在 04:00 UTC 返回 true，在其他时间返回 false
  - manual: node -e "const{isInReflectionWindow}=await import('./packages/brain/src/daily-reflection-scheduler.js');const t=new Date('2026-01-01T04:02:00Z');const f=new Date('2026-01-01T03:00:00Z');if(!isInReflectionWindow(t))process.exit(1);if(isInReflectionWindow(f))process.exit(1)"
- [ ] [BEHAVIOR] triggerDailyReflection 在非窗口时间返回 skipped_window=true
  - manual: node -e "const{triggerDailyReflection}=await import('./packages/brain/src/daily-reflection-scheduler.js');const r=await triggerDailyReflection(null,new Date('2026-01-01T10:00:00Z'));if(!r.skipped_window)process.exit(1)"
