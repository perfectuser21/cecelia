---
type: prd
branch: cp-03231029-fix-hooks-p0
brain_task_id: 286b14a9-d7c7-4515-85b8-9f9b746497db
created: 2026-03-23
---

# PRD: fix(engine): 修复 verify-step.sh/devloop-check.sh/stop-dev.sh P0/P1 bug

## 背景

审计发现 verify-step.sh 的 `_pass()` 写入的 seal key 名和 stop-dev.sh `_SEALED_STEPS` 期望的不一致，
导致 /dev pipeline Stage 4 完成后仍出现"State Machine 验签缺失"死循环。同时发现其他 macOS 兼容性问题。

## 成功标准

- verify-step.sh `_pass()` 写入的 seal key 与 stop-dev.sh `_SEALED_STEPS` 期望的 key 完全匹配
- /dev pipeline 完成 Stage 4 后不再出现"State Machine 验签缺失"死循环
- CI 全部通过（engine-ci.yml）

## 不做什么

不改流程逻辑，不加新功能，只修 bug。
