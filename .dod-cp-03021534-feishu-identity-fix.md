---
id: dod-cp-03021534-feishu-identity-fix
version: 1.0.0
created: 2026-03-02
updated: 2026-03-02
changelog:
  - 1.0.0: 初始版本
---

# DoD: 飞书群聊身份识别修复

- [ ] D1: getGroupMembers 写入 feishu_users（含 open_id + relationship）
  - Test: `manual:bash -c "grep -q 'INSERT INTO feishu_users' packages/brain/src/routes.js && echo OK"`

- [ ] D2: 群消息处理先调 getGroupMembers 再调 getFeishuUserName
  - Test: `manual:bash -c "grep -q '预拉取成员信息' packages/brain/src/routes.js && echo OK"`

- [ ] D3: memory_type 'observation' 已不存在（改为 'short'）
  - Test: `manual:bash -c "grep -c \"memory_type.*observation\" packages/brain/src/routes.js | grep -q '^0$' && echo OK"`

- [ ] D4: 版本 patch bump
  - Test: `manual:bash -c "node -e \"const v=require('./packages/brain/package.json').version; const parts=v.split('.'); process.exit(parseInt(parts[2])>=2?0:1)\""`
