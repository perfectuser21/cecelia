# PRD: Vision 数据补全 — 给 ZenithJoy OKR 链补 Vision 父节点

## 背景

intent_expand pipeline 在扩展 task context 时，查询 OKR 的 Vision 父节点，
但 ZenithJoy Q1 OKR（`33a45167`）的 `parent_id` 为 NULL，导致 `has_vision: false`，
Vision 上下文信息缺失。

Vision 目标节点（`4911164a`）已存在于 `goals` 表中，只需补上 parent_id 链接。

## 目标

添加 migration 158，将 ZenithJoy OKR 的 `parent_id` 设置为正确的 Vision 目标节点。

## 成功标准

- migration 158 文件存在于 `packages/brain/migrations/`
- 执行后 OKR `33a45167` 的 `parent_id = '4911164a-...'`
- intent_expand 触发后 `context_found.has_vision = true`
- `selfcheck.js` EXPECTED_SCHEMA_VERSION 更新为 '158'
