# PRD: OKR PR11 — 清理旧表残留收尾

> 完整 Task Card 见 `.task-cp-03240219-07e80b6e-cd22-4c77-898e-3066e0.md`

## 功能描述
清除 PR10 合并后仍残留的 4 处旧表（goals/projects）实际查询：
1. actions.js updateGoal fallback `UPDATE goals SET` → throw Error
2. planner.js projCheck → 只查新表（okr_projects/okr_scopes/okr_initiatives）
3. memory-service.js getDetail → 旧表 FROM projects/goals → 查新 OKR 表
4. project-compare.js → FROM projects → FROM okr_projects（含 UNION）

## 成功标准
- `grep -rn "FROM goals\|FROM projects\|UPDATE goals" packages/brain/src/*.js packages/brain/src/services/*.js` 不返回生产代码查询
- 4 个目标文件全部迁移至新表
