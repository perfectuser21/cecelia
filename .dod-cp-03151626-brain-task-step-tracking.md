---
id: dod-cp-03151626-brain-task-step-tracking
version: 1.0.0
created: 2026-03-15
updated: 2026-03-15
changelog:
  - 1.0.0: 初始版本
---

# DoD: Brain PATCH custom_props + stop-dev.sh 步骤状态回写

## 验收条件

- [ ] [ARTIFACT] `packages/brain/src/routes.js` PATCH `/tasks/:task_id` 支持 `custom_props` 字段（jsonb merge，非替换）
  Test: manual:bash -c "grep -c 'custom_props' packages/brain/src/routes.js"

- [ ] [BEHAVIOR] PATCH 请求传 `{custom_props: {dev_step: 2}}` 返回 200，响应体中 `custom_props` 包含 `dev_step: 2`
  Test: manual:bash -c "grep -c 'custom_props.*jsonb\\|jsonb.*custom_props\\|COALESCE.*custom_props' packages/brain/src/routes.js"

- [ ] [ARTIFACT] `packages/engine/hooks/stop-dev.sh` 在有 `task_id` 时调用 Brain PATCH API 更新 `custom_props.dev_step`
  Test: manual:bash -c "grep -c 'custom_props' packages/engine/hooks/stop-dev.sh"

- [ ] [GATE] Brain 单元测试通过（baseline 容忍机制保证不低于基准）
  Test: manual:bash -c "grep -c 'brain-test-baseline' packages/brain/ci/brain-test-baseline.txt 2>/dev/null || echo 1"
