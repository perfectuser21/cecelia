# [CONFIG] fix(engine): 重新应用 Pipeline 死锁修复（#1286 被 rebase 覆盖）

## 背景
PR #1286 修复了三个 Pipeline 死锁问题，但被 #1284 的 rebase 覆盖。需要重新应用这三个修复。

## 成功标准

1. devloop-check.sh 合并失败时 return 2（blocked）而不是 return 1（error），避免工作流终止
2. _check_codex_review 函数有 15 分钟超时降级机制，防止 Codex 审查永久阻塞
3. 01-spec.md 和 02-code.md 派发 review 时写入 registered_at 时间戳，支持超时计算
