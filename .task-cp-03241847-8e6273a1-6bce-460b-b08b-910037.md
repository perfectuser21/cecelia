# Task Card: Hook-CI 耦合重构 R12 — verify-step Gate 0d + jq 优雅降级

## 任务信息

- **Branch**: cp-03241847-8e6273a1-6bce-460b-b08b-910037
- **类型**: feat
- **影响范围**: packages/engine/hooks/verify-step.sh, packages/engine/ci/scripts/check-version-sync.sh, Engine 版本 5 文件

## PRD 摘要

verify-step.sh Gate 0d 在改动 Engine 版本文件时调用 check-version-sync.sh；
check-version-sync.sh jq 不存在时优雅跳过；Engine 版本 5 文件同步 bump。

## Scope

**IN SCOPE**:
- `packages/engine/hooks/verify-step.sh`（Gate 0d 逻辑）
- `packages/engine/ci/scripts/check-version-sync.sh`（jq 优雅降级）
- Engine 版本 5 文件（package.json/package-lock.json/VERSION/.hook-core-version/regression-contract.yaml）
- `packages/engine/features/feature-registry.yml`（changelog 条目）

**OUT OF SCOPE**:
- Brain 代码
- 其他 package 版本文件

## 成功标准 (DoD)

- [x] [PRESERVE] verify-step.sh step2 已有的 Gate 0a/0b/0c/Gate 1/Gate 2 逻辑不受影响
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('Gate 0a'))process.exit(1);if(!c.includes('Gate 0b'))process.exit(1);if(!c.includes('Gate 0c'))process.exit(1);"

- [x] [BEHAVIOR] Gate 0d 在检测到 Engine 版本文件变更时调用 check-version-sync.sh
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('check-version-sync.sh'))process.exit(1);"

- [x] [BEHAVIOR] check-version-sync.sh jq 不存在时优雅跳过（不报错退出）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/ci/scripts/check-version-sync.sh','utf8');if(!c.includes('command -v jq'))process.exit(1);"

- [x] [ARTIFACT] Engine 版本 5 文件全部同步为同一版本（13.42.0）
  Test: manual:node -e "const v=require('./packages/engine/package.json').version;const fs=require('fs');const vf=fs.readFileSync('packages/engine/VERSION','utf8').trim();const hcv=fs.readFileSync('packages/engine/.hook-core-version','utf8').trim();const rc=fs.readFileSync('packages/engine/regression-contract.yaml','utf8');if(vf!==v||hcv!==v||!rc.includes(v))process.exit(1);"

- [x] [GATE] feature-registry.yml 含本次变更条目
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/features/feature-registry.yml','utf8');if(!c.includes('hook-ci-r12-gate0d-version-sync'))process.exit(1);"
