---
id: task-cp-03220402-6dc7c198-b393-4337-bf30-7e2b1d
type: task-card
branch: cp-03220402-6dc7c198-b393-4337-bf30-7e2b1d
created: 2026-03-22
---

# Task Card: dev pipeline 补三个硬锁单元

## 需求（What & Why）

**功能描述**：在 `packages/engine/skills/dev/steps/02-code.md` 补三个强制（exit 1）单元：
1. **Step 2.0 行为快照 [PRESERVE]**：探索代码前，扫描涉及模块的现有行为并写入 Task Card `[PRESERVE]` 条目，verify-step.sh Gate 2 强制执行验证
2. **Step 2.1.5 TDD先行**：探索代码后、写实现前，必须先写失败测试（红灯确认），再写实现（绿灯确认）
3. **CI镜像强制**：修复 2.3.6 的 `|| true`，全部改为强制 `exit 1`；verify-step.sh 补全对 `[PRESERVE]` 条目的识别与执行

**背景**：dev pipeline 的 Stage 2 缺乏行为快照和 TDD 强制约束，导致改动可能在不知情的情况下破坏现有行为；CI 镜像检查因 `|| true` 而失去阻断力。

**不做什么**：不改变现有 [BEHAVIOR]/[ARTIFACT]/[GATE] 的处理逻辑；不改 02-code.md 其他章节。

## 成功标准

1. [ARTIFACT] 02-code.md 包含 Step 2.0 且有 exit 1 强制机制
2. [ARTIFACT] 02-code.md 包含 Step 2.1.5 TDD 红灯→绿灯流程
3. [ARTIFACT] 02-code.md 的 2.3.6 无 `|| true`，改为强制 exit 1
4. [BEHAVIOR] verify-step.sh Gate 2 能识别并执行 `[PRESERVE]` 条目的 Test 命令
5. [GATE] Engine 版本 bump（patch）到 13.23.3，5 个版本文件同步

## 验收条件（DoD）

- [x] [ARTIFACT] 02-code.md 包含 `## 2.0` 章节且包含 exit 1
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('## 2.0'))process.exit(1);if(!c.includes('exit 1'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] 02-code.md 包含 `2.1.5` 章节（TDD先行）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('2.1.5'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] 02-code.md 的 2.3.6 无 `|| true`
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');const s=c.split('### 2.3.6')[1]||'';if(s.includes('|| true'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] verify-step.sh Gate 2 含 [PRESERVE] 条目识别代码
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('[PRESERVE]'))process.exit(1);console.log('ok')"

- [x] [GATE] Engine 版本文件全部为 13.23.3
  Test: manual:node -e "const p=require('./packages/engine/package.json');if(p.version!=='13.23.3')process.exit(1);console.log('ok')"

## 实现方案

**要改的文件**：
- `packages/engine/skills/dev/steps/02-code.md` — 添加 2.0 行为快照、2.1.5 TDD先行，修复 2.3.6 `|| true`
- `packages/engine/hooks/verify-step.sh` — Gate 2 补充 [PRESERVE] 条目识别与执行
- Engine 版本文件（5 个）：package.json、package-lock.json、VERSION、.hook-core-version、regression-contract.yaml
- `packages/engine/features/feature-registry.yml` — 添加 changelog 条目

**Scope 锚定**：packages/engine/skills/dev/ + packages/engine/hooks/verify-step.sh
