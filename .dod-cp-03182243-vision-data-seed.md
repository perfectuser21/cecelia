# DoD: Vision 数据补全 — 给 ZenithJoy OKR 链补 Vision 父节点

## 完成定义

- [x] migration 158 文件存在：`packages/brain/migrations/158_link_zenithjoy_okr_to_vision.sql`
- [x] OKR `33a45167` 的 `parent_id` 已设置为 Vision 节点 `4911164a`
- [x] `selfcheck.js` EXPECTED_SCHEMA_VERSION 更新为 `'158'`
- [x] selfcheck 测试基线更新，`npm test` 全绿

## 验证方式

```
manual: node -e "const {execSync}=require('child_process');const r=execSync('psql cecelia -t -c \"SELECT parent_id FROM goals WHERE id=\\'33a45167-f12e-4972-a33a-9553626363c1\\'\"').toString().trim();if(!r.includes('4911164a'))process.exit(1)"
tests: packages/brain/src/__tests__/selfcheck.test.js
```
