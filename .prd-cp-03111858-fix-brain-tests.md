---
id: fix-brain-tests
version: 1.0.0
created: 2026-03-11
updated: 2026-03-11
changelog:
  - 1.0.0: 初始版本
---

# PRD: 修复 Brain 失败测试

## 背景

`npm test --prefix packages/brain` 存在 12 个失败测试。

## 目标

修复所有失败测试，使失败数降为 0，不修改测试断言逻辑，只修复源码。

## 范围

- `packages/brain/src/executor.js`：移除 getAvailableMemoryMB/getMacOSMemoryPressure 从 platform-utils 导入，内联实现
- `packages/brain/src/selfcheck.js`：版本同步（已是 143，测试需更新）
- `packages/brain/src/cortex.js`：修改 callCortexLLM 默认 timeout 为 120000
- `packages/brain/src/__tests__/startup-sync.test.js`：startup-sync 源码参数顺序修复
- 版本相关测试文件：同步版本号 142→143

## 成功标准

- `npm test --prefix packages/brain` 输出 `Tests 0 failed`
- 不修改测试断言逻辑（版本号同步除外）
