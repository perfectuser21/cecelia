---
id: dod-cp-03110522-bridge-timeout-per-request
version: 1.0.0
created: 2026-03-11
updated: 2026-03-11
changelog:
  - 1.0.0: 初始版本
---

# DoD - 修复 cecelia-bridge Math.min 截断

## 验收清单

- [x] cecelia-bridge.js line 74 修改：Math.min 第二参数改为 MAX_LLM_TIMEOUT_MS
  Test: manual:bash -c "grep -c 'MAX_LLM_TIMEOUT_MS' packages/brain/scripts/cecelia-bridge.js"

- [x] CECELIA_BRIDGE_MAX_TIMEOUT_MS 环境变量读取（默认 600000）
  Test: manual:bash -c "grep -c 'CECELIA_BRIDGE_MAX_TIMEOUT_MS' packages/brain/scripts/cecelia-bridge.js"

- [x] 原有行为不变：未传 timeout 时仍使用 BRIDGE_TIMEOUT_MS 默认值
  Test: manual:bash -c "grep -c 'timeout || BRIDGE_TIMEOUT_MS' packages/brain/scripts/cecelia-bridge.js"

- [x] 修改后 Math.min 不再以 BRIDGE_TIMEOUT_MS 为上限
  Test: manual:bash -c "node -e \"const code=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.js','utf8'); if(code.includes('Math.min(timeout || BRIDGE_TIMEOUT_MS, BRIDGE_TIMEOUT_MS)')) process.exit(1); process.exit(0);\""
