---
id: task-cp-03220202-84c967fd
type: task-card
branch: cp-03220202-84c967fd-43b9-4c6b-88a8-9ceb72
created: 2026-03-22
---

# Task Card: P0修复验收 — devloop-check.sh + stop-dev.sh runtime bug

## 需求（What & Why）

**功能描述**: 验收并确认 devloop-check.sh 和 stop-dev.sh 的 6 个 P0/P1 运行时 bug 已全部修复

**背景**: Brain 调度此任务修复 /dev pipeline runtime 层的 6 个问题。经过代码审查，所有 6 个问题已在 v15.5.0/v15.6.0 中修复并通过 PR #1350 合并入 main。

**不做什么**: 不改 step 文件，不改 verify-step.sh，不加新功能

## 成功标准

1. [ARTIFACT] devloop-check.sh cleanup 失败路径存在 `return 2` 触发重试
2. [ARTIFACT] stop-dev.sh `_collect_search_dirs` 优先搜索主仓库
3. [ARTIFACT] stop-dev.sh TTY 匹配使用 `/dev/*` 前缀精确判断
4. [ARTIFACT] stop-dev.sh LEGACY 兼容代码含删除日期 `2026-09-01`
5. [ARTIFACT] devloop-check.sh changelog 无「删除Codex」垃圾描述
6. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] devloop-check.sh cleanup 失败后有 `return 2` 重试机制
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('cleanup.sh 执行失败'))process.exit(1);if(!c.includes('return 2'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] stop-dev.sh `_collect_search_dirs` 函数正确搜索主仓库
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('_collect_search_dirs'))process.exit(1);if(!c.includes('主仓库优先'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] stop-dev.sh TTY 匹配使用 `/dev/*` 前缀
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('/dev/*'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] stop-dev.sh LEGACY 代码有删除日期
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('2026-09-01'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] stop-dev.sh 的 _collect_search_dirs 函数在 worktree 内调用时，能通过 `git worktree list` 定位主仓库目录
  Test: manual:node -e "const {execSync}=require('child_process');const out=execSync('git worktree list --porcelain 2>/dev/null').toString();if(!out.includes('worktree '))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/engine && npm test 2>&1 | tail -5"

## 实现方案

**要改的文件**: 无需改动（所有修复已在 PR #1350 合并入 main）
**Scope 锚定**: 验收任务，验证代码现状
