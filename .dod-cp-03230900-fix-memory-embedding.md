# DoD — 修复 learnings 向量化管道

## 交付物

- [x] [ARTIFACT] embedding-service.js：generateLearningEmbeddingAsync 失败时 console.warn + 写重试队列
      Test: manual:grep -q "enqueueRetry" packages/brain/src/embedding-service.js && grep -q "console.warn" packages/brain/src/embedding-service.js
- [x] [ARTIFACT] embedding-service.js：新增 backfillLearningEmbeddings() 函数（分批处理，每批10条）
      Test: manual:grep -q "backfillLearningEmbeddings" packages/brain/src/embedding-service.js
- [x] [ARTIFACT] server.js：启动时调用 backfillLearningEmbeddings()
      Test: manual:grep -q "backfillLearningEmbeddings" packages/brain/server.js
- [x] [ARTIFACT] DB migration 173_cleanup_expired_memory_stream.sql：DELETE WHERE expires_at < NOW()
      Test: manual:test -f packages/brain/migrations/173_cleanup_expired_memory_stream.sql

## 运行时验证（BEHAVIOR）

- [x] [BEHAVIOR] generateLearningEmbeddingAsync 失败时 working_memory embedding_retry_queue 有记录写入
      Test: manual:cd packages/brain && npx vitest run src/__tests__/embedding-service-backfill.test.js --reporter=verbose 2>&1 | grep -q "T5: 失败时写入 embedding_retry_queue"
- [x] [BEHAVIOR] backfillLearningEmbeddings 对 embedding=null 的 learnings 生成并写入 embedding
      Test: manual:cd packages/brain && npx vitest run src/__tests__/embedding-service-backfill.test.js --reporter=verbose 2>&1 | grep -q "T1: 对 embedding=null"
- [x] [BEHAVIOR] OPENAI_API_KEY 未设置时 backfill 返回 {processed:0, failed:0} 不报错
      Test: manual:cd packages/brain && npx vitest run src/__tests__/embedding-service-backfill.test.js --reporter=verbose 2>&1 | grep -q "T4: OPENAI_API_KEY 未设置时跳过"

## 测试用例

- [x] [ARTIFACT] 测试：backfill 逻辑 + 失败重试队列写入（6 个测试 T1-T6 全部通过）
      Test: manual:cd packages/brain && npx vitest run src/__tests__/embedding-service-backfill.test.js 2>&1 | grep -q "6 passed"
