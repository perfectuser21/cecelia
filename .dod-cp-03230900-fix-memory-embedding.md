# DoD — 修复 learnings 向量化管道

## 交付物
- [ ] embedding-service.js：generateLearningEmbeddingAsync 失败时 console.warn + 写重试队列
- [ ] embedding-service.js：新增 backfillLearningEmbeddings() 函数（分批处理，每批10条）
- [ ] server.js：启动时调用 backfillLearningEmbeddings()
- [ ] DB migration xxx_cleanup_expired_memory_stream.sql：DELETE WHERE expires_at < NOW()
- [ ] 测试：backfill 逻辑 + 失败重试队列写入

## 测试用例
- T1: backfillLearningEmbeddings 对 embedding=null 的记录生成并写入 embedding
- T2: 单次 backfill 只处理 BATCH_SIZE=10 条
- T3: generateLearningEmbeddingAsync 失败时写入 working_memory 重试队列
- T4: OPENAI_API_KEY 未设置时跳过，不报错
