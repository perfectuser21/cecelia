# Cecelia 作战循环设计（v1.10 定稿 2026-07-06）

> SSOT 说明：本文是 2026-07-06 与主理人 8 轮拍板的定稿存档。8 条拍板决策已落 decisions 表
> （a0870384 / 584a5946 / 542a86ee / e1eed454 / 467ced6b / 1ef6ec3e / 928c6054 / e035dad8）。
> 阅读版：http://38.23.47.81:9998/cecelia-battle-loop-design.html
>
> 术语说明：2026-08-06 按决策 a340f100 词汇对照表统一用词（价值流 = 旧「线/Line」，
> 特性 = 旧「Ability」，使能项 = 旧「Feature」）。拍板结论与数字未改动，
> 专名（Line04 等业务线代号）、表名、字段名保持原样。

---

**设计文档 · 已定稿待开工**

基于 2026-07-06 当日代码审计实锤 · v1.10（8 拍板全定；新增判定点显性化——FR/NFR 之外的第三类）

**目标一句话**：把 Cecelia 从**「你踩一脚才动一下」**改成**「持续自转 + 每晚一页战报 + 只有方向性决策才找你，等你回复期间其余工作照常不阻塞」**。Human in the loop → human *on* the loop。

## 目录

1. [为什么现在是死的（诊断）](#一为什么现在是死的)
2. [整个东西如何成为一个 loop（全景）](#二整个东西如何成为一个-loop)
3. [三种模式怎么做、怎么用](#三三种模式怎么做怎么用)
4. [Human 什么时候在 loop 里](#四human-什么时候在-loop-里)
5. [怎么做：四期落地设计](#五怎么做四期落地设计)
6. [需要你拍板的 8 件事（8/8 全部已定）](#六需要你拍板的-8-件事88-全部已定)
7. [开工前置与风险清单（对抗审查产出）](#七开工前置与风险清单2026-07-06-对抗审查产出)

---

## 一、为什么现在是死的

「我不推就死」是两个缺陷叠加，都已实锤：

- **发条断了。** 2026-05-04 Wave 2 调度重构后，`tick-loop.js` 只调新的纯派发 `runScheduler`，旧 `executeTick()` 里挂的**约 25 个定时总结 / 巡检 / 战略触发全部成死代码**（`tick-runner.js:1534-1710`）。时间线吻合：管家日报最后一篇 5 月 4 日，arch_review 最后一次派发 5 月 9 日，strategy_session **从未跑过一次**。
- **就算发条修好，系统也只会「消费队列」，不会「生产目标」。** 活着的意识循环（20 分钟一次）只做「从现有任务里挑下一个」；「看战况 → 决定下一步打哪 → 生成新任务」这条回路从未上线。队列一空，系统就趴窝等你。

好消息：作战记录本身一直在写。`initiative_runs` 每条价值流的 run 数、成败、PR、裁决全在（近 7 天 26 条）；`decisions` 表甚至已有 `made_by` 字段区分系统决策（96,126 条）和用户决策（266 条）。**缺的不是史料，是史官和军师。**

## 二、整个东西如何成为一个 loop

四环相扣，对应「记录 → 汇总 → 决策 → 行动」。现状：两环活、两环缺。环路顺序如下（① → ② → ③ → ④ → 回到 ①）：

1. **① 行动 Act 〔活〕** —— 三种模式执行（见第三节）：路径 A 修 Bug / 路径 B 小改动 / 路径 C Harness Sprint。产出 PR → CI → evaluator + judge 双裁决 → merge → 部署。
   *（自动回写 ↓）*
2. **② 记录 Observe 〔活〕** —— `initiative_runs`（每条 run 的价值流、成败、PR、verdict）+ `tasks` + `learnings` + `decisions`。数据完整，一直在写。
   *（P2 新建 ↓）*
3. **③ 汇总 Orient 〔缺〕** —— 按价值流聚合战况（API 不存在）→ **每晚一页战报**（diary 机制已死，重生为战报）。已有可借力的活零件：蒸馏文档 WORLD_STATE / SELF_MODEL（24h 自动更新）。
   *（P3 新建 ↓）*
4. **④ 决策 Decide 〔缺〕〔你在这里〕** —— 军师（strategy tick）读战报 + OKR 进度 + 产能预算 + **想法箱 backlog** → 生成提案。三条输入流汇入同一排期漏斗：OKR 缺口（自动）/ 巡检发现（自动）/ 你的想法（想法箱分诊）。**L0/L1 自动入队执行；L2 发决策卡给你（等你回复，不阻塞其他工作）；L3 Bark 立刻找你。**
   *（提案入队，dispatcher 派发——这部分活着 ↓）*
5. **↩ 回到 ① 行动** —— 队列由系统自己填满，这是「不推就死」的根治。

你的位置：**只挂在 ④ 的 L2/L3 出口上**——L2 等你拍板（其余工作照常），执行层保持 zero-human-gate 不变。

## 三、三种模式怎么做、怎么用

三种模式就是 /dev 的三条路径，它们是 loop 里「行动」环的三种武器，按任务重量选用。每种都有两种点火方式：**有头**（你在 tmux 里说话触发）和**无头**（Brain 自动派发，走 `cecelia-run.sh`）。

| 模式 | 适用 | 流程 | 现在谁点火 | 目标状态 |
|------|------|------|-----------|----------|
| **路径 A · Bug** | 系统性 bug、回归 | 登记 Issue → 建 Task → /dev + systematic-debugging：先写能复现的 failing test → 修代码 → test 永久进 CI | 你发现、你触发 | code_review 巡检（复活后）自动发现 → 自动建 Issue + Task → **L1 自动开修**，你在战报里看到「修了什么」 |
| **路径 B · 小改动** | 单文件级、低风险 | 建 Task（挂 journey）→ /dev 全流程 → PR → auto-merge | 你触发 | 军师提案中的小优化走这条，**L0/L1 自动跑** |
| **路径 C · Harness** | 一个特性/使能项的完整推进 | PrepPRD + 铁律清单 → Brain 建 `initiative_runs` → skill-relay 单 session 接力：planner → GAN（proposer × reviewer 对抗至共识）→ generator（TDD 两次 commit）→ evaluator 真跑验收 → judge 独立判读 → merge → report 六处回写。**1 Sprint = 1 Generator = 1 PR**，一个特性可被多个 Run 反复推进 | 你 /dev 路径 C 触发；Brain tick 派发已支持但没有东西自动往里喂 | 军师按 OKR 缺口 + 各价值流战况自动生成下一个 Sprint（`harness_mode:true`），**新 initiative 的 PRD 走 L2 决策卡**给你过目后入队 |

**怎么用（做完这套之后你的操作界面）：**

- 想干新事：还是对我说，或 /plan → /dev。**需求入口永远是你**，这不变。
- 日常推进：不用管。军师按战报和 OKR 缺口自动生成 Sprint，pipeline 自己跑。
- 每天早上 6:00（北京）：飞书收晨间对齐会——先验货（昨日交付逐件 ✅/❌）再扫战况；细看去 Dashboard 战情室。
- 偶尔：收件箱里有 L2 决策卡，看到了就拍个板（等待期间其他工作不停）。
- 紧急：Bark 响了才需要立刻处理。
- 随时丢想法：过渡期说给有头 claude（即刻落库）；P3 后加飞书 Cecelia bot（手机最顺手）；P4 后 Dashboard 收件箱输入框。三通道汇同一想法箱。

### 一天的节奏（各角色的钟表）

| 时间 | 谁 | 干什么 |
|------|-----|--------|
| 随时 | 你 | 丢想法 → 秒级入箱分诊：**紧急立即插队**（不等周期）；其余入 backlog。任何想法从说出口到看到处置方案**最长 24 小时** |
| 凌晨 5:00 | 军师日 tick | **每日 OKR 诊断**：昨日战况 + OKR 进度树 + backlog 想法同桌权衡 → 今日排期提案（含每个想法的处置与理由）。动 OKR 本身永远走 L2 卡 |
| 早 6:00 | 对齐会 | 推你手机：昨日验货队列 + 今日计划 + 想法箱状态 + 产能/硬件水位 |
| 早上某刻 | 你 | 5-10 分钟：验货 ✅/❌、拍 L2 卡、扫想法处置（不同意说一句即可捞回/改道） |
| 白天 | 系统 | 按排期自转（harness run / 巡检 / 修复），L0/L1 不打扰你 |
| 7:00（体外） | 死人开关 | Brain 体外哨兵查「今天对齐会产出没」，缺席 → Bark |
| 月度/信号 | 军师重构 tick | 深度复盘积累模式 → 重构提案走 L2；铁律清理 |

## 四、Human 什么时候在 loop 里

核心是一套**写死在代码里的分级协议**（不靠 LLM 临场判断）。L2 决策等你回复（你每天会看收件箱），但**等待期间系统其余工作照常**——你只是方向的闸，不是流水线的闸。

| 级别 | 判定条件（代码写死） | 系统行为 | 例子 |
|------|---------------------|----------|------|
| **L0 静默** | 可逆 + 例行 + 预算内 | 直接做，只留痕 | harness run、重试、巡检、requeue、容量调度 |
| **L1 先斩后奏** | 可逆 + 新动作（非例行） | 直接做，**写进当晚战报** | 自动开新 Sprint（已有特性续推）、自动修巡检发现的 bug、调任务优先级 |
| **L2 决策卡** | 满足任一：不可逆（砍价值流 / 删表 / 新对外渠道）· 超预算阈值 · OKR 层级变更（新 KR / 砍 KR）· 同一特性连败 N 次后换打法 · 军师生成全新 initiative 的 PRD | 飞书决策卡 + Dashboard 收件箱，**等你回复**（你每天会看）；等待期间系统其余 L0/L1 工作照常，不阻塞 | 「获客价值流连败 11 次，提议暂停并转投 Line04——等你拍板」 |
| **L3 立即** | 生产事故（Brain down > 5min）· 凭据泄漏 · 全账号熔断 | **Bark 推送**（按既有告警规则） | 生产 5221 挂了、凭据过期全线阻塞 |

### 你的全部固定触点（总共五个）

1. **需求入口** —— 想做什么，永远由你说。
2. **PrepPRD 确认** —— 有头走路径 C 时的现有确认点，保留；军师自动生成的 PRD 改走 L2 卡。
3. **每日晨间对齐会** —— 5-10 分钟验货：逐件 ✅/❌，❌ 自动变铁律注入后续合同。这是防「越跑越偏」的主阵地。
4. **L2 收件箱** —— 等你拍板；等待期间不阻塞其他任何工作。
5. **L3 Bark** —— 唯一需要你立刻动的场景。

**明确不需要你的：** GAN 对抗轮次、TDD、CI 修复、evaluator/judge 裁决、merge、部署、巡检、重试、熔断恢复、产能调度。这些今天就已经是 zero-human-gate，保持不变。

## 五、怎么做：四期落地设计

每期独立可用、独立有收益，不用等全套。全部走 /dev（P1/P3 是 Brain 核心，本机做，不派 Codex）。

### P1 · 点火线圈 —— 把发条修好

*约 2–3 个 PR · Brain 核心 · 是其他三期的前提*

- 新建 `packages/brain/src/scheduler-jobs.js`：声明式任务注册表，每项 `{ name, schedule, guard, handler, timeout, onError }`，挂成 server.js 独立 loop —— 这个模式现成（server.js 665-853 行已有十几个独立 loop，harness-watchdog 当年就是这么被救活的）。
- 首批只注册 3 个 job：**battle-report**（每天早上 6:00 北京时间，接 P2 的生成器）、**arch_review**（每 4h，guard：期间有 dev 完成）、**strategy-tick**（每日一次 + active_goals=0 应急触发，接 P3）。
- 清理：executeTick 里其余 20+ 个死调用逐一判死刑或迁移（列清单进 PR）；三套重叠的「每日报告」（daily-report-generator 死 / nightly-tick 活 / nightly-orchestrator 晨报 活）合并为一套。
- **救活 capture 消化**：想法箱的进箱通道——capture_atoms 表 + 确认路由代码已存在，消化 cron 死于 Wave 2，注册进 job 表即活。
- **死人开关**：Brain 体外哨兵（launchd 独立 job 或 HK VPS cron），每天 7:00 查「今天战报产出没」，没有 → Bark。所有告警都从 Brain 发 = Brain 挂了一切静默；本系统历史大故障全是「停了没人知道」这一种死法（diary 停 2 月 / keepalive cron 失效 / 9998 fileserver 静默死）。
- **job 幂等规范**：每个 job 带 DB sentinel（抄 diary 的 working_memory key 模式）——Brain 每次 merge 自动重建是常态，5:58 重启不能让 6:00 战报跳过或双发。
- 顺手根治 **brain-unit vitest --changed 漏跑 fs 读取型测试**（已两次应验；P1 大改 Brain 核心正是高发区）。

**验收：** battle-report / arch_review 两个 job 连续 3 天按时产出（DB 可查）；executeTick 死代码有处置清单；重复报告只剩一套。

### P2 · 战况室 —— 汇总到一处

*约 2 个 PR · 性价比最高，一周内你就能看到战报*

- **前置：证据链体检**——合并 relay pr_url 回写修复（PR #3563 尚未合并），并端到端验证一条真实 relay run 的四件套（pr_url + 截图 + verdict + handoff）齐全，验货队列才有米下锅。
- `GET /api/brain/harness/stats?by=journey&days=30`：每条价值流的 run 数 / 成功率 / 最近战况 / 卡点（聚合 SQL 已验证可跑，本次审计手写的就是它）。**数据卫生**：聚合过滤 smoke-* 测试任务；113 条无 journey 孤儿 run 归类或标记排除。
- **每日对齐会生成器**（战报升级版）：输入 `initiative_runs` + `learnings` + 用户 `decisions` + L1 动作记录。两个部分：
  - **A. 验货队列（核心）**——昨天每件 merge 的交付逐件展示：一句人话「我们理解你要的是 X」+ 人类可见证据（截图 / URL / 演示命令；Contract 规则本就强制产出这些，harness-report 已写 Notion 带截图，这里只是聚合）。你逐件打 **✅ 对 / ❌ 不是我想的**，5-10 分钟。
  - **B. 战况统计**——各价值流昨日战况 / 今日计划 + 产能利用段（两账号 5h/7d 窗口利用率、空转窗口）+ 硬件水位段（内存/磁盘/GPU）。周日版加「熵仪表」（高频修改文件 top10、重复主题 learning、重复率趋势）。
  - 写 `design_docs`（type=battle_report）+ 飞书推送，格式定长可重生成。
- **纠偏回路（防「越跑越偏」的负反馈环）**：你打 ❌ + 一句哪里不对 → 写成该价值流/特性的 **invariant 铁律**，**先过 Invariant Gate 合规审核**（四查：与既有铁律冲突？可验证？scope 层级恰当？与累积 FR 矛盾？——PASS 生效，存疑进收件箱）→ 经既有 Context Manifest 管道（proposer / generator / evaluator 三处已接线）注入后续所有合同。**偏差最多存活一天，无法积累。** GAN/evaluator/judge 验的都是「忠于合同」；「合同是否忠于你的想象」只有你能验——这一环就是干这个的。
- **增强既有战情室**（Dashboard `/warroom`，价值流中心化页面已在线、API 数据活跃）：顶部加「每日战报」模块；**复活 reports 页**作为战报档案库（该页现存内容停在 2026-05-04 的失败 48h 简报——与 Wave 2 同日死亡）。不新建页面。

**验收：** 连续 3 天早上 6 点飞书收到对齐会、点开落在 /warroom；验货队列逐件带证据可打 ✅/❌；一条 ❌ 完整走通「❌ → invariant 入库 → 下一条该特性的合同里出现这条铁律」；战情室能回答「Line04 这个月跑了几个 run、成了几个」。

### P3 · 军师上岗 —— 生产下一步

*约 3 个 PR · 「不推就死」的根治 · 依赖 P1 P2*

- **军师双频**：
  - **日级战术 tick**：输入 = 最新战报 JSON + OKR 进度树 + capacity/token-budget（活模块直接消费）→ 提案列表；配额富余时主动加派 run（产能利用最大化）。
  - **月级重构 tick**（防「陷在小 bug 里看不见大问题」）：皮层级（Opus）深度复盘，不看眼前 bug 专看积累模式——输入 = 30 天 learnings 聚类（同主题反复出现=没治本）+ 被修改最多文件 top10（同处修 5 次=设计问题）+ arch_review 漂移趋势 + 代码重复率（scan-code-dedup 基线）+ issue 重开记录 → 输出**重构/重设计提案**走 L2 卡；同场做**铁律清理**（过期/重复/引用已废弃模块的 invariant 合并或退役，退役走 superseded_by 不硬删）。**信号触发不等月底**：同一文件 30 天修 ≥5 次 / 同主题 learning ≥3 条 / 漂移连报 2 次，任一命中提前触发。executeTick 死代码带就是此病的实证——每次小改都合理，积累成系统性断链，靠的是这次人工「跳出来重看」才发现；月度重构 tick 就是把这次对话制度化。
- 提案 schema：`{ action_type, target(journey/ability), reason, confidence, risk_level, default_action, budget_est }`，每条按既有 AI 自审规范打分。
- 风险路由：L0/L1 → 直接建 task 入队（`harness_mode:true`，Brain 派发）；L2 → 写 `decisions`（`made_by='proposal'`, `status='pending'`）+ 发飞书卡。
- **想法收件箱（预处理 box）**：进箱三通道（对话 capture / 飞书丢话 / 有头 plan）→ 秒级 AI 分诊四路——**紧急**→立即注册 task 插队（对齐会标注）；**不紧急任务型**→挂价值流作 backlog；**洞察型**→沉淀 decision/铁律（过 Invariant Gate）；**方向型**（隐含改 OKR）→军师评估提案走 L2 卡。军师日 tick 把 backlog 与 OKR 缺口同桌权衡，提案必须交代每个想法的处置理由。**战报设「想法箱」段：每个想法状态永远可见（已开工/排队第N/已沉淀/建议废弃可捞回）——想法永不黑洞。**
- 护栏：每日提案数上限（防军师灌爆队列）；提案必须挂 journey + OKR 链条，挂不上的丢弃并记日志。

**验收：** 队列清空后 24h 内系统自己生成合法提案并至少自动开出一个 L1 Sprint；L2 提案出现在收件箱且不自动执行。

### P4 · 决策收件箱 —— 你的驾驶座

*约 2 个 PR · 依赖 P3 有产出*

- `decisions` 分流基本免费：`made_by` 字段已存在（system 96,126 / user 266），加索引 + 查询过滤即可，96k 噪音一滤就干净。
- Dashboard 收件箱页：**待拍板（L2 卡）+ 待验货（对齐队列）同一页**——你每天看一个地方就够。L2 卡显示提案理由与自审分。
- **前置：手机访问路径验证**——你手机看的是 HK Dashboard 实例，✅/❌ 要写回美国本机 Brain 5221；确认 HK→Brain API 代理通路，不通则飞书卡片回调走公网 IP 兜底。
- 飞书交互卡（批准 / 否决 / 搁置三按钮，回调写 decisions）。**不做超时默认动作**（Alex 拍板：等回复；将来若决策积压再评估）。
- 你的每次拍板自动落 `decisions`（made_by='user'）—— 作战史的「御批」部分从此自动积累。

**验收：** 一条 L2 提案完整走通「卡片 → 你点批准 → 入队执行」；未回复的提案持续可见于收件箱且不阻塞其他派发。

## 六、需要你拍板的 8 件事（8/8 全部已定）

### 拍板 1 · 产能与资源 —— 已拍板 ✅ 2026-07-06（方向反转）· decisions e1eed454

**Alex 拍板：token 配额不是问题**（两账号 Sonnet 跑不满），闸的方向反转——**不防超支，防浪费**：战报含两账号 5h/7d 窗口利用率，军师配额富余时主动多开 run（token-budget-planner + slot-allocator 活着直接喂）。真正看护的是**硬资源：OOM / 磁盘 / GPU**——进战报「硬件水位」段 + 环境 preflight。美元闸降级为极端保险。原文如下（存档）：

- **这是干嘛的：** P3 之后军师会自动提案开新 initiative，每个 initiative 要烧真实算力（token/账号配额）。预算闸 = 军师想打一场超过阈值的大仗时，先给你看一眼再打；小仗直接打。它限制的是自动化系统烧钱的手，不是你。
- **修订原因：** 审计发现 `initiative_runs.cost_usd` 全是 0——成本从没被记账，美元阈值现在没有输入数据。**修订建议：** 先用活着的 token-budget（账号 7 天配额状态 abundant→critical）作闸：军师提案时若预算状态非 abundant 则升 L2；美元线等成本记账修好（列入 P3 待办）再定。

### 拍板 2 · 熔断与找人条件 —— 已拍板 ✅ 2026-07-06（按 relay 实证重构）· decisions 542a86ee

**数据修正：** 84% 失败率是旧 LangGraph 编排（v1，245 run / 206 败）的病，作废。relay 单 session（v2，07-04 起）：12 run **9 成 3 败，3 败全是管道回写断点**（无 failure_reason / 无 pr_url，正在修）——「agent 做不出来」的失败 v2 至今零次。Alex 判断成立：新世界里时长/配额/fix 轮基本不构成约束。

**重构后的四道闸（替代「连败熔断」）：**

1. **环境 preflight**：点火前查硬依赖（RPA 需 ROG 开机、目标设备可达等），不满足→不烧 run，任务标 blocked_env 进战报；
2. **单 run fix 轮硬上限 25**（Alex 认可 20-30 区间）——不是防失败，是防「越修越偏」的跑飞保险丝；
3. **无进展熔断**：同一特性连续 3 条 run 没有 merged PR（无论何种收场）→ L2 卡问你「换打法还是先放下」。度量「没交货」，不度量「失败」；
4. **防偏航**（Alex 真正担心的「只读 PRD 越跑越偏」）：既有四防线在线——contract-is-law（合同外一字不加）+ 独立 judge（agent 自称 PASS 不算数）+ handoff 注入（该价值流最近 3 份交接单自动带上）+ invariant 铁律注入；军师生成的全新 PRD 走 L2 先过你的眼，方向由你把一次关，之后靠合同锁死。

### 拍板 3 · 战报接收地与渠道 —— 已拍板 ✅ 2026-07-06 · decisions a0870384

主接收地 = Dashboard **战情室 /warroom**（增强既有页面）+ reports 页复活为战报档案；推送 = **飞书，每天早上 6:00（北京）晨间战报**，看完飞书细看 Dashboard；Bark 只留给 L3 紧急。（两轮拍板合并）

### 拍板 4 · 超时默认动作 —— 已拍板 ✅ 2026-07-06：先不做 · decisions 584a5946

L2 卡不设超时自动执行，一律等你回复（你每天会看）。等待期间系统其余工作照常不阻塞。将来若发现决策积压再重新评估。

### 拍板 5 · 每日对齐会 + 纠偏回路 —— 已拍板 ✅ 2026-07-06 · decisions 467ced6b

AI 干活必须每日对齐。晨间战报升级为对齐会（验货队列 + 战况统计）；你的 ❌ 自动变成铁律注入后续合同，偏差最多活一天。分级保底：新方向 PRD **开工前** L2 过目（防大偏）、日常交付 **merge 后次日晨**验货（防小偏积累）、个别关键任务可标 review_required 要求 merge 前亲眼看。不设常规 pre-merge 人工门，zero-human-gate 保持。

### 拍板 6 · Invariant Gate 铁律合规审核 —— 已拍板 ✅ 2026-07-06 · decisions 1ef6ec3e

**审计事实：** FR/NFR 都有 GAN 双保险（/dev v21 Phase 1 FR 对抗 + Phase 2 Agent D 专职 NFR；合同阶段 proposer×reviewer 7 维 rubric），**唯独 invariant 零审核**——入库端点只查 level 和 target 存在性，内容不查冲突/重复/可验证性，且无过期机制。每日 ❌→铁律上线后此洞会放大：**错合同只坑一个 sprint，错铁律注入该价值流之后每份合同**。

**机制：** 新铁律入库过轻量 LLM 四查（冲突 / 可验证 / scope 恰当 / 与累积 FR 矛盾），PASS→active，存疑→pending_review 进收件箱；月度重构 tick 配套铁律清理。

### 拍板 7 · 想法收件箱（预处理 box）—— 已拍板 ✅ 2026-07-06 · decisions 928c6054

OKR 是相对固定的执行轴，但每天的新想法 A/B/C 和新洞察需要落点：进箱（三通道）→ 秒级分诊（紧急插队 / 不紧急 backlog / 洞察变铁律 / 方向型走 L2 改 OKR）→ 军师排期同桌权衡 → 战报想法箱段防黑洞。复用已有 capture_atoms 表+路由代码（消化 cron 随 P1 救活）。

### 拍板 8 · 判定点显性化 —— 已拍板 ✅ 2026-07-06 · decisions e035dad8

**判定点** = 实现对模糊现实的判断假设（如 RPA 群判定方法），FR（做什么）/NFR（做得多好）之外的第三类：**怎么知道**。FR 表面全绿但判定方法烂 = FR 实际失守，且用户从不知道该判定点存在。机制：①合同强制「判定点登记表」（候选方法/所选/依据/误判后果，reviewer 审查缺段打回）②后果严重的升 PrepPRD 拍板点主动请教你 ③验货证据附判定方法清单 ④你的纠正经 Invariant Gate 变铁律。落地=proposer/reviewer/dev skill 模板（P2 旁路）。

## 七、开工前置与风险清单（2026-07-06 对抗审查产出）

设计四环之外的工程接缝审查。核心发现：7 条里 4 条是同一个教训的变体——**本系统历史上所有大故障都不是「做错了」，而是「停了没人知道」**。

1. **死人开关**（→ 已入 P1）：所有告警从 Brain 发，Brain 挂 = 一切静默 = 手机上和天下太平无法区分。体外哨兵每天查战报产出，缺席即 Bark。
2. **验货原材料链**（→ 已入 P2 前置）：relay pr_url 回写修复 PR #3563 未合并；relay 模式证据链（pr_url/截图/verdict/handoff）无人端到端验证过。
3. **数据卫生**（→ 已入 P2）：113 条无 journey 孤儿 run + smoke-* 任务污染，不过滤会进战报。
4. **手机访问路径**（→ 已入 P4 前置）：HK Dashboard 实例 ↔ 美国本机 Brain 的回写通路未验证。
5. **CI 已知病**：vitest --changed 漏 fs 型测试（两次应验未根治，→ 已入 P1 顺手修）；auto-merge×裁判竞态刚修留意回归；多 PR 并行 BEHIND churn 用 gh pr update-branch。
6. **job 幂等**（→ 已入 P1 规范）：Brain merge 自动重建是常态，job 无 sentinel 会跳过或双发。
7. **产能竞争**（低风险备忘）：军师灌队列后与 content-pipeline 抢 slot，靠 area-scheduler 公平调度 + 提案 priority 纪律。

---

> 本文所有「活 / 死 / 缺」结论来自 2026-07-06 代码审计：`tick-loop.js:49`（Wave 2 断点）、`tick-runner.js:1534-1710`（死代码带）、`routes/harness.js:1338`（仅全局 stats）、`active-goals-zero-trigger.js:25`（strategy_session 唯一触发点在死线上）、decisions / initiative_runs / design_docs 表实查。批准后本文入库 `docs/current/`，按 P1→P4 拆 initiative 走 /dev。
