## PRD - v1.62.0 聚焦执行（Focused Execution）

**需求来源**: OKR 渐进式拆解与验证系统 — PR 1/4
**功能描述**: 减少 Project 并发（max 从 ceil(SLOTS/2) 降为 2），支持顺序执行（sequence_order），新增 deadline urgency 评分维度
**涉及文件**:
- `brain/src/capacity.js` — Project max 硬编码为 2
- `brain/src/activation-scorer.js` — 新增 deadline urgency 评分
- `brain/src/project-activator.js` — 补位查询增加 sequence_order 排序 + deadline 传递
- `brain/src/selfcheck.js` — EXPECTED_SCHEMA_VERSION 049 → 050
- `brain/migrations/050-focused-execution.sql` — 新增 sequence_order / deadline / time_budget_days 字段

**成功标准**:
1. `computeCapacity(9).project.max` 返回 2（不超过 2）
2. `computeActivationScore()` 支持 entity.deadline，7天内线性加分，超期 +150
3. project-activator 补位候选按 sequence_order ASC 排序
4. Migration 050 成功执行，selfcheck schema version = 050
5. 所有现有测试通过 + 新增测试通过

**非目标**:
- 不修改 Initiative / Task 层的容量公式
- 不实现 review gate（PR 2 的范围）
- 不修改 decomposition-checker.js
