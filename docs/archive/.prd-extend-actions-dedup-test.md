## PRD - 扩展 actions-dedup.test.js 测试套件

**需求来源**: 扩展 actions-dedup.test.js 测试套件，确保 canceled 状态任务的去重逻辑正确

**功能描述**: 为 actions-dedup.test.js 测试文件添加针对 canceled 状态任务的去重逻辑测试用例，确保系统能正确处理已取消任务的去重行为

**涉及文件**:
- brain/src/__tests__/actions-dedup.test.js （需要修改）

**成功标准**:
- 新增 4+ 个 canceled 相关测试案例
- 所有测试通过
- 测试覆盖率维持 >95%
- 测试文档清晰说明测试目的

**非目标**:
- 不修改 actions-dedup.js 的实际去重逻辑
- 不涉及其他测试文件
- 不修改数据库 Schema

## 新增测试案例

1. `should dedup when canceled task exists` - 验证 canceled 任务阻止重复创建
2. `should dedup canceled task within time window` - 测试时间窗口机制
3. `should allow re-creation after canceled task expires` - 确保 canceled 任务过期后允许重新创建
4. `should handle both canceled and cancelled spellings` - 验证 canceled/cancelled 拼写都支持

## 测试逻辑
- 验证 canceled 任务阻止重复创建
- 测试时间窗口机制
- 确保 canceled/cancelled 拼写都支持
- 验证与其他状态的交互

## 验收标准
- [ ] 新增 4+ 个 canceled 相关测试案例
- [ ] 所有测试通过
- [ ] 测试覆盖率维持 >95%
- [ ] 测试文档清晰说明测试目的