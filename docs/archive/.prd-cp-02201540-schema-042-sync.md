# PRD: 同步 schema 042 到代码

DB 已应用 migration 042（evolution_history 表），但代码缺少 SQL 文件和 selfcheck 版本号。
需要补齐：
1. brain/migrations/042_evolution_history.sql（已创建）
2. selfcheck.js EXPECTED_SCHEMA_VERSION 041 → 042
3. DEFINITION.md schema_version 041 → 042
4. version patch bump

功能描述：同步已存在于 DB 的 schema，不引入新逻辑。
成功标准：brain-deploy.sh 的 self-check 通过（不再报 DB=042 expected=041）。
需求来源：brain-deploy.sh self-check 失败阻止部署。
