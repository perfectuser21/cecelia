# PRD - 添加测试工具函数

**需求来源**: Cecelia Team Test - 验证 Agent Team 流程

**功能描述**:
在 `brain/src/test-utils.js` 中添加一个简单的时间戳格式化工具函数 `formatTimestamp(date)`，用于测试和开发工具场景。

**涉及文件**:
- 创建：`brain/src/test-utils.js`

**成功标准**:
1. 文件 `brain/src/test-utils.js` 成功创建
2. 函数 `formatTimestamp(date)` 正确实现：
   - 输入：JavaScript Date 对象
   - 输出：格式化字符串 "YYYY-MM-DD HH:mm:ss"
   - 使用 Asia/Shanghai 时区
3. 函数正确导出（ESM format）
4. 代码简洁、可读

**非目标**:
- 不需要编写测试（这是测试工具本身）
- 不需要添加复杂的错误处理
- 不需要支持其他时区
- 不需要支持其他日期格式

**实现细节**:
- 使用 `toLocaleString()` 方法配合 Asia/Shanghai 时区
- 导出为命名导出（named export）
- 添加简单的 JSDoc 注释说明用途
