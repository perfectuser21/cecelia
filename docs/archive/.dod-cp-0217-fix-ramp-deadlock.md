# DoD: 修复 Dispatch Ramp 死锁 Bug

## 验收清单

- [ ] `getRampedDispatchMax()` 在 `current_rate=0 && alertness=AWARE && pressure<0.8` 时返回 1（而非 0）
  - Test: `brain/src/__tests__/tick-dispatch.test.js` (或相关 ramp 测试文件)
- [ ] `current_rate=0 && alertness=ALERT` 时返回 0（高警戒不允许启动）
  - Test: 同上
- [ ] `current_rate=0 && pressure>0.8` 时返回 0（资源紧张不允许启动）
  - Test: 同上
- [ ] 正常 CALM + 低压力 → ramp up 逻辑不变
  - Test: 同上
- [ ] 所有现有测试通过（`npm test` in brain/）
- [ ] facts-check.mjs 通过
- [ ] version-sync 通过（patch bump for bug fix）

## 相关代码

- `brain/src/tick.js` → `getRampedDispatchMax()` (line ~793)
- 新增/更新测试覆盖 ramp deadlock 场景
