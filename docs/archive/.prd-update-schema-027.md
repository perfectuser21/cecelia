## PRD - Update Schema Version to 027

**需求来源**: Database migration 027 已执行，需要更新代码以匹配新的 schema 版本

**功能描述**:
1. 更新 `brain/src/selfcheck.js` 中的 `EXPECTED_SCHEMA_VERSION` 从 '026' 改为 '027'
2. 从 `CORE_TABLES` 数组中移除 'features' 表（已在 migration 027 中删除）
3. 确保 Brain selfcheck 能正确验证新的 schema 版本

**涉及文件**:
- `brain/src/selfcheck.js` - 更新 schema 版本常量和表列表

**成功标准**:
1. `EXPECTED_SCHEMA_VERSION = '027'`
2. `CORE_TABLES` 数组不包含 'features'
3. Brain selfcheck 能成功通过（验证 schema_version 表中版本 = 027）
4. 所有现有测试通过

**非目标**:
- 不修改任何其他代码逻辑
- 不添加新功能
- 不修改 database schema（已通过 migration 027 完成）

**上下文**:
- Migration 027 已成功执行，删除了 features 表并清理了相关外键
- Database 当前 schema 版本是 027
- Code 中的 EXPECTED_SCHEMA_VERSION 仍然是 026，需要同步
