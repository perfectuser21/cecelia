# Schema Sync 241→243 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 把 `packages/brain/src/selfcheck.js` 的 `EXPECTED_SCHEMA_VERSION` 从 `'241'` 改到 `'243'`，与最新 migration 号同步，解开所有 Brain push 的 facts-check 阻塞。

**Architecture:** 单文件单常量修改。no 测试代码新增（DoD 的两条 [BEHAVIOR] 都是 `manual:node` 一行命令，不走 vitest）。

**Tech Stack:** Node.js / `scripts/facts-check.mjs`。

---

## File Structure

- **Modify**: `packages/brain/src/selfcheck.js:23` — 一行常量改动

没有新建文件。没有新增测试文件。

---

### Task 1: 修 selfcheck.js 常量

**Files:**
- Modify: `packages/brain/src/selfcheck.js:23`

- [ ] **Step 1: 确认当前值就是 `'241'`**

Run:
```bash
grep -n "EXPECTED_SCHEMA_VERSION" packages/brain/src/selfcheck.js
```

Expected 输出：
```
23:export const EXPECTED_SCHEMA_VERSION = '241';
```

如果不是 `'241'` 立即停止 — 说明其它人已经改过，这个 task 无意义。

- [ ] **Step 2: 改成 `'243'`**

编辑 `packages/brain/src/selfcheck.js` 第 23 行：

改前：
```js
export const EXPECTED_SCHEMA_VERSION = '241';
```

改后：
```js
export const EXPECTED_SCHEMA_VERSION = '243';
```

行号、前后空行、分号、引号都保持原样，只改数字。

- [ ] **Step 3: 跑 DoD Test 1（字符串存在）**

Run:
```bash
node -e "const c=require('fs').readFileSync('packages/brain/src/selfcheck.js','utf8');if(!c.includes(\"EXPECTED_SCHEMA_VERSION = '243'\"))process.exit(1)"
echo "exit=$?"
```

Expected: `exit=0`

- [ ] **Step 4: 跑 DoD Test 2（facts-check 通过）**

Run:
```bash
node scripts/facts-check.mjs
```

Expected 关键行：
```
✓ selfcheck_version_sync: selfcheck.js='243' matches highest migration='243'
```
整体结尾 `Facts check passed.` 且 exit code 0。

如果还报 selfcheck_version_sync 不 sync，说明 migrations 目录又有新的（244+），这属于并行 race，需要先 `git pull --rebase` 再次确认。

- [ ] **Step 5: Commit**

```bash
cd /Users/administrator/worktrees/cecelia/schema-sync-241-243
git add packages/brain/src/selfcheck.js
git commit -m "fix(brain): selfcheck EXPECTED_SCHEMA_VERSION 241→243 同步最新 migration

#2515 合并 migration 242/243 时 bypass 了 facts-check，导致 selfcheck.js 常量
没同步 bump，#2538 推 brain 路径下 README 时被 local-precheck 拦下。这里补齐。

无功能性影响：该常量仅是 Brain 启动时对比 DB migration 版本的阈值，
改高后生产 Brain 启动若 DB 已在 243（本机/Brain 容器均是）正常通过，
若 DB 仍 ≤242 则 selfcheck 拒绝启动并告警（本来就是期望行为）。

Brain task: e8902f66-8b17-4072-92ed-f79328c0899b"
```

- [ ] **Step 6: 勾选 DoD**

不需要独立测试文件，DoD 已经在 Step 3/4 跑过了。只需要在 PR description 里把两条 `[BEHAVIOR]` 标记为 `[x]`。PR 创建时生成的 body 自动包含 DoD section。

---

## Self-Review

### Spec Coverage

Spec 里的要求逐条对照：

| Spec 要求 | 对应 Task 步骤 |
|---|---|
| 问题：selfcheck.js 241 vs migration 243 不同步 | Task 1 Step 2（唯一的代码改动） |
| 方案：改 `'241'` → `'243'` | Task 1 Step 2 |
| DoD [BEHAVIOR] 1：EXPECTED_SCHEMA_VERSION 改为 243 | Task 1 Step 3 验证 |
| DoD [BEHAVIOR] 2：facts-check 通过 | Task 1 Step 4 验证 |

全覆盖。

### Placeholder Scan

无 TBD / TODO / "implement later" / "add error handling" / 空 test stub。

### Type Consistency

不涉及类型/函数签名（纯常量值修改）。

### Scope

严格 1 文件 1 行。不夹带任何其它改动（spec 显式说明不顺手加防御机制）。
