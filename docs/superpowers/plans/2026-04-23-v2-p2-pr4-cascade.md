# v2 P2 PR4 cascade Middleware Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:subagent-driven-development.

**Goal:** 新建 `packages/brain/src/spawn/middleware/cascade.js` 填充 `opts.cascade`；`executeInDocker` 在 `resolveAccount` 前调它。

**Architecture:** 纯新增 + 1 行 middleware 调用链挂接。使用 `require('../../model-profile.js')` lazy load 避免循环。零行为改动（当 opts.cascade 已设时）。

**Tech Stack:** Node.js ESM + vitest。

---

## File Structure

- **Create** `packages/brain/src/spawn/middleware/cascade.js`
- **Create** `packages/brain/src/spawn/middleware/__tests__/cascade.test.js`
- **Modify** `packages/brain/src/docker-executor.js` — 加 import + 1 行 resolveCascade 调用

---

### Task 1: 建 cascade.js

**Files:**
- Create: `packages/brain/src/spawn/middleware/cascade.js`

- [ ] **Step 1: 用 Write 写文件**

```js
/**
 * cascade middleware — Brain v2 Layer 3 attempt-loop 内循环第 b 步。
 * 见 docs/design/brain-orchestrator-v2.md §5.2 + §5.3。
 *
 * 职责：若 opts.cascade 未设（caller 没显式传），用 getCascadeForTask(opts.task) 填充。
 * 显式 opts.cascade 尊重不覆盖。
 *
 * v2 P2 PR 4（本 PR）：新建 middleware。下游 account-rotation 已在 opts.cascade 上读取，
 * 填充后 selectBestAccount 能用到正确的降级链。
 *
 * @param {object} opts  { task, cascade? }
 * @param {object} ctx   { deps? } — 测试注入 { getCascadeForTask }
 */
export function resolveCascade(opts, ctx = {}) {
  if (opts.cascade) return;
  if (!opts.task) return;
  try {
    let getCascade;
    if (ctx.deps?.getCascadeForTask) {
      getCascade = ctx.deps.getCascadeForTask;
    } else {
      const mod = require('../../model-profile.js');
      getCascade = mod.getCascadeForTask;
    }
    const cascade = getCascade(opts.task);
    if (Array.isArray(cascade) && cascade.length > 0) {
      opts.cascade = cascade;
    }
  } catch (err) {
    console.warn(`[cascade] middleware failed (keeping opts.cascade undefined): ${err.message}`);
  }
}
```

- [ ] **Step 2: 语法 + smoke**

```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && node --check packages/brain/src/spawn/middleware/cascade.js && node -e "import('./packages/brain/src/spawn/middleware/cascade.js').then(m => { if(typeof m.resolveCascade !== 'function') process.exit(1); console.log('ok'); })"
```

Expected: `ok`

- [ ] **Step 3: commit**

```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && git add packages/brain/src/spawn/middleware/cascade.js && git commit -m "feat(brain): v2 P2 PR4 新增 spawn/middleware/cascade.js"
```

---

### Task 2: docker-executor.js 接线

**Files:**
- Modify: `packages/brain/src/docker-executor.js`

- [ ] **Step 1: 加 import**

用 Edit 在 import 区（PR3 加的 `import { resolveAccount } from './spawn/middleware/account-rotation.js';` 附近），在它后面加一行：

```js
import { resolveCascade } from './spawn/middleware/cascade.js';
```

- [ ] **Step 2: 在 executeInDocker 里插入 resolveCascade 调用**

用 Edit 找：
```js
  opts.env = opts.env || {};
  await resolveAccount(opts, { taskId });
```

改成：
```js
  opts.env = opts.env || {};
  resolveCascade(opts);
  await resolveAccount(opts, { taskId });
```

- [ ] **Step 3: 语法 + smoke**

```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && node --check packages/brain/src/docker-executor.js && node -e "import('./packages/brain/src/docker-executor.js').then(m => { if(typeof m.executeInDocker !== 'function') process.exit(1); console.log('ok'); })"
```

Expected: `ok`

- [ ] **Step 4: 验证 resolveCascade 在 resolveAccount 之前**

```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && node -e "const c=require('fs').readFileSync('packages/brain/src/docker-executor.js','utf8'); const ci=c.indexOf('resolveCascade(opts)'); const ai=c.indexOf('await resolveAccount(opts'); if(ci<0||ai<0||ci>ai) process.exit(1); console.log('order ok')"
```

Expected: `order ok`

- [ ] **Step 5: commit**

```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && git add packages/brain/src/docker-executor.js && git commit -m "refactor(brain): v2 P2 PR4 executeInDocker 在 resolveAccount 前调 resolveCascade"
```

---

### Task 3: 建单测

**Files:**
- Create: `packages/brain/src/spawn/middleware/__tests__/cascade.test.js`

- [ ] **Step 1: 写文件**

```js
import { describe, it, expect } from 'vitest';
import { resolveCascade } from '../cascade.js';

const mockCascade = ['claude-sonnet-4-6', 'claude-opus-4-7', 'claude-haiku-4-5-20251001'];

function makeDeps(cascade = mockCascade) {
  return { getCascadeForTask: (_task) => cascade };
}

describe('resolveCascade() cascade middleware', () => {
  it('respects explicit opts.cascade (no override)', () => {
    const opts = { task: { task_type: 'dev' }, cascade: ['explicit-model'] };
    resolveCascade(opts, { deps: makeDeps() });
    expect(opts.cascade).toEqual(['explicit-model']);
  });

  it('fills opts.cascade from task when unset', () => {
    const opts = { task: { task_type: 'dev' } };
    resolveCascade(opts, { deps: makeDeps() });
    expect(opts.cascade).toEqual(mockCascade);
  });

  it('no-op when no task', () => {
    const opts = {};
    resolveCascade(opts, { deps: makeDeps() });
    expect(opts.cascade).toBeUndefined();
  });

  it('no-op when getCascadeForTask returns empty array', () => {
    const opts = { task: { task_type: 'dev' } };
    resolveCascade(opts, { deps: makeDeps([]) });
    expect(opts.cascade).toBeUndefined();
  });

  it('keeps opts.cascade undefined when deps throw', () => {
    const opts = { task: { task_type: 'dev' } };
    const deps = { getCascadeForTask: () => { throw new Error('boom'); } };
    resolveCascade(opts, { deps });
    expect(opts.cascade).toBeUndefined();
  });
});
```

- [ ] **Step 2: 语法 + vitest**

```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && node --check packages/brain/src/spawn/middleware/__tests__/cascade.test.js && npx vitest run packages/brain/src/spawn/middleware/__tests__/cascade.test.js 2>&1 | tail -10
```

Expected: 5/5 pass

- [ ] **Step 3: commit**

```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && git add packages/brain/src/spawn/middleware/__tests__/cascade.test.js && git commit -m "test(brain): v2 P2 PR4 cascade middleware 单测 (5 cases)"
```

---

### Task 4: DoD 终验

- [ ] **DoD 1 — cascade.js export resolveCascade**
```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && node -e "import('./packages/brain/src/spawn/middleware/cascade.js').then(m => { if(typeof m.resolveCascade !== 'function') process.exit(1) })" ; echo "exit=$?"
```
Expected: `exit=0`

- [ ] **DoD 2 — docker-executor 顺序正确**
```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && node -e "const c=require('fs').readFileSync('packages/brain/src/docker-executor.js','utf8'); const ci=c.indexOf('resolveCascade(opts)'); const ai=c.indexOf('await resolveAccount(opts'); if(ci<0||ai<0||ci>ai) process.exit(1)" ; echo "exit=$?"
```
Expected: `exit=0`

- [ ] **DoD 3 — test 存在**
```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && node -e "require('fs').accessSync('packages/brain/src/spawn/middleware/__tests__/cascade.test.js')" ; echo "exit=$?"
```
Expected: `exit=0`

- [ ] **facts-check + 老测试兼容**
```bash
cd /Users/administrator/worktrees/cecelia/v2-p2-pr4-cascade-middleware && node scripts/facts-check.mjs 2>&1 | tail -3 && npx vitest run packages/brain/src/__tests__/docker-executor-account-rotation.test.js 2>&1 | tail -5
```
Expected: `All facts consistent.` + 老测试全通过

---

## Self-Review

| Spec 要求 | Task |
|---|---|
| cascade.js export resolveCascade | Task 1 |
| 测试注入（ctx.deps.getCascadeForTask） | Task 1 Step 1 |
| docker-executor 调 resolveCascade 在 resolveAccount 前 | Task 2 |
| 5 cases 单测 | Task 3 |
| DoD 3 条 | Task 4 |

Placeholder scan: 无。
Type consistency: `resolveCascade(opts, ctx)` 一致。
Scope: 不改 getCascadeForTask / selectBestAccount / 其它 middleware。
