# Sprint Report 生成流程 — Harness v3.1 Sprint 1（2026-04-08）

### 根本原因

sprint_report 任务首次执行时，.dev-mode 文件未创建，导致 Stop Hook 反复检测到 Stage 2 未完成。根本原因是 /dev 流程创建 .dev-lock 但未同步创建 .dev-mode，而 devloop-check.sh 依赖 .dev-mode 中的 step_2_code 字段判断状态。

### 下次预防
- [ ] sprint_report 任务启动时，同时创建 .dev-lock 和 .dev-mode（含 step_1_spec: done 初始值）
- [ ] harness_mode 任务完成代码输出后，立即将 step_2_code: done 写入 .dev-mode，不等 Stop Hook 提醒
- [ ] sprint-report skill 中明确说明需要在 sprint_dir 子目录写 sprint-report.md（而非 sprints/ 根目录）
