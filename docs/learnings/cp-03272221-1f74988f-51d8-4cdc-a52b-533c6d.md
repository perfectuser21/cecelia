## Stage4 自动写 CURRENT_STATE.md（2026-03-28）

### 根本原因

系统缺乏将 capability-probe 结果输出给 Claude 的机制。
probe 每小时探针结果虽存入 DB 的 cecelia_events 表，但从未被注入到对话上下文。
session-start.sh 只注入任务列表，缺少健康链路状态维度，导致 Claude 无法感知 10 条能力链路是否全绿。

### 下次预防

- [ ] 新增系统状态输出脚本时，验证从 worktree 调用路径能正确找到主仓库（git-common-dir）
- [ ] session-start.sh 扩展应只追加注入，不替换原有 Brain 任务注入逻辑
- [ ] Engine skills/hooks 同一 PR 改多个文件时，feature-registry.yml scope 字段用逗号分隔完整路径
