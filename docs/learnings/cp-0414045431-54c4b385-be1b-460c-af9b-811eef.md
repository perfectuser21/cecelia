## 重构 triggerCodexBridge 降低圈复杂度（2026-04-14）

### 根本原因
函数将多种职责（prompt 构建、分支名生成、账号选择、runner 配置、HTTP 调用）全部内嵌在主体中，导致三元嵌套叠加 try/catch 嵌套，圈复杂度达 23。

### 下次预防
- [ ] 新增 executor 函数时，每个独立职责抽成命名子函数，避免在主体内内嵌 try/catch
- [ ] 多层三元运算（isA ? x : isB ? y : z）应提取为独立 if/else 函数
- [ ] PR 合并前用 `node --check` 验证语法
