# Learning - Cortex LLM 计时日志与可观测性增强

**Branch**: cp-03122057-0be66616-38a2-4713-a66c-401fc8
**PR**: #914

### 经验总结

1. **callCortexLLM 返回值变更影响范围**：将函数从返回 `string` 改为返回 `{ text, timing }` 时，需同步更新所有依赖其返回值的测试文件。本 PR 遗漏了 `model-profile-dispatch.test.js` 中的 D15a/D15b 测试，导致 CI L3 新增 2 个失败。

2. **schema version 多处同步**：每次新增 migration 时，除 `selfcheck.js` + `DEFINITION.md` + `desire-system.test.js` 三处外，还需检查 `learnings-vectorize.test.js`、`selfcheck.test.js` 等其他测试文件中是否有硬编码 schema version 断言。

3. **token 估算策略**：字符数 ÷ 4 是合理的快速估算（英文约 4 chars/token，中文约 1-2 chars/token），无需引入 tokenizer 库。适合日志记录场景。

4. **LEARNINGS.md 必须在第一次 push 前写好**：本 PR 第二次 push 后才触发 Learning Format Gate 失败，下次应在第一次 push 前先准备好 docs/learnings/<branch>.md。

### 下次预防

- [ ] 修改函数返回类型时，grep 所有引用该函数的测试文件并更新断言
- [ ] 新增 migration 后，grep `'151'` 或当前版本号，找到所有需要同步的测试文件
- [ ] Learning 文件在写代码阶段（Step 6）完成后即准备，不要等到 Step 10
