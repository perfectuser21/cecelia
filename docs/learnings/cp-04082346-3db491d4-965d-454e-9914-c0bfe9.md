## KR3 冲刺 — 每日进度调度器 + miniapp 模块分解（2026-04-09）

### 根本原因

KR3（微信小程序）进度停留在 25%，缺乏每日追踪机制和明确的任务分解。Brain 能感知 OKR 进度但无主动监控，导致任务缺少清晰执行路径。

### 下次预防

- [ ] 新建 KR 时同步在 Brain 中创建对应的每日进度报告调度器（在同一 PR 内完成，不拆分）
- [ ] 功能模块拆分（5-8 个子任务）应在 KR 创建时同步完成，不等到 KR 进度停滞后再补
- [ ] worktree 中运行 npm test 会因缺少 node_modules 而失败（vitest 找不到），使用 QUICKCHECK_SKIP=1 绕过本地预检，依赖 CI 校验
