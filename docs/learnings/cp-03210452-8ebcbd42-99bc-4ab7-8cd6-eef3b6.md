# Learning: 验证 Pipeline 死锁修复（#1286 三处改动）

**任务**: fix(config): 验证 #1286 被覆盖的三处改动已恢复
**分支**: cp-03210452-8ebcbd42-99bc-4ab7-8cd6-eef3b6
**日期**: 2026-03-21

## 验证结论

三处改动均已在 main 中，由 #1294 和 #1296 修复：

| 改动 | 位置 | 状态 |
|------|------|------|
| merge 失败 return 2 | `devloop-check.sh:448` | ✅ 已在 main |
| 15 分钟超时降级 | `devloop-check.sh:126-148` (elapsed_secs > 900) | ✅ 已在 main |
| registered_at 写入 | `01-spec.md:260` + `02-code.md:201` | ✅ 已在 main |
| stop-dev.sh fallback exit 2 | `stop-dev.sh:871` + registered_at 超时 | ✅ 已在 main (#1296) |

### 根本原因

Brain 在 15:00 创建了"恢复 #1286 改动"任务，但 #1294 在 18:16 已合并（re-apply）、#1296 在 19:21 已合并（stop-dev.sh 同步修复），Brain 没有感知到这些后续修复导致重复派发。

同时发现 3 个预存在测试失败：
1. `pr-gate-phase1.test.ts`：check-dod-mapping.cjs 用 cwd=PROJECT_ROOT 时被上层 .prd.md 干扰
2. `generate-feedback-report.test.ts`：与 record-step.test.ts 并行共享同一 log 文件导致竞争
3. `metrics.test.ts`：metrics.sh 对大仓库做 git log 扫描，超过 vitest 默认 5s 超时

### 下次预防

- [ ] Brain 在创建"恢复/修复"类任务前，先查询 git log 确认改动是否已存在于 main
- [ ] 对"re-apply"类 PR，合并后更新关联任务状态为 completed
- [ ] devloop-check.sh 版本注释应及时更新，方便快速判断修复状态
- [ ] CI 重跑时要注意 PR title 改动不会影响旧 run 的事件 payload，需要 push 新 commit 才能让新标题生效
