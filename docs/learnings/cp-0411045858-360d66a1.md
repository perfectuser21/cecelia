### 根本原因

新增 Dashboard 页面需要：(1) Brain API 端点聚合数据，(2) Frontend API client，(3) 页面组件，(4) feature manifest 注册四步同时完成。

### 下次预防

- [ ] Brain API 端点按 sprint_dir 聚合 harness 任务，sprint_dir 是 tasks 表的正式列
- [ ] execution feature 的 navItem.group 必须是 'execution' 才能出现在"执行"导航组
- [ ] .dev-mode 必须含 tasks_created: true，否则 branch-protect hook 阻止代码修改
