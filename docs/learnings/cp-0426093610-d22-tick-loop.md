# Learning: cp-0426093610-d22-tick-loop

## 上下文

D 阶段 tick.js 拆分接力链第 4 棒。前 3 棒（D1.7a tickState / D1.7b executeTick body / D2.1 reset helpers）
已把 14 个状态字段、executeTick 函数体、9 个测试 helper 都收口到独立模块。本 PR 抽出
runTickSafe / startTickLoop / stopTickLoop 这一组「loop 控制」函数到 tick-loop.js。

## 根本原因

tick.js 历史包袱大（拆前 ~700 行），调度入口 / 状态查询 / loop 控制 / recovery / 测试 helper / Codex immune 全揉在一起。
违反「单一职责」：当我只想改一个 loop 行为，得在 700 行文件里通读上下文。

## 下次预防

- [ ] D2.3（tick-recovery.js）和 D2.4（tick-status.js）也按本 PR 模板抽：先 import 新模块，再删原函数定义，最后 export 块照常 re-export
- [ ] 每个并行 PR 必须保持「中央 export 块」修改最小化（只删自己抽走的 symbol，不动其他人的），减少 conflict
- [ ] 后合者遇到 conflict 时，conflict 块基本只是行号差异 + 删除位置不同；用 `git rebase main` + `git mergetool`（或手动）即可，不要 `git rebase --abort`
- [ ] `tickLog` 在每个新模块本地复刻（与 tick.js 同形态，独立计数器），而不是从 tick.js import — 避免循环依赖
- [ ] 所有「随函数走」的常量也搬到新模块，再 import 回老 caller — 状态和行为同处

## 测试覆盖

- tick-throttle.test.js（runTickSafe 节流）：4/4 ✓
- tick.test.js（routeTask + MAX_NEW_DISPATCHES_PER_TICK）：10/10 ✓
- tick-runner.test.js / tick-state.test.js / tick-watchdog.test.js / tick-codex-immune.test.js / tick-cleanup.test.js / tick-global-fallback-planner.test.js：全部 pass
- 共 57+ 个相关测试 100% 通过，覆盖 runTickSafe + 常量 + tick.js re-export
