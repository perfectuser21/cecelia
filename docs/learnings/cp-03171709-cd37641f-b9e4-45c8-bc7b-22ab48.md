# Learning: 知识库 BACKLOG.yaml 全量同步

**分支**: cp-03171709-cd37641f-b9e4-45c8-bc7b-22ab48
**日期**: 2026-03-17
**任务**: 知识反刍启动 — librarian 处理首批5个P0知识单元

---

## 根本原因

并行代理（多个 Codex 账号）在之前的 /librarian 运行中生成了大量 HTML 知识页，但 BACKLOG.yaml 的状态字段未同步更新。导致 meta.done 仅显示 11，而实际已生成 76/86 个页面。

BACKLOG.yaml 是唯一的知识库进度记录（git 追踪），外部 HTML 文件的完成状态必须手动同步。

---

## 解决方案

用 Node.js 脚本扫描 `/Users/administrator/claude-output/knowledge/` 下所有已存在的 HTML 文件，建立文件名→BACKLOG ID 映射表，批量更新 BACKLOG.yaml：
- `status: todo / in_progress` → `status: done`
- 新增 `output_url` 字段指向 HTML 路径
- 新增 `completed: 2026-03-17`
- 更新 `meta.done` 计数

---

## 下次预防

- [ ] /librarian 完成每批次后，BACKLOG.yaml 的 status 更新应作为每次运行的原子操作（生成 HTML + 更新 YAML 必须在同一步骤）
- [ ] 多代理并行运行时，所有代理都往同一个 BACKLOG.yaml 写（已在 librarian SKILL.md 中约定）
- [ ] Brain 任务描述应更精确区分"生成 HTML"（librarian 任务）和"同步 YAML 状态"（本次任务）
- [ ] 下次类似任务直接用脚本而非手动编辑，避免 YAML 格式错误
