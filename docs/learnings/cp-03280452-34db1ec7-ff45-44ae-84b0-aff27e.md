## write-current-state.sh 集成测试（2026-03-28）

### 根本原因

Brain 在 PR #1646 合并后重调度了同功能的任务（34db1ec7），原因是任务创建时未关联 PR，Brain 未能感知到工作已完成。

任务完成后必须及时回写 Brain（`PATCH /api/brain/tasks/{id}` + `pr_url` + `status: completed`），才能防止 Brain 重复派发相同功能的任务。

本次额外验证发现：任务虽已在 main 实现，但缺少独立的集成测试文件，无法通过 CI "feat PR 必须含测试" 检查，因此需补充 `scripts/__tests__/write-current-state.test.sh`。

### 下次预防

- [ ] PR 合并后立即调用 Brain PATCH API 回写 `status: completed` 和 `pr_url`，不能等 Stage 4 结束
- [ ] Task Card 中的 `brain_task_id` 字段应在 Stage 1 写入 `.dev-mode`，方便 Stage 4 自动回写
- [ ] 检查是否有类似的"孤岛任务"（创建后从未被关联 PR），在 Brain 侧加入 PR 关联检测

