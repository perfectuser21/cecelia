## KR 进度跟踪可观测性修复（2026-03-31）

**任务**：诊断并修复 KR 进度全 0% 的可见性问题

### 根本原因

1. `/api/brain/okr/current` 使用 `ORDER BY created_at DESC LIMIT 5`，2026-03-25 创建的 4 个无 KR 的 objectives 占满 LIMIT，导致创建于 2026-03-23、有 4 个活跃 KR 的 `4a2c8a08`（ZenithJoy 自媒体）被排除在结果之外。KR 不是不存在，而是根本不可见。

2. migration 208 修复空表问题后，内容生成和自动发布两个 KR verifier 意外使用了完全相同的 SQL（均查 content_publish_jobs 总数），导致语义不同的指标无法区分。

### 下次预防

- [ ] OKR API 的 objectives 查询禁止使用基于时间排序的硬 LIMIT，应改为基于"有 KR"的过滤条件
- [ ] 创建 KR verifier 时必须检查是否与其他 KR 使用了相同 SQL（重复指标 = 信息冗余，无诊断价值）
- [ ] 新增 migration 时同步更新 selfcheck.js EXPECTED_SCHEMA_VERSION 和 DEFINITION.md schema_version（两处必须同步，facts-check.mjs 会检测）
- [ ] KR verifier 设计原则：每个 KR 应有独立的、能准确反映该 KR 业务语义的 SQL 查询
