## /dev Stage4 集成 — cleanup.sh 自动调用 write-current-state.sh（2026-03-29）

### 根本原因

文档（04-ship.md 4.4.5 节）已描述 PR 合并后调用 write-current-state.sh，
但自动执行路径（cleanup.sh）从未实现该调用，导致 CURRENT_STATE.md 永不自动更新。
缺少 Learning 文件和 Engine 版本 bump 导致 L1/L2 CI 失败。

### 下次预防

- [ ] Engine skills/ 改动时，PR title 必须含 [CONFIG]，否则 Instruction Book 检查失败
- [ ] feat: PR 需 bump Engine 版本（5 个文件），CI L2 强制检查版本是否变化
- [ ] Learning 文件必须在第一次 push 前写好（docs/learnings/<branch>.md 含 ### 根本原因 + ### 下次预防 + - [ ]）
- [ ] 改 engine/skills/ 时需更新 feature-registry.yml + 运行 generate-path-views.sh
