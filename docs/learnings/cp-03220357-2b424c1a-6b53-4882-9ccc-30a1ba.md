# Learning: devloop-check 合并失败 return2 + 15min 超时回归测试

**Branch**: cp-03220357-2b424c1a-6b53-4882-9ccc-30a1ba
**Date**: 2026-03-22
**Task ID**: 2b424c1a-6b53-4882-9ccc-30a1ba8f7d9d

## 根本原因

Brain 派发的任务 PRD（`return 2` 修复 + `_check_codex_review` 15min 超时）描述的是「要实现的功能」，但这两个功能在 subagent 迁移 PR（约 #1367）中已经实现：
- `devloop-check.sh` 合并失败路径已使用 `return 2`（不是 `return 1`）
- `_check_codex_review` 函数已被 Agent subagent 同步架构替代并删除
- `stop-dev.sh` 已包含 `-gt 900` 超时自动 pass 降级逻辑

任务实际交付物转变为：为这些已实现行为补充回归测试断言，防止未来改动破坏行为。

## 关键决策

1. **不改 shell 脚本**，仅在测试文件中补充断言（4 个新 `it()` 块）
2. **window 大小选 500**（而非 300）：`合并失败` 到 `return 2` 的代码距离超过 300 字符，需要 500 字符窗口才能覆盖到

## 下次预防

- [ ] 收到「修复 X 行为」类任务时，先探索代码确认该行为是否已存在，避免重复实现
- [ ] `devloop-check-gates.test.ts` 中的 window 字符数（500）需与实际 shell 脚本结构匹配，避免断言无法命中目标代码
- [ ] stop-dev.sh 路径在测试文件中用 `STOP_DEV` 常量声明，便于路径变更时统一更新
