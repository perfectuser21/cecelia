# Learning - 三层大脑边界漂移：L0 脑干直接调用 L2 皮层

**Branch**: cp-03120051-4cc45e14-9c17-4d1d-93fb-50eb8f
**任务类型**: arch_review（2026-03-12 定期巡检）

### 根本原因

在 tick.js 中，`processCortexTask()`（第 686 行）和 `check48hReport()`（第 2552 行）直接 `await import('./cortex.js')` 调用 L2 皮层，绕过了 L1 丘脑的事件路由。这是多次"按需扩展"的渐进积累导致的——每次加新功能时直接在 tick.js 堆代码，没有评估是否破坏层级边界。

副作用：tick.js 已增长至 2,790 行，混合了调度、Cortex 批处理、超时自动失败等 5+ 种职责，使 L0 脑干的职责范围严重模糊。

### 下次预防

- [ ] 每次往 tick.js 新增代码前，检查"这是不是 L0 脑干的职责"，LLM 调用类逻辑应放入独立的调度子模块
- [ ] 新建 `cortex-scheduler.js` 封装所有 Cortex 批处理调用（RCA、系统报告），让 tick.js 只做调度分发
- [ ] 模型迁移后必须同步更新全局 CLAUDE.md（此次巡检发现 CLAUDE.md 仍记录 "L1 丘脑（Haiku）"，实际已是 MiniMax M2.1）
- [ ] arch_review 结束后，将 P1 问题创建为独立 Task 挂入 Brain 队列，不能只写在报告里

