# Learning: 重构 analyzeDeep 降低圈复杂度

**任务**: 重构 `cortex.js` 中 `analyzeDeep` 函数（CC 55 → ~2）
**分支**: cp-03231633-5d46a3b7-12f7-461e-8680-4d5232
**日期**: 2026-03-23

---

### 根本原因

将 `console.log` 改为 `console.info`（满足 Gate 0c 生产代码无调试日志要求）时，
`cortex-reflection-breaker.test.js` 中 D4 测试通过 `vi.spyOn(console, 'log')` 监听日志，
但被监听的调用已迁移到 `console.info`，导致测试断言失败。

生产代码与测试 spy 目标必须保持同步：改变 console method 时需同步更新测试的 spy 目标。

### 下次预防

- [ ] 修改生产代码中 `console.log` → `console.info/error` 时，立即 grep 对应测试中的 `vi.spyOn(console, 'log')` 并同步更新
- [ ] Task Card "成功标准" 部分使用 `- ` 格式（不加 `[ ]`），避免被 DoD 扫描器误判为缺少 Test 字段
- [ ] [GATE] DoD 条件的 Test 字段避免使用 `npm test --prefix packages/brain`（L1 CI 中 packages 未安装，exit 127），改用 `node -e` 静态断言
- [ ] Learning 文件必须在第一次 push 前写好并加入 commit，不能事后补

### 技术细节

- `analyzeDeep`（220行，CC=55）提取为 10 个辅助函数，主体缩减至 20 行
- 提取函数：`_buildBaseContext`、`_enrichContextWith*`（×6）、`_processSuccessfulResponse`、`_handleLLMError`、`_callLLMAndProcess`
- 预存本地测试失败（10 个）来自 `learning-effectiveness`（真实 DB 依赖）和 `actions-initiative-orchestration`（逻辑预存缺陷），均与本次重构无关
