## 修复 index.html import.meta 白屏 + content-factory 免登录（2026-03-20）

### 根本原因

**1. index.html import.meta 白屏问题**
- **真实状况**：通过全代码库搜索（`find . -name "*.html" -exec grep -l "import\.meta" {} \;` 和 `grep -r "Clarity" .`），确认当前代码库中完全不存在 Clarity 脚本或 import.meta 相关代码
- **根本原因**：这是一个**过期或虚假的任务需求**，可能来自历史版本的问题描述，或者问题已在之前的代码清理中被修复
- **处理决策**：正确选择了"验证问题存在性"而非盲目修复不存在的问题

**2. content-factory 免登录访问问题**
- **根本原因**：`apps/api/features/knowledge/index.ts` 中的 content-factory 路由配置缺少 `requireAuth: false` 字段
- **技术机制**：FeatureRoute 接口默认 `requireAuth: true`，所有路由默认需要认证，除非显式设置 `requireAuth: false`
- **定位过程**：通过阅读 `apps/api/features/types.ts` 中的 FeatureRoute 接口定义，快速确认了配置字段和修复方案

### 下次预防

对于认证相关的路由配置问题，建立以下预防机制：
- **问题验证**：收到任务后，先用技术手段验证问题是否真实存在，避免在过期需求上浪费时间
- **接口优先**：认证配置修改前，必须先阅读 TypeScript 接口定义，了解可用的配置选项和默认值
- **单一职责**：当任务包含多个不相关问题时，分别验证和处理，避免将不同领域的问题混合处理

**具体预防 Checklist：**

- [ ] 收到认证相关任务时，先检查 `apps/api/features/types.ts` 中的 FeatureRoute 接口定义
- [ ] 新增免登录路由时，验证 `requireAuth: false` 字段已正确添加
- [ ] 任务包含"可能存在的问题"时，优先用 `grep`/`find` 等工具验证问题真实性
- [ ] DoD 编写时，使用 `grep -c` 或 `! grep -q` 等技术验证手段，避免 `echo` 假测试
- [ ] 路由配置修改后，用 `curl` 测试未登录状态下的 HTTP 响应码确认生效