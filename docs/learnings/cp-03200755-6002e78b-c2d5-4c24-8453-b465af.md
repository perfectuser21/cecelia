## 修复 index.html import.meta 白屏 + content-factory 免登录（2026-03-20）

### 根本原因

本次任务涉及两个问题：
1. **index.html import.meta 白屏问题**：经调查发现当前代码库中不存在 Clarity 脚本或相关的 import.meta 使用问题。这可能是一个已经被修复或不存在的问题。
2. **content-factory 免登录访问**：问题的根本原因是 Core 实例的 FeatureRoute 配置缺少 `requireAuth: false` 字段，导致所有路由默认需要认证。

开发过程非常顺利，没有遇到重大障碍：
- 通过 FeatureRoute 接口的类型定义快速定位了解决方案
- 单文件配置修改，范围明确且风险很低
- 所有 DoD 验证都一次通过，无需返工

### 下次预防

- [ ] 对于类似的"可能不存在的问题"，优先进行代码库搜索验证，避免盲目寻找
- [ ] 认证相关的路由配置修改应该首先查看 TypeScript 接口定义，了解可用的配置选项
- [ ] 简单的配置修改任务应该保持最小化原则，专注于核心需求而不是过度设计
- [ ] 当任务描述包含多个问题时，应该逐个验证是否真实存在，避免在不存在的问题上浪费时间