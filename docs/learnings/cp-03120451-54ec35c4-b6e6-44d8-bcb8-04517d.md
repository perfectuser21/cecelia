### [2026-03-12] Brain dispatch 时注入相关 Learning 上下文

**失败统计**：CI 失败 0 次，本地测试失败 0 次

**开发情况**：并行 agent 场景 — 另一个 agent 通过 PR #896 已完成同功能，本 worktree 无需重复实现。

**本次实现内容（PR #896，by 并行 agent）**：
- 新建 `packages/brain/src/learning-retriever.js`：关键词打分检索 learnings 表
  - 5 维度评分：task_type 匹配(+10)、domain 匹配(+6)、标题关键词(+8)、failure_pattern 加成(+4)、时效性(+3)
  - normalizedScore > 0.3 阈值过滤，最多注入 3 条，按 learning_type 分组展示
  - DB 查询失败时降级（return ''，不中断 dispatch）
- `executor.js` `preparePrompt` 在所有 PRD 路径注入 learningCtx

**并行 agent 处理规则**：
- 发现 main 最新 commit 已是同功能 PR（5cc4286d3）→ 直接确认完成
- 本 worktree 无代码变更，无需创建 PR
- 清理 .dev-mode 即可

**影响程度**: Low（流程顺畅，并行场景正确处理）

**预防措施**：
- 启动 /dev 前先检查 main 最近提交，若已有同功能 PR 合并则跳过
- Brain dispatch 应在派发前检查是否已有相同 task_type + title 的完成 PR，避免重复派发
