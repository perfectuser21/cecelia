## [2026-03-12] Brain dispatch 注入相关 Learning 上下文（并行 agent 场景）

### 根本原因

本 worktree 与另一个并行 agent 处理相同任务，PR #896 已率先完成功能合并。本 branch 无需重复实现相同代码，仅记录开发知识。

### 下次预防

- [ ] 启动 /dev 前先检查 main 最新 3 条 commit（`git log --oneline -3 origin/main`），发现已有同功能 PR 合并时直接标记完成，不创建 PR
- [ ] Brain dispatch 应检查近 24h 内是否已有相同 task_type + title 的 completed 任务，避免重复派发给多个 agent

### 技术要点（PR #896 实现内容）

- `learning-retriever.js` 5 维度评分：task_type(+10)、domain(+6)、标题关键词(+8)、failure_pattern(+4)、时效性(+3)，满分 31 分
- `SCORE_THRESHOLD = 0.3`，最多注入 `MAX_INJECT = 3` 条，按 `learning_type` 分组展示
- `executor.js preparePrompt` 在所有 PRD 路径（prd_content/payload.prd_content/payload.prd_path）注入 `learningCtx`
- DB 查询失败时 catch → return ''，不中断 dispatch

**影响程度**: Low（流程顺畅，并行 agent 正确处理）
