# Learning: GTD OKR Tree 完整重建

**Branch**: cp-03252340-ef2bb051-68a5-42a9-a4ec-0054a8
**Date**: 2026-03-26

### 根本原因

1. **GTDProjects type 映射 bug**：`apps/api/src/task-system/projects.js` 将 Brain `okr_projects` 的所有记录硬编码为 `type: 'initiative'`，导致前端过滤 `type === 'project'` 时父级列表为空。根因是迁移到新 OKR 表时忘记更新 type 映射。

2. **GTDOkr 数据源错误**：使用旧 `goals` 表（area_okr/kr），而新体系已迁移到 `objectives`/`key_results`/`okr_projects`/`okr_scopes`/`okr_initiatives` 5 张表，并已有 `/api/brain/okr/area-tree` 端点可用。

3. **Area 重复**：同一 areas 表中有重复名称的记录（Cecelia×2、ZenithJoy×2），是数据 seed 时没有做 upsert 导致。Brain areas 路由缺少 PATCH，无法通过 API 清理。

### 下次预防

- [ ] `projects.js` 代理 Brain 数据时，`type` 字段应来自 Brain 数据而非硬编码
- [ ] 新 OKR 表迁移后，及时更新前端数据源（不要混用旧 goals 表和新 objectives 表）
- [ ] Brain 路由添加新表时，同步检查是否需要 PATCH/DELETE 端点
- [ ] Areas seed 脚本使用 `INSERT ... ON CONFLICT (name) DO NOTHING` 防止重复
