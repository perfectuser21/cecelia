## cross-ref-registry.yaml 跨文件引用声明（2026-03-20）

### 根本原因

本次开发流程顺畅，CI 失败 0 次，本地验证 0 次失败。
关键原因：在写 YAML 前先测试了所有 extract 命令（macOS grep 不支持 `-P`，
改用 `-E`；ESM 模块用 `node --input-type=module`），避免了命令在特定
平台不可执行的问题。

另一个要点：verify-step.sh step1 需要满足两个 Gate：
1. check-dod-mapping.cjs 所有 DoD checkbox 已勾选（`[x]`）
2. `.dev-agent-seal.${BRANCH}` 文件存在且有 `step_1_agent: approved`

因此正确顺序是：先创建产物 → 验证 DoD → 标记 `[x]` → 创建 seal → 写 .dev-mode。

### 下次预防

- [ ] 在 Task Card 的 extract 命令中，优先用 `node -e` 而非 `grep -P`，macOS 不支持 Perl regex
- [ ] verify-step.sh step1 需要 DoD 全部 `[x]` + `.dev-agent-seal` 双 Gate，先创建产物再标记
- [ ] YAML extract 命令使用 ESM 文件时用 `node --input-type=module -e "import ... from '...'"` 形式
- [ ] Brain 的 `code_quality_review` / `prd_coverage_audit` task_type 当前 DB 约束不支持，注册会失败（非阻塞）
