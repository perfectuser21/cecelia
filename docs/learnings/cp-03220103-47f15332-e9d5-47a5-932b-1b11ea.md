# Learning: .dod.md 归属校验任务已在并行 PR 中完成

## 任务背景
Brain 调度任务：知识反刍 Batch 1，step-00 加入 .dod.md 归属校验（非阻断警告）

## 发现
进入 worktree 后检查代码库，发现该功能**已由并行 agent 在 PR #1339 中完成并合并到 main**：

```
a496901e0 [CONFIG] feat(engine): branch-protect.sh v27 — .dod.md 归属校验（非阻断警告） (#1339)
```

branch-protect.sh v27（第 531-547 行）已包含完整实现：
- 检测 `.dod.md` 文件是否存在
- 提取 `branch:` frontmatter 字段
- 不匹配时输出 `[WARN]` 警告（不 exit，不阻断）

### 根本原因
Brain 同一任务被多次派发（重试机制），导致多个 agent 竞争同一功能。首个成功的 agent 已合并 PR，后续重试的 agent 进入空任务状态。

### 下次预防
- [ ] agent 进入 worktree 后应**首先检查 main 是否已有同功能提交**，若已合并则直接以"已实现"结束任务
- [ ] Brain 在派发重试任务前应检查对应功能是否已在 main 中存在，避免无效重试
- [ ] agent 遇到"功能已实现"时应调用 Brain API 标记任务完成，而非继续执行 /dev 流程
