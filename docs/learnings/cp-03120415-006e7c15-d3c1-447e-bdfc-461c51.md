### [2026-03-12] /dev Step 10 分类写入 learnings DB

**失败统计**：CI 失败 2 次，本地测试失败 0 次

**CI 失败记录**：
- 失败 #1：PRD 用 `**成功标准**:` 粗体而非 `## 成功标准` 二级标题 → 修复为二级标题 → 下次在写 PRD 时直接用 `## 成功标准`
- 失败 #2：DoD L29 使用 `curl localhost:5221` 集成测试 → CI 无 Brain 服务 → 改为 `grep` 命令；DoD 项未标 `- [x]` → check-dod-mapping.cjs 检测到未验证项 → 完成后统一标为 `- [x]`

**错误判断记录**：
- 以为 CI 集成测试可以 curl localhost:5221 → 正确：CI 环境无 Brain，所有 DoD 测试命令必须只用 grep/ls/cat 等静态命令

**影响程度**: Medium（CI 失败 2 次，有明确根因）

**预防措施**：
- DoD 测试命令避免依赖运行时服务（curl/node server），只用静态文件命令（grep/ls/cat）
- PRD 成功标准必须用 `## 成功标准` 二级标题，不能用粗体
- DoD 项完成后立即标为 `- [x]`，不要等到 CI 失败才改
