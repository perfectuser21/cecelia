# Learning: CECELIA_MAX_SEATS=10 硬上限固化 — DEFINITION.md 双层并发模型

## 任务背景

固化 `CECELIA_MAX_SEATS=10` 硬上限：在 DEFINITION.md 更新第 7 节并发模型文档，将过时的单层模型（PHYSICAL_CAPACITY=12，8核描述）更新为准确的双层并发模型。

---

### 根本原因

DEFINITION.md 第 7.1 节机器参数与实际不符：
- 写的是"8核16GB，PHYSICAL_CAPACITY=12"，实际是"10核16GB，PHYSICAL_CAPACITY=10（MAX_PHYSICAL_CAP 兜底）"
- 缺少 CECELIA_BUDGET_SLOTS（运营上限）的文档说明
- 第 7.2 节动态限流表的"满额（12）"与实际 physical_capacity=10 不符

config 文件（`docker-compose.yml`、`packages/brain/.env.example`）已由 cp-03181813 hotfix 补写完整，本 PR 仅需更新文档。

### 下次预防

- [ ] 修改并发相关 env var（CECELIA_MAX_SEATS / CECELIA_BUDGET_SLOTS）时，同步更新 DEFINITION.md 第 7 节
- [ ] DEFINITION.md 机器参数变更前先从 Brain API `/api/brain/tick/status` 获取实际值（physical_capacity、max_concurrent、auto_dispatch_max）
- [ ] 双层并发模型（Layer 1 物理 / Layer 2 硬上限 / Layer 3 运营上限）的变更需在 DEFINITION.md 中完整记录三层关系

---

**验证结果**（2026-03-19）：
- DEFINITION.md 第 7.1 节已更新为 10 核，PHYSICAL_CAPACITY=10，双层模型文档化 ✅
- Brain API `physical_capacity: 10` 确认硬上限生效 ✅
- `max_concurrent: 7` 为 CECELIA_BUDGET_SLOTS=7 控制的运营上限（预期行为）✅
