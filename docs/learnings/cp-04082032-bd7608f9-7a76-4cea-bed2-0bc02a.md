## 微信小程序核心功能完整化 — zenithjoy-miniapp 跨 Repo 开发（2026-04-09）

### 根本原因

1. **跨 Repo 任务与 /dev worktree 机制不匹配**：Brain 调度的任务代码在 `zenithjoy-miniapp` 独立 repo，而 /dev 流程默认在 cecelia monorepo worktree 中工作。branch-protect hook 拦截了对非 worktree 路径的写操作，需要为 zenithjoy-miniapp 单独创建 worktree。

2. **并行 PR 合并冲突**：main 分支在本次 PR 期间被另一个 PR（copywriter flow + scrollable title panel）合并，导致多处冲突：`ai-features/index.js` 完全重写、新增大量页面和功能文件。

3. **`Page()` 全局注册模式与 jest `require()` 不兼容**：小程序文件通过 `Page({...})` 注册而非 `module.exports`，直接 require 返回 `{}`，需要拦截 `global.Page` 捕获配置对象。

4. **Babel 版本冲突**：项目中 `@babel/runtime@7.24.0` 与 jest 内置 Babel `7.21.4` 不兼容，通过 `transform: {}` 跳过 Babel 转换解决（小程序源码为纯 CJS 无需转换）。

### 下次预防

- [ ] **跨 Repo 任务**：Brain 派发涉及 zenithjoy-miniapp 的任务时，先检查 `/Users/administrator/perfect21/zenithjoy-miniapp/.git/worktrees/` 确认 worktree 是否存在，若无则先建立再开始写代码
- [ ] **合并冲突预防**：push 前先 `git fetch origin main && git merge origin/main` 提前解决冲突，不要等到 PR 创建后才发现
- [ ] **小程序 jest 测试模板**：已建立 `__tests__/setup/wx-mock.js` + `jest.config.js` 模板，下次直接复用，无需重建 `global.Page` 捕获机制
- [ ] **.dev-mode 路径**：跨 Repo 任务的 `.dev-mode` 和 Learning 写在 cecelia worktree 中（即便代码在别处），这样 stop hook 可以正常识别完成状态
