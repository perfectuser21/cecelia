## 微信小程序核心功能完整化（2026-04-09）

### 根本原因

zenithjoy-miniapp 是独立 repo，不在 cecelia monorepo 中，导致 /dev 流程的 branch-protect.sh hook 阻止了在主仓库工作目录（非 worktree）中写代码文件。

### 关键发现

- 小程序存在于 `/Users/administrator/perfect21/zenithjoy-miniapp/`，已有约 70% 完成度
- 多套重复文件（article-detail/index.\*、ai-features/ai-features.\*、article-list/index.\*）遗留，导致开发混乱
- app.json 注册的页面和实际文件不一致（admin 页面已实现未注册）
- 并行 PR 合并到 main 引发冲突（main 做了大规模重构：新增 copywriter 流程 + assistant 页面）

### 下次预防

- [ ] 跨 repo 任务（zenithjoy-miniapp、zenithjoy 等独立 repo）必须先创建该 repo 的 worktree，不能在主仓库工作
- [ ] 开始前先 `git fetch origin && git log origin/main --oneline -5` 了解 main 最新状态，避免合并冲突
- [ ] 创建 worktree 后立即建 `.dev-mode.${BRANCH}` 文件，否则 branch-protect.sh 会拦截所有写操作
- [ ] 遇到 modify/delete 冲突（我们删了文件但 main 修改了）优先采用 `git checkout origin/main -- file` 保留 main 版本
