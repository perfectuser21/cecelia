## 技术调研：/dev Stage4 hook 机制 + CURRENT_STATE 方案（2026-03-29）

### 根本原因

调研发现 `write-current-state.sh` 已存在且在 Stage4.4.5 中有 AI 手动调用指令，但
`devloop-check.sh` 的自动合并路径没有调用它，导致 PR 自动合并时 CURRENT_STATE.md
无法被自动更新，只能依赖 AI 记得执行这一步骤。

### 下次预防

- [ ] 类似"PR 合并后执行 X"的需求，优先检查 devloop-check.sh 中 `gh pr merge` 成功分支是否已挂载该调用
- [ ] 新增 write-current-state.sh 功能时，同步检查 Brain API 字段是否有 null 值防护
- [ ] 方案文档需包含：触发点代码行号 + 实际 API 验证结果 + 降级策略
