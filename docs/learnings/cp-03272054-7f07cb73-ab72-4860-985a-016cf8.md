# Learning: 每日自动发布调度系统

## 任务概述
为 Cecelia 实现每日发布调度器（daily-publish-scheduler.js）+ 发布监控器（publish-monitor.js），
连接已有的 publisher skills + executor 路由，形成完整的每日自动发布循环。

### 根本原因
content_publish_jobs 表有 pending 积压（2026-03-19，2条），始终没有处理器
消费。content_publish 任务的创建和发布路由都已就绪，但缺少每日触发器和队列处理器
将它们连接起来。

### 下次预防
- [ ] 新增数据库表时同时评估：是否需要对应的调度器/处理器？
- [ ] content_publish_jobs 这类"队列型"表需要配套 tick 集成才能真正工作
- [ ] 每日调度器遵循 topic-selection-scheduler.js 模式：触发窗口检测 + working_memory 去重 + fire-and-forget
