# Learning: [SelfDrive] 内容选题自动化 MVP

**Branch**: cp-03312348-019a2866-b93d-436d-82e1-46b51a
**Date**: 2026-03-31

## 做了什么

- 创建 `packages/brain/src/routes/topics.js`：GET /api/brain/topics，支持默认今日 + ?date=YYYY-MM-DD 过滤
- 修改 `packages/brain/src/routes.js`：注册 topics 路由到 /topics 路径
- 创建 `tests/packages/brain/src/__tests__/topics-route.test.ts`：contract 测试（14个测试用例）
- 修改 `packages/brain/vitest.config.js`：增加 tests/packages/brain/** include，让 tests/ 下的测试被 brain vitest 扫描

### 根本原因

tests/ 目录下的测试文件（如 social-trending.test.ts）不在 brain vitest 默认扫描范围内（vitest 运行在 packages/brain/，默认只扫描 src/**），需要显式配置 include 才能被扫描。

### 下次预防

- [ ] 在 tests/packages/brain/ 路径新增测试时，确认 brain vitest.config.js 的 include 包含该路径
- [ ] tests/ 目录下的测试文件使用 `resolve(__dirname, '../../../../../packages/brain/src')` 路径（注意是5级上溯，不是4级）
- [ ] contract 测试不依赖 express/supertest，可以运行在没有 node_modules 的环境（路径计算用 __dirname + resolve）
