## Seal 文件 commit 到分支（2026-03-31）

### 根本原因

Sprint Contract 完成后 seal 文件（`.dev-gate-*` + `.sprint-contract-state`）只存在于 worktree 临时目录，没有 commit 进分支。

上下文压缩触发 worktree 消失后，这些防伪凭证全部丢失，devloop-check.sh 的 Gate 检查找不到 seal 文件，导致整个流程归零。

关键误区：认为"文件存在磁盘上"等于"文件已持久化"——但 worktree 是临时存在的，只有 `git commit` 才能真正保证文件跨 session 存活。

### 下次预防

- [ ] 任何防伪 seal 文件创建后，立即 git add + git commit（不能只是存在磁盘）
- [ ] sprint-contract-loop.sh exit 0 前执行 seal commit，确保收敛结果持久化
- [ ] Stage 2 Generator subagent 写完 `.dev-gate-generator.{BRANCH}` 后立即 commit
- [ ] Stage 4 pre-push 增加安全网：扫描未 commit 的 seal 文件并补提交
