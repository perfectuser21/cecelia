## dev-lock self-heal 放宽 + Subagent 全套回归（2026-04-14）

### 根本原因
1. PR #2338 self-heal 限制"HEAD 分支必须匹配"过于保守 → main session 无法治愈 cp-* worktree 的 dev-mode
2. PR #2338 的 Implementer 只跑新写的 test，没跑 tests/hooks/ 全套 → 漏了 T4 冲突
3. Spec Reviewer 模板没强制审查"Implementer 是否跑了全套回归"

### 下次预防
- [ ] self-heal 改用"活跃 worktree"判定（git worktree list），不用 HEAD 分支。双重保险：worktree 的 HEAD 也要匹配 dev-mode branch
- [ ] 02-code.md Subagent 模板强制"相关目录全套回归"映射表（改 hooks/ → 跑 tests/hooks/ 全套）
- [ ] Spec Reviewer 加"Implementer 是否明确报告跑了全套回归？没提就拒绝"检查
