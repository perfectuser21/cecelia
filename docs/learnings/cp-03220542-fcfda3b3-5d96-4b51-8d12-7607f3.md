### [2026-03-22] 重构 executeViaLlmIntent — 圈复杂度 33 → 分发器模式

**失败统计**：CI 失败 1 次（L1 Learning Format Gate — Learning 文件未随 push 提交），本地测试失败 0 次

#### 根本原因

Learning 文件必须在 **第一次 push 前** 就存在于提交中。本次重构完成后先 push、再写 Learning，导致 L1 Process Gate 的 Learning Format Gate 因找不到对应 Learning 文件而失败。

内存提示（MEMORY.md）明确写道："Learning 必须在第一次 push 前写好并加入 commit，不能事后补"，但执行时遗漏了这一步骤。

#### 下次预防

- [ ] Stage 2 完成（code_review_gate PASS）后，**立即写 Learning 文件并 commit**，然后才 push
- [ ] 顺序铁律：写代码 → 写 Learning → commit Learning → push → 创建 PR
- [ ] push 前用 `git log --oneline | head -3` 确认 Learning commit 已在本地
- [ ] Learning 文件命名：`docs/learnings/cp-MMDDHHNN-branch-name.md`，章节必须含 `### 根本原因` 和 `### 下次预防`

**影响程度**：Low（1 次 L1 CI 失败，补提交 Learning 后即可修复）
