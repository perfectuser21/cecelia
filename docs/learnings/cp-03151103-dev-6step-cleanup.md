### [2026-03-15] /dev 架构收尾——6步统一+步骤自检补全

**失败统计**：CI 失败 0 次，本地测试失败 0 次

### 根本原因

新老步骤文件并存（PR #957 只新增了6步文件，没有删老的12步文件）导致 SKILL.md 和 .dev-mode 格式不一致，Claude 读到哪套步骤不确定。

### 下次预防

- [ ] 新增步骤文件的 PR 必须同时删除被替代的老文件（一个 PR 完成新增+删除）
- [ ] SKILL.md 的步骤引用必须和 steps/ 目录实际文件保持一一对应
- [ ] 每次重组步骤后立即验证：`ls steps/*.md | wc -l` 数量符合预期

**影响程度**: Low（结构混乱但不影响运行，CI 0 次失败）
