# Learning - Cortex RCA 跨任务失败模式注入

**Branch**: cp-03110609-b0903cce-8721-48eb-85b2-9bd120
**PR**: #865

### 根本原因

`analyzeDeep` 原有的 Build #1（searchRelevantLearnings）只做语义搜索，不直接查询 `category='failure_pattern'` 的历史记录，导致 RCA 上下文缺乏系统级别的跨任务重复失败模式视角。

### 下次预防

- [ ] 为 cortex.js 新增上下文注入时，注意反思熔断（_checkReflectionBreaker）基于 `type+failure_class+task_type` 哈希，单元测试中多个测试用例若使用相同 task_type 会触发熔断导致后续测试失败——每个测试用不同 task_type（通过计数器递增）
- [ ] pool.query mock 中，精确匹配 SQL 时注意 `failure_pattern` 词出现在 `cortex_analyses` 表 SELECT 字段列表中，需用 `WHERE category = 'failure_pattern'` 精确匹配，而非仅检查 `failure_pattern` 关键词
- [ ] Learning 文件必须在第一次 push 前提交（与代码同 commit），否则 L1 Learning Format Gate 失败
