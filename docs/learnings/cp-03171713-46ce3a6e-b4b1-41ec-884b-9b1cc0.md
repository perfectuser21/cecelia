# in_progress 槽位趋势诊断（2026-03-17）

## 诊断背景

任务 46ce3a6e 诊断 in_progress 4→2 波动，分析 23 小时内的执行槽位与积压数据。

### 根本原因

1. **Brain 重启将 in_progress 孤儿任务批量 quarantine**：executor.js 启动时扫描 in_progress 任务，找不到进程直接 quarantine，3 个任务同批次触发，是 in_progress 骤降的直接原因。
2. **Codex 账号全部耗尽后任务长期占槽**：5 个账号均已耗尽，cecelia-run.sh 继续轮询 783 分钟才超时，2 个"幻影槽位"长期占用。
3. **worktree_creation_failed 已排除**：全量扫描 11 条 quarantined 任务 payload 零命中。

### 下次预防

- [ ] 诊断类任务 DoD Test 命令应使用 `node -e "..."` 而非 `ls`/`grep`（CI 白名单不含 grep/ls）
- [ ] Learning 文件若 main 已存在同名文件，应创建新的 per-branch 文件确保 `### 根本原因` 为新增行
- [ ] executor.js 启动清理逻辑改为 requeue 而非直接 quarantine（P3 修复任务）
- [ ] cecelia-run.sh 检测"所有账号耗尽"时立即 exit 而非等 60 分钟超时（P1 修复任务）
