### 根本原因

Brain ComplexityScanner 扫描 auto-learning.js 时记录了 `extractTaskSummary` 函数的圈复杂度为 21。
PR #1349 已完成主体重构（复杂度降至 7），但任务仍被派发（Brain 任务队列中残留过期数据）。
原函数未导出、缺少直接单元测试，本次补齐。

### 下次预防

- [ ] 任务派发前应检查函数当前复杂度是否已符合标准（去重逻辑）
- [ ] extractTaskSummary 等核心工具函数应通过 export 明确对外提供接口
- [ ] 降复杂度重构后应同步补充直接单元测试锁定行为
