## /dev 步骤自检 + 新文件同步（2026-03-15）

**失败统计**：CI 失败 0 次，本地测试失败 0 次

**影响程度**: Low

### 根本原因

/dev 反复产生同类 bug（Learning 格式错、假测试、版本漏 bump），根本原因是步骤文件只有文字说明，没有可执行的自检命令。Claude 读到说明后可能"理解了但执行时忘了"，CI 才是第一道拦截。等到 CI 失败再修，每次多花 15-30 分钟。

新步骤文件（02-code.md, 03-prci.md, 04-learning.md, 05-clean.md）已在引擎中创建（PR #957），但未同步到 `~/.claude/skills/dev/steps/`，导致 /dev 实际执行时仍走旧的 12 步格式。

### 下次预防

- [ ] 引擎步骤文件变更后，立即同步到 `~/.claude/skills/dev/steps/`（不能等下次用到再发现没同步）
- [ ] 在步骤文件的关键节点加可执行的自检 bash 命令，不能只靠文字说明
- [ ] Learning 格式自检在 push 前必须跑，不能"觉得格式对了"就直接 push
