# Learning: verify-step.sh Sprint Contract 四文件检查

## 任务

修复 verify_step1() 只检查 Test 字段格式而不验证 Sprint Contract seal 文件的漏洞。

### 根本原因

verify_step1() 原来只检查 `.dev-gate-planner` 是否存在。主 agent 可以填写任意 Test 字段，
然后创建 planner seal 文件绕过 Generator+Evaluator 对抗，直接标记 step_1_spec: done。
Sprint Contract 机制无实际约束力。

### 下次预防

- [ ] Sprint Contract Gate 新增 seal 时，同步在 verify_step1() 中加检查（不要只加生成逻辑）
- [ ] divergence_count=0 的橡皮图章检测：需配合 sprint-contract-state.round >= 2 的豁免条件，防止多轮收敛后最终 round 通过被误判
- [ ] 删除 state 文件再重跑 sprint-contract-loop.sh 会重置 round 计数器——保留 state 文件可以准确反映总轮次
- [ ] Engine hooks 修改必须同步：5 文件版本 bump + feature-registry.yml + generate-path-views.sh
