# Pipeline Rescue: cp-03251929-0efc4b12 — OKR 全树视图并行完成

## 任务摘要

Pipeline Patrol 检测到 `cp-03251929-0efc4b12` 卡在 step_2_code（超时 22 分钟），触发 rescue。

## 诊断结果

分支和 worktree 均已不存在：
- 本地/远程分支 `cp-03251929-0efc4b12-636b-4d98-ad4d-84057b` 均已删除
- Worktree `/worktrees/0efc4b12-636b-4d98-ad4d-84057b` 已被清理
- OKR 全树视图功能已通过 PR #1578 合并到 main（`25b7d87dc`）

Brain task `f05de1d6` 已由 engine 自动标记 completed。

### 根本原因

并行 agent 场景：同一任务被多个 agent 执行，先完成的 agent 合并 PR 并清理 worktree。
Pipeline Patrol 的 stuck-check 基于 .dev-mode 文件检测停留时间，未验证 worktree 目录是否存在。
已删除的 worktree 路径无法 cd 进入，导致 Patrol 误判为 step_2_code 超时阻塞，触发不必要的 rescue 任务。

### 下次预防

- [ ] Pipeline Patrol 的 stuck 检测应先验证 worktree 路径是否真实存在，不存在则跳过（worktree 删除 = 任务已清理）
- [ ] 或在 worktree 清理时同步更新 Brain task 状态为 completed，避免 Patrol 误报
