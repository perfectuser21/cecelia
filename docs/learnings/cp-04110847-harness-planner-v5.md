### 根本原因

harness-planner SKILL.md v4.1 缺少三项关键能力：
1. Step 0 只读代码文件，未调用 Brain API 获取 OKR/任务/PR/决策上下文
2. PRD 模板结构松散，无 User Stories、FR-xxx/SC-xxx 编号、独立假设/边界/范围章节
3. 无歧义自检机制，无 OKR 对齐章节

v5.0 升级通过合同驱动实现，所有验证命令均来自 GAN 对抗通过的 Round 3 合同。

### 下次预防

- [ ] 写 SKILL.md 时，验证命令中用到的字符串（如 `KR[\s\-:]`）必须在模板内精确存在
- [ ] OKR 对齐章节与 PRD 模板章节需放在同一代码块内，确保验证命令的文本提取逻辑能找到正确边界
- [ ] 9 类歧义自检类别名称必须与合同中列举的完全一致（如"UX"而非"用户体验"）
