## feat(capture): Capture Digestion Pipeline v1（2026-03-26）

### 根本原因

新增 Brain migration（199）后，`selfcheck.js` 中的 `EXPECTED_SCHEMA_VERSION` 和 `DEFINITION.md` 中的 `schema_version` 都需要同步更新，否则 push 时的 facts-check 会失败。本次 push 时遗漏了这两处更新，导致本地预检阻止推送，需额外两次 commit 修复。

### 下次预防

- [ ] 新建 DB migration 时，在同一 commit 中同步更新 `packages/brain/src/selfcheck.js` 的 `EXPECTED_SCHEMA_VERSION` 和 `DEFINITION.md` 的 `schema_version`，不要分开提交
- [ ] `contract:` 格式的 Test 字段需要在 regression-contract.yaml 中注册 RCI ID，对于新功能直接用 `manual:node -e` 验证文件内容/函数存在更简便
- [ ] migration 迁移文件命名注意检查现有最大编号（本次为 198 → 199），避免冲突
