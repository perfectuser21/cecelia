---
version: 1.0.0
created: 2026-03-11
branch: cp-03110624-d7f6ab9e-68c6-4756-b239-3e73bc
---

# Learning: DB migration quota_blocked + brain_config quota_reset_at

### 根本原因

- 并行 agent 竞争同一 migration 编号：多个 agent 同时基于旧 main（143）创建 migration 144/145，导致 `facts-check.mjs` 检测到编号冲突失败
- `brain_state` 表在 PRD 描述中提及但实际不存在，正确的 key-value 存储是 `brain_config` 表
- Learning 文件必须在 push 前准备好，不能在 CI 失败后补写

### 下次预防

- [ ] 开始写 migration 前，先 `git fetch origin main && git log origin/main --oneline` 确认最新 migration 编号，再决定自己用哪个编号
- [ ] PRD 提到 "brain_state 表" 时，先搜索 `grep -rn "brain_state" packages/brain/migrations/` 验证表是否存在
- [ ] Learning 文件必须和代码改动在同一个 commit push（或合并前 push），不能等 CI 失败再补
