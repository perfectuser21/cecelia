# Learning: arch_review 每4小时定时执行 + 路由到西安 Codex

### 根本原因

arch_review 无定时触发，只能手动调用，导致架构漂移无人发现。路由到 US 本机违背"分析/审查类任务归西安"原则。

### 下次预防

- [ ] 新增分析/审查类 task_type 时默认路由 `'xian'`，只有需要读本地代码执行的类型才用 `'us'`
- [ ] 定时调度器统一放在 `daily-review-scheduler.js`，参数可注入 `now`（测试友好）
- [ ] guard 条件（上次 review 后有 completed dev）通过两步 SQL 实现：先查历史，再查 dev 完成情况
- [ ] 去重窗口（4小时）与触发窗口（UTC hour % 4 === 0, minute < 5）独立判断，都要覆盖测试
