### [2026-03-12] migration 152 — brain_state 持久化全局 quota 冷却状态

**失败统计**：CI 失败 2 次，本地测试失败 0 次

**CI 失败记录**：

- 失败 #1（L3 Brain Unit Tests）：`EXPECTED_SCHEMA_VERSION` 更新后，3 个测试文件
  （`desire-system.test.js`、`learnings-vectorize.test.js`、`selfcheck.test.js`）
  中硬编码 `'151'` 的断言未同步更新。
  → 修复：将三处 `toBe('151')` 改为 `toBe('152')`。
  → 预防：每次修改 `EXPECTED_SCHEMA_VERSION` 时，**同步搜索**测试文件中的旧版本号：
    `grep -r "toBe('旧版本号')" packages/brain/src/__tests__/`

- 失败 #2（L1 Learning Format Gate）：未在第一次 push 前写 Learning 文件。
  → 修复：在测试修复后立即写 Learning 并 push。
  → 预防：按 /dev 流程，Learning 文件应在**首次 push 前**一并提交，不能拆成两次 push。

**错误判断记录**：
- 以为只需要改 `selfcheck.js` 版本号，没有检查测试文件中的硬编码版本断言 → 实际上每次
  migration 更新后，测试断言也需要同步更新。这类测试是版本锁定测试，不是会自动跟随的。

**影响程度**：Low

**预防措施**：
1. 修改 `EXPECTED_SCHEMA_VERSION` 时，立即运行：
   `grep -rn "EXPECTED_SCHEMA_VERSION\|toBe.*'[0-9]'" packages/brain/src/__tests__/`
   确认相关测试全部同步更新
2. Learning 文件和代码同批次 commit，不拆成多次 push，避免 L1 Learning Format Gate 失败
