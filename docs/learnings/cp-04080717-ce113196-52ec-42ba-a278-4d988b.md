---
branch: cp-04080717-ce113196-52ec-42ba-a278-4d988b
pr: 2096
date: 2026-04-08
---

# Learning: 内容生成引擎 v1 — 调度器启用 + 主题库集成

## 根本原因

`topic-selection-scheduler.js` 被 `DISABLED=true` 封锁，导致内容生成 KR 进度为 0%。
根本原因是历史上选题质量差（无 NotebookLM 数据源），但现在 `solo-company-case.yaml` 已配置 notebook_id，可以重新启用，配以精选主题库作为种子词保证选题方向。

## 下次预防

- [ ] 修改 `DISABLED` 常量时必须同步更新相关测试文件（本次影响了2个测试文件）
- [ ] 切换调度器状态（enable/disable）应视为 feat，需写测试覆盖新行为
- [ ] 新建主题库类文件时，注意 ESM 导出格式（`export const` 不能用 `module.exports`）
