## Pipeline Rescue: cp-03302008 content-pipeline-llm（2026-03-30）

### 根本原因

step_4_ship 阶段停留超过 15 分钟的原因是 CI 仍在运行（Brain Integration Shard 1/4 + Brain Unit Tests 两个 job 尚未完成），不是真正的阻塞，而是正常的 CI 等待。Pipeline Patrol 的超时阈值（15 分钟）比 CI 实际运行时间短，导致误报为 stuck。

### 下次预防

- [ ] Pipeline Patrol 对 step_4_ship 阶段的超时阈值应适当放宽（建议 30 分钟），以容纳 CI 较长的运行时间（Brain Integration 4 shard + Brain Unit Tests 合计可能超过 15 分钟）
- [ ] Rescue 任务应先检查 PR 的 CI 状态，若 CI 仍在 in_progress，直接等待而非触发复杂诊断流程
