# Learning: OKR 飞轮 Brain 侧 — okr-closer.js

## 任务概述

新建 `okr-closer.js` 为新 OKR 表（okr_initiatives / okr_scopes / okr_projects）实现完成检测飞轮，并集成到 tick.js。

### 根本原因

- 前序任务（`7ae083638`）已提交代码，但 DEFINITION.md 和 brain-manifest.generated.json 未同步更新，导致 facts-check 失败，push 被 precheck hook 阻止。
- Task Card 缺少 `[BEHAVIOR]` 类型 DoD 条目，DoD Verification Gate 和 Engine L1 均失败。
- Learning 文件未在第一次 push 前写好，Learning Format Gate 失败。

### 下次预防

- [ ] 新增 task_type 时，同步更新 DEFINITION.md（task_types 表 + action_count）并重新生成 manifest，在同一个 commit 中完成
- [ ] Task Card 必须至少包含一个 `[BEHAVIOR]` 条目，不能全是 `[ARTIFACT]`
- [ ] Learning 文件必须在第一次 push 前写好并加入 commit（格式要求：`### 根本原因` + `### 下次预防` + `- [ ]` checklist）

## 技术细节

- OKR 新表层级：key_results → okr_projects → okr_scopes → okr_initiatives → tasks（via okr_initiative_id）
- 检测逻辑与旧 initiative-closer.js 一致：total > 0 AND queued/in_progress/quarantine = 0 → 标记 completed
- 飞轮触发：initiative 完成 → okr_scope_plan 任务；scope 完成 → okr_project_plan 任务
- 新 task_type（okr_initiative_plan / okr_scope_plan / okr_project_plan）路由到 /decomp，location=xian（general B类）
