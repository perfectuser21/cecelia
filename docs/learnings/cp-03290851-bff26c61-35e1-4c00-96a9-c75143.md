## Playwright Evaluator 层 — Stage 3 端到端行为验证（2026-03-29）

### 根本原因

DoD Test 命令使用固定字符窗口（`c.slice(idx, idx+500)`）搜索代码内容，导致当锚点（`dev-gate-pw`）首次出现在注释区时，目标字符串（`return 2`）因超出窗口边界而无法匹配，verify-step.sh 失败。

并行 agent 场景：同一功能（Playwright Evaluator）被两个 agent 并行实现，本 PR 完成时 main 已包含等效实现（PR #1673/#1678），按流程直接关闭重复 PR。

### 下次预防

- [ ] DoD Test 中的内容搜索不要用固定字符窗口（`slice(idx, idx+N)`）而应用全文搜索（`c.includes(...)`）或用更精确的锚点字符串（锚定到唯一的代码片段而非可能出现在注释中的通用字符串）
- [ ] verify-step.sh 之前先本地手工运行所有 DoD Test 命令验证通过，确保锚点字符串在实际代码中唯一且位置正确
- [ ] 任务开始前先查 main 最新 commit 是否已有等效功能，避免重复实现
