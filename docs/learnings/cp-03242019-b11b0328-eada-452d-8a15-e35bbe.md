# Learning: 知识感知闭环 — context汇总接口 + Stop Hook summary

**Branch**: cp-03242019-b11b0328-eada-452d-8a15-e35bbe
**Date**: 2026-03-25
**PR**: #1557

## 实现内容

1. 新增 `GET /api/brain/context` 汇总接口（OKR + 活跃任务 + 近期决策）
2. 新增 `GET /api/brain/okr/current` OKR 进度接口（带百分比）
3. 新增 `POST /api/brain/consolidate` 主动触发对话压缩
4. Stop Hook v14.2.0：普通对话结束时触发 consolidate
5. `.claude/CLAUDE.md` 新增 Brain API 目录章节

## 根本原因

### 根本原因

Claude 每次对话启动需要重新探索系统状态，缺少一次性获取全局上下文的汇总入口。`conversation-consolidator` 依赖 tick 30分钟被动触发，无法保证每次对话结束都写入 session summary。

## 下次预防

- [ ] 修改 `packages/engine/hooks/` 下文件时，必须同时 bump Engine 版本（5文件）+ PR title 加 [CONFIG] + feature-registry.yml 新增条目
- [ ] `.claude/CLAUDE.md` 是系统敏感文件，只能用 Bash/node 方式写入，Edit/Write 工具会被权限拦截
- [ ] UNION ALL 跨子查询的 ORDER BY 在 PostgreSQL 中作用于整个结果集，语法正确
- [ ] Stop Hook 添加外部 curl 调用时，必须用 `--max-time 3 || true` 确保 fire-and-forget 不阻塞主流程
