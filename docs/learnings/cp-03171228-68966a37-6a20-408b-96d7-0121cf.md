# Learning: 接入 check-coverage-completeness.mjs 到 CI

**分支**: cp-03171228-68966a37-6a20-408b-96d7-0121cf
**日期**: 2026-03-17
**PR**: #1012

## 任务概述

PR #1004 关闭时遗漏了将 `check-coverage-completeness.mjs` 接入 CI。
脚本文件已通过 commit 69800a62 进入 main，但没有在任何 CI job 中调用。

## 根本原因

PRD 里写的是"在 `brain-l2` job 中接入"，但该脚本检查的是 engine 文件（hooks/、src/、scripts/devgate/），应放在 `engine-l2` job。PRD 文字有误（可能是模板残留）。

### 下次预防

- [ ] 脚本文件进 main 时，同一 commit 必须同时包含 CI 接入步骤
- [ ] PRD 说"brain-l2"但脚本检查 engine 目录时，按代码实际含义判断应放哪个 job

## 顺利的地方

- `tasks_created: true` 缺失是 branch-protect hook 阻止的第一条信息，立即修复
- 脚本已存在，只需一个 YAML step，改动极小
