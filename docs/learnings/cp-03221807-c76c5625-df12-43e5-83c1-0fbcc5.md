# Learning: 重构 reflection.runReflection（圈复杂度 39 → 9）

**分支**: cp-03221807-c76c5625-df12-43e5-83c1-0fbcc5
**日期**: 2026-03-22

---

### 根本原因

`runReflection` 函数承担了 6 项独立职责（静默期检查、记忆获取去重、LLM 调用、熔断器检测、Jaccard 相似度去重、写入 memory_stream），所有逻辑堆叠在同一函数体内，形成圈复杂度 39 的单体函数。重复的静默期写入逻辑（在熔断器和相似度跳过两条路径中各出现一次）进一步加剧了维护难度。

### 下次预防

- [ ] 新增涉及多条独立处理路径的函数时，若分支超过 3 条，优先拆分为职责单一的子函数
- [ ] 相同 DB 操作出现两次以上立即提取为辅助函数（`_enterSilencePeriodIfNeeded`、`_resetAccumulatorDB`）
- [ ] 重构前先确认所有测试通过（基线），重构后用相同测试验证行为不变

### 提取的子函数清单

| 函数名 | 职责 |
|--------|------|
| `_checkAndClearSilencePeriod(pool)` | 静默期检查/到期清除 |
| `_fetchAndDedupeMemories(pool)` | 记忆查询 + Jaccard 去重 |
| `_callLLMForInsight(memorySummary, accumulator)` | LLM 调用封装 |
| `_resetAccumulatorDB(pool, accumulator, reason)` | accumulator 重置（消除重复） |
| `_enterSilencePeriodIfNeeded(pool)` | 静默期写入（消除重复） |
| `_tokenize(text)` | 中英文分词（纯函数） |
| `_handleInsightDedup(pool, insight, accumulator)` | 完整去重逻辑（熔断器 + 相似度） |
| `_writeInsightToMemory(pool, insight)` | 写入 memory_stream + fire-and-forget |

重构后 `runReflection` 决策点：9（阈值 10）。
