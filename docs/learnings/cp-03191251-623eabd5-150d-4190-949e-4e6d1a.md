# Learning - 抖音发布脚本迁移到 cecelia skill

**Branch**: cp-03191251-623eabd5-150d-4190-949e-4e6d1a
**PR**: #1122
**任务**: 抖音图文+视频发布脚本从 zenithjoy 迁移至 cecelia douyin-publisher skill

### 根本原因

zenithjoy 中的抖音脚本使用 Playwright + 直连 Windows SSH 模式，而 cecelia 架构统一使用 raw WebSocket CDP + xian-mac 跳板 SCP 模式。直接移植会引入不兼容的依赖和不对齐的架构。正确做法是参照 kuaishou-publisher 的架构模式重写，内联 CDPClient 类，避免跨 skill 的 require 依赖。

### 下次预防

- [ ] 从外部仓库迁移脚本时，优先检查 cecelia 已有同类 skill（kuaishou/weibo）的架构模式，不直接复制原脚本
- [ ] 内联 CDPClient 类而非引用 `../../weibo-publisher/scripts/cdp-client.cjs`，避免跨 skill 路径在生产环境失效
- [ ] DoD 中 `[BEHAVIOR]` 的 manual: 测试命令只允许 `node`/`bash`/`curl`/`npm`/`psql`，禁止用 `echo` 假测试，用 `! node script.cjs 2>/dev/null` 验证非零退出
- [ ] `findImages()` 需显式过滤 `cover.*`，否则封面图会被当作内容图片上传
- [ ] packages/workflows/ 子树下开发：PRD/DoD 必须放两处（worktree根 + packages/workflows/），branch-protect.sh 就近检测
- [ ] Learning 文件必须在第一次 push 前写好并加入 commit，不能在 CI 失败后补写
