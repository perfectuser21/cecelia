# Learning: 清理孤立 initiatives，释放 slots

**分支**: cp-03211751-1b04c131-ce01-4038-9421-3bf64c
**日期**: 2026-03-21
**任务类型**: agent_ops — 自救方案 A

---

### 根本原因

KR1/KR2/KR3 全部被高层决策 CANCELLED 后，以下 3 个 initiatives 遗留为 `paused` 状态，占用系统 slots 但无法推进：

| Initiative | ID | 关联 KR |
|---|---|---|
| 发布流程集成（端到端） | ba4107d7 | KR3（已取消） |
| 素材组装+格式化（多媒体） | 35a9ef27 | KR3（已取消） |
| 选题+文案自动化（AI 驱动） | c6ae4112 | KR3（已取消） |

这些 initiatives 在 `projects` 表中 `type='initiative'`，其关联 KR（`fedab43c`）状态为 `cancelled`，但 initiatives 本身未同步更新，导致 slots 被占用。

### 下次预防

- [ ] KR 变更为 CANCELLED 时，应自动级联更新其下所有 paused/pending initiatives 为 `archived`
- [ ] Brain 的 capacity 计算应排除 `archived` 状态的 initiatives
- [ ] 建议在 Brain tick 中加入孤立 initiative 检测（KR cancelled 但 initiative active/paused）

### 操作记录

```sql
UPDATE projects
SET status = 'archived', updated_at = NOW()
WHERE id IN (
  'ba4107d7-f5f4-4246-b37f-2bcc28a8de0c',
  '35a9ef27-eea9-470b-84e5-437eb40c208f',
  'c6ae4112-81a9-4fec-be04-4fef0ae5742b'
);
-- UPDATE 3
```

**执行后状态**：3 个 initiatives 状态从 `paused` → `archived`，slots 释放成功。
