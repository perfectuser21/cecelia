# Learning - Cortex Stats API 路由单元测试补充

**Branch**: cp-03122229-0be66616-38a2-4713-a66c-401fc8
**PR**: #922

## 根本原因

cortex LLM 计时可观测性功能（PR #914）实现了 `estimateTokens`、`callCortexLLM` 计时逻辑、`logCortexDecision` → `decision_log.metadata` 写入、以及 `GET /api/brain/cortex/stats` 端点，但 `/cortex/stats` 路由缺少专项单元测试。单元测试 `cortex-timing.test.js` 只覆盖了 `estimateTokens` 和 `callCortexLLM`（底层函数），没有覆盖上层 HTTP 路由处理器的参数解析、数值转换和错误处理逻辑。

## 下次预防

- [ ] 添加新 API 路由时，同步在 `__tests__/` 目录创建对应路由测试（路由参数限制、正常响应、DB 失败三个场景）
- [ ] routes.js 新增端点 → checklist：单元函数测试 ✅、路由测试 ✅、DoD 验收条目 ✅
- [ ] `vi.clearAllMocks()` 与 `vi.resetAllMocks()` 区别：使用 `mockResolvedValueOnce`/`mockRejectedValueOnce` 时 `clearAllMocks` 足够；使用 `mockResolvedValue`/`mockImplementation` 时需 `resetAllMocks` 防止跨测试泄漏
