## librarian 首批P0知识单元生成 + BACKLOG.yaml 初始化（2026-03-17）

### 根本原因

本次开发流程顺畅，CI 0次失败，本地验证 0次失败。
前序 agent 已完成 HTML 知识页生成（存于 ~/claude-output/knowledge/），本次任务只需将 BACKLOG.yaml 提交到 git。
任务在被重试时工作已完成，只缺少 git commit/PR 闭环。

### 下次预防

- [ ] librarian 生成的 HTML 文件存于 ~/claude-output/knowledge/，不进 git；只有 docs/knowledge/BACKLOG.yaml 进 git
- [ ] docs/knowledge/ 首次进入 git 时需新增目录（当前为 untracked），用 `git add docs/knowledge/` 而非 `-u`
- [ ] 当任务被标记为重试但实际工作已完成时，检查 BACKLOG.yaml 状态和 HTML 文件是否存在，避免重复生成
