---
id: arch-review-20260814
version: 1.0.0
created: 2026-08-14
updated: 2026-08-14
reviewer: Codex (Brain 自动调度)
task_id: c5858ab3-32b0-482a-85fd-fb45f3ab01f7
base_sha: 23ceafea359ce956e70d14ee0af7a45787094290
---

# Architecture Review — 2026-08-14 daily（04:00 UTC，04:20 UTC 执行）

## Executive Summary

裁决仍为 **CRITICAL**：12 项高风险、18 项中风险。上一轮巡检之后，`origin/main` 只有 2026-08-13 16:00 巡检报告提交，没有新的 Brain 运行时代码；production 与 staging 继续运行 `1.273.3@813dc703`，两容器健康，production breaker 全 CLOSED。三项 DevGate 全绿，9 个执行/恢复关键测试文件共 233 项测试通过；当前 `arch_review` 只有一组 Codex 进程和一个带 run-id 的文件锁，没有再次出现 Preview 双执行。

运行合同仍未收敛。当前 task 的 run-id 只存在于 `/tmp/codex-review-locks`，tasks 表没有 `current_run_id/current_run_status` 列且 heartbeat 为空；上一轮 CRITICAL task 在约 8.5 小时内累计写入 37 条不同 run-id 的 PASS callback。staging 虽有 `CECELIA_TICK_HARD_OFF=1`，过去 12 小时仍执行约 72 轮 Promotion，并持续运行 arch-review guard；production/staging health 还返回完全相同的 Tick 时间。

部署漂移探针新增一项确定性高风险：两份 `.env.staging` 都声明 `DB_HOST=localhost`，当前 staging 容器却运行在 `host.docker.internal`，而 `scripts/staging-deploy.sh:150` 的 compose 启动没有 `--env-file .env.staging`。当前正确值依赖调用者 shell，干净重部署会改变数据库目标。测试真值也继续分裂：标准 Vitest 配置排除 `daily-review-scheduler.test.js`，强制收集后 202 项中 1 项真实失败，死测试仍断言 arch-review 应路由到 `xian`，而生产代码与当前任务均为 `us`。

## 4A Drift Findings

| 检查项 | 状态 | 04:00 UTC 发现 |
|--------|------|----------------|
| Boundary Drift | ⚠️ | `packages/brain/src/decision-executor.js:17` 与 `routes/analytics.js:5` 仍直连 Cortex，L1/L2 例外边界没有机器合同；本轮无新增跨层依赖。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:46,104-107` 仍写 Brain `1.216.0`；`packages/brain/DEFINITION.md:3` 为 `1.273.1`，运行/包版本为 `1.273.3`。 |
| Routing Integrity | ⚠️ | `task-router.js:101,249`、当前 task 与回归测试都证明 `arch_review → /arch-review review → us`；但 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 仍为 404，能力加载缺可观测入口。 |
| Core Module Complexity | ⚠️ | Brain 非测试 JS 为 640 文件/176330 行；`executor.js` 4615L/30 imports、`routes/execution.js` 4099L/33、`routes/ops.js` 3024L/19。自上一轮无代码增长。 |
| Concurrency & State Safety | 🔴 | `tick-loop.js:69-70` 仍在 60 秒后只释放共享布尔锁，不 cancel/join 旧执行；review 的文件 run-id、DB identity、heartbeat 与 callback 继续分裂。 |
| Temp Patch Accumulation | ⚠️ | 生产代码仍有多批超过 7 天的 TODO：`report-48h.js:119`（2026-04-24）、`sprint-result-contract.js:37-64`（2026-06-20）、`executor-contracts.js:305`（2026-07-10）等；根目录还跟踪任务特定 `.task` 与 `.brain-result.json`。 |
| Ledger Freshness（探针A） | 🔴 | MJ5 Journey 仍停在 2026-07-17；按 payload journey_id 的最近 completed task 为 2026-08-10，落后 24.1 天，GitHub 搜索还匹配到 2026-08-11 merged #4779。ZenithJoy 客户管理按任务兜底落后 19.6 天。 |
| Smoke Consistency（探针B） | ⚠️ | production active Journey 23 条，17 条声明 E2E；跨 Cecelia 与 `perfectuser21/zenithjoy-workspace` 核对为 12 存在、4 缺失、1 TBD、6 为空。`golden-path-2` 中“飞书”仅为迁移说明，实际 smoke 已测本地链路，未发现仍执行废弃方案。 |
| Deploy Config Drift（探针C） | 🔴 | staging 运行时 `DB_HOST=host.docker.internal`，两份 `.env.staging` 与 compose 默认均为 `localhost`，启动命令又未带 `--env-file`；production 两份 `.env.docker` 整体不同，主仓缺 `CECELIA_TICK_ENABLED`、deploy-main 为 `false`。关键运行值与持久化推导值不一致。 |

## 4B Structural Health Findings

- **环境生命周期** 🔴：staging hard-off 只覆盖主 Tick。`packages/brain/server.js:954-956` 无条件启动 Promotion loop；12 小时日志有 360 条 PromotionJob 行与 15 条 arch-review guard 行。production/staging health 的 `last_tick` 完全相同，health 无法证明实例归属。
- **review 身份与终态** 🔴：当前锁文件记录 run `9bcbb25a-...`，DB 只有 `claimed_by=brain-tick-7` 且 `driver_heartbeat_at=NULL`；上一 task `9797c817-...` 有 37 条 callback/37 个 run-id，verdict 全为 PASS，而报告裁决为 CRITICAL。`executor.js:2573` 仍只解析 `PASS|FAIL`。
- **时间完整性** 🔴：当前 task 的 API `started_at=2026-08-13T15:00:44Z`、`claimed_at=2026-08-14T04:00:44Z`，相差约 13 小时；物理 schema 继续混用 timestamp 与 timestamptz。
- **schema 权威** 🔴：production/staging 最大 migration 都是 415，但 ledger 行数为 408/405，413/414 description 不同；production 有 `tasks.retry_count`，staging 无。版本号一致仍不代表内容或物理 parity。
- **Controller ownership** 🔴：本轮无 active `initiative_runs`，因此上一轮 lease=deadline 异常没有继续暴露；源码仍只有 ownership 写入与过期扫描，没有 heartbeat 续租路径，既有 P0 未获关闭证据。
- **调度测试收集** ⚠️：执行/恢复关键 9 files / 233 tests PASS。标准 `packages/brain/vitest.config.js:109` 仍排除 `daily-review-scheduler.test.js`；用独立 config 强制运行 4 files / 202 tests 时 201 PASS、1 FAIL，失败在该文件 `:721-733` 的 `location=xian` 旧断言。
- **依赖与结构债** ⚠️：`npm audit --omit=dev` 为 58 moderate、2 high、0 critical；隔离 worktree 仍需补 `js-yaml`、Rollup musl 与匹配的 esbuild 原生包才能完整运行门禁/测试。
- **CI/发布真值** ⚠️：Smoke E2E、Nightly Regression、Integration Nightly 最近 10 次均全失败；Real-Machine 最近 10 次为 9 成功/1 失败。最新 scheduled Deploy `31768152665` 因 nightly 证据失败，Daily Drill `31766668650` 也失败；普通 CI/Gate 3/CodeQL 仍可保持绿色。
- **积极信号** ✅：当前只有一个 review 执行进程；Preview 主 Tick守卫未见回归；production/staging 同镜像同 SHA；24 小时 heartbeat-sentinel Issue 查询为 0；当前无 active Kernel Run。

## Severity

- 🔴 High: 12 项（文档正本、环境生命周期、review 身份、verdict/callback、dev→harness 路由、Controller ownership、Tick 并发、时间类型、recovery identity、schema 内容/物理 parity、MJ5 ledger、部署配置漂移）
- ⚠️ Medium: 18 项（边界、Map 遗留、复杂度、schema consumer、依赖、`/dev` 可移植性、根目录产物、ledger 时窗、Journey E2E、Preview deploy、Kernel CI、skill 可观测、语音复制、Janitor、scheduler 收集、guard-drill、nightly 真值、环境 health 语义）
- ✅ Clean: 10 组（production health/breaker、三项 DevGate、233 项聚焦回归、当前执行唯一、Preview 主 Tick守卫、migration 编号 415、同 SHA 运行、Map 生产权威、registry/ledger 可读、24h 无新 guard-drill）

## Required Actions

- [ ] **P0** — 用实例级 lifecycle mode 一次性关闭非生产主 Tick、Nightly、scheduler、projection、promotion 与外部 executor；health 必须按实例报告 loop 状态和统计来源。
- [ ] **P0** — review 使用 DB 原子 claim + environment-scoped run-id/heartbeat/callback；CRITICAL/未知裁决 fail-closed，callback 验证 HTTP 与 CAS，单 task 只允许一个权威终态。
- [ ] **P0** — 固化 staging DB_HOST 的唯一持久化值，`staging-deploy.sh` 显式使用 `--env-file .env.staging` 并在启动后对比运行时；统一主仓/deploy-main 的 production env 关键键。
- [ ] **P0** — migration ledger 增加内容 hash/合并来源，启动 parity gate 同时验证 version、description/hash、物理对象与 consumer；补齐 staging `retry_count`。
- [ ] **P0** — Controller heartbeat 事务续租并记录 lease generation；active run 的 lease、deadline、TTL 与 orphan recovery 必须有真实 PostgreSQL lifecycle 回归。
- [ ] **P0** — canonical dev→harness 路由前移到 admission 前；完成 tasks 全量 timestamptz 与 Tick cancel/join/代际合同。
- [ ] **P1** — 修正 `daily-review-scheduler.test.js` 的 us 路由断言并移出 Vitest exclude；CI 必须校验显式测试文件全部被收集。
- [ ] **P1** — 修复 Journey E2E registry、MJ5/客户管理台账、SYSTEM_MAP/DEFINITION、`/arch-review` 能力 API，并统一 nightly/Release/Deploy 成功语义。
- [ ] **P2** — 收敛 Cortex 边界、God Module、OpenTelemetry 漏洞、语音复制、Map legacy、旧 TODO 与根目录运行产物。

## Verification Evidence

- base/runtime：`origin/main=23ceafea`，运行代码 SHA 为 `813dc703`；两容器均 `1.273.3` 且 healthy，差异仅为 2026-08-13 巡检文档提交。
- DevGate：facts-check PASS、version sync PASS；DoD mapping 在复用同平台确定版本 `js-yaml` 后 PASS。
- tests：执行/恢复关键 9 files / 233 tests PASS；强制收集 scheduler/routing 4 files / 202 tests 为 201 PASS、1 FAIL。
- current review：task `c5858ab3-...`，单组 Codex 进程；lock run `9bcbb25a-...`；DB heartbeat 空且无 current_run 列。
- callback：上一 task `9797c817-...` 共 37 callbacks / 37 run-id / 全 PASS，持续约 8.5 小时。
- lifecycle：staging hard-off=1；12 小时 Promotion 360 行、arch-review 15 行；两实例 health 同一 `last_tick`。
- schema：production/staging max=415，ledger=408/405；413/414 description 分叉；`retry_count` 仅 production 存在。
- deploy config：两份 staging env 相同但与运行 DB_HOST 不同；production 两份 env 整体不同；GitHub repo variables 查询成功且为空；当前环境无 launchd。
- ledger/smoke：MJ5 落后 24.1 天；active Journey 23 / E2E 17 / 存在 12 / 缺失 4 / TBD 1 / 空 6。
- CI：三条 nightly 最近 10 次全红；Real-Machine 9/10 绿；最新 scheduled Deploy 与 Daily Drill 失败。
- dependency/code：audit 58 moderate / 2 high / 0 critical；Brain 非测试 JS 640 文件/176330 行。
- issues：更新 `b6a0dd31`、`0acbf8f3`、`a9a6e3f6`、`6adfa549`、`72546baf`、`41f68c38`、`8a782f87`、`5f3f0d07`、`925aa82e`、`af7361b3`；没有重复建单。

## ADR Needed? 是

需要定义五项跨模块合同：实例级零副作用生命周期、review 的 environment/task/run/callback 唯一身份、部署关键配置的持久化推导与运行时对账、migration 内容/物理 parity、以及 Controller lease generation 与恢复所有权。

## Escalate to /architect? 是

问题跨启动脚本、compose、executor、scheduler、dispatcher、DB migration、Kernel lifecycle、CI 与多环境；本巡检按职责只记录事实、更新既有 Issues 与报告，不直接修改 runtime。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 34,
  "health_findings": 8,
  "high_severity": 12,
  "medium_severity": 18,
  "issues_created": 0,
  "issues_updated": 10,
  "escalate_to_architect": true,
  "summary": "当前 arch_review 已恢复单执行，但 run-id/heartbeat/CRITICAL callback 仍无权威终态；staging hard-off 继续运行 Promotion，且 DB_HOST 运行值依赖调用者环境、与持久化源不一致；schema parity、时间类型、Controller lease、Journey 台账与 nightly/release 真值仍未闭合，标准配置排除的 scheduler 测试已真实失败"
}
```
