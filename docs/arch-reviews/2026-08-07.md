---
id: arch-review-20260807
version: 1.0.0
created: 2026-08-07
updated: 2026-08-07
reviewer: Codex (Brain 自动调度)
task_id: 5957d8e8-842d-4d17-bfad-570a9693fdc4
base_sha: 305180766decbffbfeadb8b5b33eb67482ac7832
previous_task_id: f981ba10-49b1-477c-a780-6307e2943fcc
---

# Architecture Review — 2026-08-07 daily（12:00 UTC）

## Executive Summary

裁决仍为 **CRITICAL**，高风险项由 3 项增至 4 项。`arch_review` 执行活性保持稳定：本任务于 12:03:50 UTC 被 claim，`/tmp/codex-review-locks/<task_id>.lock` 同时落盘，巡检期间持续为 `in_progress`、`retry_count=0`、`error_message=null`；生产 Brain 为 `healthy`，两个 circuit breaker 均为 `CLOSED`，且 `1.270.0@30518076` 与当前 main 完全一致。

昨日三项高风险未收敛：tick 仍在 60 秒后只释放共享锁而不取消旧执行；`tasks` 生命周期字段仍混用无时区和带时区类型；`SYSTEM_MAP.md` 的正文仍停在 Brain `1.216.0` / Schema `262`，实际已到 `1.270.0` / `393`。生产自本次部署启动后又出现两次 `FORCE-RELEASE`（10:29:26、12:08:50 UTC）；这两次旧 tick 都在释放后约 61ms 内结束，未观察到新一轮重入，但旧代码仍允许下一次 5 秒 loop 在旧执行存活时进入，昨日已实证的并发重叠条件没有被消除。

本轮新增一项恢复合同断点：`recovery-loop.js` 的 `defaultFetchInProgress()` 没有查询 `executor_kind`，却把结果交给 `assessTaskLiveness()`。生产日志因此把数据库中明确为 `codex-review-local` 的本任务记录成 `executor_kind=null (legacy) — fail-open`。这不会误杀当前 review（另一路 `probeTaskLiveness()` 仍用 lock 保护），但会让 recovery-loop 宣称的 wall-clock 超时安全网对所有非 harness 任务空转。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 已登记。

治理账本也仍未形成有效观测面：最新 19 条 active line ledger 的六个段落全部为“暂无”，而同一 24h 内已有 11 个 completed task、main 合入 9 个 PR；账本卫生分显示归属完整率仅 26%，欠账 459，连续第 3 天击穿。PR #4696 已合并口径修正，但产生在本轮账本之后，真实改善要以下一轮数据验证。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | L0 服务仍在 `report-48h.js:144`、`monitor-loop.js:483`、`dispatch-helpers.js:206`、`capability-probe.js:166` 直调 `cortex.js`；`decision-executor.js:17` 还静态依赖 Cortex 白名单，HTTP 路由 `routes/analytics.js:5/234`、`routes/ops.js:2147` 也直连 Cortex。`thalamus.js:1607` 才是标准 L1→L2 升级路径。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md` 的 frontmatter 为 2026-08-06，但正文仍写 Brain `1.216.0`、Schema `262`、139 个 migration；当前事实为 Brain `1.270.0`、Schema `393`。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 仍为 In progress。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us`、`executor_kind=codex-review-local` 及 lock 探活接线正确；`arch_review_followup` 已只存在于历史巡检报告，未形成当前可执行合同。AGENTS 声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 仍真实返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4609L/30；`planner.js` 1247L/4；`thalamus.js` 1807L/6；`cortex.js` 1593L/13。上一轮后 `executor.js` 又增加 62 行并增加 1 个静态 import，继续承担主要复杂度。 |
| Concurrency Safety | 🔴 | `tick-loop.js:65-108` 仍在 60 秒后清 `tickRunning/tickLockTime`，没有取消、join 或代际隔离旧 `doTick()`。当前生产启动后两次走到该分支；虽都在释放后约 61ms 完成而未观测到重入，昨日约 3 分 15 秒的真实重叠根因仍在。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`。 |
| Temporal Integrity | 🔴 | `created_at/started_at/updated_at/queued_at/completed_at` 仍为 `timestamp without time zone`，`claimed_at/driver_heartbeat_at` 为 `timestamptz`；DB 时区为 `America/Chicago`。本任务 SQL 内 claim delay 为 `2m51.207s`，API 却展示 created `2026-08-06T23:00:59Z`、claimed `2026-08-07T12:03:50Z`，表面相差 13 小时。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js:48-53` 未 SELECT `executor_kind/last_attempt_at/claimed_by`，`autoFailTimedOutTasks()` 随后统一得到 `unknown/no_executor_kind` 并 fail-open。生产已对本任务打印该日志；因此 wall-clock 超时网无法收口任何非 harness 卡死任务。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`。 |
| Dev Workflow Portability | ⚠️ | `/dev` 唯一 worktree 入口 `worktree-manage.sh:120` 仍执行 `flock -w 5 201`；当前 `/usr/bin/flock` 为 BusyBox 1.37，真实返回 `unrecognized option: w`、exit 1。本任务只能按协议显式建立隔离 worktree。Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7`。 |
| Temp Patch Accumulation | ⚠️ | 超过 7 天的结构债仍在：`report-48h.js:119`（`mock_cortex`）、`sprint-result-contract.js:37-64`、`trace-routes.js:237/243`、`decision-executor.js:225/304`、`alertness/healing.js:448/475/1037`、`thalamus.js:847`（重复失败分类表）。 |
| Ledger Integrity（探针A） | ⚠️ | 19 条 active journey 均按时生成 line ledger，但 19/19 的六段全为空；最新卫生分为归属完整率 26%、欠账 459，连续第 3 天击穿。Issue `6c6dce69-2841-4872-b16b-80bfac5c5f6e` 已随 PR #4696 关闭，但其真实数据效果尚未出现。 |
| Smoke Consistency（探针B） | ⚠️ | 19 条 active journey 中 17 条有 E2E 声明；逐条核查 `cecelia/main` 与 `zenithjoy-workspace/main` 后仍是 12 条存在、4 条缺失、1 条 TBD，合计 5 条非法，较昨日无改善。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 的标题仍停在旧基线 14/9。 |
| Deploy Config Drift（探针C） | ⚠️ | 生产为 `healthy`、`1.270.0@30518076`，与 main 完全一致；staging 为 `degraded`、`1.268.0@e6645c99`，scheduler/hard-off 符合隔离设计，但版本落后且 notifier/docker runtime 未配置。主仓与部署仓 `.env.docker` key-set 仍差 3 项：`ACCEPTANCE_API_TOKEN`、`ACCEPTANCE_PUBLIC_HOST`、`CECELIA_TICK_ENABLED`。 |

## 4B Structural Health Findings

本次为 daily 轻量扫描：

- 上一轮 12:00 UTC 巡检后，`packages/brain/src` 的 JS/TS 文件数从 1995 增至 2027（+32）；全仓 152 个文件、`+10558/-774`。
- 新增 6 个生产模块：`acceptance-spec.js`、`acceptance-state.js`、`lib/task-event-log.js`、`routes/acceptance-ai.js`、`routes/acceptance-gates.js`、`routes/acceptance-review.js`，均有对应 unit/integration test；新增 acceptance 状态与公开路由的测试覆盖较完整。
- 主要生产代码 churn 为 `routes/acceptance-ai.js`（+208）、`acceptance-state.js`（+180）、`routes/acceptance.js`（+145/-19）、`routes/acceptance-review.js`（+142）、`acceptance-spec.js`（+134）、`routes/acceptance-gates.js`（+111）与 `harness-judge.js`（+108/-10）。
- `executor.js` 在本轮继续增长到 4609 行；同时 recovery-loop 与 executor 内置 liveness probe 对同一 review 任务形成两套不一致的字段投影与日志语义，说明统一 executor contract 尚未真正成为所有恢复路径的输入 SSOT。
- 根目录仍跟踪任务特定的 `.brain-result.json` 与 `.task`；前者固定指向旧任务 `93161b22-...`，后者仍写 migration 164 修复，可能被后续执行误读。

## Severity

- 🔴 High: 4 项（tick 超时互斥失效、tasks 时间契约分裂、recovery 超时网空转、CURRENT_STATE 假新鲜）
- ⚠️ Medium: 8 项（边界、路由/API 文档、复杂度、`/dev` 可移植性、TODO、line ledger、E2E 声明、部署源/暂存版本）
- ✅ Clean: 4 项（arch-review lock 活性、DEFINITION facts、版本同步、生产 Compose/runtime/main 一致）

## Required Actions

- [ ] **P0** — 处理 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：为 tick 引入可传播的取消/截止合同，或在旧执行真实终止前保留全局租约；回归测试必须断言旧 tick 仍存活时新 tick 不得进入。
- [ ] **P0** — 处理 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一 `tasks` 生命周期字段为明确 UTC/timestamptz 契约，补 PostgreSQL 非 UTC + Node 不同时区测试，并审计所有 `Date(row.*_at)` 消费点。
- [ ] **P1** — 处理 Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`：让 recovery-loop 查询完整 executor contract 字段，补生产等价测试证明活执行不杀、死/超龄执行按合同收口。
- [ ] **P1** — 处理 Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`：重建 `SYSTEM_MAP.md` 当前事实，并增加正文 facts 机器校验；同步修正 AGENTS 的 skills API 与已消失的 followup 合同描述。
- [ ] **P1** — 以下一轮 line ledger 验证 PR #4696：归属完整率和 19/19 空账本必须出现真实改善；若无改善，重开 `6c6dce69-...` 并把 journey/task/run 锚点完整性纳入入库门禁。
- [ ] **P1** — 处理 Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7` 与 `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`：修复 BusyBox/GNU worktree 锁兼容，并逐条修正 5 个 active journey 非法 E2E 声明。
- [ ] **P2** — 定义 L0/HTTP 路由直调 Cortex 的豁免边界；继续拆分 `executor.js`，清理根目录任务结果产物与超期 TODO。
- [ ] **P2** — 明确生产/暂存版本差和双 `.env.docker` 的唯一配置 SSOT，使 staging 落后与 degraded 原因进入可观测告警。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.270.0`、Schema `393`、migration 无冲突、LLM fetch timeout 完整）。
- `bash scripts/check-version-sync.sh`：PASS（四处版本均为 `1.270.0`）。
- `node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- 生产真实状态：Brain `healthy`、`1.270.0@30518076`，任务 lock 新鲜且无 retry/error，`dispatch`/`cecelia-run` circuit breaker 均关闭；生产镜像与 main 一致。
- 生产日志：10:29:26、12:08:50 UTC 两次 `FORCE-RELEASE via setTimeout`，对应旧 tick 分别在约 69ms、61ms 后结束；12:05 recovery-loop 对本任务输出 `executor_kind=null (legacy) — fail-open`。
- 生产 DB 只读探针：Schema `393`；tasks 七个时间字段类型、DB 时区、SQL claim delay 与 API 序列化值均已实测；本任务数据库 `executor_kind=codex-review-local`。
- line ledger 只读探针：20h 内 19 条 active ledger 全部生成、19/19 六段全空；卫生分归属完整率 26%、欠账 459；24h 内 tasks completed=11。
- GitHub/main 内容核查：17 条 active E2E 声明中 12 条有效、4 条缺失、1 条 TBD；过去一轮 main 合入 9 个 PR。
- BusyBox 实测：`flock -w 1 201` 返回 `unrecognized option: w`、exit 1。

## ADR Needed? 是

需要明确三项治理决策：tick 超时后的取消/互斥租约合同、任务生命周期时间的唯一类型/时区合同，以及 L0/HTTP 路由直调 Cortex 的允许边界。recovery-loop 字段投影缺失属于实现缺陷，不需要另立 ADR。

## Escalate to /architect? 是

tick 取消传播、时间类型迁移与消费端兼容、CURRENT_STATE 正本和 Cortex 边界涉及跨模块契约；本巡检只记录事实与风险，不在此 PR 中设计或修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 12,
  "health_findings": 5,
  "high_severity": 4,
  "escalate_to_architect": true,
  "summary": "生产与 arch-review 当前可运行，但 tick 超时互斥、tasks 时间契约和 CURRENT_STATE 正本仍未收敛；新发现 recovery-loop 漏传 executor_kind 使 wall-clock 超时安全网整体 fail-open"
}
```
