---
id: arch-review-20260807
version: 1.1.0
created: 2026-08-07
updated: 2026-08-07
reviewer: Codex (Brain 自动调度)
task_id: 273e6dc0-01b5-4a94-89d8-585834b15117
base_sha: 0c893a7ffbf5f4581b7195660ebf5db59a3fcaf1
previous_task_id: 5957d8e8-842d-4d17-bfad-570a9693fdc4
---

# Architecture Review — 2026-08-07 daily（16:01 UTC）

## Executive Summary

裁决仍为 **CRITICAL**，维持 4 项高风险、8 项中风险，没有新增未覆盖风险。生产 Brain 为 `healthy`，版本 `1.270.1@0c893a7f` 与 main 完全一致，两个 circuit breaker 均为 `CLOSED`；staging 已从上一轮的 degraded 恢复为 `healthy` 并升至 `1.270.0`。当前 review 的 `codex-review-local` lock 在 claim 时同步落盘，任务保持 `in_progress`、`retry_count=0`、`error_message=null`。

上一轮四项高风险均未收敛：tick 仍在 60 秒后只释放共享互斥状态而不终止旧执行；`tasks` 生命周期字段仍混用无时区和带时区类型；`SYSTEM_MAP.md` 正文仍停在 Brain `1.216.0` / migration `262`；`recovery-loop.js` 仍未投影 `executor_kind`。本轮生产日志再次对当前任务连续输出 `executor_kind=null (legacy) — fail-open`，而数据库中该任务实际为 `codex-review-local`，证明 wall-clock 超时网空转不是历史残留。

12:00 UTC 报告后 main 仅新增一组 Golden Path/Issue API 修复及对应测试，没有触碰上述四项根因，也没有引入新的跨层依赖。生产自本次部署后未出现 `FORCE-RELEASE`，所有可见 tick 均正常结束；这只能说明当前观测窗未复发，不能消除 `tick-loop.js:65-94` 仍允许旧执行存活时重入的代码事实。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | 12:00 后变更只涉及 `routes/golden-paths.js` 与 `routes/journeys.js`，未新增 Cortex 直连；既有越层仍在 `report-48h.js:144`、`monitor-loop.js:483`、`dispatch-helpers.js:206`、`capability-probe.js:166`、`decision-executor.js:17`、`routes/analytics.js:5/234`、`routes/ops.js:2147`。标准 L1→L2 升级入口仍是 `thalamus.js:1607`。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:46/65/79/104-107` 仍写 Brain `1.216.0`、139 个 migration、Schema `262`；当前事实为 Brain `1.270.1`、Schema `393`。PR #4708 更新了 DEFINITION 与 API 实现，但未修 CURRENT_STATE 正文。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 仍为 In progress。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us`、`executor_kind=codex-review-local` 与 lock 接线可用；`arch_review_followup` 仍不在当前 task type 合同中。AGENTS 声明的 `/api/brain/skills/count` 和 `/api/brain/skills/arch-review` 继续返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4609L/30；`planner.js` 1247L/4；`thalamus.js` 1807L/6；`cortex.js` 1593L/13。12:00 后五个核心文件均未变化，`executor.js` 仍是主要 God Module。 |
| Concurrency Safety | 🔴 | `tick-loop.js:65-94` 仍在 60 秒后清 `tickRunning/tickLockTime`，没有取消、join 或代际隔离旧 `doTick()`。新容器观测窗内未出现 `FORCE-RELEASE`，但已登记竞态根因未变。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0` 仍为 In progress。 |
| Temporal Integrity | 🔴 | public `tasks.created_at/started_at/completed_at/updated_at/queued_at` 仍是 `timestamp without time zone`，`claimed_at/driver_heartbeat_at` 是 `timestamptz`，DB 时区为 `America/Chicago`。本任务 SQL 内 claim delay 为 `27.047s`，API 却把 created/claimed 序列化成表面相差 13 小时。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b` 仍为 In progress。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js:48-53` 仍只 SELECT `id/title/status/priority/started_at/updated_at/payload/task_type`，遗漏 `executor_kind/last_attempt_at/claimed_by`。生产每 5 分钟对当前任务输出 `executor_kind=null (legacy) — fail-open`，数据库实际值为 `codex-review-local`。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 仍为 In progress。 |
| Dev Workflow Portability | ⚠️ | `/dev` worktree 入口仍依赖 BusyBox 不支持的 `flock -w`。本次审查通过显式隔离 worktree 执行；Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7` 仍为 In progress。 |
| Temp Patch Accumulation | ⚠️ | 超过 7 天的结构债未减少：`report-48h.js:119`、`sprint-result-contract.js:37-64`、`trace-routes.js:237/243`、`decision-executor.js:225/304`、`alertness/healing.js:448/475/1037`、`thalamus.js:847`。 |
| Ledger Freshness（探针A） | ⚠️ | 任务关联兜底比对发现 `ZenithJoy 客户管理` 落后最近 completed task 19 天、`工厂 · MJ5 承诺地图闭环` 落后 10 天；均超过 7 天但未超过 21 天，因此本轮不创建 followup。19 条 21:00 UTC 账本仍各含 6 个“暂无”，但它们生成于 PR #4696 合并前，真实修复效果应由下一次账本生成验证。 |
| Smoke Consistency（探针B） | ⚠️ | 17 条 E2E 声明保持 12 条存在、4 条缺失、1 条 TBD；另有 2 条 active journey 未声明 E2E。12:00 后没有 smoke 文件变化，也没有新增“废弃/弃用/改用”类决策。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 仍为 In progress。 |
| Deploy Config Drift（探针C） | ⚠️ | 生产和 staging 的关键 runtime env 均与部署仓 Compose + `.env.docker` 推导值一致；生产 `1.270.1` 与 main 一致，staging `1.270.0` 健康但落后一个 patch。主仓与部署仓 `.env.docker` key-set 仍差 `ACCEPTANCE_API_TOKEN`、`ACCEPTANCE_PUBLIC_HOST`、`CECELIA_TICK_ENABLED`；GitHub repo variables 为空且命令成功。review executor 不具备 launchd 观测面，本轮该子项记为不可观测。 |

## 4B Structural Health Findings

本次为 daily 轻量增量扫描，基线为 12:00 UTC 报告使用的 `30518076`：

- `packages/brain/src` JS/TS 文件数从 2033 增至 2034；唯一新增文件是 `src/__tests__/golden-paths-get-by-id.test.js`，不是生产模块。
- 生产代码只增加 `routes/golden-paths.js` 35 行与 `routes/journeys.js` 29 行；对应新增/扩展测试共 123 行，定向 4 文件 40 tests 全部通过。
- 五个核心文件均未变化；没有新增跨层 import、没有新增孤岛生产模块。
- `executor.js` 仍为 4609 行；recovery-loop 与 executor 内置 liveness probe 继续使用两套不一致的字段投影。
- 根目录仍跟踪任务特定的 `.brain-result.json` 与 `.task`，分别固定指向旧任务 `93161b22-...` 和 migration 164 修复，仍有后续执行误读风险。

## Severity

- 🔴 High: 4 项（tick 超时互斥失效、tasks 时间契约分裂、recovery 超时网空转、CURRENT_STATE 假新鲜）
- ⚠️ Medium: 8 项（边界、路由/API 文档、复杂度、`/dev` 可移植性、超期结构债、Journey/line ledger、E2E 声明、部署源）
- ✅ Clean: 5 项（arch-review lock 活性、DEFINITION facts、版本同步、生产 runtime/main 一致、12:00 后新增 API 测试配对）

## Required Actions

- [ ] **P0** — 处理 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：为 tick 引入可传播的取消/截止合同，或在旧执行真实终止前保留全局租约；回归测试必须断言旧 tick 存活时新 tick 不得进入。
- [ ] **P0** — 处理 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一 `tasks` 生命周期字段为明确 UTC/timestamptz 契约，补 PostgreSQL 非 UTC + Node 不同时区测试，并审计所有时间消费点。
- [ ] **P1** — 处理 Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`：让 recovery-loop 查询完整 executor contract 字段，增加生产等价回归，证明活执行不杀、死/超龄执行按合同收口。
- [ ] **P1** — 处理 Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`：重建 `SYSTEM_MAP.md` 当前事实并增加正文 facts 校验；同步修正 AGENTS 的 skills API 和已消失的 followup 合同描述。
- [ ] **P1** — 下一次 line ledger 生成后验证 PR #4696：若 19/19 空账本仍无改善，重开归属完整率问题并把 journey/task/run 锚点完整性纳入入库门禁。
- [ ] **P1** — 处理 Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7` 与 `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`：修复 worktree 锁兼容，并逐条修正 4 个缺失、1 个 TBD、2 个空 E2E 声明。
- [ ] **P2** — 定义 L0/HTTP 路由直调 Cortex 的豁免边界；继续拆分 `executor.js`，清理根目录任务结果产物与超期结构债。
- [ ] **P2** — 收敛双 `.env.docker` 为唯一配置 SSOT，并把 staging 版本落后与 launchd 不可观测状态纳入部署观测。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.270.1`、Schema `393`、migration 无冲突、LLM fetch timeout 完整）。
- `bash scripts/check-version-sync.sh`：PASS（四处版本均为 `1.270.1`）。
- `NODE_PATH=... node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- Brain 定向 Vitest：`golden-paths-get-by-id`、`issues-list-route`、`recovery-loop`、`tick-loop` 共 4 文件、40 tests PASS。
- 生产真实状态：Brain `healthy`、`1.270.1@0c893a7f`，当前任务 lock 存在且无 retry/error，两个 circuit breaker 均关闭；staging `1.270.0` 为 healthy。
- 生产日志：新容器观测窗无 `FORCE-RELEASE`；recovery-loop 对当前任务和另一条 in-progress 任务持续输出 `executor_kind=null (legacy) — fail-open`。
- 生产 DB 只读探针：Schema `393`；public tasks 七个时间字段类型、DB 时区、当前任务 SQL claim delay 与 API 序列化值均已实测；当前任务数据库 `executor_kind=codex-review-local`。
- 台账探针：19 条 active journey、17 条 E2E 声明；Journey 新鲜度有 2 条超过 7 天且无超过 21 天项；19 条当前 line ledger 共 114 个“暂无”。
- 部署探针：生产/staging 关键 Compose→runtime 值全部相等；双 `.env.docker` key-set 仍差 3 项；GitHub variables 查询成功且列表为空。

## ADR Needed? 是

需要明确 tick 超时后的取消/互斥租约合同、任务生命周期时间的唯一类型/时区合同，以及 L0/HTTP 路由直调 Cortex 的允许边界。recovery-loop 字段投影缺失属于实现缺陷，不需要另立 ADR。

## Escalate to /architect? 是

tick 取消传播、时间类型迁移与消费端兼容、CURRENT_STATE 正本和 Cortex 边界涉及跨模块契约；本巡检只记录事实与风险，不在本 PR 中设计或修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 12,
  "health_findings": 5,
  "high_severity": 4,
  "escalate_to_architect": true,
  "summary": "生产与 arch-review 当前可运行，staging 已恢复健康；但 tick 超时互斥、tasks 时间契约、recovery-loop executor 投影和 CURRENT_STATE 正本四项高风险仍未收敛"
}
```
