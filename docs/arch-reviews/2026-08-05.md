---
id: arch-review-20260805
version: 1.3.0
created: 2026-08-05
updated: 2026-08-05
reviewer: Codex (Brain 自动调度)
task_id: 8c7bf474-7467-470d-88bc-c718c43b9a67
base_sha: 62b298441e8054e89425f8f0ce2822bfc5214ca3
previous_task_id: 4e419b04-49b7-4e20-b03a-a1e560a53777
---

# Architecture Review — 2026-08-05 daily（16:00 UTC）

## Executive Summary

裁决为 **CRITICAL**，高风险项从 08:04 UTC 巡检的 2 项增至 3 项。`arch_review` 执行活性继续正常：接管复核时本任务由 `codex-review-local` 于 16:20:22 UTC claim，review lock 同时创建，`retry_count=0`、`error_message=null`；生产 Brain 已部署到当前 `main`，两个 circuit breaker 均为 `CLOSED`。

本轮新增的高风险是 `tasks` 时间契约分裂：`created_at/started_at/updated_at/queued_at` 是 `timestamp without time zone`，`claimed_at` 是 `timestamptz`。生产 PostgreSQL 会话时区为 `America/Chicago`，Node 容器时区为 `Asia/Shanghai`；当前 16:00 UTC 任务的无时区字段经 `pg`/JSON 后显示为 03:00 UTC，早 13 小时，而 `claimed_at` 正确显示 16:20 UTC。数据库内 `claimed_at-created_at` 为 20 分 12 秒，但 `executor.js`、`tick-runner.js`、`tick-status.js` 等直接用 JS `Date` 计算 runtime/stale age，存在误判风险。独立 P0 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b` 已记录。

治理正本风险继续扩大：`SYSTEM_MAP.md` 仍停在 Brain `1.216.0` / Schema `262`，实际已到 `1.267.231` / migration `389`；`Agent 系统 hardening` journey 仍停在 2026-05-22。PR #4662 已把 16 条 smoke journey 标为 deprecated 并建立 `biz_area` 三桶；PR #4666 又为工厂 F0–F7 新增 8 条有效 smoke 声明，但 hardening 新鲜度与原有 E2E 路径漂移仍未闭环。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | L0 服务仍在 `report-48h.js:144`、`dispatch-helpers.js:201`、`monitor-loop.js:483`、`capability-probe.js:166` 动态 import `cortex.js`；HTTP 路由 `routes/analytics.js:5/234`、`routes/ops.js:2147` 也直连 Cortex。`thalamus.js:1607` 才是标准 L1→L2 升级路径。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:45/103-106/115` 仍写 Brain `1.216.0`、Schema `262`，最后更新于 2026-07-19；当前事实为 Brain `1.267.231`、`EXPECTED_SCHEMA_VERSION=389`、生产 DB migration `389`。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 仍为 In progress。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us`、`executor_kind=codex-review-local` 接线正确；但 `arch_review_followup` 不在 task type/DB 合同中，仓库内也没有可执行 followup 合同。AGENTS 声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 均返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4547L/29；`planner.js` 1247L/4；`thalamus.js` 1807L/6；`cortex.js` 1593L/13。过去 24 小时 `executor.js` 又增加 64 行、删除 1 行，仍是主要 God Module。 |
| Concurrency Safety | ✅ | 接管复核时本任务 16:20:22 UTC claim，`/tmp/codex-review-locks/<task>.lock` 同时创建；任务保持 `in_progress`、`retry_count=0`、无 error，生产 `cecelia-run`/`dispatch` 熔断均关闭。PR #4653 的 lock-based review liveness 修复继续生效。 |
| Temporal Integrity | 🔴 | `public.tasks` 的四个生命周期时间是无时区类型，`claimed_at` 却为带时区类型；当前任务标题/claim 为 16:00/16:20 UTC，API 的 `created_at/started_at` 却为 03:00/03:20 UTC。DB 内 `claimed_at-created_at=20m12s`，JS 取出的时间却表面相差 13h20m；仓库至少 30 处生产逻辑用 `new Date(row.*_at)` 做 age/runtime/排序。该漂移可直接放大 watchdog、stale、资源隔离与 Dashboard 时长误判；P0 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b` 已建立。 |
| Temp Patch Accumulation | ⚠️ | 超过 7 天的结构债仍在：`report-48h.js:119`（2026-04-24，`mock_cortex`）、`sprint-result-contract.js:37-64`（2026-06-20）、`trace-routes.js:237/243`（2026-02-13）、`decision-executor.js:225/304`（2026-02-06）、`alertness/healing.js:448/475/1037`（2026-02-08）、`thalamus.js:847`（2026-07-10，重复失败分类表）。这些文件自 08:04 巡检后均未变化。 |
| Ledger Freshness（探针A） | 🔴 | `Agent 系统 hardening` journey `57df0a2e-a37f-47a6-bf14-cf231a480eed` 已被 PR #4662 归入 `biz_area=zenithjoy`，但 `updated_at` 仍为 `2026-05-22T04:39:58Z`，距本轮约 75.5 天。Issue `7d2cc1d3-18ca-465e-96e2-f904af103c6c` 仍为 In progress。 |
| Smoke Consistency（探针B） | ⚠️ | 当前共有 27 条非 smoke/test active journey，19 条声明非空 `e2e_test_path`。对 `cecelia` 与 `zenithjoy-workspace` 的 main 逐条核查：12 条真实存在（含 PR #4666 新增的 F0–F7 8 条），5 条缺失，另有 1 条 handoff 文档和 1 条 TBD，合计 7 条非法。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 的旧标题仍写 14/9，事实基线需刷新为 19/7。 |
| Deploy Config Drift（探针C） | ⚠️ | 生产容器为 `1.267.231@62b29844`，与 `origin/main` 一致；staging 为 `1.267.231@b3702cc3`，落后当前 main 一个工厂 smoke 合并提交，health 因 tick/scheduler 硬关闭显示 degraded。生产与 staging 的 DB/端口/tick/容量关键 env 均与各自 Compose 解析值一致。主仓与部署仓 `.env.docker` 的 key-set 仍差 5 项（仅记录键名差异，不输出值）；GitHub Variables 查询成功且为空，launchd 宿主观测面在当前执行容器内不可见。 |

## 4B Structural Health Findings

本次为 daily 轻量扫描：

- 过去 24 小时 `packages/brain/src` 的 JS/TS 文件数从 1972 增至 1985（+13）。新增生产模块 `lib/codex-fatal-patterns.js`、`lib/codex-review-liveness.js`、`notion-capture-ingest.js` 均有对应单元或集成测试。
- PR #4664 给 Kernel 增加 `reopen_gan_contract` 状态动作，`orchestrator/loop.js` 增加 66 行、相关 derive/attempt-store 测试增加 156 行；合同故障从人工死等改为最多一次自动回退 GAN，改动集中在既有 orchestrator 边界内。
- 24 小时生产代码热点为 `notion-capture-ingest.js`（+168）、`routes/journeys.js`（+111）、`routes/captures.js`（84 行 churn）、`alertness/escalation.js`（83）和 `orchestrator/loop.js`（+66）；对应测试与 smoke 均存在。
- 根目录仍跟踪任务特定的 `.brain-result.json` 与 `.task`；前者固定指向旧任务 `93161b22-...` / PR #4620，容易被后续执行误读为当前结果。

## Severity

- 🔴 High: 3 项（tasks 时间契约分裂、CURRENT_STATE 正本失真、journey 台账约 75.5 天漂移）
- ⚠️ Medium: 6 项（边界、路由合同、复杂度、TODO、E2E 声明、部署源/版本）
- ✅ Clean: 4 项（review 执行活性、DEFINITION facts、版本同步、生产 Compose→runtime 关键变量）

## Required Actions

- [ ] **P0** — 处理 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一 `public.tasks` 生命周期字段为明确 UTC/timestamptz 契约，补 PostgreSQL 非 UTC + Node 不同时区的回归测试，并审计所有 JS `Date(row.*_at)` 的 stale/runtime 路径；Issue `6adfa549-5b69-4883-b087-649be182b8db` 继续仅跟踪已修复的 lock liveness 链路。
- [ ] **P1** — 处理 Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`：重建 `SYSTEM_MAP.md` 当前事实，并统一 followup task type/路由/DB/回调合同及 skill API 文档入口。
- [ ] **P1** — 处理 Issue `7d2cc1d3-18ca-465e-96e2-f904af103c6c`：回写 `Agent 系统 hardening` 台账，并建立 merge/report 自动更新与新鲜度守卫。
- [ ] **P1** — 更新并处理 Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`：将基线刷新为 19 条声明中 7 条非法，路径必须在声明的 base repo main 上真实存在且可执行。
- [ ] **P2** — 消除双 `.env.docker` 的 5 个 key-set 差异，明确唯一部署配置 SSOT，并使 staging 部署版本策略可观测。
- [ ] **P2** — 定义 L0/HTTP 路由直调 Cortex 的豁免边界，继续拆分 `executor.js`；移除或迁出根目录任务结果产物。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.267.231`、Schema `389`、migration 无冲突、LLM fetch timeout 完整）。
- `bash scripts/check-version-sync.sh`：PASS（四处版本均为 `1.267.231`）。
- `NODE_PATH=... node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- 生产 DB 只读探针：`schema_version MAX=389`；`tasks` 时间字段类型、DB/Node 时区组合、SQL age 与 API/JS 序列化值均已实测；hardening journey 时间与 `biz_area` 已复核。
- 生产真实状态：Brain `healthy`，`1.267.231@62b29844`，review lock 存在，当前 claim 时间 16:20:22 UTC、无 retry/error，两个 circuit breaker 全部关闭；生产/staging 关键 env 均与各自 Compose 一致。
- GitHub 内容 API：19 条 E2E 声明逐条核查，12 条有效、5 条缺失、1 条文档、1 条 TBD；PR #4666 的 F0–F7 8 条新声明在 `cecelia/main` 全部真实存在。PR #4661/#4662/#4664/#4666 均已合并，生产已实测部署到最新 SHA。

## ADR Needed? 是

需要明确三项治理决策：任务生命周期时间的唯一类型/时区契约、L0/HTTP 路由直调 Cortex 的允许边界，以及架构巡检 followup 的唯一合法 task type 与回调合同。

## Escalate to /architect? 是

原因是时间类型迁移与消费端兼容、CURRENT_STATE 正本、边界豁免和 followup 路由合同均涉及跨模块契约；本巡检只记录事实与风险，不在此 PR 中设计或修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 9,
  "health_findings": 4,
  "high_severity": 3,
  "escalate_to_architect": true,
  "summary": "arch_review 执行活性正常；tasks 时间类型分裂导致 JS/API 时间早 13 小时，SYSTEM_MAP、journey 台账和 E2E 声明仍存在严重治理漂移"
}
```
