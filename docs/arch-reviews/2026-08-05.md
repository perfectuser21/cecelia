---
id: arch-review-20260805
version: 1.1.0
created: 2026-08-05
updated: 2026-08-05
reviewer: Codex (Brain 自动调度)
task_id: 4e419b04-49b7-4e20-b03a-a1e560a53777
base_sha: 5c9fe4594556d19c264714767d0cf2c1f9a96e85
previous_task_id: 4fcea11b-3c65-42b8-a7c2-cf8cc87f97a3
---

# Architecture Review — 2026-08-05 daily（08:04 UTC）

## Executive Summary

裁决为 **CRITICAL**，高风险项从 04:03 UTC 巡检的 3 项降为 2 项。PR #4653 已修复 `arch_review` 被 60 秒活性探针恒判死的问题：本任务连续运行超过 10 分钟仍保持 `in_progress`，`codex-review-local` lock 存在，生产 Brain 已部署到当前 `main`，两个 circuit breaker 均为 `CLOSED`。

剩余高风险集中在治理正本：`SYSTEM_MAP.md` 仍停在 Brain `1.216.0` / Schema `262`，与实际 `1.267.228` / migration `387` 严重脱节；`Agent 系统 hardening` journey 台账距本轮相关合并已约 75 天。E2E 声明、followup task type 合同、双 `.env.docker` 配置源与核心模块复杂度仍需收敛。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | L0 服务仍在 `report-48h.js:144`、`dispatch-helpers.js:201`、`monitor-loop.js:483`、`capability-probe.js:166` 动态 import `cortex.js`；`thalamus.js:1607` 才是标准 L1→L2 升级路径。路由层 `routes/analytics.js:5/234` 也直接依赖 Cortex。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:45/103-106/115` 仍写 Brain `1.216.0`、Schema `262`，文件最后更新于 2026-07-18；当前事实为 Brain `1.267.228`、`EXPECTED_SCHEMA_VERSION=386`、最高 migration `387`。过去 24 小时 Brain 与 Kernel 有密集结构改动，CURRENT_STATE 正本未同步。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 仍为 In progress。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us`、`executor_kind=codex-review-local` 均已正确接线；但 skill 仍要求创建未注册的 `arch_review_followup` task type，`VALID_TASK_TYPES`/DB 合同不支持。AGENTS 文档入口 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 均返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4547L/29；`planner.js` 1247L/4；`thalamus.js` 1807L/6；`cortex.js` 1593L/13。过去 24 小时 `executor.js` 增加 64 行、删除 1 行并新增 1 个 import，仍是主要 God Module。 |
| Concurrency Safety | ✅ | PR #4653 已把 review 类任务活性 SSOT 改为 `/tmp/codex-review-locks/<task>.lock`，并注册 `codex-review-local` 合同。本任务 11:08:03 UTC 启动，11:18:47 UTC 仍 `in_progress`、lock 存在、无 error；生产 `cecelia-run`/`dispatch` 熔断均为 `CLOSED`。定向 25 tests 全绿。 |
| Temp Patch Accumulation | ⚠️ | 超过 7 天的结构债仍在：`report-48h.js:119`（2026-04-24，`mock_cortex`）、`sprint-result-contract.js:37-64`（2026-06-20）、`trace-routes.js:237/243`（2026-02-13）、`decision-executor.js:225/304`（2026-02-06）、`alertness/healing.js:448/475/1037`（2026-02-08）、`thalamus.js:847`（2026-07-10，重复失败分类表）。 |
| Ledger Freshness（探针A） | 🔴 | `Agent 系统 hardening` journey `57df0a2e-a37f-47a6-bf14-cf231a480eed` 的 `updated_at=2026-05-22T04:39:58Z`；相关活性修复 PR #4653 于 2026-08-05T10:39:05Z 合并，台账落后约 75.3 天。Issue `7d2cc1d3-18ca-465e-96e2-f904af103c6c` 仍为 In progress。 |
| Smoke Consistency（探针B） | ⚠️ | 30 条非 smoke/test active journey 中 14 条声明了非空 `e2e_test_path`；跨 `cecelia` 与 `zenithjoy-workspace` 的 main 分支核查仅 5 条真实存在，6 条缺失，另 3 条是 handoff 文档、TBD 或两个路径拼接，合计 9 条非法。现存脚本中的“飞书”命中仅为去飞书迁移说明或报警通道，不是已废弃业务流程依赖。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 仍为 In progress。 |
| Deploy Config Drift（探针C） | ⚠️ | 生产与 staging 容器的 DB 主机/端口/库名/用户、Brain 端口、tick 开关/间隔和容量变量均与实际 Compose 解析值一致；生产为 `1.267.228@5c9fe459`，与 `origin/main` 完全一致。主仓与部署仓 `.env.docker` 仍有 5 个差异键（3 个凭据类键、`ACCEPTANCE_PUBLIC_HOST`、`CECELIA_TICK_ENABLED`），未输出敏感值；GitHub Variables 查询成功且为空。当前执行环境无法读取宿主 `/Library/LaunchDaemons`，launchd 子探针无观测面。 |

## 4B Structural Health Findings

本次为 daily 轻量扫描：

- 过去 24 小时 `packages/brain/src` 的 JS/TS 文件数从 1969 增至 1982（+13）。新增生产模块 `lib/codex-fatal-patterns.js`、`lib/codex-review-liveness.js` 均有同名测试。
- 核心复杂度继续集中于 `executor.js`；30 天热点还包括 `orchestrator/dispatcher.js`、`selfcheck.js`、`harness-skill-relay.js` 与 `harness-relay-watchdog.js`。
- liveness 修复具备合同接线、lock 探针、缺失/超龄双确认、requeue 清 claim 与 migration 守卫；本地定向验证 3 个文件、25 个测试全部通过。
- 根目录仍跟踪任务特定的 `.brain-result.json`，内容固定指向旧任务 `93161b22-...` / PR #4620 / SHA `660d61ba...`；这是可能被后续执行误读的持久化任务产物。

## Severity

- 🔴 High: 2 项（CURRENT_STATE 正本失真、journey 台账约 75 天漂移）
- ⚠️ Medium: 7 项（边界、路由合同、复杂度、TODO、E2E 声明、部署源、根目录任务产物）
- ✅ Clean: 4 项（自动执行活性、DEFINITION facts、版本同步、Compose→runtime 关键变量）

## Required Actions

- [ ] **P1** — 处理 Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`：重建 `SYSTEM_MAP.md` 当前事实，并统一 `arch_review_followup` 的合法 task type/路由/DB/回调合同及 skill API 文档入口。
- [ ] **P1** — 处理 Issue `7d2cc1d3-18ca-465e-96e2-f904af103c6c`：回写 `Agent 系统 hardening` 台账，并建立 merge/report 自动更新与新鲜度守卫。
- [ ] **P1** — 处理 Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`：逐条修复 9 个缺失或非法 `e2e_test_path`，路径必须在声明的 base repo main 上真实存在且可执行。
- [ ] **P1** — 本任务成功回调后复核 Issue `6adfa549-5b69-4883-b087-649be182b8db`；若任务保持单次执行并正常完成，将“60 秒误杀”部分标记已验证恢复，保留其他未闭环范围。
- [ ] **P2** — 消除双 `.env.docker` 的 5 个差异键，明确唯一部署配置 SSOT，并给执行容器提供 launchd 只读观测面。
- [ ] **P2** — 定义 L0/HTTP 路由直调 Cortex 的豁免边界，继续拆分 `executor.js`；移除或迁出根目录 `.brain-result.json` 任务产物。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.267.228`、Schema floor `386`、最高 migration `387`、LLM fetch timeout 完整）。
- `bash scripts/check-version-sync.sh`：PASS（四处版本均为 `1.267.228`）。
- `NODE_PATH=... node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- liveness 定向 Vitest：3 个文件、25 tests PASS；首次根目录调用因错误模块解析未启动测试，改用 Brain 配置后成功。
- 生产真实状态：Brain `healthy`，`git_sha=5c9fe459...`，review lock 存在，任务连续运行超过 10 分钟仍 `in_progress`，两个 circuit breaker 全部关闭。
- PR #4653：MERGED；`brain-unit (1..6/all)`、`brain-integration`、`core-regression`、`real-env-smoke`、`ci-passed` 均 SUCCESS。非阻断 `Deploy Preview Environment` 为 FAILURE，但当前生产部署与 main SHA 已实测一致。
- DB 探针：直接连接生产 `cecelia` 查询 journeys/decisions；GitHub 内容 API 对两个 base repo 的 14 条声明做存在性核查；Docker inspect 与 Compose config 对关键变量逐项比对且不输出密钥。

## ADR Needed? 是

需要明确 L0/HTTP 路由直调 Cortex 的允许边界，以及架构巡检 followup 的唯一合法 task type 与回调合同。

## Escalate to /architect? 是

原因是 CURRENT_STATE 正本、边界豁免与 followup 路由合同互相矛盾；本巡检只记录事实与风险，不在此 PR 中设计或修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 8,
  "health_findings": 3,
  "high_severity": 2,
  "escalate_to_architect": true,
  "summary": "arch_review 自动执行活性已真实恢复；SYSTEM_MAP/followup 合同、journey 台账和 E2E 声明仍存在严重治理漂移"
}
```
