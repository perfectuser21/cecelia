---
id: arch-review-20260805
version: 1.0.0
created: 2026-08-05
updated: 2026-08-05
reviewer: Codex (Brain 自动调度)
task_id: 4fcea11b-3c65-42b8-a7c2-cf8cc87f97a3
base_sha: 72ccbd208a80678a803a25c4fdbe64de4e2c8b65
---

# Architecture Review — 2026-08-05 daily

## Executive Summary

裁决为 **CRITICAL**。PR #4640/#4643 已让 `arch_review` 从连续 `never_started` 恢复到能够启动，但本次巡检仍在活跃执行时被 watchdog 从 `in_progress` 改回 `queued`，同时生产 `cecelia-run` circuit breaker 为 `OPEN`（复核时 21 failures）；自动执行闭环尚未恢复。

代码事实与 `DEFINITION.md` 一致，生产/暂存容器的关键运行时变量也与实际部署根解析值一致。主要架构风险集中在治理面漂移：`SYSTEM_MAP.md` 严重过期、journey 台账与 E2E 声明脱离现实、`arch-review` 的 followup 合同引用了未注册 task type，以及 L0 调度服务仍有多处直接调用 Cortex。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | `report-48h.js:144`、`dispatch-helpers.js:201`、`monitor-loop.js:483`、`capability-probe.js:166` 直接 import `cortex.js`；只有 `thalamus.js:1607` 是标准 L1→L2 升级路径。`tick.js` 已缩到 188 行，但绕过 L1 的调用只是搬家，未消失。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md` 仍写 Brain `1.216.0`、Schema `262`、旧模型/模块/CI 数量；当前 main 为 Brain `1.267.225`、Schema `386`。24h 内大量 Brain/Kernel 改动只同步了 `DEFINITION.md`，CURRENT_STATE 架构图已不能作为可靠正本。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us` 映射正确，`dev→talk` 静默降级已删除；但 skill 规定 `NEEDS_ATTENTION` 创建 `arch_review_followup`，该值不在 `VALID_TASK_TYPES` 或 DB 约束中，回调联动合同不可落地。`/api/brain/skills/count` 文档入口也返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4532L/28 imports；`planner.js` 1247L/4；`thalamus.js` 1807L/6；`cortex.js` 1593L/13。过去 24h `executor.js` 又增加 89 行、2 个 import，仍是主要 God Module。 |
| Concurrency Safety | 🔴 | 任务 `4fcea11b-...` 在会话仍运行时被 watchdog 回退为 `queued + claimed_by=NULL`，随后由本会话 claim 防止重复派发；生产 health 同时显示 `cecelia-run=OPEN`。新增的 configError/never_started/drain 回归测试已在合并 PR CI 通过，但真实状态机仍误判。Issue `6adfa549-5b69-4883-b087-649be182b8db`。 |
| Temp Patch Accumulation | ⚠️ | 多处 TODO 超过 7 天：`report-48h.js:119`（2026-04-24，仍标 `mock_cortex`）、`sprint-result-contract.js:37-64`（2026-06-20）、`trace-routes.js:237/243`（2026-02-13）、`decision-executor.js:225/304`（2026-02-06）、`alertness/healing.js:448/475/1037`（2026-02-08）、`thalamus.js:847`（2026-07-10，重复失败分类表）。 |
| Ledger Freshness（探针A） | 🔴 | `Agent 系统 hardening` journey 的 `updated_at=2026-05-22`，关联最新 completed task 为 `2026-07-14`，落后 54.0 天；另有 5 条 active journey 落后 9.0–19.6 天。Issue `7d2cc1d3-18ca-465e-96e2-f904af103c6c`。 |
| Smoke Consistency（探针B） | ⚠️ | 近 60 天废弃关键词未命中仍存在的 5 个 smoke；但 14 条非空 `e2e_test_path` 中有 9 条缺失或非法（TBD、handoff 文档、两个路径拼接），包括 `agent-hardening-smoke.sh`、`dev-pipeline-runner-fleet-smoke.sh`、`harness-pipeline-1node-smoke.sh`、`publisher-smoke.sh`、`customer-admin.spec.ts`。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`。 |
| Deploy Config Drift（探针C） | ⚠️ | 生产和 staging 容器的关键 env 与 `cecelia-deploy-main` Compose 解析值全部一致；主仓与部署仓 `.env.docker` 仅 `CECELIA_TICK_ENABLED` 互相矛盾。GitHub Variables 可访问且为空。执行容器无法读取宿主 `/Library/LaunchDaemons`，launchd 子探针无观测面。生产运行 `1.267.224@7778d42b`，main 已到 `1.267.225@72ccbd20`，属于刚合并版本的部署滞后。 |

## 4B Structural Health Findings

本次为 daily 轻量扫描：

- 过去 24h `packages/brain/src` 的 JS/TS 文件数从 1961 增至 1979（+18）；新增生产模块 `lib/codex-fatal-patterns.js` 与 `orchestrator/rubric-trend.js` 均有同名测试。
- `tick.js` 拆分结果稳定，24h 内行数/import 数无增长；主要复杂度继续向 `executor.js` 聚集。
- 状态/并发高热点包含 `executor.js`、`drain.js`、`alertness/escalation.js` 与 orchestrator，共约 +1239/-45；对应新增/更新了 configError、never_started、drain stale restore、escalation SQL 与 orchestrator 测试。
- 根目录仍跟踪任务特定的 `.brain-result.json`，内容指向旧任务 `93161b22-...`/PR #4620；这是易被后续执行误读的持久化临时产物。

## Severity

- 🔴 High: 3 项（CURRENT_STATE 文档失真、watchdog/熔断闭环、台账 54 天漂移）
- ⚠️ Medium: 7 项（边界、路由合同、复杂度、TODO、smoke、部署源、结构产物卫生）
- ✅ Clean: 3 项（DEFINITION facts、核心版本同步、生产/暂存 Compose→runtime 关键变量）

## Required Actions

- [ ] **P0** — 处理 Issue `6adfa549-5b69-4883-b087-649be182b8db`：以本任务 status_history/driver heartbeat/容器生命周期复现活执行误回队，并验证 circuit breaker 可自动复位。
- [ ] **P1** — 处理 Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`：更新 `SYSTEM_MAP.md`，并统一 `arch_review_followup` 的合法 task type/路由/DB 合同。
- [ ] **P1** — 处理 Issue `7d2cc1d3-18ca-465e-96e2-f904af103c6c`：修复 Agent 系统 hardening 台账 54 天漂移，并为 journey 新鲜度建立自动守卫。
- [ ] **P1** — 处理 Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`：逐条修复 9 个缺失/非法 `e2e_test_path`，要求路径真实存在且可执行。
- [ ] **P2** — 统一主仓/部署仓 `CECELIA_TICK_ENABLED` 的 SSOT，并补可从执行容器读取 launchd 持久化/运行时差异的只读探针。
- [ ] **P2** — 给 L0 直调 Cortex 定义明确豁免边界或统一 gateway；继续拆分 `executor.js`，避免 tick 瘦身后复杂度转移。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain 1.267.225、Schema 386、migration 无冲突、LLM fetch 均有 timeout）。
- `bash scripts/check-version-sync.sh`：PASS（四处版本均为 1.267.225）。
- `node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- PR #4640/#4643：已合并；`brain-unit (1..6/all)`、`brain-integration`、`core-regression`、`real-env-smoke`、`ci-passed` 均 SUCCESS。#4640 的 preview deploy 曾失败，后续 #4644 已针对 preview/生产隔离补修。
- 真实产出验证：本任务从历史 `never_started` 进入执行并完成 9 项探针；watchdog 于 04:49 UTC 回队后，本执行仍在 05:10 UTC 写出报告关联 Issue，直接证明活执行被误判；05:33 UTC 重试重新取得 claim，并已登记 P0 Issue。

## ADR Needed? 是

需要明确两项治理决策：L0 服务直调 Cortex 的允许边界，以及架构巡检 followup 的唯一合法 task type/回调合同。

## Escalate to /architect? 是

原因不是要求本巡检直接设计新架构，而是现有 CURRENT_STATE 正本、边界豁免与 followup 路由合同已互相矛盾，需要 `/architect` 统一后再由 `/dev` 实施。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 9,
  "health_findings": 4,
  "high_severity": 3,
  "escalate_to_architect": true,
  "summary": "arch_review 已能启动，但活执行仍被 watchdog 误回队且 cecelia-run 熔断 OPEN；另发现 CURRENT_STATE、journey 台账与 E2E 声明系统性漂移"
}
```
