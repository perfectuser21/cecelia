---
id: arch-review-20260812
version: 1.2.0
created: 2026-08-12
updated: 2026-08-12
reviewer: Codex (Brain 自动调度)
task_id: 4340824a-08e1-41aa-8f4b-e354fc46ab5d
base_sha: a925cb588032fdb420fc3ea4526eeb6a241bfb0d
previous_task_id: 3814a72e-e704-4515-92c9-0b96c154201f
latest_task_id: f348b01b-7fd8-4352-83ac-42d5ab4a82dc
latest_base_sha: 928b8084185045322f00ed7907582875cf5e32ac
current_task_id: ea363f53-94a7-4b68-b07d-dcefcf66aa6d
current_base_sha: d9b01327ee8a309b547d2316938fc754565a968d
---

# Architecture Review — 2026-08-12 daily（04:00 UTC，04:21 UTC 执行）

## Executive Summary

裁决仍为 **CRITICAL**：9 项高风险、19 项中风险。上一轮 20:00 UTC 后 main 只新增 #4800（derive 取证死循环双修），没有关闭既有架构高风险。本轮最强证据不是静态推断，而是当前巡检任务自身再次复现 preview/production 身份分裂：preview-4827 从克隆库认领 task `4340824a-...`，preview DB 显示 `in_progress / brain-tick-4413 / codex-review-local`，production DB 对同一 UUID 仍显示 `queued` 且无 claim、run-id、heartbeat、executor kind；当前 Codex 子进程也确由 preview Brain PID 4413 启动。

preview-4827 设置 `CECELIA_TICK_ENABLED=false`，但没有 `CECELIA_TICK_HARD_OFF`、环境专属 `BRAIN_URL` 或 internal token；health 仍报告 scheduler running/enabled。`triggerCodexReview()` 又把缺省 `BRAIN_URL` 固定为 `http://localhost:5221`，因此 preview 执行结束会把 callback 发向 production，而本次执行身份只存在 preview DB。上一轮“preview 会重复执行且 callback 串环境”的风险已在当前任务原样复发。

production/staging 已同时部署 `1.272.18@a925cb58`，health 与 breaker 正常；但生产容器仍没有 `CECELIA_INTERNAL_TOKEN`，宿主匿名敏感请求继续返回 503 `INTERNAL_AUTH_NOT_CONFIGURED`。修复 PR #4814 仍为 OPEN/DRAFT/DIRTY，auto-merge 失败，代码成功态尚未转成部署成功态。

staging 的 `CECELIA_TICK_HARD_OFF=1` 仍只关闭主 tick。启动日志显示 Nightly、33 个 scheduler jobs、3 个 projection jobs 与 PromotionJob 全部运行；health 还复用了 production 的 `last_tick=2026-08-12T04:23:58.174Z`，错误呈现 scheduler running/enabled。production/staging 的 migration ledger 最大值均为 410，但 staging 仍缺物理列 `tasks.retry_count`；两库的 schema ledger 行数也分别为 403/400，继续证明最大版本号不能代表物理 schema 完整。

夜间质量闸继续失真：Smoke E2E Nightly、Nightly Regression Gate、Integration Nightly 最新运行全部失败，且均已连续至少 5 次失败；只有 Real-Machine Smoke 连续成功。`incident-reporter` 仍持续因 `recurrence_count` 列歧义写入失败，guard-drill 过去 24h 又在 production/staging 分别生成 26/22 条同名 Issue，失败聚合与去重均未形成可信合同。

本轮 line ledger 在 21:00 UTC 蒸馏后恢复：production 1024 total / 23 recent 20h，当前 task payload 含 23 条摘要；但 20:00 巡检先于 21:00 蒸馏的固定空窗未消失。active journey 增至 23 条，其中 17 条声明 E2E、6 条为空；staging 则为 21 条 active、0 条声明 E2E，环境正本仍显著分叉。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | `report-48h.js`、`monitor-loop.js`、`dispatch-helpers.js`、`capability-probe.js`、`routes/analytics.js`、`routes/ops.js` 等仍绕过 Thalamus 直调 Cortex；标准 L1→L2 入口与豁免边界未统一。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md` 仍写 Brain `1.216.0`、schema 262；当前正本为 `1.272.18`、schema floor 410。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`。 |
| Preview / Staging Scheduler Isolation | 🔴 | preview-4827 在 tick=false、无 hard-off 下真实认领并执行当前巡检；staging hard-off 下 Nightly/33 scheduler/3 projection/Promotion 仍启动。Issue `a9a6e3f6-06c7-42a7-840f-09e6ef7f448a`。 |
| Internal Auth Deployment Contract | 🔴 | 生产容器没有 `CECELIA_INTERNAL_TOKEN`，宿主敏感写面实测 503；PR #4814 仍 OPEN/DRAFT/DIRTY。 |
| Review Execution Contract | 🔴 | preview DB 持有 claim/executor，production DB 保持 queued；文件 lock 有 run id `c1b7b601-...`，两库 payload 均无 run-id/heartbeat。Issue `6adfa549-5b69-4883-b087-649be182b8db`。 |
| Review Verdict / Callback Integrity | 🔴 | executor 仍只解析 `PASS|FAIL`，未知/CRITICAL 会按 exit code 压成 PASS；callback fetch 不校验 HTTP/CAS `applied`。preview 无 `BRAIN_URL`，默认回调 production。Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`。 |
| Routing / Skill Integrity | ⚠️ | `arch_review → codex-review-local → us` 可派发；但声明的 `packages/workflows/skills/arch-review/SKILL.md` 不存在，`/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 均为 404。 |
| Universal Map Authority | ✅ | manifest 写、projection store、read service 当前统一走 `src/lib/*`；生产写权威维持单一。 |
| Universal Map Legacy Residue | ⚠️ | `src/map/manifest-schema.js`、manifest-store/projector/state-resolver 与对应测试仍保留；生产引用边界与 archive 合同未明确。 |
| Core Module Complexity | ⚠️ | `executor.js` 4615L、`routes/execution.js` 4091L、`routes/ops.js` 3024L、`routes/analytics.js` 2310L、`thalamus.js` 1816L；Brain 非测试 JS 为 633 文件/174947 行。 |
| Concurrency Safety | 🔴 | `tick-loop.js` 超过 60 秒仍只释放共享布尔锁，不 cancel/join 旧 `doTick()`；测试仍把 force release 当正确行为。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`。 |
| Temporal Integrity | 🔴 | tasks 的 created/queued/started/completed/updated 为 `timestamp without time zone`，claimed/heartbeat 为 `timestamptz`；DB 时区为 America/Chicago，本任务 API 时间继续与 04:21 UTC 执行错位。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`。 |
| Recovery Contract Integrity | 🔴 | 同一任务在 production 投影为空、preview 投影为 `codex-review-local`；liveness/recovery 无法基于单一权威身份判断。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`。 |
| Registry Freshness | ✅ | production API/db_schema/test 三表可读；本轮 12 表上下文查询全部 HTTP 200。staging registry 仍无独立可信基线。 |
| Schema Ledger / Physical Parity | 🔴 | production/staging ledger 都到 410，但 staging `tasks.retry_count` 物理列缺失；最大 migration 号 selfcheck 仍无法发现局部漂移。Issue `8a782f87-16d3-4ad6-a901-843efd57d50f`。 |
| Schema Consumer Integrity | ⚠️ | `incident-reporter` 的 upsert 在生产持续报 `recurrence_count` 列歧义；3 个 mock 单测全绿仍未覆盖真实 PostgreSQL conflict。 |
| Dependency Exposure | ⚠️ | `packages/brain npm audit --omit=dev` 为 56 项：54 moderate、2 high、0 critical；high 来自 OpenTelemetry 依赖链。 |
| Dev Workflow Portability | ⚠️ | 隔离 `/dev` worktree 无 `node_modules`：DoD mapping 因缺 `js-yaml` 失败，Vitest ESM 配置也无法加载；显式复用宿主依赖后才通过。Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7`。 |
| Temp Patch Accumulation | ⚠️ | 根目录继续跟踪任务特定 `.brain-result.json`/`.task`；结果、通知、预测与失败分类等结构债未归位。 |
| Ledger Freshness | ⚠️ | 当前 23 条 line ledger 已注入，但固定 20:00 review 早于 21:00 dreaming，20:00 空窗结构性存在。Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803`。 |
| Journey Smoke Registry | ⚠️ | production active journey 23 条，17 条声明 E2E、6 条为空；staging 21 条 active、E2E 0 条。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`。 |
| Deploy Config Drift | ⚠️ | production/staging 同 SHA，但 production 缺 token、staging DB_HOST 仍依赖调用者环境；preview 又继承 `host.docker.internal`。Issue `b6a0dd31-432b-4048-8fde-66b9f5a4e3b5`。 |
| Deployment Closure | ⚠️ | main 大部分 CI 与部署成功，但同 SHA 的 Release Gate 失败，真实 token 效果也未验证；成功态没有覆盖配置交付。 |
| Kernel CI Truth Compression | ⚠️ | failed checks、UNKNOWN merge state 与 Release Gate 的 fail-closed 合同仍未统一。Issue `d44556d8-4cf0-4355-afeb-050604911dc6`。 |
| Kernel PR Identity Drift | ⚠️ | 已有 expected branch 时仍保留 task-short fallback；签发身份未成为唯一真相。Issue `7a276ffd-34b5-4753-99d2-2c5dc4e5915a`。 |
| Realtime Voice Duplication | ⚠️ | 5 个物理 `useRealtimeVoice` 文件、两组重复实现尚未收敛。Issue `1daab7e4-088c-44b7-862b-29dd7e1394d5`。 |
| Janitor /dev Lock Contract | ⚠️ | `/dev` 默认 `~/worktrees` 与 Janitor 扫描/锁路径仍无跨模块行为合同；本任务继续运行在外部 worktree 根。Issue `c00b0090-ab86-49c0-836a-edafef37c4dd`。 |
| Scheduler Test Collection | ⚠️ | 定向 10 files / 81 tests 全通过；完整 `daily-review-scheduler.test.js` 显式请求仍因标准配置 exclude 而 0 收集。Issue `0acbf8f3-38dc-42ff-9d0b-f2e627d45c02`。 |
| Guard Drill Idempotency | ⚠️ | rolling 24h production/staging 分别新增 26/22 条同名 Issue；失败不更新持久 gate，Issue 写入无 guard_id upsert。Issue `9893e28b-cbb8-483f-8b14-e2044fa9da5e`。 |
| Nightly Test Truth | ⚠️ | Smoke E2E、Nightly Regression、Integration Nightly 最新均失败且各连续 ≥5 次；Real-Machine Smoke 连续成功，四闸语义未统一。Issue `af7361b3-1873-424c-9aee-60c2b24f7e79`。 |

## 4B Structural Health Findings

- 当前任务同 UUID 同时存在三套事实：production DB queued、preview DB in_progress、文件 lock/run-id active；执行与 callback 又分别默认指向 preview 进程和 production URL。
- `CECELIA_TICK_ENABLED=false` 不能表达 preview 的副作用隔离，`CECELIA_TICK_HARD_OFF=1` 也不能表达 staging 的副作用隔离；server 启动的独立 loops 没有统一生命周期总闸。
- health 的 scheduler 状态由共享 DB/working-memory 投影得出，staging/preview 在主 tick 未运行时仍报告 running/enabled 与 production `last_tick`，监控面无法证明隔离。
- internal auth 的应用层 fail-closed 正常，但部署层 token 交付缺失；PR、CI、镜像与运行态配置没有共同完成判据。
- schema_version=410 与物理列完整性继续分裂，当前 selfcheck 只能验证 floor/最大号，不能验证 migration ledger、物理 schema、consumer 三方一致。
- review executor 把架构裁决压缩为 `PASS|FAIL`，同时不验证 callback HTTP 状态/CAS；即使报告正确，任务终态也可能被静默误记。
- line ledger 04:00 恢复并不能关闭空窗问题：20:00 review 固定早于 21:00 dreaming，两个调度器的时间合同天然错位。
- 三条 nightly 连续红、Real-Machine Smoke 连续绿，说明测试池环境前提与发布语义混杂；incident reporter 又无法聚合 recurrence，红灯没有可信归因闭环。
- `/arch-review` instruction-book 存在，但执行 SKILL、能力 API 与标准调度测试缺失；当前能力依靠 executor 特例运行，而非注册表可证明地加载。
- God Module 与复制债无改善：五个核心模块合计 15856 行，Realtime Voice 仍有 5 个物理副本。

## Severity

- 🔴 High: 9 项（CURRENT_STATE、环境副作用隔离、internal auth 部署、review 身份、verdict/callback、tick 互斥、tasks 时间、recovery 投影、schema ledger/物理 parity）
- ⚠️ Medium: 19 项（边界、路由/skill、Map 遗留、复杂度、incident、依赖、`/dev` 可移植性、结构债、ledger 时窗、Journey E2E、deploy 配置/闭环、Kernel CI/PR、语音复制、Janitor、scheduler 测试、guard-drill、nightly 真值）
- ✅ Clean: 7 组（Brain health、breaker、facts/version 门禁、宿主依赖下 DoD mapping、当前 04:00 ledger 注入、Map 单一生产权威、Real-Machine Smoke）

## Required Actions

- [ ] **P0** — 关闭 preview/staging 写副作用：统一注册全部 loops，非 production 默认 hard-off；preview 必须使用环境专属 callback URL/token，禁止克隆后认领已有 production task。
- [ ] **P0** — 完成 PR #4814 并做运行态验收：生产匿名敏感请求=401、带凭据请求=2xx，禁止 503；部署成功必须验证容器 token key 与真实 API 效果。
- [ ] **P0** — 将 review claim/run-id/heartbeat/callback 合并为单一原子合同；callback 必须校验 HTTP 与 CAS `applied`，未知/CRITICAL verdict fail-closed，严禁按 exit code改写 PASS。
- [ ] **P0** — 完成 tasks 全量 `timestamptz`、tick cancel/join 或租约，以及 recovery `executor_kind` 权威投影。
- [ ] **P0** — 建立 production/staging/test 物理 schema ↔ migration ↔ code consumer 通用 parity gate，先补齐 staging `tasks.retry_count`。
- [ ] **P1** — 三条 nightly 只运行显式 registry；每项声明 owner/fixture/DB/repo endpoint/expiry，统一 Real-Machine 与 release 语义，连接失败不得伪装成 schema 缺失。
- [ ] **P1** — 修复 `incident-reporter` 限定表名的 recurrence upsert并增加真实 PostgreSQL conflict 回归；guard-drill 用持久 gate + guard_id upsert。
- [ ] **P1** — 恢复完整 scheduler 测试进标准 CI，补回 `/arch-review` SKILL/注册 API，统一 20:00 review 与 21:00 dreaming 时序，更新 SYSTEM_MAP 与 Journey E2E 正本。
- [ ] **P2** — 收敛 Cortex 边界、God Module、OpenTelemetry 漏洞、语音 hook 复制、Map legacy 与根目录任务产物。

## Verification Evidence

- 三项 DevGate：`facts-check` PASS、`check-version-sync` PASS；DoD mapping 在隔离 worktree 默认因缺 `js-yaml` 失败，显式复用宿主 `node_modules` 后 PASS。
- production：healthy、`1.272.18@a925cb58`、breakers all CLOSED；容器 token key 缺失，宿主敏感写面 503。
- staging：healthy、同 SHA、`CECELIA_TICK_HARD_OFF=1`；日志仍启动 Nightly/33 scheduler/3 projection/Promotion，health 复用 production last_tick。
- preview-4827：`1.272.19@791e4058`、DB `cecelia_preview_4827`、tick=false、无 hard-off/BRAIN_URL/token；PID 4413 真实 spawn 当前 Codex 子进程。
- review task：preview DB 为 in_progress + `brain-tick-4413` + `codex-review-local`；production DB 同 UUID 为 queued 且 claim/executor/run-id/heartbeat 全空；lock run id=`c1b7b601-...`。
- DB：两库时区 America/Chicago、schema max=410；production 有 `tasks.retry_count`，staging 无；tasks 生命周期时间类型仍混用 timestamp/timestamptz。
- 定向测试：10 files / 81 tests 全通过；`daily-review-scheduler.test.js` 显式请求后仍 0 收集。`tick-loop.test.js` 继续断言 65s force-release。
- ledger/Journey：production line ledger 1024 total / 23 recent 20h；active journeys 23 / E2E 17 / missing 6。staging active 21 / E2E 0。
- guard/incident：24h guard-drill issues production 26、staging 22；production 日志持续报 `recurrence_count` 歧义。
- CI：Smoke E2E `31530646737`、Nightly Regression `31530668738`、Integration Nightly `31537612044` 均失败；各连续至少 5 次失败。Real-Machine Smoke 最新成功。
- dependency audit：Brain 54 moderate / 2 high / 0 critical；high 来自 OpenTelemetry 链。
- GitHub：PR #4814 仍 OPEN/DRAFT/DIRTY；main SHA 大部分 CI 与部署检查成功，但 Release Gate 失败。

## ADR Needed? 是

需要定义五项跨模块合同：环境级统一 loop 生命周期与 clone task 隔离、internal token 在 host/container/sidecar/CI 的唯一交付、review claim/run-id/heartbeat/callback 唯一身份、物理 schema/migration/code consumer 三方 parity，以及 nightly registry 的 owner/fixture/expiry/诊断语义。

## Escalate to /architect? 是

preview 当前任务串环境、staging 副作用隔离、内部鉴权、review 终态、时间/互斥、schema parity 与 nightly 真值跨越 server、scheduler、DB、sidecar、CI 和 Dashboard；本巡检只记录事实与约束，不在报告 PR 中修改运行时代码。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 30,
  "health_findings": 10,
  "high_severity": 9,
  "medium_severity": 19,
  "issues_created": 0,
  "issues_updated": 0,
  "escalate_to_architect": true,
  "summary": "preview-4827 从克隆库认领并执行当前 production task，同 UUID 双库状态分裂且 callback 默认指向 production；生产 internal token 仍缺，staging hard-off 下独立 loops 继续运行，schema ledger=410 仍缺 tasks.retry_count；三条 nightly 连续失败，review/time/tick/recovery 合同未闭合"
}
```

---

# Architecture Review — 2026-08-12 daily（08:01 UTC，08:10 UTC 执行）

## Executive Summary

裁决仍为 **CRITICAL**：9 项高风险、17 项中风险。04:00 巡检后合并的 #4814 已把 internal token SSOT 贯通蓝绿部署，#4830 已补齐已有 PR 权威字段与漏 webhook 终态收账；production/staging 现同为 `1.272.21@2f44dc8d`，容器 token hash 与宿主凭据一致，匿名敏感写面实测 401，当前 Gate 3 与部署成功。上一轮 internal auth 部署高风险和 Kernel PR 身份中风险因此关闭。

运行身份合同仍然失真。当前 task `993558e5-...` 在 production 为 `in_progress / brain-tick-7 / codex-review-local`，文件 lock 持有 run id `bb8c4f99-...`，但 DB payload 没有 run-id 或 heartbeat；更严重的是，04:00 task `4340824a-...` 已从 `in_progress` 回退为 `queued`，却保留 claim、started_at 与 executor kind，且 lock 已消失。`triggerCodexReview()` 仍只解析 `PASS|FAIL`，CRITICAL 报告会因进程 exit 0 被改写成 PASS，callback 也不校验 HTTP/CAS `applied`。

staging 主 tick 的 health 已能正确报告 stopped，但 hard-off 并未关闭全部副作用：启动日志仍显示 Nightly、33 个 scheduler jobs、3 个 projection jobs 与 PromotionJob 启动。production/staging migration ledger 最大值均为 410，staging 仍缺物理列 `tasks.retry_count`；生产任务生命周期字段继续混用 `timestamp without time zone` 与 `timestamptz`，当前 task 的 `started_at` 又比实际 claim 早 13 小时。

本轮新增一项红色台账证据：active Journey “工厂 · MJ5 承诺地图闭环” 的 `updated_at` 落后相关 PR #4772 合并时间 24.3 天，超过 21 天阈值，已创建 P1 Issue `5f3f0d07-...`。Journey E2E 声明也不可信：17 条声明中 4 个路径在两个正本仓库均不存在、1 条仍为 TBD，另有 6 条为空；已更新既有 P1 Issue `925aa82e-...`。当前任务类型合同没有 `arch_review_followup`，因此巡检只能写通用 Issue，follow-up 路由缺口继续保留。

夜间质量闸仍连续红：Smoke E2E、Nightly Regression、Integration Nightly 最新及最近 8 次均失败，只有 Real-Machine Smoke 连续成功；生产 `incident-reporter` 仍因 `recurrence_count` 列歧义写入失败，guard-drill 过去 24h 在 production/staging 又各写入 23/21 条同名 Issue。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | `report-48h.js`、`monitor-loop.js`、`dispatch-helpers.js`、`capability-probe.js`、analytics/ops routes 仍绕过 Thalamus 直调 Cortex。 |
| Undocumented Changes | 🔴 | `SYSTEM_MAP.md` 仍写 Brain `1.216.0` 与旧 schema 规模；运行正本已为 `1.272.21`、schema floor 410、Brain 非测试 JS 635 文件/174753 行。 |
| Preview / Staging Scheduler Isolation | 🔴 | staging `CECELIA_TICK_HARD_OFF=1` 只关闭主 tick，Nightly/33 scheduler/3 projection/Promotion 仍启动。Issue `a9a6e3f6-...`。 |
| Internal Auth Deployment Contract | ✅ | #4814 已合并；宿主 SSOT、production/staging 容器 token hash 一致，匿名敏感写面返回 401，Gate 3 与部署成功。 |
| Review Execution Contract | 🔴 | 当前 task 的 run-id 只存在文件 lock，DB 无 heartbeat；04:00 task 回退 queued 却保留 claim/executor，且无 lock。Issue `6adfa549-...`。 |
| Review Verdict / Callback Integrity | 🔴 | executor 只解析 `PASS|FAIL`，CRITICAL + exit 0 会变 PASS；callback fetch 不验证 HTTP 或 CAS `applied`。Issue `72546baf-...`。 |
| Routing / Skill Integrity | ⚠️ | `arch_review → /arch-review review → codex-review-local` 可派发，但声明的 workflow SKILL 与 skills API 不存在，任务类型也没有 `arch_review_followup`。 |
| Universal Map Authority | ✅ | manifest 写、projection store、read service 继续统一走 `src/lib/*`。 |
| Universal Map Legacy Residue | ⚠️ | `src/map/*` legacy 模块与测试仍保留，archive/生产引用边界未成合同。 |
| Core Module Complexity | ⚠️ | tick/executor/planner/thalamus/cortex 五个核心文件合计 9459 行；`executor.js` 单文件 4615 行。 |
| Concurrency Safety | 🔴 | tick 超时仍只释放共享布尔锁，不 cancel/join 旧 `doTick()`。Issue `9aa89588-...`。 |
| Temporal Integrity | 🔴 | lifecycle timestamp 类型混用；DB 时区 America/Chicago，当前 task `started_at` 比实际 claim 早 13 小时。Issue `41f68c38-...`。 |
| Recovery Contract Integrity | 🔴 | review run-id/heartbeat 未原子写入任务；dispatch liveness 日志还把 executor kind 判为 legacy null 并 fail-open。Issue `4411cb51-...`。 |
| Registry Freshness | ✅ | production registry 与本轮 23 条 line ledger 可读取并注入任务 payload。 |
| Schema Ledger / Physical Parity | 🔴 | 两库 schema max=410、ledger 行数 403/400，但 staging 仍缺 `tasks.retry_count`。Issue `8a782f87-...`。 |
| Schema Consumer Integrity | ⚠️ | production `incident-reporter` 继续报 `recurrence_count` 列歧义，mock 测试未覆盖真实 conflict。 |
| Dependency Exposure | ⚠️ | production audit 为 54 moderate / 2 high / 0 critical；high 来自 OpenTelemetry 链。 |
| Dev Workflow Portability | ⚠️ | 隔离 Linux worktree 无依赖，复用 Darwin `node_modules` 又缺 Linux Rollup/esbuild；需隔离安装平台包后才能跑测试。 |
| Temp Patch Accumulation | ⚠️ | 根目录仍跟踪任务特定 `.brain-result.json`/`.task`，多处 TODO/placeholder 尚未归位。 |
| Ledger Freshness | 🔴 | MJ5 Journey 台账落后相关已合并 PR 24.3 天；其余 18/23 Journey 因无 journey_id/匹配任务只能判 unknown。新 Issue `5f3f0d07-...`。 |
| Ledger Schedule Window | ⚠️ | 08:01 payload 有 23 条摘要，但固定 20:00 review 仍早于 21:00 dreaming。Issue `a1ed9884-...`。 |
| Journey Smoke Registry | ⚠️ | 17 条声明中 4 路径跨两仓均缺失、1 TBD，另有 6 条为空；staging 21 条 active 全无路径。更新 Issue `925aa82e-...`。 |
| Deploy Config Drift | ⚠️ | token SSOT 已统一，但 main/deploy `.env.docker` 的 tick 默认值仍分叉，staging DB_HOST 依赖调用环境。Issue `b6a0dd31-...`。 |
| Deployment Closure | ✅ | 当前 HEAD Gate 3、Brain deploy、staging deploy 成功，运行镜像/SHA/token/API 效果一致。 |
| Kernel CI Truth Compression | ⚠️ | promote-all-prod workflow 无运行记录；nightly/real-machine 的发布语义仍未统一。Issue `d44556d8-...`。 |
| Kernel PR Identity | ✅ | #4830 已贯通已有 PR canonical URL，并补上漏 webhook 终态 reconcile；定向回归通过。 |
| Realtime Voice Duplication | ⚠️ | 5 个物理 `useRealtimeVoice` 文件、两组重复实现未收敛。Issue `1daab7e4-...`。 |
| Janitor /dev Lock Contract | ⚠️ | `/dev` worktree 与 Janitor 扫描/锁路径仍无跨模块行为合同。Issue `c00b0090-...`。 |
| Scheduler Test Collection | ⚠️ | 标准 Vitest config 显式 exclude `daily-review-scheduler.test.js`，定向请求仍 0 收集。Issue `0acbf8f3-...`。 |
| Guard Drill Idempotency | ⚠️ | rolling 24h production/staging 各 23/21 条同名 Issue，仍无 guard_id upsert。Issue `9893e28b-...`。 |
| Nightly Test Truth | ⚠️ | 三条 nightly 最近 8 次全失败，Real-Machine Smoke 连续成功；质量闸语义分裂。Issue `af7361b3-...`。 |

## 4B Structural Health Findings

- 当前 review 身份被拆成 DB claim、文件 lock/run-id 和回调 CAS 三套事实；04:00 task 的状态回退证明这不是静态风险。
- staging health 能证明主 tick 停止，却不能证明所有副作用循环停止；独立 scheduler/projection/promotion 没有统一生命周期总闸。
- internal auth 已形成 host credential、container env、CI assertion 与真实 API 的共同完成判据，是本轮明确关闭的高风险。
- schema 最大版本号、ledger 行数和物理列继续分裂；当前 selfcheck 仍无法证明 migration/physical schema/consumer 三方一致。
- Journey 台账探针能确认 1 条红色，但 18 条因任务无 journey_id 无法确定新鲜度；审计关系本身仍不可证明。
- E2E registry 的“声明存在”不等于路径存在，17 条声明中只有 12 条可在两个权威仓库之一验证。
- 三条 nightly 持续红、Real-Machine 持续绿，且 incident reporter 无法聚合 recurrence，发布信号缺少统一诊断语义。
- God Module、Cortex 绕行、Map legacy、Realtime Voice 复制均无改善；新增生产代码虽少，既有复杂度没有下降。

## Severity

- 🔴 High: 9 项（CURRENT_STATE、环境副作用隔离、review 身份、verdict/callback、tick 互斥、tasks 时间、recovery 投影、schema parity、MJ5 ledger freshness）
- ⚠️ Medium: 17 项（边界、路由/skill、Map 遗留、复杂度、incident、依赖、`/dev` 可移植性、结构债、ledger 时窗、Journey E2E、deploy 配置、Kernel CI、语音复制、Janitor、scheduler 测试、guard-drill、nightly 真值）
- ✅ Clean: 9 组（Brain health/breaker、三项 DevGate、84 项定向回归、internal auth 运行态、同 SHA 部署、Map 权威、当前 ledger 注入、PR 权威、Real-Machine Smoke）

## Required Actions

- [ ] **P0** — 统一注册并 hard-off 非 production 全部 loops，staging/preview 默认不得启动 scheduler、projection、promotion 或认领 production task。
- [ ] **P0** — 将 review claim/run-id/heartbeat/callback 收敛为 DB 原子合同；callback 必须验证 HTTP 与 CAS `applied`，CRITICAL/未知 verdict fail-closed，禁止 exit 0 改写 PASS。
- [ ] **P0** — 完成 tasks 全量 `timestamptz`、tick cancel/join 或租约，以及 liveness/recovery 的 executor kind 权威投影。
- [ ] **P0** — 建立 migration ledger ↔ 物理 schema ↔ consumer 通用 parity gate，先补齐 staging `tasks.retry_count`。
- [ ] **P1** — 修复 MJ5 承诺地图台账，并让完成任务携带 journey_id/相关 PR，使 21 天 freshness 可被自动证明；补齐 `arch_review_followup` 类型。
- [ ] **P1** — 校验并修复 Journey E2E 路径；registry 必须验证文件存在、owner、fixture、repo endpoint 与 expiry。
- [ ] **P1** — 修复 incident recurrence upsert、guard_id 去重和三条 nightly，统一 Real-Machine/release 语义；恢复 scheduler 测试进标准 CI。
- [ ] **P2** — 更新 SYSTEM_MAP，收敛 Cortex 边界、God Module、OpenTelemetry 漏洞、语音复制、Map legacy 与根目录任务产物。

## Verification Evidence

- 三项 DevGate：`facts-check`、`check-version-sync`、DoD mapping 全部 PASS；facts 为 Brain `1.272.21`、schema floor 410。
- runtime：production/staging 镜像均为 `1.272.21@2f44dc8d`；production scheduler running，staging 主 scheduler stopped；breakers 全 CLOSED。
- internal auth：宿主 SSOT 与两容器 token hash 一致；`assert-internal-auth-ready.sh` PASS，匿名敏感写面=401；Gate 3 run `31576829999` 成功。
- review task：当前 lock run id=`bb8c4f99-...`，DB 无 run-id/heartbeat；04:00 task 已回退 queued 并保留 claim/executor，lock 缺失。
- DB：两库时区 America/Chicago、schema max=410；ledger rows production/staging=403/400；staging 无 `tasks.retry_count`。
- targeted regression：8 files / 84 tests 全通过，覆盖 callback authority、existing PR authority、漏 webhook reconcile、liveness/circuit/deadline。
- ledger/Journey：production 1024 total / recent20h 23；MJ5 lag=24.3 天。active 23，声明路径 17（12 存在、4 缺失、1 TBD），另 6 空；staging active 21、路径 0。
- CI：Smoke E2E `31530646737`、Nightly Regression `31530668738`、Integration Nightly `31537612044` 失败且最近 8 次全红；Real-Machine Smoke `31503837621` 成功。
- issue：创建 `5f3f0d07-79be-4de8-bec4-fe96883864a8`；更新 `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`。

## ADR Needed? 是

需要定义四项跨模块合同：环境级统一 loop 生命周期、review claim/run-id/heartbeat/callback 唯一身份、migration/物理 schema/consumer 三方 parity，以及 Journey ledger/E2E registry 的可验证关联模型。

## Escalate to /architect? 是

staging 副作用隔离、review 终态、时间/互斥、schema parity、Journey 台账和 nightly 真值跨越 server、scheduler、DB、sidecar、CI 与多仓 registry；本巡检只记录证据与约束，不修改运行时代码。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 31,
  "health_findings": 8,
  "high_severity": 9,
  "medium_severity": 17,
  "issues_created": 1,
  "issues_updated": 1,
  "escalate_to_architect": true,
  "summary": "internal auth 与已有 PR 权威链已闭环；staging hard-off 仍启动独立副作用 loops，04:00 review task 状态回退且 run-id/heartbeat/callback 非原子，schema=410 仍缺物理列；MJ5 台账落后相关 PR 24.3 天，Journey E2E 声明失真，三条 nightly 最近 8 次全红"
}
```

---

# Architecture Review — 2026-08-12 daily（12:02 UTC，12:56 UTC 执行）

## Executive Summary

裁决仍为 **CRITICAL**：9 项高风险、17 项中风险。08:01 巡检后 main 合并了 #4832、#4833、#4834、#4835；其中 #4832/#4835 收紧 canonical PR URL、shell argv 边界和 terminal/open reconcile 公平性，当前 production/staging 已同时部署 `1.272.24@928b8084`，本 SHA 的 Gate 3 与 staging deploy 均成功。这些变更强化了 PR 权威链，但没有修改 `triggerCodexReview()` 的 review verdict、run-id、heartbeat 或 callback HTTP/CAS 合同，因此既有架构高风险没有关闭。

当前任务 `f348b01b-...` 于 12:56:18 UTC 被 production `brain-tick-7` claim，`/tmp/codex-review-locks` 中 run id `5927a668-...` 与 Codex 进程持续存活，历史 `process_disappeared` 本轮未复现；但 DB 仍只有 `executor_kind=codex-review-local`，`current_run_id`、`run_status`、`driver_heartbeat_at` 全空。`started_at=2026-08-11T23:56:18Z` 比 `claimed_at=2026-08-12T12:56:18Z` 早 13 小时，当前任务再次直接证明生命周期时间类型分裂。

04:00 task `4340824a-...` 已在 12:54 UTC 以 `duplicate_dispatch=true`、复用 PR #4828 的方式收敛为 completed；上一轮观察到的 queued 回退不再是当前终态。但这条任务直到当前巡检开始前两分钟才完成，生产日志在 20:47–20:54 本地时间多次以“与 sibling 高度相似”为由跳过 12:02 task，说明 review 的去重、产物复用和派发时序仍依赖异步修复，而非单一原子身份合同。

环境隔离继续失败。staging 容器明确设置 `CECELIA_TICK_HARD_OFF=1`，主 tick 日志也显示跳过启动；但 Nightly、33 个 scheduler jobs、3 个 projection jobs 与 PromotionJob 仍全部启动。更严重的是，staging `/health` 现又报告 scheduler `running/enabled` 并展示 production 的 `last_tick=2026-08-12T13:00:23Z`，共享 DB 投影使健康面无法证明 staging 已隔离。

生产日志在 20:53–20:54 再次出现约 60 秒 tick 卡住与 `FORCE-RELEASE via setTimeout`；代码随后只清共享布尔锁，不 cancel/join 原 `doTick()`，测试仍把 65 秒后继续执行下一 tick 当正确行为。schema ledger 仍为 production/staging `410`，行数分别 `403/400`，而 staging 继续缺 `tasks.retry_count`。`incident-reporter` 仍因 `recurrence_count` 歧义失败，rolling 24h 两库各存在 13 条同名 guard-drill Issue。

质量闸没有改善：Smoke E2E、Nightly Regression、Integration Nightly 最近 8 次全部失败，Real-Machine Smoke 最近 5 次成功；Release Gate 仍因缺 nightly 绿色证据失败。依赖面有所收敛：`npm audit --omit=dev` 从上一轮 54 moderate / 2 high 降至 28 moderate / 2 high，两个 high 仍来自 OpenTelemetry 链。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | `report-48h.js`、`monitor-loop.js`、`dispatch-helpers.js`、`capability-probe.js`、analytics/ops routes 仍绕过 Thalamus 直调 Cortex；标准 L1→L2 入口与豁免边界未统一。 |
| Undocumented Changes | 🔴 | `SYSTEM_MAP.md` 仍写 Brain `1.216.0`；运行正本已为 `1.272.24`、schema floor 410、Brain 非测试 JS 634 文件/174680 行。Issue `bb78074b-...`。 |
| Preview / Staging Scheduler Isolation | 🔴 | staging hard-off 只关闭主 tick，Nightly/33 scheduler/3 projection/Promotion 仍启动；health 又把 production tick 投影为自身 running。Issue `a9a6e3f6-...`。 |
| Internal Auth Deployment Contract | ✅ | #4814 的宿主/容器 token SSOT 继续有效；production/staging 同 SHA 运行，匿名敏感写面保持 fail-closed。 |
| Review Execution Contract | 🔴 | 当前 task 的文件 lock/run-id 与进程均活跃，但 DB 无 run-id/heartbeat；04:00 duplicate 直到 12:54 才由产物复用收敛。Issue `6adfa549-...`。 |
| Review Verdict / Callback Integrity | 🔴 | `triggerCodexReview()` 仍只解析 `PASS|FAIL`，未知/CRITICAL + exit 0 会改写为 PASS；callback fetch 不校验 HTTP 或 CAS `applied`。Issue `72546baf-...`。 |
| Routing / Skill Integrity | ⚠️ | `arch_review → /arch-review review → codex-review-local` 可派发；但 workflow SKILL 不存在，`/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 仍为 404，`arch_review_followup` 也无 task type。 |
| Universal Map Authority | ✅ | manifest 写、projection store、read service 继续统一走 `src/lib/*`。 |
| Universal Map Legacy Residue | ⚠️ | `src/map/*` legacy 模块与测试仍保留，archive/生产引用边界未成合同。 |
| Core Module Complexity | ⚠️ | `executor.js` 4615L、`routes/execution.js` 4099L、`routes/ops.js` 3024L、`routes/analytics.js` 2310L、`routes/harness.js` 2174L；God Module 未收敛。 |
| Concurrency Safety | 🔴 | 本轮生产再次发生 60 秒 force-release；旧 `doTick()` 未 cancel/join，锁释放后允许新 tick 重叠。Issue `9aa89588-...`。 |
| Temporal Integrity | 🔴 | lifecycle 字段仍为 timestamp without time zone，claim/heartbeat 为 timestamptz；当前 task `started_at` 比 `claimed_at` 早 13 小时。Issue `41f68c38-...`。 |
| Recovery Contract Integrity | 🔴 | executor kind、文件 lock、run-id 与 heartbeat 仍分散；DB 无 run-id/heartbeat 时 recovery 只能依赖文件侧信号。Issue `4411cb51-...`。 |
| Registry Freshness | ✅ | 12 表上下文端点全部 HTTP 200；当前 task payload 含 23 条 line ledger。 |
| Schema Ledger / Physical Parity | 🔴 | 两库 max schema=410、ledger rows=403/400，但 staging 无 `tasks.retry_count`；selfcheck 仍不能证明物理 schema 完整。Issue `8a782f87-...`。 |
| Schema Consumer Integrity | ⚠️ | production `incident-reporter` 继续报 `recurrence_count` 列歧义，mock 测试未覆盖真实 PostgreSQL conflict。 |
| Dependency Exposure | ⚠️ | audit 已降至 28 moderate / 2 high / 0 critical；high 仍为 `@opentelemetry/propagator-jaeger` 与 `@opentelemetry/sdk-node` 链。 |
| Dev Workflow Portability | ⚠️ | `/dev` 入口在本机再次因 BusyBox `flock` 不支持 `-w` 阻断；共享依赖还缺 Linux ARM64 Rollup/esbuild，隔离安装平台包后测试才可运行。Issue `5ff60dff-...`。 |
| Temp Patch Accumulation | ⚠️ | 根目录继续跟踪任务特定 `.brain-result.json`/`.task`，结果、通知与失败分类等结构债未归位。 |
| Ledger Freshness | 🔴 | MJ5 Journey 台账落后相关已合并 PR 24.3 天；多数 Journey 仍因任务缺 journey_id/相关 PR 不能自动证明 freshness。Issue `5f3f0d07-...`。 |
| Ledger Schedule Window | ⚠️ | 当前 payload 有 23 条摘要，但固定 20:00 review 仍早于 21:00 dreaming，空窗结构性存在。Issue `a1ed9884-...`。 |
| Journey Smoke Registry | ⚠️ | production active 23 / E2E 17 / missing 6；staging active 21 / E2E 0。既有 4 路径缺失与 1 TBD 尚未修复。Issue `925aa82e-...`。 |
| Deploy Config Drift | ⚠️ | production 容器 `CECELIA_TICK_ENABLED=false` 却由 DB 启动 tick；staging hard-off 又不能关闭独立 loops，环境变量与运行态语义继续分叉。Issue `b6a0dd31-...`。 |
| Deployment Closure | ✅ | production/staging 均运行 `1.272.24@928b8084`；Gate 3 run `31597269129` 与 staging deploy `31597269146` 成功。 |
| Kernel CI Truth Compression | ⚠️ | scheduled Deploy 的 Release Gate 因 nightly 证据失败，独立 Promote All workflow 仍无运行记录；发布语义未统一。Issue `d44556d8-...`。 |
| Kernel PR Identity | ✅ | #4832/#4835 将 PR URL 各来源独立校验、shell argv 与 reconcile 游标/预算接线完成；#4835 全套 PR checks 通过。 |
| Realtime Voice Duplication | ⚠️ | 5 个物理 `useRealtimeVoice` 文件、至少两组不同实现仍未收敛。Issue `1daab7e4-...`。 |
| Janitor /dev Lock Contract | ⚠️ | `/dev` worktree 与 Janitor 扫描/锁路径仍无跨模块行为合同；入口失败后只能走原生 worktree。Issue `c00b0090-...`。 |
| Scheduler Test Collection | ⚠️ | 定向 8 files / 88 tests 全通过；`daily-review-scheduler.test.js` 显式请求仍因标准 config exclude 而 0 收集。Issue `0acbf8f3-...`。 |
| Guard Drill Idempotency | ⚠️ | rolling 24h production/staging 各 13 条同名 Issue；失败不更新持久 gate，仍无 guard_id upsert。Issue `9893e28b-...`。 |
| Nightly Test Truth | ⚠️ | 三条 nightly 最近 8 次全失败，Real-Machine 最近 5 次成功；Release Gate 因此持续红，四闸语义仍分裂。Issue `af7361b3-...`。 |

## 4B Structural Health Findings

- 当前 review 活性已能由 lock + OS 进程证明，但 DB 身份仍只有 claim/executor kind；run-id、heartbeat 与 callback CAS 没有形成单一原子记录。
- 04:00 duplicate 最终收敛说明 terminal reconcile 能工作，但从异常状态到 completed 延迟数小时，并在此期间阻塞后续相似任务，恢复路径不具备时效合同。
- staging 的主 tick hard-off 与全系统副作用 hard-off 是两个不同概念；health 又读取共享 DB 事实，无法区分“本实例在跑”和“同库其他实例在跑”。
- 当前生产日志再次实证 force-release：布尔锁被释放不代表旧 scheduler 已终止，代码与测试仍把可能重叠当恢复策略。
- schema version、ledger rows 与物理列继续三分；相同镜像和最大 migration 号不能证明两个环境 schema 等价。
- #4832/#4835 改善的是 PR URL/命令执行/reconcile 权威，不覆盖 review executor 的 verdict 枚举和 callback 响应确认；两类 callback 合同不能混为同一成功态。
- nightly 三红、Real-Machine 连绿、Release Gate 连续拒绝，incident reporter 又不能聚合 recurrence，质量信号仍缺统一 owner/fixture/诊断语义。
- God Module、Cortex 越层、Map legacy、Realtime Voice 复制与台账不可证明关系均无结构性下降。

## Severity

- 🔴 High: 9 项（CURRENT_STATE、环境副作用隔离、review 身份、verdict/callback、tick 互斥、tasks 时间、recovery 投影、schema parity、MJ5 ledger freshness）
- ⚠️ Medium: 17 项（边界、路由/skill、Map 遗留、复杂度、incident、依赖、`/dev` 可移植性、结构债、ledger 时窗、Journey E2E、deploy 配置、Kernel CI、语音复制、Janitor、scheduler 测试、guard-drill、nightly 真值）
- ✅ Clean: 9 组（Brain health/breaker、三项 DevGate、88 项定向回归、internal auth、同 SHA 部署、Map 权威、当前 ledger 注入、PR 权威、Real-Machine Smoke）

## Required Actions

- [ ] **P0** — 统一注册并 hard-off 非 production 全部 loops；health 必须按 instance 身份报告，不得把共享 DB 的 production tick 投影成 staging 自身活性。
- [ ] **P0** — 将 review claim/run-id/heartbeat/callback 收敛为 DB 原子合同；callback 必须验证 HTTP 与 CAS `applied`，CRITICAL/未知 verdict fail-closed。
- [ ] **P0** — 完成 tasks 全量 `timestamptz`、tick cancel/join 或租约，以及 liveness/recovery 的 executor kind 权威投影。
- [ ] **P0** — 建立 migration ledger ↔ 物理 schema ↔ consumer 通用 parity gate，先补齐 staging `tasks.retry_count`。
- [ ] **P1** — 修复 MJ5 台账并让完成任务携带 journey_id/相关 PR；补齐 `arch_review_followup` 与可验证 E2E registry 合同。
- [ ] **P1** — 修复 incident recurrence upsert、guard_id 去重和三条 nightly，统一 Real-Machine/Release Gate 语义；恢复 scheduler 测试进标准 CI。
- [ ] **P1** — 修复 `/dev` 对 BusyBox flock 与多平台 optional dependency 的入口合同，确保隔离 worktree 可直接跑门禁和测试。
- [ ] **P2** — 更新 SYSTEM_MAP，收敛 Cortex 边界、God Module、OpenTelemetry 漏洞、语音复制、Map legacy 与根目录任务产物。

## Verification Evidence

- 三项 DevGate：`facts-check`、`check-version-sync`、DoD mapping 全部 PASS；facts 为 Brain `1.272.24`、schema floor 410。
- runtime：production/staging 镜像均为 `1.272.24@928b8084`，breakers all CLOSED；production tick running，staging 主 tick hard-off，但 staging health 错报 scheduler running/enabled。
- review task：production DB `in_progress / brain-tick-7 / codex-review-local`；lock run id=`5927a668-...` 与 Codex PID 存活；DB current_run_id/run_status/heartbeat 全空。
- temporal：当前 task `started_at=2026-08-11T23:56:18Z`、`claimed_at=2026-08-12T12:56:18Z`；生命周期列 timestamp、claim/heartbeat 列 timestamptz，DB timezone America/Chicago。
- DB：production/staging schema max=410，ledger rows=403/400；staging 缺 `tasks.retry_count`。active Journey 分别 23/21，E2E 路径分别 17/0。
- scheduler logs：staging hard-off 后仍启动 Nightly/33 scheduler/3 projection/Promotion；production 20:53–20:54 出现 tick 60s force-release，incident recurrence 继续报列歧义。
- targeted regression：8 files / 88 tests 全通过；`daily-review-scheduler.test.js` 仍被标准配置排除为 0 收集。
- `/dev`：worktree 脚本因 BusyBox `flock -w` 失败；共享依赖缺 Linux ARM64 Rollup/esbuild，隔离平台包后测试通过。
- ledger/guard：production line ledger 1024 total / recent20h 23；staging 234/21。rolling 24h 两库各 13 条同名 guard-drill Issue。
- CI：Smoke E2E `31530646737`、Nightly Regression `31530668738`、Integration Nightly `31537612044` 最近 8 次全红；Real-Machine `31503837621` 最近 5 次成功；Release Gate run `31590026672` 失败。
- deployment：#4835 checks 全绿；Gate 3 `31597269129`、staging deploy `31597269146` 成功，运行 SHA 与 main 一致。
- dependency/code：audit 28 moderate / 2 high / 0 critical；Brain 非测试 JS 634 文件/174680 行，最大文件 `executor.js` 4615 行。
- routing：workflow arch-review SKILL 缺失；两个声明 skills API 真实返回 404。

## ADR Needed? 是

需要定义四项跨模块合同：环境级统一 loop 生命周期与 instance-aware health、review claim/run-id/heartbeat/callback 唯一身份、migration/物理 schema/consumer 三方 parity，以及 nightly/Release Gate 的 owner/fixture/expiry/诊断语义。

## Escalate to /architect? 是

staging 副作用隔离、review 终态、时间/互斥、schema parity、Journey 台账和 nightly 真值跨越 server、scheduler、DB、sidecar、CI 与多仓 registry；本巡检只记录证据与约束，不修改运行时代码。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 31,
  "health_findings": 8,
  "high_severity": 9,
  "medium_severity": 17,
  "issues_created": 0,
  "issues_updated": 0,
  "escalate_to_architect": true,
  "summary": "PR URL 与 reconcile 权威链已加固且 1.272.24 同 SHA 部署成功；但 staging hard-off 仍运行独立 loops 并错报 production tick，当前 review 的 run-id/heartbeat/callback 仍非原子，tick force-release、时间错位、schema parity、MJ5 台账和三条 nightly 继续未闭合"
}
```

---

# Architecture Review — 2026-08-12 daily（16:02 UTC，16:30 UTC 执行）

## Executive Summary

裁决仍为 **CRITICAL**：10 项高风险、16 项中风险。12:02 巡检后 main 合并了 #4840、#4841、#4843；production/staging 已同时运行 `1.272.28@d9b01327ee`，三项 DevGate、238 项定向回归、migration 411 的物理列/约束/trigger 与本 SHA 的 Gate 3、staging deploy 均通过。#4841 把 GAN 批准测试冻结到精确 SHA，新增代码与数据库迁移已真实落地；#4843 将相同 runner 合同推广到 fleet 节点。这两项没有引入新的物理 schema 漂移。

本轮新增的 P0 是 #4840 的 `dev → harness_initiative` 改道位置和跨仓身份合同。`dispatcher.js` 在 536/552 行按原始 `dev` 候选绕过 harness cap，在 704 行仍执行 bridge 可用性检查，直到 800/807 行才改写 task type；分类器又只看 `payload.repo`，不看已被 Harness 使用的 `payload.base_repo`。因此改道任务既绕过 admission/initiative lock，又可能在 bridge 不可用时永远到不了无需 bridge 的 Harness；跨仓任务 `cb40da23-...` / run `dcd791fb-...` 已以 `workspace_repo_not_supported:perfectuser21/zenithjoy-workspace` 终止。P0 Issue `e0370d57-...` 已存在，本轮不重复开单。

review 执行合同出现了更强的在线复现。当前 16:02 task `ea363f53-...` 的 lock `42067bd1-...` 与 Codex PID 820/831 持续存活，历史 `process_disappeared` 尚未发生；但 DB 仍无 `driver_heartbeat_at` 或 run-id。相邻 12:02 task `f348b01b-...` 已由 PR #4838 交付，却累计 10 次 `watchdog_safe_requeue` 和 15 条已处理 `AI Done` callback，当前仍为 `in_progress`，且没有 lock/进程/completed_at/result。最新子进程 exit 0 后，executor 又把报告中的 `CRITICAL` 按既有 fallback 改写成 `PASS`，发送 callback 后不检查 HTTP/CAS 是否应用。用户给出的 `process_disappeared/never_started` 失败模式，其身份、心跳、裁决和终态根因仍在生产持续复现。

环境隔离、时间与 schema parity 也未关闭。staging 明确设置 `CECELIA_TICK_HARD_OFF=1`，主 tick 没启动，但 Nightly、NightlyOrchestrator、33 个 scheduler jobs 与 PromotionJob 仍启动，并在本次重启真实执行 guard-drill、创建 P1 Issue；staging health 还与 production 同报 scheduler `running/enabled` 和相同 `last_tick`。当前 task 的 `started_at` 继续比 `claimed_at` 早 13 小时。两库 schema max 都是 411，migration 411 完整存在，但 staging 仍缺 `tasks.retry_count`，最大 migration 号依旧不能证明物理 schema 等价。

质量与台账没有改善。MJ5 Journey 仍停在 07-17，落后 08-10/08-11 的 #4772/#4782/#4786/#4788 超过 25 天；production active Journey 为 23 条，其中 4 个 E2E 路径两仓 main 都不存在、1 条为 TBD、6 条为空，staging 21 条全部为空。rolling 24h production/staging 各有 12 条同名 guard-drill Issue，`incident-reporter` 仍报 `recurrence_count` 歧义。三条 nightly 最近 8 次全部失败，Real-Machine 最近 6 次成功；依赖审计当前为 54 moderate / 2 high / 0 critical。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | `routes/analytics.js` 仍直引 Cortex，`decision-executor.js` 直接消费 Cortex whitelist；L1→L2 的统一入口与豁免边界未收敛。 |
| Undocumented Changes | 🔴 | `SYSTEM_MAP.md` 仍写 Brain `1.216.0`/schema 262；`DEFINITION.md` 同页同时写 schema 411 与“当前 410”；`KERNEL_HARNESS_MAP.md` 未记录 #4840 的 dispatch 阶段改道与跨仓限制。Issue `bb78074b-...` 已更新。 |
| Preview / Staging Scheduler Isolation | 🔴 | staging hard-off 后仍启动 Nightly/33 scheduler/Promotion 并真实创建 guard-drill Issue；health 错报 production tick 为本实例 running。Issue `a9a6e3f6-...` 已更新。 |
| Internal Auth Deployment Contract | ✅ | 宿主 token SSOT 与 production/staging 容器 hash 一致；匿名敏感入口实测拒绝，`assert-internal-auth-ready.sh` PASS。 |
| Review Execution Contract | 🔴 | 12:02 task 已交付却 10 次回队、15 次 callback 后仍 in_progress 且无进程；当前 task 有 lock/PID 但 DB 无 heartbeat/run-id。Issue `6adfa549-...` 已更新。 |
| Review Verdict / Callback Integrity | 🔴 | `executor.js:2573-2588` 仍只解析 PASS/FAIL、exit 0 fallback PASS，且不检查 callback HTTP/CAS；12:02 CRITICAL 被真实回写为 PASS。Issue `72546baf-...` 已更新。 |
| Dev → Harness Routing Integrity | 🔴 | `dispatcher.js:536/552/704` 的 admission/bridge 判断早于 `800/807` 改道；`dispatch-code-routing.js` 只看 `payload.repo`，跨仓 run 已 fatal。Issue `e0370d57-...`。 |
| Frozen Contract Artifact Integrity | ✅ | migration 411 在两库均有 `approved_sha`/`frozen_artifacts`、约束与不可变 trigger；代码按批准 SHA 冻结内容和摘要，相关定向测试通过。 |
| Universal Map Authority | ✅ | manifest 写、projection store、read service 继续统一走 `src/lib/*`；本轮 fleet 变更未新增第二写源。 |
| Universal Map Legacy Residue | ⚠️ | `src/map/*` legacy 模块与测试仍保留，archive/生产引用边界未形成合同。 |
| Core Module Complexity | ⚠️ | `executor.js` 4615L、`routes/execution.js` 4099L、`routes/ops.js` 3024L、`routes/analytics.js` 2310L、`routes/harness.js` 2174L；Brain 非测试 JS 为 636 文件/175127 行。 |
| Concurrency Safety | 🔴 | `tick-loop.js:65-93` 超时后仍只清共享布尔锁，不 cancel/join 旧 `doTick()`；20 项测试继续把 65s force-release 当正确行为。Issue `9aa89588-...`。 |
| Temporal Integrity | 🔴 | 当前 task `started_at=03:30:59Z`、`claimed_at=16:30:59Z`；生命周期列仍为 timestamp，claim/heartbeat 为 timestamptz，DB 时区 America/Chicago。Issue `41f68c38-...` 已更新。 |
| Recovery Contract Integrity | 🔴 | lock、OS PID、executor kind、run-id、heartbeat 与 callback 仍分散；15 条 processed callback 不能把同一任务收敛到 terminal。Issue `4411cb51-...`。 |
| Registry Freshness | ✅ | `/dev` 要求的 12 表上下文端点全部 HTTP 200；API/db_schema/test registry 均可读。 |
| Schema Ledger / Physical Parity | 🔴 | production/staging max schema=411、ledger rows=404/401；migration 411 完整，但 staging 仍缺 `tasks.retry_count`。Issue `8a782f87-...` 已更新。 |
| Schema Consumer Integrity | ⚠️ | production `incident-reporter` 继续报 `recurrence_count` 列歧义；真实 PostgreSQL upsert 缺陷未被当前 mock 测试关闭。Issue `25493ed6-...` 已更新。 |
| Dependency Exposure | ⚠️ | `npm audit --omit=dev` 为 54 moderate / 2 high / 0 critical；high 仍来自 OpenTelemetry 依赖链。 |
| Dev Workflow Portability | ⚠️ | 隔离测试必须显式 `DB_NAME=cecelia_test` 才能通过 DB guard；BusyBox 工具差异与外置 worktree 依赖合同仍由既有 Issue `5ff60dff-...` 跟踪。 |
| Temp Patch Accumulation | ⚠️ | 根目录继续跟踪任务特定 `.brain-result.json`/`.task`；非测试代码仍有 mock Cortex、通知/预测、S3/NAS 与 healing TODO。 |
| Ledger Freshness | 🔴 | MJ5 Journey 台账落后相关合并 PR 已超过 25 天；多数 Journey 与 PR/任务仍无可机械证明的关联字段。Issue `5f3f0d07-...` 已更新。 |
| Ledger Schedule Window | ⚠️ | 当前 payload 有 23 条 line ledger，但固定 20:00 review 早于 21:00 dreaming 的空窗合同未改。Issue `a1ed9884-...`。 |
| Journey Smoke Registry | ⚠️ | production active 23：12 路径存在、4 缺失、1 TBD、6 空；staging active 21 / E2E 0。Issue `925aa82e-...` 已更新。 |
| Deploy Config Drift | ⚠️ | 两份 `.env.docker` 仅 deploy-main 声明 production tick=false；compose 默认 true、DB 又启用 tick。staging hard-off 值与持久化源一致，但其语义不能关闭独立 loops；GitHub variables 为空且查询成功。Issue `b6a0dd31-...`。 |
| Deployment Closure | ✅ | production/staging 同为 `1.272.28@d9b01327ee`；Gate 3 `31617178437`、staging deploy `31617178287`、CI/CodeQL/Smoke Glob 均成功。 |
| Kernel CI Truth Compression | ⚠️ | 三条 nightly 连续红而 main deploy 持续绿，独立 Promote All workflow 仍无 run；release/部署成功语义没有统一。Issue `d44556d8-...`。 |
| Kernel PR Identity | ✅ | #4832/#4835 的 PR URL 独立校验、shell argv 与 reconcile 公平性继续有效；本轮 fleet rollout 未回退。 |
| Realtime Voice Duplication | ⚠️ | 5 个物理 `useRealtimeVoice` 文件、至少两组实现仍未收敛。Issue `1daab7e4-...`。 |
| Janitor /dev Lock Contract | ⚠️ | `/dev` 预建 worktree 位于 `/Users/administrator/worktrees`，Janitor 扫描/锁路径仍无统一跨模块合同。Issue `c00b0090-...`。 |
| Scheduler Test Collection | ⚠️ | arch-review 6 files/42 tests 全过，但 `daily-review-scheduler.test.js` 显式请求仍因标准 config exclude 而 0 收集。Issue `0acbf8f3-...`。 |
| Guard Drill Idempotency | ⚠️ | rolling 24h production/staging 各 12 条同名 Issue；staging 重启又新开单，持久 gate/guard_id upsert 未落地。Issue `9893e28b-...` 已更新。 |
| Nightly Test Truth | ⚠️ | Smoke E2E、Nightly Regression、Integration Nightly 最近 8 次全失败；Real-Machine 最近 6 次成功，四闸语义仍分裂。Issue `af7361b3-...`。 |

## 4B Structural Health Findings

- #4840 把路由归一放在 executor 调用前，却没有放在 admission 前；文件头注释声称“在此之前已改道”，实际控制流相反，文档、测试与执行顺序形成三账。
- `payload.repo`、`payload.base_repo` 与 Kernel 解析后的 canonical repo 仍是三套身份；跨仓任务既可能被误当 Cecelia，又可能在 Kernel 深处才以不支持终止。
- 当前 review 的 lock/PID 足以证明本进程活着，但 DB 无 heartbeat/run-id；相邻任务的 15 次 callback 和 10 次回队又证明“有回执”仍不等于“终态已应用”。
- staging 的主 tick hard-off、独立 scheduler hard-off 与 instance-aware health 是三个不同合同；当前只实现第一个。
- migration 411 本身完整，但 schema max/ledger rows/物理列继续分裂；相同镜像和最大版本号不能证明环境等价。
- nightly 三红、Real-Machine 连绿、main 自动部署连绿；质量闸、发布闸与部署闸缺少共同 owner/fixture/诊断语义。
- God Module、Cortex 越层、Map legacy、Realtime Voice 复制与根目录任务产物均无结构性下降。
- 新文件均有对应测试；新增功能的 7 files/196 tests 与 review 6 files/42 tests 全过，但测试绿色没有覆盖本轮真实暴露的 admission 顺序和跨仓 repo 行为。

## Severity

- 🔴 High: 10 项（文档正本、环境副作用隔离、review 身份、verdict/callback、dev→harness 路由、tick 互斥、tasks 时间、recovery 投影、schema parity、MJ5 ledger freshness）
- ⚠️ Medium: 16 项（边界、Map 遗留、复杂度、incident、依赖、`/dev` 可移植性、结构债、ledger 时窗、Journey E2E、deploy 配置、Kernel CI、语音复制、Janitor、scheduler 测试、guard-drill、nightly 真值）
- ✅ Clean: 10 组（Brain health/breaker、三项 DevGate、238 项定向回归、internal auth、migration 411、同 SHA 部署、Map 权威、当前 ledger 注入、Kernel PR 权威、Real-Machine Smoke）

## Required Actions

- [ ] **P0** — 将 `dev → harness` canonical routing 前移到 candidate admission 之前；repo/base_repo 使用单一 resolver，改道任务必须经过 harness cap/initiative lock、跳过 bridge，并用跨仓真实 dispatcher 回归证明。
- [ ] **P0** — 统一注册并 hard-off 非 production 全部 loops；health 必须按 instance 身份报告，staging/preview 不得创建 task/issue/notification/deploy 副作用。
- [ ] **P0** — 将 review claim/run-id/heartbeat/callback 收敛为 DB 原子合同；callback 必须验证 HTTP 与 CAS `applied`，CRITICAL/未知 verdict fail-closed，已 terminal 任务不得被 startup/recovery 重派。
- [ ] **P0** — 完成 tasks 全量 `timestamptz`、tick cancel/join 或租约，以及 liveness/recovery 的 executor kind 权威投影。
- [ ] **P0** — 建立 migration ledger ↔ 物理 schema ↔ consumer 通用 parity gate，先补齐 staging `tasks.retry_count`，再关闭旧 schema Issue。
- [ ] **P1** — 修复 MJ5 台账、Journey E2E registry、`arch_review_followup` 路由与 20:00/21:00 账本时序。
- [ ] **P1** — 修复 incident recurrence upsert、guard_id 去重和三条 nightly，统一 Real-Machine/Release/Deploy 成功语义；恢复 scheduler 测试进标准 CI。
- [ ] **P2** — 更新 SYSTEM_MAP/KERNEL_HARNESS_MAP/DEFINITION schema 文案，收敛 Cortex 边界、God Module、OpenTelemetry 漏洞、语音复制、Map legacy 与根目录任务产物。

## Verification Evidence

- base/runtime：`origin/main=d9b01327ee`；production/staging 均为 `1.272.28@d9b01327ee`，healthy，breakers all CLOSED。
- 三项 DevGate：facts-check、version sync、DoD mapping 全部 PASS；facts 为 Brain `1.272.28`、schema floor 411。
- 新变更定向回归：7 files / 196 tests 全过；arch-review/liveness/tick 回归 6 files / 42 tests 全过。完整 `daily-review-scheduler.test.js` 仍 0 收集。
- review：当前 task lock run id=`42067bd1-...`、PID 820/831 存活，DB heartbeat/run-id 空；12:02 task 15 callbacks + 10 safe requeues 后仍 in_progress、无 lock/PID。
- routing：`dispatcher.js:536/552/704` 早于 `800/807` 改道；跨仓 task `cb40da23-...` / run `dcd791fb-...` 以 `workspace_repo_not_supported` failed。
- temporal：当前 task started/claimed 相差 13 小时；两库 lifecycle timestamp 与 claim/heartbeat timestamptz 混用。
- DB：production/staging schema max=411、ledger rows=404/401；migration 411 列/约束/trigger 完整，staging `tasks.retry_count` 缺失。
- runtime isolation：staging hard-off 日志仍启动 Nightly/33 scheduler/Promotion，重启后真实创建 guard-drill Issue；health 与 production 同报 scheduler running/last_tick。
- ledger/smoke：production active 23 / E2E 17 / empty 6，17 中 12 存在、4 缺失、1 TBD；staging 21 / E2E 0。MJ5 lag >25 天。
- guard/incident：rolling 24h 两库各 12 条同名 guard-drill Issue；production 日志持续报 `recurrence_count` 歧义。
- CI：三条 nightly run `31530646737` / `31530668738` / `31537612044` 最近 8 次全红；Real-Machine 最新 `31609046255` 成功且最近 6 次全绿。
- dependency/code：audit 54 moderate / 2 high / 0 critical；Brain 非测试 JS 636 文件/175127 行，最大文件 `executor.js` 4615 行。
- issues：复用现有追踪，更新 10 个 Issue；本轮不重复创建 Issue。

## ADR Needed? 是

需要定义五项跨模块合同：candidate admission 前的 canonical task/repo routing、环境级统一 loop 生命周期与 instance-aware health、review claim/run-id/heartbeat/callback 唯一身份、migration/物理 schema/consumer 三方 parity，以及 nightly/Release/Deploy 的共同成功语义。

## Escalate to /architect? 是

dev→harness admission、跨仓 repo 身份、staging 副作用隔离、review 终态、时间/互斥、schema parity 与 nightly 真值跨越 dispatcher、executor、scheduler、DB、runner、CI 和多仓 registry；本巡检只记录证据与约束，不修改运行时代码。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 32,
  "health_findings": 8,
  "high_severity": 10,
  "medium_severity": 16,
  "issues_created": 0,
  "issues_updated": 10,
  "escalate_to_architect": true,
  "summary": "1.272.28 与 migration 411 已同 SHA 部署且冻结合同测试落地；但 #4840 的 dev→harness 改道晚于 admission/bridge 且跨仓 repo 身份分裂，12:02 review 已交付仍 10 次回队/15 次 callback 后 in_progress，CRITICAL 继续被回退 PASS；staging hard-off 仍写 Issue，时间/schema parity/MJ5/nightly 继续未闭合"
}
```
