---
id: arch-review-20260808
version: 1.0.0
created: 2026-08-08
updated: 2026-08-08
reviewer: Codex (Brain 自动调度)
task_id: 114cc58a-dc6b-411f-b3e8-b25e8414368d
base_sha: b11b9d9831ff004b2a70a2383775b611337bf9f1
previous_task_id: 273e6dc0-01b5-4a94-89d8-585834b15117
---

# Architecture Review — 2026-08-08 daily（00:00 UTC）

## Executive Summary

裁决仍为 **CRITICAL**：5 项高风险、9 项中风险。上一轮四个高风险根因均未收敛：tick 超时后仍只释放共享互斥状态而不终止旧执行；`tasks` 生命周期字段仍混用无时区和带时区类型；`recovery-loop.js` 仍未投影 `executor_kind`；`SYSTEM_MAP.md` 仍停在 Brain `1.216.0` / migration `262`。最新 PR #4709 没有触碰这四处，它修复的是 Harness 孤儿 run 复活与 spawn-guard 死锁。

本轮新增一项高风险实证：同一任务工作树先后留下 00:31 UTC 的已推送提交、00:43 UTC 的未提交收口修订，task lock 又在 00:52 UTC 被新 run 覆盖；但 Brain task 到 00:55 UTC 仍为 `queued`，`executor_kind/started_at/claimed_at/last_attempt_at/driver_heartbeat_at` 全为空。00:56 UTC 后仅 `started_at` 被补写，直到 01:01 UTC（PR 已建立约 30 分钟后）才进入 `in_progress` 并写入 `claimed_by=brain-tick-7`、`executor_kind=codex-review-local`；01:06 UTC 复核时 heartbeat/last_attempt 仍为空。也就是说 review runner 的进程/lock 与 Brain task claim 曾长期是两本账，并已造成同任务重复执行，历史 `never_started/process_disappeared` 风险尚未闭环；已把本轮证据追加到既有 P0 Issue `6adfa549-5b69-4883-b087-649be182b8db`，未重复建单。

照相层自动刷新出现明确回归：`api_registry`、`db_schema_registry`、`test_registry` 的最新扫描均停在 2026-07-18，初始复核时账龄 488.5 小时，收口时已达 489.1 小时。原 Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457` 曾因 PR #4082 标记修复，本轮已按真实接口结果重开。E2E 台账基线同步刷新为：19 条非 smoke/test active journey，17 条有声明，其中 12 条真实存在、4 条路径缺失、1 条是 TBD 文本，另有 2 条为空。

初始快照时生产 Brain 为 `healthy`，版本 `1.270.2@b11b9d98` 与 main 一致，dispatch circuit breaker 为 `CLOSED`。但 00:41 UTC 后生产转为 `degraded`，收口时已随 PR #4714 升至 `1.270.3@41707aa5`：`cecelia-run` 失败计数一度达到 31，01:06 UTC 仍以 27 次失败保持 `OPEN`，同时 dispatch 仍为 `CLOSED`；日志还记录了 `reportIncident` 因 `recurrence_count` 列歧义上报失败。staging 实际端口 5222，版本 `1.270.2@b11b9d98`，因 `CECELIA_TICK_HARD_OFF=1` 按设计报告 degraded，落后生产一个 patch。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | L0/服务层仍在 `report-48h.js:144`、`monitor-loop.js:483`、`dispatch-helpers.js:206`、`capability-probe.js:166` 直调 `cortex.js`；HTTP 路由 `analytics.js:5/234`、`ops.js:2147` 也直连 Cortex。标准 L1→L2 入口仍是 `thalamus.js:1607`。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:46/65/79/104-107` 仍写 Brain `1.216.0`、139 个 migration、Schema `262`；收口事实为 Brain `1.270.3`、Schema `393`。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 未收敛。 |
| Review Execution Contract | 🔴 | 当前任务 worktree/PR/lock 都有真实进展，但生产 DB/API 长时间维持 `queued` 且没有 claim/executor/heartbeat；00:56 UTC 只补了 `started_at`，直到 01:01 UTC 才进入 `in_progress` 并补 claim/executor，此时 PR 已存在约 30 分钟，且 heartbeat/last_attempt 仍为空。Brain executor 的 `triggerCodexReview()` 会写 lock 并打 `codex-review-local`，当前外部 runner 没有原子同步同一合同，claim 前队列行可被再次消费。收口时 `cecelia-run` 熔断器仍为 OPEN，且熔断告警的 incident 写入自身失败。Issue `6adfa549-5b69-4883-b087-649be182b8db` 已补充本轮证据。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us` 与 `codex-review-local` 合同在代码内存在，但外部 runner 没有原子 claim；`arch_review_followup` 仍不在 task type 合同中。AGENTS 声明的 `/api/brain/skills/count` 和 `/api/brain/skills/arch-review` 继续返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4609L/30；`planner.js` 1247L/4；`thalamus.js` 1807L/6；`cortex.js` 1593L/13。五个核心文件自上一轮均未变化，`executor.js` 仍是主要 God Module。 |
| Concurrency Safety | 🔴 | `tick-loop.js:65-94` 仍在 60 秒后清 `tickRunning/tickLockTime`，没有 cancel、join 或代际隔离旧 `doTick()`；现有单测甚至明确断言“超时强制释放锁”。本轮生产观测窗无 `FORCE-RELEASE`，但竞态代码事实未变。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0` 未收敛。 |
| Temporal Integrity | 🔴 | `tasks.created_at/started_at/completed_at/updated_at/queued_at` 仍是 `timestamp without time zone`，`claimed_at/driver_heartbeat_at` 是 `timestamptz`，DB 时区为 `America/Chicago`。当前 00:00 UTC 任务的 SQL 创建时间为 Chicago 19:00:44，API 却序列化为 11:00:44 UTC，再次产生 13 小时表面偏差。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b` 未收敛。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js:48-53` 仍只 SELECT `id/title/status/priority/started_at/updated_at/payload/task_type`，遗漏 `executor_kind/last_attempt_at/claimed_by`，wall-clock liveness 继续拿不到执行合同。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 未收敛。 |
| Registry Freshness | ⚠️ | 三类 registry API 均返回 `stale=true`，账龄从初始 488.5h 增至收口 489.1h，最新扫描停在 2026-07-18；部署仓只有脚本和“cron 已安装”的历史声明，当前事实证明调度没有持续点火。原 Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457` 已重开。 |
| Dev Workflow Portability | ⚠️ | `/dev` worktree 入口仍在 `packages/engine/skills/dev/scripts/worktree-manage.sh:120` 使用 BusyBox 不支持的 `flock -w`；本任务继续通过显式隔离 worktree 执行。Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7` 未收敛。 |
| Temp Patch Accumulation | ⚠️ | 超期结构债未减少；根目录仍跟踪任务特定 `.brain-result.json` 与 `.task`。前者可被后续执行误读为当前结果。 |
| Ledger Freshness（探针A） | ⚠️ | 按最近 merged PR、无匹配时以 completed task 兜底，`ZenithJoy 客户管理` 台账落后 19 天、`工厂 · MJ5 承诺地图闭环` 落后 10 天；均超过 7 天但未超过 21 天。当前任务携带 19 条 line ledger，仍有 113 个“暂无”；PR #4696 后仅 `工厂 · F2 部署闭环` 出现 3 条 failed run，其余 18 条仍全空。 |
| Smoke Consistency（探针B） | ⚠️ | 19 条非 smoke/test active journey 中，17 条声明 E2E：12 条在声明对应的 `cecelia/main` 或 `zenithjoy-workspace/main` 真实存在，4 条路径在两仓均缺失，1 条为 TBD 文本；另有 2 条未声明。废弃关键词命中仅是“已去飞书”的合规注释或不适用的告警通道，没有新增真实冲突；Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 已纠正基线。 |
| Deploy Config Drift（探针C） | ⚠️ | 收口时生产 `1.270.3@41707aa5` 与 main 一致，但健康状态因 `cecelia-run=OPEN` 为 degraded；staging `1.270.2@b11b9d98` 因硬关闭 tick 为 degraded，落后一个 patch。生产关键 tick 变量与部署仓持久化值一致；主仓与部署仓 `.env.docker` key-set 仍差 3 项：部署仓单独含 `ACCEPTANCE_API_TOKEN`、`ACCEPTANCE_PUBLIC_HOST`、`CECELIA_TICK_ENABLED`。GitHub variables 查询成功且为空；当前执行环境无 `/Library/LaunchDaemons`，launchd 子项不可观测。 |

## 4B Structural Health Findings

本次为 daily 增量扫描，基线为上一轮使用的 `0c893a7f`：

- 初始快照 main 新增上一轮报告 PR #4710 与 Harness 孤儿 run 修复 PR #4709；收口前又合入 D3 背靠背裁剪 PR #4714。`packages/brain/src` JS/TS 从 2028 增至 2035，其中生产文件从 588 增至 590。
- #4709 新增 `harness-run-guard.js` 与 `relay-forensics.js` 两个生产模块，配套 4 个新增测试文件、既有测试扩展、真实 PostgreSQL integration test 和 `orphan-run-revival-smoke.sh`；本轮专用 smoke PASS。
- #4714 修改 acceptance public server/routes 并新增 1 个 647 行测试文件与真实 smoke；该 PR 的 Sprint/DoD/TDD、6 个 brain-unit shard、integration、real-env smoke 与 CodeQL 均通过，没有触碰本轮五个高风险根因。
- 合入 #4714 后硬门禁真实捕获 `.brain-versions` 仍为 `1.270.2`、与 package/DEFINITION 的 `1.270.3` 不一致；本报告 PR 同步该版本指针，`check-version-sync.sh` 已由红转绿，不把已知配置漂移带入 main。
- 前序执行完成两组 8 文件定向测试：124/124 与 133/133，均 PASS（集合有重叠，不累加）；接续收口又独立实跑 9 文件 78/78 PASS，覆盖 tick、recovery、review lock liveness、line ledger 注入、startup revival、run guard 与 forensic。这些测试同时证明 tick 强制解锁与 recovery 字段缺口仍是“测试保护的现状”，不是已修复设计。
- 核心五文件、Cortex 直连点、`SYSTEM_MAP` 旧事实与 recovery 投影自上一轮均未变化，没有发现 #4709 新增未配对的生产孤岛。
- 当前任务的 commit、未提交修订、PR 与 lock 均存在，但 DB 长时间未 claim；PR 建立约 30 分钟后才补 claim/executor，heartbeat/last_attempt 继续为空。这是运行时合同漂移，不是静态 import 或测试配对问题。

## Severity

- 🔴 High: 5 项（review runner/Brain claim 双账、tick 超时互斥失效、tasks 时间契约分裂、recovery 超时网字段缺口、CURRENT_STATE 假新鲜）
- ⚠️ Medium: 9 项（边界、路由/API 文档、复杂度、registry 账龄、`/dev` 可移植性、超期结构债、line ledger、E2E 声明、部署源/版本）
- ✅ Clean: 5 项（#4709 测试配对、facts 校验、版本同步、生产 runtime/main 一致、dispatch circuit breaker 保持关闭）

## Required Actions

- [ ] **P0** — 处理 Issue `6adfa549-5b69-4883-b087-649be182b8db`：外部 review runner 启动进程/写 lock 时必须原子 claim task、写 `executor_kind=codex-review-local` 与 heartbeat；回归必须证明活执行不保持 queued、同 task 不会二次派发、回调后 lock 与 task 同步终态。
- [ ] **P0** — 处理 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：为 tick 引入可传播的取消/截止合同，或在旧执行真实终止前保留全局租约；旧 tick 存活时新 tick 不得进入。
- [ ] **P0** — 处理 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一 `tasks` 生命周期字段为明确 UTC/timestamptz 契约，并覆盖 PostgreSQL 非 UTC + Node 不同时区回归。
- [ ] **P1** — 处理 Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`：让 recovery-loop 查询完整 executor contract 字段，证明活执行不杀、死/超龄执行按合同收口。
- [ ] **P1** — 处理 Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`：重建 `SYSTEM_MAP.md` 当前事实，修正 skills API 与 followup 合同文档。
- [ ] **P1** — 处理已重开的 Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457`：恢复 registry 日扫/事件扳机并增加连续账龄告警，验收下一次 main merge 后三表 `scanned_at` 真更新。
- [ ] **P1** — 继续验证 PR #4696 的 line ledger 归属：本轮只有 F2 run 局部出账，需把 decisions/issues/learnings 与其余 18 条 line 的真实变化接入。
- [ ] **P1** — 处理 Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 与 `5ff60dff-d910-4101-b3b8-5d3695b819b7`：修复 E2E 路径/空声明和 BusyBox worktree 锁兼容。
- [ ] **P2** — 定义 L0/HTTP 路由直调 Cortex 的豁免边界；继续拆分 `executor.js`，清理根目录任务结果产物，并收敛双 `.env.docker` 为唯一配置 SSOT。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.270.2`、Schema `393`、migration 无冲突、LLM fetch timeout 完整）。
- `bash scripts/check-version-sync.sh`：初始快照 PASS（`1.270.2`）；合入 #4714 后因 `.brain-versions` 漏升至 `1.270.3` 先 FAIL，本 PR 同步后复跑 PASS。
- `NODE_PATH=... node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- Brain 定向 Vitest：前序两组分别为 8 files / 124 tests 与 8 files / 133 tests，接续收口为 9 files / 78 tests，均 PASS；#4709 `orphan-run-revival-smoke.sh` 再次实跑 PASS。
- PR #4709 已合并，全部 required checks、6 个 brain-unit shard、integration、real-env smoke、CodeQL 与 auto-merge 均成功。
- PR #4714 已合并，全部 Sprint/DoD/TDD、6 个 brain-unit shard、integration、real-env smoke、CodeQL 与 auto-merge 均成功；收口 main 为 `41707aa5`。
- 生产真实状态：初始快照为 Brain `healthy`、`1.270.2@b11b9d98`、dispatch breaker CLOSED；收口时已为 `degraded`、`1.270.3@41707aa5`，`cecelia-run` 失败计数一度 31、01:06 UTC 为 27 且仍 OPEN，dispatch 仍 CLOSED，incident reporter 因 SQL 列歧义未能落库。Schema `393`。staging `degraded`、`1.270.2@b11b9d98`，硬关闭 tick。
- 当前执行活性：同一任务先有 00:31 UTC 提交/PR、00:43 UTC 未提交修订，00:52 UTC task lock 被新 run 覆盖；DB/API 到 00:55 UTC 仍 queued 且执行字段全空，00:56 UTC 后仅 `started_at` 非空，01:01 UTC 才补 claim/executor 并转 `in_progress`，01:06 UTC heartbeat/last_attempt 仍为空。
- Registry API：api/db_schema/test 三类账龄从 488.5h 增至 489.1h；19 条 active journey 的 E2E 路径通过两个 GitHub main 分支逐条核查。

## ADR Needed? 是

需要明确三项跨模块合同：review 外部 runner 与 Brain task 的唯一 claim/lock/heartbeat 生命周期、tick 超时后的取消/互斥租约、任务生命周期时间的唯一类型/时区。Registry 调度与 recovery 字段投影属于实现缺陷，不需要另立 ADR。

## Escalate to /architect? 是

review 执行合同、tick 取消传播、时间类型迁移与 CURRENT_STATE 正本涉及跨模块边界；本巡检只记录事实、更新既有 Issue，不在报告 PR 中设计或修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 14,
  "health_findings": 5,
  "high_severity": 5,
  "escalate_to_architect": true,
  "summary": "生产版本与 main 一致且 #4709/#4714 测试健康；但 review runner/Brain claim 双账复发并伴随 cecelia-run 熔断重开，tick、tasks 时间、recovery 与 CURRENT_STATE 四项既有高风险未收敛，registry 自动刷新再次失效"
}
```
