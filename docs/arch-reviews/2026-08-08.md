---
id: arch-review-20260808
version: 1.1.0
created: 2026-08-08
updated: 2026-08-08
reviewer: Codex (Brain 自动调度)
task_id: afc5dd0e-807c-4ef2-89f6-f07ff0f435ed
base_sha: c0ad461b5f1f6fedff42927c6130b6fdceffd488
previous_task_id: 114cc58a-dc6b-411f-b3e8-b25e8414368d
---

# Architecture Review — 2026-08-08 daily（12:00 UTC）

## Executive Summary

裁决仍为 **CRITICAL**：5 项高风险、10 项中风险。上一轮五个高风险合同均未完成收敛：review runner 的任务/进程活性仍缺完整 heartbeat 与 run 身份；tick 超时仍会在旧执行结束前释放共享互斥状态；`tasks` 生命周期时间仍混用无时区和带时区类型；`recovery-loop.js` 仍未投影 `executor_kind`；`SYSTEM_MAP.md` 仍停在 Brain `1.216.0` / Schema `262`。本次任务由 Brain 于 16:41:48 UTC 原子 claim 并同步生成 lock，未复发 queued/lock 双账，但 `last_attempt_at`、`driver_heartbeat_at` 与 `payload.current_run_id` 仍为空，因此只能证明当前一次启动成功，不能关闭执行合同缺口。

本轮新增一项 P1 架构漂移：生产 `schema_version` 已登记 migration 153，但 `public.task_execution_metrics` 真实 relation 不存在。`execution-callback` 在每次任务回调时持续输出 `relation "task_execution_metrics" does not exist`，`brain-meta.js` 的消费查询也失去数据源。新 Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3` 已登记；这证明“migration 已应用”不能替代关键 relation 存在性校验。

上一轮收口后 main 合入 runner 凭据单链、kernel stdio 落盘、fleet canonical digest 修复与 callback 单消费者租约。生产当前为 `healthy`、`1.270.8@c0ad461b`，与 main 一致，两个 circuit breaker 均为 `CLOSED`；staging 为 `1.270.7@c33649e3`，按 `tick hard-off` 设计报告 `degraded`，落后一个 patch。callback lease 修复有 migration 394 与配套测试，但没有触碰上述五个高风险根因。

生产在 16:35:57、16:44:53、16:47:58 UTC 三次触发 `FORCE-RELEASE`；旧 tick 分别在 2.8 秒、0.2 秒、0.1 秒后才结束。本观测窗没有看到新 tick 抢入这几个短窗口，但代码仍允许该竞态，且当天早先已有真实多轮重叠证据，风险不降级。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | L0/服务层仍在 `report-48h.js:144`、`monitor-loop.js:483`、`dispatch-helpers.js:206`、`capability-probe.js:166` 直调 `cortex.js`；HTTP 路由 `analytics.js:5/234`、`ops.js:2147` 也直连 Cortex。标准 L1→L2 入口为 `thalamus.js:1616`，豁免边界仍未定义。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:46/65/104-107/116` 仍写 Brain `1.216.0`、139 个 migration、Schema `262`；当前事实为 Brain `1.270.8`、Schema `394`。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 仍为 In progress。 |
| Review Execution Contract | 🔴 | 当前任务的 DB claim、`executor_kind=codex-review-local` 与 lock 在 16:41:48 UTC 同步建立，`retry_count=0`、`error_message=null`；但 lock 的 `runId=19263f9d-...` 没有进入 task payload，`last_attempt_at/driver_heartbeat_at` 为空。当前未重复派发，但 owner liveness 仍由 task、lock、宿主进程三套事实拼接。Issue `6adfa549-5b69-4883-b087-649be182b8db` 仍为 In progress。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us` 可用；`arch_review_followup` 仍不在 task type 合同中。AGENTS 声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 均真实返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4615L/30；`planner.js` 1247L/4；`thalamus.js` 1816L/6；`cortex.js` 1593L/13。`executor.js` 仍是主要 God Module，本轮再净增 6 行。 |
| Concurrency Safety | 🔴 | `tick-loop.js:65-94` 仍在 60 秒后清 `tickRunning/tickLockTime`，没有 cancel、join 或代际隔离旧 `doTick()`。生产本轮三次在旧执行结束前先释放互斥状态；虽然空窗很短且未观测到新 tick 抢入，代码与历史重叠事实均未收敛。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0` 仍为 In progress。 |
| Temporal Integrity | 🔴 | `tasks.created_at/started_at/completed_at/updated_at/queued_at` 仍为 `timestamp without time zone`，`claimed_at/driver_heartbeat_at` 为 `timestamptz`，DB 时区为 `America/Chicago`。当前任务 SQL 中 claim 与 start 相差毫秒，API 却把 `started_at` 显示为 03:41 UTC、`claimed_at` 显示为 16:41 UTC，继续制造 13 小时表面差。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b` 未收敛。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js:50` 仍只 SELECT `id/title/status/priority/started_at/updated_at/payload/task_type`，遗漏 `executor_kind/last_attempt_at/claimed_by`，无法按执行器合同判断 wall-clock liveness。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 仍为 In progress。 |
| Registry Freshness | ⚠️ | `api_registry`、`db_schema_registry`、`test_registry` 最新扫描仍停在 2026-07-18，账龄已达 504.9 小时；相比上一轮继续增长约 15.8 小时。Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457` 仍为 In progress。 |
| Migration Ledger Integrity | ⚠️ | `schema_version` 记录 version 153 已应用，migration 文件也声明创建 `task_execution_metrics`，但生产 `to_regclass('public.task_execution_metrics')` 为 NULL；回调写入与 brain-meta 读取均失去 relation。已新建 P1 Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3`。 |
| Dev Workflow Portability | ⚠️ | `worktree-manage.sh:120` 仍调用 BusyBox 不支持的 `flock -w 5 201`。本任务通过显式隔离 worktree 执行；Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7` 仍为 In progress。 |
| Temp Patch Accumulation | ⚠️ | 根目录仍跟踪任务特定 `.brain-result.json` 与 `.task`；前者固定指向旧任务 `93161b22-...` / PR #4620。`report-48h.js:119` 的 `mock_cortex` TODO 等超期结构债也未清理。 |
| Ledger Freshness（探针A） | ⚠️ | 当前任务携带 19 条 line ledger，共 113 个“暂无”；只有 `工厂 · F2 部署闭环` 出现 3 条 `task_failed_upstream`，其余 18 条仍全空。PR #4696 后归属完整率仍未形成可用信号。 |
| Smoke Consistency（探针B） | ⚠️ | 19 条非 smoke/test active journey 中 17 条声明 E2E：跨 `cecelia/main` 与 `zenithjoy-workspace/main` 逐条核验后，12 条存在、4 条缺失、1 条为 TBD，另有 2 条为空。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 的基线仍准确。 |
| Deploy Config Drift（探针C） | ⚠️ | 生产 `1.270.8@c0ad461b` 与 main 一致且 healthy；staging `1.270.7@c33649e3` 因 tick hard-off 为 degraded，落后一个 patch。staging 日志持续报告两个 Claude account 凭据路径不存在并把 token pool 估为 abundant，说明禁调度环境的资源观测仍有噪声。 |

## 4B Structural Health Findings

本次为 daily 增量扫描，基线为上一轮收口 main `0faee437`：

- main 共变化 63 个文件，`+2026/-163`；大头是 fleet 设计/计划、runner smoke、callback 测试与 execution route，`packages/brain/src` JS/TS 文件总数为 2038，生产文件为 590。
- 没有新增 `packages/brain/src` 生产模块；主要生产 churn 为 `routes/execution.js`（`+58/-12`）、`codex-bridge.cjs`（`+47/-21`）、`callback-processor.js`（`+30/-16`）与 `callback-worker.js`（`+28/-7`）。
- PR #4720 把 runner 凭据从副本切到单链挂载；PR #4721 为 kernel launch 增加 stdout/stderr 落盘；PR #4722 为 canonical runner digest 增加 build/install/rollout 双向守卫；PR #4724 引入 callback queue 单消费者租约与 run-id compare-and-set。四组 required `ci-passed` 均成功。
- migration 394 已真实应用，生产 Schema MAX=394；但 migration 153 的 relation 缺失，说明按最大 schema 版本与“已应用集合”检查都无法发现中间对象被删/漏建。
- `tick-loop.js`、`recovery-loop.js`、`SYSTEM_MAP.md` 与 Cortex 直连点在增量中均未被修改；核心高风险不是新增回归，而是持续未收敛。
- 根目录 `.brain-result.json` / `.task` 仍是被版本控制的旧任务产物；后续 agent 若把根目录结果文件视为当前输出，仍可能误读。

## Severity

- 🔴 High: 5 项（review runner/Brain 活性合同、tick 超时互斥失效、tasks 时间契约分裂、recovery 字段投影缺口、CURRENT_STATE 假新鲜）
- ⚠️ Medium: 10 项（边界、路由/API 文档、复杂度、registry 账龄、migration relation 漂移、`/dev` 可移植性、超期结构债、line ledger、E2E 声明、部署观测）
- ✅ Clean: 6 项（当前 review claim/lock 同步、生产 runtime/main 一致、两个 breaker 关闭、callback lease 测试配对、runner/fleet smoke 配对、Schema 394 已应用）

## Required Actions

- [ ] **P0** — 处理 Issue `6adfa549-5b69-4883-b087-649be182b8db`：统一 review runner 的 claim、run_id、lock、heartbeat、回调终态，证明活执行不保持 queued、不被二次派发、宿主消失能准确收口。
- [ ] **P0** — 处理 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：为 tick 引入可传播的取消/截止合同，或在旧执行真实终止前保留全局租约；旧 tick 存活时新 tick 不得进入。
- [ ] **P0** — 处理 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一 `tasks` 生命周期字段为明确 UTC/timestamptz 契约，并覆盖 PostgreSQL 非 UTC + Node 不同时区回归。
- [ ] **P1** — 处理 Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 与 `bb78074b-eca4-414e-a7f3-a9425824f7de`：补齐 recovery executor 投影，重建 `SYSTEM_MAP.md` 当前事实，并修正 skills API / followup 合同文档。
- [ ] **P1** — 处理新 Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3`：用可重入 migration 恢复 `task_execution_metrics` 表与索引，并让 selfcheck 验证关键 relation，而非只检查 Schema MAX。
- [ ] **P1** — 处理 Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457`：恢复 registry 日扫/事件扳机并增加连续账龄告警，验收三表 `scanned_at` 真更新。
- [ ] **P1** — 继续验证 line ledger 归属，并处理 Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 与 `5ff60dff-d910-4101-b3b8-5d3695b819b7`：修复 4 个缺失、1 个 TBD、2 个空 E2E 声明及 BusyBox worktree 锁兼容。
- [ ] **P1** — 处理既有 Issue `25493ed6-ebd4-4dd6-8a23-e5eba953055f`：修复 incident reporter 的 `recurrence_count` 列歧义；当前 launchd-patrol 告警仍持续写入失败。
- [ ] **P2** — 定义 L0/HTTP 路由直调 Cortex 的豁免边界，继续拆分 `executor.js`，移除根目录任务结果产物，并收敛 staging 资源观测噪声。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.270.8`、selfcheck floor `393` 不高于最高 migration `394`，migration 编号与 LLM timeout facts 一致）。
- `bash scripts/check-version-sync.sh`：PASS（Brain 版本四处同步为 `1.270.8`）。
- `NODE_PATH=... node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- Brain 定向 Vitest：tick-loop、recovery-loop、callback processor/worker、migration 394、review liveness 共 8 文件 / 75 tests 全部通过；这些测试同时证明现有 tick force-release 与 recovery 投影仍是受测试保护的当前行为。
- 生产真实状态：Brain `healthy`、`1.270.8@c0ad461b`，两个 circuit breaker CLOSED；当前任务 lock/claim 同步、无 retry/error，但 heartbeat/last_attempt/current_run_id 为空。staging `1.270.7@c33649e3`，tick hard-off。
- 生产日志：16:35:57、16:44:53、16:47:58 UTC 三次 `FORCE-RELEASE`；持续出现 `task_execution_metrics` relation 缺失与 incident reporter `recurrence_count` 歧义。
- 生产 DB 只读探针：Schema MAX=394；tasks 七个时间字段类型与 America/Chicago 时区；migration 153 已登记但 `task_execution_metrics` relation 为 NULL；registry 三表账龄 504.9h。
- 台账探针：19 条 line ledger、113 个“暂无”、3 条 F2 upstream failure；19 条 active journey 中 E2E 为 12 存在 / 4 缺失 / 1 TBD / 2 空。
- 增量 PR #4720/#4721/#4722/#4724 均已合并且 required `ci-passed` 成功；生产已部署到对应 main SHA。

## ADR Needed? 是

需要明确 review runner 的唯一 claim/lock/heartbeat 生命周期、tick 超时后的取消/互斥租约、任务生命周期时间的唯一类型/时区，以及 L0/HTTP 直调 Cortex 的合法边界。migration 153 relation 缺失、registry 刷新与 recovery 字段投影属于实现/治理缺陷，不需要另立 ADR。

## Escalate to /architect? 是

review 执行合同、tick 取消传播、时间类型迁移、CURRENT_STATE 正本与 Cortex 边界涉及跨模块契约；本巡检只记录事实与行动项，不在报告 PR 中设计或修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 15,
  "health_findings": 6,
  "high_severity": 5,
  "escalate_to_architect": true,
  "summary": "生产与当前 arch-review 可运行，callback/runner/fleet 增量有测试保护；但 review 活性、tick、tasks 时间、recovery、CURRENT_STATE 五项高风险未收敛，并新增 migration 153 已登记但关键 relation 缺失的 schema 双账"
}
```
