---
id: arch-review-20260808
version: 1.2.0
created: 2026-08-08
updated: 2026-08-08
reviewer: Codex (Brain 自动调度)
task_id: e91f09c5-9807-43b7-b773-6ab602ef0db3
base_sha: c5d62250f9267c268f9478e5087aaddea2117b53
previous_task_id: afc5dd0e-807c-4ef2-89f6-f07ff0f435ed
---

# Architecture Review — 2026-08-08 daily（16:01 UTC）

## Executive Summary

裁决仍为 **CRITICAL**：6 项高风险、12 项中风险。上一轮五个高风险合同均未收敛，本轮又把 review 输出与终态回调之间的双重断链跑成了真实事故：16:01 任务第一轮于 16:58:54 UTC exit 0，报告原生 verdict 为 `CRITICAL`，但 `triggerCodexReview()` 的提取正则只接受 `PASS|FAIL`，因此日志实际发送 `verdict=PASS`。同时该派发从未把 lock 中的 run id 写入 `payload.current_run_id`，而 callback UPDATE 又要求两者相等；任务收到回调后没有进入终态，5 分钟后被 liveness 判死、回队，并于 17:06:24 UTC启动第二轮。新 P0 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7` 已登记。

上一轮 12:00 任务也呈现同一闭环缺口：PR #4726 已于 17:03:51 UTC 合并，任务在 17:05:09 UTC exit 0 并发送 PASS callback，却继续保持 `in_progress`；17:10:35 UTC 又被判死并重新派发。17:12 UTC 实测两个 review lock、两个 Codex 进程并存，分别执行已经完成过的 12:00 任务和当前 16:01 任务。当前两行 DB 的 `executor_kind=codex-review-local`，但 `payload.current_run_id`、heartbeat 和完成时间均为空。历史 `process_disappeared` 不是偶发进程问题，而是 run 身份、callback CAS 与恢复器组合出的确定性重跑环。

tick 互斥风险也从“短空窗”升级为本轮直接重叠：16:57:39 UTC 第一个 tick 超时释放锁后，新 scheduler 立即进入并派发任务；16:58:39 UTC 又出现第二次 `FORCE-RELEASE`，说明前一轮仍未闭合时第二轮已建立自己的 60 秒 timer。两个执行共享同一 `tickRunning` 布尔值，没有取消、join 或代际隔离。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0` 的生产证据继续扩大。

schema 消费双账新增第二条活跃断链：migration 153 已登记但 `task_execution_metrics` relation 仍不存在；同时 `notion-product-push` 每分钟报 `column "notion_page_id" of relation "tasks" does not exist`，Notion product push 全链持续失败。前者 Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3` 未收敛，后者已登记 P1 Issue `ff663429-5d75-4581-960d-fd676351c2eb`。三类 registry 账龄继续增长到 505.4 小时。

代码基线自 12:00 报告后没有新的生产代码变化。生产仍为 `healthy`、`1.270.8@c0ad461b`，两个 circuit breaker 均为 `CLOSED`；staging 为 `degraded`、`1.270.7@c33649e3`，按 hard-off 设计停调度。静态 facts、版本同步和 DoD 映射均通过，定向 8 文件 / 111 tests 通过；这些绿灯证明现有实现可重复，却没有覆盖 `CRITICAL` verdict、run-id CAS 或真实 schema 消费契约。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | L0/服务层仍在 `report-48h.js:144`、`monitor-loop.js:483`、`dispatch-helpers.js:206`、`capability-probe.js:166` 直调 `cortex.js`；HTTP 路由 `analytics.js:5/234`、`ops.js:2147` 也直连 Cortex。标准 L1→L2 入口为 `thalamus.js:1616`，豁免边界仍未定义。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:46/65/104-107/116` 仍写 Brain `1.216.0`、139 个 migration、Schema `262`；当前事实为 Brain `1.270.8`、Schema `394`。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 仍为 In progress。 |
| Review Execution Contract | 🔴 | 16:01 任务第一轮 exit/callback 后被判死并于 17:06 重跑；12:00 任务在 PR #4726 已合并、exit/callback 后仍保持 `in_progress`，17:10 再次重跑。17:12 实测两个历史/当前 review 进程和 lock 并存。Issue `6adfa549-5b69-4883-b087-649be182b8db` 未收敛。 |
| Review Verdict / Callback Integrity | 🔴 | `triggerCodexReview()` 只提取 `PASS|FAIL`，因此 `CRITICAL` 报告被 fallback 成 PASS；它写 lock 但不写 `payload.current_run_id`，而 execution callback 的 CAS 要求 run id 相等，拒绝更新后仍返回成功。P0 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us` 可用；`arch_review_followup` 仍不在 task type 合同中。AGENTS 声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 均真实返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4615L/30；`planner.js` 1247L/4；`thalamus.js` 1816L/6；`cortex.js` 1593L/13。`executor.js` 仍是主要 God Module，本轮基线后无代码变化。 |
| Concurrency Safety | 🔴 | `tick-loop.js:65-94` 仍在 60 秒后清共享布尔锁。16:57:39 force-release 后新 scheduler 进入，16:58:39 第二个 timer 再次 force-release，直接证明两轮未闭合执行重叠。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0` 未收敛。 |
| Temporal Integrity | 🔴 | `tasks.created_at/started_at/completed_at/updated_at/queued_at` 仍为 `timestamp without time zone`，`claimed_at/driver_heartbeat_at` 为 `timestamptz`，DB 时区为 `America/Chicago`。当前任务 SQL 中 claim 与 start 相差毫秒，API 却把 `started_at` 显示为 04:06 UTC、`claimed_at` 显示为 17:06 UTC，继续制造 13 小时表面差。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b` 未收敛。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js:50` 仍只 SELECT `id/title/status/priority/started_at/updated_at/payload/task_type`，遗漏 `executor_kind/last_attempt_at/claimed_by`，无法按执行器合同判断 wall-clock liveness。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 仍为 In progress。 |
| Registry Freshness | ⚠️ | `api_registry`、`db_schema_registry`、`test_registry` 最新扫描仍停在 2026-07-18，账龄已达 505.4 小时。Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457` 未收敛。 |
| Migration Ledger Integrity | ⚠️ | `schema_version` 记录 version 153 已应用，migration 文件也声明创建 `task_execution_metrics`，但生产 `to_regclass('public.task_execution_metrics')` 为 NULL；回调写入与 brain-meta 读取均失去 relation。既有 P1 Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3`。 |
| Schema Consumer Integrity | ⚠️ | `notion-product-push` 每分钟访问生产不存在的 `tasks.notion_page_id` 并失败，消费链没有启动时 relation/列契约校验。新 P1 Issue `ff663429-5d75-4581-960d-fd676351c2eb`。 |
| Dependency Exposure | ⚠️ | `npm audit --omit=dev` 返回 33 项生产依赖漏洞（31 moderate / 2 high），高危集中于直接依赖 `@opentelemetry/sdk-node` 及传递依赖，修复需要 semver major；既有 Issue `a46e7823-a98c-4ada-a421-4034419e6279` 仍为 In progress。 |
| Dev Workflow Portability | ⚠️ | 本任务再次真实复现 `worktree-manage.sh:120` 的 `flock -w 5 201` 在 BusyBox 首步失败，只能改用显式 Git worktree。Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7` 未收敛。 |
| Temp Patch Accumulation | ⚠️ | 根目录仍跟踪任务特定 `.brain-result.json` 与 `.task`；前者固定指向旧任务 `93161b22-...` / PR #4620。`report-48h.js:119` 的 `mock_cortex` TODO 等超期结构债也未清理。 |
| Ledger Freshness（探针A） | ⚠️ | 当前任务携带 19 条 line ledger，共 113 个“暂无”；只有 `工厂 · F2 部署闭环` 出现 3 条 `task_failed_upstream`，其余 18 条仍全空。PR #4696 后归属完整率仍未形成可用信号。 |
| Smoke Consistency（探针B） | ⚠️ | 19 条非 smoke/test active journey 中 17 条声明 E2E：跨 `cecelia/main` 与 `zenithjoy-workspace/main` 逐条核验后，12 条存在、4 条缺失、1 条为 TBD，另有 2 条为空。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 的基线仍准确。 |
| Deploy Config Drift（探针C） | ⚠️ | 生产 `1.270.8@c0ad461b` 与 main 一致且 healthy；staging `1.270.7@c33649e3` 因 tick hard-off 为 degraded，落后一个 patch。staging 日志持续报告两个 Claude account 凭据路径不存在并把 token pool 估为 abundant，说明禁调度环境的资源观测仍有噪声。 |

## 4B Structural Health Findings

本次为 daily 增量扫描，基线为上一轮报告 main `c5d62250`：

- 基线后没有新的生产代码提交；运行时仍为 `1.270.8@c0ad461b`，main 额外提交仅是上一轮文档报告。静态模块规模保持 `tick.js` 188L、`executor.js` 4615L、`planner.js` 1247L、`thalamus.js` 1816L、`cortex.js` 1593L。
- review 链至少有三套相互不闭合的运行事实：`/tmp/codex-review-locks`、task 的 claim/executor 字段、`payload.current_run_id`。spawn 只写前两者，callback CAS 依赖第三者，恢复器又用第四套内存/文件探针收尾；局部实现各自合理，组合后确定性重跑。
- 16:01 与 12:00 两个任务均在 exit 0 / callback 后保留 `in_progress`。后者已有 merged PR，仍被重新派发；说明“PR 已交付”“进程已成功退出”“Brain task 已完成”三种终态没有统一提交点。
- tick 的单布尔锁同样缺代际身份。16:57 和 16:58 两个独立 60 秒 timer 先后触发，证明 force-release 允许下一代 scheduler 与上一代共享并覆盖同一控制面状态。
- migration 394 已应用、Schema MAX=394，但 `task_execution_metrics` relation 和 `tasks.notion_page_id` 消费列分别缺失；版本账本、真实 schema 与运行消费者形成三本账。
- 定向测试覆盖了 callback CAS、review lock probe、tick force-release 与 recovery 四条网，却没有端到端组合断言，因此 8 文件 / 111 tests 全绿仍能与生产重复执行同时成立。
- 根目录 `.brain-result.json` / `.task` 仍是被版本控制的旧任务产物；后续 agent 若把根目录结果文件视为当前输出，仍可能误读。

## Severity

- 🔴 High: 6 项（review 活性/重复派发、verdict 与 callback 终态、tick 超时互斥、tasks 时间契约、recovery 字段投影、CURRENT_STATE 假新鲜）
- ⚠️ Medium: 12 项（边界、路由/API、复杂度、registry、migration relation、schema 消费列、生产依赖、`/dev` 可移植性、结构债、line ledger、E2E 声明、部署观测）
- ✅ Clean: 5 项（生产 runtime/main 代码一致、两个 breaker 关闭、facts 校验、版本同步、定向测试执行成功）

## Required Actions

- [ ] **P0** — 处理 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`：统一 review 原生 verdict 与 Brain verdict；`CRITICAL/WARN` 必须显式映射，未知枚举 fail-closed，禁止 exit 0 默认 PASS。
- [ ] **P0** — 处理 Issue `6adfa549-5b69-4883-b087-649be182b8db`：spawn 时原子写 claim、executor kind、lock/run_id 与 heartbeat；callback CAS 必须真实 applied，PR 已合并或进程 exit 后不得保持 `in_progress` 并被恢复器重派。
- [ ] **P0** — 处理 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：为 tick 引入可传播的取消/截止合同，或在旧执行真实终止前保留全局租约；旧 tick 存活时新 tick 不得进入。
- [ ] **P0** — 处理 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一 `tasks` 生命周期字段为明确 UTC/timestamptz 契约，并覆盖 PostgreSQL 非 UTC + Node 不同时区回归。
- [ ] **P1** — 处理 Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 与 `bb78074b-eca4-414e-a7f3-a9425824f7de`：补齐 recovery executor 投影，重建 `SYSTEM_MAP.md` 当前事实，并修正 skills API / followup 合同文档。
- [ ] **P1** — 处理 Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3` 与 `ff663429-5d75-4581-960d-fd676351c2eb`：用可重入 migration 恢复 `task_execution_metrics` 及 `tasks.notion_page_id` 消费合同，让 selfcheck 验证关键 relation/列和真实消费成功。
- [ ] **P1** — 处理 Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457`：恢复 registry 日扫/事件扳机并增加连续账龄告警，验收三表 `scanned_at` 真更新。
- [ ] **P1** — 继续验证 line ledger 归属，并处理 Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 与 `5ff60dff-d910-4101-b3b8-5d3695b819b7`：修复 4 个缺失、1 个 TBD、2 个空 E2E 声明及 BusyBox worktree 锁兼容。
- [ ] **P1** — 处理既有 Issue `a46e7823-a98c-4ada-a421-4034419e6279`：评估并升级 `@opentelemetry/sdk-node`，清除生产依赖中的 2 个 high 漏洞，保留兼容性与 telemetry 回归。
- [ ] **P1** — 处理既有 Issue `25493ed6-ebd4-4dd6-8a23-e5eba953055f`：修复 incident reporter 的 `recurrence_count` 列歧义；当前 launchd-patrol 告警仍持续写入失败。
- [ ] **P2** — 定义 L0/HTTP 路由直调 Cortex 的豁免边界，继续拆分 `executor.js`，移除根目录任务结果产物，并收敛 staging 资源观测噪声。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.270.8`、selfcheck floor `393` 不高于最高 migration `394`，migration 编号与 LLM timeout facts 一致）。
- `bash scripts/check-version-sync.sh`：PASS（Brain 版本四处同步为 `1.270.8`）。
- `node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- Brain 定向 Vitest：tick-loop、recovery-loop、callback processor、review callback、executor contracts 与 review liveness 共 8 文件 / 111 tests 全部通过；现有测试明确保护 tick force-release 与 callback CAS 局部行为，但没有组合闭环用例。
- 生产真实状态：Brain `healthy`、`1.270.8@c0ad461b`，两个 circuit breaker CLOSED；staging `degraded`、`1.270.7@c33649e3`，tick hard-off。16:01 与 12:00 两个任务均为 `in_progress`、`current_run_id=NULL`，17:12 有两个 review lock/进程并存。
- 生产日志：16:58:54 当前任务第一轮 exit 0 后发送 `verdict=PASS`；17:05 recovery 判死回队，17:06 重派。12:00 任务 17:05 exit/callback 后同样未终态，17:10 重派。16:57:39 与 16:58:39 连续两个 tick timer force-release。
- 生产 DB 只读探针：Schema MAX=394；tasks 七个时间字段继续混用 timestamp/timestamptz，DB 时区 America/Chicago；`task_execution_metrics` relation 为 NULL；registry 三表账龄 505.4h。
- 生产日志持续出现 `task_execution_metrics` relation 缺失和 `tasks.notion_page_id` 列缺失；后者新建 Issue `ff663429-5d75-4581-960d-fd676351c2eb`。
- `npm audit --omit=dev`：33 项生产依赖漏洞（31 moderate / 2 high / 0 critical），high 涉及 `@opentelemetry/sdk-node`。
- 台账探针：19 条 line ledger、113 个“暂无”、3 条 F2 upstream failure；19 条 active journey 中 E2E 为 12 存在 / 4 缺失 / 1 TBD / 2 空。
- 12:00 报告 PR #4726 已合并且 CI 成功；其 task 仍被重派，证明 PR/CI 终态未与 Brain task 终态闭合。基线后无生产代码提交。

## ADR Needed? 是

需要明确 review runner 的唯一 claim/lock/run-id/heartbeat/callback 生命周期、review verdict 枚举边界、tick 超时后的取消/互斥租约、任务生命周期时间的唯一类型/时区，以及 L0/HTTP 直调 Cortex 的合法边界。schema 对象缺失、registry 刷新与 recovery 字段投影属于实现/治理缺陷，不需要另立 ADR。

## Escalate to /architect? 是

review verdict/终态合同、tick 取消传播、时间类型迁移、CURRENT_STATE 正本与 Cortex 边界涉及跨模块契约；本巡检只记录事实与行动项，不在报告 PR 中设计或修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 18,
  "health_findings": 7,
  "high_severity": 6,
  "escalate_to_architect": true,
  "summary": "生产服务可用，但 review 的 CRITICAL verdict 被降为 PASS，callback CAS 因 run-id 缺失拒绝终态，恢复器已把当前和已合并 PR 的历史任务同时重派；tick 亦再次出现真实重叠，时间/recovery/CURRENT_STATE 与 schema 双账均未收敛"
}
```
