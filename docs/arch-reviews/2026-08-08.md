---
id: arch-review-20260808
version: 1.3.0
created: 2026-08-08
updated: 2026-08-08
reviewer: Codex (Brain 自动调度)
task_id: 42acce17-b78b-4eaf-b38f-02d8de5961d8
base_sha: c8f1dffd51046cfc382a973dfb04b66ccf979ccb
previous_task_id: e91f09c5-9807-43b7-b773-6ab602ef0db3
---

# Architecture Review — 2026-08-08 daily（20:01 UTC）

## Executive Summary

裁决仍为 **CRITICAL**：6 项高风险、14 项中风险。16:01 报告后的两个生产版本 `1.270.9` / `1.270.10` 集中修复 Kernel 的账号展开、Claude 凭据单链、Generator PR 投影和 CI 观测，但没有触及上一轮 6 个高风险合同。当前 review 任务依旧只把 run id 写入 `/tmp/codex-review-locks`，任务的 `payload.current_run_id` 与 heartbeat 均为空；`triggerCodexReview()` 仍只解析 `PASS|FAIL`，没有调用已经存在的 `updateTaskRunInfo()`，也不检查 callback HTTP / `applied`。本轮暂时只有一个 review 进程与一个 lock，尚未再次重派，但进程成功退出后仍会撞上同一 callback CAS 断链。

运行态继续证明恢复合同的双轨：生产 DB 中当前任务 `executor_kind=codex-review-local`，20:18 UTC 的 recovery 却记录 `executor_kind=null (legacy) — fail-open`，因为 `recovery-loop.js` 的投影仍漏该字段；另一条 `probeTaskLiveness()` 又靠 task type 特判读取 lock。两条网对同一执行体看到不同身份，活性保护当前有效，终态与恢复仍不闭合。

本轮新增三项 P1。第一，`line-dreaming` 每 24 小时在 21:00 UTC 产账，而 arch review 只消费最近 20 小时，形成每天约 17:00–21:00 UTC 的确定性空窗；当前库有 940 条历史 ledger、最近 20 小时为 0，任务 payload 因此完全没有各线摘要。第二，PR #4734 将“失败 check + mergeStateStatus 非 BLOCKED”压成 `ci=pass`，连 GitHub 定义为“当前无法判定”的 `UNKNOWN` 也会放行。第三，PR #4730 在已有精确 `workspace_spec.branch` 时仍允许 task short-id 子串替代，形成两套 PR 分支身份正本。Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803`、`d44556d8-4cf0-4355-afeb-050604911dc6`、`7a276ffd-34b5-4753-99d2-2c5dc4e5915a` 已登记。

生产为 `healthy`、`1.270.10@c8f1dffd`，两个 circuit breaker 均为 `CLOSED`；staging 为 `degraded`、`1.270.9@d937fe78`，按 hard-off 设计停调度。静态 facts、版本同步、DoD 映射均通过，定向 15 文件 / 404 tests 通过。绿灯证明局部实现稳定，却未覆盖 review verdict/run-id/callback 组合、24h→20h 时间窗口、`UNKNOWN + failed check` 或双分支身份合同。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | L0/服务层仍在 `report-48h.js:144`、`monitor-loop.js:483`、`dispatch-helpers.js:206`、`capability-probe.js:166` 直调 `cortex.js`；HTTP 路由 `analytics.js:5/234`、`ops.js:2147` 也直连 Cortex。标准 L1→L2 入口仍为 `thalamus.js:1616`，豁免边界未定义。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:46/65/104-107/116` 仍写 Brain `1.216.0`、139 个 migration、Schema `262`；当前事实为 Brain `1.270.10`、最高 migration / 生产 Schema `394`。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 未收敛。 |
| Review Execution Contract | 🔴 | 当前 lock 的 run id 为 `920eb58d-...`，但 task `payload.current_run_id=NULL`、heartbeat=NULL。spawn 仍未调用现有 `updateTaskRunInfo()`；callback CAS 与任务终态没有共享 run 身份。Issue `6adfa549-5b69-4883-b087-649be182b8db`。 |
| Review Verdict / Callback Integrity | 🔴 | `triggerCodexReview()` 的 JSON 提取仍只接受 `PASS|FAIL`，`CRITICAL/WARN` 会 fallback；callback fetch 不检查 HTTP 状态、响应体或 `applied`。P0 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us` 当前可派发；`arch_review_followup` 仍不在 task type 合同中。AGENTS 声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 合同仍与真实 API 不一致。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4615L/30；`planner.js` 1247L/4；`thalamus.js` 1816L/6；`cortex.js` 1593L/13。`executor.js` 继续承载 review、bridge、prompt、进程与 callback 多职责。 |
| Concurrency Safety | 🔴 | `tick-loop.js:65-94` 仍在 60 秒后清共享布尔锁，但不取消或 join 旧 `doTick()`。旧执行存活时新 tick 仍可进入；Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0` 未收敛。 |
| Temporal Integrity | 🔴 | `tasks.created_at/started_at/completed_at/updated_at/queued_at` 仍为 `timestamp without time zone`，`claimed_at/driver_heartbeat_at` 为 `timestamptz`，DB 时区为 `America/Chicago`。当前任务 API 显示创建/开始为 07:01/07:13 UTC，claim 为真实 20:13 UTC，继续相差 13 小时。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js:50` 仍只投影 `id/title/status/priority/started_at/updated_at/payload/task_type`。20:18 UTC 当前任务在 DB 已有 `executor_kind=codex-review-local`，恢复日志却实测为 `executor_kind=null (legacy) — fail-open`。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`。 |
| Registry Freshness | ⚠️ | `api_registry`、`db_schema_registry`、`test_registry` 最新扫描均停在 2026-07-18，账龄增至 508.4 小时。Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457` 未收敛。 |
| Migration Ledger Integrity | ⚠️ | `schema_version=394`，migration 153 启动时被标记已应用，但 `to_regclass('public.task_execution_metrics')` 仍为 NULL；回调观测 relation 缺失。Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3`。 |
| Schema Consumer Integrity | ⚠️ | 生产 `notion-product-push` 在本轮重启后继续每分钟报 `tasks.notion_page_id` 不存在；information_schema 同样无该列。Issue `ff663429-5d75-4581-960d-fd676351c2eb`。 |
| Dependency Exposure | ⚠️ | `npm audit --omit=dev` 返回 60 项生产依赖漏洞（58 moderate / 2 high），高危仍落在直接依赖 `@opentelemetry/sdk-node` 及传递依赖；上一轮为 33 项。Issue `a46e7823-a98c-4ada-a421-4034419e6279`。 |
| Dev Workflow Portability | ⚠️ | BusyBox 环境仍不支持 `worktree-manage.sh` 所依赖的 GNU `flock -w 5 201` 语义；本轮使用 Brain 已预建的显式 Git worktree。Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7`。 |
| Temp Patch Accumulation | ⚠️ | 根目录仍跟踪任务特定 `.brain-result.json` 与 `.task`；`report-48h.js:119` 的 `mock_cortex` TODO 等结构债未清理。 |
| Ledger Freshness Contract | ⚠️ | `line-dreaming` 每日 UTC 21:00 产出，arch review 仅取最近 20h。20:01 任务正落在固定空窗：940 条历史记录、最近 20h=0、最新为 2026-08-07 21:00:30 UTC，payload 无 ledger 摘要。新 Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803`。 |
| Smoke Consistency | ⚠️ | 17 条非 smoke/test active journey 中，10 条 E2E 路径存在、4 条缺失、1 条 TBD、2 条为空；缺失项仍包括西安机群、客户管理、视频翻拍和智能发布。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 的缺口未收敛。 |
| Deploy Config Drift | ⚠️ | 生产 `1.270.10@c8f1dffd` 与 main 一致且 healthy；staging `1.270.9@d937fe78`、scheduler hard-off、API degraded，落后一个 patch。staging 仍高频报告两个 Claude account 凭据路径不存在。 |
| Kernel CI Truth Compression | ⚠️ | `ground-truth.js:66-88` 对失败 checks 只把 `BLOCKED` 视为阻断，`UNKNOWN/null` 等也可返回 pass。GitHub 官方将 `UNKNOWN` 定义为状态当前无法判定；这里应 fail-closed 为 pending。新 Issue `d44556d8-4cf0-4355-afeb-050604911dc6`。 |
| Kernel PR Identity Drift | ⚠️ | `harness-callback.js:299-306` 在 `expectedBranch` 已签发时仍接受 `headRef.includes(taskShort)`，服务端分支与 SKILL 惯例分支成为双正本。新 Issue `7a276ffd-34b5-4753-99d2-2c5dc4e5915a`。 |

## 4B Structural Health Findings

- 16:01 基线后仅 PR #4730 / #4734 改生产代码，均集中在 Kernel。review、tick、recovery、时间和 schema 高风险实现没有变化。
- 当前 review 首轮运行期间只有一个 Codex 进程与一个 lock，且 20:18 recovery 未重排任务；这证明 task-type lock 特判当前有效，但不是 run-id/callback 终态闭环已修复。
- 同一 recovery 周期对当前任务记录 `executor_kind=null (legacy)`，而 API/DB 明确返回 `codex-review-local`；恢复投影与活性合同的结构分裂已由生产日志再次实证。
- Kernel CI 修复把“CI 是否成功”改成“是否被 branch protection BLOCKED”的推断，解决非 required check 死循环的同时，把 GitHub 不确定状态也压进 pass；局部单测保护了 `UNSTABLE` 案例，却没有完整枚举矩阵。
- Generator 分支修复选择在 callback 验证端放宽身份，而不是让签发端与 SKILL 使用同一派生规则；精确身份与惯例身份继续并存。
- line ledger 的产出与消费各自都通过单测，但 24h 调度和 20h freshness 只有组合后才暴露每日 4h 空窗；本轮新增 4 文件 / 39 tests 仍未覆盖 20:00 UTC 场景。
- migration 账本、information_schema 与运行消费者仍是三本账；启动日志的 `[SKIP] 153` 与真实 relation 缺失同时成立。
- 生产依赖漏洞由 33 增至 60，CURRENT_STATE 文档与 registry 则继续老化；可重复测试和静态门禁没有 freshness / dependency-ratchet 约束。

## Severity

- 🔴 High: 6 项（CURRENT_STATE、review 执行终态、verdict/callback、tick 互斥、tasks 时间、recovery 投影）
- ⚠️ Medium: 14 项（边界、路由、复杂度、registry、migration、schema 消费、依赖、`/dev` 可移植性、结构债、ledger 窗口、E2E 声明、部署观测、Kernel CI 真相、Kernel PR 身份）
- ✅ Clean: 5 项（生产 runtime/main 一致、两个 breaker 关闭、facts 校验、版本同步、定向测试）

## Required Actions

- [ ] **P0** — 处理 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`：统一 review verdict 枚举；未知值 fail-closed，禁止 exit 0 默认 PASS。
- [ ] **P0** — 处理 Issue `6adfa549-5b69-4883-b087-649be182b8db`：spawn 原子写 claim/executor/run-id/heartbeat；callback 必须验证 `applied=true`，进程成功退出后任务必须进入唯一终态。
- [ ] **P0** — 处理 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：tick 超时必须传播取消或保留全局租约，旧执行真实结束前不得开启下一代。
- [ ] **P0** — 处理 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一任务生命周期时间为 UTC/timestamptz，并覆盖 PostgreSQL/Node 不同时区回归。
- [ ] **P1** — 处理 Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 与 `bb78074b-eca4-414e-a7f3-a9425824f7de`：补 recovery 投影，重建 CURRENT_STATE 与真实代码/API 契约。
- [ ] **P1** — 处理 Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803`：统一 ledger 产出/消费 freshness；验收 20:00 UTC review 仍能取到完整最新批次。
- [ ] **P1** — 处理 Issue `d44556d8-4cf0-4355-afeb-050604911dc6` 与 `7a276ffd-34b5-4753-99d2-2c5dc4e5915a`：CI 状态完整枚举并 fail-closed；PR 分支收敛为唯一服务端身份合同。
- [ ] **P1** — 处理 Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3` 与 `ff663429-5d75-4581-960d-fd676351c2eb`：可重入恢复缺失 relation/列，让 selfcheck 验证真实 schema 消费成功。
- [ ] **P1** — 恢复 registry 扫描，修复 4 个缺失、1 个 TBD、2 个空 E2E 声明，并收敛 BusyBox worktree 锁兼容。
- [ ] **P1** — 升级 OpenTelemetry 依赖并修复 incident reporter 的 `recurrence_count` 列歧义；本轮生产日志仍有 4 个 launchd 异常无法落 incident。
- [ ] **P2** — 定义 L0/HTTP 直调 Cortex 豁免边界，拆分 `executor.js`，移除根目录旧任务结果产物，收敛 staging 资源观测噪声。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.270.10`、selfcheck floor `393`、最高 migration `394`）。
- `bash scripts/check-version-sync.sh`：PASS；`node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- Brain 定向 Vitest：review/tick/recovery/callback/Kernel 11 文件 365 tests；ledger/scheduler 4 文件 39 tests；合计 15 文件 / 404 tests 全部通过（`DB_NAME=cecelia_test`）。
- 生产真实状态：Brain `healthy`、`1.270.10@c8f1dffd`，两个 breaker CLOSED；staging 容器 API `degraded`、`1.270.9@d937fe78`、scheduler stopped。
- 当前 task/lock：task `42acce17-...` 为 `in_progress`、`executor_kind=codex-review-local`、`current_run_id=NULL`、heartbeat=NULL；lock 存在且含 run id `920eb58d-...`；仅一个 review 进程。
- 生产 recovery 日志：20:18 UTC 对当前任务输出 `executor_kind=null (legacy) — fail-open`，随后 `orphansRequeued=0`。
- 生产 DB：时区 `America/Chicago`；tasks 生命周期字段继续混用 timestamp/timestamptz；Schema MAX=394；`task_execution_metrics` relation 与 `tasks.notion_page_id` 列均缺失。
- 生产日志：`notion-product-push` 重启后继续每分钟报缺列；incident reporter 对 4 个 launchd 异常继续报 `recurrence_count` ambiguous。
- registry：三表最新扫描 2026-07-18，age=508.4h；line ledger 总数 940、最近 20h=0、最新 2026-08-07 21:00:30 UTC。
- `npm audit --omit=dev`：60 项（58 moderate / 2 high / 0 critical），high 涉及 `@opentelemetry/sdk-node`。
- E2E 声明：17 条 active journey 中 10 存在 / 4 缺失 / 1 TBD / 2 空；Cecelia 本仓与 ZenithJoy main 均逐路径核验。
- GitHub `MergeStateStatus` 官方枚举：[`UNKNOWN` = state cannot currently be determined，`UNSTABLE` = mergeable with non-passing commit status](https://docs.github.com/en/graphql/reference/enums#mergestatestatus)。

## ADR Needed? 是

需要明确 review runner 的唯一 claim/lock/run-id/heartbeat/callback 生命周期、review verdict 枚举、tick 超时取消/租约、任务时间类型，以及 Kernel 的 CI 真相枚举与 PR 分支唯一身份。schema 缺失、registry/ledger freshness 和 recovery 投影属于实现/治理缺陷，不需要单独 ADR。

## Escalate to /architect? 是

review 终态、tick 取消、时间迁移、CURRENT_STATE、Cortex 边界以及 Kernel CI/PR 身份均涉及跨模块合同；本巡检只记录事实、Issue 与行动项，不在报告 PR 中修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 20,
  "health_findings": 8,
  "high_severity": 6,
  "escalate_to_architect": true,
  "summary": "生产服务健康且定向测试全绿，但 review 的 verdict/run-id/callback、tick 互斥、时间/recovery/CURRENT_STATE 高风险合同仍未闭合；本轮新增 ledger 固定空窗与 Kernel CI/PR 身份两项 fail-open 漂移"
}
```
