---
id: arch-review-20260810
version: 1.2.0
created: 2026-08-10
updated: 2026-08-10
reviewer: Codex (Brain 自动调度)
task_id: fa84335c-ee62-4043-9e05-3bfd91a46823
base_sha: 5642ee547874a2a8c5d91689363558136b58f609
previous_task_id: 73834f1d-ad73-41f0-a3a0-421ad4cba40c
---

# Architecture Review — 2026-08-10 daily（04:01 UTC）

## Executive Summary

裁决仍为 **CRITICAL**：8 项高风险、15 项中风险。上一轮后合入 7 个 PR，其中 PR #4754 只补齐状态迁移时的 `started_at` / `completed_at` 写入，没有迁移 tasks 生命周期字段类型；生产 DB 仍在 `America/Chicago` 下混用 `timestamp without time zone` 与 `timestamptz`，当前任务的 `started_at` 与真实 `claimed_at` 仍相差约 13 小时。PR #4751 把 projection 拆成独立 loop，却没有把环境级 hard-off 扩展到 scheduler-jobs 与 PromotionJob；staging 明确打印 hard-off 后仍启动 33 个 jobs 和 PromotionJob。

review runner 的终态断链继续在当前任务上真实复现：lock 保存 run id `8a288f3a-...`，task 写入 `executor_kind=codex-review-local`，但 `payload.current_run_id`、`run_status` 与 `driver_heartbeat_at` 为空。`triggerCodexReview()` 仍不调用已有的 `updateTaskRunInfo()`，只解析 `PASS|FAIL`，callback fetch 也不校验 HTTP 状态或 CAS `applied`；`recovery-loop.js` 的任务投影仍漏 `executor_kind`。

本轮新增一项部署闭环红色发现：main SHA `2187e492` 的 Gate3 在 10 分钟内始终看到生产 `19d81a3` / `1.271.2` 而失败，Auto Staging Deploy 又因等待同一生产 SHA 超时失败。巡检开始后生产通过另一条路径追到 `2187e492` / `1.271.3`，但 staging 仍停在 `19d81a3` / `1.271.2`。这证明“当前 runtime 对齐”不能反推 Gate3 闭环成功，既有 P0 Issue `22591b2c-cab4-4e77-b7da-12889247abae` 仍有效。

巡检报告提交后，PR #4756 于 04:41 UTC 合入，后续 Gate3 run `31356271937` 成功，生产追到 `d0d5f464`；05:09 UTC 的 staging `:5222` 则不可达，且该 SHA 没有可见的 Auto Staging 成功 run。该后续恢复证明 production 单点已收敛，但没有推翻 04:01 时点的失败链，也没有证明 production/staging 由同一闭环共同到达期望 SHA，因此本项仍保留为高风险。

4B 新发现是实时语音状态机存在三路复制：两份 `useRealtimeVoice.ts` 为逐字相同的 494 行文件，第三份为 458 行近似实现。已登记 P2 Issue `1daab7e4-088c-44b7-862b-29dd7e1394d5`。生产当前 `healthy`、breaker 全 CLOSED、schema 396 与 `initiative_runs.gear` 均已落库；三项 DevGate 和 20 个定向测试文件 / 339 tests 全部通过，但这些局部绿灯不覆盖上述跨进程、跨时区与跨部署入口合同。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | `report-48h.js:144`、`monitor-loop.js:483`、`dispatch-helpers.js:206`、`capability-probe.js:166`、`routes/analytics.js:5/234` 与 `routes/ops.js:2147` 继续越过 Thalamus 直调 Cortex；标准 L1→L2 入口与豁免边界仍未统一。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:46/65/66/104-107` 仍写 Brain `1.216.0`、139 migrations、schema 139/262；真实为 Brain `1.271.3`、最高 migration/schema 396。Tailscale enforcer、Workbench projection 与 gear 分流也未进入正本。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`。 |
| Review Execution Contract | 🔴 | 当前 lock 有 run id `8a288f3a-...`，task 的 `current_run_id=NULL`、heartbeat=NULL；`executor.js:2433-2600` 未调用 `updateTaskRunInfo()`。同一任务在 Brain 重启前后被重复派发。Issue `6adfa549-5b69-4883-b087-649be182b8db`。 |
| Review Verdict / Callback Integrity | 🔴 | `executor.js:2573` 仍只接受 `PASS|FAIL`，CRITICAL/NEEDS_ATTENTION 会 fallback；callback fetch 不检查 `response.ok`、响应体或 `applied`。P0 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us` 可派发；`arch_review_followup` 仍不在 task type 合同中。AGENTS 声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 继续返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4615L/30；`planner.js` 1247L/4；`thalamus.js` 1816L/6；`cortex.js` 1593L/13。`executor.js` 继续承载 review、bridge、prompt、进程与 callback 多职责。 |
| Concurrency Safety | 🔴 | `tick-loop.js:65-97` 超时后只清共享布尔锁，不取消或 join 旧 `doTick()`；最近 2 小时生产出现 5 次 `Tick already running`。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`。 |
| Temporal Integrity | 🔴 | PR #4754 补了写入但未改类型：created/started/completed/updated/queued 仍为 `timestamp without time zone`，claimed/heartbeat 为 `timestamptz`，DB 时区为 `America/Chicago`。当前任务 `started_at=2026-08-09T15:22Z`，真实 `claimed_at=2026-08-10T04:22Z`。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js:50` 的 SELECT 仍漏 `executor_kind`；staging 最近 2 小时出现 22 次 `executor_kind=null (legacy)` fail-open。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`。 |
| Registry Freshness | ⚠️ | `api_registry`、`db_schema_registry`、`test_registry` 最新扫描仍停在 2026-07-18 15:50 UTC，账龄 540.6 小时。Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457`。 |
| Migration Ledger Integrity | ⚠️ | schema 396 与 gear 列已落库，但 `to_regclass('public.task_execution_metrics')` 仍为 NULL。Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3`。 |
| Schema Consumer Integrity | ⚠️ | `tasks.notion_page_id` 仍不存在；最近 2 小时生产日志仍有 2 条缺列错误，`recurrence_count` ambiguous 也有 2 条。Issue `ff663429-5d75-4581-960d-fd676351c2eb`。 |
| Dependency Exposure | ⚠️ | `npm audit --omit=dev` 仍为 60 项生产依赖漏洞（58 moderate / 2 high / 0 critical）。Issue `a46e7823-a98c-4ada-a421-4034419e6279`。 |
| Dev Workflow Portability | ⚠️ | 当前 Linux arm64 执行环境复用 Darwin `node_modules` 时缺 Rollup/Esbuild 原生二进制；需在任务 `.runtime` 注入同版本平台包后测试才真实启动。BusyBox `flock -w` 可移植性 Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7` 仍未收敛。 |
| Temp Patch Accumulation | ⚠️ | 根目录仍跟踪任务特定 `.brain-result.json` 与 `.task`；`report-48h.js:119`、`executor-contracts.js:305`、`thalamus.js:847` 的长期 TODO 仍在。 |
| Ledger Freshness（探针A） | ⚠️ | line ledger 总计 978 条、最近 20h 为 19 条、最新 21:00:39 UTC；本任务注入的 19 条摘要只有 F1 有事实，其余 18 条均为空。24h 产出与 20h 消费固定空窗未收敛。Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803`。 |
| Smoke Consistency（探针B） | ⚠️ | 19 条 active journey 中只有 8 条 E2E 路径存在，8 条文件缺失，1 条为 TBD，2 条为空。上一轮记录为 12/4/1/2，但过去 7 个 PR 没有删除这些路径，说明声明真实性之外还存在巡检口径漂移。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`。 |
| Deploy Config Drift（探针C） | 🔴 | 生产 runtime/main 当前对齐；staging runtime `DB_HOST=host.docker.internal`，两仓 `.env.staging` 都写 `localhost`，compose 默认值也为 `localhost`，调用者环境继续决定重部署结果。Issue `b6a0dd31-432b-4048-8fde-66b9f5a4e3b5`。 |
| Deployment Closure Integrity | 🔴 | main run `31354126417` 的 Gate3 因 1.271.3→1.271.2 版本不收敛失败，run `31354126422` 的 Auto Staging Deploy 因生产 SHA 等待 15 分钟超时失败；当前 production 后续追上，staging 仍落后一版。P0 Issue `22591b2c-cab4-4e77-b7da-12889247abae` 与 P2 `32470fae-ca72-46f5-89c0-f3ef2313ea0a`。 |
| Staging Scheduler Isolation | ⚠️ | `CECELIA_TICK_HARD_OFF=1` 只停主 tick/watchdog；启动日志随后仍显示 `scheduler-jobs started (33 jobs)` 与 PromotionJob，staging DB 仍留有 queued `ci_patrol`。Issue `9dda9de0-9955-4d79-a4ab-afa46bb2621d`。 |
| Kernel CI Truth Compression | ⚠️ | `ground-truth.js:77-83` 对失败 checks 仍只把 `BLOCKED` 视为阻断，未知/null merge state 可返回 pass；72 个测试保护的是现状而非 fail-closed 合同。Issue `d44556d8-4cf0-4355-afeb-050604911dc6`。 |
| Kernel PR Identity Drift | ⚠️ | `routes/harness-callback.js:306-308` 在 `expectedBranch` 已签发时仍接受 `headRef.includes(taskShort)`，精确身份与惯例身份继续双轨。Issue `7a276ffd-34b5-4753-99d2-2c5dc4e5915a`。 |
| Realtime Voice Duplication | ⚠️ | `frontend/src/features/core/shared/hooks/useRealtimeVoice.ts` 与 `frontend/src/features/shared/hooks/useRealtimeVoice.ts` 为相同 SHA-256、494 行逐字副本，execution 目录另有 458 行近似实现。新 Issue `1daab7e4-088c-44b7-862b-29dd7e1394d5`。 |

## 4B Structural Health Findings

- 非测试生产 JS 从 587 增至 594；新增 7 个 production 文件均围绕 projection / task queue / error report，并有对应测试，没有发现无消费者的新生产孤岛。
- `executor.js`、`thalamus.js`、`cortex.js`、`planner.js` 仍全部超过 500 行；其中 `executor.js` 4615 行/30 imports，是持续扩张风险最高的 God Module。
- review 执行仍存在 lock、task claim/executor、payload run-id 三套身份；当前任务只写前两套，callback 依赖第三套，recovery 又因投影遗漏把新任务看成 legacy。
- PR #4754 的生命周期测试证明“状态变化会写时间”，但 DB 类型、连接时区与 API 序列化未进入同一合同，因此局部修复没有消除 13 小时漂移。
- staging 的 hard-off 只覆盖 `startTickLoop()`；server 级 scheduler-jobs、projection jobs、PromotionJob 与其他独立 interval 没有统一生命周期注册表。
- Gate3 和 Auto Staging 已能诚实报红，但部署副作用由 CI 之外路径最终完成；“报红后谁负责收敛、staging 何时追平”仍无唯一状态机。
- migration 396 与 gear 列本轮一致，证明最新功能 schema 已部署；migration 153 relation、notion 列与 registry 扫描仍是历史三本账。
- 19 条 Journey 本轮只有 8 个声明文件真实存在；与上一轮 12 个的差异无法由代码变更解释，说明 smoke registry 和巡检探针都缺少统一、可复跑的校验器。
- 实时语音 hook 的三路复制把 WebSocket、录音、队列与播放状态机分散在 1446 行代码中，任一修复都可能只落一条入口。

## Severity

- 🔴 High: 8 项（CURRENT_STATE、review 执行终态、verdict/callback、tick 互斥、tasks 时间、recovery 投影、staging DB_HOST、Gate3/staging 部署闭环）
- ⚠️ Medium: 15 项（边界、路由、复杂度、registry、migration、schema 消费、依赖、`/dev` 可移植性、结构债、ledger、E2E 声明、staging 调度隔离、Kernel CI 真相、Kernel PR 身份、语音 hook 复制）
- ✅ Clean: 8 项（生产 runtime/main、Brain health、breaker、三项 DevGate、schema 396/gear、20 文件 339 测试、7 个新增生产文件均有测试、review 路由仍可派发）

## Required Actions

- [ ] **P0** — 修复 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7` 与 `6adfa549-5b69-4883-b087-649be182b8db`：统一 verdict 枚举；spawn 原子写 claim/executor/run-id/heartbeat；callback 验证 `applied=true` 并形成唯一终态。
- [ ] **P0** — 修复 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：tick 超时传播取消或保留全局租约，旧执行真实结束前不得开启下一代。
- [ ] **P0** — 完成 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：把生命周期字段迁为 UTC/timestamptz，并覆盖 PostgreSQL/Node 不同时区回归；不能只补写值。
- [ ] **P0** — 收敛 Issue `22591b2c-cab4-4e77-b7da-12889247abae`：Gate3 报红后必须有同一状态机接管恢复，生产与 staging 都以期望 SHA 达成终态。
- [ ] **P1** — 修复 Issue `b6a0dd31-432b-4048-8fde-66b9f5a4e3b5` 与 `9dda9de0-9955-4d79-a4ab-afa46bb2621d`：统一 staging 配置源；环境 hard-off 覆盖全部 loop，并以 DB 零副作用证明隔离。
- [ ] **P1** — 修复 recovery/CURRENT_STATE、registry/schema/ledger、Journey E2E、Kernel CI/PR 身份等既有 Issue，保持 fail-closed 与单一正本。
- [ ] **P2** — 修复 Issue `1daab7e4-088c-44b7-862b-29dd7e1394d5`：提取唯一 `useRealtimeVoice` 正本，三条消费入口统一引用并保留跨入口回归。

## Verification Evidence

- `node scripts/facts-check.mjs`、`bash scripts/check-version-sync.sh`、`check-dod-mapping.cjs`：全部 PASS；Brain `1.271.3`，selfcheck floor 395，最高 migration 396。
- 定向 Vitest：20 文件 / 339 tests 全部通过；为避免复用 Darwin 依赖产生零执行，使用任务 `.runtime` 中同版本 Linux arm64 Rollup/Esbuild 原生包启动。
- 生产：`healthy`、`1.271.3@2187e492`，scheduler running，dispatch / cecelia-run breaker 均 CLOSED；schema max=396，`initiative_runs.gear` 存在。
- staging：`degraded`、`1.271.2@19d81a3`，主 scheduler stopped；hard-off 后仍启动 33 jobs 与 PromotionJob。
- GitHub Actions：main Gate3 run `31354126417` failure；Auto Staging run `31354126422` failure；普通 CI 与 smoke glob 为 success。
- 巡检后复核：PR #4756 对应 Gate3 run `31356271937` success，production 为 `1.271.3@d0d5f464`；staging `:5222` 不可达，未取得同 SHA 的 Auto Staging 成功证据。
- 当前 task/lock：task `73834f1d-...` 为 `in_progress`、`executor_kind=codex-review-local`、`current_run_id=NULL`、heartbeat=NULL；lock 含 run id `8a288f3a-...`。
- 生产 DB：时区 `America/Chicago`；tasks 生命周期字段混用 timestamp/timestamptz；`task_execution_metrics` relation 与 `tasks.notion_page_id` 均缺失。
- registry 三表 age=540.6h；line ledger 总数 978、最近 20h=19、最新 2026-08-09 21:00:39 UTC。
- Journey E2E：8 exists / 8 missing / 1 TBD / 2 empty；`npm audit --omit=dev`：60 项（58 moderate / 2 high）。
- `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 继续返回 404。

## ADR Needed? 是

需要明确 review runner 的唯一 claim/lock/run-id/heartbeat/callback 生命周期、review verdict 枚举、tick 超时取消/租约、任务时间类型、环境级 hard-off，以及 Gate3 报红后的部署恢复所有权。schema/registry/Journey 声明与语音 hook 复制属于实现/治理缺陷，不需要单独 ADR。

## Escalate to /architect? 是

review 终态、tick 取消、时间迁移、CURRENT_STATE、Cortex 边界、环境级调度隔离与部署恢复状态机均涉及跨模块合同；本巡检只记录事实、Issue 与行动项，不在报告 PR 中修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 23,
  "health_findings": 9,
  "high_severity": 8,
  "issues_created": 1,
  "escalate_to_architect": true,
  "summary": "生产当前已追上 main 且 schema 396/gear 落库，但 review verdict/run-id/callback、tick 互斥、时间/recovery/CURRENT_STATE 高风险合同未闭合；本轮新增 Gate3 与 Auto Staging 同 SHA 收敛失败，以及 useRealtimeVoice 三路复制"
}
```

---

# Architecture Review — 2026-08-10 daily（16:00 UTC）

## Executive Summary

裁决仍为 **CRITICAL**：10 项高风险、13 项中风险。最严重的新发现不是单个模块报错，而是 preview 环境没有隔离 `scheduler-jobs`：preview-4762/4764/4765/4771/4775 在同一窗口各创建一条同名 `arch_review`，共享锁池允许 task `fa84335c-...` 与 `0267bdc0-...` 同时派发两组 Codex；生产 Brain 另有 task `e77029ae-...` 保持 queued。两条 preview task 虽已运行，却都没有 `current_run_id`/heartbeat；child 的 `BRAIN_URL` 又固定回到生产 `:5221`，退出 callback 将找不到 preview task，而 executor 不检查 HTTP/CAS 结果。

数据库治理出现新的红色三本账：`origin/main@5642ee54` 最高 migration 为 396，生产 `schema_version` 却在 12:21 UTC 连续写入 397–399，且 `journeys.parent_journey_id` 已真实存在。精确来源是仍未合并、仅有 CodeQL checks 的 PR #4772（head `c9ad8058`），其包含 migrations 397–400；生产只执行前三个、400 未落，形成半套迁移。与此同时，已登记的 migration 153 仍没有 `task_execution_metrics` relation。生产 schema 因而既领先 main 正本，又缺中间历史对象。

既有高风险合同没有被 04:01 后的 6 个 PR 修复：当前两条 preview task 的 `started_at` 仍比 `claimed_at` 早 13 小时，生产 4 小时内出现 47 次 `Tick already running`，staging 在 hard-off 日志之后仍三次启动 33 个 scheduler jobs 与 PromotionJob，staging 仍停在 `1.271.2@19d81a3`。CURRENT_STATE 继续写 Brain `1.216.0`、139 migrations、schema 139/262。

本轮也确认两项局部恢复：三张 registry 在 12:45 UTC 完成刷新；PR #4759 的 preview reaper 在可执行任务临时目录中 15/15 通过，OrbStack 红线与真实 PID 终止均生效。但标准 Brain 配置只收集显式指定的 9 个测试文件中的 8 个（76 tests）；被排除的 `daily-review-scheduler.test.js` 用任务局部配置强制执行后为 79/80，唯一失败仍断言废弃的 `location='xian'`。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | 8 个非 Thalamus 生产入口继续直接 import Cortex；L1→L2 标准入口与豁免边界未统一。 |
| Undocumented Changes | 🔴 | `SYSTEM_MAP.md` 仍写 Brain `1.216.0`、139 migrations、schema 139/262；真实包版本为 `1.271.3`，仓库 migration 396，生产 ledger 399。Issue `bb78074b-...`。 |
| Preview Scheduler Isolation | 🔴 | 5 个 preview Brain 各建一条 16:00 巡检，2 条并发派发。preview 只设 `CECELIA_TICK_ENABLED=false`，`server.js` 仍无条件启动 scheduler/projection/Promotion loops。新 P0 Issue `a9a6e3f6-06c7-42a7-840f-09e6ef7f448a`。 |
| Review Execution Contract | 🔴 | task `fa84335c-...` 与 `0267bdc0-...` 均为 in_progress/executor_kind 已写，但 payload run-id 与 heartbeat 为空；锁文件分别持有真实 run-id。Issue `6adfa549-...`。 |
| Review Callback Integrity | 🔴 | 两个 child 的 `BRAIN_URL=http://localhost:5221` 指向生产，而任务存在于 preview DB；`triggerCodexReview()` 仍只解析 PASS/FAIL，fetch 不检查 `response.ok` 或 `applied`。Issue `72546baf-...`。 |
| Routing Integrity | ⚠️ | `arch_review → us` 可派发；声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 仍为 404。 |
| Core Module Complexity | ⚠️ | `executor.js` 4615L/30 imports；`thalamus.js` 1816L；`cortex.js` 1593L；`planner.js` 1247L，均未收敛。 |
| Concurrency Safety | 🔴 | 生产最近 4h 有 47 次 `Tick already running`；60s safety timer 仍只释放布尔锁，不取消/join 旧执行。Issue `9aa89588-...`。 |
| Temporal Integrity | 🔴 | tasks 的 created/queued/started/completed/updated 仍为 timestamp，claimed/heartbeat 为 timestamptz，DB 时区 America/Chicago；两条当前 task 均实证 13h 偏差。Issue `41f68c38-...`。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js` 的 in-progress 投影仍漏 `executor_kind`，无法把 codex-review-local 交给精确 liveness 合同。Issue `4411cb51-...`。 |
| Registry Freshness | ✅ | api/db_schema/test registry 最新扫描均为 2026-08-10 12:45 UTC，上一轮 540h 账龄已消除。 |
| Migration Ledger Integrity | 🔴 | 生产 max=399、main max=396；精确来源为 open PR #4772 的 migrations 397–400，但生产只执行 397–399，400 未落；migration 153 relation 仍缺失。新 P0 Issue `8a782f87-16d3-4ad6-a901-843efd57d50f`。 |
| Schema Consumer Integrity | ⚠️ | `tasks.notion_page_id` 仍不存在；生产近 4h 仍有 3 条 `task_execution_metrics` 写入错误。Issue `ff663429-...` / `ce752b77-...`。 |
| Dependency Exposure | ⚠️ | `npm audit --omit=dev` 实跑仍为 60 项（58 moderate / 2 high / 0 critical），涉及直接依赖 `@opentelemetry/sdk-node`。Issue `a46e7823-a98c-4ada-a421-4034419e6279`。 |
| Dev Workflow Portability | ⚠️ | BusyBox `flock` 不支持 `/dev` worktree 脚本使用的 `-w`；共享依赖仍缺 Linux arm64 Rollup/Esbuild，零收集风险未消除。Issue `5ff60dff-...`。 |
| Temp Patch Accumulation | ⚠️ | 根目录继续跟踪任务特定 `.brain-result.json` 与 `.task`，三处长期 TODO 未收口。 |
| Ledger Freshness | ⚠️ | line ledger 总数 978、最近 20h 为 19、最新仍是 2026-08-09 21:00:39 UTC；本任务 19 条摘要仅 F1 有事实，24h/20h 固定空窗未修。Issue `a1ed9884-...`。 |
| Deploy Config Drift | 🔴 | production 为 `1.271.3@d0d5f464`，staging 为 `1.271.2@19d81a3`；staging 的 DB_HOST 仍由调用入口环境决定。Issue `b6a0dd31-...`。 |
| Deployment Closure | 🔴 | production 与 staging 仍未由同一闭环到达期望版本；main 后续 ops/dashboard 变更没有给出 staging 追平证据。Issue `22591b2c-...`。 |
| Kernel CI Truth Compression | ⚠️ | failed checks + 非 BLOCKED/UNKNOWN merge state 仍可返回 pass，UNKNOWN 未 fail-closed。Issue `d44556d8-...`。 |
| Kernel PR Identity Drift | ⚠️ | 已有 expectedBranch 时仍接受 `headRef.includes(taskShort)`，签发身份继续被惯例身份降级。Issue `7a276ffd-...`。 |
| Realtime Voice Duplication | ⚠️ | 实际为 5 份物理副本：3 份 494 行文件同 SHA-256，2 份 458 行文件同 SHA-256，横跨 frontend 与 apps/api。Issue `1daab7e4-...` 已补充范围。 |
| Janitor /dev Lock Contract | ⚠️ | `/dev` 默认锁写在 `~/worktrees/<project>`，Janitor 只扫仓库根与 `.claude/worktrees`；现有测试只 grep 字符串。新 P1 Issue `c00b0090-ab86-49c0-836a-edafef37c4dd`。 |
| Test Collection Integrity | ⚠️ | 标准配置把 `daily-review-scheduler.test.js` 排除，显式 9 文件静默收集 8；强制执行该文件为 79/80，死断言仍要求 xian。新 P1 Issue `0acbf8f3-38dc-42ff-9d0b-f2e627d45c02`。 |
| Preview Reaper Process Identity | ✅ | 15/15 shell tests 真实通过：正常 preview PID 被终止，身份不符/OrbStack 进程不 kill，PID 缺失不回落到端口持有者。 |

## 4B Structural Health Findings

- preview、staging 与 production 都启动同一 server 入口，但环境隔离只控制旧 tick；33 个 scheduler jobs、projection、Promotion、nightly 与 recovery 没有统一生命周期注册表。
- review 执行身份仍分裂为 DB claim/executor、文件 lock/run-id、payload run-id/heartbeat 三套；preview 又引入第四个维度——callback DB 地址，当前实际指向错误环境。
- migration 治理同时存在“ledger 已登记但 relation 缺失”和“ledger/物理对象领先 main”两种相反漂移，现有 selfcheck 的 `floor <= max` 两种都发现不了。
- F1 最近 24h 仍出现多条 `callback_runner_failure`，16:11 UTC 又有一条 run 失败；另有活跃 evaluate/generate run，说明账本中的失败不是陈旧历史。
- registry 刷新恢复与 dependency 数下降是局部改善；它们不覆盖 scheduler 环境副作用、任务身份、时间类型和 schema 正本合同。
- PR #4759 的进程身份修复已证明有效，历史 `process_disappeared` 的 OrbStack 误杀路径已封堵；当前 preview 重复调度与错误 callback 是新的独立终态断链。
- Janitor 迁回 Cecelia 后获得 CI，但测试只验证函数/字符串存在，没有把 Engine 默认 worktree 路径作为跨模块合同输入。
- 完整 scheduler 测试被 exclude 后继续保留过时 xian 断言，拆分的小测试让 CI 保护局部行为，却掩盖完整合同已腐烂。

## Severity

- 🔴 High: 10 项（CURRENT_STATE、preview 调度隔离、review 身份、verdict/callback、tick 重叠、tasks 时间、recovery 投影、schema 397–399、staging 配置、部署闭环）
- ⚠️ Medium: 13 项（边界、路由、复杂度、schema 消费、依赖、`/dev` 可移植性、结构债、ledger、Kernel CI、Kernel PR 身份、语音复制、Janitor 锁路径、测试收集）
- ✅ Clean: 7 项（Brain health、breaker、三项 DevGate、registry 刷新、8 文件/76 定向测试、preview reaper 15/15、main 最新 CI）

## Required Actions

- [ ] **P0** — 修复 Issue `a9a6e3f6-...`：preview/staging 默认禁用所有有副作用 loop；显式白名单纯观测 job；验收 4h 窗口零 task/notification/deploy 写入、零外部 executor。
- [ ] **P0** — 修复 review 统一身份：claim、executor、lock、run-id、heartbeat、callback URL/CAS 必须同一环境同一原子合同；未知 verdict 与 callback `applied!=true` fail-closed。
- [ ] **P0** — 处置 Issue `8a782f87-...`：追溯 397–399 执行来源，用可重入 forward migration 纳入 main 正本；禁止改写已应用 ledger，并增加“生产领先 main”门禁。
- [ ] **P0** — 完成 tick 取消/租约、tasks timestamptz、recovery 投影与 production/staging 同 SHA 部署闭环。
- [ ] **P1** — 修复 Janitor/Engine worktree path 合同与完整 scheduler 测试；CI 必须断言显式测试文件全部被收集。
- [ ] **P1/P2** — 继续收敛 CURRENT_STATE、Cortex 边界、schema 消费、ledger 窗口、Kernel fail-open、PR 身份和语音状态机复制。

## Verification Evidence

- 三项 DevGate：facts-check PASS、version-sync PASS、DoD mapping PASS（后者通过显式 `NODE_PATH` 真实执行）。
- Brain 定向测试：标准配置真实收集 8 files / 76 tests，全部通过；显式请求的第 9 个文件被 exclude。
- scheduler 死测试强制执行：1 file / 80 tests，79 passed / 1 failed，失败为 `location='xian'` 过时断言。
- preview reaper：任务局部可执行 TMPDIR 下 15/15 PASS；默认 `/tmp` 在当前 runner 为 noexec，首跑夹具不可执行，不计生产失败。
- Janitor：CI 模式静态测试 30/30 PASS、kill 豁免 4/4 PASS；但没有默认 `~/worktrees` 锁路径行为测试。
- production：healthy、`1.271.3@d0d5f464`、breaker all closed；最近 4h 47 次 tick overlap、3 次 metrics relation 错误。
- staging：degraded、`1.271.2@19d81a3`；hard-off 后 3 次启动 scheduler-jobs 与 PromotionJob。
- preview：5 个日志各有 16:00 `Created arch_review task`；task `fa84335c`/`0267bdc0` 两组 Codex 同时存活，child `BRAIN_URL` 均为生产 `:5221`。
- production DB：America/Chicago；tasks timestamp/timestamptz 混用；schema max=399、main max=396；PR #4772 包含 397–400 而生产只落 397–399；`task_execution_metrics=NULL`；registry 三表 12:45 UTC 已刷新。
- line ledger：978 total / 19 recent 20h / latest 2026-08-09 21:00:39 UTC；dependency audit 58 moderate / 2 high。
- `SYSTEM_MAP.md` 仍为 version 1.3.1、updated 2026-08-06，正文 Brain 1.216.0 / migration 139 / schema 139/262。

## ADR Needed? 是

需要定义跨环境统一 loop 生命周期与副作用白名单、review 执行身份及 callback DB 路由、migration 正本/运行库双向一致性、tick 取消/租约和部署恢复所有权。Janitor 路径与测试收集属于实现/治理缺陷，不单独立 ADR。

## Escalate to /architect? 是

preview/staging 环境隔离、review 终态、schema 正本、时间与 tick 生命周期均是跨模块合同；本巡检只记录事实、Issue 与行动项，不在报告 PR 中直接设计或修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 24,
  "health_findings": 8,
  "high_severity": 10,
  "medium_severity": 13,
  "issues_created": 4,
  "escalate_to_architect": true,
  "summary": "五个 preview Brain 同窗创建巡检并并发派发，callback 指向错误生产 DB；open PR #4772 的 migration 397-399 半套提前落生产；review/tick/time/recovery/deploy 高风险合同仍未闭合，完整 scheduler 测试被排除且已腐烂"
}
```
