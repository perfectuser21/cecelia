---
id: arch-review-20260809
version: 1.1.0
created: 2026-08-09
updated: 2026-08-09
reviewer: Codex (Brain 自动调度)
task_id: 34c0eb19-5c43-4e56-ab2c-2ec3e5f28a8e
base_sha: 596d991bc2d663b6319614513bc27f0a5c5f0612
previous_task_id: b0ae39d4-679b-4315-83c3-44ddcf69cc3d
---

# Architecture Review — 2026-08-09 daily（04:03 UTC）

## Executive Summary

裁决仍为 **CRITICAL**：7 项高风险、14 项中风险。上一轮后的生产改动只有 PR #4741 与 #4742。#4742 已把 Kernel 人工评审路由挂入生产，未授权请求真实返回 401；18 个定向测试文件、272 个测试全部通过。#4741 根据 decision `f540b758` 增加西安执行机美国 Tailscale 出口持久化与 OrbStack 自愈。两组改动均未触及上一轮的 review 终态、verdict/callback、tick 代际互斥、任务时间与 recovery 投影六项高风险合同。

本轮 review 再次复现自身终态断链：锁文件保存 run id `ceaf4e53-...`，任务行已有 `executor_kind=codex-review-local`，但 `payload.current_run_id`、`run_status` 与 `driver_heartbeat_at` 均为空。`recovery-loop.js:50` 的查询不投影 `executor_kind`，生产 recovery 因此对同一任务连续输出 `executor_kind=null (legacy) — fail-open`。`triggerCodexReview()` 仍只解析 `PASS|FAIL`，不调用已有的 `updateTaskRunInfo()`；callback fetch 也不校验 HTTP 状态或响应中的 CAS `applied`。

本轮新增一项部署配置红色发现：运行中的 staging 使用 `DB_HOST=host.docker.internal`，但主仓与 CD 专用仓的 `.env.staging` 都写 `localhost`，两份 `docker-compose.staging.yml:54` 也以 `localhost` 为默认值。`staging-deploy.sh` 启动 compose 时不固定 `DB_HOST`，因此从生产 Brain 容器执行会继承 `host.docker.internal`，从宿主机执行则会得到 `localhost`。Issue `b6a0dd31-432b-4048-8fde-66b9f5a4e3b5` 已登记。

生产真实状态为 `healthy`、`1.270.12@596d991b`，scheduler running，两个 breaker 均 CLOSED。staging 为 `degraded`、`1.270.11@1dbfce40`，主 tick stopped；但最近 70 分钟 `PromotionJob` 仍运行 7 次，staging DB 最近 24 小时仍有自动创建的 queued `ci_patrol`。静态门禁与定向测试证明局部改动稳定，但不覆盖上述跨进程、跨时间类型与跨部署入口合同。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | `report-48h.js:144`、`monitor-loop.js:483`、`dispatch-helpers.js:206`、`capability-probe.js:166` 继续越过 Thalamus 直调 Cortex；`routes/analytics.js:5/234` 与 `routes/ops.js:2147` 也直连 Cortex。标准 L1→L2 入口与豁免边界仍未统一。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:46/65/66/104-107` 仍写 Brain `1.216.0`、139 migrations、schema 139/262；真实为 Brain `1.270.12`、最高 migration 394。PR #4741 新增系统级 Tailscale enforcer，也未进入 CURRENT_STATE。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 未收敛。 |
| Review Execution Contract | 🔴 | 当前 lock 有 run id `ceaf4e53-...`，task `current_run_id=NULL`、heartbeat=NULL；`executor.js:2433-2600` 的 review spawn 路径未调用 `updateTaskRunInfo()`。Issue `6adfa549-5b69-4883-b087-649be182b8db`。 |
| Review Verdict / Callback Integrity | 🔴 | `executor.js:2573` 仍只接受 `PASS|FAIL`；CRITICAL/NEEDS_ATTENTION 会 fallback。`executor.js:2507/2583` 的 callback fetch 不检查 `response.ok`、响应体或 `applied`。P0 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us` 可派发；`arch_review_followup` 仍不在 task type 合同中。AGENTS 声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 真实返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4615L/30；`planner.js` 1247L/4；`thalamus.js` 1816L/6；`cortex.js` 1593L/13。`executor.js` 继续承载 review、bridge、prompt、进程与 callback 多职责。 |
| Concurrency Safety | 🔴 | `tick-loop.js:65-97` 超时后只清共享布尔锁，不取消或 join 旧 `doTick()`；现有测试仍显式保护 force-release 行为。最近 70 分钟生产出现 2 次 `Tick already running`。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`。 |
| Temporal Integrity | 🔴 | public.tasks 的 created/started/completed/updated/queued 仍为 `timestamp without time zone`，claimed/heartbeat 为 `timestamptz`，DB 时区为 `America/Chicago`。当前任务 API `started_at=2026-08-08T15:05Z`，而真实 `claimed_at=2026-08-09T04:05Z`，仍有 13 小时表面偏差。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js:50` 的 SELECT 仍漏 `executor_kind`；当前 task DB 已是 `codex-review-local`，生产 recovery 却连续 4 次看成 `null (legacy)` 并 fail-open。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`。 |
| Registry Freshness | ⚠️ | `api_registry`、`db_schema_registry`、`test_registry` 最新扫描仍停在 2026-07-18 15:50 UTC，账龄 516.3 小时。Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457`。 |
| Migration Ledger Integrity | ⚠️ | `schema_version=394`，但 `to_regclass('public.task_execution_metrics')` 仍为 NULL。Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3`。 |
| Schema Consumer Integrity | ⚠️ | `tasks.notion_page_id` 仍不存在；生产最近 70 分钟相关缺列日志 140 条。incident reporter 同期出现 `recurrence_count` ambiguous 20 条。Issue `ff663429-5d75-4581-960d-fd676351c2eb`。 |
| Dependency Exposure | ⚠️ | `npm audit --omit=dev` 仍为 60 项生产依赖漏洞（58 moderate / 2 high / 0 critical）。Issue `a46e7823-a98c-4ada-a421-4034419e6279`。 |
| Dev Workflow Portability | ⚠️ | BusyBox 环境仍不支持 GNU `flock -w`；本轮再次使用显式 Git worktree。Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7`。 |
| Temp Patch Accumulation | ⚠️ | 根目录仍跟踪任务特定 `.brain-result.json` 与 `.task`；`report-48h.js:119` 的 mock TODO 自 2026-04-24 存在，`executor-contracts.js:305` 与 `thalamus.js:847` 的 TODO 自 2026-07-10 存在。 |
| Ledger Freshness（探针A） | ⚠️ | 19 条非 smoke/test active journey 中，任务兜底锚显示「工厂 · MJ5 承诺地图闭环」落后 10.0 天、「ZenithJoy 客户管理」落后 19.6 天；智能客服最近匹配 PR #1606 比 journey 更新时间晚约 8 天。无一项达到可确认的 >21 天红线。line ledger 总计 959 条、最近 20h 为 19 条、最新 21:00:24 UTC；24h 产出与 20h 消费的固定空窗仍在。Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803`。 |
| Smoke Consistency（探针B） | ⚠️ | 19 条 active journey 中：12 条 E2E 路径存在、4 条文件缺失、1 条为 TBD、2 条为空。废弃词命中均是说明“已移除飞书”的注释，不构成行为冲突；声明缺口仍由 Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 跟踪。 |
| Deploy Config Drift（探针C） | 🔴 | 生产 runtime/main 对齐；staging runtime `DB_HOST=host.docker.internal` 与两仓 `.env.staging=localhost` 不一致，调用者环境决定重部署结果，新 Issue `b6a0dd31-432b-4048-8fde-66b9f5a4e3b5`。staging hard-off 仍未覆盖 PromotionJob，Issue `9dda9de0-9955-4d79-a4ab-afa46bb2621d`。当前执行容器没有 `/Library/LaunchDaemons`/`launchctl`，PR #4741 的真实 LaunchDaemon 运行态无法在本轮直接取证，记为探针覆盖警告。GitHub repository variables 查询 exit=0 且为空。 |
| Kernel CI Truth Compression | ⚠️ | `ground-truth.js:77-83` 对失败 checks 仍只把 `BLOCKED` 视为阻断，未知/null merge state 可返回 pass。Issue `d44556d8-4cf0-4355-afeb-050604911dc6`。 |
| Kernel PR Identity Drift | ⚠️ | `routes/harness-callback.js:306-308` 在 `expectedBranch` 已签发时仍接受 `headRef.includes(taskShort)`，精确身份与惯例身份继续双轨。Issue `7a276ffd-34b5-4753-99d2-2c5dc4e5915a`。 |

## 4B Structural Health Findings

- 非测试生产源文件仍为 587 个，与上一轮一致；总 JS 文件从 1992 增至 1993，唯一新增文件是 `notifier-review-pending.test.js`。本轮没有新增生产孤岛。
- PR #4742 修改的 notifier、derive、kernel handler、approval route 与 route mount 均有对应测试；生产 `/api/brain/kernel-reviews/:runId/approve` 未授权请求返回 401，证明路由真实挂载。
- `executor.js`、`thalamus.js`、`cortex.js`、`planner.js` 仍全部超过 500 行；其中 `executor.js` 4615 行/30 imports，是持续扩张风险最高的 God Module。
- review 执行至少存在 lock、task claim/executor、payload run-id 三套身份；当前 run 只写前两套，callback 依赖第三套，recovery 又因字段投影遗漏看成 legacy。
- staging 的 `CECELIA_TICK_HARD_OFF=1` 只约束 tick-recovery/tick-status；server 级 PromotionJob 没有共同环境闸，health 的 scheduler stopped 不能代表“没有调度副作用”。
- migration 账本、information_schema 与运行消费者继续三本账；静态门禁和 272 个定向测试没有启动时 schema consumption 断言。
- PR #4741 的 enforcer 为 439 行并配 432 行回归测试，未超过单文件 500 行门槛；但真实 LaunchDaemon 状态对本执行容器不可见，运行态守卫仍需由宿主巡检入口提供。

## Severity

- 🔴 High: 7 项（CURRENT_STATE、review 执行终态、verdict/callback、tick 互斥、tasks 时间、recovery 投影、staging DB_HOST 持久化漂移）
- ⚠️ Medium: 14 项（边界、路由、复杂度、registry、migration、schema 消费、依赖、`/dev` 可移植性、结构债、ledger、E2E 声明、staging 全调度隔离、Kernel CI 真相、Kernel PR 身份）
- ✅ Clean: 7 项（生产 runtime/main、Brain health、breaker、三项 DevGate、272 定向测试、评审路由真挂载、生产源文件数稳定）

## Required Actions

- [ ] **P0** — 修复 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`：统一 review verdict 枚举，未知值 fail-closed，禁止 exit 0 默认 PASS。
- [ ] **P0** — 修复 Issue `6adfa549-5b69-4883-b087-649be182b8db`：spawn 原子写 claim/executor/run-id/heartbeat；callback 必须验证 `applied=true`，进程成功退出后任务进入唯一终态。
- [ ] **P0** — 修复 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：tick 超时必须传播取消或保留全局租约，旧执行真实结束前不得开启下一代。
- [ ] **P0** — 完成 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一任务生命周期时间为 UTC/timestamptz，并覆盖 PostgreSQL/Node 不同时区回归。
- [ ] **P1** — 修复 Issue `b6a0dd31-432b-4048-8fde-66b9f5a4e3b5`：统一 staging DB_HOST 持久化源，部署后断言 runtime 与推导值完全一致。
- [ ] **P1** — 修复 Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 与 `bb78074b-eca4-414e-a7f3-a9425824f7de`：补 recovery 投影，重建 CURRENT_STATE 与真实代码/API 契约。
- [ ] **P1** — 修复 Issue `9dda9de0-9955-4d79-a4ab-afa46bb2621d`：环境级 hard-off 必须覆盖 scheduler-jobs、PromotionJob 与全部独立 loop，并用 staging 真 DB 证明零自动任务。
- [ ] **P1** — 修复 Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803` 与 `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`：统一 ledger 产消窗口，修正 journey 新鲜度与 E2E 声明。
- [ ] **P1** — 修复 Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3`、`ff663429-5d75-4581-960d-fd676351c2eb`、`2288b43c-b5f1-4e83-9fd7-beb4e7cda457`：恢复真实 schema relation/列与 registry 扫描。
- [ ] **P1** — 修复 Issue `d44556d8-4cf0-4355-afeb-050604911dc6` 与 `7a276ffd-34b5-4753-99d2-2c5dc4e5915a`：CI 状态完整枚举并 fail-closed；PR 分支收敛为唯一服务端身份。
- [ ] **P2** — 定义 Cortex 越层豁免边界，拆分 `executor.js`，移除根目录旧任务结果产物，并为容器化 arch-review 提供宿主 launchd 只读探针。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.270.12`、selfcheck floor `393`、最高 migration `394`）。
- `bash scripts/check-version-sync.sh`：PASS；`check-dod-mapping.cjs` 使用主工作区依赖解析后 PASS（3 项映射）。
- Brain 定向 Vitest：18 文件 / 272 tests 全部通过，使用 `DB_NAME=cecelia_test`；首次根级入口明确返回 no tests，已改用 `packages/brain` 配置真实执行。
- 生产真实状态：`healthy`、`1.270.12@596d991b`，scheduler running，dispatch / cecelia-run breaker 均 CLOSED。
- staging 真实状态：`degraded`、`1.270.11@1dbfce40`，tick scheduler stopped；70 分钟内 PromotionJob 运行 7 次，staging DB 有 queued `ci_patrol`。
- 当前 task/lock：task `34c0eb19-...` 为 `in_progress`、`executor_kind=codex-review-local`、`current_run_id=NULL`、heartbeat=NULL；lock 含 run id `ceaf4e53-...`。
- 生产 recovery 日志：当前任务连续 4 次被投影成 `executor_kind=null (legacy) — fail-open`。
- 生产 DB：时区 `America/Chicago`；tasks 生命周期字段混用 timestamp/timestamptz；Schema MAX=394；`task_execution_metrics` relation 与 `tasks.notion_page_id` 列均缺失。
- registry：三表最新扫描 2026-07-18，age=516.3h；line ledger 总数 959、最近 20h=19、最新 2026-08-08 21:00:24 UTC。
- `npm audit --omit=dev`：60 项（58 moderate / 2 high / 0 critical）。
- 生产 `/api/brain/kernel-reviews/<run>/approve` 未授权请求返回 401；`/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 返回 404。

## ADR Needed? 是

需要明确 review runner 的唯一 claim/lock/run-id/heartbeat/callback 生命周期、review verdict 枚举、tick 超时取消/租约、任务时间类型，以及环境级 hard-off 与部署配置推导的作用域。schema 缺失、registry/ledger freshness、recovery 投影与 smoke 声明属于实现/治理缺陷，不需要单独 ADR。

## Escalate to /architect? 是

review 终态、tick 取消、时间迁移、CURRENT_STATE、Cortex 边界与环境级调度隔离均涉及跨模块合同；本巡检只记录事实、Issue 与行动项，不在报告 PR 中修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 21,
  "health_findings": 8,
  "high_severity": 7,
  "issues_created": 1,
  "escalate_to_architect": true,
  "summary": "生产与 main 对齐且 Kernel 评审路由已真实挂载，但 review verdict/run-id/callback、tick 互斥、时间/recovery/CURRENT_STATE 高风险合同仍未闭合；本轮新增 staging DB_HOST 由调用者环境决定的持久化漂移"
}
```
