---
id: arch-review-20260809
version: 1.0.0
created: 2026-08-09
updated: 2026-08-09
reviewer: Codex (Brain 自动调度)
task_id: b0ae39d4-679b-4315-83c3-44ddcf69cc3d
base_sha: 1dbfce40f3c657009b9ccc9352e70bccdaa61562
previous_task_id: 42acce17-b78b-4eaf-b38f-02d8de5961d8
---

# Architecture Review — 2026-08-09 daily（00:02 UTC）

## Executive Summary

裁决仍为 **CRITICAL**：6 项高风险、14 项中风险。上一轮后的生产改动只有 PR #4737 与 #4739：#4737 已把 Kernel 的 `gh pr update-branch` 替换为兼容 gh 2.45 的 REST API，定向测试通过，Issue `c0baa53c-57f9-4ebc-814c-8bc114d8bf38` 已关闭；#4739 仅给 playground 增加 `/kernel-e` 终验标记，不改变生产控制面。上一轮 6 个高风险合同均未被触及。

本轮 review 自身再次提供了完整的失败前置证据：锁文件记录 run id `dd7fb5b8-...`，任务行已有 `executor_kind=codex-review-local`，但 `payload.current_run_id` 与 `driver_heartbeat_at` 仍为空。`triggerCodexReview()` 继续只解析 `PASS|FAIL`，不调用已有的 `updateTaskRunInfo()`，callback fetch 也不验证 HTTP 状态或 CAS `applied`。00:09 UTC recovery 同时把该任务投影成 `executor_kind=null (legacy)` 并 fail-open。进程尚在运行、只有一份 lock，因此当前尚未重派；但进程退出后的终态断链仍是确定性的。

本轮新增 P1 staging 隔离缺口。staging 的 `CECELIA_TICK_HARD_OFF=1` 确实令 health 显示 scheduler stopped，并阻止 tick loop/watchdog；但 `server.js` 仍无条件启动 `scheduler-jobs` 与 `PromotionJob`。真实 staging 日志持续显示两者运行，`cecelia_staging` 最近 24 小时已被自动创建一条 queued `ci_patrol`。Issue `9dda9de0-9955-4d79-a4ab-afa46bb2621d` 已登记。独立 staging DB 当前避免了生产污染，但“hard-off = 调度隔离”与实际行为不一致。

21:00 UTC 的 19 条 line ledger 已进入本轮 payload，证明生产端正常产账；然而生产周期 24 小时、消费窗口 20 小时的组合合同没有变化，17:00–21:00 UTC 固定空窗仍存在。账本还保留 F2 的 `container_orphaned` 历史：数据库定位到 8 月 7 日 22:37 至 8 月 8 日 06:29 UTC 的 13 条 run 记录，涉及 4 个最终已完成任务；06:29 后未发现新同类 run，因此本轮不重复建单。

生产为 `healthy`、`1.270.11@1dbfce40`，scheduler running，两个 breaker 均 CLOSED；staging 为 `degraded`、`1.270.10@c8f1dffd`，主 tick stopped，但独立 scheduler 仍运行。三项 DevGate 全部通过，定向 19 文件 / 329 tests 通过。绿灯证明局部实现稳定，却未覆盖 review run-id/verdict/callback 组合、tick 代际互斥、时间/schema 三本账或环境级全调度隔离。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | L0/服务层仍由 `report-48h.js`、`monitor-loop.js`、`dispatch-helpers.js`、`capability-probe.js` 越过 Thalamus 调 Cortex；HTTP 路由 `analytics.js`、`ops.js` 也有直连。标准 L1→L2 入口与豁免边界仍未统一。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md:46/65/104-107/116` 仍写 Brain `1.216.0`、139 个 migration、Schema `262`；真实为 Brain `1.270.11`、最高 migration / 生产 Schema `394`。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 未收敛。 |
| Review Execution Contract | 🔴 | lock 有 run id `dd7fb5b8-...`，task `payload.current_run_id=NULL`、heartbeat=NULL；spawn 未调用 `updateTaskRunInfo()`，callback CAS 与执行进程没有共享 run 身份。Issue `6adfa549-5b69-4883-b087-649be182b8db`。 |
| Review Verdict / Callback Integrity | 🔴 | `triggerCodexReview()` 的 JSON 提取仍只接受 `PASS|FAIL`，`CRITICAL/WARN` 会 fallback；callback fetch 不检查 HTTP 状态、响应体或 `applied`。P0 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us` 当前可派发；`arch_review_followup` 仍不在 task type 合同中。AGENTS 声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 仍真实返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4615L/30；`planner.js` 1247L/4；`thalamus.js` 1816L/6；`cortex.js` 1593L/13。`executor.js` 继续承载 review、bridge、prompt、进程与 callback 多职责。 |
| Concurrency Safety | 🔴 | `tick-loop.js` 仍在 60 秒后清共享布尔锁，但不取消或 join 旧 `doTick()`；旧执行存活时新 tick 可进入，现有单测仍显式保护该 force-release 行为。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`。 |
| Temporal Integrity | 🔴 | tasks 的 created/started/completed/updated/queued 仍为 `timestamp without time zone`，claimed/heartbeat 为 `timestamptz`，DB 时区为 `America/Chicago`。本任务 started_at API 为 11:04 UTC，claimed_at 为真实 00:04 UTC，继续产生 13 小时表面偏差。 |
| Recovery Contract Integrity | 🔴 | `recovery-loop.js` 查询仍漏 `executor_kind`。00:09 UTC 当前任务 DB 已有 `codex-review-local`，恢复日志却实测 `executor_kind=null (legacy) — fail-open`。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`。 |
| Registry Freshness | ⚠️ | `api_registry`、`db_schema_registry`、`test_registry` 最新扫描仍停在 2026-07-18 15:50 UTC，账龄增至 512.4 小时。Issue `2288b43c-b5f1-4e83-9fd7-beb4e7cda457`。 |
| Migration Ledger Integrity | ⚠️ | `schema_version=394`，但 `to_regclass('public.task_execution_metrics')` 仍为 NULL；migration 账本与真实 relation 不一致。Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3`。 |
| Schema Consumer Integrity | ⚠️ | 生产 `notion-product-push` 继续每分钟报 `tasks.notion_page_id` 不存在；information_schema 同样无该列。incident reporter 对 4 个 launchd 异常继续报 `recurrence_count` ambiguous。 |
| Dependency Exposure | ⚠️ | `npm audit --omit=dev` 仍为 60 项生产依赖漏洞（58 moderate / 2 high / 0 critical），高危涉及直接依赖 `@opentelemetry/sdk-node` 的传递链。Issue `a46e7823-a98c-4ada-a421-4034419e6279`。 |
| Dev Workflow Portability | ⚠️ | `worktree-manage.sh` 仍依赖 BusyBox 不支持的 GNU `flock -w 5 201` 语义；本轮使用显式 Git worktree。Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7`。 |
| Temp Patch Accumulation | ⚠️ | 根目录仍跟踪任务特定 `.brain-result.json` 与 `.task`；前者指向历史 PR #4620，可能被后续执行体误读为当前结果。 |
| Ledger Freshness Contract | ⚠️ | 当前 21:00 UTC 批次已产出：总计 959 条 ledger、最近 20h 为 19 条、最新 21:00:24 UTC；但 24h 产出与 20h 消费的固定空窗未修。Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803`。 |
| Smoke Consistency | ⚠️ | 基线后只有 Kernel 与 playground 变更，19 条非 smoke/test active journey 的 E2E 声明缺口未收敛：12 条存在、4 条缺失、1 条 TBD、2 条为空。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`。 |
| Deploy Config / Staging Isolation | ⚠️ | 生产与 main 一致；staging 落后一个 patch。staging 主 tick hard-off，但独立 `scheduler-jobs` / `PromotionJob` 仍启动，并已在 staging DB 自动创建 queued `ci_patrol`。新 Issue `9dda9de0-9955-4d79-a4ab-afa46bb2621d`。 |
| Kernel CI Truth Compression | ⚠️ | `ground-truth.js` 对失败 checks 仍只把 `BLOCKED` 视为阻断，`UNKNOWN/null` 等可返回 pass；外部不确定状态未 fail-closed。Issue `d44556d8-4cf0-4355-afeb-050604911dc6`。 |
| Kernel PR Identity Drift | ⚠️ | `routes/harness-callback.js` 在 `expectedBranch` 已签发时仍接受 `headRef.includes(taskShort)`，精确身份与惯例身份继续双轨。Issue `7a276ffd-34b5-4753-99d2-2c5dc4e5915a`。 |

## 4B Structural Health Findings

- 上一轮后只有 #4737 修改生产 Kernel：它真实修复了 gh 版本兼容问题，但没有改变 review、tick、recovery、时间或 schema 高风险合同；#4739 仅为 playground 终验标记。
- review 执行至少有 lock、task claim/executor、payload run-id 三套身份。当前 run 再次只写前两套；callback 依赖第三套，recovery 又因字段投影遗漏看见第四种 `legacy` 身份。
- `CECELIA_TICK_HARD_OFF` 的命名与注释承诺“staging 隔离”，实现却只覆盖 tick-recovery/tick-status。server 级独立 loop 没有共同环境闸，health 的 scheduler stopped 因此无法代表“没有调度副作用”。
- F2 账本中的 orphan 记录均早于本轮基线，且相关 task 最终完成；最近一条为 06:29 UTC，之后没有新同类 run。它证明历史恢复成本，但本轮证据不足以宣告凭据/callback 修复后仍复发。
- line ledger 当前批次可用并不否定固定空窗：00:02 任务恰好在 21:00 产账后；20:01 上一任务恰好在产账前。两轮相邻巡检直接构成组合时序对照。
- migration 账本、information_schema 与运行消费者继续三本账；静态门禁和 329 个定向测试没有启动时 schema consumption 断言。
- Kernel CI 与 PR 身份问题没有代码变化；gh update-branch 已收敛说明局部兼容补丁有效，但不能替代外部状态完整枚举与唯一身份合同。

## Severity

- 🔴 High: 6 项（CURRENT_STATE、review 执行终态、verdict/callback、tick 互斥、tasks 时间、recovery 投影）
- ⚠️ Medium: 14 项（边界、路由、复杂度、registry、migration、schema 消费、依赖、`/dev` 可移植性、结构债、ledger 窗口、E2E 声明、部署/隔离、Kernel CI 真相、Kernel PR 身份）
- ✅ Clean: 6 项（生产 runtime/main 一致、两个 breaker 关闭、facts、版本同步、DoD 映射、定向测试）

## Required Actions

- [ ] **P0** — 处理 Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`：统一 review verdict 枚举；未知值 fail-closed，禁止 exit 0 默认 PASS。
- [ ] **P0** — 处理 Issue `6adfa549-5b69-4883-b087-649be182b8db`：spawn 原子写 claim/executor/run-id/heartbeat；callback 必须验证 `applied=true`，进程成功退出后任务必须进入唯一终态。
- [ ] **P0** — 处理 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：tick 超时必须传播取消或保留全局租约，旧执行真实结束前不得开启下一代。
- [ ] **P0** — 处理 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一任务生命周期时间为 UTC/timestamptz，并覆盖 PostgreSQL/Node 不同时区回归。
- [ ] **P1** — 处理 Issue `4411cb51-2969-4193-ab7e-7caa197ffa89` 与 `bb78074b-eca4-414e-a7f3-a9425824f7de`：补 recovery 投影，重建 CURRENT_STATE 与真实代码/API 契约。
- [ ] **P1** — 处理 Issue `9dda9de0-9955-4d79-a4ab-afa46bb2621d`：把环境级 hard-off 扩展到 scheduler-jobs、PromotionJob 与所有独立 loop，用 staging 真容器证明不再自动写任务。
- [ ] **P1** — 处理 Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803`：统一 ledger 产出/消费 freshness，覆盖 20:00 UTC 调度组合测试。
- [ ] **P1** — 处理 Issue `d44556d8-4cf0-4355-afeb-050604911dc6` 与 `7a276ffd-34b5-4753-99d2-2c5dc4e5915a`：CI 状态完整枚举并 fail-closed；PR 分支收敛为唯一服务端身份。
- [ ] **P1** — 处理 Issue `ce752b77-1809-4b80-8628-f2bf3c874aa3` 与 `ff663429-5d75-4581-960d-fd676351c2eb`：可重入恢复缺失 relation/列，让 selfcheck 验证真实 schema 消费成功。
- [ ] **P1** — 恢复 registry 扫描，修复 E2E 声明与 BusyBox worktree 锁兼容，升级 OpenTelemetry 依赖，并修复 incident reporter 列歧义。
- [ ] **P2** — 定义 Cortex 越层豁免边界，拆分 `executor.js`，移除根目录旧任务结果产物。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.270.11`、selfcheck floor `393`、最高 migration `394`）。
- `bash scripts/check-version-sync.sh`：PASS；`node packages/quality/scripts/devgate/check-dod-mapping.cjs`：PASS（3 项映射）。
- 定向 Vitest：Brain review/tick/recovery/callback/Kernel 18 文件 / 324 tests；playground `/kernel-e` 1 文件 / 5 tests；合计 19 文件 / 329 tests 全部通过（Brain 使用 `DB_NAME=cecelia_test`）。
- 生产真实状态：`healthy`、`1.270.11@1dbfce40`，scheduler running，dispatch / cecelia-run breaker 均 CLOSED，alertness=CALM。
- staging 真实状态：容器内 5222 为 `degraded`、`1.270.10@c8f1dffd`，tick scheduler stopped；日志同时出现 `scheduler-jobs started` 与周期 PromotionJob，staging DB 有 1 条自动 queued `ci_patrol`。
- 当前 task/lock：task `b0ae39d4-...` 为 `in_progress`、`executor_kind=codex-review-local`、`current_run_id=NULL`、heartbeat=NULL；lock 含 run id `dd7fb5b8-...`。
- 生产 recovery 日志：00:09 UTC 对当前任务输出 `executor_kind=null (legacy) — fail-open`，随后 `orphansRequeued=0`。
- 生产 DB：时区 `America/Chicago`；tasks 生命周期字段混用 timestamp/timestamptz；Schema MAX=394；`task_execution_metrics` relation 与 `tasks.notion_page_id` 列均缺失。
- registry：三表最新扫描 2026-07-18，age=512.4h；line ledger 总数 959、最近 20h=19、最新 2026-08-08 21:00:24 UTC。
- 生产日志：`notion-product-push` 持续报缺列；incident reporter 对 4 个 launchd 异常持续报 `recurrence_count` ambiguous。
- `npm audit --omit=dev`：60 项（58 moderate / 2 high / 0 critical）。
- F2 orphan 数据：最近 36h 共定位 13 条 `container_orphaned` run，最后发生于 2026-08-08 06:29 UTC，关联 4 个最终 completed task；此后无新增。
- PR #4737 的 `kernel-handlers` 回归 12 tests 通过，Issue `c0baa53c-...` 已关联该 PR 并关闭。

## ADR Needed? 是

需要明确 review runner 的唯一 claim/lock/run-id/heartbeat/callback 生命周期、review verdict 枚举、tick 超时取消/租约、任务时间类型，以及环境级 hard-off 的作用域。schema 缺失、registry/ledger freshness、recovery 投影和 gh 兼容属于实现/治理缺陷，不需要单独 ADR。

## Escalate to /architect? 是

review 终态、tick 取消、时间迁移、CURRENT_STATE、Cortex 边界与 staging 全调度隔离均涉及跨模块合同；本巡检只记录事实、Issue 与行动项，不在报告 PR 中修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 20,
  "health_findings": 7,
  "high_severity": 6,
  "issues_created": 1,
  "issues_closed": 1,
  "escalate_to_architect": true,
  "summary": "生产健康且 gh 2.45 update-branch 已修复，但 review 的 verdict/run-id/callback、tick 互斥、时间/recovery/CURRENT_STATE 高风险合同仍未闭合；本轮新增 staging hard-off 未覆盖独立 scheduler 的隔离漂移"
}
```
