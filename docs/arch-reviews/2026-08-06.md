---
id: arch-review-20260806
version: 1.0.0
created: 2026-08-06
updated: 2026-08-06
reviewer: Codex (Brain 自动调度)
task_id: f981ba10-49b1-477c-a780-6307e2943fcc
base_sha: 20bc8fd8c21512041217abe120acad79ff288148
previous_task_id: 57e00be1-8348-43bd-9e89-03d0c209c7b0
---

# Architecture Review — 2026-08-06 daily（12:00 UTC）

## Executive Summary

裁决仍为 **CRITICAL**，高风险项维持 3 项，但构成发生变化。`arch_review` 本身已稳定启动：本任务于 12:01:45 UTC claim，review lock 同时创建，复核期间保持 `in_progress`、`retry_count=0`、`error_message=null`，两个 circuit breaker 均为 `CLOSED`。昨日的 `Agent 系统 hardening` 台账风险也已部分收敛：journey 于 05:50 UTC 更新并退役，active E2E 非法声明从 7 条降至 5 条。

本轮新增一项生产 P0：12:00:45 开始的 tick 超过 60 秒后，safety timer 只释放 `tickRunning` 锁而不取消旧 `doTick()`；12:01:45 与 12:03:45 又启动并完成新 tick，原 tick 直到 12:05:00 才结束，记录耗时 `255539ms`。生产已真实发生多轮 scheduler 生命周期重叠，Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0` 已登记。

时间契约与 CURRENT_STATE 正本风险仍未收敛。当前任务再次证明 `tasks` 五个生命周期时间使用无时区类型、`claimed_at` 使用带时区类型，API 展示早 13 小时；`SYSTEM_MAP.md` 虽在 PR #4681 把 `updated` 改成 2026-08-06，却仍保留 Brain `1.216.0` / Schema `262`，实际为 `1.267.247` / `391`，形成“元数据新鲜、事实过期”的更强误导。此外，`/dev` 唯一 worktree 入口依赖 GNU `flock -w`，当前 BusyBox 执行环境会在 Step 0 直接失败，Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7` 已登记。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | L0 服务仍在 `report-48h.js:144`、`dispatch-helpers.js:201`、`monitor-loop.js:483`、`capability-probe.js:166` 动态 import `cortex.js`；HTTP 路由 `routes/analytics.js:5/234`、`routes/ops.js:2147` 也直连 Cortex。`thalamus.js:1607` 才是标准 L1→L2 升级路径。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md` 的 frontmatter 已在 PR #4681 更新为 2026-08-06，但正文仍写 Brain `1.216.0`、Schema `262`、旧模块/CI 数量；当前事实为 Brain `1.267.247`、Schema `391`。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de` 仍为 In progress。 |
| Routing Integrity | ⚠️ | `arch_review → /arch-review review → us`、`executor_kind=codex-review-local` 接线正确；但 `arch_review_followup` 仍不在 task type/DB 合同中。AGENTS 声明的 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 均真实返回 404。 |
| Core Module Complexity | ⚠️ | `tick.js` 188L/12 imports；`executor.js` 4547L/29；`planner.js` 1247L/4；`thalamus.js` 1807L/6；`cortex.js` 1593L/13。上一轮后这些核心模块没有继续增长，但 `executor.js` 仍是主要 God Module。 |
| Concurrency Safety | 🔴 | `tick-loop.js:87-100` 的 60 秒 safety timer 只清锁，不取消 `await doTick()`。生产日志证明旧 tick 存活至 12:05:00 时，12:01:45 与 12:03:45 已有新 tick 完成；重叠执行约 3 分 15 秒。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`。本次 arch-review 自身的 lock liveness 正常。 |
| Temporal Integrity | 🔴 | `created_at/started_at/updated_at/queued_at/completed_at` 为 `timestamp without time zone`，`claimed_at` 为 `timestamptz`；DB 时区 `America/Chicago`，Node 容器时区 `Asia/Shanghai`。SQL 内本任务 claim delay 仅 `1m42.175s`，API 却显示 created `2026-08-05T23:00:03Z`、claimed `2026-08-06T12:01:45Z`，表面早 13 小时。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b` 仍为 In progress。 |
| Dev Workflow Portability | ⚠️ | `worktree-manage.sh:120` 调用 `flock -w 5 201`；当前 `/usr/bin/flock -> /bin/busybox`，BusyBox 1.37 不支持 `-w`，本任务 `/dev` Step 0 实际失败后只能按协议显式创建隔离 worktree。Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7`。DoD gate 还依赖主工作区未声明的 `NODE_PATH` 才能找到 `js-yaml`。 |
| Temp Patch Accumulation | ⚠️ | 超过 7 天的结构债仍在：`report-48h.js:119`（`mock_cortex`）、`sprint-result-contract.js:37-64`、`trace-routes.js:237/243`、`decision-executor.js:225/304`、`alertness/healing.js:448/475/1037`、`thalamus.js:847`（重复失败分类表）。 |
| Ledger Freshness（探针A） | ⚠️ | `Agent 系统 hardening` 已于 05:50 UTC 更新并标为 `deprecated`，昨日约 75 天的 active 漂移已消除；但 Issue `7d2cc1d3-18ca-465e-96e2-f904af103c6c` 仍为 In progress，本任务的 24h 账本仍把它作为活跃问题呈现，状态联动未收口。 |
| Smoke Consistency（探针B） | ⚠️ | 当前 19 条非 smoke/test active journey，17 条有 E2E 声明；逐条核查 `cecelia/main` 与 `zenithjoy-workspace/main` 后 12 条存在、4 条缺失、1 条 TBD，合计 5 条非法。相比昨日 19/7 已改善，Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 的旧标题仍停在 14/9。 |
| Deploy Config Drift（探针C） | ⚠️ | 生产为 `1.267.247@20bc8fd8`，与 main 完全一致；staging 为 `1.267.245`，落后两个版本。生产/暂存的端口、tick 开关与 hard-off 均匹配各自 Compose。主仓与部署仓 `.env.docker` key-set 差异从 5 项降至 3 项（`ACCEPTANCE_API_TOKEN`、`ACCEPTANCE_PUBLIC_HOST`、`CECELIA_TICK_ENABLED`）；GitHub Variables 为空。 |

## 4B Structural Health Findings

本次为 daily 轻量扫描：

- 上一轮 20:00 UTC 巡检后，`packages/brain/src` 的 JS/TS 文件数从 1985 增至 1995（+10）；全仓 79 个文件、`+10337/-379`。
- 新增生产模块 `notion-inbox-push.js`、`notion-verdict-ingest.js`、`vocab-alias.js` 均有同名测试；新增/更新的 F1/WS3、Judge 与 orchestrator 状态机逻辑也有对应测试。
- 主要生产代码 churn 为 `notion-verdict-ingest.js`（+222）、`notion-inbox-push.js`（+187）、`harness-judge.js`（125）与 `orchestrator/derive.js`（78）；核心五模块行数/import 数未增长。
- 根目录仍跟踪任务特定的 `.brain-result.json` 与 `.task`；前者固定指向旧任务 `93161b22-...` / PR #4620，后者仍写 migration 164 修复，均可能被后续执行误读。

## Severity

- 🔴 High: 3 项（tick 超时后并发重叠、tasks 时间契约分裂、CURRENT_STATE 假新鲜）
- ⚠️ Medium: 8 项（边界、路由合同、复杂度、`/dev` 可移植性、TODO、台账状态联动、E2E 声明、部署源/版本）
- ✅ Clean: 4 项（arch-review 执行活性、DEFINITION facts、版本同步、生产 Compose/runtime/main 一致）

## Required Actions

- [ ] **P0** — 处理 Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`：为 tick 引入可传播的取消/截止合同，或在旧执行真实终止前保留全局租约；必须补“旧 tick 超时仍存活时新 tick 不得进入”的生产等价回归测试。
- [ ] **P0** — 处理 Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`：统一 `tasks` 生命周期字段为明确 UTC/timestamptz 契约，补 PostgreSQL 非 UTC + Node 不同时区测试，并审计所有 `Date(row.*_at)` 消费点。
- [ ] **P1** — 处理 Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`：重建 `SYSTEM_MAP.md` 当前事实；禁止只更新 frontmatter 而不校验正文 facts，并统一 followup task type/路由/DB/API 文档合同。
- [ ] **P1** — 处理 Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7`：让 worktree 锁在 BusyBox/GNU `flock` 下行为一致，并让 DoD gate 的 `js-yaml` 依赖可从隔离 worktree 自包含解析。
- [ ] **P1** — 更新 Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a` 基线为 17 条声明中 5 条非法；路径必须在声明的 base repo main 上真实存在。
- [ ] **P1** — 关闭或重写 Issue `7d2cc1d3-18ca-465e-96e2-f904af103c6c`，并让 journey 退役自动同步 Issue/line ledger，避免已收敛风险继续出现在新任务 PRD。
- [ ] **P2** — 消除双 `.env.docker` 的 3 个 key-set 差异，明确唯一部署配置 SSOT，并使 staging 版本滞后可观测。
- [ ] **P2** — 定义 L0/HTTP 路由直调 Cortex 的豁免边界，继续拆分 `executor.js`，清理根目录任务结果产物与超期 TODO。

## Verification Evidence

- `node scripts/facts-check.mjs`：PASS（Brain `1.267.247`、Schema `391`、migration 无冲突、LLM fetch timeout 完整）。
- `bash scripts/check-version-sync.sh`：PASS（四处版本均为 `1.267.247`）。
- `NODE_PATH=/Users/administrator/perfect21/cecelia/node_modules node packages/quality/scripts/devgate/check-dod-mapping.cjs ...`：PASS（3 项映射）；无 `NODE_PATH` 时真实失败为 `js-yaml` 不可解析。
- 生产 DB 只读探针：`schema_version MAX=391`；tasks 六个时间字段类型、DB 时区、SQL claim delay 与 API 序列化值均已实测。
- 生产日志：12:01:45 输出 `FORCE-RELEASE via setTimeout`；新 tick 于 12:01:45/12:03:45 完成，旧 tick 于 12:05:00 完成；health 暴露 `last_duration_ms=255539`。
- 生产真实状态：Brain `healthy`、`1.267.247@20bc8fd8`，任务 lock 存在且无 retry/error，`dispatch`/`cecelia-run` circuit breaker 均关闭；生产镜像与 main 一致。
- GitHub 内容 API：17 条 active E2E 声明逐条核查，12 条有效、4 条缺失、1 条 TBD；hardening journey 已读取单条 API 复核为 `deprecated`。

## ADR Needed? 是

需要明确三项治理决策：tick 超时后的取消/互斥租约合同、任务生命周期时间的唯一类型/时区合同，以及 L0/HTTP 路由直调 Cortex 与 arch-review followup 的合法边界。

## Escalate to /architect? 是

tick 取消传播、时间类型迁移与消费端兼容、CURRENT_STATE 正本和 Cortex 边界均涉及跨模块契约；本巡检只记录事实与风险，不在此 PR 中设计或修改架构。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 11,
  "health_findings": 4,
  "high_severity": 3,
  "escalate_to_architect": true,
  "summary": "arch_review 执行活性稳定，但生产 tick 超时后发生多轮 scheduler 重叠；tasks 时间契约、SYSTEM_MAP 正本与 /dev worktree 入口仍有系统性漂移"
}
```
