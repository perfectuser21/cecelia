---
id: arch-review-20260811
version: 1.2.0
created: 2026-08-11
updated: 2026-08-11
reviewer: Codex (Brain 自动调度)
task_id: 3814a72e-e704-4515-92c9-0b96c154201f
base_sha: 1d588ec6e1529cc80f23fd53a7e1ba3a82901540
previous_task_id: 8cd4aa40-5d11-4371-a853-bd7c25dbd3c3
---

# Architecture Review — 2026-08-11 daily（20:00 UTC，20:02 UTC 执行）

## Executive Summary

裁决仍为 **CRITICAL**：9 项高风险、19 项中风险。16:00 基线后 main 只合入 3 个实现提交，未关闭此前 9 项高风险。生产当前运行 `1.272.17@1d588ec6e1` 且 health/两个 breaker 正常，但 `cecelia-node-brain` 仍没有 `CECELIA_INTERNAL_TOKEN`，宿主匿名无效请求 `POST /api/brain/issues {}` 继续返回 503 `INTERNAL_AUTH_NOT_CONFIGURED`。修复 PR #4814 仍为 OPEN/DRAFT/DIRTY；因此应用层 fail-closed 正常，部署层凭据交付仍未完成。

环境隔离与 schema 真相出现了更强证据。staging 设置 `CECELIA_TICK_HARD_OFF=1`，却仍启动 Nightly、33 个 scheduler jobs、3 个 projection jobs、PromotionJob，并在 20:00 后真实派发 1 个任务；其 health 又复用了生产式 `scheduler=running/enabled=true` 投影。production/staging 的 `schema_version` 最大值都为 410，但 staging 的物理 `tasks.retry_count` 列缺失，证明“最大 migration 号相等”不能代表 schema 完整。PR #4821 只补了 `harness_attempts.failure_class` 单列 parity guard，尚未建立通用物理 schema ↔ migration ledger 合同。

preview-4794 也在 `CECELIA_TICK_ENABLED=false`、没有 hard-off 的状态下于同窗创建并派发 task `14a91cbc-...`，production 同时派发当前 task `3814a72e-...`；两者各有 lock/run id 与一组 Codex 进程。preview child 没有环境专属 `BRAIN_URL`，因而默认回调 `http://localhost:5221` production，但 production DB 根本没有 preview task。此前“preview 会重复调度”的风险已经从建单扩展成真实外部执行与必然失配的 callback。

20:00 Smoke E2E Nightly 实跑仅 8/25 通过、17/25 失败，且已有 P1 Issue `af7361b3-1873-424c-9aee-60c2b24f7e79` 记录连续三次失败。池中 16/25 是按历史任务短 SHA 命名的 `relay-*` 脚本；runner 没有提供可用的 `DATABASE_URL`，多个脚本回退到 runner 本机 PostgreSQL，连接失败后又把错误压成“migration 316 未跑”。只读直查证明 production/staging 的 `initiative_runs.tmux_killed_at` 都存在，因此 nightly 当前同时包含真实失败和错误诊断。同期 Nightly Regression 也因 Real-Env Smoke job 失败，日志同样包含缺 DB、缺 repo path 与失效 preview fixture；两条夜间闸都不能提供可信发布结论。

本次 `arch_review` 执行身份继续分裂：lock 文件持有 run id `3b3c6b9a-...`，task 已由 `brain-tick-7` claim，且 `executor_kind=codex-review-local`；但 payload `current_run_id`、`run_status` 与 `driver_heartbeat_at` 仍为空。生产日志中的 liveness 投影又把同一 task 读成 `executor_kind=null (legacy) — fail-open`。标题/claim 为 20:00 UTC，API 的 `created_at/started_at` 却为 07:00/07:02 UTC，时间与 recovery 合同均未收敛。

registry 本轮已真实刷新到 20:05 UTC，但 line ledger 的固定空窗也被当前 task 精确复现：production 共 1001 条，最近 20h 为 0，最新停在 2026-08-10 21:00:28 UTC；当前 task 的 `prd_summary` 没有任何账本摘要。active journey 当前为 20 条，其中 14 条声明 E2E、6 条为空。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | `report-48h.js`、`monitor-loop.js`、`dispatch-helpers.js`、`capability-probe.js`、`routes/analytics.js`、`routes/ops.js` 等仍绕过 Thalamus 直调 Cortex；标准 L1→L2 入口与豁免边界未统一。 |
| Undocumented Changes | 🔴 | `docs/current/SYSTEM_MAP.md` 仍写 Brain `1.216.0`、schema 262；当前正本为 `1.272.17`、schema floor 410。Issue `bb78074b-eca4-414e-a7f3-a9425824f7de`。 |
| Preview / Staging Scheduler Isolation | 🔴 | preview-4794 与 production 同窗各派一个 arch review；preview callback 又默认指向 production。staging hard-off 只停主 tick，Nightly/33 scheduler/3 projection/Promotion 仍启动并真实派发。Issue `a9a6e3f6-06c7-42a7-840f-09e6ef7f448a`。 |
| Internal Auth Deployment Contract | 🔴 | 生产容器仍缺 `CECELIA_INTERNAL_TOKEN`，宿主 mutation API 实测 503；PR #4814 仍 OPEN/DRAFT/DIRTY。 |
| Review Execution Contract | 🔴 | lock 有 run id，task payload/heartbeat 无 run id；liveness 投影把 DB 中的 `codex-review-local` 读成 legacy null。Issue `6adfa549-5b69-4883-b087-649be182b8db`。 |
| Review Verdict / Callback Integrity | 🔴 | executor 仍只解析 `PASS|FAIL`，callback fetch 不校验 HTTP/CAS `applied`；preview task 把 callback 发向不存在该 task 的 production DB。Issue `72546baf-0d20-4a4f-8ee5-1405672756d7`。 |
| Routing / Skill Integrity | ⚠️ | `arch_review → codex-review-local → us` 可派发；但路由声明的 `packages/workflows/skills/arch-review/SKILL.md` 不存在，`/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 均为 404，`arch_review_followup` 也无 task type/回调合同。 |
| Universal Map Authority | ✅ | manifest 写、projection store、read service 当前统一走 `src/lib/*`；生产写权威维持单一。 |
| Universal Map Legacy Residue | ⚠️ | `src/map/manifest-schema.js`、manifest-store/projector/state-resolver 与对应测试仍保留但无生产引用，仍需删除或明确 archive。 |
| Core Module Complexity | ⚠️ | `executor.js` 4615L、`routes/execution.js` 4091L、`routes/ops.js` 3024L、`routes/analytics.js` 2310L、`thalamus.js` 1816L；Brain 非测试 JS 为 633 文件/174265 行。 |
| Concurrency Safety | 🔴 | `tick-loop.js` 超过 60 秒仍只清共享布尔锁，不 cancel/join 旧 `doTick()`；现有测试仍把 force release 当正确行为。Issue `9aa89588-9dfc-496c-aad0-7784773b2ed0`。 |
| Temporal Integrity | 🔴 | tasks 的 created/queued/started/completed/updated 为 `timestamp without time zone`，claimed/heartbeat 为 `timestamptz`，DB 时区为 America/Chicago；本任务继续复现 13 小时时间分裂。Issue `41f68c38-c475-4c2a-a859-31fd34d8bf3b`。 |
| Recovery Contract Integrity | 🔴 | recovery/liveness 投影继续漏失或覆盖 `executor_kind`，真实 `codex-review-local` 被评估为 legacy null。Issue `4411cb51-2969-4193-ab7e-7caa197ffa89`。 |
| Registry Freshness | ✅ | production 三表最新扫描为 20:05:37/38/38 UTC；staging 三表仍为空。 |
| Schema Ledger / Physical Parity | 🔴 | production/staging ledger 都到 410，但 staging `tasks.retry_count` 物理列缺失；最大版本号 selfcheck 无法发现局部 schema 漂移。PR #4821 仅守一列。Issue `8a782f87-16d3-4ad6-a901-843efd57d50f`。 |
| Schema Consumer Integrity | ⚠️ | `incident-reporter` 的 upsert 在生产持续报 `recurrence_count` 列歧义；mock 单测仍锁定错误 SQL，incident recurrence 台账漏写。 |
| Dependency Exposure | ⚠️ | `packages/brain npm audit --omit=dev` 实跑为 56 项：54 moderate、2 high、0 critical；两个 high package 均来自 OpenTelemetry 依赖链。 |
| Dev Workflow Portability | ⚠️ | BusyBox `flock` 不支持 `/dev` 脚本的 `-w`；隔离 worktree 的 DoD mapping 也依赖宿主 `node_modules` 才能解析 `js-yaml`。Issue `5ff60dff-d910-4101-b3b8-5d3695b819b7`。 |
| Temp Patch Accumulation | ⚠️ | 根目录继续跟踪任务特定 `.brain-result.json`/`.task`；结果合同、通知、预测和重复失败分类等结构债未归位。 |
| Ledger Freshness | ⚠️ | production line ledger 1001 total / 0 recent 20h，最新为 2026-08-10 21:00:28 UTC；当前 task payload 无账本摘要。Issue `a1ed9884-eeb7-46e2-81a1-8d6922728803`。 |
| Journey Smoke Registry | ⚠️ | active journey 20 条，14 条声明 E2E、6 条为空；跨仓路径真实性仍缺单一探针。Issue `925aa82e-e59a-442a-b3ba-fbcfa28e3c1a`。 |
| Deploy Config Drift | ⚠️ | production/staging 同为 `1.272.17@1d588ec6e1`，但 staging DB_HOST 仍依赖调用者环境，production token 交付仍缺。Issue `b6a0dd31-432b-4048-8fde-66b9f5a4e3b5`。 |
| Deployment Closure | ⚠️ | main Push/CI/Gate3 对当前 SHA 成功，但生产仍缺 token；发布检查证明“镜像/SHA 成功”没有验证真实配置效果。 |
| Kernel CI Truth Compression | ⚠️ | failed checks 与 UNKNOWN merge state 的 fail-closed 合同仍未统一。Issue `d44556d8-4cf0-4355-afeb-050604911dc6`。 |
| Kernel PR Identity Drift | ⚠️ | 已有 expected branch 时仍存在 task-short 身份 fallback，签发身份未成为唯一真相。Issue `7a276ffd-34b5-4753-99d2-2c5dc4e5915a`。 |
| Realtime Voice Duplication | ⚠️ | 5 个物理 hook 文件、两组重复实现尚未收敛。Issue `1daab7e4-088c-44b7-862b-29dd7e1394d5`。 |
| Janitor /dev Lock Contract | ⚠️ | `/dev` 默认 `~/worktrees` 与 Janitor 扫描/锁路径仍无跨模块行为合同；本任务再次运行在该外部 worktree 根。Issue `c00b0090-ab86-49c0-836a-edafef37c4dd`。 |
| Scheduler Test Collection | ⚠️ | 10 个 review/tick/recovery/liveness/incident 文件共 68/68 通过；完整 `daily-review-scheduler.test.js` 显式请求仍因标准配置 exclude 而 0 收集。Issue `0acbf8f3-38dc-42ff-9d0b-f2e627d45c02`。 |
| Guard Drill Idempotency | ⚠️ | rolling 24h production/staging 分别新增 28/24 条同名 P1；失败不更新持久 gate，Issue 写入无 guard_id upsert。Issue `9893e28b-cbb8-483f-8b14-e2044fa9da5e`。 |
| Nightly Test Truth | ⚠️ | Smoke E2E run `31530646737` 为 8 PASS/17 FAIL；25 个脚本中 16 个是历史 `relay-*`，DB 连接失败被误报为缺 migration。Nightly Regression `31530668738` 也因 Real-Env Smoke 失败。连续三次失败 Issue `af7361b3-1873-424c-9aee-60c2b24f7e79`。 |

## 4B Structural Health Findings

- 16:00 基线后只有 3 个实现提交：fleet runner 安装依赖、`harness_attempts.failure_class` 单列 parity guard、harness judge 精确 PR 命令证据；没有一项关闭本报告的 9 个高风险合同。
- internal auth 的代码与 sidecar 修法存在，但生产容器仍无 token；当前 deploy 成功态没有验证配置交付效果。
- staging 的 hard-off 不是环境级副作用开关；独立 loops 仍写任务/Issue/调度状态，health 也不能表达真实隔离状态。
- schema_version=410 与物理列完整性已经实证分裂；“只比最大 migration 号”的 selfcheck 与 deploy gate 不足以防 schema 漂移。
- 两条 nightly 都把大量不同环境前提的 smoke 汇入单池，却没有 owner、期限、fixture、DB/repo path 与退役合同；当前红灯既不定位产品故障，也不能稳定复现。
- review 执行仍有 DB claim、文件 lock/run-id、payload heartbeat 三套身份；liveness 又使用第四套不完整投影。
- guard-drill 的 Bark 有 dedupe，Issue 无 dedupe；incident reporter 本应聚合 recurrence，却因 SQL 歧义失效，三套告警台账继续分裂。
- `/arch-review` 的 instruction-book 存在，但执行 SKILL 与两个声明 API 缺失；定时任务依靠 executor 特例仍能启动，能力注册表不能证明能力可加载。
- CURRENT_STATE、Journey/E2E、line ledger 与实际代码/运行态继续不同步；文档正本保鲜仍靠巡检报告人工指出。
- God Module 与复制债无改善：五个核心模块合计约 13.9k 行，Realtime Voice 仍有 5 个物理副本。

## Severity

- 🔴 High: 9 项（CURRENT_STATE、环境副作用隔离、internal auth 部署、review 身份、verdict/callback、tick 互斥、tasks 时间、recovery 投影、schema ledger/物理 parity）
- ⚠️ Medium: 19 项（边界、路由/skill、Map 遗留、复杂度、incident、依赖、`/dev` 可移植性、结构债、ledger、Journey E2E、deploy 配置/闭环、Kernel CI/PR、语音复制、Janitor、scheduler 测试、guard-drill、nightly 真值）
- ✅ Clean: 7 组（Brain health、breaker、三项 DevGate、registry 基线、Map 单一生产权威、production schema=410、当前 main Push/CI/Gate3）

## Required Actions

- [ ] **P0** — 完成 PR #4814 并做运行态验收：生产匿名敏感请求=401、带凭据请求=2xx，禁止 503；deploy 成功判据必须验证容器 token key 与真实 API 效果。
- [ ] **P0** — 处理 Issue `a9a6e3f6-06c7-42a7-840f-09e6ef7f448a`：建立统一 loop 生命周期注册表，preview/staging 默认禁用全部写副作用；health 必须与 hard-off 一致。
- [ ] **P0** — 完成 review claim/run-id/heartbeat/callback 原子合同、未知 verdict fail-closed、tasks 全量 timestamptz、tick 取消/租约与 recovery `executor_kind` 投影。
- [ ] **P0** — 扩展 Issue `8a782f87-16d3-4ad6-a901-843efd57d50f`：从“最大 migration 号比较”升级为 production/staging/test 物理 schema ↔ migration ↔ code consumer 通用 parity gate，至少先补齐 staging `tasks.retry_count`。
- [ ] **P1** — 处理 Issue `af7361b3-1873-424c-9aee-60c2b24f7e79`：两条 nightly 只运行显式 registry；每项声明 owner/fixture/DB/repo endpoint/expiry，移出一次性 relay 资产，连接失败不得伪装成 schema 缺失。
- [ ] **P1** — 修复 `incident-reporter` 为限定表名的 recurrence upsert，增加真实 PostgreSQL conflict 回归；guard-drill 用持久 gate + guard_id upsert。
- [ ] **P1** — 恢复完整 scheduler 测试进标准 CI，补回 `/arch-review` SKILL/注册 API 的可加载合同，更新 SYSTEM_MAP、Journey E2E 与 line ledger 正本。
- [ ] **P2** — 收敛 Cortex 边界、God Module、staging DB_HOST、OpenTelemetry 漏洞、语音 hook 复制与根目录任务产物。

## Verification Evidence

- 三项 DevGate：`facts-check` PASS、`check-version-sync` PASS；DoD mapping 在显式使用宿主 `node_modules` 后 PASS，隔离 worktree 默认运行因缺 `js-yaml` 失败。
- production：healthy、`1.272.17@1d588ec6e1`、breakers all CLOSED；容器 token key=0，宿主 issue mutation 探针为 503。
- staging：healthy、同 SHA、token key present、`CECELIA_TICK_HARD_OFF=1`；日志仍启动 Nightly/33 scheduler/3 projection/Promotion，并派发 1 个任务。
- DB：两库时区 America/Chicago、schema ledger max=410；production 有 `tasks.retry_count`，staging 无；两库均有 `initiative_runs.tmux_killed_at`。
- review task：lock run id=`3b3c6b9a-...`；task payload 无 run-id/heartbeat，API 时间 07:00/07:02 与 20:02 claim 分裂；日志 liveness 读成 legacy null。
- preview-4794：PID 5063、SHA `f8fd3ada`、DB `cecelia_preview_4794`、tick false、无 hard-off；task `14a91cbc-...` 与 production task 同窗派发，两组 child 都使用 `BRAIN_URL=http://localhost:5221`。
- 定向测试：10 files / 68 tests 全通过；`daily-review-scheduler.test.js` 显式请求后仍 0 收集。
- Smoke E2E run `31530646737`：8/25 PASS、17/25 FAIL；16 个 `relay-*`，KV 写入成功，Bark 失败；连续三次失败 Issue 已存在。
- Nightly Regression run `31530668738`：仅 Real-Env Smoke job 失败；失败池包含缺 DB socket、repo `realpath`、preview fixture 与纯函数 smoke 等多种前提错误，其余 Brain unit shards 成功。
- guard-drill rolling 24h：production 28、staging 24 条同标题 P1；incident reporter 日志持续报 `recurrence_count` 歧义。
- registry/ledger/Journey：registry 最新 20:05 UTC；line ledger 1001 total / 0 recent 20h；active journeys 20 / E2E 14 / missing 6。
- dependency audit：Brain 54 moderate / 2 high / 0 critical；high package 来自 OpenTelemetry 链。
- GitHub：当前 main Push/CI/Gate3 对 `1d588ec6` 成功；PR #4814 仍 OPEN/DRAFT/DIRTY；20:00 Smoke E2E 与 Nightly Regression 均失败。

## ADR Needed? 是

需要定义五项跨模块合同：环境级统一 loop 生命周期与副作用白名单、internal token 在 host/container/sidecar/CI 的唯一交付、review claim/run-id/heartbeat/callback 唯一身份、物理 schema/migration/code consumer 三方 parity，以及 Smoke E2E registry 的 owner/fixture/expiry/诊断语义。

## Escalate to /architect? 是

环境隔离、内部鉴权、review 终态、时间/互斥、schema parity 与 Nightly 真值跨越 server、scheduler、DB、sidecar、CI 和 Dashboard；本巡检只记录事实与约束，不在报告 PR 中修改运行时代码。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 30,
  "health_findings": 10,
  "high_severity": 9,
  "medium_severity": 19,
  "issues_created": 0,
  "issues_updated": 0,
  "escalate_to_architect": true,
  "summary": "preview 与 production 同窗重复派发且 preview callback 指向 production；生产 internal token 仍缺且宿主写面 503；staging hard-off 下独立写 loops 继续运行，schema ledger=410 仍缺 tasks.retry_count；两条 nightly 均失败且诊断失真；review/time/tick/recovery 合同未闭合"
}
```
