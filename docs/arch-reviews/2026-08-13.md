---
id: arch-review-20260813
version: 1.3.0
created: 2026-08-13
updated: 2026-08-13
reviewer: Codex (Brain 自动调度)
task_id: 60d93710-d277-40b0-9222-1b6f0f012925
base_sha: b4d41ad27d90b218d83d107fe78edd3b4ee499a8
latest_task_id: 9797c817-22e2-48aa-a232-a5f678ac89b7
latest_base_sha: 813dc7037c15e4946d51f921a9a9bff00c575c16
---

# Architecture Review — 2026-08-13 daily（04:00 UTC，05:07 UTC 执行）

## Executive Summary

裁决为 **CRITICAL**：11 项高风险、18 项中风险。当前 production 与 staging 都运行 `1.272.36@b4d41ad`；三项 DevGate 全绿，Preview scheduler/projection 守卫、Kernel 合同产物传输与持久日志等新代码均已进入 main。当前 production health 为 healthy、breaker 全 CLOSED，staging 主 Tick health 为 stopped，说明局部保护能够如实生效。

但 PR #4853 只关闭 Preview 的 scheduler-jobs 与 projection loops，没有关闭主 Tick、Nightly 或克隆任务燃料。preview-4860/4861 都设置 `BRAIN_PREVIEW=1` 与 `CECELIA_TICK_ENABLED=false`，日志也确认两个新守卫生效；克隆库中的 `working_memory.tick_enabled=true` 仍令主 Tick 启动，启动脚本又只终止克隆的 `harness_initiative`。两个 Preview 因而分别 claim 同一 `arch_review` 快照任务 `60d93710-...`，并同时启动两组 Codex。production 对同一 UUID 仍是 `queued`；两个 Preview 各自是 `in_progress`，均无 DB run-id/heartbeat。共享文件锁被第二次启动覆盖，第一执行继续存活却失去文件侧唯一身份。

数据库权威出现更严重的漂移。`origin/main` 的 migration 与 selfcheck 最大值是 412，production ledger 已写入 413/414，物理 `work_routing_receipts` 也存在；这两份 migration 来自已关闭且未合并的 PR #4851，staging 仍停在 412。开放 PR #4860 又把另一份 migration 413 用于 Session Controller。它的 Preview DB 从 production 克隆后已占用 413，因此该 migration 未执行，两个预期 controller 列真实不存在。生产先落未合并 schema 已从“版本超前”发展成“同号不同语义”，会让 Preview/CI 对开放 PR 给出失真的成功信号。

环境隔离、review 终态、时间类型与质量闸继续未闭合。staging hard-off 后仍启动 Nightly、33 scheduler、3 projection 与 Promotion，并真实创建 guard-drill Issue。`triggerCodexReview()` 仍只解析 `PASS|FAIL`，CRITICAL 在 exit 0 时降为 PASS，callback 不检查 HTTP/CAS；当前 Preview 又把 callback 默认指向 production。任务生命周期字段继续混用 timestamp/timestamptz，API 中 started/claimed 仍相差约 13 小时。三条 nightly 最近 8 次全红，Real-Machine 最近 6 次全绿；最新 scheduled Deploy 的 Release Gate 因缺 nightly 绿色证据失败。

## 4A Drift Findings

| 检查项 | 状态 | 发现 |
|--------|------|------|
| Boundary Drift | ⚠️ | `decision-executor.js` 与 `routes/analytics.js` 仍直连 Cortex；L1→L2 的统一入口与例外清单没有形成机器合同。 |
| Undocumented Changes | 🔴 | `SYSTEM_MAP.md` 仍写 Brain `1.216.0`；当前为 `1.272.36`。`DEFINITION.md` 顶部写 schema 412，后文仍写 411。Issue `bb78074b-...`。 |
| Preview / Staging Isolation | 🔴 | #4853 仅关闭 Preview scheduler/projection；两个 Preview 主 Tick仍从克隆 DB 派发同一 review。staging hard-off 下独立 loops 仍运行并写 Issue。Issue `a9a6e3f6-...` 已更新。 |
| Review Execution Contract | 🔴 | production task queued；两个 Preview task in_progress；两组进程共用一个可覆盖 lock，三库均无 current_run_id/heartbeat。Issue `6adfa549-...` 已更新。 |
| Review Verdict / Callback Integrity | 🔴 | executor 仍把裁决压成 `PASS|FAIL`、exit 0 缺省 PASS，且不验证 callback HTTP/CAS；Preview callback 缺省指向 production。Issue `72546baf-...` 已更新。 |
| Dev → Harness Routing Integrity | 🔴 | routing 持久化失败已改为 fail-closed，但 canonical 改道仍晚于 harness admission/bridge；`repo/base_repo` 身份未统一。Issue `e0370d57-...`。 |
| Session Controller Ownership | 🔴 | main 的 kernel-v1 入口仍无持久 Controller ownership；开放 PR #4860 的迁移因 413 同号碰撞未在其 Preview DB 建列。Issue `962d399c-...`。 |
| Frozen Contract Artifact Integrity | ✅ | migration 412、批准 SHA/产物冻结、执行前重封与路径顺序归一已进入 main；相关提交都有定向测试与 smoke。 |
| Universal Map Authority | ✅ | manifest 写、projection store、read service 继续统一走 `src/lib/*`，本轮 Kernel 改动没有新增生产写源。 |
| Universal Map Legacy Residue | ⚠️ | `src/map/*` legacy 模块与测试仍保留，archive/生产引用边界没有形成合同。 |
| Core Module Complexity | ⚠️ | Brain 非测试 JS 为 639 文件/176005 行；`executor.js` 4615L、`execution.js` 4099L、`ops.js` 3024L、`analytics.js` 2310L、`harness.js` 2174L。 |
| Concurrency Safety | 🔴 | Tick 超时仍只释放共享布尔锁，不 cancel/join 旧 `doTick()`；现有测试继续认可 65 秒后开放下一轮。Issue `9aa89588-...`。 |
| Temporal Integrity | 🔴 | tasks 的 created/queued/started/completed/updated 为 timestamp，claimed/heartbeat 为 timestamptz；DB 时区 America/Chicago，当前任务 API 时间继续错位约 13 小时。Issue `41f68c38-...`。 |
| Recovery Contract Integrity | 🔴 | claim、文件 lock、run-id、heartbeat、callback 与环境 URL 仍分散；共享 lock 被覆盖直接证明文件存在不能代表唯一执行。Issue `4411cb51-...`。 |
| Registry Freshness | ✅ | `/dev` 要求的 12 表上下文端点全部成功；API/db_schema/test registry 可读，任务 payload 含 23 条 line ledger。 |
| Schema Authority / Collision | 🔴 | main=412、production=414、staging=412；production 413/414 来自 CLOSED 未合并 #4851。OPEN #4860 又定义另一份 413，Preview ledger 占号后 controller 列缺失。Issue `8a782f87-...` 已更新。 |
| Schema Physical Parity | 🔴 | production 有 `tasks.retry_count`，staging 无；相同镜像与最大 migration 值仍不能证明物理 schema 等价。 |
| Schema Consumer Integrity | ⚠️ | production incident recurrence upsert 的真实 PostgreSQL conflict 缺陷仍由既有 Issue 跟踪，mock 测试不足以关闭。 |
| Dependency Exposure | ⚠️ | `npm audit --omit=dev`：58 moderate、2 high、0 critical；两个 high 仍来自 OpenTelemetry 链。 |
| Dev Workflow Portability | ⚠️ | 隔离 worktree 无依赖时 DoD mapping 与 Vitest不能启动；复用同平台确定版本依赖后门禁与 58 项测试才真实执行。Issue `5ff60dff-...`。 |
| Temp Patch Accumulation | ⚠️ | 根目录继续跟踪任务特定 `.brain-result.json` 与 `.task`，运行产物边界未归位。 |
| Ledger Freshness | 🔴 | MJ5 Journey 台账仍落后近期合并 PR 超过 25 天，多数 Journey 与交付缺少可机械证明的关联字段。Issue `5f3f0d07-...`。 |
| Ledger Schedule Window | ⚠️ | 当前任务有 23 条摘要，但固定 20:00 review 早于 21:00 dreaming 的结构空窗未改。Issue `a1ed9884-...`。 |
| Journey Smoke Registry | ⚠️ | production active Journey 23 / 声明 E2E 17；staging active 21 / E2E 0，既有缺失路径与 TBD 未收敛。Issue `925aa82e-...`。 |
| Deploy Config Drift | ⚠️ | production/staging 同 SHA；但 Preview 依赖 DB tick 状态而非环境零副作用合同，staging 的 hard-off 也不控制独立 loops。 |
| Deployment Closure | ⚠️ | Gate 3 与 Auto Staging 对 main@b4d41ad 成功，运行镜像同 SHA；scheduled Deploy `31665488146` 的 Release Gate 因 nightly 证据失败。 |
| Kernel CI Truth Compression | ⚠️ | 三条 nightly 连续红、Real-Machine 连续绿、main deploy 持续成功，release/部署成功语义仍分裂；Promote All workflow 无运行记录。 |
| Routing / Skill Integrity | ⚠️ | `/arch-review` 注册表项 active，但 `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 仍返回 404，能力加载缺少可观测证明。 |
| Realtime Voice Duplication | ⚠️ | 5 个物理 `useRealtimeVoice` 文件与至少两组实现仍未收敛。Issue `1daab7e4-...`。 |
| Janitor /dev Lock Contract | ⚠️ | `/dev` worktree、Janitor 扫描与任务锁路径仍缺跨模块行为合同。Issue `c00b0090-...`。 |
| Scheduler Test Collection | ⚠️ | 本轮 5 files / 58 tests 通过；完整 daily-review scheduler 测试仍不在标准收集面。Issue `0acbf8f3-...`。 |
| Guard Drill Idempotency | ⚠️ | staging 本次重启后再次创建 heartbeat-sentinel P1；持久 gate/guard_id upsert 未落地。Issue `9893e28b-...`。 |
| Nightly Test Truth | ⚠️ | Smoke E2E、Nightly Regression、Integration Nightly 最近 8 次全失败；Real-Machine 最近 6 次全成功。Issue `af7361b3-...`。 |

## 4B Structural Health Findings

- 环境隔离目前是多个局部开关的并集，而不是实例级不变量。`BRAIN_PREVIEW` 关闭两个 loops，`CECELIA_TICK_ENABLED=false` 不覆盖克隆 DB 的 enabled=true，`CECELIA_TICK_HARD_OFF` 又只控制主 Tick；任何单项绿色都不能证明零副作用。
- review 以 task UUID 命名共享文件锁，却不使用原子独占创建，也不把 environment/run-id 写进 DB。两个 Preview 对同一 UUID 的第二次写入覆盖第一 run-id，说明锁既不是互斥量，也不是权威身份。
- migration 权威不受合并状态约束。已关闭 PR 的 413/414 可以进入 production，开放 PR 又可复用 413；ledger 只按版本号去重，把“同号不同迁移”静默压成已应用。
- #4860 的 Session Controller 设计依赖新列，但其 Preview 已实证没有这些列。单测/PR 状态无法替代在目标 DB 上验证 migration 描述、hash 与物理对象。
- staging 主 Tick health 已能报告 stopped，却仍运行并写入独立 scheduler/promotion 副作用；health 仍只描述部分系统生命周期。
- 三条 nightly 全红、Real-Machine 全绿、Gate 3/Auto Staging 绿色、scheduled Release Gate 红色，质量、部署和发布没有共同的完成语义。
- 本轮 main 增加大量 Kernel 合同测试与 smoke，但核心文件规模仍上升，跨模块入口、迁移和运行身份的不变量没有被同一 E2E 覆盖。

## Severity

- 🔴 High: 11 项（文档正本、环境隔离、review 身份、verdict/callback、dev→harness 路由、Controller ownership、Tick 并发、时间类型、recovery 身份、schema authority/parity、MJ5 ledger）
- ⚠️ Medium: 18 项（边界、Map 遗留、复杂度、schema consumer、依赖、`/dev` 可移植性、根目录产物、ledger 时窗、Journey E2E、deploy 配置/闭环、Kernel CI、skill 可观测、语音复制、Janitor、scheduler 测试、guard-drill、nightly 真值）
- ✅ Clean: 9 组（production health/breaker、三项 DevGate、58 项定向回归、migration 412 合同、同 SHA 运行、Map 生产权威、registry/ledger 可读、Gate 3、Real-Machine Smoke）

## Required Actions

- [ ] **P0** — 将 Preview 变成实例级 hard-off：主 Tick、Nightly、scheduler、projection、promotion 与外部 executor 默认全部关闭；克隆过程终止所有可派发任务，或从源头不复制运行态任务/working_memory。
- [ ] **P0** — review 使用 DB 原子 claim + environment-scoped run-id/heartbeat/callback；文件锁使用独占创建且不得跨环境共享；callback 必须校验 HTTP 与 CAS `applied`，CRITICAL/未知裁决 fail-closed。
- [ ] **P0** — 建立 migration authority gate：production 只接受已合并 main 的编号/描述/hash；preview/test fail-closed 拒绝 production DATABASE_URL。保留已应用 413/414，#4860 改用新的 forward migration 415+。
- [ ] **P0** — canonical dev→harness 路由前移到 admission 前，统一 repo/base_repo resolver；改道任务必须经过 harness cap/initiative lock且不依赖 bridge。
- [ ] **P0** — Session Controller ownership 以未占用 migration 与真实 PostgreSQL lifecycle 回归落地；任何 active Kernel Run 必须有有效 controller lease，fatal 后有唯一恢复所有者。
- [ ] **P0** — 完成 tasks 全量 timestamptz 与 Tick cancel/join 或租约代际合同；schema parity gate 同时验证 ledger、物理对象与 consumer。
- [ ] **P1** — 统一环境生命周期 health、nightly/Real-Machine/Release/Deploy 成功语义；修复 incident recurrence、guard_id 去重与标准 scheduler 测试收集。
- [ ] **P1** — 更新 SYSTEM_MAP/DEFINITION、Journey ledger/E2E registry、`/arch-review` 能力 API 与 20:00/21:00 调度时序。
- [ ] **P2** — 收敛 Cortex 边界、God Module、OpenTelemetry 漏洞、语音复制、Map legacy 与根目录任务产物。

## Verification Evidence

- base/runtime：`origin/main=b4d41ad`，production/staging 均为 `1.272.36@b4d41ad`；production healthy，staging 主 Tick stopped。
- DevGate：facts-check、version sync、DoD mapping 全部 PASS；代码事实为 Brain `1.272.36`、schema floor/max 412。
- tests：清除父 Preview 继承的 `BRAIN_PREVIEW` 后，5 files / 58 tests 全通过；直接带父环境运行会令旧 scheduler 测试进入 Preview 分支，证明执行环境会改变测试语义。
- Preview：4860/4861 都打印 scheduler/projection Preview guard 生效，同时打印 DB enabled 导致主 Tick启动；health 都报告 scheduler running/enabled，并各自 claim task `60d93710`。
- review identity：production queued；preview-4860/4861 in_progress，claimed_by 分别为 `brain-tick-12390`/`brain-tick-11927`，run-id/heartbeat 均空；两组 Codex 存活，共享 lock 只保留第二 run-id。
- schema：production max=414/ledger rows=407，staging max=412/rows=402；production 413/414 来自 CLOSED #4851。preview-4860 max=414，但 #4860 所需两个 controller 列都不存在。
- DB parity：production 有 `tasks.retry_count`，staging 无；两库时区均 America/Chicago，tasks lifecycle 继续混用 timestamp/timestamptz。
- staging isolation：容器 hard-off=1，主 Tick health stopped；日志仍启动 Nightly、33 scheduler、3 projection、Promotion，并新建 guard-drill P1。
- CI：三条 nightly 最新 run `31635403491` / `31635487070` / `31642174681` 失败且最近 8 次全红；Real-Machine `31609046255` 成功且最近 6 次全绿；scheduled Deploy `31665488146` 的 Release Gate 失败。
- dependency/code：audit 58 moderate / 2 high / 0 critical；Brain 非测试 JS 639 文件/176005 行，最大文件 `executor.js` 4615 行。
- issue：更新 `a9a6e3f6-...`、`6adfa549-...`、`72546baf-...`、`8a782f87-...`；没有重复建单。

## ADR Needed? 是

需要定义五项跨模块合同：环境实例级零副作用、review 的 environment/task/run/callback 唯一身份、migration 合并权威与内容 hash、candidate admission 前 canonical work routing、以及 Controller lease/恢复唯一所有权。

## Escalate to /architect? 是

Preview/暂存生命周期、跨环境 review、migration authority、Session Controller、路由 admission、时间/Tick 与 release 真值跨越启动脚本、dispatcher、executor、scheduler、DB、CI 和多工作区；本巡检只记录事实与约束，不修改运行时代码。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 33,
  "health_findings": 8,
  "high_severity": 11,
  "medium_severity": 18,
  "issues_created": 0,
  "issues_updated": 4,
  "escalate_to_architect": true,
  "summary": "#4853 已关闭 Preview scheduler/projection 局部 loop，但两个 Preview 主 Tick仍并发执行同一克隆 review；production 已落 CLOSED #4851 的 schema 413/414 并与 OPEN #4860 的另一份 413 碰撞，controller 列真实缺失；review 身份/裁决、staging 副作用、时间/Tick 与 nightly/release 真值继续未闭合"
}
```

# Architecture Review — 2026-08-13 daily（16:00 UTC，16:21 UTC 执行）

## Executive Summary

裁决仍为 **CRITICAL**：11 项高风险、18 项中风险。当前 `origin/main`、production 与 staging 已对齐到 `1.273.3@813dc703`；production health 为 healthy、breaker 全 CLOSED。三项 Brain DevGate 全绿，9 个聚焦测试文件共 211 项测试通过，最新 main CI、Gate 3、Auto Staging、CodeQL、Smoke 与 Real-Machine Nightly 均成功。

04:00 巡检的两项阻断已有实质进展。#4866 在 Preview 的 tick-recovery 入口加入主 Tick 守卫；#4865 把 413/414 纳入 main 并把 Session Controller migration 前移到 415。production 与 staging 的 migration 最大版本都已到 415，Controller 两列也真实存在，原 Preview 主 Tick重复执行和 migration 同号碰撞已经解除。

但系统级合同尚未闭合。staging 设置 `CECELIA_TICK_HARD_OFF=1` 后仍持续运行 PromotionJob，health 又复用 production 的 Tick 统计；非生产实例仍不具备统一的零副作用生命周期。当前巡检任务已有存活 Codex 进程和带 run-id 的文件锁，DB 中 `current_run_id/current_run_status/driver_heartbeat_at` 却仍为空。04:00 的 CRITICAL 报告在 callback_queue 中被多次记成 PASS，真实证明 executor 的 verdict/callback 合同仍会制造错误终态和重复完成。

数据库从“编号碰撞”转为“相同最大版本、不同内容与物理形态”。production 的 413/414 ledger description 与 main/staging 不同，且 production 有 `tasks.retry_count`、staging 没有。Controller ownership 已落库，但 4 个 active Kernel Run 的 controller lease 都精确等于约 8 小时的 deadline；源码声明 30 分钟租约和 heartbeat 续租，却找不到生产续租写路径。基于代码与运行态的交叉证据，Controller Issue 已重新打开。

## 4A Drift Findings

| 检查项 | 状态 | 16:00 UTC 发现 |
|--------|------|----------------|
| Boundary Drift | ⚠️ | `decision-executor.js`、analytics route 到 Cortex 的跨层直连仍未形成机器可检的例外合同。 |
| Undocumented Changes | 🔴 | `SYSTEM_MAP.md` 仍把 Brain SSOT 写成 1.216.0；`DEFINITION.md` 顶部为 1.273.1，运行/包版本已为 1.273.3。 |
| Preview Main Tick | ✅ | #4866 的 Preview tick-recovery 守卫已进入 main，并有定向回归；04:00 的两个 Preview 主 Tick重复 claim 入口已关闭。 |
| Preview / Staging Lifecycle | 🔴 | staging hard-off 下仍持续运行 PromotionJob；health 的 `last_tick/stats` 与 production 完全相同，实例级零副作用仍不可证明。Issue `a9a6e3f6-...` 已更新。 |
| Review Execution Contract | 🔴 | 当前 task 已 claim、进程和文件 lock 均存活，lock 有 run-id；DB run-id/status/heartbeat 仍全空。Issue `6adfa549-...` 已更新。 |
| Review Verdict / Callback | 🔴 | executor 仍只解析 `PASS|FAIL`，exit 0 缺省 PASS，不验证 callback HTTP/CAS；04:00 CRITICAL 报告已有多条 PASS callback 实证。Issue `72546baf-...` 已更新。 |
| Dev → Harness Routing | 🔴 | canonical 改道、`repo/base_repo` 身份与 admission/bridge 的顺序仍由既有 P0 `e0370d57-...` 跟踪，未见关闭证据。 |
| Session Controller Ownership | 🔴 | active Kernel Run 都有 controller id，但 lease=deadline，代码注释/默认值/运行值/续租写路径四账不一。Issue `962d399c-...` 已重新打开。 |
| Frozen Contract Artifacts | ✅ | 冻结、重封、顺序归一、GP identity 与 run store 的新增合同在 211 项聚焦测试中通过。 |
| Universal Map Authority | ✅ | manifest/projection/read 继续集中在 `src/lib/*`；本轮变更没有新增生产写源。 |
| Universal Map Legacy | ⚠️ | `src/map/*` legacy 边界仍未形成 archive 或 production-reference 合同。 |
| Core Module Complexity | ⚠️ | Brain 非测试 JS 640 文件/176330 行；`executor.js` 4615L、`execution.js` 4099L、`ops.js` 3024L。 |
| Tick Concurrency | 🔴 | 现有 tick-loop 测试仍认可 65 秒后强制释放共享锁，旧 `doTick()` 没有 cancel/join 或代际隔离。Issue `9aa89588-...`。 |
| Temporal Integrity | 🔴 | lifecycle 字段继续混用 timestamp/timestamptz；当前 task 的 started/claimed 仍相差约 13 小时。Issue `41f68c38-...` 已更新。 |
| Recovery Identity | 🔴 | claim、文件 lock、run-id、heartbeat、callback 与 executor kind 仍分散，当前任务再次实证文件身份没有写回 DB。 |
| Registry Freshness | ✅ | `/dev` 要求的 12 表上下文均可读；任务携带 23 条近 20 小时 line ledger。 |
| Schema Number Authority | ✅ | #4865 后 main/production/staging 的最大 migration 均为 415，413 同号碰撞已用 forward 迁移解除。 |
| Schema Content / Physical Parity | 🔴 | production 413/414 描述与 main/staging 分叉；production 有 `retry_count`、staging 无。Issue `8a782f87-...` 已更新。 |
| Schema Consumer Integrity | ⚠️ | production 日志仍重复出现 incident reporter 的 `recurrence_count is ambiguous`，真实 PostgreSQL consumer 缺陷未关闭。 |
| Dependency Exposure | ⚠️ | `npm audit --omit=dev` 为 54 moderate、2 high、0 critical。 |
| Dev Workflow Portability | ⚠️ | BusyBox `flock` 不支持 `-w`；干净 worktree 又暴露 js-yaml、Rollup musl 与 esbuild 版本不匹配。Issue `5ff60dff-...` 已更新。 |
| Temp Patch Accumulation | ⚠️ | 根目录任务结果/锁类运行产物边界仍未统一归位。 |
| Ledger Freshness | 🔴 | MJ5 Journey 仍停在 2026-07-17 且无 E2E，已落后超过 27 天。Issue `5f3f0d07-...` 已更新。 |
| Ledger Schedule Window | ⚠️ | 20:00 review 早于 21:00 dreaming 的固定空窗未改变。Issue `a1ed9884-...`。 |
| Journey Smoke Registry | ⚠️ | production active Journey 23 / E2E 17；staging active 21 / E2E 0。Issue `925aa82e-...`。 |
| Deployment Runtime | ✅ | production/staging 同为 `1.273.3@813dc703` 且容器 healthy，真实运行 SHA 与最新 main 对齐。 |
| Preview Deployment Closure | ⚠️ | PR #4873 核心检查成功并合并，但 Preview Deploy 在“触发 preview-env-start”失败；Preview 健康不在合并闭环内。 |
| Kernel CI Truth Compression | ⚠️ | main CI/Gate 3/Auto Staging 与 Real-Machine 绿色；三条 nightly 最近 10 次全红，Daily Drill 最近 10 次全红，Promote All 无运行。 |
| Routing / Skill Integrity | ⚠️ | `/api/brain/skills/count` 与 `/api/brain/skills/arch-review` 仍为 404，巡检能力缺运行可观测入口。 |
| Realtime Voice Duplication | ⚠️ | 多份 `useRealtimeVoice` 实现未收敛。Issue `1daab7e4-...`。 |
| Janitor /dev Lock Contract | ⚠️ | `/dev` worktree、Janitor 与任务锁仍缺跨模块行为合同。Issue `c00b0090-...`。 |
| Scheduler Test Collection | ⚠️ | 9 files / 211 tests 通过；`daily-review-scheduler.test.js` 仍被标准 Vitest 配置排除。Issue `0acbf8f3-...`。 |
| Guard Drill Idempotency | ⚠️ | production/staging 滚动 24h 各有 14 条 guard-drill Issue，持久幂等键仍缺失。Issue `9893e28b-...` 已更新。 |
| Nightly Test Truth | ⚠️ | Smoke E2E、Regression、Integration Nightly 最近 10 次全失败；Real-Machine 最近 7 次全成功。Issue `af7361b3-...`。 |

## 4B Structural Health Findings

- 环境守卫仍是局部开关集合。Preview 主 Tick已关闭，但 staging hard-off 不覆盖 Promotion，health 又不能区分本实例统计与共享 DB 统计。
- review 的权威身份仍不在单一事务中。当前 run-id 只存在于文件锁，DB 无 heartbeat；历史 callback 又证明终态可重复且可把 CRITICAL 降成 PASS。
- migration 的最大版本已经对齐，但 ledger 没有内容 hash，物理 parity 也不属于启动门禁；同为 415 不能证明两库等价。
- Session Controller 的列与初始 ownership 已落地，但租约值、默认时长、heartbeat 语义与实际 UPDATE 路径没有共同合同，因此 fatal 后唯一恢复所有者仍不可机械证明。
- 主干合并与线上部署链绿色，但 Preview、三条 nightly、Daily Drill 与 Promote All 仍给出互相矛盾或空缺的发布信号。
- `/dev` 在当前机器上需要绕过脚本的 BusyBox `flock` 假设，并人工拼接精确依赖树；开发门禁自身仍不是可复现运行单元。

## Severity

- 🔴 High: 11 项（文档正本、环境生命周期、review 身份、verdict/callback、dev→harness 路由、Controller lease、Tick 并发、时间类型、recovery 身份、schema content/physical parity、MJ5 ledger）
- ⚠️ Medium: 18 项（边界、Map 遗留、复杂度、schema consumer、依赖、`/dev` 可移植性、根目录产物、ledger 时窗、Journey E2E、Preview deploy、Kernel CI、skill 可观测、语音复制、Janitor、scheduler 收集、guard-drill、nightly 真值、环境 health 语义）
- ✅ Clean: 10 组（production health/breaker、三项 DevGate、211 项回归、Preview 主 Tick守卫、migration 编号收敛、Controller 物理列、同 SHA 运行、Map 生产权威、registry/ledger 可读、main/Real-Machine CI）

## Required Actions

- [ ] **P0** — 用实例级 lifecycle mode 统一关闭非生产主 Tick、Nightly、scheduler、projection、promotion 与外部 executor；health 必须按实例报告 loop 状态和统计来源。
- [ ] **P0** — review 使用 DB 原子 claim + environment-scoped run-id/heartbeat/callback；CRITICAL/未知裁决 fail-closed，并验证 callback HTTP 与 CAS `applied`。
- [ ] **P0** — migration ledger 增加内容 hash/合并来源，启动 parity gate 同时验证 version、description/hash、物理对象与 consumer；补齐 staging `retry_count`。
- [ ] **P0** — Controller heartbeat 事务续租并记录 lease generation；active run 的 lease、deadline、默认 TTL 与 orphan recovery 必须有真实 PostgreSQL lifecycle 回归。
- [ ] **P0** — canonical dev→harness 路由前移到 admission 前，统一 `repo/base_repo`；完成 tasks 全量 timestamptz 与 Tick cancel/join/代际合同。
- [ ] **P1** — 修复 incident recurrence、guard_id 去重、scheduler 标准收集和 `/dev` BusyBox/依赖自举；统一 nightly/Preview/Release/Deploy 的成功语义。
- [ ] **P1** — 更新 SYSTEM_MAP/DEFINITION、MJ5/Journey E2E 与 `/arch-review` 能力 API。
- [ ] **P2** — 收敛 Cortex 边界、God Module、OpenTelemetry 漏洞、语音复制、Map legacy 与根目录运行产物。

## Verification Evidence

- base/runtime：`origin/main=813dc703`；production/staging 均为 `1.273.3@813dc703`，容器 healthy；production breaker 全 CLOSED。
- DevGate：facts-check、version sync、DoD mapping 全 PASS；facts 为 Brain 1.273.3、schema max 415。
- tests：tick recovery/loop、review executor/preflight/liveness、Controller lifecycle、run store、dispatcher 共 9 files / 211 tests PASS；daily-review scheduler 独立执行为 no test files found。
- review identity：task `9797c817-...` 已 claim，Codex 进程存活，文件 lock 记录 run `9ffd665a-...`；DB current_run_id/status/heartbeat 为空。
- callback：04:00 task 的 callback_queue 有多条 processed AI Done，报告写 CRITICAL 而 result_json 均为 PASS，任务经重复 callback 才完成。
- schema：两库 max=415；production 413/414 description 与 main/staging 不同；production 有 `retry_count`、staging 无；两库都有 Controller 两列。
- Controller：production 4 个 active kernel_v2 run 都有 controller id，lease 全部精确等于 deadline；生产代码只发现创建与 orphan scan，未发现续租 UPDATE。
- staging：`CECELIA_TICK_HARD_OFF=1`，health 仍显示 scheduler running/enabled 且复用 production Tick 统计；日志持续执行 PromotionJob。
- CI：最新 main CI `31696303563`、Gate 3 `31696303666`、Auto Staging `31696303604` 成功；Real-Machine `31712224814` 成功。三条 nightly 最近 10 次全红，Daily Drill 最近 10 次全红；#4873 Preview Deploy `31695374732` 失败。
- dependency/code：audit 54 moderate / 2 high / 0 critical；Brain 非测试 JS 640 文件/176330 行，最大文件 `executor.js` 4615 行。
- issues：更新 9 个既有 Issue，重新打开 `962d399c-...`，没有重复建单。

## ADR Needed? 是

需要定义环境实例级生命周期、review 的 environment/task/run/callback 唯一身份、migration 内容权威与物理 parity、Controller lease generation/恢复所有权，以及跨 CI/部署/发布的统一完成语义。

## Escalate to /architect? 是

这些问题跨启动脚本、executor、scheduler、dispatcher、DB migration、Kernel lifecycle、CI 与多环境。本轮按 arch-review 职责只记录事实、更新 Issues 与报告，不直接修改 runtime。

## Machine-Readable Verdict

```json
{
  "verdict": "CRITICAL",
  "drift_findings": 33,
  "health_findings": 6,
  "high_severity": 11,
  "medium_severity": 18,
  "issues_created": 0,
  "issues_updated": 9,
  "escalate_to_architect": true,
  "summary": "#4866 与 #4865 已关闭 Preview 主 Tick重复执行和 migration 同号碰撞；但 staging hard-off 仍运行 Promotion，arch_review 的文件 run-id 未写入 DB 且历史 CRITICAL 被回调为 PASS，schema 内容/物理 parity 与 Controller lease 续租仍未形成可验证合同"
}
```
