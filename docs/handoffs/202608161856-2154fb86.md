# Handoff：fix(deploy): .env.docker 为 KERNEL_FLEET_BRIDGE_TOKEN 唯一权威 + fleet transport 启动自检/health（第 8 类死法，Brain 1.273.62）

- verdict: PASS
- task_id: 2154fb86-3214-4ee9-ad28-0a6303e79808
- created_at: 2026-08-16T10:56:02.000Z

## 完成
- 根因：docker-compose.yml environment 用 ${KERNEL_FLEET_BRIDGE_TOKEN:-} 覆盖 env_file；09:38Z 另一会话改 .env.docker(NOTION_INBOX_*) 后不带 --env-file 重建 Brain → token 空 → transport fail-closed → run 48d57838 lease 过期、dbe7ca64 launch 即死
- 删覆盖行；brain-docker-up.sh/brain-rollback.sh 补 --env-file；describeFleetTransportReadiness + 启动红日志 + /health fleet_transport（enabled&unavailable→degraded，不回显密钥）
- 守卫：scripts/smoke/brain-compose-envfile-authority-smoke.sh + vitest 7 段 + production-compose 契约改为禁止覆盖行；PR #4915 合并
- 现场：docker compose --env-file .env.docker up -d node-brain 重建，token_len=64 恢复

## 没完成
- staging Brain 无 token（未启用 remote → disabled，不降级）

## 下一步
- Gate3 部署 1.273.62 后确认 /health.fleet_transport.status=ready
- 有头 /dev 任务被 harness-watchdog 20 分钟无 run 判 failed（本次两任务均被误杀，psql 纠正）——有头签发口保留与否待 Alex 裁

## 数据源
- docker-compose.yml node-brain environment/env_file
- packages/brain/src/orchestrator/production-transport.js describeFleetTransportReadiness
- /api/brain/health.fleet_transport

## 决策引用
- 1f846cd9-a565-498f-8791-accf8d07e33d

## 产物指针
- PR: https://github.com/perfectuser21/cecelia/pull/4915
- branch: cp-08161830-compose-bridge-token-envfile
