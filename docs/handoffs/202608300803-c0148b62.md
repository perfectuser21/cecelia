# Handoff：fix(kernel): 第46批——静默等待活锁三修（替换提案游标对齐/人审去重只认未裁决/停摆心跳行+事件唤醒 Commander） — F1 step3 (Brain 1.273.149)

- verdict: PASS　task: c0148b62-8b33-47ef-912f-c3607e39476c　journey: e6f803f2-8c48-4cce-a7a1-5b1bda5e9c29
- created_at: 2026-08-30T00:03:38.000Z

## 完成
- r80（run 5100560e）翻案：不是控制循环半死，是静默等待活锁——协调器 stale 替换提案 bundle 游标滞后被静默丢弃 + 人审去重认了已裁决旧请求 + 静默等待无上限；r83 hop 63-66 实时复现同一发生器（替换 bundle 96 vs 状态 102）
- commander-bundle：buildCommanderBundle 显式 eventCursor（只许向前，倒拨抛 commander_bundle_cursor_behind_events）
- commander-coordinator：stale 替换派发传 eventCursor=nextCursor 对齐状态游标，滞后场景 wakeup 无 wake 也强制派替换；'状态游标>bundle=已消费' 语义不动（once 语义由既有单测锁死）
- loop：人审去重只认未裁决请求（humanReviewRequestDecided）；新增 silentWait() 覆盖人审等待/wait:running/Commander 在途/过期 attempt 收养四个静默点，≥15min 无新行 → result:wait_stalled 行 + run.wait_stalled 事件 + P1 告警
- commander-wakeup：run.wait_stalled 计入 material（Commander 被叫醒会诊）；run.js 递 runEventStore 给 loop；新模块 silent-wait.js + 配套单测
- GP 步骤断言 tests/gp/f1/step3-silent-wait-livelock.test.js 10 用例 RED→GREEN；orchestrator 全量 1750 tests 全绿；DevGate 三闸 + lint-gp-anchor + lint-test-pairing 全过
- zenithjoy-skills #190 已合并：监工 SKILL 停摆扫描改按 result:wait_stalled 行对号处置
- #5090 已合并（cbe4c9deb），Gate 3 部署生产 Brain 1.273.149 确认（/api/brain/health）

## 没完成
- Phase C：Commander 新动作 extend_deadline / reopen_contract（需 commander-contract + entrypoint codex schema 枚举同步 + 镜像 repin）
- Commander 被拒仲裁 / 幽灵回执 crossCheckMismatch 拦截 / failed_targets TTL 分级 / S6.5 manifest 409 噪音
- 任务泵（产线空转自动取 backlog 点火）
- 统一凭据 keeper 二期（codex 4 账号 + Grok 纳入守护、Brain 凭据台账 + Bark）
- 生产实证：需一个 run 在静默等待 ≥15min 后出现 result:wait_stalled 行且 Commander 被唤醒（部署后观察）
- r83（run 32873c79）人肉 rebase 后 20 秒被 deadline 判死：人审冻结只认旧 head + 新控制器进程未走 Commander 会诊即 failRun——新缺陷，待批次修

## 下一步
- 47 批 PR #5093（r83 derive 修复收编，1.273.150）合并部署
- 部署 1.273.149 后盯第一条 result:wait_stalled 行与随后的 spawn:commander（reason 含 wait_stalled）
- 若 Commander 对 wait_stalled 会诊反复 continue_default，考虑把 waiting_on 作为 defaultDecision 上下文喂进 bundle observed（当前只在事件 payload）

## 数据源
- packages/brain/src/orchestrator/silent-wait.js
- packages/brain/src/orchestrator/loop.js（silentWait / humanReviewRequestDecided）
- packages/brain/src/orchestrator/commander-coordinator.js（stale 替换派发段）
- orchestrator_decision_log.action='result:wait_stalled'
- harness_run_events.event_type='run.wait_stalled'
- memory validation-clock-delivered-r71.md 08-30 追记

## 决策引用
- 08-29 分权翻转（Commander 判死权归位）

## 产物指针
- https://github.com/perfectuser21/cecelia/pull/5090
- https://github.com/perfectuser21/zenithjoy-skills/pull/190
- branch: cp-0830000357-batch46-stall-livelock
