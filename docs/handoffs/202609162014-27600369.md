# Handoff：飞书群交办入账——四轮迭代收官（源头改道 → 去 LLM → 证据窗口 → 四类根因）

- verdict: **PASS**
- task_id: `27600369-aa08-4738-b045-22b6b06216b6`
- PRs: #5363, #5366, #5368, #5370
- created_at: 2026-09-16T12:14:10.000Z

## 完成了什么

- 【已上生产并跑通】Brain 1.298.2，scheduler job feishu-task-ledger 每 60min 自 gate；最后一轮 E2E：扫 921 条群消息 → 入账 9 条 → 9/9 已推 Notion，零错误，20 秒，全程零 LLM 零 API 费用
- 四轮迭代，每轮都由真实数据推翻上一轮假设：①前序交接单『从 OpenClaw task_runs 搬运』作废——实勘证明 OpenClaw 不持久化群消息原文（ingress payload 完成后清空、transcript 表 0 行、飞书群在 task_runs 只留 13 行 CLI 噪音），唯一可信源是飞书开放平台 API（PR #5363）
- ②去 LLM 化（PR #5366）：判据2『秋米有没有真派 agent 去干』是机械事实，OpenClaw task_runs 已记录，不需要 LLM 猜意图。原实现在 us-vps 遇 Anthropic 欠费 + MiniMax 超额即整条腿卡死。同 PR 修阻断性 bug——飞书历史消息 API 的 mentions[].id 是扁平字符串而非 {open_id}，模块只认后者导致悦升云端群候选恒空、一条都入不了账
- ③证据覆盖窗口（PR #5368）：回溯 14 天但 task_runs 只保 7 天，早于证据范围的消息仍硬判会把 09-03~09-08 全误判成 dropped 灌进账本。新增 resolveEvidenceFloor，窗外判 unknown 不入账；同 PR 修 blocked 未带 blocked_at 撞 chk_blocked_at_not_null 导致 created 恒为 0
- ④四类根因（PR #5370）：主理人追问『为什么没做』把判据推到正确位置——秋米每条都回了，回复里就写着原因。改为看机器回复的固定套话分 fault/waiting/done/answer，零 LLM。同 PR 修回复串台（按 30min 窗口取回复会把 17 分钟后另一件事的 401 回复算到本条头上，导致『表在哪』被误判成系统故障）与 title 残留 @_user_N 占位符
- 判据三段（主理人拍板）：①@ 对象必须是秋米 open_id 而非人（实测 14 天 232 条带 @ 消息里只有 81 条 @秋米）②30min 窗口重发去重（同一任务因无响应被重发 3 次）③按秋米回复分根因
- 账目自带『卡在哪』证据：fault/waiting 类附 bot_reply_excerpt。实测最有价值的两条——「帮我做6个朋友圈AI员工的skill」→ waiting + 秋米原话「先把两份资料发来」；「抖音读昵称沉淀成 Skill」→ fault + 「HTTP 401 invalid api key」「还没能连接到可用工作手机」
- Brain 镜像加 sqlite CLI 只读查 OpenClaw 库（容器 Node 20 无 node:sqlite，22.5+ 才内置）；秋米凭据从 us-vps clawdbot.json 明文收编进 1Password CS『Feishu-Qiumi-Bot』，双写 ~/.credentials/feishu-qiumi.env(600)，注入 .env.docker
- 61 单测 + smoke 16 项断言，其中 4 道闸 proven-to-fire（禁 queued / 证据窗口 / blocked_at / 回复串台 / 故障优先级，各自注入缺陷报红后还原）

## 没完成 / 范围外

- 边界噪音未清干净：最后一轮 9 条入账里约 3 条属边界误判——「你能打开这个链接不」判 done、两条终端报错粘贴因秋米回『provider internal error』判 fault。精度够用但可再收紧
- 首轮 E2E 产生的 11 条试跑数据只能作废不能删除（work_routing_receipts 是 append-only 账本，外键挡住物理删除），已 status=cancelled + payload.voided=true 标注
- OpenClaw automation_run 机器执行流水入账（1015 行/7天）属二期，决策 2dbabb48，本单未做
- VPS状态群 / 外部Ai体验区群未实测（数据量小，悦升云端是唯一有实质交办的群）

## 下一步

- 观察 1-2 天真实运行：scheduler 每 60min 自动跑，看 Notion Tasks 库里 fault/waiting 两类是否真能帮主理人发现卡点
- 边界收紧：把「你能打开X吗」这类疑问句、终端报错粘贴排除掉（可加最短长度 + 疑问句尾判据，仍零 LLM）
- 【本单外·基础设施】Tailscale OAuth key 已失效（invalid key: API key kSicJxxa7221CNTRL not valid），所有 PR 的 Deploy Preview Environment 全红（#5363/5366/5368/5370 同症），非 required 不挡合并但预览环境全挂，需换 key
- 【本单外·钱】Anthropic API 余额不足 + MiniMax Token Plan 超额，us-vps 上只剩 OpenAI 通道可用（gpt-4o-mini 实测可用）。本单已去 LLM 化不受影响，但其他依赖 callLLM 的 Brain 组件会受影响
- 【本单外·已复发4次】Brain watchdog 把正常工作中的有头任务误判为僵尸：清 claimed_by + 标 failed，且 failed 是终端态无法经 API 回正只能直写 DB。本会话 4 个任务全中（abcbc09f/2091c21b/9e949390/27600369）。建议把有头豁免窗口从 40min 放宽，或改用 .cecelia/lights 活性信号判活
- 【本单外·需主理人】飞书话术表 A1/A2/B 启用状态全为停用（09-15 12:02 起），触达线空转、守卫每轮告警

## 数据源

- packages/brain/src/feishu-task-ledger.js（模块本体，464 行）
- packages/brain/scripts/smoke/feishu-task-ledger-smoke.sh（16 项断言，4 道 proven-to-fire）
- docs/superpowers/specs/2026-09-16-feishu-task-ledger-design.md（首轮设计与实勘证据）
- 1Password CS vault / Feishu-Qiumi-Bot（秋米 app 凭据唯一源）
- 飞书群：悦升云端 oc_ee3fe04cf2541c4187f0fc054ae826de（唯一有实质交办）/ VPS状态 oc_ef60d6e3f199d90dd695b6ecc213d662 / 外部Ai体验区 oc_e5ff09de4c2e30a332df0d3cf87f41ae
- us-vps /opt/openclaw/state/state/openclaw.sqlite（只读；task_runs 只保 7 天，且不含群消息原文）
- 镜像回滚梯：cecelia-brain:cur21/cur22 仍在 us-vps，当前 latest=cur23

## 决策引用

`1c6679cd`, `398d5f36`, `2dbabb48`, `95477a66`
