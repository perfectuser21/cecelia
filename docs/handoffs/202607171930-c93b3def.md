# Handoff：MJ5 承诺地图首刀·刀1 账本落库（349 和解补齐 + 350 两域 seed + 落账 API）

> task_id: c93b3def-af70-4a4a-b490-76e9e5ba7c59｜verdict: **PASS**｜2026-07-17 19:30 CST
> PR: https://github.com/perfectuser21/cecelia/pull/4048（merged，Gate3 部署 1.265.0 生产验火 PASS）

## 完成

- **刀1 全量上生产**：schema 349/350 落库、智能客服域 7 journeys（5 GP+家②+域锚）、119 格子行、GP-B 四步承诺逐字入账、blast-radius API 实弹（CRM 表底座=4 步，与全景图"四处红"一致）
- **migration 349 和解补齐**：在容器 thin 版 348 之上补 journeys.domain / cell_key / cell_kind+cell_key CHECK / 删旧 UNIQUE 换双 partial unique（一步多格子）/ feature_id FK 改 ON DELETE SET NULL / backbone_version+softness 归一 NOT NULL
- **migration 350 两域 seed**：固定 UUID 幂等+空库自足；cell 行 notion_synced_at=NOW() 不推 Notion；**取代并删除手动脚本 seed-promise-map-domains.mjs**（账本写入=流水线副作用铁律）
- **落账 API**：POST /journey_step_links 双通道 upsert + journey_id 一致性校验 + base_ref 强制 feature_id；GET 默认排除格子行（cells=1 反转）；PATCH /journeys/:id（home/domain/trigger/endpoint）；GET /journey_features/:id/blast-radius；保留容器版 PATCH step_links 回写与 /features/:id/blast-radius
- 判定点①②③④写库：c6d6d0bc / 1d1cc811 / 705cd806 / 979951a5
- CI 三只拦路虎：补 promise-map-ledger-smoke.sh；修 #4038 埋的 cockpit-route 测试 cwd 雷（repo-root 锚定）；修 softness NOT NULL 后 POST 显式 NULL 炸 real-env-smoke
- 生产事故两起闭环：审查代理误打 347 进生产（单事务补偿回滚，零面客损伤，P1 4df6b2c1）；容器 4 波抢跑重复实现（杀容器+三次抢回 claim，P1 9c7ae233 / 8a48e037）

## 没完成（范围外/遗留）

- 刀2 锚点闸：容器版已 merged（#4039）并上生产（零误杀已确认），但**未按 PRD proven-to-fire 验火**
- 刀3 联动清单 + 刀4 nightly：在 PR #4042（S4 #4043 叠在 S3 分支上），基于旧 thin schema 写成，未对齐 cell_key/双通道语义，未合并
- CodeQL 3 个 rate-limiting 告警（内部 localhost API，非必需 check，与存量路由同风格）
- db-update / mapper skill 补新表字段写法（zenithjoy-skills 仓库）；PATCH /journeys 不重置 notion_synced_at（mapper PR 跟进）；blast-radius 双端点并存待收敛

## 下一步

1. **刀2 验火**：故意造无锚新 dev 任务，亲眼看 missing_anchor terminal failed（proven-to-fire ①）
2. **处置 #4042**：rebase + 对齐 cell_key/双通道/枚举语义后再合（cascade-list/nightly 查询需适配 349），或关闭重做刀3/刀4
3. **抢跑病根治三案**：hold_for 零实现（8a48e037）/ migrate 裸跑打生产（4df6b2c1）/ completed_no_pr 洗有头 claim（9c7ae233，本次被反复洗 claim 的直接根因）
4. worktree 收割器本 session 两次犯病（累计第 6/7 次，含未 push 状态清元数据）——根治任务已在队列，修复 SOP 见 learning

## 数据源

- docs/prd/2026-07-17-mj5-promise-map-first-cut.prd.md
- docs/superpowers/specs/2026-07-17-mj5-knife1-ledger-design.md（含编号勘误注）
- packages/brain/migrations/349_promise_map_reconcile.sql / 350_seed_promise_map_two_domains.sql
- docs/learnings/cp-07171620-mj5-knife1-ledger.md
- memory `gp-doctrine-20260717`

## 决策引用

c6d6d0bc（S3 联动强度 thin）/ 1d1cc811（豁免白名单窄）/ 705cd806（格子扩展禁平行表）/ 979951a5（存量豁免有截止日）/ 5b0690ca / dc18d43d

## 产物指针

- PR #4048（branch cp-07171845-mj5-knife1-rebase）
- 生产：brain 1.265.0，schema 350，blast-radius API 可查
