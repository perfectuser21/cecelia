# Handoff：conversation-capture 修复：排除无头会话目录 + 每 session 一份 capture

- task_id: 60dc221a-511f-4bca-9361-35a75efe8842
- initiative_id: N/A
- journey_id: N/A
- verdict: PASS
- created_at: 2026-07-22T07:31:00.000Z

## 完成了什么
- 根因诊断：captures 表 16055 条 session_summary 里 99.5%（15970 条）摘的是 Brain 无头 LLM 调用会话（-private-tmp-cecelia-llm 目录），非 Alex 交互对话；摘要器调 Haiku 又生成新无头会话被再捕获，形成自我喂养循环
- extractClaudeSessions 排除 -private-tmp- 前缀目录，掐源头+终止自我喂养
- sessionDedupeKey 改绑 sessionId（去掉 lastEntryId 后缀），复聊后更新已有 capture 而非产生新行
- pushCapture 去掉多余存在性预检查（原逻辑会在 ON CONFLICT DO UPDATE 执行前短路返回），改为始终走 INSERT...ON CONFLICT DO UPDATE SET content=EXCLUDED.content
- TDD：3 个 failing test 先行（extractClaudeSessions 目录过滤 / sessionDedupeKey 相同 key / 集成测试复聊更新），修复后变绿
- 更新 conversation-capture-multitool-smoke.sh 中断言旧（有 bug）设计的静态检查，改为断言新行为
- brain package 版本 bump 1.267.34，四处版本文件同步（package.json/package-lock.json/.brain-versions/DEFINITION.md），顺带补齐此前已存在的版本漂移（存量 30→34）
- PR #4188 CI 全绿并 auto-merge 合并到 main

## 没完成什么
- 存量清理未做：repo LIKE '-private-tmp-%' 的历史行仍在 captures 表里（约 7 万行中的绝大部分），需要备份后 DELETE，PrepPRD 验收标准里列为 PR 合并后的手动步骤

## 下一步建议
- 跑一次存量清理：SELECT/COPY 备份 repo LIKE '-private-tmp-%' 的 captures 行到文件，然后 DELETE，汇报清理前后行数（PrepPRD 验收标准最后一项）

## 数据源（下一个大脑要加载的）
- packages/brain/src/conversation-capture.js
- packages/brain/src/conversation-capture-claude.js
- packages/brain/src/capture-inbox.js
- packages/brain/scripts/smoke/conversation-capture-multitool-smoke.sh
- memory: project_captures_inbox_no_consumer.md（根因调查全过程）

## 关键决策引用
- e145fadc-2d23-40c6-a0f4-b9cc29c5872d

## 产物指针
- https://github.com/perfectuser21/cecelia/pull/4188
- sprint_dir: sprints/07221443-conversation-capture-noise-fix
- branch: cp-0722144648-conversation-capture-noise-fix
