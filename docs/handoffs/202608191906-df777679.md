# Handoff — df777679 · capability-change-v1 合同尺度走 GAN

- PR #4967 / Brain 1.273.93 merged（2026-08-19 19:06）
- 决策 b14dc8e4 第 1 件。改能力线不再 rn>=1 自动 force_approve；走 deriveGan，reviewer 恢复上场。
- 热循环防护：deriveGan F1 守卫（identity-policy 驳回当前 SHA → REVISION → 趋势闸让路 → proposer）。
- 验证要点：下一条 capability_change run 应出现 `spawn:reviewer` + 真实 `verdict:reviewer`（非 `force_approved`）。
- 下一步：b14dc8e4 第 2 件（Bug/参数按有面无面分验）、第 3 件（三镜头接 kernel）。
