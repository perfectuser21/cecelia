# 秋米任务路由 PR3 —— 接力单（任务 9b32b2c5）

三刀全部合并进 main。**合并 ≠ 放开**：`QIUMI_DISPATCH_ENABLED` 缺省 `false`，
入账期照写 `headed_manual=true`，第一道闸还在。放开是主理人拍板后的独立动作。

| PR | 内容 | squash 进 main |
| --- | --- | --- |
| [#5505](https://github.com/perfectuser21/cecelia/pull/5505) | PR3 实施计划（含补充一~五） | `7b11b137d` |
| [#5504](https://github.com/perfectuser21/cecelia/pull/5504) | PR3-甲 路由判定 | `4fd0fda5e` |
| [#5508](https://github.com/perfectuser21/cecelia/pull/5508) | PR3-乙 接线与执行 | `8180771a8` |

main 上的 Brain 版本落到 1.314.1。

## done

- **判定层六个模块**（甲）：`routing/{env,redact,jev-client,cheap-gates,qiumi-router,device-delegation}.js`。
  Jev 六问 schema、发问前打码、便宜闸三源（`device_locks` phones / `ops_agents` / `ops_workflows`）
  且优先于 Jev、设备 fail-closed、账号仅池内、三分支持久化加 `task_events` 留痕。
- **接线与执行**（乙）：dispatcher 专用出口（接线点在原子 claim 之后、标 `in_progress` 之前）、
  `openclaw-agent` 执行体（prompt 走 stdin、`.log`/`.exit`/`.pid` 三件套）、60s 收割器、
  设备子任务对账器、第二道闸放开加免锚、入账期 `headed_manual` 改由开关门控、
  `PUSH_QIUMI_QUERY` 收窄到 `qiumi_task`。
- **切换运维**：幂等四步切换脚本（生产确认闸、在途清零、顺序闸）、在途检查器、切换与回滚 runbook。
- **真库 smoke 两支**：`qiumi-routing-smoke`（判定三闸，29 项断言）、
  `qiumi-dispatch-smoke`（收割与推送收窄三闸，10 项断言）。各带变异点，都验过由绿转红。

## not_done

- `QIUMI_DISPATCH_ENABLED` 与 `QIUMI_SYNC_ENABLED` 都没开，生产一条秋米任务都还没真派过。
- 影子跑没跑过，切换脚本只在守卫测试与本机 smoke 里跑过，没在生产执行机上真跑。
- `Deploy Preview Environment` 三刀全程红：Brain preview API 连吃 12 次 503。
  与本组改动无关，非必需闸，但它一直红着。

## next_steps

1. **按 `docs/runbooks/qiumi-cutover.md` 做影子跑**。正确开法是先 `--step=1` 停旧 cron、
   `--step=2` 等在途清零，再开 `QIUMI_SYNC_ENABLED`；`--step=3` 会回 us-vps 验一遍旧 cron
   是否真退役，没退役直接退 6。别直接跑 `--step=3`，那一步会把 `QIUMI_DISPATCH_ENABLED`
   也开成 true。切换凭据在执行机本地 `~/.cache/qiumi-cutover`，step1 与 step3 必须同机执行。
2. **迁移 462/464 上机前先查孤儿**。这两支是 `VALIDATE CONSTRAINT`，库里若已有不合规的
   `task_type` / `executor_kind` 行，VALIDATE 会直接失败。上机前先查一遍再跑。
3. **债包 b4f394e8**：对账器与收割器全库扫、无实例标识；子 `device_job` 一次 `failed`
   父即终态无重试；`qiumi_routed_device` / `qiumi_route_failed` 计入派发成功率看板
   （不触熔断）待做 reason 白名单；免锚使秋米任务不进锚点覆盖率统计。

## 这一程里值得记的两件事

- **PR3 原始分支的基线比 PR2 终版旧六个提交**，整体复制文件会把 PR2 的后续修复抹掉。
  必须按「base = PR3 原基线 / ours = 目标基底 / theirs = PR3 头」做三方合并。
- **仓库的 fetch refspec 只收 main**，cp-* 分支没有远端跟踪引用，裸用 `--force-with-lease`
  会以 `stale info` 被拒且并没有真的在比对。要先用显式 refspec 补建跟踪引用，
  再带显式期望值推。
