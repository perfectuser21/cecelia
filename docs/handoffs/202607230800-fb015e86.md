# Handoff：fix(deploy): bluegreen TEMP_PORT 默认值与 dashboard-slot-server SLOT_PORT 撞车(5223)

**Task ID**: fb015e86-e671-441e-93c0-cbca20d9cbd5
**Verdict**: PASS
**PR**: https://github.com/perfectuser21/cecelia/pull/4214
**Branch**: cp-0723074349-fix-bluegreen-temp-port-collision

## 完成
- 根因确认：dashboard-slot-server.cjs(常驻staging预览服务, SLOT_PORT默认5223) 与 bluegreen.sh/brain-deploy.sh 的 TEMP_PORT 默认值(5223) 撞车；在 OrbStack 下 host.docker.internal 从容器内解析会绕过容器间端口发布转发、直接命中宿主机原生监听进程——已用 docker exec cecelia-node-brain curl host.docker.internal:5223 现场复现验证
- 把 TEMP_PORT 默认值改到 5233(scripts/lib/bluegreen.sh + scripts/brain-deploy.sh 两处，含注释与dry-run回显)，不改 SLOT_PORT
- 新增回归测试 scripts/__tests__/bluegreen-temp-port-collision.test.sh 锁死两端口不能再相等，先失败后变绿
- 全仓库搜索确认没有其他地方硬编码 TEMP_PORT=5223 遗漏(final review 已核实)
- PR #4214 已合并到 cecelia main

## 没完成 / 范围外
- 未改 host.docker.internal 探测机制本身(07-15事故遗留的必要修复，超出本次范围)
- 未验证下一次真实 Gate3 部署(PR #4200 的 green canary)是否真的用新端口跑通——此 PR 只解决端口撞车本身，实际验证要等下一次触发部署时观察

## 下一步
- 关注下一次 Gate3 部署(任何 packages/brain/ 改动合并到 main 时触发)，确认 green canary 用 5233 探测成功、PR #4200 的 Codex/Grok 路由修复真正上线生产
- 已记录 Notion issue 3d07d13c(蓝绿部署端口撞车) — 部署成功后可标注该 issue 为已修复

## 数据源
- docs/superpowers/specs/2026-07-23-bluegreen-temp-port-collision-design.md
- docs/superpowers/plans/2026-07-23-bluegreen-temp-port-collision.md
- scripts/lib/bluegreen.sh
- scripts/brain-deploy.sh
- scripts/dashboard-slot-server.cjs

## 决策引用
- 236e1610-bf93-4010-9e3b-ad01a1f86d8f
