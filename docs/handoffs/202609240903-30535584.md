# Handoff：kalloc 哨兵阈值 4/8/11G → 3/5/7G，并把 ops-tests-shell 接进合并闸门

**verdict**: PASS ｜ task `30535584-801a-4965-8748-8f5dbca06b87` ｜ PR #5536

## 完成

- 三档阈值下调：WARN 4G→3G（仅日志）、ALERT 8G→5G（Brain P1）、CRITICAL 11G→7G（凌晨3-5点自动重启）；动作/日志措辞/告警payload/安全时段窗口一概不动
- 测试注入值改为边界精确值 3G/5G/7G（阈值本身，同时验证 -ge 的等号侧），另补 3GB-1KB 下边界用例；八进制炸弹回归（hour=08 需 10# 前缀）断言一字未动只换注入值
- 把 ops-tests-shell 接进 ci-passed 的 needs（47→48）与 check 清单——该 job 跑 scripts/ops/__tests__ 下 11 个 shell 测试（含本次守卫），此前一直在跑但不在闸门里，红了 ci-passed 照样绿
- 三个变异点全部实跑、全是断言红且 git diff HEAD 零行还原：①阈值整组改回 4/8/11 → 5 条断言红 ②只改 ALERT 一档 → 恰好红 1 条，WARN/CRITICAL 仍 PASS，证明三档各自独立钉住 ③闸门项从 needs 删掉 → 48→47、True→False
- 更正了自己计划文档里的错数：原写「恰好 3 条 FAIL」，实际 5 条（7G 注入值被 UNSAFE/SAFE/OCTAL 三条独立断言复用）

## 没完成

- 未追查 kalloc 泄漏的元凶函数：Apple Silicon + SIP enabled 无法远程开 zlog1 boot-arg（需进 Recovery Mode），2026-09-18 已记录为做不到
- 未把 CRITICAL 改成读真实空闲内存而非 kalloc 代理值——更精确但引入新失败模式，本刀只动数字不动结构
- 未处理 11 个 tmux + 14 个 mosh-server（从 9-18 挂到现在，全 attached）——主理人的会话，不由本刀决定；这部分占用重启也还不回来
- B 步（手动重启 MMV 回收当前 5.5G 内核内存）未做——需主理人挑 OpenClaw 空闲时段

## 下一步

- 观察合并后一小时内（cecelia-deploy-main 每小时20分 git pull --ff-only → ~/bin/janitor.sh 软链）新阈值是否上机，以及当前 5.5G 是否如预期触发一次 Brain P1 告警——那是验证闸门真会响的实证
- B 步：挑 OpenClaw 空闲时段重启 MMV，回收 data.kalloc.1024 占的 5.49G（内核 zone 内存只涨不缩，purge 已实测无效，只有重启能回收）
- 重启前先关掉不用的 tmux/mosh 会话，否则重启收益很快被吃回去

## 数据源

- scripts/ops/janitor.sh 的 check_kalloc_guard（三档阈值真身）
- scripts/ops/__tests__/janitor/janitor_kalloc_guard.test.sh（KALLOC_KB/KALLOC_HOUR 注入做逐档行为断言）
- /tmp/janitor-frequent.log（哨兵每15分钟记一次 kalloc 读数，增长曲线现成的，勿新建采集）
- docs/superpowers/specs/2026-09-24-kalloc-threshold-recalibrate-design.md

## 决策引用

- d71efe6b
- 64d38870

## 产物指针

- PR: https://github.com/perfectuser21/cecelia/pull/5536
- 分支: `cp-0924082917-kalloc-threshold-recalibrate`
