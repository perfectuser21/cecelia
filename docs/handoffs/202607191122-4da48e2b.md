# Handoff：fix(deploy): brain部署前同步宿主机checkout依赖——堵住生产部署连续3次原地失败

**verdict**: PASS

## 完成
- 根因排查(推翻了最初的lockfile假设，用真实docker build --no-cache+宿主机环境实测验证)：真正问题不是package-lock.json，是scripts/lib/bluegreen.sh的pre-swap smoke检查里harness-schema-validation-smoke.sh直接在宿主机本地Node进程import brain源码模块，而deploy-local.sh的部署根守卫块从未为宿主机checkout同步npm依赖
- 修复scripts/deploy-local.sh：NEED_BRAIN==true分支、调用brain-deploy.sh之前，为$MAIN_ROOT跑npm ci --workspace=packages/brain同步宿主机依赖，同步失败则拒绝部署
- 新增4个测试(deploy-host-deps-sync.test.js)全部通过，既有部署回归测试(14个)全绿
- Brain版本1.267.15→1.267.16
- PR#4122合并后触发的Gate3部署workflow run 29684824319 实测success，生产/api/brain/version验证真实追上1.267.16——此前卡住的PR#4108(features表更名)/PR#4116(tick drain持久化)/PR#4118(watchdog区段C)三个bug修复也随之真正上线

## 没完成
- (无)

## 下一步
- 完成，无下一步

## 数据源
- scripts/deploy-local.sh (NEED_BRAIN分支的npm ci同步逻辑)
- scripts/lib/bluegreen.sh (pre-swap smoke调用逻辑)
- packages/brain/scripts/smoke/harness-schema-validation-smoke.sh (唯一白盒宿主机检查)

## 决策引用
- 1ded1da5-8a11-4064-a507-814e7bf36156

## 产物
- PR: https://github.com/perfectuser21/cecelia/pull/4122
- 分支: cp-0719185108-package-lock-sync-fix

## 备注
本任务最初诊断假设错误(以为是packages/brain/package-lock.json缺11个依赖记录)，
systematic-debugging过程中用真实docker build --no-cache验证推翻该假设，
最终定位到真正根因(宿主机checkout依赖滞后)。这是本session内第二次"初步假设
被证实推翻，用受控实验重新定位真因"的案例(第一次是tick drain持久化任务里，
最初以为是payload残留字段，实际是liveness probe误杀)。

**created_at**: 2026-07-19T11:22:13.000Z
