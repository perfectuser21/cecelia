# Handoff：系统性排查 launchd 静默禁用（第二次独立出现）——launchd-patrol 哨兵 + zenithjoy-api 迁 LaunchDaemon

- task_id: a5a6209a-054e-457d-aa09-4c2716c89070 ｜ verdict: **PASS** ｜ 2026-07-12

## 完成
1. **盘点**：系统域 4 个 cecelia LaunchDaemon 健康；**gui/501 域在本机不存在**（sudo 亦 125）→ `~/Library/LaunchAgents` 约 20 个 plist 永不加载（两次事故共同结构性根因）；`com.cecelia.frontend`/`com.n8n` 判废弃（决策 6e9db0a8）；T17 三根因（bridge 禁用/keepalive 域名/codex flag）无复发。
2. **launchd-patrol 哨兵**（PR #3771，brain 1.253.0）：scheduler-jobs 新 job，15min 自 gate，容器 ssh 逃逸宿主核对 manifest（bridge state=running；bridge-keepalive/token-refresh/pf-firewall loaded+enabled；端口 3457/5200），异常 sendBark（DB 级 6h 去重）+ raise P1，宿主不可达 fail-open。
3. **ssh 密钥 bug 热修**（PR #3775，brain 1.253.1）：宿主只有 id_rsa，硬编码 id_ed25519 致首跑 Permission denied → 发现式回退。生产实测巡检真跑 `checked:6, anomalies:[]`。
4. **proven-to-fire 闭环**：临时 disable pf-firewall → 下轮检出 `disabled:com.cecelia.pf-firewall` → Bark 真发出（action_receipts confirmed, code 200）→ 已恢复 enable。
5. **运维修复**：zenithjoy-api 从 nohup 孤儿迁**系统域 LaunchDaemon**（UserName=administrator，KeepAlive+RunAtLoad；旧 LaunchAgent 改名 `.migrated-to-daemon` 防双跑），本机 /health ok + 公网 401（后端通），**重启自愈——第一次事故遗留 P1 关闭**。

## 没完成（显式范围外）
- 其余 ~18 个 LaunchAgents 逐个判废弃/迁移（另立；manifest 先守生产关键面）
- OrbStack 自启 plist 也在永不加载的 LaunchAgents——机器重启后 docker/brain 是否自启未验证
- 外部公网拨测（补充手段，另立）
- 巡检中途 ssh 掉线可能一次误报（审查 Nit，6h dedupe 限噪，接受）

## 下一步
完成，无下一步。（规矩：本机新增常驻服务 → 系统域 LaunchDaemon + 登记 `packages/brain/src/launchd-patrol.js` manifest，禁放 LaunchAgents——见 memory launchd-patrol-sentinel）

## 数据源
- `packages/brain/src/launchd-patrol.js` ｜ `docs/superpowers/specs/2026-07-12-launchd-patrol-design.md`
- working_memory `scheduler_job_last_run:launchd-patrol` ｜ `/Library/LaunchDaemons/com.zenithjoy.api.plist`

## 决策引用
d172e54a（双信号判定）/ 6e9db0a8（frontend 判废弃）/ dad3bce1（立项）

## 产物
PR #3771 + #3775（均已 merge，brain 1.253.1 已上生产）｜ sprint: sprints/07120809-launchd-patrol
