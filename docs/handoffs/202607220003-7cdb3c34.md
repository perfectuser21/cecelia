# Handoff：补第三处codex隐藏写者(headed模式) + 修cockpit playwright spec被vitest误跑 + dep-audit脚本改判真实CVE严重度

**Verdict**: PASS
**Task ID**: 7cdb3c34-f6df-4f98-9cf6-470ac8942962
**Journey**: 工厂 · F4 故障自愈（91c17939-225c-4491-92f3-67d8b0ace4d9）
**Branch**: cp-07220707-headed-snapshot-and-playwright-exclude
**PR**: https://github.com/perfectuser21/cecelia/pull/4176

## 完成
- harness-skill-relay.js 的 _spawnHeadedSession（SSH+tmux 直跑分支）补上第三处隐藏写者修复：headed 模式此前直接用真实 CODEX_RELAY_HOME 路径跑 codex，跟已修的 docker relay / codex-bridge 降级模式是同一类问题；改为先 snapshotCodexRelayHome 出临时隔离目录，CODEX_HOME 指向快照目录，不再直接碰源 auth.json/config.toml
- harness-skill-relay.test.js 和 headed-dispatch.test.js 补齐全部 8 个 deps 工厂的 snapshotCodexHome mock（含此前遗漏的 makeSprintDirDeps 和 headed-dispatch 自己的 7 个工厂），本地用 CODEX_RELAY_HOME=/tmp/ci-fake-codex-home npx vitest run 复现验证无遗漏
- vitest.config.js exclude 列表加入 tests/regression/cockpit-route-wire/e2e-verify.spec.ts 和 relay-07b2fd3b/inbox-e2e.spec.ts（真实 Playwright 规范文件，被过宽的 include glob 误当 vitest 用例加载报错，仓库未装 @playwright/test 也无 CI job 跑它们）
- scripts/ci/dep-audit-runtime-high.sh 白名单加入 @opentelemetry/propagator-jaeger（全仓 grep 确认零使用，genuine high CVSS 7.5）
- scripts/ci/dep-audit-runtime-high.sh 核心逻辑重写：改判每个包自己 via 里的 advisory dict 的真实 severity，不再信任 npm 顶层 severity 聚合字段（会被依赖链上别的包的高危漏洞拉高，导致纯继承者如 sdk-node 被误判 high）。本地验证：sdk-node 不再出现在 UNWAIVED；去掉白名单后 propagator-jaeger 仍被正确拦下，证明检测能力未削弱
- PR #4176 已合并（squash），所有必需检查（ci-passed / Harness V5 Gate Passed / Smoke Glob Runner Passed）全绿；唯一失败的 Deploy Preview Environment 不在必需检查列表内，不影响 auto-merge

## 没完成
（无）

## 下一步
- 完成，无下一步

## 数据源
- packages/brain/src/harness-skill-relay.js
- packages/brain/src/__tests__/harness-skill-relay.test.js
- packages/brain/src/__tests__/headed-dispatch.test.js
- packages/brain/vitest.config.js
- scripts/ci/dep-audit-runtime-high.sh
- memory: project_codex_token_consolidation_us.md

## 产物指针
- PR: https://github.com/perfectuser21/cecelia/pull/4176
- Branch: cp-07220707-headed-snapshot-and-playwright-exclude
