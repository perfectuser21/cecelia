# Handoff：秋米任务路由 PR2 入口——中文GTD表↔英文Tasks双向同步、标记行入账 qiumi_task、状态回写+急停、30s 同步循环（默认关）

- task_id: b7efdbff-0ab0-46f3-8009-64c8cb9898d6
- initiative_id: N/A
- journey_id: N/A
- verdict: PASS
- created_at: 2026-09-22T23:41:08.000Z

## 完成了什么
- PR #5502 合并 ed35001a9a907d70ac421c388b6ce6edaf54bb92（squash 进 main），brain 1.312.3→1.312.4，26 文件 +1997/-114。
- 六个 Task 全部 TDD 两段式交付：三方状态映射表 lib/qiumi-status-map.js（15 态显式、缺项即红、等待态不占人工阻塞位）；lib/notion-backoff.js 的 withBackoff（429/5xx 指数退避 4 次、其它错误不重试）；notion-gtd-sync.js 中文GTD↔英文Tasks 双向解析与建行（[zh:<id32>] / [en:<id32>] / [en-native] 三种标记）；pullNotionTasks 抽出 ingestDelegatedPage 让标记行入账 qiumi_task；状态回写 zh 通道每轮≤50 行 + 两条人工急停（淘汰→cancel_requested 走状态机、阻塞→blockTask owner_hold、拖回委派→unblockTask）；scheduler-jobs 接 30s 自循环（QIUMI_SYNC_ENABLED 默认关）。
- 迁移 463_notion_gtd_inlet_registry（notion_projection_map 登记中英两库 face=inlet + executor_kind 扩 openclaw-agent，NOT VALID）+ 464_validate_executor_kind_check（VALIDATE 收尾）。两次改号：原 458→461→463，因 main 先后被 relay 接力棒与 PR1-B 占号。
- qiumi_task 开启 V(router_valid) 标签，同时打上 tick_dispatchable=false 第二道闸（headed_manual 之外再拦一道），PR3 放开派发。
- 附带修 main 侧 project 类型：#5486 加它进注册表时 tick_dispatchable 落的是 true，本刀拨 false。它现在够不着 tick 只因 main 里都以 in_progress 建、而 tick 谓词要 status='queued'——那是建行方的巧合不是闸。注册表+审计剔除+专属断言三处原子改，变异实测拨回 true 单测必红。
- 附带补进 PR1 三刀交接单 docs/handoffs/202609230712-15f42776.md。
- 验证：单元 10 文件 150 passed；registry-vs-base 审计 68 站点 PASS 66/SKIP 2/FAIL 0；qiumi-entry-smoke 六闸 ALL PASS；qiumi-foundation-smoke 六闸 ALL PASS；task-type-registry-smoke 全通过；六个迁移按序各跑两遍全幂等。CI 66 SUCCESS / 18 SKIPPED，零失败零修复一次过。

## 没完成什么
- 同步循环默认关闭（QIUMI_SYNC_ENABLED）。打开是 PR3 切换 runbook 的一步，本刀不开。
- qiumi_task 的路由与派发（Jev 判 engine/is_device/account、SKILL_WHITELIST/LOCATION_MAP 接线）明确排在 PR3，本刀只做入口。
- 463/464 尚未在生产库上机。两者必须与 main 的 461/462 同一个窗口跑完：463 拿 ACCESS EXCLUSIVE 的那一下要短，464 的 VALIDATE 留到下个窗口会让约束长期悬在 NOT VALID。
- 债 2ada61d7（queued）EN_TASKS_DB 硬编码不读 NOTION_TASKS_DB_ID，staging 隔离做不了。
- 债 ae1d4d6c（blocked，待主理人拍板）秋米急停两处盲区 + 取消后是否清中文页任务号。
- 小项未单独开单：标记行每轮多一次 GET；ingestDelegatedPage 的 dev 分支沿用旧的 receipt.slice(0,1900) 截法（铁律「非标记行不改」）；work_routing_receipts 拒 DELETE 导致 smoke 每跑留 2 条 archived 行。
- .brain-versions 在 main 上停在 1.312.2 落后 package.json，由 auto-version bot 合并后统一刷，本 PR 未动。

## 下一步建议
- PR3 路由刀：qiumi_task 的 Jev 路由与派发放开，含 SKILL_WHITELIST 豁免收口（当前是 task-router-failover.test.js 里的硬编码豁免数组，PR3 应改为从注册表按 surface!=='openclaw-agent' 派生）。
- 切换 runbook 第一步必须先停 us-vps 的 notion-qiumi-delegate.py cron，再打开 QIUMI_SYNC_ENABLED。不能并行观察：QIUMI_SYNC_SINCE 之后的新中文行两边都够得着，重叠窗口会把同一件事建两次。
- 生产上机 463/464 时与 461/462 同窗口，跑完确认 tasks_executor_kind_check 含 openclaw-agent 且 convalidated=t。
- 请主理人拍板 ae1d4d6c（淘汰后是否清中文页 OpenClaw任务号）——当前实现按「人工态永不写入」铁律选择不清，导致淘汰行的任务号标记永不消解。

## 数据源（下一个大脑要加载的）
- docs/superpowers/plans/2026-09-23-qiumi-task-router-pr2.md（PR2 六个 Task 的实施计划，已随 docs PR #5503 进 main）
- packages/brain/src/notion-gtd-sync.js（解析/建行/反向回填/runGtdSyncOnce/gtdSyncJobHandler）
- packages/brain/src/lib/qiumi-status-map.js（三方状态映射表，15 态显式）
- packages/brain/src/notion-push-sync.js 的 ingestDelegatedPage / writeStatusReceipt / qiumiDescription
- packages/brain/migrations/463_notion_gtd_inlet_registry.sql 与 464_validate_executor_kind_check.sql
- packages/brain/scripts/smoke/qiumi-entry-smoke.sh 与 .mjs（真库六闸）
- Brain tasks 表：2ada61d7-ba8e-4f8f-9b83-a5180f05c613 与 ae1d4d6c-ad16-4883-80a7-ed9f95125ce1 两笔债
- 中文 GTD 库 c69c40c2-ba63-8271-badf-01c5410d8929 / 英文 Tasks 库 d5bc40c2-ba63-82ef-965a-8153b7ad81a0

## 关键决策引用
- b8abd28c
- 297ffee5

## 产物指针
- https://github.com/perfectuser21/cecelia/pull/5502
- sprint_dir: N/A
- branch: cp-0923041130-qiumi-pr2-entry
- doc: docs/superpowers/plans/2026-09-23-qiumi-task-router-pr2.md
- doc: docs/handoffs/202609230712-15f42776.md
