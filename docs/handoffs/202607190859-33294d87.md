# Handoff：fix(brain): tick drain持久化 + 完成态硬闸拒绝转blocked避免liveness误杀

**verdict**: PASS

## 完成
- drain.js的draining状态持久化到working_memory表(key=tick_draining)，复用tick_enabled同款模式；新增restoreDrainState()，Brain启动时(tick-recovery.js:initTickLoop)读库恢复内存态，避免Gate3部署重启把'停止后台自动派发'静默清零
- PATCH /tasks/:id completed被Rule1(REVIEW_NOT_APPROVED)/Rule2(PR_NOT_MERGED)拒绝(422)时，调用task-updater.js现成的blockTask()把任务转blocked，避免executor.js:probeTaskLiveness()（只扫status='in_progress'）误判'容器进程退出=死了'触发requeueTask完整重新派发(dc69c8df任务实测复现产出4106→4108→4112三个重复PR)
- 顺带修复DEFINITION.md的schema_version文档漂移(PR#4108已把EXPECTED_SCHEMA_VERSION bump到352但漏改文档)
- Brain版本 1.267.13→1.267.14
- 新增测试：integration/tick-drain-persist.integration.test.js(4个,真实DB)+drain.test.js(5个,mock,满足lint-test-pairing)+tasks-completed-gate.test.js(新增3个)

## 没完成
- 3个harness_initiative失败任务(never started graph)是独立缺陷，已有Notion issue d21dbea5跟踪，本次未处理
- worktree收割器复发问题(误删活跃session worktree)已登记Notion issue 88922e5a，本次未根治

## 下一步
- 完成，无下一步

## 数据源
- packages/brain/src/drain.js
- packages/brain/src/routes/tasks.js (Rule1/Rule2硬闸)
- packages/brain/src/tick-recovery.js (restoreDrainState调用点)
- packages/brain/src/task-updater.js (blockTask/unblockTask)

## 决策引用
- a09953de-4f24-41d8-9f65-27ea3def72d2

## 产物
- PR: https://github.com/perfectuser21/cecelia/pull/4116
- 分支: cp-0719161155-tick-drain-persist-blocked-on-completion-gate
- Sprint: sprints/07191605-tick-drain-persist-payload-cleanup

**created_at**: 2026-07-19T08:59:13.000Z
