# Handoff：fix(brain) warroom line-command 三处列名对齐真库

- task_id: 9eae3edd-a4a4-43b1-a28f-daae6a12dd32 · verdict: PASS
- PR: https://github.com/perfectuser21/cecelia/pull/4615（已合并，brain 1.267.214）
- 决策: af0d0818（军师台三层方案）/ df1ccf5a（四默认答案）/ 13660836（bug-fix）

## 完成
- 三处列名修复：`journey_features."group" AS group_name`；`initiative_runs.status→phase`（JS 映射前端契约 done→completed/failed→failed/其余→in_progress）；删除不存在的 `result` 列引用；health 成功判定改 `phase='done'`
- 真库回归测试 `warroom-line-command-columns.test.js`（禁 mock pool）Red 3/3 → Green 3/3，永久进 CI
- 版本 1.267.214 四处同步（含**根 package-lock workspace 条目**——gp-assertion-command smoke 校验的是根 lock，不是嵌套 lock）

## 没完成
- Deploy Preview Environment 全分支 401 存量病（已立 Notion issue，非本 PR 范围）
- Line 指挥页 E2E（归军师台 UI 批次）

## 下一步
- 军师台落地序列 7 个任务已带 `payload.anchor` 三件套重新入队（S2 锚点执法要求 journey_id+gp_id+step_id，顶层 journey_id 不算数）
- Gate3 部署完成后验证：`curl /api/brain/warroom/line/<F1 journey id>/command` abilities 应非空（F1 有 78 条真数据）

## 数据源
- packages/brain/src/routes/warroom.js
- packages/brain/src/__tests__/warroom-line-command-columns.test.js
- docs/superpowers/specs/2026-08-04-warroom-line-command-columns-prep-prd.md

## 教训
- PATCH /api/brain/tasks 只收 status/result，payload 不可 PATCH；`failed` 是死端转移表——修 anchor/requeue 只能 psql 直改
- 交互 claim（PATCH in_progress）无心跳会被 liveness/orphan 巡守打成 failed；终态回写前先查状态
- brain 版本号在高频 main 上是竞态资源，被抢就 +1 重同步
