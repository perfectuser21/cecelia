# Handoff: fix(janitor): 修复Brain告警task_type非法枚举 + 并入kalloc.1024内核泄漏哨兵

**Verdict:** PASS
**Task ID:** unknown
**Branch:** cp-09181715-janitor-kalloc-guard
**PR:** https://github.com/perfectuser21/cecelia/pull/5400

## 完成
- janitor.sh 全部4处 Brain 告警 task_type 从非法枚举 "alert" 改为已实测可用的 harness_intervention（CPU高压/孤儿分支/磁盘/新增kalloc各一处）——此前三处存量告警从写下起就被 Postgres CHECK constraint 拒绝，被 2>/dev/null 静默吞掉，从未真正推送过Brain任务
- 新增 check_kalloc_guard() 函数嵌入 janitor.sh frequent 模式：kalloc.1024 内核内存泄漏三档检测 WARN(4G仅日志)/ALERT(8G Brain P1告警)/CRITICAL(11G，凌晨3-5点安全时段自动重启止损)，支持 KALLOC_KB/KALLOC_HOUR 环境变量注入（同 DISK_PCT 测试约定）
- 顺手修复安全时段小时判断的八进制解析漏洞（hour=08/09 前导零未加 10# 前缀会报错，同 etime_to_secs 曾踩过的雷）
- 新增两个 proven-to-fire 回归测试：janitor_task_type_valid.test.sh（手动验证过修复前报红 PASS=0 FAIL=2）+ janitor_kalloc_guard.test.sh（mock curl/sudo/sleep 覆盖三档阈值+08点回归，10项全绿），已接入 CI scripts/ops/__tests__ glob
- 手动清理4个孤儿 launchd 配置（ai.openclaw.node + 3个zenithjoy服务，指向不存在路径），备份在 ~/.orphaned-launchagents-archive-20260918/
- 废弃已误建的独立 kalloc-watchdog.sh + com.cecelia.kalloc-watchdog.plist（用户明确要求不新建独立监控权威，改为并入 janitor）
- PR #5400 已合并（squash commit ac3d31a）

## 没完成 / 范围外
- kalloc.1024 内核泄漏的精确函数级根因未定位——Apple Silicon + SIP enabled 硬限制，无法远程开 zlog1 boot-arg 做泄漏追踪，除非用户亲自进 Recovery Mode 配合，否则永久做不到"点名元凶"，只能靠本次的早发现早处理三档哨兵兜底
- ~/.orphaned-launchagents-archive-20260918/ 备份目录是否需要一并删除未最终确认（用户原话倾向于'空的话就该删'，但备份本身不是'空'，留作后续用户确认）
- 本地机器的 ~/bin/janitor.sh 软链指向的仓库文件已通过PR合并更新，但本机 launchd frequent 任务（每15分钟跑一次）会在下次调度时自动拉取新代码，未做额外手动触发验证

## 下一步
- 下次 frequent 模式运行（≤15分钟内）后，建议查一次 ~/logs/janitor-frequent.log 或对应 cron 日志，确认 check_kalloc_guard 在真实环境跑起来无报错（本次只做了 mock 测试验证，未做真实环境冒烟）
- 若用户后续确认要删 ~/.orphaned-launchagents-archive-20260918/，直接 rm -rf 即可（纯本机文件操作，无需走 /dev）

## 数据源
- scripts/ops/janitor.sh（v5.1，check_kalloc_guard 函数）
- scripts/ops/__tests__/janitor/janitor_task_type_valid.test.sh
- scripts/ops/__tests__/janitor/janitor_kalloc_guard.test.sh
- decisions 表 id=64d38870-79be-44b2-ad9f-00e5b59ccc84（bug-fix category，本次改动的用户拍板记录）

## 决策引用
- 64d38870-79be-44b2-ad9f-00e5b59ccc84

created_at: 2026-09-18T09:44:20.000Z