# Handoff：对话原始捕获——机械过滤JSONL真人文本进inbox(零LLM)

- Task: `9abaecf6-a7ca-4a80-a17a-e8bf5fb39e01`
- Initiative: `5402c2e5-975a-41ac-9a5c-e07184aa2d7c`
- PR: https://github.com/perfectuser21/cecelia/pull/4135（已合并）
- Verdict: **PASS**

## 完成

- 新建 `packages/brain/src/conversation-capture.js`：纯机械（零 LLM）从 `~/.claude/projects/*/*.jsonl` 抽取用户真人文本轮次（排除 tool_result/assistant 消息）
- `captures.js` `VALID_SOURCES` 新增 `conversation`；`scheduler-jobs.js` 接入 10 分钟自 gate 的 `conversation-capture` job
- 修复经真实 DB 验证会触发的静默丢数据 bug：`repo` 字段超 `varchar(100)` 时 `pushCapture` 静默失败未被计数——已加 `else` 分支计入 errors + 字段截断 + scheduler 层 `errors>0` 上报为失败
- 修复 CI 四闸：brain 版本 bump（1.267.20→1.267.22）/ prep-prd 挪出 `sprints/` 避开 contract 闸 / smoke 脚本登记 allowlist / CodeQL `sha1→sha256`
- 27 个测试全绿（单元 7 + 路由契约 2 + scheduler 注册 13 + 集成 5），三轮 subagent 独立 spec-compliance + code-quality review（含一轮发现 Critical 静默丢数据 bug 并修复复核通过）

## 没做

- 不做 LLM 摘要/日汇总层（Alex 明确选择先落地原始捕获）
- 不做检索/追溯能力（"最近某项目问到哪、走了哪条线"——下一期）
- 不接入真 Stop Hook / 不改 `packages/engine`（用高频轮询替代，见 architecture.md 决策表）

## 下一步

完成，无下一步。若后续要做检索/追溯能力或加摘要层，需重新走 `/plan` 识别层级。

## 数据源

- `packages/brain/src/conversation-capture.js`
- `docs/architecture/2026-07-20-conversation-capture/architecture.md`
- `docs/superpowers/specs/2026-07-20-conversation-capture-design.md`
- `captures` 表（`source=conversation`）— `GET /api/brain/captures?source=conversation`
- `working_memory` 表 `key=conversation_capture_last_scan` — 扫描哨兵

## 决策引用

- `f64adaaf-5313-4d1c-95a4-e4822de4b7f6`
- `0c9e1652-f35b-4322-8687-8161f8be87f6`
- `a823206d`（历史轨道C退役，本次方案避坑参照）

## 产物

- PR: https://github.com/perfectuser21/cecelia/pull/4135
- Branch: `cp-07201618-conversation-capture`
- Docs: `docs/architecture/2026-07-20-conversation-capture/`、`docs/superpowers/specs/2026-07-20-conversation-capture-design.md`、`docs/superpowers/plans/2026-07-20-conversation-capture.md`、`docs/superpowers/plans/2026-07-20-conversation-capture-prep-prd.md`、`docs/learnings/cp-07201618-conversation-capture.md`
