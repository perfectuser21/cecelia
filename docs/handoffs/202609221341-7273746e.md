# Handoff：Deploy Preview 长期假红真根因：Step 7 账本回写写错库 + 失败被当非致命咽掉

- verdict: PASS
- task_id: 7273746e-bd21-4958-ad71-c95ab473c5a7
- PR: https://github.com/perfectuser21/cecelia/pull/5477

## 完成
- 定位真根因：不是预览起不来。预览环境一直好好的（PR#5475 实例 :5305 health 正常），是 preview-env-start.sh Step 7 的 psql 把账本库名硬编码成 cecelia。预览自 2026-09-17 下放执行机后账本在 MMV 的 cecelia_staging，MMV 上没有 cecelia 库 → FATAL: database "cecelia" does not exist（铁证 /tmp/preview-5475.log:906）
- 第二层：该错误被 `|| log "⚠ 非致命"` 咽掉，脚本照样打印「✅ 启动完成」退出 0，账本永远停在 starting，CI wait-preview-active.sh 干等 1200s 报红，日志里唯一线索就是那句被忽略的警告
- 修三处：①LEDGER_DB=${PREVIEW_LEDGER_DB:-${DB_NAME:-cecelia}}，且必须在 DB_NAME 被 $4（预览库名）覆盖前求值 ②回写拆成 scripts/preview-ledger-activate.sh，RETURNING 计行，UPDATE 0 判失败 ③改裸调，靠 set -euo pipefail 令失败即终止
- mock 豁免收窄到「整个 postgres 不可达」（preview-env-start.test.sh 用恒退 0 的 psql 桩跑全脚本）；绝不能放宽成「这个库不存在」——那会让根因静默复发。守卫 ② 与 ②b 是配对断言
- 守卫 packages/brain/scripts/smoke/preview-ledger-activate-smoke.sh 拿真 postgres 跑，8 项断言，7 项变异逐个实跑验证被抓，已登记 smoke-allowlist
- 已上机：~/perfect21/cecelia 的两个脚本覆盖成 origin/main（逐字节核对一致）。该检出落后 255 提交、230 脏文件，故只外科式覆盖两个文件，不动其余
- 端到端生产实证：走 CI 同一个接口 POST agent:5241/api/brain/preview/start（PR 5479）→ 日志出现「Step 7 ...（账本库=cecelia_staging）」「[preview-ledger] 已置 active（命中 1 行）」→ 账本 60 秒内翻 active（此前是干等 1200s 超时）。测试预览已 stop，零残留
- 顺手救回 PR#5478：它的预览起于上机前 4 分钟、吃的旧代码，实例本身健康（uptime 940s），用新脚本回写后账本已 active，不必再烧 20 分钟假红

## 没完成
- 守卫第一版把「失败不许降级」写成 grep「非致命」这个词，被变异测试当场打脸（换个措辞再留一个走不到的 exit 1 就全绿）。已改成钉控制流结构，但这类「守卫一个词而不是一个行为」的写法值得当成通用反面教材
- preview-agent 仍跑在落后 255 提交的 ~/perfect21/cecelia 上，5 个 launchd 生产服务同源。本次只治了 preview 这条，检出本身的分叉是存量债
- host-disk-sampler 在 agent 里的报错是 2026-09-18 的历史残留，当前采样正常（样本 7 秒新鲜），非活缺陷

## 下一步
- 观察后续 PR 的 Deploy Preview Environment 是否稳定转绿（本次之后第一个真实 CI 预览是 #5478，已手动救回）
- 存量债：~/perfect21/cecelia 落后 255 提交却喂 5 个 launchd 生产服务，需要统一分发方案

## 数据源
- scripts/preview-env-start.sh（LEDGER_DB 与 Step 7）
- scripts/preview-ledger-activate.sh
- packages/brain/scripts/smoke/preview-ledger-activate-smoke.sh
- MMV cecelia_staging.preview_environments（账本真身）
- ~/Library/LaunchAgents/com.cecelia.preview-agent.plist（DB_NAME=cecelia_staging 注入点）
- /tmp/preview-<PR>.log（现场日志）

## 决策引用
- 96054a8b
