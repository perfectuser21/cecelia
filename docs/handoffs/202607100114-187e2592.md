# Handoff：zombie-sweep.js + zombie-cleaner.js 删worktree前补未提交改动检查（PR #3694 已合并）

task_id: 187e2592-e7bb-475d-834e-d31ef059caab
verdict: PASS

## 完成
- `zombie-sweep.js::sweepStaleWorktrees` 和 `zombie-cleaner.js::cleanupOrphanWorktrees` 删除
  worktree 前加 `git status --porcelain` 检查，非空则 skip；检查本身失败也保守 skip
- 回归测试：`zombie-sweep.test.js` 新增 2 个用例，已接入 brain-unit CI（29/29 绿）
- `zombie-cleaner.test.js` 顺带修复三处陈旧 fixture（`resetAllMocks` 误清 `withLock` 工厂 mock /
  `WORKTREE_BASE` 硬编码值跟生产 fallback 不一致 / `readdirSync` vs `existsSync` mock 不匹配），
  本地 62/62 全绿，但尝试接入 CI 后发现所在 shard(2) 已贴近 8192MB heap 上限，加入后连续 3 次
  OOM 导致同一批用例失败——已放回 exclude 维持原状，登记 issue `bdbc08fd` 追踪 shard 内存问题
- PR #3694 已合并

## 没做
- `zombie-cleaner.test.js` 未能接入 brain-unit CI（shard 内存上限问题，issue `bdbc08fd`，P2）
- `zombie-sweep.js` 的守卫已生效；`zombie-cleaner.js` 的 source fix 已合并但缺 CI 守卫覆盖，本地
  可随时验证（`npx vitest run src/__tests__/zombie-cleaner.test.js`，需临时移出 exclude 名单）

## 下一步
完成，无下一步。若要接入 `zombie-cleaner.test.js` 到 CI，需先解决 issue `bdbc08fd`（shard(2) 瘦身
或调高 heap 上限）。

## 数据源
- `packages/brain/src/zombie-sweep.js`
- `packages/brain/src/zombie-cleaner.js`
- `docs/handoffs/202607092313-zombie-worktree-dataloss-and-dedup-pr.md`

## 决策引用
- `64765431-9f8b-4767-9470-7b33ed718572`

## 产物
- PR: https://github.com/perfectuser21/cecelia/pull/3694
- Sprint dir: `sprints/07092334-zombie-cleaner-uncommitted-guard`
- Branch: `cp-0709233601-zombie-cleaner-uncommitted-guard`
