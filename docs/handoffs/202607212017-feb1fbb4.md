# Handoff：codex-request.sh 单一写者模式改造——西安只读不回传+过期兜底

**Verdict**: PASS

## 完成
- cecelia 仓库 `scripts/codex-request.sh` 改成单一写者只读模型：删除 `push_token_back` 回传逻辑与 `trap EXIT`，改为拉取快照 → 校验剩余有效期 <48h 拒绝运行 → `exec` 跑 codex，用完不再 scp 覆盖美国侧文件（PR [#4159](https://github.com/perfectuser21/cecelia/pull/4159) 已合并）
- `codex-request.test.sh` 同步重写，TDD 两段式 commit（Red 6 failed → Green 12 passed）
- infrastructure 仓库 `refresh-codex-tokens-us.sh` 刷新阈值 24h(86400s)→72h(259200s)，给西安借出去的快照留更宽安全冗余（PR [#68](https://github.com/perfectuser21/infrastructure/pull/68) 已合并）
- 同步更新两仓库 README / CI 注释 / 旧 harness 契约测试（sprint 07071200）里过时的 86400 断言
- 排查中发现 infrastructure 仓库 `gh pr checks` 因当前 gh 默认 PAT scope 不够而报错，`ci-poll.sh` 把查询失败误判成 CI_GREEN（假绿），换用 1Password 里权限更全的 GitHub classic token 才查出两个真实失败的 check（Node.js Tests 因旧契约测试断言 86400 而挂），已修复并确认真绿后再合并
- 西安机（xian-m4）`~/perfect21/cecelia/scripts/codex-request.sh` 已手动 scp 同步新版本

## 未完成
- 西安机 cecelia 仓库整体落后 origin/main 768 个 commit（还停在 5 月 21 号）+ 3 个文件本地未提交修改，未在本任务范围内解决——未来该仓库其他文件更新，西安机大概率还是拿不到，需要另开任务处理
- 西安机 `refresh-codex-tokens-xian.sh`（team3/4/5）的阈值未同步改 72h——team3/4/5 只在西安本地使用，不存在跨机 lost-update 风险，本次判断为 out-of-scope；如果之后也要支持跨机借用这几个账号，需要重新评估
- "同账号跨地理位置使用会不会被 OpenAI 风控判定异常"这个问题只查了公开资料佐证方向（把出口 IP 统一到美国能消除 geolocation 信号），没有也无法用真账号做实测验证——这是产品/合规判断，不是代码能解决的；用户后续如果要落地"统一 exit node 出口"，还需要另外验证 codex CLI 是否真的支持走代理
- `ci-poll.sh`（`zenithjoy-skills` 仓库共享脚本）在 `gh pr checks` 报错时会把 stderr 静默吞掉并误判为 CI_GREEN 的问题本身没有修——这次是手动换 token 绕过去的，根因（该脚本对查询失败和"真的没有 pending/fail"没有区分）还在，建议另立 issue 修

## 下一步
完成，无下一步

## 数据源
- cecelia repo scripts/codex-request.sh
- cecelia repo scripts/__tests__/codex-request.test.sh
- infrastructure repo scripts/codex-token-refresh/refresh-codex-tokens-us.sh
- infrastructure repo scripts/codex-token-refresh/README.md
- infrastructure repo sprints/07071200-codex-token-refresh-repo/tests/codex-token-refresh.test.ts

## 决策引用
- 367afb57-f8e8-400d-8261-5ca1596281c1

## 产物
- PR: https://github.com/perfectuser21/cecelia/pull/4159
- PR: https://github.com/perfectuser21/infrastructure/pull/68
- 分支: cp-07211943-codex-single-writer（cecelia）/ cp-07212000-codex-refresh-threshold-72h（infrastructure）
