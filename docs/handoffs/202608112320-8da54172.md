# Handoff：fix(fleet-worker): pin cecelia runner UID/GID=5999, stop drifting into system range

- task_id: 8da54172-1c2d-4ae4-aed6-57b8c004cbad
- journey_id: e6f803f2-8c48-4cce-a7a1-5b1bda5e9c29
- verdict: PASS
- created_at: 2026-08-11T15:20:12.000Z

## 完成了什么
- 根因端到端定位（非猜测）：attempt-runner.cjs 的 --tmpfs 挂载对 .codex/.config/gh 硬编码 uid=999,gid=999；runner 镜像 cecelia 账户由 useradd -r 动态分配，08-11 被 playwright webkit 系统依赖挤占从 999 漂移到 997；非 evaluator 角色容器无 --user root 可自愈，entrypoint 的 chmod 700 因属主不符 EPERM，容器在读 GitHub 凭据 FIFO 前退出，host 侧写入落空（attempt_github_credential_fifo_write_failed），本机 harness 派发 9 连尸。
- TDD 两次 commit：commit-1 加失败回归测试（新 dockerfile-cecelia-uid-pinned.test.sh + 更新 attempt-runner.test.cjs 断言，对未修复代码跑红）；commit-2 修复（Dockerfile 显式钉 cecelia UID/GID=5999 + worker 两处硬编码同步，108/108 单测绿）。
- 999 本身先试后被本地真实 docker build 否决（该基座镜像 systemd-journal 组已占 GID 999），改选系统账户段之上、真正空闲的 5999。
- 真实验收（不止测试绿）：docker build 通过；docker run --entrypoint id 确认容器默认非 root 身份为 uid=5999(cecelia)；正对照（uid=5999 挂载下 mkdir+chmod+touch 成功）+ 负对照（同序列打在旧 uid=999 挂载上精确复现生产报错）证明复现方法本身有效。
- digest 全仓九处 canonical-pin 位置一次性同步（fleet-node-profiles.json/node-profile.js/rollout/reconcile/install/smoke/DEFINITION.md），commit-3 完成。
- CI 追出两个额外闸门并修复：brain-version-bump-gate（brain/src 有改动必须 bump 版本，commit-5 把 4 个版本文件从 1.272.11 bump 到 1.272.13）、canonical-pin-consistency.test.sh（新发现的第九个 pin 点 DEFINITION.md，commit-4 补齐）。
- PR #4812 经历两轮 rebase：第一轮 BEHIND 纯 rebase；第二轮真实冲突（main 并发把 Brain 版本也推到 1.272.12，为 Impact Contract 功能），手工消解冲突，我方版本改钉 1.272.13，DEFINITION.md 两段并存不覆盖对方内容。
- PR #4812 已合并（auto-merge，CI 全绿）。

## 没完成什么
- 未做真实端到端部署验证：生产 Brain 容器尚未拉取新代码/重新构建+re-pin 本机 runner 镜像并派发一个真实 harness 任务验证 attempt start 200——这是 PrepPRD 里勾选未完成的那一条，需要下一个大脑在合并部署窗口完成。
- 今晚发现但未修的三个衍生 issue（已记 Notion，未派修复任务）：cap 修复 #4789 曾一度缺配套迁移（已修，见另一任务 3baf1b65 系列）、derive 取证死循环护栏字段没填（已记 issue dbea513f，未修）、merge 执行层不容忍 pending checks（issue 71527603，未修）、基础设施手术类任务死后不还原现场（issue 3b029ff5，今晚 webkit digest 被别的并发任务改乱 + docker socket ACL 被 OrbStack 重建打掉，人工两次现场善后，未做自动化根治）。
- 今晚还有一条并发进行、状态未知的关联任务：'fix(deploy): Harness evaluator 验收 Dashboard #4794 ... Runner 1.272.6 UID 修复' 与本次修复内容部分重叠（都碰 runner UID），合并顺序上我的 PR 后合并，需要下一个大脑确认两者对 runner digest pin 的最终状态是否一致，不冲突。

## 下一步建议
- 合并后立即执行：确认本机 fleet-worker 拉到新 digest（sha256:3ac5b30...，若又被后续 PR 覆盖需先核对 fleet-node-profiles.json 当前值），必要时本地 docker build 重建镜像、更新 LaunchDaemon plist 的 CECELIA_RUNNER_DIGEST、bootout+bootstrap 重载 worker（kickstart 不重读 plist，是今晚踩过的坑）。
- 重载后派一个真实 harness_initiative 任务（可复用今晚已注册但因本次瘫痪而卡死的几个 P0：51740e13 合并闸 / c9043059 derive 死循环修复，均已挂起等这条修复解锁），验证 attempt start 不再 500，github 凭据 FIFO 正常写入。
- 跟进今晚记的 issue：dbea513f（derive 死循环护栏）、71527603（merge 执行层脆弱）、3b029ff5（手术任务不还原现场）——都是 P0/P1，建议下一轮排期修复。

## 数据源（下一个大脑要加载的）
- GET /api/brain/tasks/8da54172-1c2d-4ae4-aed6-57b8c004cbad（result.handoff 本体）
- GET https://github.com/perfectuser21/cecelia/pull/4812（PR 全文与 5 次 commit）
- packages/brain/config/fleet-node-profiles.json（当前 canonical digest pin，随时可能被后续 PR 再次改动）
- GET /api/brain/issues（今晚新增 8 个 issue：cap 账号选择/derive 死循环/DB_URL 不注入/pr_url 不回填/merge 执行层脆弱/failure_class 缺迁移/手术任务不还原现场，均待跟进）

## 关键决策引用
- bf361265（Alex 拍板：改代码任务派发时打标强制路由 kernel harness，不靠执行体自觉）
- 9659e9fe（本次 bug-fix 决策：runner UID 改造遗漏 worker 侧硬编码 999）

## 产物指针
- https://github.com/perfectuser21/cecelia/pull/4812
- branch: cp-08112157-runner-uid-pin
