# Handoff — orchestrator 进程远程化第一刀 + 机器角色模型（含当晚三连环生产病修复）

task_id: 7c95ef71-d566-4782-a841-3be038328276
verdict: PASS
PR: #5300(代码) / #5301(spec+plan docs) / #5303(installer 缺件) / #5305(alertness 邻居免疫) / #5307(createdSource 白名单)——全部已合并
branch: cp-09131145-orchestrator-remote-launch（主干）
spec/plan: docs/superpowers/specs/2026-09-13-orchestrator-remote-launch-design.md / docs/superpowers/plans/2026-09-13-orchestrator-remote-launch.md
决策链: 2e756506(方案B) / a9773a84(DB通路) / 9c5219c7(闸部署) / 96054a8b·ca6bf8e7(invariant)
生产部署: us-vps Brain 1.288.1 (GIT_SHA fa5df26f)，当晚三次滚动部署

## 一句话

Brain(us-vps 纯调度器) 派 harness 任务时 orchestrator 改为经 fleet-worker 远程桥起在 primary worker(现 MMV)；机器角色模型(scheduler/primary/secondary)落地，8 处 us-mac-m4 字面量改角色解析——10 月 Mac Studio(2TB/128GB) 到货迁移退化为改一行机器清单。**e2e 三项硬验证全过**：orchestrator 实起 MMV(orchestrator_host=aad17-2.macminivault.com)、跨机心跳 90s 续约滚动、杀进程后租约判死 verdict=dead/controller_lease_expired（proven-to-fire 亲验，worker 槽位 exit 钩子同场景实证释放）。

## 完成（done）

1. **机器角色模型**：machine-registry.js 叶子 SSOT（恰一台 primary，违反即加载 throw）；SERVERS/COMPUTE_SERVERS/CANONICAL_MACHINE_IDS/DEFAULT_LOCAL_MACHINE_ID/两 broker 权威判据全部派生；A 类 7 文件角色字面量清零；B 类 7 行 per-machine 配置=迁移清单，由 machine-registry-role-guard.test.sh（文件级白名单+命中数棘轮，已接 CI lint-deploy-effect-assert job）锁定
2. **远程化主干**：worker 侧 orchestrator-runner.cjs（独立槽位池/预占防竞态/exit 即释放/spawn error 兜底不崩 worker）；fleet-worker /harness/orchestrators/* 路由；Brain 侧 orchestrator-remote-bridge.js；闸语义反转（false=禁本机放行远程，非 kernel 仍拒绝，缺省零变化——终审逐语句核对）
3. **判活**：租约过期=正面死亡证据（心跳写失败即自杀链 heartbeat.js:40→run.js:500→:561 exit(1)）；legacy 无租约列回落 pid 探活，fail-open 不破
4. **部署全链**：us-vps pg 加听 Tailscale(100.79.41.61)+pg_hba 限 tailnet（决策 a9773a84）；MMV worker installer 更新+12 项 env+GH_TOKEN；Brain 三次滚动部署至 1.288.1
5. **当晚顺手根治两个既有生产病**（都是 e2e 现场抓获）：
   - #5305 alertness 邻居免疫：collectCPUMetric 用 os.loadavg() 读到 openclaw 邻居 200% 负载，Escalation 升 emergency_brake+safe_mode 把调度器自刹——与 PR#5290 executor 同病另一器官。修后 escalation 实测安静
   - #5307 createdSource 白名单：kernel_dispatch_remote 不在 CREATED_SOURCES(JS+DB CHECK 双层)，dispatch_fail_autoblock 3 连击打 blocked。改用既有 kernel_dispatch（铁律 76cb816c）
6. **10 任务 SDD 全评审**：每任务 spec+quality 双裁决，终审抓 2 Critical(槽位只借不还/spawn error 崩 worker)+1 Important(部署 env 落错机器)全修

## 没完成（not_done）——每条都是下一刀候选

- **fleet 容量喂数坏死（自动派发仍不通，最优先）**：fleet-resource-cache 对 us-mac-m4 走 isLocal:true 采**本机(VPS)**数据——把 VPS 的 200% 压力记在 MMV 头上 → effectiveSlots=0 → tick 恒 pool_c_full 不派发；西安两台 ssh 采集自容器失败离线。正确方向=容量从 fleet-worker :5231 HTTP health 喂（Brain 已可达），弃 ssh/isLocal。**在此之前 harness 任务只能手动单发**（本次 e2e 即此法，模板在下方 data_sources）
- **map_stale 准入**：kernel 准入要求 product-map 投影 fresh，扫描器 cron 未随 Brain 迁 us-vps（199ae170 家族缺件）。老靶子 feef7d3f 即卡此（其 routing receipt 亦不满足新准入）。e2e 用数据层合成 authority 绕过（准入机制本刀未触碰）
- **installer 模板回归吐**：install-fleet-worker.sh 重渲染 plist 会把 CECELIA_FLEET_WORKER_HOST 打回 127.0.0.1（今晚实撞，Tailscale 绑定丢失 worker 假死），且手加的 12 项 env（DB_*/GH_TOKEN/KERNEL_FLEET_*）下次重装全丢——模板需 host 参数化+env 保全机制
- Codex 泛化西安（ca6bf8e7 约束下的 secondary 路径）/ 方案 C 减层 / 守卫不扫 fleet-worker *.cjs 字面量 / 19+4 条记债 Minor（各任务台账 .superpowers/sdd/progress.md）
- **watchdog 有头误杀（issue d6174890）今日第二次活体复发**：本任务（正确 claimed_by 串）有头 4.5h 仍被判死置 failed，证明 40min 窗对真实交付必失效，与 claimed_by 无关。仍靠 psql 修回

## 下一步（next_steps）

1. **fleet 容量喂数改 worker HTTP**——自动派发的唯一堵点，修完 tick 才能自己派 harness
2. map 扫描器迁 us-vps（或产出物同步机制），解开 kernel 准入
3. installer 模板 host/env 参数化（Mac Studio 到货前必须，否则每次重装都回归）
4. Anthropic 余额充值（thalamus/consolidator 全链失败中——主理人已表态那些不需要跑，可改为直接关停，另任务）
5. us-vps 盘治理：openclaw-gateway 可写层仍 ~3GB+/opt/openclaw/state 8.6G（第三方，需主理人拍板）；brain 镜像滚动策略今晚已实践（删旧 baseline 保当前在跑）

## 数据源（data_sources）

- e2e 手动单发模板（自动派发修复前的应急 SOP）：①合成 authority——INSERT kernel_controller_sessions(source='kernel_dispatch',generation=1)+initiative_runs(SELECT s.lease_expires_at 保时间戳逐微秒一致，触发器强制)+回填 session.run_id；task 须 in_progress 且 payload 带 sprint_dir ②容器内 `node --input-type=module` import orchestrator-remote-bridge → prepare({run_id,task_id,repo})+start({run_id,controller_session_id,controller_generation}) ③观测：initiative_runs.orchestrator_host/heartbeat、MMV /var/lib/cecelia/fleet-worker/state/orchestrator-logs/kernel-<run>.log
- 台账：.superpowers/sdd/progress.md（10 任务+评审全记录）
- 关键坐标：orchestrator-runner.cjs(worker 生命周期)/orchestrator-remote-bridge.js(Brain 桥)/machine-registry.js(角色 SSOT)/kernel-liveness.js 租约判据/alertness/metrics.js 邻居免疫
- issue：d6174890(watchdog 有头误杀,两次实证)
