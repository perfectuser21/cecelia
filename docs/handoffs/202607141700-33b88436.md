# Handoff：刀1c/1d — zenithjoy schema迁移到独立库 + 切换ZenithJoy后端DATABASE_NAME

**Verdict**: PASS
**Task ID**: 33b88436-fc54-4a66-be98-cd846bb5a09e
**PR**: https://github.com/perfectuser21/cecelia/pull/3858

## 完成

### 刀1c — Schema 迁移

**发现状态**：独立 `zenithjoy` 库已有69张表和完整数据（刀1a/1b已执行完整dump），但 `schema_migrations` 记录缺少5条（20260710/20260712的migration）。导致若 ZenithJoy API 启动时 migration runner 会找到5个 pending，虽然 SQL 是幂等的但缺字段问题会影响运行。

**执行**：在 mmv (38.23.47.81) 对独立 zenithjoy 库运行官方 migration runner：
```bash
DATABASE_NAME=zenithjoy npm run migrate --workspace=apps/api
# → 5 pending migrations completed successfully
```

**结果**：
- `acquisition_collect_videos.comments_reported_at` ✅ 已补
- `acquisition_collect_videos.judgment_status/judgment_reason/capture_type` ✅ 已补
- `acquisition_leads.outreach_eligible` ✅ 已补
- `acquisition_config.target_profile_desc` ✅ 已补
- `dm_assignments.chk_dm_assign_status` (含 `cancelled`) ✅ 已补
- `schema_migrations`: 两库均 110 条 ✅

### 刀1d — DATABASE_NAME 切换

| 组件 | 变更 | 执行方式 |
|------|------|------|
| ZenithJoy prod plist | cecelia → zenithjoy（刀1d 已更新）| launchd plist 已更新 |
| ZenithJoy staging plist | zenithjoy_test → zenithjoy | `sed -i` 直接修改 + backup |
| Brain Docker容器 | `ZENITHJOY_DB_NAME=zenithjoy` | 容器 env 已包含，无需修改 |

### 双写验证脚本

新增 `scripts/zenithjoy-db-compare.sh` 监控脚本，对比 `cecelia.zenithjoy` vs `zenithjoy.zenithjoy` 关键表记录数。2026-07-13 首次运行结果：

```
wechat_publish_task: cecelia=7  zenithjoy=7   OK
works:               cecelia=89 zenithjoy=89  OK
publish_logs:        cecelia=131 zenithjoy=131 OK
agents:              cecelia=184 zenithjoy=184 OK
tenants:             cecelia=261 zenithjoy=250 WARN↓(-11)  ← CI smoke test 数据
agent_events:        cecelia=1265 zenithjoy=1265 OK
Migration 记录: 110/110 ✅
```

### 架构文档

新增 `docs/architecture/2026-07-13-cecelia-zenithjoy-db-separation/architecture.md`，记录三刀分解、迁移执行命令、连接架构图、双写验证说明。

## 没完成 / 显式遗留

**ZenithJoy API 进程需重启**：mmv 上 PID 24070 进程（运行中）仍使用 `DATABASE_NAME=cecelia`（prod plist 已更新但进程未重启）。这是 keepalive-managed 进程，需通过正式部署重启生效。11条 tenants 差异均为 CI smoke test 数据，非真实用户。

**双写验证期**：需在 2026-07-16（3天后）及以后执行 `scripts/zenithjoy-db-compare.sh` 验证：
- `zenithjoy` 库持续接收写入（works/publish_logs count增长）  
- `cecelia.zenithjoy` 无新写入

**验证通过后操作**（未来单独任务）：
- 从 `cecelia` 库删除 `zenithjoy` schema（一次性清理）
- 从 Brain `.env` 或 plist 中确认无残留 `cecelia.zenithjoy` 直接引用

## 下一步

1. 正式重启 ZenithJoy API（下次部署时自动生效）
2. 2026-07-16 运行双写监控脚本确认验证通过
3. 通过后提单清理 `cecelia.zenithjoy` schema

## 数据源

- docs/architecture/2026-07-13-cecelia-zenithjoy-db-separation/architecture.md
- scripts/zenithjoy-db-compare.sh
- mmv: /Library/LaunchDaemons/com.zenithjoy.api.plist
- mmv: ~/Library/LaunchAgents/com.zenithjoy.api.staging.plist

## 产物指针

- PR: https://github.com/perfectuser21/cecelia/pull/3858
- branch: cp-07141649-zenithjoy-db-separation
