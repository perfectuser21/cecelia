# Handoff：MJ5 刀2-4 验火 + 纸门修复（a8437970）

> task_id: a8437970-e619-47db-a240-91755b2dd320｜verdict: **PASS**｜2026-07-17 21:00 CST
> PR: https://github.com/perfectuser21/cecelia/pull/4058（merged，1.266.1 生产上线）

## 三闸 proven-to-fire 全命中（PRD §二 验火条款：没见过报红的守卫不算守卫）

| 闸 | 实弹 | 结果 |
|---|---|---|
| ① 锚点闸（S2） | 无锚 drill 任务走手动派发 `POST /tasks/:id/dispatch` | **HTTP 422 missing_anchor**，任务原地 queued 未点火（20:51 实录） |
| ② 联动清单（S3） | 改 CRM 表底座守卫文件 → `POST /cascade-list` | 清单自动含**全部 4 个引用步骤**（B·S2/B·S4/D·S1/E·S3，带承诺原文）；`step-impact` 返回 GP-B·S2 全部 21 格 |
| ③ 保鲜对账（S4） | 生产数据首跑 + 三报红集成测试 | 首跑**四断言全绿**；无锚 merge→A2 红、豁免类型不误伤、孤儿底座件→A3 红——三连进 CI 永久留跑 |

## 验火挖出的四组真枪窟窿（全部 TDD 修复，PR #4058）

1. **手动派发双旁路**：`/tasks/:id/dispatch` 与 `/dispatch-now` 原本完全绕过锚点闸（闸只站在 tick dispatcher）→ 两端点接闸
2. **存量豁免 13h 时区偏移**：tasks.created_at 无时区（DB 会话 -05）+ node-pg 本机 CST 解析 → 豁免窗拉长 13h（闸实际今晚 23 点才会开始执法）→ 改**日历日边界**
3. **S4 三纸门**：A3 `kind='base'`（枚举不存在，断言永绿）→ 家②③ group 判定；A3 全库扫（34 个存量步骤永久误红）→ 按承诺地图域收口；A2 无豁免过滤 → 与 S2 闸同口径（单一来源 import）
4. W7.7 契约测试无锚 fixture 被新闸正当拦下 → 补锚

## 现场处置

- **Gate3 部署 1.266.0 失败根因**：主仓被容器霸在 `cp-07171220-mj5-s4-nightly` 分支 → deploy webhook 拉不到 main → 归位 main 后 1.266.0/1.266.1 相继部署成功（becfd554 假跳过任务继续在队列另行根治）
- **#4042 处置**：其 S3+S4 内容已被直落 commit 9a227f2fd（v1.266.0）取代合入 main，PR 已 CLOSED；本任务在其上验收并修掉纸门
- MJ5 journey 已入账（家④工厂，51754939，四步承诺）；本任务带真锚（MJ5·S4）执行——**锚点自举达成**；三步验火断言已登 MJ5 格子（效果确认 green + assertion_ref）

## 在膛/待验

- 锚点闸 **tick 派发路**验火弹 13d8b8cc 在队列（pool_c_full 产能释放后 tick 击发 → terminal failed missing_anchor 自动记录在 task 行），Monitor 已布
- S4 nightly **调度器**首跑 = 明早北京 10:00（UTC 02:00）——查 `working_memory key='promise-map-nightly'` 落盘
- dev_records 数据源稀疏（今日 merges 未写该表 → A1/A2 覆盖受限）——S4 数据源加固归后续小刀

## 下一步

1. 明早 10:00 后：验 nightly 调度首跑落盘 + 验火弹 13d8b8cc 击发记录
2. S4 数据源加固小刀：merges 改从 tasks.pr_url + pr_merged_at 取数
3. 抢跑病三案根治排期不变（8a48e037 / 4df6b2c1 / 9c7ae233）
