# Handoff：fix(brain) drain 残留跨重启粘滞根治

- task_id: 468ef3d7-a503-4ab0-9834-33a58988a623 · verdict: PASS
- PR: https://github.com/perfectuser21/cecelia/pull/4631（已合并，brain 1.267.220）
- Issue: cc28d1af 根因D（本次为第 3/4 次复发的根治件）+ 新 Notion issue（drain 一夜 4 停摆）

## 完成
- drain.js `restoreDrainState`：drain_started_at 超 15min（`DRAIN_RESTORE_MAX_AGE_MS`）或缺失/不可解析 → 判部署残留，不恢复 + 清 working_memory 行（fail-safe）
- brain-deploy.sh：两处 swap 后 drain-cancel 改 `drain_cancel_with_retry`（5次×5s 跨端口切换窗口）
- 真库回归测试 `drain-stale-restore.integration.test.js` Red 2/3 → Green 3/3 永久进 CI；drain 测试族 12/12 绿
- 4 个僵尸 in_progress 诊断任务（r22/r26/r27/r28，drain 等不完的病灶）已被清（另一会话收尾），当前 in_progress 仅剩真活

## 没完成 / 已知残余
- 15min 自愈只在启动时判一次：若 drain 在新鲜期被恢复且 cancel 重试全败且存在僵尸 in_progress，仍可能粘住（三条件叠加概率极低；根治依赖僵尸任务不再产生——kernel "markRunFailed 不回写 tasks" 修复线）
- 本地仓 `.git/worktrees/cp-08050330-capture-destination-link` 指向容器内路径 /workspace，宿主机 `git worktree` 命令 fatal——等数据地基②运行结束后 prune

## 下一步
- 观察本次合并触发的 Gate3 部署：drain 应被 retry 正常取消（监视器 b1bj0dx0z 盯着）
- 完成，无其他下一步

## 数据源
- packages/brain/src/drain.js / scripts/brain-deploy.sh
- packages/brain/src/__tests__/integration/drain-stale-restore.integration.test.js
- 决策 af0d0818（军师台序列）· 任务 468ef3d7 description（根因链全文）

## 教训
- 本地 origin/main 引用可能因坏 worktree 条目而 fetch 失灵——版本竞态判断要用 `gh api contents` 直读远端，别信本地 ref
- session worktree 元数据又被收割（第 8 次，轻型形态）：SOP 三文件重建 + `git read-tree HEAD`（禁 -u）几分钟恢复
