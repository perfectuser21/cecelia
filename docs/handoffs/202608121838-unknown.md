# Handoff：mcp-readonly Notion 连接排障 + 生产事故处理

**Verdict**: PASS
**Task ID**: unknown（本次是响应用户实时反馈的连续修复，未提前注册Brain task）
**Journey ID**: cccccccc-f0f0-4000-8000-000000000003（管家·G3 凭据与安全守卫）

## 完成
- 修复 GET/DELETE /mcp 返回404而非405导致Notion连接502的bug（PR #4811）
- 修复共享McpServer实例并发下"Already connected to a transport"崩溃导致502的bug（PR #4816），根因：createApp()只buildMcpServer()一次全部请求共享同一实例，改成每请求新建+补try/catch
- 修复限流20次/分钟在真实Notion多对象探索场景下误报429的问题，调到60次/分钟（PR #4833）
- 诊断并解决了一次真实的Cloudflare Tunnel(54de2a32)持续性502基础设施故障：耗时约1.5小时排除了本地app/DNS/ingress配置/connector健康/IPv4-IPv6/防火墙/WAF等所有可能性后，确认是该tunnel实例本身进入不明损坏状态（Cloudflare状态页对应组件显示operational，本地日志无对应错误，纯靠边缘HTTP Analytics API交叉验证才确认），最终绕过方案是给mcp-readonly新建独立专用Tunnel(f30f63b2-b347-4a96-8ec2-27457f48d843)+独立LaunchDaemon(com.cloudflare.mcp-readonly-tunnel.plist)完全隔离风险
- 升级了cloudflared 2026.3.0→2026.7.3（顺手做的，非本次故障根因但版本确实过旧）
- Token两次轮换（因为多次被贴进对话），当前有效token在1Password mcp-readonly-bearer-token条目
- 记录Notion Issue b83d534b跟踪tunnel故障详情，供后续n8n等同tunnel服务复发同类问题时直接抄作业
- 最终经Notion AI实测确认：Value Stream/Capability/Cross-cut/Boundary四类地图对象查询全部成功，MCP连接正式打通

## 没完成 / 已知限制
- 排障过程中一度怀疑影响到kaoshi/label两个服务，后确认是另一个不相关的、更早就存在的问题（远程HK VPS 100.86.118.99对应端口未监听），未处理，不在本次范围内
- n8n目前仍跑在原来那条不稳定的54de2a32 tunnel上，如果之后规律性502复发，应直接照本次方案迁移到独立tunnel，不要重复排查
- 未查明54de2a32 tunnel本身故障的根本原因（怀疑是Cloudflare侧该tunnel实例路由/会话状态损坏），受限于API token权限（WAF/Analytics权限是排障过程中临时申请的cfut_...token，已存1Password），未能完全定位，需要Cloudflare支持介入才能查清
- get_deployment_status读宿主机git状态而非容器实际commit的已知偏差（Notion Issue f2a44988）仍未修

## 下一步
完成，无下一步（用户已确认连接成功且开始正常使用）

## 数据源
- packages/mcp-readonly/ 全部代码
- 1Password CS Vault "Cloudflare" 条目（含新增的WAF/Analytics token和mcp-readonly专用tunnel token）
- Notion Issue b83d534b-1efb-4327-8c22-68703fdc602b（tunnel故障详情）
- Notion Issue f2a44988（get_deployment_status已知偏差，独立遗留项）

## 决策引用
- 美国M4只读MCP Server：单Bearer Token模型 + 复用现有Cloudflare Tunnel（decision 79d320c9-9f55-4f8d-a70f-3a1d0b065ff0，注：Tunnel复用这条实际因本次事故被部分推翻，mcp-readonly现在用的是独立tunnel）

## 产物
- PR: https://github.com/perfectuser21/cecelia/pull/4811, https://github.com/perfectuser21/cecelia/pull/4816, https://github.com/perfectuser21/cecelia/pull/4833
- Sprint dir: 无（均为直接响应式修复，未走完整PrepPRD流程）
- Branch: cp-08112203-mcp-get-delete-405, cp-0811231804-mcp-shared-server-fix, cp-0812182247-mcp-ratelimit-bump（均已合并，worktree已清理）
- MCP URL: https://mcp-readonly.zenjoymedia.media/mcp（现在跑在独立tunnel f30f63b2上）

---
生成时间: (见文件名时间戳)
