# Handoff：美国M4只读MCP Server（Notion 读 Cecelia 状态）

**Verdict**: PASS
**Task ID**: a6a0916e-30fe-497e-8285-41548e08298a
**Journey ID**: cccccccc-f0f0-4000-8000-000000000003

## 完成
- 新增独立包 packages/mcp-readonly：只读 MCP Streamable HTTP Server，6个固定工具（get_schema_version/get_map_summary/get_map_nodes/get_map_edges/get_deployment_status/get_service_logs）
- 单Bearer Token鉴权（常量时间比较+fail-closed）、独立只读PostgreSQL账号（packages/brain/scripts/setup-mcp-readonly-role.sql，不在自动migration执行路径）、限流（sha256 hash分桶）、日志脱敏、启动自检（has_table_privilege探测拒绝带病启动）
- Bark四类告警（鉴权失败/DB故障/重启风暴/限流触发）跨进程持久化；LaunchDaemon常驻+ResidentSetSize内存软限制（macOS advisory机制，非确定性硬顶，已在文档中如实标注）
- 生产凭据注入链路（dotenv+~/.credentials/合并）、CI接入（.github/workflows/ci.yml新增mcp-readonly-tests纳入ci-passed阻塞合并检查）
- 14任务TDD实现，每任务规格合规+代码质量双重审查，两轮修复真实发现的问题：SQL注入(timeoutMs字符串拼接)、迁移文件会打挂Brain启动(psql专属语法进了自动执行的migrations目录)、get_service_logs指向三个月前停用部署的僵尸日志路径、oom_score_adj/MemoryLimit是编造的不存在macOS配置项、告警发送阻塞主响应路径、生产凭据从未真正注入进程、包完全未接入CI
- PR因pr-size-check 3000行硬门槛拆分为两个PR：#4807(纯文档，设计文档+实现计划+PrepPRD)先合并，#4806(代码)随后合并

## 没完成 / 已知限制
- get_deployment_status 读宿主机git状态，跟容器cecelia-node-brain实际运行commit可能不一致，已建Notion Issue f2a44988跟踪，修法是改查Brain /api/brain/health的git_sha字段
- 部署阶段验证未做（需要生产环境）：Cloudflare Tunnel ingress规则配置、5211/5221部署前后响应对比、Bark四条阈值真实触发一次、真实密码生成+写入1Password
- 重启计数持久化文件(logs/mcp-readonly-restarts.json)读写非原子操作，理论有并发竞态，实际风险低（launchd KeepAlive单实例语义），未做temp+rename加固

## 下一步
- 部署时：生成真实MCP_BEARER_TOKEN/mcp_readonly_password写入1Password CS Vault + ~/.credentials/，跑packages/mcp-readonly/deploy/deploy.sh
- 在Cloudflare Zero Trust后台给现有Tunnel加一条public hostname ingress规则指向本地MCP端口
- 跟进Notion Issue f2a44988（get_deployment_status数据源改查Brain health端点）

## 数据源
- packages/mcp-readonly/ 全部代码
- docs/superpowers/specs/2026-08-11-readonly-mcp-notion-design.md
- docs/superpowers/plans/2026-08-11-readonly-mcp-notion.md
- sprints/08111711-readonly-mcp-notion/prep-prd.md
- packages/brain/scripts/setup-mcp-readonly-role.sql/.sh

## 决策引用
- 美国M4只读MCP Server：单Bearer Token模型 + 复用现有Cloudflare Tunnel（decision 79d320c9-9f55-4f8d-a70f-3a1d0b065ff0）

## 产物
- PR: https://github.com/perfectuser21/cecelia/pull/4806, https://github.com/perfectuser21/cecelia/pull/4807
- Sprint dir: sprints/08111711-readonly-mcp-notion
- Branch: cp-08111712-readonly-mcp-notion
- 文档: docs/superpowers/specs/2026-08-11-readonly-mcp-notion-design.md, docs/superpowers/plans/2026-08-11-readonly-mcp-notion.md

---
生成时间: 2026-08-11T12:30:50.000Z
