# Handoff：刀1 PR2：grok 配额探针不再编造 0%，未读到即诚实留 null

- verdict: PASS
- task_id: 60cbe35b-e86e-49ce-bd97-244a14874dac
- PR: https://github.com/perfectuser21/cecelia/pull/5475

## 完成
- normalizeGrokUsage 原本无条件返回 seven_day_pct:0——注释里写的 proto3 默认值语义从头到尾没实现过，测试却把这个假事实钉死了（守卫在保护一个不存在的行为）
- 生产实证：ops_model_accounts 里 grok 行恒为 5h=NULL / 7d=0，于是刀1 PR1 装的三态配额闸对 grok 必然判 usable——一个可能已耗尽的号永远不会被排除
- 改为返回 null：judgeAccount 落到 unknown/pct_unknown「弃权」，不加分不减分，交给认证失败/真 429 回调去定夺。假的 0 比未知更坏，因为它会被当成事实
- 测试里钉死方向：expect(n.seven_day_pct).not.toBe(0)，并写明为何不敢猜

## 没完成
- grok 7d 用量字段仍未真解析。拿真实响应解码（2026-09-22，grpc-status:0，111 字节）确有候选字段（内层 f1 w5(float32)=1、f7={f1=2,f2(float)=1}），但只有一个样本且恰好取在极值 1.0 上，无从分辨它是「剩余比例」还是「已用比例」——两种解释在这个点上完全同形。拿到第二个非极值样本前不猜
- dispatcher 扩池（刀1 PR2 原计划的另一半）未做

## 下一步
- 等 grok 号跑到非极值用量时再取一次 gRPC 响应，两点即可定向，届时把 seven_day_pct 真接上
- 刀1 剩余：dispatcher 扩池

## 数据源
- packages/brain/src/model-accounts-usage-probe.js（normalizeGrokUsage）
- packages/brain/src/__tests__/model-accounts-usage-probe.test.js:131
- ops_model_accounts 表（grok 行）

## 决策引用
- 738c24f9
