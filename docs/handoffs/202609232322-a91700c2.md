# Handoff：便宜闸读不到 Notion 指定的 Agent/Workflow — 规格分叉修复 + 共享真身

**verdict**: PASS ｜ task `a91700c2-4fd4-47fd-bbf6-b0ef81ce89ba` ｜ PR #5524

## 完成

- 根因定位：不是笔误是规格分叉——pr2.md 写实现 agent_workflow_ids、pr3.md:495/499 的实现伪代码写 src.relations?.workflows，出问题的代码是照文档抄的；8 处测试也照 pr3 抄
- cheap-gates.js 改读 src.agent_workflow_ids（Agent/Workflow 混合数组），分两趟匹配 ops_workflows/ops_agents，删死形状
- qiumi_source 抽成唯一真身 packages/brain/src/lib/qiumi-source.js，两层导出（buildQiumiSource 扁平层 / qiumiSourceFromNotion Notion 层）
- tie-break 由池字母序改为用户在 Notion 的选择顺序，并补唯一区分用例
- 契约守卫三段串联 parseZhPage → qiumiSourceFromNotion → cheapGates，workflow/agent 两条都断言
- 收编 12 处自造形状（8 处死形状 + 2 smoke + 2 测试）+ 改掉 pr2/pr3 文档的分叉合同
- buildQiumiSource 入参误写蛇形当场抛 TypeError（与本 bug 同型的失败模式护栏）
- 修好一条假绿：qiumi-router.test.js「agentRef 反查不到序列号」原断言全负向，入参清空照样 PASS（实跑验证），补正向断言后入参清空即报 expected null to be '小白'
- 三个变异点全部实跑由绿转红且全是断言红：改 builder 键名 / 只喂 workflows 池 / 调换两个 if 块顺序

## 没完成

- 全量 989 文件本地未跑完（16G 机器内存不足被杀），只跑了爆炸半径 17 文件 282 测试，全量由 CI 覆盖
- Deploy Preview Environment 全程红——MMV 的 preview-agent 被 SIGTERM 杀掉（退出码 -15）导致 503，非本 PR 引入，且非必需检查
- agentRef:serial 支路此前从未真跑过，本 PR 起首次带电（主理人拍板放行，判定点 aae169cd），生产表现待观察

## 下一步

- 观察 agentRef:serial 支路首次带电后的生产表现：若 ops_agents 里有名字含手机序列号的行被指定，相关任务会锁到那台手机并跳过 Jev
- MMV preview-agent 被杀需单独查（退出码 -15，疑与内存压力有关，空闲内存一度只剩 0.02G）
- 本 bug 只修通了「你指定了就生效」；Brain 按额度选账号派单仍未做，见 Brain task d24d4b19（账本判据漏算七天额度）与 1a6656c8（派单前写 auth order）

## 数据源

- packages/brain/src/lib/qiumi-source.js（qiumi_source 唯一真身，一切消费方从这里 import，勿手抄形状）
- packages/brain/src/routing/__tests__/qiumi-source-cheapgates-contract.test.js（三段串联契约守卫）
- docs/superpowers/specs/2026-09-23-qiumi-cheapgates-relation-design.md
- docs/superpowers/plans/2026-09-23-qiumi-cheapgates-relation.md

## 决策引用

- 522e9c8e
- 0aa5d290
- aae169cd
- 76cb816c

## 产物指针

- PR: https://github.com/perfectuser21/cecelia/pull/5524
- 分支: `cp-0923184138-qiumi-cheapgates-relation`
