# Handoff：preview Brain 与生产 harness 完全隔离（PR #4644）

## 完成
- 根因破案（铁证链完整）：preview 环境 `pg_dump|pg_restore` 整库克隆生产库，
  快照携带 in_progress harness 任务；preview Brain 作为生产 Brain 子进程启动、
  继承生产 env（同一 `KERNEL_FLEET_BRIDGE_TOKEN`、callback 指回生产 5221）；
  `server.js:740` 的 `scanOrphanedRelayTasks` 无条件运行，把克隆任务当孤儿重
  点火——spawn 出的 Kernel orchestrator 与生产 Brain 争抢同一 fleet-worker/
  工作区分支/run。实拍证据：生产容器内 pid 3890 跑着
  `cecelia-previews/preview-4643/.../orchestrator/run.js --task-id <生产任务>`；
  preview 库同 run 双时间线 + 成串 `worker_attempt_*_after_lease` 租约战损。
  这就是 r35（PR#1581 Kernel 验证）Generator 容器环境变量被搅丢
  （`MISSING_HARNESS_RUNTIME_CONTEXT`）的直接原因。
- 现场止血：杀掉影子 orchestrator；灭活 `cecelia_preview_4437`（7条）+
  `cecelia_preview_4643`（2条）共 9 条克隆 harness 任务。
- 根治（PR #4644，已合并已部署 1.267.225，容器内已验证）：
  ① `preview-env-start.sh` Step 4.5 克隆后灭活（掐燃料）；
  ② `spawnSkillRelaySession` 入口 `BRAIN_PREVIEW` 拒绝闸（掐点火器，
  四条派发路径唯一咽喉）。TDD 全程，DevGate 全过。
- 同日早前收尾：PR#1581 坏合同断言已修（伪 RED 占位桩→真实 TDD RED，
  commit 5d805999 直推合同分支）；contract-reviewer 9.13.0 第 25 条
  防复发规则（zenithjoy-skills #178 + cecelia 快照 #4625 均已合并）。

## 没完成 / 范围外
- r35 验证 run 已 failed（被本事故毒死），未重新点火 r36——GAN 收敛机制
  本身已两次实证（r33 七轮、r35 十一轮均真实收敛进 Generator），是否再打
  一轮完整验证由主理人决定。
- preview 库克隆快照里的其他生产状态（decisions、initiative_runs 等）仍
  原样保留，只灭活了 harness 任务；如 preview Brain 还有其他"续跑生产状态"
  的回路（tick 已关、watchdog 随 tick 不启动，startup-sync 已被闸拦），
  暂未发现但未穷举。
- session-ee0dd243 worktree 的 git 元数据被收割器打掉（本次已换新 worktree
  绕过），收割器复发病未处理（memory 有案底）。

## 下一步
- 如需完成 PR#1581 端到端验证：用 r35 同款 payload（含 thin_prd + 完整
  role_assignments）重新 POST harness_initiative 点火 r36，这次没有影子
  大脑干扰，Generator 应能跑到真实 commit/push/PR。
- 可选根治加固：`scanOrphanedRelayTasks` 入口也加 BRAIN_PREVIEW 早退
  （当前靠 spawn 咽喉闸兜底已足够，属锦上添花）。

## 数据源
- `packages/brain/src/harness-skill-relay.js`（preview 闸）
- `scripts/preview-env-start.sh` Step 4.5
- `cecelia_preview_4643` 库 harness_attempts（事故战损证据）
- DEFINITION.md「## Brain 1.267.225」条目

## 决策引用
- 无新增（技术根因修复，无分叉点）

## 产物
- PR: https://github.com/perfectuser21/cecelia/pull/4644（merged, 1.267.225）
- 关联: zenithjoy-skills#178、cecelia#4625、合同分支 commit 5d805999

task_id=unknown（本会话非 /dev --task-id 注册流程）
