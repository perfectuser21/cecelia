# Handoff：调度 job 入运行舱 + notion-gtd-sync 整轮有界（09-24 卡死 8.4h 根治）

- task_id: 50a2c256-0828-418a-a9a6-9547654db1ce
- journey_id: 8bb8252f-29b4-4c34-acb9-1accda7ddfcf（工厂·F5 指挥舱 / GP 804520f5 / 步骤 3「看到的等于真相」）
- verdict: PASS
- PR: https://github.com/perfectuser21/cecelia/pull/5541（已合并）
- branch: cp-09241015-scheduler-jobs-ops-cockpit
- created_at: 2026-09-24T04:21:00Z

## 完成

- 根因查实：09-24 00:40:35Z Brain 事件循环停顿 ≥5s（pg 连接 5s 超时被 reset、tick already running），gtd 下一轮某个 await 永不返回，`inFlight` 永真 8.4h；该 job 不在 `ops_workflows` 故无告警；整轮唯一无界 await 是 pg 查询。
- 交付：`classifyDeclaredLiveness`；pg `query_timeout`（10min）；notion-gtd-sync 整轮总超时 + 步名门控 + 步边界放弃 + env 校验 + `liveness_at`（循环启动兜底）；哨兵透传 `liveness_at`；新 job `scheduler-liveness`（JOBS 末尾，哨兵→`ops_workflows source=scheduler`，10min 降噪 + dead 行刷新，Bark 按轮合并 + P1 兜底，恢复 P2，僵尸行 cold，classifyError 心跳，self 计活）；`scheduler-jobs` 单测 mock 掉真 ssh/docker（55s→1s）；便宜闸与 Notion 派单反查只认 `source=n8n`；smoke `scheduler-liveness-smoke.sh` 登记 allowlist；版本碎片。
- 决策 69cd802f（activity 混合执行方 / 三套图合一 / 入图从代码生成）与 bug-fix 决策落库；issue e13df2b5（tick 依赖检查脏 uuid `9b32b2c5`）登记。
- 主理人中文 GTD 库两行委派任务在重启后已被接收。

## 没完成

- 上产验收 proven-to-fire 未做：等 auto-version bump + Gate3 部署后 `psql` 查 `ops_workflows source='scheduler'` 行数 = 49、`notion-gtd-sync` 行 liveness=ok；人为把哨兵 `liveness_at` 改老，看 Bark 红一次。
- `pool.connect()`+`client.release()`（不传 err）路径超时不丢连接（migrate.js / preview-destroyer.js）。
- `ops_agents` 反查未筛 source（误选 launchd/brain 行只导致标签失真）。
- 串行轮多个长超时 job 同时超时（≈15min）会让排头 job 误翻 dead 再恢复；`missing_since` 配置坏时驾驶舱仍绿。
- issue e13df2b5 tick 脏 uuid 未修。
- activity + 边 模型（决策 69cd802f ①②④）待走 /capability。

## 下一步

1. 部署到 us-vps 后按 PR 描述「上产验收」四步做 proven-to-fire。
2. /capability 归位：activity 模型（脚本 / agent / 人 / 外部四类执行方 + 边契约），派发链作第一个样本。

## 数据源

- docs/superpowers/specs/2026-09-24-scheduler-jobs-ops-cockpit-liveness-design.md
- docs/superpowers/specs/2026-09-24-scheduler-jobs-ops-cockpit-prep-prd.md
- docs/superpowers/plans/2026-09-24-scheduler-jobs-ops-cockpit-liveness.md
- packages/brain/src/ops-scheduler-liveness.js / notion-gtd-sync.js / scheduler-jobs.js
- `ops_workflows(source='scheduler')`、`working_memory scheduler_job_last_run:*`

## 决策引用

- 69cd802f（运行舱补齐 activity 层：三套图合一 + 强制入图从代码生成）

## 产物指针

- PR #5541；changes/cp-09241015-scheduler-jobs-ops-cockpit.md；docs/learnings/cp-09241015-scheduler-jobs-ops-cockpit.md
