# L4 Integration 从 macOS 迁移到 ubuntu-latest

## 背景
L4 Runtime Gate 的 brain-integration job 跑在 self-hosted macOS runner（xian-mac-m1），runner 数量有限（5个）且经常 busy，导致所有 PR 的 L4 无限排队。

## 成功标准
L4 brain-integration 在 ubuntu-latest 上成功运行，PostgreSQL + pgvector 正确安装，所有集成测试通过。
