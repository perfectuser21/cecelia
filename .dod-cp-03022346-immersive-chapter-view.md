---
id: dod-immersive-chapter-view
version: 1.0.0
created: 2026-03-02
---

# DoD：说明书章节沉浸式体验三连优化

- [x] 点击章节卡片进入沉浸式全屏视图（detailChapter state）
  Test: manual:bash -c "grep -q 'detailChapter' apps/api/features/brain/pages/SuperBrain.tsx"

- [x] 沉浸式视图有「← 说明书」返回按钮
  Test: manual:bash -c "grep -q '← 说明书' apps/api/features/brain/pages/SuperBrain.tsx"

- [x] 目录点击进入对应章节沉浸式视图
  Test: manual:bash -c "grep -q 'setDetailChapter' apps/api/features/brain/pages/SuperBrain.tsx"

- [x] SVG 在沉浸式视图中高度 420px
  Test: manual:bash -c "grep -q '420' apps/api/features/brain/pages/SuperBrain.tsx"

- [x] Dashboard build 无 TS 错误
  Test: manual:bash -c "cd apps/dashboard && npm run build 2>&1 | grep -i 'error' | grep -v 'warning' | grep -v 'chunk' && exit 1 || exit 0"
