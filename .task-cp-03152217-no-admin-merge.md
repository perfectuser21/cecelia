---
id: task-cp-03152217-no-admin-merge
type: task-card
branch: cp-03152217-no-admin-merge
created: 2026-03-15
---

# Task Card: 03-prci.md 禁止 --admin 合并

## 需求（What & Why）

**功能描述**: 在 Step 3 (PR+CI) 步骤文件的「禁止行为」列表里明确禁止 `gh pr merge --admin`。

**背景**: 2026-03-15 PR #967 中，因 L3 runner 看似 pending 超时，使用了 `--admin` 绕过 CI 强行合并。实际上 L3 只是在正常跑测试（11-16 分钟），并未卡死。GitHub 端已开启 `enforce_admins`，`--admin` 在系统层面已失效，但步骤文件里还没有明确禁止说明。

**不做什么**: 不改 CI 配置、不改 GitHub branch protection（已在系统层面设置）、不改其他步骤文件。

## 成功标准

1. [ARTIFACT] 03-prci.md 的「禁止行为」章节包含禁止 `--admin` 的条目
2. [ARTIFACT] 03-prci.md 包含 L3 Engine 正常耗时说明（11-16 分钟）
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] 03-prci.md 禁止行为列表加入「禁止 gh pr merge --admin」
  Test: manual:bash -c "grep -c 'admin' packages/engine/skills/dev/steps/03-prci.md"

- [x] [ARTIFACT] 03-prci.md 加入 L3 耗时说明（CI pending 不等于卡死）
  Test: manual:bash -c "grep -c 'pending\|11-16\|等待' packages/engine/skills/dev/steps/03-prci.md"

- [x] [BEHAVIOR] check-dod-mapping.cjs 本地通过
  Test: manual:bash -c "node scripts/devgate/check-dod-mapping.cjs 2>&1 | tail -3"

- [x] [GATE] CI 全部通过
  Test: manual:bash -c "gh run list --repo perfectuser21/cecelia --branch cp-03152217-no-admin-merge --limit 1 --json conclusion | jq -r '.[0].conclusion'"

## 实现方案

**要改的文件**: `packages/engine/skills/dev/steps/03-prci.md`
**Scope**: 只在「禁止行为」章节追加 2-3 行，不改逻辑
