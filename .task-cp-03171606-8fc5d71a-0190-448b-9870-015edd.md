---
id: task-cp-03171606-8fc5d71a-0190-448b-9870-015edd
type: task-card
branch: cp-03171606-8fc5d71a-0190-448b-9870-015edd
created: 2026-03-17
---

# Task Card: 重启恢复集成测试套件 + 回归契约

## 需求（What & Why）
**功能描述**: 为 Brain 重启恢复路径建立完整的集成测试套件，覆盖关键场景，并在 regression-contract.yaml 中登记回归契约。
**背景**: PR1 修复了 syncOrphanTasksOnStartup 孤儿改 requeue 逻辑；PR2 统一了 startup-recovery.js 与 executor.js 的重复代码路径。现在需要为这两个 PR 建立测试覆盖和回归契约。
**不做什么**: 不修改 executor.js 逻辑，不修改 startup-recovery.js，只添加测试和契约。

## 成功标准
> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [ARTIFACT] executor-startup-sync.test.js 新增 场景2（OOM 孤儿 → failed）和 场景3（并发幂等保护）测试
2. [BEHAVIOR] 新增测试全部通过（vitest 运行无错误）
3. [ARTIFACT] packages/quality/regression-contract.yaml 新增 B-STARTUP-REQUEUE-001 条目（含 expires 字段）
4. [GATE] CI 测试通过（brain-ci.yml L1+L3）

## 验收条件（DoD）

- [ ] [ARTIFACT] executor-startup-sync.test.js 包含 OOM 孤儿保持 failed 的测试（场景2）
  Test: manual:bash -c "grep -c 'OOM\|oom_likely\|oom_killed' packages/brain/src/__tests__/executor-startup-sync.test.js"

- [ ] [ARTIFACT] executor-startup-sync.test.js 包含并发幂等保护测试（场景3）
  Test: manual:bash -c "grep -c '幂等\|idempot\|second call\|第二次' packages/brain/src/__tests__/executor-startup-sync.test.js"

- [ ] [BEHAVIOR] getDmesgInfo mock 已导入且用于 OOM 场景测试（platform-utils mock 链路验证）
  Test: manual:bash -c "node -e 'const f=require(\"fs\").readFileSync(\"packages/brain/src/__tests__/executor-startup-sync.test.js\",\"utf8\");if(!f.includes(\"getDmesgInfo\"))process.exit(1)'"

- [ ] [ARTIFACT] packages/quality/regression-contract.yaml 包含 B-STARTUP-REQUEUE 条目
  Test: manual:bash -c "grep -c 'B-STARTUP-REQUEUE' packages/quality/regression-contract.yaml"

- [ ] [GATE] 测试基准文件可读且包含合法数字（Brain CI 基准完整）
  Test: manual:bash -c "node -e 'const f=require(\"fs\").readFileSync(\"packages/brain/ci/brain-test-baseline.txt\",\"utf8\");if(isNaN(parseInt(f.trim())))process.exit(1)'"

## 实现方案
**要改的文件**:
- `packages/brain/src/__tests__/executor-startup-sync.test.js` — 新增 getDmesgInfo 导入 + 2 个测试场景
- `packages/quality/regression-contract.yaml` — 新增 B-STARTUP-REQUEUE-001 条目

**Scope 锚定**: 只改测试文件和回归契约，不改生产代码
