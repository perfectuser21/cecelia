---
id: dod-fix-executor-error-message
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

## DoD - fix(brain): executor 孤儿检测路径补写 error_message 列

### 验收清单

- [x] syncOrphanTasksOnStartup 路径写入 error_message
  Test: manual:bash -c "grep -c 'orphan_detected.*reason=.*toISOString' packages/brain/src/executor.js"

- [x] liveness probe 超时路径写入 error_message
  Test: manual:bash -c "grep -c 'liveness_timeout.*reason=.*toISOString' packages/brain/src/executor.js"

- [x] watchdog quarantine 路径写入 error_message
  Test: manual:bash -c "grep -c 'watchdog.*reason=.*toISOString' packages/brain/src/executor.js"

- [x] error_message 格式正确（[类型] reason=X at TIMESTAMP）
  Test: manual:bash -c "grep -c '\[orphan_detected\]\|\[liveness_timeout\]\|\[watchdog\]' packages/brain/src/executor.js"

- [x] 单测文件存在并验证 error_message 写入
  Test: manual:bash -c "grep -c 'error_message' packages/brain/src/__tests__/executor-error-message.test.js"
