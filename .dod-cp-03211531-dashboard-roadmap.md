# DoD: Dashboard Roadmap 页面

- [x] [ARTIFACT] 文件 `apps/api/features/planning/pages/RoadmapPage.tsx` 存在且导出 React 组件
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/planning/pages/RoadmapPage.tsx','utf8');if(!c.includes('export default'))process.exit(1)"

- [x] [BEHAVIOR] RoadmapPage 发起 GET 请求到 `/api/brain/goals` 获取 KR 数据并渲染进度条
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/planning/pages/RoadmapPage.tsx','utf8');if(!c.includes('/api/brain/goals'))process.exit(1)"

- [x] [BEHAVIOR] 页面包含 Now/Next/Later 三列布局按 current_phase 分组展示 projects
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/planning/pages/RoadmapPage.tsx','utf8');if(!c.includes('now')|| !c.includes('next')|| !c.includes('later'))process.exit(1)"

- [x] [ARTIFACT] Brain 新增 `/api/brain/self-drive/latest` 端点返回最近一次 SelfDrive 事件
  Test: manual:curl -s http://localhost:5221/api/brain/self-drive/latest | node -e "const d=require('fs').readFileSync('/dev/stdin','utf8');const j=JSON.parse(d);if(!j.success)process.exit(1)"

- [x] [BEHAVIOR] planning feature manifest 注册 RoadmapPage 组件替换旧 RoadmapView
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/planning/index.ts','utf8');if(!c.includes('RoadmapPage'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd apps/dashboard && npx tsc --noEmit 2>&1 | tail -5"
