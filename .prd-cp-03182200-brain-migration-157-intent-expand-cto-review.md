# PRD: Brain Migration 157 — 新增 intent_expand + cto_review task_type

## 问题
1. tasks_task_type_check 约束里没有 `intent_expand` / `cto_review`，导致创建子任务时报 constraint violation
2. actions.js isSystemTask() 没有豁免这两个 task_type，导致 goal_id required 报错

## 修复
1. migration 157：ALTER TABLE tasks DROP/ADD CHECK 约束，加入 `intent_expand` + `cto_review`
2. actions.js：systemTypes 数组加入 `intent_expand` + `cto_review`
3. selfcheck.js：EXPECTED_SCHEMA_VERSION 157
4. 3个测试文件同步更新版本号

## 成功标准
- POST /tasks/:id/request-intent-expand 不再报 constraint violation
- POST /tasks/:id/request-cto-review 同样
- migration 157 文件存在
