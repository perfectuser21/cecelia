# DoD: tick 感知模块移到 dispatch 前面

- [x] [BEHAVIOR] 感知模块在 thalamus 之前执行
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');const thaIdx=c.indexOf('0. Thalamus');const zsIdx=c.indexOf('僵尸巡检');const ppIdx=c.indexOf('Pipeline Patrol');const hcIdx=c.indexOf('Layer 2 运行健康监控');const icIdx=c.indexOf('Initiative 闭环检查');const krIdx=c.indexOf('KR 进度同步');if(zsIdx>thaIdx||ppIdx>thaIdx||hcIdx>thaIdx||icIdx>thaIdx||krIdx>thaIdx){console.error('FAIL');process.exit(1)}console.log('PASS')"

- [x] [ARTIFACT] 感知层注释标记存在
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('感知层：不受 canDispatch 限制'))process.exit(1);console.log('PASS')"

- [x] [BEHAVIOR] 各模块间隔检查逻辑未被修改
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');const checks=['zombieSweepElapsed >= ZOMBIE_SWEEP_INTERVAL_MS','pipelinePatrolElapsed >= PIPELINE_PATROL_INTERVAL_MS','healthCheckElapsed >= CLEANUP_INTERVAL_MS','krProgressElapsed >= CLEANUP_INTERVAL_MS'];for(const ck of checks){if(!c.includes(ck)){console.error('FAIL: '+ck);process.exit(1)}}console.log('PASS')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"
