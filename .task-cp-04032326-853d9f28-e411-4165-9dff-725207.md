---
id: task-cp-04032326-853d9f28-e411-4165-9dff-725207
type: task-card
branch: cp-04032326-853d9f28-e411-4165-9dff-725207
created: 2026-04-04
---

# Task Card: 重构 gatherFailureContext（圈复杂度 25 → 10）

## 需求（What & Why）
**功能描述**: 拆分 `packages/brain/src/monitor-loop.js` 中的 `gatherFailureContext` 函数，将圈复杂度从 25 降至 10 以下。
**背景**: 复杂度扫描发现该函数包含大量 `||` 短路运算符 + 嵌套 if 条件 + try/catch，总 CC=25，超出阈值（10）。
**不做什么**: 不改变函数的外部签名和返回值结构；不改动其他函数。

## 成功标准

- [x] [BEHAVIOR] `gatherFailureContext` 函数可调用，返回正确的 ctx 对象
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/monitor-loop.js','utf8');if(!c.includes('async function gatherFailureContext'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] 新增至少 2 个私有辅助函数（`_fetch` 前缀），分担原函数逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/monitor-loop.js','utf8');const m=c.match(/async function _fetch/g);if(!m||m.length<2)process.exit(1);console.log('ok')"

## 验收条件（DoD）

- [x] [BEHAVIOR] 重构后文件仍导出 `gatherFailureContext`（可被 callCortexForRca 调用）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/monitor-loop.js','utf8');if(!c.includes('gatherFailureContext'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] 提取出 `_fetchTaskMetadata`、`_fetchRunPayload`、`_fetchSimilarFailures` 三个子函数
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/monitor-loop.js','utf8');['_fetchTaskMetadata','_fetchRunPayload','_fetchSimilarFailures'].forEach(f=>{if(!c.includes(f)){console.error('missing:',f);process.exit(1)}});console.log('ok')"

## 实现方案
**要改的文件**: `packages/brain/src/monitor-loop.js`（第 237 行附近）
**受影响函数**: `gatherFailureContext`（拆分为 4 个函数）
**不改什么**: 函数签名、ctx 返回结构、其他所有函数
