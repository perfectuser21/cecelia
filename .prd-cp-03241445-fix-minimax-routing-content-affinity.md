# PRD: MiniMax 路由清理 + content-* EXECUTOR_AFFINITY 补全

## 问题

1. `token-budget-planner.js` 第 94-98 行：explore/research/talk/data/dept_heartbeat 还用 `primary: minimax`，与 task-router.js 路由到西安 Codex 矛盾
2. content-pipeline/content-research/content-generate/content-review/content-export 在 EXECUTOR_AFFINITY 中完全缺失

## 修改

文件：`packages/brain/src/token-budget-planner.js`
1. 注释改为"始终走 Codex（不消耗 Claude）"，minimax → codex
2. 补充 content-* 类型定义 → codex

## 成功标准

- token-budget-planner.js 不再有 minimax 引用
- content-* 类型在 EXECUTOR_AFFINITY 中有定义，路由到 codex
- CI 通过
