---
id: task-cp-03182115-add-goals-projects-api
type: task-card
branch: cp-03182115-add-goals-projects-api
created: 2026-03-18
---

# Task Card: 新增 GET /goals/:id + GET /projects/:id API 端点

## 需求（What & Why）

**功能描述**: 在 `packages/brain/src/routes.js` 新增两个只读 API 端点：`GET /goals/:id` 和 `GET /projects/:id`，返回对应表的单条记录。

**背景**: intent-expand skill 需要沿 `task.project_id → projects.kr_id → goals(KR).parent_id → goals(OKR).parent_id → goals(Vision)` 链路查询上下文。Brain 目前没有这两个端点，导致 intent-expand 无法查询 OKR 层级。

**不做什么**:
- 不新增数据库 migration（纯只读端点，不改 schema）
- 不 bump schema version
- 不做写操作（POST/PUT/DELETE）
- 不返回超出指定字段的数据（严格按规格）

## 成功标准

1. [ARTIFACT] `GET /api/brain/goals/:id` 返回 `{id, type, title, description, parent_id, project_id}` 字段
2. [ARTIFACT] `GET /api/brain/projects/:id` 返回 `{id, title, description, kr_id, goal_id}` 字段
3. [BEHAVIOR] 两个端点均有 404 处理（记录不存在时返回 `{"error": "goal not found"}` 或 `{"error": "project not found"}`）
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [ ] [ARTIFACT] `packages/brain/src/routes.js` 包含 `router.get('/goals/:id'` 路由
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes.js','utf8');if(!c.includes(\"router.get('/goals/:id'\"))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] `packages/brain/src/routes.js` 包含 `router.get('/projects/:id'` 路由
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes.js','utf8');if(!c.includes(\"router.get('/projects/:id'\"))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] GET /api/brain/goals/:id 对不存在 ID 返回 404
  Test: manual:curl -s -o /dev/null -w "%{http_code}" http://localhost:5221/api/brain/goals/00000000-0000-0000-0000-000000000000

- [ ] [BEHAVIOR] GET /api/brain/projects/:id 对不存在 ID 返回 404
  Test: manual:curl -s -o /dev/null -w "%{http_code}" http://localhost:5221/api/brain/projects/00000000-0000-0000-0000-000000000000

- [ ] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -10"

## 实现方案（Step 2 探索后填写）

**要改的文件**: `packages/brain/src/routes.js`
**Scope 锚定**: 仅在已有 router 上添加两个 GET 路由，不改其他逻辑
