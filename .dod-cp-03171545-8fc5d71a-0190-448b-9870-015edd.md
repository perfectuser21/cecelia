# DoD: 重启恢复集成测试套件 + 回归契约

## 验收清单

- [ ] D1: [BEHAVIOR] OOM/kill 孤儿测试 — dmesg 含 OOM 信号时，reason=oom_likely，status=failed
  - Test: executor-startup-sync.test.js > OOM/kill 孤儿（无论重试次数）→ reason=oom_likely 标记为 failed

- [ ] D2: [BEHAVIOR] 并发重启安全 — 第二次调用 syncOrphanTasksOnStartup 时不重复 requeue
  - Test: executor-startup-sync.test.js > 并发重启安全：第二次调用不会重复 requeue

- [ ] D3: regression-contract.yaml 新增 C-STARTUP-REQUEUE-001 条目（含 expires 字段）
  - Test: `grep -c 'C-STARTUP-REQUEUE-001' packages/quality/regression-contract.yaml`

- [ ] D4: CI 全量测试通过
  - Test: vitest run packages/brain/src/__tests__/executor-startup-sync.test.js
