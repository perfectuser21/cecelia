---
id: task-cp-03182112-delete-workflows-cto-review
type: task-card
branch: cp-03182112-delete-workflows-cto-review
created: 2026-03-18
---

# Task Card: 删除 packages/workflows/skills/cto-review/（位置错误）

## 需求（What & Why）
**功能描述**: 删除误放在 `packages/workflows/skills/cto-review/` 的 cto-review skill 目录。
**背景**: cto-review skill 的正确位置是 `packages/engine/skills/cto-review/`（已存在）。在某次合并中，错误地将同一份内容也合并到了 `packages/workflows/skills/cto-review/`，导致重复。
**不做什么**: 不修改 engine 中的 cto-review，不修改任何其他文件。

## 成功标准

1. [ARTIFACT] `packages/workflows/skills/cto-review/` 目录不再存在
2. [ARTIFACT] `packages/engine/skills/cto-review/` 目录保持完整不变
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] packages/workflows/skills/cto-review/ 目录已删除
  Test: manual:node -e "const fs=require('fs');if(fs.existsSync('packages/workflows/skills/cto-review'))process.exit(1);console.log('OK');"

- [x] [ARTIFACT] packages/engine/skills/cto-review/SKILL.md 仍然存在
  Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/engine/skills/cto-review/SKILL.md'))process.exit(1);console.log('OK');"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案
**要改的文件**: 删除 `packages/workflows/skills/cto-review/`（整个目录）
**Scope 锚定**: 仅删除 workflows 下的错误副本，不做其他修改
