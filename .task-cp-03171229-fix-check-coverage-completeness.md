---
id: task-cp-03171229-fix-check-coverage-completeness
type: task-card
branch: cp-03171229-fix-check-coverage-completeness
created: 2026-03-17
---

# Task Card: 补 check-coverage-completeness.mjs CI 接入与测试

## 需求（What & Why）
**功能描述**:
- `check-coverage-completeness.mjs` 脚本已存在于 `packages/engine/scripts/devgate/`（commit 69800a629），但没有接入任何 CI，也没有对应的测试文件。
- 需要：① 写测试文件；② 接入 `ci-l2-consistency.yml` 的 `engine-l2` job；③ bump engine 版本。

**背景**: PR #1004 关闭时并行 agent 未完整合并所有文件，后来 69800a629 虽然补了脚本文件本身，但 CI 接入和测试仍缺失。

**不做什么**:
- 不修改 `check-coverage-completeness.mjs` 脚本内容
- 不接入 `brain-l2`（该脚本是 engine 工具，属于 `engine-l2`）

## 成功标准

1. [ARTIFACT] `packages/engine/tests/devgate/check-coverage-completeness.test.ts` 测试文件存在，覆盖 checkHooksCoverage / checkSrcCoverage / checkDevgateCoverage 三个核心函数
2. [ARTIFACT] `ci-l2-consistency.yml` 的 `engine-l2` job 新增步骤调用 `node packages/engine/scripts/devgate/check-coverage-completeness.mjs`
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] 测试文件存在于正确位置
  Test: manual:bash -c "ls packages/engine/tests/devgate/check-coverage-completeness.test.ts"

- [x] [ARTIFACT] CI 配置中有接入脚本的步骤
  Test: manual:bash -c "grep -c 'check-coverage-completeness' .github/workflows/ci-l2-consistency.yml"

- [x] [BEHAVIOR] 脚本能在 dry-run 模式下正常执行不报错
  Test: manual:bash -c "cd packages/engine && node scripts/devgate/check-coverage-completeness.mjs --dry-run 2>&1 | grep -c 'Coverage Completeness Check'"

- [x] [GATE] 所有现有测试通过，版本文件同步
  Test: manual:bash -c "cd packages/engine && bash ci/scripts/check-version-sync.sh 2>&1 | grep -c '✅'"

## 实现方案（Step 2 探索后填写）
**要改的文件**:
- 新建：`packages/engine/tests/devgate/check-coverage-completeness.test.ts`
- 修改：`.github/workflows/ci-l2-consistency.yml`（engine-l2 job 末尾新增步骤）
- 修改：`packages/engine/package.json`（version 12.94.0 → 12.95.0）
- 修改：`packages/engine/VERSION`（同步版本）
- 修改：`packages/engine/features/feature-registry.yml`（changelog 新增条目）
- 修改：`packages/engine/.brain-versions`（版本同步）
- 修改：`DEFINITION.md`（版本同步，如有 engine 版本引用）

**Scope 锚定**: Engine tests + CI workflow + version bump
