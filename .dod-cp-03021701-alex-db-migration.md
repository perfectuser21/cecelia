---
id: dod-alex-db-migration
version: 1.0.0
created: 2026-03-02
---

# DoD: Alex 个人数据表迁移

## 验收清单

- [ ] migration 文件 `100_alex_personal_tables.sql` 存在于 `packages/brain/migrations/`
  - Test: `ls packages/brain/migrations/100_alex_personal_tables.sql`

- [ ] migration 成功执行，三张新表存在
  - Test: `docker exec cecelia-postgres psql -U cecelia -d cecelia -c "\dt areas ideas knowledge"`

- [ ] `notes` 表有 `owner` 字段，默认值 'cecelia'
  - Test: `docker exec cecelia-postgres psql -U cecelia -d cecelia -c "\d notes" | grep owner`

- [ ] `areas` 表结构正确（id, name, domain, archived, notion_id, created_at, updated_at）
  - Test: `docker exec cecelia-postgres psql -U cecelia -d cecelia -c "\d areas"`

- [ ] `ideas` 表结构正确（id, title, content, intent_type, status, area_id, parent_id, notion_id, created_at, updated_at）
  - Test: `docker exec cecelia-postgres psql -U cecelia -d cecelia -c "\d ideas"`

- [ ] `knowledge` 表结构正确（id, name, type, status, sub_area, content, version, area_id, notion_id, created_at, updated_at）
  - Test: `docker exec cecelia-postgres psql -U cecelia -d cecelia -c "\d knowledge"`

- [ ] 迁移脚本 `migrate-from-notion.mjs` 存在且可运行
  - Test: `node packages/brain/scripts/migrate-from-notion.mjs --dry-run`
