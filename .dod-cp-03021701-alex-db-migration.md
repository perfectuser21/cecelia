---
id: dod-alex-db-migration
version: 1.0.0
created: 2026-03-02
---

# DoD: Alex 个人数据表迁移

## 验收清单

- [x] migration 文件 `100_alex_personal_tables.sql` 存在于 `packages/brain/migrations/`
  Test: manual:bash -c "test -f packages/brain/migrations/100_alex_personal_tables.sql && echo ok"

- [x] migration 成功执行，ideas 和 knowledge 表存在
  Test: manual:psql -U cecelia -d cecelia -c "\dt ideas knowledge"

- [x] `notes` 表有 `owner` 字段，默认值 'cecelia'
  Test: manual:psql -U cecelia -d cecelia -c "\d notes" | grep owner

- [x] `areas` 表新增 domain、archived、notion_id 字段
  Test: manual:psql -U cecelia -d cecelia -c "SELECT column_name FROM information_schema.columns WHERE table_name='areas' AND column_name IN ('domain','archived','notion_id')" | grep domain

- [x] `ideas` 表结构正确（id, title, content, intent_type, status, area_id, parent_id, notion_id, created_at, updated_at）
  Test: manual:psql -U cecelia -d cecelia -c "\d ideas" | grep notion_id

- [x] `knowledge` 表结构正确（id, name, type, status, sub_area, content, version, area_id, notion_id, created_at, updated_at）
  Test: manual:psql -U cecelia -d cecelia -c "\d knowledge" | grep notion_id

- [x] 迁移脚本 `migrate-from-notion.mjs` 存在
  Test: manual:node -e "require('fs').existsSync('packages/brain/scripts/migrate-from-notion.mjs') || process.exit(1)"
