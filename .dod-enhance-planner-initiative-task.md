## DoD - 增强 Planner：识别有 Initiative 无 Task 的 KR 并自动派发

### 验收清单

- [ ] **scoreKRs() 新增判断**
  - 当 KR 下有 active Initiative（projects 表中 type='initiative' 且 status='active'）但无 queued Task 时，score += 15
  - Test: `scoreKRs_initiative_bonus.test.js` 覆盖此场景

- [ ] **selectTargetProject() 增强**
  - 无 queued Task 时，优先选有 Initiative sub-project（parent_id 指向当前项目）的项目
  - Test: `selectTargetProject_initiative_priority.test.js`

- [ ] **新增 generateInitiativePlanTask() 函数**
  - 接收 kr、initiative 对象
  - 向 DB 插入 initiative_plan 类型任务
  - 返回新建的任务对象
  - Test: 覆盖成功创建路径

- [ ] **generateNextTask() 调用 generateInitiativePlanTask()**
  - 无 queued/in_progress task 时，检查是否有 Initiative
  - 有 Initiative → 调用 generateInitiativePlanTask()
  - Test: 覆盖无 task 但有 Initiative 的场景

- [ ] **所有现有测试不被破坏**
  - `npm test` 全部通过

- [ ] **新增测试文件**
  - `packages/brain/src/__tests__/planner-initiative-plan.test.js`
  - 至少 4 个测试用例覆盖新功能
