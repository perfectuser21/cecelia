---
id: task-cp-03201703-70fe8019-0572-430f-a395-a15767
type: task-card
branch: cp-03201703-70fe8019-0572-430f-a395-a15767
created: 2026-03-20
---

# Task Card: 修复 heartbeat 垃圾循环

## 需求（What & Why）
**功能描述**: 修复 dept_heartbeat 任务的三个问题：路由错误、防重复漏洞、垃圾数据清理
**背景**: heartbeat 任务被错误路由到 Codex Bridge 导致执行失败，同时防重复检查缺少 quarantined 状态判断，现有数据库中存在大量 quarantined heartbeat 任务需要清理
**不做什么**: 不修改 heartbeat 的核心逻辑，只修复路由和防重复机制

## 成功标准
> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [ARTIFACT] dept_heartbeat 加入 US_ONLY_TYPES 防止错误路由
2. [BEHAVIOR] 防重复检查正确处理 quarantined 状态的任务
3. [BEHAVIOR] 清理现有数据库中的 quarantined heartbeat 任务
4. [GATE] CI 全部通过

## 验收条件（DoD）
> Test 字段在 Stage 2 探索后填写，现在写条目即可。

- [ ] [ARTIFACT] executor.js 的 US_ONLY_TYPES 数组包含 dept_heartbeat
  Test: manual:node -e "const r=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!r.includes('dept_heartbeat'))process.exit(1)"

- [ ] [ARTIFACT] 防重复检查逻辑包含 quarantined 状态判断
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/dept-heartbeat.js','utf8');if(!c.includes('quarantined'))process.exit(1)"

- [ ] [BEHAVIOR] 数据库中不存在 status='quarantined' 且 task_type='dept_heartbeat' 的任务
  Test: manual:node -e "const pg=require('pg');const c=new pg.Client({connectionString:process.env.DATABASE_URL||'postgresql://localhost/cecelia'});c.connect().then(()=>c.query(\"SELECT count(*) FROM tasks WHERE status='quarantined' AND task_type='dept_heartbeat'\")).then(r=>{if(parseInt(r.rows[0].count)>0)process.exit(1);c.end()}).catch(()=>process.exit(1))"

- [ ] [BEHAVIOR] heartbeat 任务不再错误路由到 Codex Bridge
  Test: manual:node -e "const r=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!r.includes('dept_heartbeat'))process.exit(1)"

- [ ] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案（Stage 2 探索后填写）
**要改的文件**:
- packages/brain/src/executor.js: 添加 dept_heartbeat 到 US_ONLY_TYPES
- packages/brain/src/dept-heartbeat.js: 防重复检查添加 quarantined 状态
- scripts/cleanup-quarantined-heartbeat.js: 清理脚本（已执行）

**Scope 锚定**: Brain 路由和心跳任务管理