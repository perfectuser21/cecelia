# Task Card: refactor(brain) — 重构 analyzeDeep 提取子函数

## 任务描述

将 `cortex.analyzeDeep`（216 行，圈复杂度 55）拆解为 6 个职责单一的私有子函数，
主函数体缩短至 ≤ 40 行，行为完全不变。

## 成功标准

- analyzeDeep 主体 ≤ 40 行
- 6 个子函数全部存在
- 所有 cortex 测试通过

## DoD

- [x] [PRESERVE] 所有现有 cortex 测试通过（17 文件，153 tests）
    Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');if(!c.includes('_fetchDecisionHistory'))process.exit(1)"

- [x] [BEHAVIOR] analyzeDeep 主体 ≤ 40 行
    Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');const lines=c.split('\n');const start=lines.findIndex(l=>l.includes('async function analyzeDeep('));if(start<0)process.exit(1);let depth=0,end=start;for(let i=start;i<lines.length;i++){depth+=(lines[i].match(/\{/g)||[]).length-(lines[i].match(/\}/g)||[]).length;if(i>start&&depth<=0){end=i;break}};if(end-start>42)process.exit(1)"

- [x] [BEHAVIOR] 6 个子函数存在
    Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');['_fetchDecisionHistory','_fetchSystemStatus','_enrichContextWithCrossTaskPatterns','_buildAnalysisContext','_handleLLMSuccess','_handleLLMError'].forEach(f=>{if(!c.includes(f))process.exit(1)})"

- [x] [ARTIFACT] cortex.js 包含所有 6 个子函数定义（async function _xxx 格式）
    Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');if((c.match(/async function _\w+\(/g)||[]).length<6)process.exit(1)"

- [x] [GATE] cortex.js 文件存在且包含 _handleLLMError
    Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');if(!c.includes('_handleLLMError'))process.exit(1)"
