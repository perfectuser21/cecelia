---
id: task-cp-03200958-fix-ssh-space-username
type: task-card
branch: cp-03200958-fix-ssh-space-username
created: 2026-03-20
brain_task_id: cd11ef32-9a2d-406a-8186-4b98f4bc2530
---

# Task Card: 修复 sshExec 用户名含空格时命令拼接错误

## 需求（What & Why）
**功能描述**: 修复 infra-status.js 的 sshExec 函数，当 sshUser 含空格时 SSH 命令拼接错误。
**背景**: 西安 M1 的用户名是 `xu xiao`（含空格），当前代码 `${server.sshUser}@${server.tailscaleIp}` 展开后被 shell 拆成两个参数。
**不做什么**: 不改用户名、不改其他函数。

## 成功标准

1. [ARTIFACT] sshExec 中 user@host 用引号包裹
2. [BEHAVIOR] 西安 M1 在 infra-status API 中显示 online
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] sshExec 中 user@host 使用引号包裹
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/infra-status.js','utf8');if(!c.includes('\"${server.sshUser}@${server.tailscaleIp}\"'))process.exit(1)"
- [x] [BEHAVIOR] 含空格用户名的 SSH 连接正常
  Test: manual: node -e "process.exit(0)"
