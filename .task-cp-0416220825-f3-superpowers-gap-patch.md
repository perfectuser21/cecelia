---
id: task-cp-0416220825-f3-superpowers-gap-patch
type: task-card
branch: cp-0416220825-f3-superpowers-gap-patch
task_id: 572df3f3-e14f-4775-a6ca-1161f06ef853
created: 2026-04-16
---

# Task Card: F3 — 补 Superpowers 三个核心缺口

## 需求
Explore agent 一比一对比报告的 High 缺口包。三块 prompt 指令追加到 02-code.md 的 Implementer 角色定义。

## 实现方案
**改文件**: `packages/engine/skills/dev/steps/02-code.md`
追加新 section `## Implementer 必须遵守的 Superpowers 纪律（v14.16.0 补全）`，内含三块：
1. Condition-Based Waiting（禁 setTimeout/sleep，用 waitFor）
2. Pre-Completion Verification（报 DONE 前必跑命令 + 贴证据）
3. Root-Cause Tracing（bug fix 向上追 4 步）

**版本 bump**: Engine 14.15.1 → 14.16.0（minor，新增规则）。用 `bump-version.sh` 一键同步 6 文件。

**feature-registry**: 加 14.16.0 changelog 条目。

## 不做
- 不改 Implementer 派遣机制
- 不引入自动化 scanner 工具
- 不碰 01/03/04 step 文件
- 不补 receiving-code-review / defense-in-depth（留下一 PR）

## 验收条件 DoD
- [x] [ARTIFACT] 02-code.md 含三块标题关键词
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');['Condition-Based Waiting','Pre-Completion Verification','Root-Cause Tracing'].forEach(t=>{if(!c.includes(t))process.exit(1)})"
- [x] [ARTIFACT] 含 waitFor 示例
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('waitFor')||!c.includes('setTimeout'))process.exit(1)"
- [x] [ARTIFACT] Engine 14.16.0 六文件同步
  Test: manual:bash -c "v=\$(cat packages/engine/VERSION);[[ \$v == 14.16.0 ]] && grep -q \"\\\"\$v\\\"\" packages/engine/package.json && grep -q \"^\$v\" packages/engine/.hook-core-version && grep -q \"^\$v\" packages/engine/hooks/VERSION"
- [x] [ARTIFACT] feature-registry 14.16.0 changelog
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/feature-registry.yml','utf8');if(!c.includes('14.16.0'))process.exit(1)"
