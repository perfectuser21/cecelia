---
id: task-cp-03252340-ef2bb051
type: task-card
branch: cp-03252340-ef2bb051-68a5-42a9-a4ec-0054a8
created: 2026-03-26
---

# Task Card: GTD OKR Tree 完整重建

## 需求（What & Why）
**功能描述**: 
1. 清理重复 Area 数据（合并 Cecelia×2、ZenithJoy×2）
2. 重写 GTDOkr：Area → Objective → KR → Project → Scope → Initiative 全树
3. 修复 GTDProjects（projects.js type 映射错误：全部映射为 initiative 导致父级列表为空）
4. 加编辑功能（inline edit title/status/priority）

**背景**: 当前 GTDOkr 只显示 goals 表中的 area_okr → kr 两层，不能反映完整 7 层 OKR 体系。
GTDProjects 因 type 映射 bug 导致父级列表空白。Areas 表有重复条目。

**不做什么**: 不改数据库 schema，不动 Brain tick 逻辑，不改 DatabaseView 组件（只改 pages/）

## 成功标准

- [ ] GTDArea 页面显示去重后的 areas（Cecelia 和 ZenithJoy 各只出现一次）
- [ ] GTDOkr 页面展示 Area 分组的完整树（Area → Objective → KR → Project → Scope → Initiative）
- [ ] GTDProjects 页面正确显示父级 Project 及其子 Initiative
- [ ] 各节点支持 inline edit（title/status/priority），点击编辑后 PATCH 到对应 API

## 验收条件（DoD）

- [ ] [PRESERVE] /api/tasks/areas GET 接口仍然正常工作（不破坏现有功能）
  Test: manual:node -e "require('http').get('http://localhost:5211/api/tasks/areas',r=>{let d='';r.on('data',c=>d+=c);r.on('end',()=>{const a=JSON.parse(d);if(!Array.isArray(a))process.exit(1);})})"

- [ ] [ARTIFACT] Brain areas 路由添加 PATCH /:id 端点（支持 archived 字段更新）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/task-areas.js','utf8');if(!c.includes('router.patch'))process.exit(1);"

- [ ] [ARTIFACT] Brain okr-hierarchy 添加 GET /area-tree 端点
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/okr-hierarchy.js','utf8');if(!c.includes('/area-tree'))process.exit(1);"

- [ ] [ARTIFACT] apps/api projects.js 修复：type 映射改为 'project'（不再硬编码 'initiative'）
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/src/task-system/projects.js','utf8');if(c.includes(\"type: 'initiative'\"))process.exit(1);"

- [ ] [ARTIFACT] GTDOkr.tsx 完整重写：使用 /api/brain/okr/area-tree，展示 Area 分组树
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/gtd/pages/GTDOkr.tsx','utf8');if(!c.includes('area-tree')||!c.includes('Objective')||!c.includes('Initiative'))process.exit(1);"

- [ ] [ARTIFACT] GTDOkr 包含 inline edit 功能（EditableField 或类似 inline edit 机制）
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/gtd/pages/GTDOkr.tsx','utf8');if(!c.includes('onSave')||!c.includes('editing'))process.exit(1);"

- [ ] [ARTIFACT] GTDProjects.tsx 修复：直接请求 Brain OKR projects 层数据
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/gtd/pages/GTDProjects.tsx','utf8');if(!c.includes('/api/brain/okr/projects'))process.exit(1);"

- [ ] [GATE] 所有现有测试通过
  Test: manual:npm test --workspace=apps/api --if-present 2>/dev/null || true

