# 记忆系统 PR13 — Rumination 阈值对齐：salience 过滤升级与 PR9 的 8 维度对齐

## 背景

PR9 将 computeSalience 扩展到 8 个维度，但 rumination.js 的过滤阈值仍是旧的单一 0.7 硬阈值。

## 成功标准

- classifySaliencePriority 函数存在，返回 HIGH/MID/LOW/SKIP
- fetchMemoryStreamItems 使用 0.55 最低阈值
- allItems 按优先级排序（HIGH 先，LOW 后）
- 向后兼容（无 salience_score 的旧记录安全降级）
- 新增单元测试覆盖各优先级分桶
