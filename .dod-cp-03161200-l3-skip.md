---
id: dod-cp-03161200-l3-skip
version: 1.0.0
created: 2026-03-16
updated: 2026-03-16
changelog:
  - 1.0.0: 初始版本
---

# DoD: L3 CI 按 Commit Type 智能跳过

## 成功标准

## 验收清单

- [ ] [ARTIFACT] `ci-l3-code.yml` 包含 `detect-commit-type` job，有 `commit_type` 和 `should_run_l3` 两个 output
- [ ] [ARTIFACT] `brain-unit` / `engine-l3` / `workspace-l3` / `quality-l3` / `workflows-l3` 均已添加 `needs: detect-commit-type` 和 `should_run_l3` 的 if 条件
- [ ] [BEHAVIOR] `fix:` 类型 PR 运行后，`detect-commit-type` job 输出 `should_run_l3=false`，所有 L3 sub-jobs 为 skipped 状态
- [ ] [BEHAVIOR] `feat:` 类型 PR 运行后，`should_run_l3=true`，L3 正常运行（本 PR 即为验证）
- [ ] [GATE] Engine 版本已 bump 到 12.88.0（5 个文件全部同步）
- [ ] [GATE] CI L1 + L2 + L3 + L4 全部通过

## 测试命令

```bash
# 验证 detect-commit-type job 存在
grep -c "detect-commit-type" .github/workflows/ci-l3-code.yml

# 验证 should_run_l3 输出
grep -c "should_run_l3" .github/workflows/ci-l3-code.yml

# 验证版本 bump
grep "12.88.0" packages/engine/VERSION

# 验证 feature-registry.yml 更新
grep -c "l3-commit-type-skip" packages/engine/features/feature-registry.yml
```
