# PRD: 清理废弃 Workflow Skills + 完善 .gitignore

## 背景
随着系统演进，`packages/workflows/skills/` 目录下积累了大量已废弃或迁移的 skill 文件。
这些文件占用仓库空间、造成代码噪音，需要统一清理。
同时 `.gitignore` 缺少对开发日志目录的忽略规则，需补充。

## 需求

### A. 清理废弃 Skills
删除以下已废弃 skill 的 SKILL.md 及相关脚本：
- batch-luxury-card-generator
- batch-notion-analyzer
- content-analyzer
- content-creator
- content-rewriter
- creator（旧版）
- image-gen-workflow
- luxury-card-generator
- two-layer-parallel-analyzer

### B. 完善 .gitignore
补充以下目录/文件的忽略规则：
- dev-execution-log/
- dev-seal/
- test-phase1/

### C. 新增 devgate 脚本
添加 `scripts/devgate/check-manual-cmd-whitelist.cjs` 白名单校验脚本。

## 成功标准
- 废弃 skill 目录的 SKILL.md 和脚本文件已从仓库删除
- `.gitignore` 包含 dev-execution-log、dev-seal、test-phase1 忽略规则
- `check-manual-cmd-whitelist.cjs` 脚本已提交到 scripts/devgate/
- skill-index.md 更新，移除废弃 skill 索引记录
- CI 通过，无功能回归
- orchestrator 存储 rule_scores 和 llm_reviewed 到 tasks.payload
- /stages API 返回 rule_scores 和 llm_reviewed 字段
- entry 中正确附加 rule_scores 和 llm_reviewed

# PRD: Pipeline 审核评分 DB 存储和 API 返回

## 成功标准
- orchestrator 存储 rule_scores 和 llm_reviewed 到 tasks.payload
- /stages API 返回 rule_scores 和 llm_reviewed 字段
- entry 中正确附加 rule_scores 和 llm_reviewed
