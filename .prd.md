# PRD: Cortex persist await + Capacity 硬顶

## 背景
1. cortex-dedup-persist 测试 flaky：_persistReflectionEntry 是 fire-and-forget，测试用 setTimeout(200ms) 等待写入完成，CI 环境下不可靠
2. capacity.js 的 getMaxStreams() 在高配机器上可能返回过大的值，需要 MAX_PHYSICAL_CAP 硬顶

## 目标
- 消除 cortex-dedup-persist 测试的 flaky 问题
- 为 capacity 并行数增加物理硬顶
