## PRD - 删除全部 Python 代码 + 死迁移文件

**需求来源**: 用户确认所有 Python 代码都是旧架构遗留，全部删除
**功能描述**: 彻底移除 Python Support Service 及相关文件，清理 CI 和文档

**涉及文件**:

删除目录:
- `src/` — 整个 Python 源码目录（api, db, state）
- `tests/test_agent_monitor.py`, `tests/test_patrol_api.py`, `tests/conftest.py` — Python 测试（保留 tests/database/ 和 tests/frontend/）
- `requirements.txt` — Python 依赖

删除文件:
- `brain/migrations/003_trigger_source.sql` — 死迁移文件（与 004 完全相同，永远不会执行）

修改 CI:
- `.github/workflows/ci.yml` — 删除 semantic-test job，从 ci-passed 和 notify-failure 移除引用
- GitHub Branch Protection — 从 required checks 移除 "Semantic Brain (Python)"

修改文档:
- `README.md` — 移除 Python Support Service 引用
- `DEFINITION.md` — 移除 Python 支持服务描述（如果有）
- `.gitignore` — 可保留 __pycache__ 规则（不碍事）

**成功标准**:
1. src/ 目录不存在
2. Python 测试文件已删除（JS 测试保留）
3. requirements.txt 不存在
4. 003_trigger_source.sql 不存在
5. CI 无 Python job，仍能正常通过
6. Branch protection 不再要求 Python check
7. README 无 Python/5220 引用
8. Brain 测试不回归
9. DevGate 通过

**非目标**:
- 不修改 Brain 核心代码
- 不删除 tests/database/ 和 tests/frontend/ 下的 JS 测试
