---
id: prd-cp-03171550-fix-l2-concurrency-group
version: 1.0.0
created: 2026-03-17
---

## PRD - fix: L2 concurrency group 去掉 run_number，恢复正确并发取消语义

**功能描述**: 将 ci-l2-consistency.yml 的 concurrency group 从 `l2-v2-${{ github.ref }}-${{ github.run_number }}` 改回 `l2-${{ github.ref }}`，保留 cancel-in-progress: true，使同分支的新 push 能正确取消旧的 pending/running L2 jobs。

## 成功标准
- [ ] ci-l2-consistency.yml 的 concurrency.group 不含 run_number
- [ ] cancel-in-progress: true 保留
