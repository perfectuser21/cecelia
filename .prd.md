---
id: task-T-8A364B
version: 1.0.0
created: 2026-01-30
---

# PRD: 添加 /ping 端点到 Brain API

## 功能描述

在 src/api/main.py 中添加一个简单的 /ping 端点，返回 'pong' 字符串。这是一个轻量级的存活检查端点，无需依赖任何服务初始化状态。

## 成功标准

1. GET /ping 返回 200 状态码
2. 响应体为 { "message": "pong" }
3. 端点无需认证，无需服务初始化即可响应
4. pytest 测试通过
