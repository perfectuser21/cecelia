## PRD - 审计修复：pool.py DB defaults + DEFINITION.md 版本 + 路径修正

**需求来源**: 第二轮 5-agent 并行审计发现的遗留问题
**功能描述**: 修复 pool.py 中的旧数据库默认值、DEFINITION.md 正文中的过期版本号、regression-contract.yaml 中的旧路径

**涉及文件**:

修复 DB 配置:
- `src/db/pool.py` — 默认值仍为旧 n8n 时代（cecelia_tasks/n8n_user/n8n_password_2025），需改为 cecelia/cecelia/CeceliaUS2026

修复文档版本:
- `DEFINITION.md` — 第 483 行和第 646 行仍引用 1.9.5，需更新为当前版本

修复路径:
- `docs/regression-contract.yaml` — 第 9 行引用旧路径 /home/xx/dev/cecelia-quality/，需改为 /home/xx/perfect21/cecelia-quality/

**成功标准**:
1. pool.py 的 DB 默认值与 brain/src/db-config.js 一致（database=cecelia, user=cecelia）
2. DEFINITION.md 正文中所有版本号与 package.json 一致
3. regression-contract.yaml 使用正确的 /home/xx/perfect21/ 路径
4. 所有现有测试通过
5. DevGate 通过

**非目标**:
- 不修改 Brain 核心代码
- 不修改 .claude/learning/ 中的历史文件（那些是归档记录）
