# Task Card: Codex skill — codex_test_gen 自动生成测试

## 需求（What & Why）
**功能描述**: 新增 codex_test_gen task_type 和对应 SKILL.md，让 Codex 自动扫描覆盖率低的模块并生成测试。注册到 task-router（location=xian）、executor（skillMap）、role-registry（CTO task_types）。

**背景**: Codex 只有 3 种任务类型（codex_qa/codex_dev/codex_playwright），大部分时间空闲。codex_test_gen 是第一个新的 Codex 任务类型，直接推动免疫系统 KR（自修事件≥200次）。

**不做什么**: 不改 Codex bridge、不改调度逻辑

## 成功标准
1. [ARTIFACT] SKILL.md 定义 codex_test_gen 执行流程
2. [ARTIFACT] task-router.js 注册 codex_test_gen → location=xian
3. [ARTIFACT] executor.js skillMap 包含 codex_test_gen
4. [GATE] 所有现有测试通过

## 验收条件（DoD）
- [x] [ARTIFACT] packages/workflows/skills/codex-test-gen/SKILL.md 存在
  Test: manual:node -e "require('fs').accessSync('packages/workflows/skills/codex-test-gen/SKILL.md')"

- [x] [BEHAVIOR] task-router.js VALID_TASK_TYPES 包含 codex_test_gen
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/task-router.js','utf8');if(!c.includes('codex_test_gen'))process.exit(1)"

- [x] [BEHAVIOR] executor.js skillMap 包含 codex_test_gen
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('codex_test_gen'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"
