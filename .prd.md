## PRD - 深度审计清理：删除旧 Python 基础设施 + 修复过时文档配置

**需求来源**: 用户要求深度审计后，5 个并行 Agent 发现的问题清单
**功能描述**: 清理 PR #162 之后仍遗留的旧 Python 服务基础设施文件、修复过时文档引用、清理死代码和配置

**涉及文件**:

删除（旧 Python 服务基础设施）:
- `Dockerfile` — 旧 Python 服务 Dockerfile（引用已删除的 sor/ 目录）
- `scripts/start.sh` — 旧 Python 启动脚本
- `brain.service` — 错误路径的 systemd service

修复文档:
- `README.md` — 引用已删除的 Python Intelligence Service 5220
- `DOCKER.md` — 30+ 处引用已删除的 semantic-brain 服务

修复配置:
- `scripts/verify-deployment.sh` — 检查已删除的 cecelia-semantic-brain 容器
- `docs/regression-contract.yaml` — 引用已删除的 parser/scheduler 模块
- `.gitignore` — 缺少 `__pycache__/` 和 `*.pyc`

清理代码:
- `tests/conftest.py` — 删除未使用的 temp_dir fixture 和 OPENAI_API_KEY
- `requirements.txt` — 移除 `openai>=1.10.0`（未被实际 import）
- `brain/src/actions.js` — 移除孤立导出 `logDecision`

**成功标准**:
1. 所有旧 Python 服务基础设施文件已删除
2. README.md 和 DOCKER.md 反映当前架构（Node.js Brain only）
3. verify-deployment.sh 不再检查已删除的容器
4. 所有现有测试仍然通过
5. DevGate（facts-check, version-sync）通过

**非目标**:
- 不修改 frontend-proxy.js（已确认是活跃组件）
- 不修改 orchestrator_routes.py（用户要求保留供将来 Node.js 集成）
- 不修改 Brain 核心代码逻辑
