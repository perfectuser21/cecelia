# 清理旧概念 — 删除 exploratory 评分逻辑 + pr_plans 架构

## 功能描述

清理代码库中已过时的两个旧概念：
1. **exploratory 评分逻辑**：Content-aware score、phase 排序（exploratory→dev）
2. **pr_plans 架构**：三层拆解调度、PR Plans API 端点、pr_plans 表

## 成功标准

- planner.js: 删除 applyContentAwareScore() 函数 + Content-aware 常量
- planner.js: 删除 phase 排序逻辑（exploratory/dev score）
- planner.js: 删除所有 pr_plans 相关函数和 dispatch 块
- routes.js: 删除 exploratory phase transition 回调代码（5b + 5b2）
- routes.js: 删除 pr-plans API 端点（POST/GET/PATCH/DELETE）
- tick.js: 删除 pr_plans completion check 代码块
- migration 058: DROP TABLE pr_plans + tasks.pr_plan_id 列
- selfcheck.js: schema version 057 → 058
- 删除所有 exploratory/pr-plans 相关测试文件
- npm test 全部通过
- DevGate 三项检查全部通过
