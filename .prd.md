# PRD: 修复 Brain CI macOS 测试兼容性

## 功能描述
brain-ci.yml 从 ubuntu-latest 改为 macos-latest 后，11 个测试失败。
需要修复测试使其跨平台兼容。

## 成功标准
- cpu-sampler.test.js: mock platform-utils 而非 fs.readFileSync
- cortex-feedback-loop.test.js: 补齐 output dedup 的 DB query mock
- minimax-provider.test.js: mock checkServerResources 返回 ok=true
- npm test 全部通过
