# 统一 /dev skill 版本号策略

## 功能描述

同步 workflows 包和 engine 包的 /dev skill 文件，统一使用 auto-version.yml 自动版本管理策略。消除手动 bump 版本号的过时指令，防止 /dev 流程中浪费时间在不必要的版本冲突上。

## 成功标准

1. workflows 包的 SKILL.md 和 08-pr.md 不再指示手动 bump 版本
2. engine 包的 SKILL.md（已在全局生效）保持正确的自动化版本策略
3. 两个包的版本号策略描述一致
