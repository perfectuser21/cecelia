---
id: dept-head-db-and-tick
version: 1.0.0
created: 2026-02-23
---

# PRD: 部门主管架构 - DB 变更 + Brain Tick 集成

## 背景

部门主管架构实施路径 Step 1 + Step 3。
Step 2（SKILL.md + agents/repo-lead.md）已完成。

## Step 1：三个 DB Migrations

### 065：device_locks 表
### 066：tasks.created_by + tasks.dept
### 067：dept_configs 表 + ZenithJoy 默认配置

## Step 3：Brain Tick 集成

- 新增 brain/src/dept-heartbeat.js
- 修改 tick.js 末尾调用 triggerDeptHeartbeats()
- 新增 Device Lock API（server.js）

## 验收标准

- [ ] 3 个 migration 运行成功
- [ ] dept_configs 有 zenithjoy 记录
- [ ] triggerDeptHeartbeats() 正确创建 dept_heartbeat tasks
- [ ] device-locks API 正常工作
- [ ] npm test 通过
