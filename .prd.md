## PRD - 更新 Schema Version 常量到 037

**需求来源**: 已成功执行 Migration 036 和 037，需要同步更新代码中的 EXPECTED_SCHEMA_VERSION 常量

**功能描述**:
将 `brain/src/selfcheck.js` 中的 `EXPECTED_SCHEMA_VERSION` 从 '035' 更新为 '037'，以匹配新执行的数据库 migrations。

**相关 Migrations**:
- Migration 036: Fix goal hierarchy - 插入 Global KR 层，修正 Area OKR parent_id 关系
- Migration 037: Capability Registry - 创建 system_capabilities 和 capability_runs 表

**涉及文件**:
- `brain/src/selfcheck.js` (修改第 18 行)

**成功标准**:
1. `EXPECTED_SCHEMA_VERSION` 值为 '037'
2. selfcheck 通过（`npm run selfcheck` 成功）
3. 所有相关测试通过
4. DevGate facts-check 通过（DEFINITION.md 中的 schema_version 已更新）

**非目标**:
- 不修改其他文件
- 不添加新功能
- 不执行新的 migrations
