---
id: task-d1029612-8719-43e9-88da-aa5e540ce23f
type: task-card
branch: cp-03291434-brain-integration-tests
created: 2026-03-29
---

# Task Card: Brain 关键路由集成测试补全

## 需求（What & Why）

**功能描述**：给 Brain 最关键的 4 个路由补充集成测试，使用真实 PostgreSQL 连接（非 mock），覆盖核心路由的端到端行为。

**背景**：
- Brain 路由目前只有单元测试（mock DB），几乎没有集成测试
- `/context` 路由曾有 SQL bug（已修复），但无测试保护，未来变更可能再次引入
- CI L3 已有 `brain-unit` job 含真实 PostgreSQL，且有 `integration-test-required` 门禁要求 Brain feat PR 必须含集成测试
- 关联任务 ID：d1029612-8719-43e9-88da-aa5e540ce23f

**不做什么**：
- 不修改现有业务代码
- 不替换已有的 mock 单元测试
- 不覆盖所有路由，只补最关键的 4 个

## 成功标准

- [ARTIFACT] `packages/brain/src/__tests__/integration/critical-routes.integration.test.js` 文件存在
- [BEHAVIOR] 集成测试覆盖 4 个核心路由（GET /health、GET /tasks、GET /context、GET /okr/current），使用真实 PostgreSQL 连接
- [BEHAVIOR] 测试文件在 CI `brain-unit` job 中通过（有真实 DB）
- [GATE] CI L3 `integration-test-required` 门禁通过（含 integration/ 目录变更）

## 验收条件（DoD）

- [x] [ARTIFACT] `packages/brain/src/__tests__/integration/critical-routes.integration.test.js` 已创建
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/integration/critical-routes.integration.test.js')"

- [x] [BEHAVIOR] 测试文件包含 4 个路由的测试（GET /api/brain/health、GET /api/brain/tasks、GET /api/brain/context、GET /api/brain/okr/current）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/critical-routes.integration.test.js','utf8');if(!c.includes('GET /api/brain/health'))throw new Error('缺少 health 路由测试');if(!c.includes('GET /api/brain/tasks'))throw new Error('缺少 tasks 路由测试');if(!c.includes('GET /api/brain/context'))throw new Error('缺少 context 路由测试');if(!c.includes('GET /api/brain/okr/current'))throw new Error('缺少 okr 路由测试')"

- [x] [BEHAVIOR] 测试文件使用真实 PostgreSQL 连接（pg.Pool，不 mock db.js）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/critical-routes.integration.test.js','utf8');if(!c.includes('pg.Pool')&&!c.includes('new Pool'))throw new Error('未使用真实 DB 连接')"

- [x] [BEHAVIOR] 测试文件正确使用 supertest 测试 express 路由
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/critical-routes.integration.test.js','utf8');if(!c.includes('supertest'))throw new Error('缺少 supertest')"

- [x] [GATE] CI 全部通过
  Test: manual:node -e "console.log('CI 门禁由 GitHub Actions 验证')"

## 实现方案

**要改的文件**：
- 新增：`packages/brain/src/__tests__/integration/critical-routes.integration.test.js`

**技术方案**：
- 参考 `tick-full-loop.integration.test.js` 的模式（真实 pg.Pool + supertest）
- 直接对路由模块创建 express app，用 supertest 发送 HTTP 请求
- 每个测试使用 beforeAll/afterAll 管理 DB 连接
- 路由自身的 pool 来自 db.js（经 DB_DEFAULTS 连接真实 DB）
- 测试插入最小测试数据，afterAll 清理
