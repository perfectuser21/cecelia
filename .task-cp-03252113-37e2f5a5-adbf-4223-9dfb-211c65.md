---
id: task-cp-03252113-37e2f5a5-adbf-4223-9dfb-211c65
type: task-card
branch: cp-03252113-37e2f5a5-adbf-4223-9dfb-211c65
created: 2026-03-25
---

# Task Card: 修复 areas.js GET /:id 断路

## 需求（What & Why）
**功能描述**: PR#1583 已修复 goals/projects/areas 的 GET / 端点，但 areas.js GET /:id 仍查询不存在的 `goals` 和 `projects` 旧表，调用时会报 "relation does not exist"。

**背景**: 旧 goals/projects 表已在 OKR 迁移 PR12 被 DROP，areas.js /:id 未同步更新。

**不做什么**: 不修改 GET / 端点（已正确），不新增功能。

## 成功标准

1. [ARTIFACT] areas.js GET /:id 不再引用 `goals`/`projects` 表
2. [BEHAVIOR] GET /api/tasks/areas/:id 返回 area 数据，不报错
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [ ] [PRESERVE] GET /api/tasks/areas 列表端点仍然正常
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/src/task-system/areas.js','utf8');if(!c.includes('SELECT * FROM areas'))process.exit(1);console.log('OK: areas.js has correct query')"

- [ ] [ARTIFACT] areas.js GET /:id 不查询 goals/projects 表
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/src/task-system/areas.js','utf8');if(c.includes('FROM goals')||c.includes('FROM projects')){console.error('FAIL: still queries goals/projects');process.exit(1)}console.log('OK')"

- [ ] [GATE] 所有现有测试通过
  Test: manual:node -e "require('fs').accessSync('apps/api/src/task-system/areas.js')"

## 实现方案

**要改的文件**: `apps/api/src/task-system/areas.js`
**Scope 锚定**: 仅修改 GET /:id 路由，移除对不存在的 `goals`/`projects` 表的查询，只返回 area 基本信息。
