# Task Card: 记忆系统 PR10 — conversation_turn embedding fire-and-forget

Branch: cp-03250910-memory-embedding
Task ID: cc2c6d80-0e2b-41d1-8277-80da337a7c82

## DoD

- [ ] [ARTIFACT] orchestrator-chat.js 从 embedding-service.js 导入 generateMemoryStreamEmbeddingAsync
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/orchestrator-chat.js','utf8');if(!c.includes('generateMemoryStreamEmbeddingAsync'))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] 用户消息 INSERT 后 fire-and-forget 调用 generateMemoryStreamEmbeddingAsync
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/orchestrator-chat.js','utf8');const idx=c.indexOf('userRecordId');const chunk=c.substring(idx,idx+300);if(!chunk.includes('generateMemoryStreamEmbeddingAsync'))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] Cecelia 回复 INSERT 后 fire-and-forget 调用 generateMemoryStreamEmbeddingAsync
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/orchestrator-chat.js','utf8');const idx=c.indexOf('replyRecordId');const chunk=c.substring(idx,idx+300);if(!chunk.includes('generateMemoryStreamEmbeddingAsync'))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] memory-embedding-wiring.test.js 4 个测试全部通过（vitest 静态源码验证）
  Test: manual:node -e "const fs=require('fs');fs.accessSync('packages/brain/src/__tests__/memory-embedding-wiring.test.js');const c=fs.readFileSync('packages/brain/src/__tests__/memory-embedding-wiring.test.js','utf8');if(!c.includes('import.meta.url'))process.exit(1);console.log('OK')"

## 成功标准

- orchestrator-chat.js 导入 generateMemoryStreamEmbeddingAsync from embedding-service.js
- 用户消息 INSERT 后 fire-and-forget 调用 generateMemoryStreamEmbeddingAsync
- Cecelia 回复 INSERT 后 fire-and-forget 调用 generateMemoryStreamEmbeddingAsync
- 新增测试文件 packages/brain/src/__tests__/memory-embedding-wiring.test.js 全部通过
