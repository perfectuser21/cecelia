---
id: task-cp-03311046-fix-stop-hook-stage4-completion
type: task-card
branch: cp-03311046-fix-stop-hook-stage4-completion
created: 2026-03-31
---

# Task Card: stop.sh Stage 4 完整完成条件

## 验收条件（DoD）

- [x] [ARTIFACT] CI pass 后不再直接 exit 0，而是进入 Stage 4 检查
  Test: manual:node -e "const c=require('fs').readFileSync('packages/quality/hooks/stop.sh','utf8');if(/任务完成.*CI.*全绿[\s\S]{0,20}exit 0/.test(c))process.exit(1);console.log('ARTIFACT OK: no premature exit 0 after CI pass')"

- [x] [ARTIFACT] 读 .dev-mode，cleanup_done: true → exit 0（唯一出口）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/quality/hooks/stop.sh','utf8');if(!c.includes('cleanup_done'))process.exit(1);console.log('ARTIFACT OK: cleanup_done check present')"

- [x] [ARTIFACT] step_4_ship: pending → exit 2，输出提示写 Learning
  Test: manual:node -e "const c=require('fs').readFileSync('packages/quality/hooks/stop.sh','utf8');if(!c.includes('step_4_ship'))process.exit(1);console.log('ARTIFACT OK: step_4_ship check present')"

- [x] [ARTIFACT] PR 未合并 + step_4_ship done → 执行 gh pr merge → exit 2
  Test: manual:node -e "const c=require('fs').readFileSync('packages/quality/hooks/stop.sh','utf8');if(!c.includes('gh pr merge'))process.exit(1);console.log('ARTIFACT OK: gh pr merge present')"

- [x] [ARTIFACT] PR 已合并 + step_4_ship done → 写 cleanup_done: true → exit 2
  Test: manual:node -e "const c=require('fs').readFileSync('packages/quality/hooks/stop.sh','utf8');if(!c.includes('cleanup_done: true'))process.exit(1);console.log('ARTIFACT OK: cleanup_done write present')"

- [x] [ARTIFACT] bash -n 语法检查通过
  Test: manual:bash -c "bash -n packages/quality/hooks/stop.sh && echo 'ARTIFACT OK: syntax valid'"

- [x] [BEHAVIOR] stop.sh 在 CI pass 后不输出 "任务完成（CI 全绿）" 即 exit 0，而是检查 cleanup_done 状态后决定 exit 路径
  Test: manual:node -e "const c=require('fs').readFileSync('packages/quality/hooks/stop.sh','utf8');if(c.includes('任务完成（CI 全绿）'))process.exit(1);if(!c.includes('cleanup_done'))process.exit(1);if(!c.includes('Step 10'))process.exit(1);console.log('BEHAVIOR OK: old exit replaced with Stage 4 checks')"

- [x] [BEHAVIOR] stop.sh 在 step_4_ship 未完成时输出 "Stage 4 Ship 未完成" 提示并 exit 2
  Test: manual:node -e "const c=require('fs').readFileSync('packages/quality/hooks/stop.sh','utf8');if(!c.includes('Stage 4 Ship'))process.exit(1);if(!c.includes('exit 2'))process.exit(1);console.log('BEHAVIOR OK: Stage 4 Ship prompt present')"

## 测试计划

- bash -n 语法检查
- 逻辑审查：CI pass 分支不再有直接 exit 0
