---
id: dod-burst-limiter
version: 1.0.0
created: 2026-03-11
updated: 2026-03-11
changelog:
  - 1.0.0: 初始版本
---

## DoD - feat(tick): 加 burst limiter — MAX_NEW_DISPATCHES_PER_TICK 防雪崩

### 验收清单

- [x] 常量 MAX_NEW_DISPATCHES_PER_TICK = 2 加在 tick.js 顶部（与 UNBLOCK_BATCH_LIMIT 等同区）
  Test: manual:bash -c "grep -c 'MAX_NEW_DISPATCHES_PER_TICK = 2' packages/brain/src/tick.js"

- [x] Step 7a 循环中有 newDispatchCount 计数器，达到上限 break
  Test: manual:bash -c "grep -c 'newDispatchCount' packages/brain/src/tick.js"

- [x] Step 7b 循环中同样有 burst limit 检查
  Test: manual:bash -c "grep -c 'newDispatchCount >= MAX_NEW_DISPATCHES_PER_TICK' packages/brain/src/tick.js"

- [x] tick summary 日志输出 burst_limited 字段
  Test: manual:bash -c "grep -c 'burst_limited' packages/brain/src/tick.js"

- [x] 返回对象 dispatch 字段中有 burst_limited
  Test: manual:bash -c "grep -c 'burst_limited' packages/brain/src/tick.js"

- [x] 新建 tick-burst-limiter.test.js 覆盖 burst limit 场景
  Test: manual:bash -c "ls packages/brain/src/__tests__/tick-burst-limiter.test.js"

- [x] 测试：5 个 slot 剩余但只派发 2 个（burst limit 生效）
  Test: manual:bash -c "grep -c 'burst' packages/brain/src/__tests__/tick-burst-limiter.test.js"

- [x] 现有 tick.test.js 回归测试通过
  Test: manual:bash -c "grep -c 'routeTask' packages/brain/src/__tests__/tick.test.js"
