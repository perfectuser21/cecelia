---
id: dod-burst-limiter
version: 1.0.0
created: 2026-03-11
updated: 2026-03-11
changelog:
  - 1.0.0: 初始版本
---

# DoD - feat(tick): burst limiter MAX_NEW_DISPATCHES_PER_TICK

## 验收清单

- [x] tick.js 顶部添加常量 MAX_NEW_DISPATCHES_PER_TICK = 2
  Test: manual:bash -c "grep -c 'MAX_NEW_DISPATCHES_PER_TICK = ' packages/brain/src/tick.js"

- [x] Step 7a 派发循环加 newDispatchCount 计数器和 burst 跳出逻辑
  Test: manual:bash -c "grep -c 'newDispatchCount' packages/brain/src/tick.js"

- [x] Step 7b fallback 循环也加 burst 限制
  Test: manual:bash -c "grep -c 'newDispatchCount' packages/brain/src/tick.js"

- [x] tick summary 日志输出 burst_limited: true/false
  Test: manual:bash -c "grep -c 'burst_limited' packages/brain/src/tick.js"

- [x] MAX_NEW_DISPATCHES_PER_TICK 加入 export 列表
  Test: manual:bash -c "grep -c 'MAX_NEW_DISPATCHES_PER_TICK' packages/brain/src/tick.js"

- [x] tick.test.js 验证常量值 = 2
  Test: manual:bash -c "grep -c 'MAX_NEW_DISPATCHES_PER_TICK' packages/brain/src/__tests__/tick.test.js"

- [x] tick-burst-limiter.test.js 验证 rampedDispatchMax > 2 时只派发 2 个任务
  Test: manual:bash -c "grep -c 'burst' packages/brain/src/__tests__/tick-burst-limiter.test.js"

- [x] 现有 tick.test.js 不回归（routeTask 和 TASK_TYPE_AGENT_MAP 测试全绿）
  Test: manual:bash -c "cd packages/brain && node --experimental-vm-modules ../../node_modules/.bin/vitest run src/__tests__/tick.test.js 2>&1 | grep -c 'pass'"

- [x] LEARNINGS.md 更新
  Test: manual:bash -c "grep -c 'burst' docs/LEARNINGS.md"
