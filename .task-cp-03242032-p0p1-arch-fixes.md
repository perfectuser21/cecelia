# Task Card: P0/P1 Brain 架构问题修复

**Task ID**: a2f3e8db-c3a9-458e-82f3-d317a3005b7c
**Branch**: cp-03242032-p0p1-arch-fixes
**Priority**: P1
**Type**: fix

## 目标

修复 3 个 Brain 运行时问题，消除 tick 每次报错和两个潜在死循环。

## 修改文件

| 文件 | 操作 | 说明 |
|------|------|------|
| `packages/brain/src/decision.js` | 修改 | UNION 子查询加 priority 列 |
| `packages/brain/src/actions.js` | 修改 | createTask dedup 覆盖 failed 72h |
| `packages/brain/src/auto-fix.js` | 修改 | 添加 MAX_AUTO_FIX_ATTEMPTS = 3 守卫 |

## 成功标准

- Brain tick 不再出现 `column g.priority does not exist` 错误
- Rumination 同洞察 72h 内不重复创建 failed 过的 research 任务
- auto-fix 对同一错误签名最多重试 3 次，超限后停止

## 验收清单

- [x] [ARTIFACT] decision.js UNION 含 priority（key_results）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/decision.js','utf8');if(!c.includes('created_at, priority FROM key_results'))process.exit(1)"

- [x] [ARTIFACT] decision.js UNION 含 priority（objectives）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/decision.js','utf8');if(!c.includes('0 AS progress, created_at, priority FROM objectives'))process.exit(1)"

- [x] [ARTIFACT] actions.js 含 72h failed 去重
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/actions.js','utf8');if(!c.includes('72 hours'))process.exit(1)"

- [x] [ARTIFACT] actions.js 覆盖 rumination trigger_source
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/actions.js','utf8');if(!c.includes('rumination'))process.exit(1)"

- [x] [ARTIFACT] auto-fix.js 含最大重试常量 MAX_AUTO_FIX_ATTEMPTS
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/auto-fix.js','utf8');if(!c.includes('MAX_AUTO_FIX_ATTEMPTS'))process.exit(1)"

- [x] [ARTIFACT] auto-fix.js 含失败次数检查
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/auto-fix.js','utf8');if(!c.includes('failed_count'))process.exit(1)"

- [x] [BEHAVIOR] decision.js 测试文件存在且包含 compareGoalProgress
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/decision.test.js','utf8');if(!c.includes('compareGoalProgress'))process.exit(1)"

- [x] [GATE] vitest 配置存在
  Test: manual:node -e "require('fs').accessSync('packages/brain/vitest.config.js')"
