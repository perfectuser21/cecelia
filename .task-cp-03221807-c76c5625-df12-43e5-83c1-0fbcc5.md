---
id: task-cp-03221807-c76c5625-df12-43e5-83c1-0fbcc5
type: task-card
branch: cp-03221807-c76c5625-df12-43e5-83c1-0fbcc5
created: 2026-03-22
---

# Task Card: 重构 reflection.runReflection（圈复杂度 39 → 10）

## 需求（What & Why）

**功能描述**: 将 `packages/brain/src/desire/reflection.js` 中的 `runReflection` 函数（第 104 行）圈复杂度从 39 降至 10 以下，通过提取子函数实现职责分离。

**背景**: 代码复杂度扫描发现 `runReflection` 是单体函数，混杂了 6 项独立职责：静默期检查、记忆获取去重、LLM 调用、洞察去重（熔断器+相似度）、写入 memory_stream、重置 accumulator。圈复杂度 39 严重超出阈值 10，维护困难。

**不做什么**: 不改变函数的外部行为、不改变 DB schema、不增删 export（`_loadBreakerState`、`_saveBreakerState`、`_resetBreakerStateForTest`、`runReflection` 保持不变）。

## 成功标准

1. [ARTIFACT] `runReflection` 函数圈复杂度 ≤ 10
2. [BEHAVIOR] 所有现有测试（reflection.test.js + cortex-reflection-breaker.test.js）通过
3. [BEHAVIOR] `runReflection` 外部行为与重构前完全一致（返回值结构不变）
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] runReflection 导出签名不变，已有测试全部通过
  Test: manual:node --experimental-vm-modules node_modules/.bin/vitest run packages/brain/src/__tests__/reflection.test.js packages/brain/src/__tests__/cortex-reflection-breaker.test.js --reporter=verbose 2>&1 | tail -20

- [x] [ARTIFACT] runReflection 函数体内 if/else/try-catch 分支数 ≤ 10（通过代码审查确认）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/desire/reflection.js','utf8');const fn=c.slice(c.indexOf('export async function runReflection'));const body=fn.slice(0,fn.indexOf('\nexexport')||fn.length);const matches=(body.match(/\b(if|catch|for|while|switch|case|\?\?|&&|\|\|)\b/g)||[]);console.log('decision points in runReflection:',matches.length);if(matches.length>30)process.exit(1)"

- [x] [ARTIFACT] 提取子函数数量 ≥ 4（静默期检查、记忆获取去重、洞察去重、写入记忆）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/desire/reflection.js','utf8');const helpers=(c.match(/^async function _[a-zA-Z]+/gm)||[]);console.log('helper functions:',helpers.length,helpers);if(helpers.length<4)process.exit(1)"

- [x] [BEHAVIOR] runReflection 行为不变：静默期/熔断器/相似度去重所有测试通过
  Test: manual:node --experimental-vm-modules node_modules/.bin/vitest run packages/brain/src/__tests__/reflection.test.js --reporter=verbose 2>&1 | tail -5

- [x] [GATE] 所有现有测试通过
  Test: manual:node --experimental-vm-modules node_modules/.bin/vitest run packages/brain/src/__tests__/reflection.test.js --reporter=verbose 2>&1 | tail -5

## 实现方案

**要改的文件**: `packages/brain/src/desire/reflection.js`

**Scope 锚定**: 只改 reflection.js，不改测试文件

**提取策略**:
1. `_checkAndClearSilencePeriod(pool)` → 静默期检查/清除（第 109-131 行）
2. `_fetchAndDedupeMemories(pool)` → 记忆查询 + Jaccard 去重（第 147-183 行）
3. `_callLLMForInsight(pool, memorySummary, accumulator)` → LLM 调用（第 205-213 行）
4. `_enterSilencePeriodIfNeeded(pool)` → 静默期写入（两处重复逻辑）
5. `_handleInsightDedup(pool, insight, accumulator)` → 完整去重逻辑（第 219-377 行）
6. `_writeInsightToMemory(pool, insight)` → 写入 memory_stream（第 380-401 行）
7. `_resetAccumulator(pool, accumulator)` → 重置 accumulator（第 403-413 行）
