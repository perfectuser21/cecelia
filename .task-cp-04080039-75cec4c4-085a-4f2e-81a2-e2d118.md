---
id: task-cp-04080039-75cec4c4
type: task-card
branch: cp-04080039-75cec4c4-085a-4f2e-81a2-e2d118
created: 2026-04-08
---

# Task Card: 24h任务成功率39%根因修复 — 成功率去噪 + SelfDrive去重加固

## 需求（What & Why）
**功能描述**：
根因分析已完成（docs/learnings/cp-04072300-task-success-rate-analysis.md）：
- 故障1: pipeline_rescue storm（PR #2004已修复）
- 故障2: account3 OAuth token过期（运营问题）
- 次生: SelfDrive自诊断循环放大（22个额外失败任务）

本次修复两个遗留代码问题，防止同类事件放大：
1. `getTaskStats24h()` 将 pipeline_rescue 纳入成功率计算 → storm 时噪音掩盖真实健康状态
2. SelfDrive 去重仅检查 `queued/in_progress`，忽略近期 `quarantined` → 诊断任务被反复创建

**背景**：2026-04-06 起两个离散故障叠加导致24h成功率从94.8%跌至39%
**不做什么**：不处理account3 OAuth token刷新（运营操作）；不改变pipeline_rescue任务本身逻辑

## 成功标准

> [BEHAVIOR] getTaskStats24h() 排除 pipeline_rescue，返回业务任务真实成功率
> [BEHAVIOR] SelfDrive 去重查询覆盖近24h内quarantined的任务，防止重复创建诊断任务

## 验收条件（DoD）

- [x] [BEHAVIOR] getTaskStats24h() 查询中排除 task_type='pipeline_rescue'
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/self-drive.js','utf8');if(!c.includes(\"task_type != 'pipeline_rescue'\"))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] SelfDrive 去重查询扩展到近24h内quarantined任务
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/self-drive.js','utf8');if(!c.includes('quarantined') || !c.includes('24 hours'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] Brain 能正常启动（语法无误）
  Test: manual:node --input-type=module -e "import('./packages/brain/src/self-drive.js').then(()=>console.log('OK')).catch(e=>{console.error(e.message);process.exit(1)})"

## 实现方案
**要改的文件**: `packages/brain/src/self-drive.js`
**受影响函数**: `getTaskStats24h()`（第436行 SQL 加过滤条件）、SelfDrive 去重查询（第189-195行）
**不改什么**: 不改 pipeline_rescue 创建逻辑、不改 pipeline-patrol.js
