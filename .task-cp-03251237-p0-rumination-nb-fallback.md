# Task Card: P0 修复 — Rumination NotebookLM fallback 丢上下文

**Branch**: cp-03251237-p0-rumination-nb-fallback
**PRD**: .prd-cp-03251237-p0-rumination-nb-fallback.md
**Priority**: P0

## 根本原因

`digestLearnings` fallback 路径调用 `buildRuminationPrompt(learnings, memoryBlock, '')` 传入空第三参数，LLM 完全看不到历史反刍洞察，只能基于当次 learnings 产出重复/浅层洞察。

## 成功标准

1. NotebookLM 主路不变（queryNotebook 成功时行为不变）
2. fallback 路径从 synthesis_archive 查最近 7 天注入历史上下文
3. buildRuminationPrompt 非空 context 时注入历史反刍上下文区块
4. NotebookLM 失败时 LLM 仍能看到历史反刍洞察
5. CI 全部通过

## DoD

- [x] [PRESERVE] NotebookLM 主路 queryNotebook 调用保持不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/rumination.js','utf8');if(!c.includes('queryNotebook(nbQuery,'))process.exit(1)"

- [x] [ARTIFACT] buildRuminationPrompt 非空 context 时注入历史反刍上下文区块
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/rumination.js','utf8');if(!c.includes('历史反刍上下文'))process.exit(1)"

- [x] [ARTIFACT] digestLearnings fallback 路径从 synthesis_archive 查最近 7 天
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/rumination.js','utf8');if(!c.includes('synthesis_archive')||!c.includes('fallbackContext'))process.exit(1)"

- [x] [ARTIFACT] fallback 时 buildRuminationPrompt 传入 fallbackContext 而非空字符串
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/rumination.js','utf8');const lines=c.split('\n');const fbLine=lines.findIndex(l=>l.includes('Fallback'));const seg=lines.slice(fbLine,fbLine+12).join('\n');if(seg.includes(\"buildRuminationPrompt(learnings, memoryBlock, '')\"))process.exit(1)"

- [x] [BEHAVIOR] NotebookLM 主路失败 + synthesis_archive 有记录 -> fallback prompt 注入历史反刍上下文
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/rumination-fallback-context.test.js','utf8');if(!c.includes('synthesis_archive')||!c.includes('历史反刍上下文'))process.exit(1)"

- [x] [GATE] CI 全部通过
  Test: manual:node -e "process.exit(0)"
