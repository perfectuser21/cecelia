# DoD: cecelia-claude 统一无头调用 Wrapper

## 验收清单

- [x] D1: `llm-caller.js` 正确提取 `selectBestAccount()` 返回的 accountId（修复 object → string bug）
  - Test: `manual:bash -c "grep -n 'bestAccount.accountId\\|typeof bestAccount' packages/brain/src/llm-caller.js | grep -q accountId"`

- [x] D2: `cecelia-bridge.cjs` 的 `/llm-call` 读取并使用 `configDir` 设置 `CLAUDE_CONFIG_DIR`
  - Test: `manual:bash -c "grep -q 'CLAUDE_CONFIG_DIR' /home/xx/bin/cecelia-bridge.cjs"`

- [x] D3: `/home/xx/bin/cecelia-claude` wrapper 脚本存在且可执行
  - Test: `manual:bash -c "test -x /home/xx/bin/cecelia-claude"`

- [x] D4: wrapper 脚本支持 `CECELIA_CREDENTIALS=accountX` 覆盖
  - Test: `manual:bash -c "grep -q 'CECELIA_CREDENTIALS' /home/xx/bin/cecelia-claude"`

- [x] D5: wrapper 脚本查询 Brain API `/api/brain/account/best` fallback
  - Test: `manual:bash -c "grep -q 'account/best' /home/xx/bin/cecelia-claude"`

- [x] D6: Brain 新增 `GET /api/brain/account/best` 端点，返回 `{ account, model }`
  - Test: `manual:bash -c "grep -q 'account/best' packages/brain/src/routes.js"`

- [x] D7: Brain tests 通过
  - Test: `manual:npm run test --workspace packages/brain -- --run src/__tests__/`
