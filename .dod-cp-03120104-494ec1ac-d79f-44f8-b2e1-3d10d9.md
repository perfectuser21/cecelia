---
id: dod-cortex-insight-autoclose
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

## DoD - Cortex insight → action 自动闭合机制

### 验收清单

- [x] migration 150 存在且添加 `source_learning_id UUID` 到 tasks 表
  Test: manual:bash -c "ls packages/brain/migrations/150_tasks_source_learning_id.sql"

- [x] `insight-to-task.js` 存在并导出 `hasCodeFixSignal` 和 `triggerInsightTask`
  Test: manual:bash -c "grep -c 'export.*hasCodeFixSignal\|export.*triggerInsightTask' packages/brain/src/insight-to-task.js"

- [x] `hasCodeFixSignal` 在内容含 ≥2 个关键词时返回 true
  Test: manual:bash -c "grep -c 'CODE_FIX_KEYWORDS\|matches.length' packages/brain/src/insight-to-task.js"

- [x] `triggerInsightTask` 去重：已有 queued/in_progress task 时返回 skipped
  Test: manual:bash -c "grep -c 'source_learning_id.*queued\|task_already_exists' packages/brain/src/insight-to-task.js"

- [x] `triggerInsightTask` 创建 task 后标记 `applied=true`
  Test: manual:bash -c "grep -c 'applied.*true\|applied_at' packages/brain/src/insight-to-task.js"

- [x] cortex.js `recordLearnings` 在 INSERT 成功后调用 `triggerInsightTask`
  Test: manual:bash -c "grep -c 'triggerInsightTask' packages/brain/src/cortex.js"

- [x] `selfcheck.js` EXPECTED_SCHEMA_VERSION 更新为 '150'
  Test: manual:bash -c "grep -c '150' packages/brain/src/selfcheck.js"

- [x] 单元测试覆盖 insight→task 核心链路
  Test: manual:bash -c "ls packages/brain/src/__tests__/insight-to-task.test.js"
