---
id: task-cp-03221802-844576b3-ebc1-475a-a61b-d3b4ee
type: task-card
branch: cp-03221802-844576b3-ebc1-475a-a61b-d3b4ee
created: 2026-03-22
---

# Task Card: 重构 cortex.analyzeDeep（圈复杂度 55 → 10 以下）

## 需求（What & Why）

**功能描述**: 将 `packages/brain/src/cortex.js` 中 `analyzeDeep` 函数的圈复杂度从 55 降至 10 以下，通过提取子函数拆分职责。

**背景**: 代码复杂度扫描发现 `analyzeDeep`（第 575 行）圈复杂度为 55，远超阈值 10。该函数承担了过多职责：反思熔断、上下文构建、多路数据获取（决策历史/系统状态/learnings/历史分析/自模型/跨任务模式）、LLM 调用、响应处理、错误处理等，需要通过提取子函数降低复杂度。

**不做什么**:
- 不改变任何业务逻辑和行为
- 不新增公开 export
- 不修改其他函数

## 成功标准

1. [ARTIFACT] `analyzeDeep` 函数内部分支数降至 10 以下
2. [BEHAVIOR] 所有 cortex 测试仍然通过，行为不变
3. [ARTIFACT] 提取的子函数均为模块内私有（无新 export）

## 验收条件（DoD）

- [x] [ARTIFACT] analyzeDeep 函数存在且为 async
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');if(!c.includes('async function analyzeDeep'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] analyzeDeep 函数内 if/catch 分支总数 ≤ 10
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');const m=src.match(/async function analyzeDeep[\s\S]*?\n\}/);if(!m){process.stdout.write('FAIL:no match\n');process.exit(1);}const ifs=(m[0].match(/\bif\b/g)||[]).length;const catches=(m[0].match(/\bcatch\b/g)||[]).length;const total=ifs+catches+1;process.stdout.write('complexity='+total+'\n');if(total>10)process.exit(1);"

- [x] [BEHAVIOR] cortex 单测全部通过
  Test: manual:node --experimental-vm-modules node_modules/.bin/vitest run packages/brain/src/cortex.test.js --reporter=verbose 2>&1 | tail -5

- [x] [ARTIFACT] 无新增 export（只允许已有的 analyzeDeep 等）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');const ex=(c.match(/^export\s+(?:async\s+)?function\s+\w+/gm)||[]);const allowed=['analyzeDeep','searchRelevantAnalyses','searchRelevantLearningsForCortex','storeAbsorptionPolicy','createCortexFallback','classifyTimeoutReason','estimateTokens'];const bad=ex.filter(e=>!allowed.some(n=>e.includes(n)));if(bad.length){console.log('非预期新增export:',bad.join(', '));process.exit(1);}console.log('OK exports:',ex.length)"

## 实现方案（Stage 2 探索后填写）

**要改的文件**:
1. `packages/brain/src/cortex.js` — 提取子函数，重构 `analyzeDeep`

**提取计划**:
- `_buildBaseContext(event, thalamusDecision)` — 构建基础上下文对象
- `_enrichWithDecisionHistory(context)` — 获取决策历史（try/catch 内）
- `_enrichWithSystemStatus(context)` — 获取系统状态（try/catch 内）
- `_enrichWithLearnings(context, event)` — 注入相关 learnings
- `_enrichWithHistoricalAnalyses(context, event)` — 注入历史分析（try/catch 内）
- `_enrichWithSelfModel(context)` — 注入 self-model（try/catch 内）
- `_enrichWithRcaParams(context, event, thalamusDecision)` — RCA 参数注入
- `_enrichWithCrossTaskPatterns(context)` — 跨任务失败模式（try/catch 内）
- `_processLLMResponse(event, response, timing)` — 解析/验证/去重/记录决策
- `_handleAnalysisError(err, event)` — 错误分类/记录/降级
