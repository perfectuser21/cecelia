## PRD

构建 `packages/engine/scripts/pipeline-trace.sh <branch>`，读取某个 branch 所有 pipeline 证据文件并输出结构化执行摘要。

## 成功标准

- 传入 branch 名称，输出每个 Stage 的状态、时间、输入、输出产物
- 显示 seal 文件关键字段（verdict、divergence_count）
- 显示 PR URL + CI 结果
- 显示 Learning 文件存在性
- 无该 branch 数据时优雅提示
- 可对已完成和进行中的 branch 使用
