# Task Card: 重构 runDesireSystem（圈复杂度 38 → 10）

## PRD

**文件**：`packages/brain/src/desire/index.js`
**函数**：`runDesireSystem`
**当前圈复杂度**：38（阈值：10）

重构目标：将 runDesireSystem 拆分为职责单一的子函数，降低圈复杂度到 10 以下，同时保持所有现有测试通过。

### 重构方案

提取以下子函数：
1. `runPerceptionAndEmotion(pool, result)` — Layer 1 + 1.5
2. `runMemoryLayer(pool, observations, result)` — Layer 2
3. `runReflectionAndFormation(pool, result)` — Layer 3 + 4
4. `getExpressionCandidate(pool)` — Layer 5（包装错误处理）
5. `handleExploreDesire(pool, desire, result)` — explore 分支
6. `handleActFollowupDesire(pool, desire, result)` — act/follow_up 分支
7. `handleNotifyDesire(pool, candidate, result)` — 通知类型分支

主函数 `runDesireSystem` 变为纯编排器，圈复杂度降至 ≤5。

## 成功标准

- [x] [BEHAVIOR] runDesireSystem 已拆分为子函数，主函数不含内联 SQL（explore/act 逻辑已提取）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/desire/index.js','utf8');if(c.includes('existing_explore')&&!c.includes('async function handleExploreDesire')){process.exit(1)}if(!c.includes('handleExploreDesire')||!c.includes('handleActFollowupDesire')||!c.includes('runPerceptionAndEmotion')){console.error('helper functions missing');process.exit(1)}console.log('helper functions exist')"

- [x] [BEHAVIOR] explore/act 内联 SQL 已从 runDesireSystem 提取到独立函数（不再直接含 desire_explore 字符串）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/desire/index.js','utf8');const fnMatch=c.indexOf('export async function runDesireSystem');const afterFn=c.slice(fnMatch);const firstBrace=afterFn.indexOf('{');const fnBody=afterFn.slice(0,afterFn.indexOf('\nexport',firstBrace+1)||afterFn.length);if(fnBody.includes('desire_explore')){console.error('inline SQL found in runDesireSystem');process.exit(1)}console.log('no inline SQL in runDesireSystem')"

- [x] [BEHAVIOR] 欲望系统入口文件仍导出 runDesireSystem 函数
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/desire/index.js','utf8');if(!c.includes('export async function runDesireSystem')){console.error('runDesireSystem not exported');process.exit(1)}console.log('export OK')"
