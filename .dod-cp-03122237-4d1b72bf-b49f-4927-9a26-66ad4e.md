---
id: dod-hook-gates-7-true-locks
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

# DoD - 7 个 Hook Gate 真锁

## 验收清单

- [x] Gate 1 (bash-guard): git push 命令被拦截，先运行 local-precheck.sh，失败时 exit 2 阻止 push
  Test: manual:bash -c "grep -c 'local-precheck' packages/engine/hooks/bash-guard.sh"

- [x] Gate 5 (bash-guard): git commit -m 消息格式不符时（非 feat:/fix: 等前缀）被阻止
  Test: manual:bash -c "grep -c 'conventional commits\\|commit.*message' packages/engine/hooks/bash-guard.sh"

- [x] Gate 3 (branch-protect): 写 .prd-*.md 时自动验证 PRD 内容包含 ## 成功标准 章节
  Test: manual:bash -c "grep -c 'prd.*\\.md\\|\\.prd-.*\\.md' packages/engine/hooks/branch-protect.sh"

- [x] Gate 4 (branch-protect): 写 .dod-*.md 时调用 check-dod-mapping.cjs 验证 Test 字段
  Test: manual:bash -c "grep -c 'dod.*\\.md\\|\\.dod-.*\\.md' packages/engine/hooks/branch-protect.sh"

- [x] Gate 7 (branch-protect): 改动 packages/engine/skills/ 时输出 Engine 三要素提醒
  Test: manual:bash -c "grep -c 'engine.*skills\\|Engine.*三要素\\|CONFIG' packages/engine/hooks/branch-protect.sh"

- [x] Gate 2 (devloop-check): step_10 done 后运行 check-learning.sh 验证 Learning 格式
  Test: manual:bash -c "grep -c 'check-learning\\|learning.*format\\|LEARNING.*format' packages/engine/lib/devloop-check.sh"

- [x] Gate 6 (devloop-check): Brain 代码有改动时检查 DEFINITION.md 是否同步更新
  Test: manual:bash -c "grep -c 'DEFINITION.*md\\|definition.*check\\|brain.*DEFINITION' packages/engine/lib/devloop-check.sh"

- [x] Engine 版本 bump（package.json + VERSION + .hook-core-version + regression-contract.yaml + package-lock.json x2）
  Test: manual:bash -c "grep -c '\"version\"' packages/engine/package.json"

- [x] feature-registry.yml 新增 S 系列 changelog 条目
  Test: manual:bash -c "grep -c 'hook-gate\\|Hook Gate\\|7-true-locks' packages/engine/features/feature-registry.yml"

- [x] 所有改动在 packages/engine/ 内，不影响其他 package
  Test: manual:bash -c "git diff --name-only origin/main...HEAD | grep -v 'packages/engine' | grep -c '\\.' || true"
