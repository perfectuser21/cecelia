## 目标

把 Sprint Contract Gate 的收敛循环从 MD 伪代码改为真正的 shell 脚本管理状态。

## 背景

当前问题：Sprint Contract 的 `while loop` 是 Claude 读 MD 指令后"自觉执行"的伪代码，round/divergence 状态存在 Claude context 里，session 重启后状态丢失。官方 Anthropic 的做法是代码（Python while True）管理循环状态。

## 要做的事

1. 新建 `packages/engine/scripts/devgate/sprint-contract-loop.sh`
2. 更新 `packages/engine/skills/dev/steps/01-spec.md` v3.5.0（Step 4 调用脚本，移除死循环检测）
3. Engine 版本 bump 13.71.0 → 13.72.0

## 成功标准

- sprint-contract-loop.sh 存在且可执行
- 脚本读两个 seal 文件，统计 blocker_count，写磁盘状态
- 01-spec.md Step 4 调用脚本
- 无死循环检测（prev_divergence / P1 升级）
