---
id: task-cp-03231748-5d46a3b7-12f7-461e-8680-4d5232
type: task-card
branch: cp-03231748-5d46a3b7-12f7-461e-8680-4d5232
created: 2026-03-23
---

# Task Card: 验证 analyzeDeep 重构完成（CC 55 → 2）

## 需求（What & Why）

**功能描述**: 验证 cortex.analyzeDeep 圈复杂度已从 55 降至阈值以下（目标 <10）
**背景**: Brain 复杂度扫描发现 analyzeDeep CC=55（超阈值 10）。PR #1450 已完成重构，将函数拆解为 7 个辅助函数，CC 降至 ~2。本任务为验证任务，确认重构结果正确且测试全部通过。
**不做什么**: 不再重新重构（已在 PR #1450 完成），不做其他功能修改

## 成功标准

1. [ARTIFACT] analyzeDeep 函数主体 ≤ 30 行，仅调用辅助函数
2. [BEHAVIOR] Brain 所有现有测试通过（npm test）
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [ ] [PRESERVE] cortex.analyzeDeep 函数签名不变（仍导出、仍接收 event + thalamusDecision）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');if(!c.includes('async function analyzeDeep(event, thalamusDecision'))process.exit(1);console.log('ok')"

- [ ] [ARTIFACT] analyzeDeep 函数主体不超过 30 行（重构已完成）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');const m=c.match(/async function analyzeDeep[\s\S]*?\n\}/);if(!m)process.exit(1);const lines=m[0].split('\n').length;if(lines>30){console.error('too many lines:'+lines);process.exit(1);}console.log('lines:'+lines)"

- [ ] [BEHAVIOR] Brain 测试套件全部通过
  Test: manual:npm test --prefix packages/brain

- [ ] [GATE] CI 全部通过
  Test: manual:npm test --prefix packages/brain

## 实现方案

**要改的文件**: 无（重构已在 PR #1450 完成）
**Scope 锚定**: packages/brain/src/cortex.js 第 808 行 analyzeDeep，已重构为调用 _buildBaseContext / _enrichContext* / _callLLMAndProcess 等辅助函数
