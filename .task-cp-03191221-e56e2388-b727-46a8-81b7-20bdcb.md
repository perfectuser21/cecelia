---
id: task-cp-03191221-e56e2388-b727-46a8-81b7-20bdcb
type: task-card
branch: cp-03191221-e56e2388-b727-46a8-81b7-20bdcb
created: 2026-03-19
---

# Task Card: N8N 三平台统一 Webhook 路由升级

## 需求（What & Why）
**功能描述**: 升级 N8N `flow-内容发布.json`，支持抖音/快手/小红书三平台 + 图文/视频两类型的统一 Webhook 路由。
**背景**: 当前 Webhook 仅处理抖音和小红书图文。快手无 N8N 接入，视频类型无路由。需统一入口。
**不做什么**: 不改发布脚本本体（publish-*.cjs），不新建发布脚本，不改 N8N 服务配置。

## 成功标准

1. [ARTIFACT] `flow-内容发布.json` 包含 5 条路由分支（douyin / kuaishou+image / kuaishou+video / xiaohongshu+image / xiaohongshu+video）
2. [ARTIFACT] Webhook 接受 `platform`（单值）和 `contentType` 字段，向后兼容 `targetPlatforms` 数组
3. [ARTIFACT] 未知 platform 有 400 错误响应节点（Respond to Webhook 400）
4. [BEHAVIOR] N8N workflow JSON 通过 `node -e "JSON.parse(require('fs').readFileSync(...))"` 合法性校验
5. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] flow-内容发布.json 包含 Switch 节点，有 5 条路由规则（douyin / kuaishou_image / kuaishou_video / xiaohongshu_image / xiaohongshu_video）
  Test: manual:node -e "const w=JSON.parse(require('fs').readFileSync('packages/workflows/n8n/workflows/media/flow-内容发布.json','utf8'));const sw=w.nodes.find(n=>n.name==='平台类型路由');if(!sw||sw.parameters.rules.values.length<5)process.exit(1);console.log('OK routes:',sw.parameters.rules.values.length)"

- [x] [ARTIFACT] 包含 Respond to Webhook 400 节点处理非法平台
  Test: manual:node -e "const w=JSON.parse(require('fs').readFileSync('packages/workflows/n8n/workflows/media/flow-内容发布.json','utf8'));const n=w.nodes.find(n=>n.type==='n8n-nodes-base.respondToWebhook'&&n.parameters&&n.parameters.responseCode===400);if(!n)process.exit(1);console.log('OK 400 node:',n.name)"

- [x] [ARTIFACT] 准备节点代码包含 contentType 字段解析
  Test: manual:node -e "const w=JSON.parse(require('fs').readFileSync('packages/workflows/n8n/workflows/media/flow-内容发布.json','utf8'));const prep=w.nodes.find(n=>n.name==='准备');if(!prep||!prep.parameters.jsCode.includes('contentType'))process.exit(1);console.log('OK contentType in 准备')"

- [x] [ARTIFACT] 包含快手图文 SSH 节点（调用 publish-kuaishou-api.cjs）
  Test: manual:node -e "const w=JSON.parse(require('fs').readFileSync('packages/workflows/n8n/workflows/media/flow-内容发布.json','utf8'));const n=w.nodes.find(n=>n.name==='SSH-快手图文');if(!n||!n.parameters.command.includes('publish-kuaishou-api.cjs'))process.exit(1);console.log('OK kuaishou image SSH')"

- [x] [BEHAVIOR] Workflow JSON 文件合法
  Test: manual:node -e "JSON.parse(require('fs').readFileSync('packages/workflows/n8n/workflows/media/flow-内容发布.json','utf8'));console.log('OK valid JSON')"

- [x] [GATE] 所有现有测试通过
  Test: manual:node --test packages/workflows/skills/kuaishou-publisher/scripts/__tests__/publish-kuaishou-video.test.cjs

## 实现方案

**要改的文件**:
- `packages/workflows/n8n/workflows/media/flow-内容发布.json` — 完全重写

**核心改动**:
1. Webhook 改为 `responseMode: responseNode`
2. 准备节点：解析 `platform`（兼容 `targetPlatforms[0]`）+ `contentType`（默认 image）+ 校验
3. 是否有效 IF 节点：error=true → Respond-400；false → Respond-202 → 平台路由
4. Switch 节点：5 条规则（douyin/kuaishou+image/kuaishou+video/xiaohongshu+image/xiaohongshu+video）
5. 5 个 SSH 节点：均 SSH 至 100.108.7.63（CN Mac mini Tailscale）
6. 共享 解析通知 Code 节点 + 一个飞书通知节点
