# PRD: 修复 sshExec 用户名含空格时命令拼接错误

## 背景
infra-status.js 的 sshExec 函数拼接 SSH 命令时，用户名含空格（如 `xu xiao`）会被 shell 拆成两个参数，导致 SSH 连接失败。

## 需求
给 sshExec 中的 `${server.sshUser}@${server.tailscaleIp}` 加引号包裹。

## 成功标准
- infra-status API 中西安 M1（sshUser='xu xiao'）显示 online
