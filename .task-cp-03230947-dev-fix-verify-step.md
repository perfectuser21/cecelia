---
id: task-cp-03230947-dev-fix-verify-step
type: task-card
branch: cp-03230947-dev-fix-verify-step
created: 2026-03-23
brain-task-id: c60992c8-889c-406e-a1bc-4a6cb49f920f
---

# Task Card: fix(/dev) 修复5个bug + 4个软门禁编译进verify-step.sh

## 需求（What & Why）

**功能描述**：

1. **verify-step.sh step2 新增4个硬门禁**（当前只在AI指令里，hook不拦截）：
   - PRESERVE 条目 ≥1（改代码前必须有行为快照）
   - tdd_red_confirmed（必须先跑失败测试再写代码）
   - 垃圾清理（console.log/注释代码/TODO done 不能带进PR）
   - 周边一致性扫描（改A时同目录文件不能引用A的旧版本）

2. **修复S1-7**：`01-spec.md` Stage 1 末尾调用 `check-dod-mapping.cjs`，但该脚本要求所有条目已勾选[x]，Stage 1 阶段条目全是[ ]，必定失败。应删除此调用，只保留 `check-prd.sh`。

3. **修复S4-7**：`fire-learnings-event.sh` 用 `**预防措施**` 匹配"下次预防"内容，但 Learning 模板用 `### 下次预防`，永远提取不到，脚本逻辑bug。

4. **修复S4-8/S4-9**：`04-ship.md` 有手动 `gh pr merge` 和 Brain callback 代码块，但这两个动作已由 devloop-check.sh 统一负责，职责重叠。删除 04-ship.md 里的手动执行版本。

5. **修复S4-12**：`04-ship.md` cleanup 清理的是 `.dev-seal.{branch}`（幽灵文件，从未被创建），真实的 seal 文件是 `.dev-gate-spec.*` 和 `.dev-gate-crg.*`，应修正。

**背景**：/dev 工作流行为审计发现，4个声称是"强制门禁"的行为实际只是AI指令，hook层完全不拦截。另有5处bug影响流程正确性。修完后这些行为才能注册进RCI被CI锁死。

**不做什么**：不改 RCI 注册（下一个PR），不改 devloop-check.sh 逻辑，不改 Brain 相关代码。

## 成功标准

1. [BEHAVIOR] verify-step.sh step2 拦截缺少PRESERVE条目的Task Card
2. [BEHAVIOR] verify-step.sh step2 拦截缺少tdd_red_confirmed的.dev-mode
3. [BEHAVIOR] verify-step.sh step2 拦截含console.log的变更文件
4. [BEHAVIOR] verify-step.sh step2 拦截同目录引用旧版本的矛盾文件
5. [ARTIFACT] 01-spec.md 不再包含 check-dod-mapping.cjs 调用
6. [ARTIFACT] fire-learnings-event.sh 正确匹配 `### 下次预防`
7. [ARTIFACT] 04-ship.md 不再包含 gh pr merge 手动执行代码块
8. [ARTIFACT] 04-ship.md cleanup 清理的是 .dev-gate-spec.* 和 .dev-gate-crg.*
9. [GATE] CI 全部通过

## 验收条件（DoD）

- [ ] [PRESERVE] verify-step.sh 目前支持 step1/step2/step4 三个参数
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('step1')||!c.includes('step2')||!c.includes('step4'))process.exit(1)"

- [ ] [PRESERVE] verify_step2 的 Gate 1（npm test）和 Gate 2（DoD逐条执行）逻辑仍然保留
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('Gate 1')||!c.includes('Gate 2'))process.exit(1)"

- [ ] [PRESERVE] _pass 函数写入 .dev-seal 验签文件逻辑仍然保留
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('.dev-seal'))process.exit(1)"

- [ ] [BEHAVIOR] verify-step.sh step2 代码中包含 PRESERVE 检查逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('PRESERVE'))process.exit(1)"

- [ ] [BEHAVIOR] verify-step.sh step2 代码中包含 tdd_red_confirmed 检查逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('tdd_red_confirmed'))process.exit(1)"

- [ ] [BEHAVIOR] verify-step.sh step2 代码中包含垃圾清理检查逻辑（console.log/debugger）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('console.log')&&!c.includes('debugger'))process.exit(1)"

- [ ] [BEHAVIOR] verify-step.sh step2 代码中包含周边一致性扫描逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('consistency')||!c.includes('sibling'))process.exit(1)"

- [ ] [ARTIFACT] 01-spec.md 不含 check-dod-mapping 调用
  Test: manual:node -e "const c=require('fs').readFileSync('/Users/administrator/.claude-account3/skills/dev/steps/01-spec.md','utf8');if(c.includes('check-dod-mapping'))process.exit(1)"

- [ ] [ARTIFACT] fire-learnings-event.sh 匹配 ### 下次预防
  Test: manual:node -e "const c=require('fs').readFileSync('/Users/administrator/.claude-account3/skills/dev/scripts/fire-learnings-event.sh','utf8');if(!c.includes('下次预防'))process.exit(1)"

- [ ] [ARTIFACT] 04-ship.md 不含手动 gh pr merge 代码块（4.2节）
  Test: manual:node -e "const c=require('fs').readFileSync('/Users/administrator/.claude-account3/skills/dev/steps/04-ship.md','utf8');if(c.includes('## 4.2 合并 PR'))process.exit(1)"

- [ ] [ARTIFACT] 04-ship.md cleanup 含 .dev-gate-spec 清理
  Test: manual:node -e "const c=require('fs').readFileSync('/Users/administrator/.claude-account3/skills/dev/steps/04-ship.md','utf8');if(!c.includes('dev-gate-spec'))process.exit(1)"

- [ ] [GATE] Engine 现有测试通过
  Test: manual:cd packages/engine && npm test

## 实现方案

**要改的文件**：
1. `packages/engine/hooks/verify-step.sh` — step2 新增4个检查
2. `~/.claude-account3/skills/dev/steps/01-spec.md` — 删除 check-dod-mapping 调用
3. `~/.claude-account3/skills/dev/scripts/fire-learnings-event.sh` — 修正匹配模式
4. `~/.claude-account3/skills/dev/steps/04-ship.md` — 删除手动merge/callback，修正cleanup

**Scope 锚定**：只改这4个文件，不动其他任何逻辑。
