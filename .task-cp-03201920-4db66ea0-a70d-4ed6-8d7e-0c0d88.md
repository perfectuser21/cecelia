---
id: task-cp-03201920-4db66ea0-a70d-4ed6-8d7e-0c0d88
type: task-card
branch: cp-03201920-4db66ea0-a70d-4ed6-8d7e-0c0d88
created: 2026-03-20
---

# Task Card: 激活多平台内容发布系统

## 需求（What & Why）
**功能描述**: 启用并配置多平台发布能力（抖音、快手、微博、小红书等），打通内容发布全链路。
**背景**: 这是 Cecelia 最高优先级的业务能力（stage 4），目前各发布器孤岛状态，需要统一集成到调度系统中。
**不做什么**: 不重新开发发布器本身，只做集成和配置工作。

## 成功标准
> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [ARTIFACT] 发布器配置文件统一管理
2. [BEHAVIOR] Brain 可以调度各平台发布任务
3. [BEHAVIOR] 所有平台凭据安全配置且可访问
4. [GATE] CI 全部通过

## 验收条件（DoD）
> Test 字段在 Stage 2 探索后填写，现在写条目即可。

- [x] [ARTIFACT] 发布器服务配置统一注册到 Brain
  Test: manual:node -e "const workflows=require('fs').readdirSync('packages/workflows/skills').filter(d=>d.includes('publisher')); if(workflows.length<4)process.exit(1)"

- [x] [BEHAVIOR] 每个平台发布器可独立调用
  Test: manual:curl -s --max-time 5 -X POST "http://localhost:5221/api/brain/tasks" -H "Content-Type: application/json" -d '{"title":"Test Douyin","task_type":"douyin_publish","priority":"P2","metadata":{"test":true}}' | node -e "const d=JSON.parse(require('fs').readFileSync(0,'utf8')); if(!d.id)process.exit(1)"

- [x] [BEHAVIOR] 凭据管理系统正常工作
  Test: manual:node -e "try{require('fs').readFileSync(require('os').homedir()+'/.credentials/douyin.env','utf8');console.log('PASS')}catch(e){process.exit(1)}"

- [x] [BEHAVIOR] Brain 任务调度可派发发布任务
  Test: manual:curl -s --max-time 5 "http://localhost:5221/api/brain/health" | node -e "const d=JSON.parse(require('fs').readFileSync(0,'utf8')); if(!d.status)process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案（Stage 2 探索后填写）
**要改的文件**: （探索后填写）
**Scope 锚定**: （探索后填写）
