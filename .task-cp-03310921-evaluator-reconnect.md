---
id: task-cp-03310921-evaluator-reconnect
type: task-card
branch: cp-03310921-evaluator-reconnect
created: 2026-03-31
---

# Task Card：修复对抗网络两个缺陷 — MAX_ROUNDS 无限收敛 + Evaluator 接回 Stage 2

## 需求（What & Why）
**功能描述**: 修复 Sprint Contract Gate 的轮数上限问题，并将断开的独立 Evaluator 步骤接回 Stage 2 Pipeline
**背景**: MAX_ROUNDS=3 用超时代替收敛，背离对抗网络本意；playwright-evaluator.sh 脚本存在但从未接入流程，Generator 自验自过无第三方确认
**不做什么**: 不修改 playwright-evaluator.sh 脚本本身；不改变 CRG 逻辑；不引入新 skill

## 成功标准

- 01-spec.md Sprint Contract 收敛不再有固定轮数上限，改为死循环检测
- 02-code.md Stage 2 在自验证之后、CRG 之前有独立 Evaluator 步骤

## 验收条件（DoD）

- [x] [PRESERVE] spec_review subagent seal 文件格式不变（verdict/independent_test_plans/negotiation_result/issues）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('independent_test_plans'))process.exit(1);if(!c.includes('verdict'))process.exit(1)"

- [x] [PRESERVE] devloop-check.sh 对 .dev-gate-spec.{branch} 的检查逻辑不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('.dev-gate-spec.'))process.exit(1);if(!c.includes('spec_seal_verdict'))process.exit(1);if(!c.includes('divergence_count'))process.exit(1)"

- [x] [ARTIFACT] 01-spec.md Sprint Contract Gate 不再含 MAX_ROUNDS 变量和固定轮数上限
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(c.includes('MAX_ROUNDS'))process.exit(1);if(/最多\s*[0-9]+\s*轮/.test(c))process.exit(1)"

- [x] [BEHAVIOR] 01-spec.md Sprint Contract Gate 有死循环检测：连续 2 轮 divergence 列表相同则报 P1 FAIL
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('死循环'))process.exit(1);const ok=c.includes('连续 2 轮')||c.includes('previous_divergence')||c.includes('prev_divergence');if(!ok)process.exit(1);if(!c.includes('P1'))process.exit(1)"

- [x] [ARTIFACT] 02-code.md Stage 2 流程图中包含独立 Evaluator 步骤（playwright-evaluator.sh）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('playwright-evaluator.sh'))process.exit(1)"

- [x] [BEHAVIOR] 02-code.md 独立 Evaluator 步骤在自验证之后、CRG 之前，FAIL 打回 Generator
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');const ei=c.indexOf('playwright-evaluator.sh');if(ei===-1)process.exit(1);const si=c.indexOf('自验证');const ci=c.indexOf('code_review_gate');if(!(si<ei&&ei<ci))process.exit(1);if(!c.includes('打回')||!c.includes('Generator'))process.exit(1)"

- [x] [GATE] Engine 版本 bump，feature-registry.yml 新增 evaluator-reconnect 条目
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/features/feature-registry.yml','utf8');if(!c.includes('evaluator-reconnect'))process.exit(1);const v=require('fs').readFileSync('packages/engine/VERSION','utf8').trim();const p=v.split('.');if(p.length!==3)process.exit(1)"

## 实现方案
**要改的文件**:
- `packages/engine/skills/dev/steps/01-spec.md`（去 MAX_ROUNDS，加死循环检测）
- `packages/engine/skills/dev/steps/02-code.md`（加 2.3.4 独立 Evaluator 步骤）
- Engine 版本 5 个文件 + feature-registry.yml

**Scope 锚定**: packages/engine/skills/dev/steps/ + feature-registry.yml + 版本文件
