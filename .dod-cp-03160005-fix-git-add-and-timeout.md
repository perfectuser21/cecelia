---
id: dod-fix-git-add-and-timeout
version: 1.1.0
created: 2026-03-16
updated: 2026-03-16
changelog:
  - 1.0.0: 初始版本
  - 1.1.0: 补充 Test 字段 + [BEHAVIOR] 标签
---

# DoD: git add -A 改 -u + Stop Hook 90 分钟超时保护

## 验收清单

- [x] [ARTIFACT] `packages/engine/skills/dev/steps/03-prci.md` Step 8.4 的 `git add -A` 已改为 `git add -u`
  Test: manual:bash -c "grep -c 'git add -u' packages/engine/skills/dev/steps/03-prci.md"

- [x] [ARTIFACT] `packages/engine/skills/dev/steps/03-prci.md` Step 9.2b ⑤ 的 `git add -A` 已改为 `git add -u`
  Test: manual:bash -c "! grep -q 'git add -A' packages/engine/skills/dev/steps/03-prci.md"

- [x] [ARTIFACT] `packages/engine/lib/devloop-check.sh` 在 CI pending 检测前加入超时检查（5400 秒 = 90 分钟）
  Test: manual:bash -c "grep -c '5400' packages/engine/lib/devloop-check.sh"

- [x] [BEHAVIOR] 超时后打印警告并 `exit 0`（允许会话结束，不卡死）
  Test: manual:bash -c "grep -c 'exit 0' packages/engine/lib/devloop-check.sh"

- [x] [BEHAVIOR] `git add -u` 在提交步骤中替代 `git add -A`，防止意外 stage 未追踪文件（行为验证：03-prci.md 的提交步骤中使用 git add -u）
  Test: manual:bash -c "grep -c 'git add -u' packages/engine/skills/dev/steps/03-prci.md && node -e \"const fs=require('fs'); const c=fs.readFileSync('packages/engine/skills/dev/steps/03-prci.md','utf8'); if(c.includes('git add -A')) process.exit(1); process.stdout.write('1');\""

- [x] [ARTIFACT] Engine 版本 7 文件全部 bump 到 12.82.0
  Test: manual:bash -c "grep -c '12.82.0' packages/engine/VERSION packages/engine/.hook-core-version packages/engine/ci-tools/VERSION | grep -c ':1'"

- [x] [ARTIFACT] `feature-registry.yml` 新增 12.82.0 changelog 条目
  Test: manual:bash -c "grep -c '12.82.0' packages/engine/features/feature-registry.yml"

- [x] [ARTIFACT] `generate-path-views.sh` 已运行（path-views 文件已更新）
  Test: manual:bash -c "ls packages/engine/features/path-views/ | wc -l | tr -d ' '"

- [x] [ARTIFACT] CI 配置文件 engine-ci.yml 存在且包含 engine 触发条件
  Test: manual:bash -c "grep -c 'packages/engine' .github/workflows/engine-ci.yml"
