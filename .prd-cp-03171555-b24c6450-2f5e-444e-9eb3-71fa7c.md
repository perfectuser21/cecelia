# PRD - feat(executor): MAX_SEATS 重启状态日志 + 并发天花板评估报告

## 背景
初始化 Brain 重启孤儿恢复 initiative 的最后一环。增加并发状态可观测性，防止 MAX_SEATS 天花板成为静默失败根源。

## 目标
1. Brain 启动时打印当前 MAX_SEATS / INTERACTIVE_RESERVE 配置值
2. syncOrphanTasksOnStartup 执行后，在日志中输出 requeued/rebuilt/failed 汇总
3. 新增 GET /api/brain/capacity 端点，返回当前并发配置含 max_seats 字段

## 成功标准
- [ ] Brain 启动日志包含 MAX_SEATS 和 INTERACTIVE_RESERVE 配置值
- [ ] syncOrphanTasksOnStartup 完成后打印 requeued/rebuilt/failed 汇总
- [ ] GET /api/brain/capacity 返回 max_seats 字段
- [ ] 测试覆盖 capacity 端点 max_seats 字段存在
