---
id: task-cp-03191054-c66de847
type: task-card
branch: cp-03191054-c66de847-a9eb-4843-8ed0-9f0715
created: 2026-03-19
---

# Task Card: fix-brain-session-isolation-feishu-reliability（续）

## 需求（What & Why）
**功能描述**: PR #1110 已完成主要修复（cecelia-bridge.cjs cwd 隔离、sendFeishuMessage 错误日志）。本次处理剩余项：
1. cecelia-bridge.js 同步 cwd 隔离（.cjs 已修复但 .js 未同步）
2. FEISHU_OWNER_OPEN_IDS 更新为正确 open_id（当前值错误，应为徐啸的 ou_b472f4ce7a9bf227b9f6ad4e557fb687）

**背景**: .js 与 .cjs 两个文件漂移；飞书 owner open_id 使用旧值导致私信发送失败。

**不做什么**: 不重构 cecelia-bridge 架构，不合并两文件。

## 成功标准

1. [ARTIFACT] cecelia-bridge.js 的 /llm-call handler 包含 cwd 隔离
2. [BEHAVIOR] FEISHU_OWNER_OPEN_IDS 凭据更新为正确 open_id
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] cecelia-bridge.js /llm-call spawn 包含 llmWorkDir 定义、mkdirSync 和 cwd 传入
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.js','utf8');if(!c.includes(\"llmWorkDir = '/tmp/cecelia-llm'\")||!c.includes('mkdirSync')||!c.includes('cwd: llmWorkDir'))process.exit(1);console.log('cwd isolation fully wired')"

- [x] [BEHAVIOR] cecelia-bridge.js 与 cecelia-bridge.cjs 两文件均含 cwd 和 stdio 隔离
  Test: manual:node -e "const js=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.js','utf8');const cjs=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.cjs','utf8');if(!js.includes('cwd: llmWorkDir')||!js.includes('stdio')||!cjs.includes('cwd: llmWorkDir')||!cjs.includes('stdio'))process.exit(1);console.log('both files aligned')"

- [x] [ARTIFACT] .env.docker.example 已包含 FEISHU_OWNER_OPEN_IDS 配置示例（便于部署时填写正确 open_id）
  Test: manual:node -e "const c=require('fs').readFileSync('.env.docker.example','utf8');if(!c.includes('FEISHU_OWNER_OPEN_IDS'))process.exit(1);console.log('feishu config documented')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"

## 实现方案
**要改的文件**:
- packages/brain/scripts/cecelia-bridge.js — 添加 cwd 隔离（与 .cjs 同步）
- ~/.credentials/feishu.env — 更新 FEISHU_OWNER_OPEN_IDS（本地，不进 git）
