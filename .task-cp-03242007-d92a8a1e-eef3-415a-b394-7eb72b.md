---
id: task-cp-03242007-d92a8a1e
type: task-card
branch: cp-03242007-d92a8a1e-eef3-415a-b394-7eb72b
created: 2026-03-24
---

# Task Card: CI scope 补全 — workspace L4 触发器 + Brain-API集成测试 + routing-map

## 需求（What & Why）

**功能描述**：PR #1533 已关闭（落后 main 27 commit），但以下改动未进 main，需补全：
1. `ci-l4-runtime.yml` — 增加 workspace 触发器（apps/ 改动触发 L4 workspace job）
2. `apps/api/src/__tests__/brain-api-integration.test.ts` — Brain↔Frontend proxy 集成测试
3. `ci/routing-map.yml` — 补充 `scripts` subsystem + `ci_infra` subsystem 两个条目

**背景**：三项 CI scope 缺口：L4 不检测 workspace 改动；apps/api 缺少 Brain proxy 测试；routing-map 未覆盖 scripts/ 全目录和 ci/ 非配置文件。

**不做什么**：不修改现有 brain-integration job；不动 L2/L3；不重构测试基础设施。

## 成功标准

1. apps/ 改动时，L4 workflow 的 changes job 输出 `workspace=true`，并触发新 workspace-integration job
2. brain-api-integration.test.ts 覆盖 proxy 代理层正确性（≥3个测试用例通过）
3. routing-map.yml 新增 `scripts` 和 `ci_infra` 两个 subsystem 条目
4. CI 全部通过

## 验收条件（DoD）

- [ ] [ARTIFACT] ci-l4-runtime.yml changes job 新增 workspace 输出，并有 workspace-integration job
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes('workspace-integration'))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] brain-api-integration.test.ts 文件存在且包含至少3个测试用例
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/src/__tests__/brain-api-integration.test.ts','utf8');const m=c.match(/it\(/g)||[];if(m.length<3)process.exit(1);console.log('OK: '+m.length+' tests')"

- [ ] [ARTIFACT] routing-map.yml 新增 scripts 和 ci_infra 两个 subsystem
  Test: manual:node -e "const c=require('fs').readFileSync('ci/routing-map.yml','utf8');if(!c.includes('ci_infra:')||!c.includes('\n  scripts:'))process.exit(1);console.log('OK')"

- [ ] [PRESERVE] 现有 brain-integration job 逻辑不变（仅 brain 变动时触发）
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes(\"needs.changes.outputs.brain == 'true'\"))process.exit(1);console.log('OK')"

- [ ] [GATE] 所有现有测试通过
  Test: manual:node -e "require('fs').accessSync('apps/api/src/__tests__/brain-api-integration.test.ts')"

## 实现方案

**要改的文件**：
- `.github/workflows/ci-l4-runtime.yml` — 在 changes job 加 workspace 输出，新增 workspace-integration job
- `apps/api/src/__tests__/brain-api-integration.test.ts` — 新建，测试 Brain proxy 层
- `ci/routing-map.yml` — 新增 scripts + ci_infra 两个 subsystem

**Scope 锚定**：纯 CI scope 补全，不改业务逻辑代码
