---
id: task-cp-04080717-ce113196-52ec-42ba-a278-4d988b
type: task-card
branch: cp-04080717-ce113196-52ec-42ba-a278-4d988b
created: 2026-04-08
---

# Task Card: 内容生成引擎 v1 — AI一人公司主题库 + 日更调度器启用

## 需求（What & Why）
**功能描述**: 建立「AI一人公司」精选主题库，集成到每日选题调度器，实现每天自动创建 ≥5 条 content-pipeline 任务。
**背景**: 内容生成 KR 目标是「AI每天产出≥5条内容」。Pipeline 6阶段基础设施已就绪，但 topic-selection-scheduler.js 因「无 NotebookLM 数据源」被禁用（DISABLED=true）。现在 solo-company-case.yaml 已有 NotebookLM notebook_id，可以重新启用，但需要补充精选主题库作为种子，保证选题质量。
**不做什么**: 不修改 6-stage pipeline 逻辑；不修改 content-type-registry；不改变发布系统；不修改 MAX_REVIEW_RETRY 等参数。

## 要改的文件
- 新增: `packages/brain/src/content-types/ai-solopreneur-topic-library.js`
- 修改: `packages/brain/src/topic-selector.js`（generateTopics 接受 seedKeywords）
- 修改: `packages/brain/src/topic-selection-scheduler.js`（DISABLED=false，集成主题库）
- 新增: `packages/brain/src/__tests__/content-generation-engine-v1.test.ts`

## 成功标准
> [BEHAVIOR] 调度器启用，每日触发 ≥5 条 content-pipeline 任务

## 验收条件（DoD）

- [x] [ARTIFACT] `packages/brain/src/content-types/ai-solopreneur-topic-library.js` 存在，包含 ≥20 个精选 AI一人公司主题关键词
  Test: manual:node -e "const m=require('./packages/brain/src/content-types/ai-solopreneur-topic-library.js'); if(!m.AI_SOLOPRENEUR_TOPICS||m.AI_SOLOPRENEUR_TOPICS.length<20)process.exit(1); console.log('ok:',m.AI_SOLOPRENEUR_TOPICS.length,'topics')"

- [x] [BEHAVIOR] `topic-selection-scheduler.js` 中 `DISABLED` 已改为 `false`
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/topic-selection-scheduler.js','utf8'); if(c.includes('const DISABLED = true'))process.exit(1); console.log('DISABLED=false confirmed')"

- [x] [BEHAVIOR] `generateTopics()` 接受 `seedKeywords` 参数并注入 Prompt
  Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/topic-selector.js','utf8'); if(!c.includes('seedKeywords'))process.exit(1); console.log('seedKeywords param confirmed')"

- [x] [ARTIFACT] 测试文件 `packages/brain/src/__tests__/content-generation-engine-v1.test.ts` 存在
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/content-generation-engine-v1.test.ts'); console.log('test file exists')"

## 实现方案
**核心逻辑**: 主题库提供30个精选「AI一人公司」关键词 → 调度器每日随机取 5 个种子词传给 generateTopics() → LLM 围绕这些种子生成完整选题（含 hook/title_candidates）→ 存入 topic_suggestions 队列 → 2小时后自动晋级创建 content-pipeline tasks。
