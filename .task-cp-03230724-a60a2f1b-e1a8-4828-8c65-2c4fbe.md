---
id: task-cp-03230724-a60a2f1b-e1a8-4828-8c65-2c4fbe
type: task-card
branch: cp-03230724-a60a2f1b-e1a8-4828-8c65-2c4fbe
created: 2026-03-23
---

# Task Card: /dev Stage3 drift detection + spec_review 测试层检查

## 需求（What & Why）
**功能描述**: 两个独立改进：
1. Stage 3 push 前新增 drift detection 步骤（3.1.4）：检测当前 git 改动文件是否偏离 Task Card `## 实现方案` 中声明的范围，输出警告（不阻塞）
2. spec_review SKILL.md 新增维度 F：测试层检查（unit/integration/e2e 匹配），验证 DoD Test 命令的测试层级与被测行为是否匹配

**背景**: Stage 3 目前没有 scope 检查，改动可能悄悄超出 DoD 范围。spec_review 只验证 Test 命令格式，不验证测试层级是否合适（如用单元测试覆盖 API 行为）。

**不做什么**: 不阻塞 push（drift detection 仅 warning）；不修改 CI 流程；不改 Stage 2 代码步骤。

## 成功标准

- Stage 3 `03-integrate.md` 在 3.1.3 之后、3.1.5 之前有 3.1.4 Drift Detection 步骤
- spec-review `SKILL.md` 有维度 F：测试层检查，包含 unit/integration/e2e 匹配规则
- Engine 版本文件 5 个（package.json/package-lock.json/VERSION/.hook-core-version/regression-contract.yaml）全部同步更新到 13.41.2，feature-registry.yml 含 13.41.2 changelog 条目
- 仓库内 `packages/engine/skills/dev/steps/03-integrate.md` 包含 3.1.4 和 drift 关键词

## 验收条件（DoD）

- [x] [ARTIFACT] `packages/engine/skills/dev/steps/03-integrate.md` 包含 `3.1.4 Drift Detection` 章节
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('3.1.4'))process.exit(1);if(!c.includes('drift'))process.exit(1)"

- [x] [ARTIFACT] `packages/workflows/skills/spec-review/SKILL.md` 包含维度 F 测试层检查
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('维度 F'))process.exit(1);if(!c.includes('unit'))process.exit(1)"

- [x] [ARTIFACT] Engine package.json 版本为 13.41.2
  Test: manual:node -e "const v=require('./packages/engine/package.json').version;if(v!=='13.41.2')process.exit(1)"

- [x] [ARTIFACT] Engine VERSION/.hook-core-version/regression-contract.yaml 均为 13.41.2
  Test: manual:bash -c "node -e \"const fs=require('fs');['packages/engine/VERSION','packages/engine/.hook-core-version'].forEach(f=>{const c=fs.readFileSync(f,'utf8').trim();if(c!=='13.41.2'){console.error('Wrong version in '+f+': '+c);process.exit(1)}});const rc=fs.readFileSync('packages/engine/regression-contract.yaml','utf8');if(!rc.includes('13.41.2')){console.error('regression-contract.yaml missing 13.41.2');process.exit(1)}\""

- [x] [ARTIFACT] feature-registry.yml 有 13.41.2 changelog 条目
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/features/feature-registry.yml','utf8');if(!c.includes('13.41.2'))process.exit(1)"

- [x] [ARTIFACT] 仓库内 03-integrate.md 包含 drift 检测内容
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('3.1.4'))process.exit(1);if(!c.includes('drift'))process.exit(1)"

- [x] [PRESERVE] 03-integrate.md 的 3.1.3 Engine 版本检查章节仍然存在
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('3.1.3'))process.exit(1)"

- [x] [PRESERVE] spec-review SKILL.md 维度 A 至 E 均保留
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');['维度 A','维度 B','维度 C','维度 D','维度 E'].forEach(d=>{if(!c.includes(d)){console.error('Missing: '+d);process.exit(1)}})"

- [x] [BEHAVIOR] drift detection bash 代码块语法正确（可执行）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');const parts=c.split('```');const idx=parts.findIndex(p=>p.startsWith('bash')&&p.includes('Drift Detection'));if(idx<0){console.error('Drift Detection block not found');process.exit(1)};fs.writeFileSync('/tmp/drift-syntax-check.sh',parts[idx].slice(4));const r=require('child_process').spawnSync('bash',['-n','/tmp/drift-syntax-check.sh'],{encoding:'utf8'});if(r.status!==0){console.error(r.stderr);process.exit(1)}"

- [x] [PRESERVE] check-dod-mapping.cjs 正常运行
  Test: manual:node packages/engine/scripts/devgate/check-dod-mapping.cjs

- [x] [GATE] CI DevGate 核心脚本语法正确
  Test: manual:bash -c "bash -n packages/engine/scripts/devgate/check-dod-mapping.cjs 2>/dev/null; node --check packages/engine/scripts/devgate/check-dod-mapping.cjs"

## 实现方案

**要改的文件**:
- `packages/engine/skills/dev/steps/03-integrate.md`（新增 3.1.4）
- `packages/workflows/skills/spec-review/SKILL.md`（新增维度 F）
- `packages/engine/package.json`（版本 bump）
- `packages/engine/package-lock.json`（版本 bump）
- `packages/engine/VERSION`（版本 bump）
- `packages/engine/.hook-core-version`（版本 bump）
- `packages/engine/regression-contract.yaml`（版本 bump）
- `packages/engine/features/feature-registry.yml`（changelog + generate-path-views）
- `~/.claude-account1/skills/dev/steps/03-integrate.md`（全局同步）

**Scope 锚定**: `packages/engine/skills/dev/`, `packages/workflows/skills/spec-review/`, Engine 版本文件
