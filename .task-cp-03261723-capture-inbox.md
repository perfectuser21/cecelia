# Task Card: feat(capture) — 建立 Capture 基础设施

## 目标
为 Cecelia 建立用户数据捕获入口（Capture Inbox），同时为 OKR 相关表添加 owner 字段区分用户数据与系统数据。

## 分支
cp-03261723-capture-inbox

## 成功标准

1. captures 表已在 DB 中创建，含 id/content/source/status/area_id/project_id/extracted_to/owner/created_at/updated_at 字段
2. owner 字段已添加到 areas/objectives/key_results/okr_projects（默认 'user'）
3. GET /api/captures 返回 200 + JSON 数组
4. POST /api/captures 创建 capture 返回 201
5. PATCH /api/captures/:id 更新 status 返回 200
6. /gtd/inbox 页面可访问并展示收件箱列表
7. QuickCapture 组件可见，Enter 提交后 inbox 可见新条目
8. Brain 迁移文件已创建（198_captures_table.sql）
9. GTDInbox 组件文件存在（GTDInbox.tsx）
10. QuickCapture 组件文件存在（QuickCapture.tsx）

## DoD

- [x] [BEHAVIOR] GET /api/captures 返回列表（行为验证）
  Test: tests/capture-api.test.js
  Verify: GET /api/captures 响应 200 + JSON 数组

- [x] [BEHAVIOR] POST /api/captures 创建成功（行为验证）
  Test: tests/capture-api.test.js
  Verify: POST /api/captures with content 响应 201

- [x] [BEHAVIOR] PATCH /api/captures/:id 更新 status（行为验证）
  Test: tests/capture-api.test.js
  Verify: PATCH /api/captures/:id with status 响应 200

- [x] [ARTIFACT] captures 路由已注册到 server.ts
  Test: manual: node -e "const c=require('fs').readFileSync('apps/api/src/dashboard/server.ts','utf8');if(!c.includes('/api/captures'))process.exit(1)"
  Verify: server.ts 中包含 /api/captures 路由注册

- [x] [ARTIFACT] /gtd/inbox 路由已注册到 GTD feature
  Test: manual: node -e "const c=require('fs').readFileSync('apps/api/features/gtd/index.ts','utf8');if(!c.includes('/gtd/inbox'))process.exit(1)"
  Verify: gtd/index.ts 中包含 /gtd/inbox 路由

- [x] [ARTIFACT] Brain 迁移文件已创建
  Test: manual: node -e "require('fs').accessSync('packages/brain/migrations/198_captures_table.sql')"
  Verify: 迁移文件存在

- [x] [ARTIFACT] GTDInbox 组件文件存在
  Test: manual: node -e "require('fs').accessSync('apps/api/features/gtd/pages/GTDInbox.tsx')"
  Verify: GTDInbox.tsx 组件存在

- [x] [ARTIFACT] QuickCapture 组件文件存在
  Test: manual: node -e "require('fs').accessSync('apps/api/features/gtd/components/QuickCapture.tsx')"
  Verify: QuickCapture.tsx 组件存在

## 改动范围

### 1. DB Migration
- `packages/brain/migrations/198_captures_table.sql`

### 2. API
- `apps/api/src/task-system/captures.js`
- `apps/api/src/dashboard/server.ts`

### 3. Dashboard UI
- `apps/api/features/gtd/components/QuickCapture.tsx`
- `apps/api/features/gtd/pages/GTDInbox.tsx`
- `apps/api/features/gtd/index.ts`

### 4. Tests
- `tests/capture-api.test.js`
- `apps/api/tests/capture/api.test.ts`
- `apps/api/tests/capture/db.test.ts`
