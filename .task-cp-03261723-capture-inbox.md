# Task Card: feat(capture) — 建立 Capture 基础设施

## 目标
为 Cecelia 建立用户数据捕获入口（Capture Inbox），同时为 OKR 相关表添加 owner 字段区分用户数据与系统数据。

## 分支
cp-03261723-capture-inbox

## 成功标准

- [ ] captures 表已在 DB 中创建，含 id/content/source/status/area_id/project_id/extracted_to/owner/created_at/updated_at 字段
- [ ] owner 字段已添加到 areas/objectives/key_results/okr_projects（默认 'user'）
- [ ] GET /api/captures 返回 200 + JSON 数组
- [ ] POST /api/captures 创建 capture 返回 201
- [ ] PATCH /api/captures/:id 更新 status 返回 200
- [ ] /gtd/inbox 页面可访问并展示收件箱列表
- [ ] QuickCapture 组件可见，Enter 提交后 inbox 可见新条目

## DoD

### [BEHAVIOR] captures 表已创建
  Test: tests/capture/db.test.ts
  Verify: captures 表在 DB 中存在，含必要字段

### [BEHAVIOR] areas/objectives/key_results/okr_projects 含 owner 字段
  Test: manual: node -e "const {Pool}=require('pg');const p=new Pool({host:'localhost',port:5432,database:'cecelia',user:'cecelia',password:'CeceliaUS2026'});p.query(\"SELECT column_name FROM information_schema.columns WHERE table_name='areas' AND column_name='owner'\").then(r=>{if(r.rows.length===0)process.exit(1);p.end()})"
  Verify: areas 表有 owner 字段

### [BEHAVIOR] GET /api/captures 返回列表
  Test: tests/capture/api.test.ts
  Verify: GET /api/captures 响应 200 + JSON 数组

### [BEHAVIOR] POST /api/captures 创建成功
  Test: tests/capture/api.test.ts
  Verify: POST /api/captures with content 响应 201

### [BEHAVIOR] PATCH /api/captures/:id 更新 status
  Test: tests/capture/api.test.ts
  Verify: PATCH /api/captures/:id with status 响应 200

### [BEHAVIOR] captures 路由已注册
  Test: manual: node -e "const c=require('fs').readFileSync('apps/api/src/dashboard/server.ts','utf8');if(!c.includes('/api/captures'))process.exit(1)"
  Verify: server.ts 中包含 /api/captures 路由注册

### [BEHAVIOR] /gtd/inbox 路由已注册到 GTD feature
  Test: manual: node -e "const c=require('fs').readFileSync('apps/api/features/gtd/index.ts','utf8');if(!c.includes('/gtd/inbox'))process.exit(1)"
  Verify: gtd/index.ts 中包含 /gtd/inbox 路由

### [BEHAVIOR] GTDInbox 组件文件存在
  Test: manual: node -e "require('fs').accessSync('apps/api/features/gtd/pages/GTDInbox.tsx')"
  Verify: GTDInbox.tsx 组件存在

### [BEHAVIOR] QuickCapture 组件文件存在
  Test: manual: node -e "require('fs').accessSync('apps/api/features/gtd/components/QuickCapture.tsx')"
  Verify: QuickCapture.tsx 组件存在

### [BEHAVIOR] Brain 迁移文件已创建
  Test: manual: node -e "require('fs').accessSync('packages/brain/migrations/197_captures_table.sql')"
  Verify: 迁移文件存在

## 改动范围

### 1. DB Migration
- `packages/brain/migrations/197_captures_table.sql`

### 2. API
- `apps/api/src/task-system/captures.js`
- `apps/api/src/dashboard/server.ts`

### 3. Dashboard UI
- `apps/api/features/gtd/components/QuickCapture.tsx`
- `apps/api/features/gtd/pages/GTDInbox.tsx`
- `apps/api/features/gtd/index.ts`

### 4. Tests
- `apps/api/tests/capture/api.test.ts`
- `apps/api/tests/capture/db.test.ts`
