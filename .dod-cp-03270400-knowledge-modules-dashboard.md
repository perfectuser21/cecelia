# DoD: Knowledge Modules 深度知识页接入 Dashboard

branch: cp-03270400-knowledge-modules-dashboard

## 产出物

- [x] [ARTIFACT] KnowledgeHome.tsx 新增深度知识页卡片（5th card → /knowledge/modules）
  Test: `node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/pages/KnowledgeHome.tsx','utf8');if(!c.includes('/knowledge/modules'))process.exit(1);console.log('OK')"`

- [x] [ARTIFACT] KnowledgePageViewer.tsx 新组件（fetch HTML + dangerouslySetInnerHTML，无 iframe）
  Test: `node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/pages/KnowledgePageViewer.tsx','utf8');if(c.includes('<iframe'))process.exit(1);if(!c.includes('dangerouslySetInnerHTML'))process.exit(1);console.log('OK')"`

- [x] [ARTIFACT] knowledge/index.ts 注册 KnowledgePageViewer 路由
  Test: `node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/index.ts','utf8');if(!c.includes('KnowledgePageViewer'))process.exit(1);console.log('OK')"`

- [x] [ARTIFACT] KnowledgeModuleDetail.tsx 已完成模块链接到 /knowledge/view（Dashboard 内联）
  Test: `node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/pages/KnowledgeModuleDetail.tsx','utf8');if(!c.includes('/knowledge/view'))process.exit(1);console.log('OK')"`

## 行为验证

- [x] [BEHAVIOR] KnowledgeModules 页面按 brain/engine/workflows/system 分组展示模块卡片
  Test: tests/knowledge-modules.test.ts

- [x] [BEHAVIOR] KnowledgePageViewer 不使用 iframe 渲染 HTML 内容
  Test: tests/knowledge-modules.test.ts
