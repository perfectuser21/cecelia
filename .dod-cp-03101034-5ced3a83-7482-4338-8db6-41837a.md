## DoD - 修复 Cortex _reflectionState 持久化恢复不检查过期

### 验收清单

- [x] 探索完成：已确认 `_loadReflectionStateFromDB`（line 200）和 `_checkReflectionBreaker`（line 307）均使用 `firstSeen` 作为过期判断依据，需改为 `lastSeen`（滑动窗口语义）
  Test: manual:bash -c "grep -n 'firstSeen.*REFLECTION_WINDOW' packages/brain/src/cortex.js | wc -l | grep -E '^0$'"

- [x] `_loadReflectionStateFromDB`：过期检查从 `firstSeen` 改为 `lastSeen`（`val.lastSeen ?? val.firstSeen`，向下兼容旧 DB 条目）
  Test: manual:bash -c "grep -c 'lastSeen.*REFLECTION_WINDOW_MS\|REFLECTION_WINDOW_MS.*lastSeen' packages/brain/src/cortex.js"

- [x] `_checkReflectionBreaker`：in-memory 过期检查从 `state.firstSeen` 改为 `state.lastSeen`（滑动窗口语义一致）
  Test: manual:bash -c "grep -c 'state.lastSeen.*REFLECTION_WINDOW_MS\|REFLECTION_WINDOW_MS.*state.lastSeen' packages/brain/src/cortex.js"

- [x] 新增单测：模拟 Brain 重启，过期（lastSeen >30min）条目不被恢复，count 重置为 1
  Test: manual:bash -c "grep -c 'restart.*expired\|expired.*restart\|DoD-5\|不被恢复' packages/brain/src/__tests__/cortex-dedup-persist.test.js"

- [x] 新增单测：lastSeen 在 30min 内的条目在重启后正确恢复（不误删活跃状态）
  Test: manual:bash -c "grep -c 'lastSeen.*recent\|recent.*lastSeen\|DoD-6\|活跃.*恢复\|恢复.*活跃' packages/brain/src/__tests__/cortex-dedup-persist.test.js"

- [x] 现有测试 DoD-3 通过（expiredHash 的 lastSeen=31min ago > 30min，仍然过期）
  Test: manual:bash -c "grep -c 'DoD-3' packages/brain/src/__tests__/cortex-dedup-persist.test.js"

### 非目标

- 不修改 `_outputDedupState` 相关逻辑（不在本 PRD 范围）
- 不修改 `REFLECTION_WINDOW_MS` 的值（保持 30 分钟）
- 不修改熔断阈值 `REFLECTION_BREAK_THRESHOLD`
