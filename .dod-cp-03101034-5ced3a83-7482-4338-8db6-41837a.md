## DoD - 修复 Cortex _reflectionState 持久化恢复不检查过期

### 验收清单

- [x] `_loadReflectionStateFromDB`：过期检查从 `firstSeen` 改为 `lastActivity`（`val.lastSeen ?? val.firstSeen`，向下兼容旧 DB 条目）
  Test: manual:bash -c "grep -c 'lastActivity.*REFLECTION_WINDOW_MS\|REFLECTION_WINDOW_MS.*lastActivity' packages/brain/src/cortex.js"

- [x] `_checkReflectionBreaker`：in-memory 过期检查从 `state.firstSeen` 改为 `lastActivity`（`state?.lastSeen ?? state?.firstSeen ?? 0`，滑动窗口语义一致）
  Test: manual:bash -c "grep -c 'lastActivity' packages/brain/src/cortex.js"

- [x] 新增单测：模拟 Brain 重启，过期（lastSeen >30min）条目不被恢复，count 重置为 1
  Test: manual:bash -c "grep -c 'DoD-5' packages/brain/src/__tests__/cortex-dedup-persist.test.js"

- [x] 新增单测：lastSeen 在 30min 内的活跃条目在重启后被正确恢复（不误删活跃状态）
  Test: manual:bash -c "grep -c 'DoD-6' packages/brain/src/__tests__/cortex-dedup-persist.test.js"

- [x] 现有测试 DoD-3 通过（expiredHash 的 lastSeen=31min ago > 30min，仍然过期）
  Test: manual:bash -c "grep -c 'DoD-3' packages/brain/src/__tests__/cortex-dedup-persist.test.js"

### 非目标

- 不修改 `_outputDedupState` 相关逻辑（不在本 PRD 范围）
- 不修改 `REFLECTION_WINDOW_MS` 的值（保持 30 分钟）
- 不修改熔断阈值 `REFLECTION_BREAK_THRESHOLD`
