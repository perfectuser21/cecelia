---
id: dod-cp-03100836-tick-timeout-requeue
version: 1.1.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
  - 1.1.0: 修复测试命令格式（migration 号升到 143，去掉 echo OK）
---

## DoD - feat(tick): 超时任务自动 requeue，最多3次后隔离

### 验收清单

- [x] Migration 143 已创建：`tasks.retry_count INTEGER DEFAULT 0`
  Test: manual:bash -c "ls packages/brain/migrations/143_task_retry_count.sql"

- [x] selfcheck.js EXPECTED_SCHEMA_VERSION 更新为 '143'
  Test: manual:bash -c "grep -q \"EXPECTED_SCHEMA_VERSION = '143'\" packages/brain/src/selfcheck.js"

- [x] tasks 查询 SELECT 包含 retry_count 字段
  Test: manual:bash -c "grep -q 'retry_count' packages/brain/src/tick.js"

- [x] autoFailTimedOutTasks：retry_count < 3 时 requeue（status=queued, retry_count+1, started_at=NULL）
  Test: manual:bash -c "grep -q 'retryCount < 3' packages/brain/src/tick.js"

- [x] autoFailTimedOutTasks：retry_count >= 3 时直接 quarantine
  Test: manual:bash -c "grep -q 'quarantineTask' packages/brain/src/tick.js"

- [x] startup-recovery.js 孤儿任务按 retry_count 逻辑处理（<3 requeue，>=3 quarantine）
  Test: manual:bash -c "grep -q 'ORPHAN_MAX_RETRIES' packages/brain/src/startup-recovery.js"

- [x] DEFINITION.md schema_version 同步为 143
  Test: manual:bash -c "grep -q 'Schema 版本: 143' DEFINITION.md"
