---
id: dod-cp-03100836-tick-timeout-requeue
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

## DoD - feat(tick): 超时任务自动 requeue，最多3次后隔离

### 验收清单

- [x] Migration 142 已创建：`tasks.retry_count INTEGER DEFAULT 0`
  Test: manual:bash -c "ls packages/brain/migrations/142_*.sql | grep retry"

- [x] selfcheck.js EXPECTED_SCHEMA_VERSION 更新为 '142'
  Test: manual:bash -c "grep -q \"EXPECTED_SCHEMA_VERSION = '142'\" packages/brain/src/selfcheck.js && echo OK"

- [x] tasks 查询 SELECT 包含 retry_count 字段
  Test: manual:bash -c "grep 'SELECT id, title, status' packages/brain/src/tick.js | grep -q retry_count && echo OK"

- [x] autoFailTimedOutTasks：retry_count < 3 时 requeue（status=queued, retry_count+1, started_at=NULL）
  Test: manual:bash -c "grep -q 'retry_count < 3\|retryCount < 3' packages/brain/src/tick.js && echo OK"

- [x] autoFailTimedOutTasks：retry_count >= 3 时直接 quarantine
  Test: manual:bash -c "grep -q 'quarantineTask' packages/brain/src/tick.js && echo OK"

- [x] startup-recovery.js 孤儿任务按 retry_count 逻辑处理（<3 requeue，>=3 quarantine）
  Test: manual:bash -c "grep -q 'retry_count' packages/brain/src/startup-recovery.js && echo OK"

- [x] DEFINITION.md schema_version 同步为 142
  Test: manual:bash -c "grep -q '142' DEFINITION.md && echo OK"

- [x] tick.js 单元测试覆盖 requeue 和 quarantine 两个分支
  Test: manual:bash -c "grep -q 'retry_count\|requeue' packages/brain/tests/tick.test.js 2>/dev/null && echo OK || echo SKIP"
