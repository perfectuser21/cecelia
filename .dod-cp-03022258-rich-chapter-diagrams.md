---
id: dod-rich-chapter-diagrams
version: 1.0.0
created: 2026-03-02
---

# DoD：各章 SVG 配图深度重绘

- [ ] 感知层图包含全部 16 个信号
  - Test: manual:bash -c "grep -q 'task_fail_rate_24h' apps/api/features/brain/pages/SuperBrain.tsx"

- [ ] 意识核心图包含 L0/L1/L2 三层
  - Test: manual:bash -c "grep -q 'L0 脑干' apps/api/features/brain/pages/SuperBrain.tsx && grep -q 'L1 丘脑' apps/api/features/brain/pages/SuperBrain.tsx && grep -q 'L2 皮层' apps/api/features/brain/pages/SuperBrain.tsx"

- [ ] 风险标注存在（⚠️ warnBox）
  - Test: manual:bash -c "grep -q 'warnBox' apps/api/features/brain/pages/SuperBrain.tsx"

- [ ] 五章 SVG 都有对应 blockId
  - Test: manual:bash -c "grep -q 'interfaceDiagram' apps/api/features/brain/pages/SuperBrain.tsx && grep -q 'perceptionDiagram' apps/api/features/brain/pages/SuperBrain.tsx && grep -q 'coreDiagram' apps/api/features/brain/pages/SuperBrain.tsx"

- [ ] Dashboard build 无 TS 错误
  - Test: manual:bash -c "cd apps/dashboard && npm run build 2>&1 | grep -i 'error' | grep -v 'warning' | grep -v 'chunk' && exit 1 || exit 0"
