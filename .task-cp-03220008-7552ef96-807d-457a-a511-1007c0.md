---
id: task-cp-03220008-7552ef96-807d-457a-a511-1007c0
type: task-card
branch: cp-03220008-7552ef96-807d-457a-a511-1007c0
created: 2026-03-22
---

# Task Card: 知识反刍 Batch 1/8 — 消化25条积压知识·提炼行动任务

## 需求（What & Why）

**功能描述**: 从知识库读取前 25 条 Active 知识条目，系统性消化并转化为可执行的 dev 任务，写入 Brain DB。输出反刍报告（Learning 文件）。

**背景**: 知识库中有 200 条 Active 知识条目，全部为 learning_rule 类型，积累自历次 PR Learning。这些知识条目尚未转化为系统行动，是"认知债"。需在系统低负载窗口清零，Batch 1/8 处理前 25 条。

**不做什么**: 不修改知识条目 status（只读不写）；不写业务代码；不改 Brain 逻辑代码；Learning 文件是本任务唯一 git 产物。

## 成功标准

1. [ARTIFACT] `docs/learnings/` 下存在本分支 Learning 文件，含反刍报告（处理了哪 25 条、生成了几个 dev 任务、3 个核心洞察）
2. [BEHAVIOR] Brain DB 中新增了 ≥3 个 dev 任务（有 RETURNING id 证明）
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] Learning 文件存在于 `docs/learnings/cp-03220008-7552ef96-807d-457a-a511-1007c0.md`，含 `### 根本原因` 章节
  Test: manual:node -e "const c=require('fs').readFileSync('docs/learnings/cp-03220008-7552ef96-807d-457a-a511-1007c0.md','utf8');if(!c.includes('### 根本原因'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] Learning 文件含反刍报告正文（包含"处理了"字样或条目列表）
  Test: manual:node -e "const c=require('fs').readFileSync('docs/learnings/cp-03220008-7552ef96-807d-457a-a511-1007c0.md','utf8');if(!c.includes('Batch 1'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] Brain DB 中存在 ≥3 个由本任务创建的 dev 任务（title 含"来自知识反刍 Batch 1"）
  Test: manual:bash -c "curl -s 'http://localhost:5221/api/brain/tasks?limit=50&status=queued' | node -e \"const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));const n=d.filter(t=>t.title&&t.title.includes('知识反刍 Batch 1')).length;console.log('匹配任务数:'+n);if(n<3)process.exit(1)\""

- [x] [ARTIFACT] Learning 文件含有 3 个核心洞察章节（确认反刍报告完整性）
  Test: manual:node -e "const c=require('fs').readFileSync('docs/learnings/cp-03220008-7552ef96-807d-457a-a511-1007c0.md','utf8');if(!c.includes('### 下次预防'))process.exit(1);console.log('OK')"

## 实现方案（Stage 2 探索后填写）

**要改的文件**:
- `docs/learnings/cp-03220008-7552ef96-807d-457a-a511-1007c0.md`（新建 Learning 文件）

**Scope 锚定**:
1. 读取知识 API：`GET http://localhost:5221/api/brain/knowledge?limit=25&offset=0`（已完成于 Stage 1 探索）
2. 分析 25 条知识 → 提炼 3-5 个行动方向
3. POST Brain API 创建 ≥3 个 dev 任务
4. 写 Learning 文件记录反刍报告
