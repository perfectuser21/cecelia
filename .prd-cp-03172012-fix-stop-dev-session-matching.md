# PRD: 修复 stop-dev.sh 第4匹配case — lock无标识符时按分支匹配

## 功能描述

修复 hooks/stop-dev.sh session 匹配逻辑：当 .dev-lock 文件中 tty=not-a-tty 且 session_id 为空时，三个现有匹配 case 均无法命中，导致 /dev 会话中途自动退出。

## 成功标准

1. hooks/stop-dev.sh 两处 for loop 各含第4个 elif case（lock 无标识符 → 按分支匹配）
2. record-step.sh JSONL 输出改为紧凑格式（每行一条记录，避免并发写入解析冲突）
3. record-step.test.ts 和 generate-feedback-report.test.ts 并发测试不再失败
4. CI 全部通过
