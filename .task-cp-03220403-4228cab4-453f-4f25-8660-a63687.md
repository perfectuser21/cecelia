# Task Card: 重构 cecelia-routes.mapStatus（复杂度 26 → 10）

## PRD

### 背景

Brain 复杂度扫描器（ComplexityScanner）于 2026-03-21 报告 `packages/brain/src/cecelia-routes.js`
中的 `mapStatus` 函数圈复杂度为 26（阈值 10），触发本任务。

### 实际情况分析

调查发现这是一个**已修复的误报**：

1. PR #1353 (`fix(brain): 修复 ComplexityScanner 函数边界检测，解决 mapStatus 误报`)
   已修复了导致该误报的 scanner bug（函数边界检测算法缺陷）
2. 当前 `mapStatus` 实现圈复杂度 = 2（远低于阈值 10）
3. 重新运行扫描器确认：`mapStatus` 不再出现在高复杂度列表中

### 任务目标

导出 `mapStatus` 函数以支持直接单元测试，并补充直接覆盖所有 STATUS_MAP 映射的测试用例，
替换现有只能通过路由间接测试的方式。

## 成功标准

- mapStatus 圈复杂度 < 10（当前 CC=2，已满足）
- mapStatus 可直接 import 测试，无需通过路由绕行
- 所有 STATUS_MAP 映射（6种状态 + 未知状态默认值）有直接单元测试覆盖

## DoD

- [ ] [ARTIFACT] `cecelia-routes.js` 中 `mapStatus` 已命名导出
- [ ] [BEHAVIOR] 测试文件直接 import `mapStatus`，验证所有 6 种状态映射正确（queued/in_progress/running/completed/failed/cancelled）及未知状态默认 pending
- [ ] [GATE] `tests:packages/brain/src/__tests__/cecelia-routes.test.js` 全部通过
