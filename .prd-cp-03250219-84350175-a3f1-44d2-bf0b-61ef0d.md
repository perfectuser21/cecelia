# PRD: 扩展 check-coverage-completeness 覆盖 Brain 代码

## 背景
将 packages/engine/scripts/devgate/check-coverage-completeness.mjs 的覆盖率检查逻辑扩展到 packages/brain/src/。
扫描 Brain 所有 .js 文件，检查是否有对应测试文件。高风险模块（tick.js/thalamus.js/executor.js/cortex.js/planner.js）缺测试 → error，其余 → warning。结果写入 CI L3，Brain tick 定期触发扫描并自动创建补测试任务。

## 成功标准

- check-coverage-completeness.mjs 新增 checkBrainCoverage() 函数并导出 HIGH_RISK_BRAIN_MODULES
- CI L3 brain-unit job 新增步骤：运行 brain coverage completeness 检查，高风险模块缺测试时 exit 1
- 现有 Engine 覆盖率测试仍然全部通过（无回归）
- 高风险模块通过前缀匹配检测（executor-*.test.js 视为已覆盖）
