---
id: dod-cp-03122057-learning-dedup
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

# DoD - feat(learning): OAuth 401 失败 Learning 聚合去重

## 验收清单

- [x] Migration 152 文件存在
  Test: manual:bash -c "ls packages/brain/migrations/152_learnings_occurrence_count.sql | grep -c 'sql'"

- [x] Migration 152：learnings 表新增 occurrence_count INTEGER DEFAULT 1
  Test: manual:bash -c "grep -c 'occurrence_count' packages/brain/migrations/152_learnings_occurrence_count.sql"

- [x] Migration 152：learnings 表新增 error_type VARCHAR(50) 和 updated_at TIMESTAMP
  Test: manual:bash -c "grep -c 'error_type' packages/brain/migrations/152_learnings_occurrence_count.sql"

- [x] Migration 153：历史去重聚合（按 category + day 分组，保留最新代表记录）
  Test: manual:bash -c "grep -c 'digested' packages/brain/migrations/153_learnings_historical_dedup.sql"

- [x] selfcheck.js EXPECTED_SCHEMA_VERSION 更新为 '153'
  Test: manual:bash -c "grep -c \"'153'\" packages/brain/src/selfcheck.js"

- [x] learning.js 新增 upsertFailureLearning() 导出函数
  Test: manual:bash -c "grep -c 'upsertFailureLearning' packages/brain/src/learning.js"

- [x] upsertFailureLearning 检查 24h 内同 category + error_type 是否存在
  Test: manual:bash -c "grep -c 'INTERVAL' packages/brain/src/learning.js"

- [x] quarantine.js 调用 upsertFailureLearning（不再直接 INSERT）
  Test: manual:bash -c "grep -c 'upsertFailureLearning' packages/brain/src/quarantine.js"

- [x] 单元测试：upsertFailureLearning 去重逻辑覆盖（存在时 increment，不存在时 insert）
  Test: manual:bash -c "grep -c 'upsertFailureLearning' packages/brain/src/__tests__/learning-dedup.test.js"

- [x] 单元测试：历史聚合 migration SQL 关键字验证
  Test: manual:bash -c "ls packages/brain/src/__tests__/learning-dedup.test.js | grep -c 'learning-dedup'"
