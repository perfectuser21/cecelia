# Task Card: Desire Unblock — suggestion-cycle.js

Branch: cp-03250816-memory-desire-unblock
Brain Task: 0f8fd4a5-269b-4a3e-8875-38dae63ffd23

## 成功标准

- `getActiveDesiresForSuggestion` 查询 pending + urgency≥7 的 desire（最多5条）
- `buildSuggestionPrompt` 有 desire 时注入欲望上下文，无 desire 时返回纯上下文
- `runSuggestionCycle` 在 tick 中可调用，创建 suggestion 记录
- 现有 suggestion-dispatcher / suggestion-triage 接口无破坏

## DoD

- [x] [ARTIFACT] `getActiveDesiresForSuggestion(dbPool)` 函数存在于 `packages/brain/src/suggestion-cycle.js`
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/suggestion-cycle.js');console.log('PASS')"

- [x] [ARTIFACT] `buildSuggestionPrompt(context, desires)` 函数存在于 `packages/brain/src/suggestion-cycle.js`
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/suggestion-cycle.js','utf8');if(c.indexOf('buildSuggestionPrompt')<0)process.exit(1);console.log('PASS')"

- [x] [ARTIFACT] `runSuggestionCycle(dbPool)` 函数存在于 `packages/brain/src/suggestion-cycle.js`
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/suggestion-cycle.js','utf8');if(c.indexOf('runSuggestionCycle')<0)process.exit(1);console.log('PASS')"

- [x] [BEHAVIOR] 测试文件 `desire-suggestion-cycle.test.js` 存在，覆盖有/无 desire 的 prompt 构建
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/desire-suggestion-cycle.test.js');console.log('PASS')"

- [x] [PRESERVE] 现有 `dispatchPendingSuggestions`、`createSuggestion`、`runTriage` 签名不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/suggestion-dispatcher.js','utf8');if(c.indexOf('dispatchPendingSuggestions')<0)process.exit(1);console.log('PASS')"

- [x] [GATE] Brain CI 通过
  Test: manual:node -e "console.log('CI gate checked by pipeline')"
