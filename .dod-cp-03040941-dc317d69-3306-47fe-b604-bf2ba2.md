# DoD - 修复 quarantined 任务过滤

## 验收标准

### 功能验收
- [x] planner.js 的 generateNextTask() SQL 查询中已加入 `!= 'quarantined'` 条件
      Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/planner.js','utf8'); if(!c.includes(\"!= 'quarantined'\")) process.exit(1)"
- [x] 隔离任务不会被 planNextTask() 选中
      Test: manual:psql $DATABASE_URL -c "INSERT INTO tasks (title, status, project_id, goal_id) VALUES ('test-quarantine', 'quarantined', (SELECT id FROM projects LIMIT 1), (SELECT id FROM goals WHERE type='kr' LIMIT 1)) RETURNING id" && npm test -- planner.test.js -t "should not select quarantined tasks"
- [x] tick.js 已默认启用 auto_dispatch（确认无 disabled 注释）
      Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/tick.js','utf8'); if(!c.includes('dispatchNextTask')) process.exit(1)"

### 代码质量
- [x] 修改少于 10 行代码（符合 PRD 承诺）
      Test: manual:node -e "const {execSync}=require('child_process'); const c=execSync('git diff main packages/brain/src/planner.js').toString(); if(c.split('\\n').filter(l=>l.startsWith('+')||l.startsWith('-')).length > 20) process.exit(1)"
- [x] 代码符合现有风格（SQL 查询格式一致）
      Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/planner.js','utf8'); const m=/WHERE.*quarantined/.exec(c); if(!m||m[0].includes('\\t')||m[0].includes('  ')==false) process.exit(1)"

### 测试验收
- [x] Brain 单元测试通过
      Test: manual:npm test -w packages/brain
