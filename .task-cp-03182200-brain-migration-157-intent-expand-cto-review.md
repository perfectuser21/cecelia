---
id: task-cp-03182200-brain-migration-157-intent-expand-cto-review
type: task-card
branch: cp-03182200-brain-migration-157-intent-expand-cto-review
created: 2026-03-18
---

# Task Card: Brain Migration 157 — 新增 intent_expand + cto_review task_type

## 需求（What & Why）
**功能描述**: 新增 migration 157，将 `intent_expand` 和 `cto_review` 加入 tasks.task_type CHECK 约束，并在 actions.js 的 isSystemTask() 中豁免这两种类型。
**背景**: 创建 intent_expand / cto_review 子任务时触发 constraint violation 错误；同时 actions.js 未豁免这两类型导致 goal_id required 报错。
**不做什么**: 不改前端，不改其他业务逻辑，只修改 DB 约束 + 一处 JS 数组 + schema version 同步。

## 成功标准
> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [ARTIFACT] `packages/brain/migrations/157_add_intent_expand_cto_review_task_types.sql` 文件存在
2. [BEHAVIOR] actions.js isSystemTask() 包含 `intent_expand` 和 `cto_review`
3. [BEHAVIOR] selfcheck.js EXPECTED_SCHEMA_VERSION 为 `'157'`
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] migration 157 SQL 文件存在，DROP/ADD CHECK 约束包含全部 task_type 值（含 intent_expand + cto_review）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/migrations/157_add_intent_expand_cto_review_task_types.sql','utf8');if(!c.includes('intent_expand'))process.exit(1);if(!c.includes('cto_review'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] actions.js systemTypes 数组包含 intent_expand 和 cto_review
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/actions.js','utf8');const m=c.match(/systemTypes\s*=\s*\[([^\]]+)\]/);if(!m||!m[1].includes('intent_expand')||!m[1].includes('cto_review'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] selfcheck.js EXPECTED_SCHEMA_VERSION 为 '157'
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/selfcheck.js','utf8');if(!c.includes(\"'157'\"))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] DEFINITION.md schema_version 字段更新为 157
  Test: manual:node -e "const c=require('fs').readFileSync('DEFINITION.md','utf8');if(!c.includes('Schema 版本: 157'))process.exit(1);console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -10"

## 实现方案
**要改的文件**:
1. `packages/brain/migrations/157_add_intent_expand_cto_review_task_types.sql`（新建）
2. `packages/brain/src/actions.js`（systemTypes 数组）
3. `packages/brain/src/selfcheck.js`（EXPECTED_SCHEMA_VERSION）
4. `packages/brain/src/__tests__/desire-system.test.js`（'156' → '157'）
5. `packages/brain/src/__tests__/selfcheck.test.js`（'156' → '157'）
6. `packages/brain/src/__tests__/learnings-vectorize.test.js`（'156' → '157'）
7. `DEFINITION.md`（schema_version: 156 → 157）
**Scope 锚定**: 仅修改上述 7 处，不涉及其他文件
