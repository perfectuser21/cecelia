---
id: dod-cp-03021752-brain-config-db
version: 1.0.0
created: 2026-03-02
updated: 2026-03-02
changelog:
  - 1.0.0: 初始版本
---

# DoD：大脑架构 DB 配置化 + React Flow 可视化编辑

## 验收清单

- [x] **migration 100：brain_nodes + brain_connections 表**
  Test: manual:bash -c "psql cecelia -c \"SELECT count(*) FROM brain_nodes\" 2>&1 | grep -E '^[ ]*[0-9]+'"
  - brain_nodes(id, block_id, label, nature, pos_x, pos_y, created_at)
  - brain_connections(id, from_node, to_node, path_type, is_broken, created_at)
  - seed 数据：15 个节点 + 21 条连接写入 DB

- [x] **GET /api/brain/architecture 端点**
  Test: manual:bash -c "curl -s http://localhost:5221/api/brain/architecture | python3 -c \"import json,sys; d=json.load(sys.stdin); print('nodes:', len(d['nodes']), 'connections:', len(d['connections']))\""
  - 返回 { nodes: [...], connections: [...] }
  - 每个 node 含 id, block_id, label, nature, pos_x, pos_y
  - 每个 connection 含 id, from_node, to_node, path_type, is_broken

- [x] **PATCH /api/brain/architecture/nodes/:id 端点**
  Test: manual:bash -c "curl -s -X PATCH http://localhost:5221/api/brain/architecture/nodes/tick -H 'Content-Type: application/json' -d '{\"pos_x\": 60, \"pos_y\": 120}' | python3 -c \"import json,sys; d=json.load(sys.stdin); assert d['ok'] == True\""
  - 更新节点 pos_x/pos_y
  - 返回 { ok: true }

- [x] **SuperBrain Level 2 使用 React Flow 渲染**
  Test: manual:bash -c "cd apps/dashboard && npm run build 2>&1 | tail -5"
  - @xyflow/react 已安装
  - Level 2 视图用 React Flow 替代自绘 SVG
  - 节点数据从 /api/brain/architecture 获取（不再 hardcode）
  - 拖拽节点后自动调用 PATCH API 保存位置

- [x] **断路连接显示**
  Test: manual:bash -c "cd apps/dashboard && npm run build 2>&1 | tail -5"
  - is_broken=true 的连接显示为红色虚线
  - 从 DB 读取（不再 hardcode BROKEN_CONNECTIONS_SET）

- [x] **所有现有测试通过**
  Test: manual:npm run test --workspace packages/brain -- --run
