name: Workspace CI

on:
  push:
    branches: [main]
    paths: ['apps/**']
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: workspace-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'push' }}

jobs:
  # Detect which paths changed to skip heavy jobs on non-workspace PRs
  changes:
    name: Detect Changes
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions:
      contents: read
      pull-requests: read
    outputs:
      workspace: ${{ steps.filter.outputs.workspace }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Detect workspace changes
        id: filter
        run: |
          if [ "${{ github.event_name }}" = "push" ]; then
            echo "workspace=true" >> $GITHUB_OUTPUT
            exit 0
          fi
          BASE_REF="${{ github.base_ref }}"
          if git diff --name-only "origin/${BASE_REF}...HEAD" | grep -q "^apps/"; then
            echo "workspace=true" >> $GITHUB_OUTPUT
          else
            echo "workspace=false" >> $GITHUB_OUTPUT
          fi

  build:
    needs: changes
    if: needs.changes.outputs.workspace == 'true'
    name: Dashboard Build
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install API dependencies
        working-directory: apps/api
        run: npm ci

      - name: Install Dashboard dependencies
        working-directory: apps/dashboard
        run: npm ci

      - name: Build Dashboard
        working-directory: apps/dashboard
        run: npm run build

  api-check:
    needs: changes
    if: needs.changes.outputs.workspace == 'true'
    name: API Typecheck
    runs-on: ubuntu-latest
    timeout-minutes: 10
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install API dependencies
        working-directory: apps/api
        run: npm ci

      - name: API Typecheck
        working-directory: apps/api
        run: npm run typecheck

  dashboard-test:
    needs: changes
    if: needs.changes.outputs.workspace == 'true'
    name: Dashboard Tests
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: read
    steps:
      - uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install monorepo dependencies
        run: npm ci

      - name: Dashboard Tests
        working-directory: apps/dashboard
        run: npm run test:run

  ci-passed:
    needs: [changes, build, api-check, dashboard-test]
    if: always()
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Workspace CI Gate
        run: |
          if [ "${{ needs.changes.result }}" = "failure" ]; then
            echo "FAIL: Detect Changes job failed - cannot determine what changed"
            exit 1
          fi
          if [ "${{ needs.changes.outputs.workspace }}" != "true" ]; then
            echo "No workspace changes detected, skipping checks"
            exit 0
          fi
          if [ "${{ needs.build.result }}" != "success" ]; then
            echo "Dashboard Build failed"
            exit 1
          fi
          if [ "${{ needs.api-check.result }}" != "success" ]; then
            echo "API Typecheck failed"
            exit 1
          fi
          if [ "${{ needs.dashboard-test.result }}" != "success" ]; then
            echo "Dashboard Tests failed"
            exit 1
          fi
          echo "All Workspace CI checks passed"
