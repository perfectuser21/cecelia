name: Archive Old Learnings

# 每月 1 号自动把 30 天前的 docs/learnings/*.md 归档成 tar.gz，
# 按 YYYY-MM 分桶（以 git log 首次入库时间为准，不用文件 mtime）。
# 可手动触发（workflow_dispatch）跑一次性大清理。
# v1.1 (2026-04-20): 从 bot 直推 main 改成开 PR + harness label 自动合并。

on:
  schedule:
    - cron: '0 4 1 * *'   # 每月 1 号 04:00 UTC
  workflow_dispatch:

jobs:
  archive:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0   # 需要全 git log 拿每个文件首次入库时间

      - name: Archive learnings older than 30 days
        id: archive
        run: |
          cd docs/learnings
          mkdir -p archive

          # cutoff = 30 天前的 unix 时间戳
          CUTOFF=$(date -u -d '30 days ago' +%s 2>/dev/null || date -u -v-30d +%s)
          echo "cutoff_ts=$CUTOFF ($(date -u -d @$CUTOFF 2>/dev/null || date -u -r $CUTOFF))"

          TMP_LIST_DIR=$(mktemp -d)
          TOTAL_OLD=0

          # 遍历顶层 *.md（不递归到 archive/ 子目录）
          for f in *.md; do
            [ -f "$f" ] || continue

            # 首次入库 commit 的 author time（unix ts）— 稳定不随 checkout 变
            FIRST_TS=$(git log --follow --diff-filter=A --format=%at -1 -- "$f" 2>/dev/null || echo "")
            if [ -z "$FIRST_TS" ]; then
              echo "  ⚠️  $f 无 git history，跳过"
              continue
            fi

            if [ "$FIRST_TS" -ge "$CUTOFF" ]; then
              continue   # 30 天内，不归档
            fi

            # 分桶 key = YYYY-MM
            BUCKET=$(date -u -d "@$FIRST_TS" +%Y-%m 2>/dev/null || date -u -r "$FIRST_TS" +%Y-%m)
            echo "$f" >> "$TMP_LIST_DIR/$BUCKET.list"
            TOTAL_OLD=$((TOTAL_OLD + 1))
          done

          echo "📊 30 天前 learning 总数：$TOTAL_OLD"

          if [ "$TOTAL_OLD" -eq 0 ]; then
            echo "✅ 无需归档"
            echo "changed=false" >> "$GITHUB_OUTPUT"
            exit 0
          fi

          # 每个桶 tar.gz 一次，删原文件
          BUCKETS_DONE=0
          for list in "$TMP_LIST_DIR"/*.list; do
            [ -f "$list" ] || continue
            BUCKET=$(basename "$list" .list)
            COUNT=$(wc -l < "$list" | tr -d ' ')
            TAR="archive/${BUCKET}.tar.gz"

            echo "📦 归档 $COUNT 个文件 → $TAR"
            # -T 从文件列表读；每行一个文件名（相对 cwd=docs/learnings）
            tar -czf "$TAR" -T "$list"

            # 删原文件
            xargs -a "$list" git rm -q
            BUCKETS_DONE=$((BUCKETS_DONE + 1))
          done

          cd ../..
          echo "changed=true" >> "$GITHUB_OUTPUT"
          echo "total_archived=$TOTAL_OLD" >> "$GITHUB_OUTPUT"
          echo "buckets=$BUCKETS_DONE" >> "$GITHUB_OUTPUT"

      # 走 PR 流程：main 受保护（必须 ci-passed 绿），不能 bot 直推。
      # 创 cp-archive-YYYYMMDD-HHMM 分支，开 PR 打 harness 标签（跳过 pr-size-check），
      # 让 ci.yml 里的 auto-merge job 在 ci-passed 绿时自动 squash merge。
      - name: 开 PR（harness 标签触发自动合并）
        if: steps.archive.outputs.changed == 'true'
        env:
          # 必须 PAT：GITHUB_TOKEN 的 gh pr create 报 "Resource not accessible by
          # integration"（0701 月度 run 实锤），且 GITHUB_TOKEN 建的 PR 不触发 CI →
          # required checks 永远 pending。孤儿分支 cp-archive-20260701-0534-learnings 即此因。
          GH_TOKEN: ${{ secrets.GH_PAT_BOT }}
          TOTAL: ${{ steps.archive.outputs.total_archived }}
          BUCKETS: ${{ steps.archive.outputs.buckets }}
        run: |
          if [ -z "${GH_TOKEN:-}" ]; then
            echo "::error::GH_PAT_BOT secret 未配置（开归档 PR 必需 PAT）"
            exit 1
          fi
          # 分支名必须匹配 branch-naming lint 的 ^cp-[0-9]{8,10}-（0706 首次真跑到此路径才暴露：
          # 旧格式 cp-archive-YYYYMMDD-HHMM 被 ci-passed 里的 branch-naming 拒 → PR 永远合不进）
          BRANCH="cp-$(TZ=Asia/Shanghai date +%m%d%H%M)-archive-learnings"
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git checkout -b "$BRANCH"
          git add docs/learnings/archive/
          git commit -m "chore(learnings): 月度归档（${TOTAL} 文件 / ${BUCKETS} 月桶）[bot]"
          git push origin "$BRANCH"

          BODY=$(printf "自动触发：archive-learnings workflow。归档 ${TOTAL} 个 30 天前的 learning 到 ${BUCKETS} 个 YYYY-MM.tar.gz。\n\nharness 标签用于跳过 pr-size-check 硬门禁（1500 行），并触发 auto-merge job。")
          PR_URL=$(gh pr create \
            --title "chore(learnings): 月度归档 ${TOTAL} 文件" \
            --body "$BODY" \
            --label harness \
            --head "$BRANCH" \
            --base main)
          echo "✅ PR 已开: $PR_URL"
