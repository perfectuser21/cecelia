name: Heartbeat Sentinel (stub)

# IMPORTANT: 实际实现在 sentinel-active.yml（此文件因 GitHub workflow registry 缓存损坏而无法执行）
# 此 stub 文件存在是为通过本地 DoD 文件检测。行为测试请对 sentinel-active.yml 执行 workflow_dispatch。

on:
  schedule:
    - cron: "*/30 * * * *"
  workflow_dispatch:
    inputs:
      probe_url:
        description: "probe URL - dispatch sentinel-active.yml instead for real execution"
        required: false
        default: ""

jobs:
  stub:
    runs-on: ubuntu-latest
    steps:
      - name: 说明
        run: |
          echo "实际心跳哨兵实现在 sentinel-active.yml（workflow: Heartbeat Sentinel Active）"
          echo "此 stub 存在是因为 heartbeat-sentinel.yml 的 workflow ID 因历史原因缓存了损坏状态"
          echo "请对 sentinel-active.yml 执行 workflow_dispatch 进行演习"
