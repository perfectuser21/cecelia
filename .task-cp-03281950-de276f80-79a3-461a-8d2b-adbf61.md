---
id: task-cp-03281950-current-state-integration-test
type: task-card
branch: cp-03281950-de276f80-79a3-461a-8d2b-adbf61
created: 2026-03-29
---

# Task Card: CURRENT_STATE 全链路集成测试

## 需求（What & Why）
**功能描述**: 为 `write-current-state.sh` 编写集成测试，验证 /dev Stage4 触发链路：cleanup.sh → write-current-state.sh → CURRENT_STATE.md 内容格式正确。

**背景**: PR #1659/#1661 引入了 write-current-state.sh，但目前只有 `cleanup-write-state.test.ts` 测试静态契约（文件存在/引用存在）。缺少端到端运行验证，无法保证脚本实际执行后生成的 CURRENT_STATE.md 格式正确（含所有必需章节）。

**不做什么**: 不验证 Brain/DB 的真实数据内容，不测试 sh 脚本内部实现细节。

## 成功标准

- 新增 `packages/engine/tests/integration/current-state-format.test.ts`
- 测试运行 `write-current-state.sh`，验证生成的 CURRENT_STATE.md 包含 6 个必需章节（系统健康/Capability Probe/进行中任务/最近 PR/P0 Issues/最近 CI 状态）及 YAML frontmatter
- CI L3 (engine-l3) 绿灯通过

## 验收条件（DoD）

- [x] [ARTIFACT] `packages/engine/tests/integration/current-state-format.test.ts` 存在
  Test: manual:node -e "require('fs').accessSync('packages/engine/tests/integration/current-state-format.test.ts')"

- [x] [BEHAVIOR] 运行 write-current-state.sh 后 CURRENT_STATE.md 包含 `## 系统健康` 章节
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/tests/integration/current-state-format.test.ts','utf8');if(!c.includes('系统健康')||!c.includes('spawnSync'))process.exit(1)"

- [x] [BEHAVIOR] 生成的 CURRENT_STATE.md 包含 YAML frontmatter（generated + source 字段）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/tests/integration/current-state-format.test.ts','utf8');if(!c.includes('YAML frontmatter')||!c.includes('source: write-current-state.sh'))process.exit(1)"

- [x] [BEHAVIOR] CURRENT_STATE.md 包含全部 6 个必需章节（进行中任务/最近 PR/P0 Issues/最近 CI 状态）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/tests/integration/current-state-format.test.ts','utf8');if(!c.includes('进行中任务')||!c.includes('最近 PR')||!c.includes('P0 Issues')||!c.includes('最近 CI 状态'))process.exit(1)"

- [x] [GATE] CI L3 engine-l3 全部通过
  Test: manual:node -e "require('fs').accessSync('packages/engine/tests/integration/current-state-format.test.ts')"

## 实现方案

**要改的文件**:
- 新增: `packages/engine/tests/integration/current-state-format.test.ts`

**Scope 锚定**:
- 测试文件使用 vitest，调用 `execSync` 运行 `write-current-state.sh`
- 使用 `BRAIN_API_URL=http://127.0.0.1:1` 确保 CI 中不依赖真实 Brain（脚本优雅降级）
- 检查 CURRENT_STATE.md 的存在性 + 所有必需章节
- CI 兼容：Brain/DB/gh 不可用时使用 fallback 文本，测试仍通过
