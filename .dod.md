# DoD: harness 独立 release 阶段（promote-dashboard.sh 拆 release → deploy）

- [x] [BEHAVIOR] promote-dashboard.sh 三模式：--release-only 冻结产物进 .dist-releases/<vX>+打tag+写manifest（不动 live/current）；--deploy <vX> 从库换入 live+写current；无参=两步
  Test: manual:node -e "require('fs').accessSync('packages/engine/tests/integration/release-deploy-stage.test.sh')"
- [x] [BEHAVIOR] 回档网不破坏：promote v1→v2→rollback 全生命周期回归仍全绿（deploy 与 rollback 都从 .dist-releases/<tag> cp，对称）
  Test: manual:node -e "require('fs').accessSync('packages/engine/tests/integration/deploy-rollback.test.sh')"
- [x] [ARTIFACT] promote-dashboard.sh 含 --release-only / --deploy 模式 + do_release/do_deploy 函数
  Test: manual:node -e "const c=require('fs').readFileSync('scripts/promote-dashboard.sh','utf8'); if(!c.includes('--release-only')||!c.includes('do_release()')||!c.includes('do_deploy()'))process.exit(1)"
