# DoD: Contract Gate 放行 inline curl|grep-q（修 dashboard 合同被误判弱断言导致 GAN 空转）

- [x] [BEHAVIOR] curl-no-jq 放行 inline `curl ... | grep -q '<字面量>'`（验 HTML/文本响应的合法强 oracle），但裸 curl 无断言仍命中（不放水）
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/lib/__tests__/contract-gate-inline-grep-oracle.test.js')"
- [x] [ARTIFACT] contract-gate.js 导出 hasInlineGrepAssert 且 curl-no-jq detect 调用它
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/lib/contract-gate.js','utf8'); if(!c.includes('export function hasInlineGrepAssert')||!c.includes('!hasInlineGrepAssert(line)'))process.exit(1)"
