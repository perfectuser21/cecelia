# DoD: Pipeline Output Page - Brain API 增强

## 功能验收条件

- [x] Brain API 返回 article_text（读取 content-output/[keyword]/article/article.md）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf-8');if(!c.includes('article_text'))process.exit(1);if(!c.includes('article.md'))process.exit(1);console.log('PASS')"

- [x] Brain API 返回 cards_text（读取 content-output/[keyword]/cards/copy.md）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf-8');if(!c.includes('cards_text'))process.exit(1);if(!c.includes('copy.md'))process.exit(1);console.log('PASS')"

- [x] Brain API 返回 image_urls（扫描 ~/claude-output/images/ 构建 9998 端口 URL）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf-8');if(!c.includes('image_urls'))process.exit(1);if(!c.includes('9998'))process.exit(1);if(!c.includes('existsSync'))process.exit(1);console.log('PASS')"

- [x] [BEHAVIOR] output 端点响应结构包含 article_text 和 image_urls 字段（静态代码验证）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf-8');if(!c.includes('article_text'))process.exit(1);if(!c.includes('image_urls'))process.exit(1);if(!c.includes('cards_text'))process.exit(1);console.log('PASS')"

## 非功能

- [x] 使用静态 import 语法引入 existsSync
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf-8');if(!c.includes('existsSync, readFileSync'))process.exit(1);console.log('PASS')"
