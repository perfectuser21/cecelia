# DoD: C1 — tasks 加 claim 语义，防止并行 dispatch 重复

## 完成定义

- [x] [ARTIFACT] migration 234 存在且 schema 加了 `claimed_by` + `claimed_at` 两列
  Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/234_add_tasks_claim_columns.sql','utf8');if(!c.includes('claimed_by TEXT')||!c.includes('claimed_at TIMESTAMPTZ'))process.exit(1);console.log('OK')"`

- [x] [ARTIFACT] `packages/brain/src/tick.js` dispatch 流程加 claim 步骤（UPDATE ... WHERE claimed_by IS NULL）
  Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('C1 Atomic claim')||!c.includes('already_claimed'))process.exit(1);console.log('OK')"`

- [x] [ARTIFACT] `packages/brain/src/routes/task-tasks.js` 新增 POST /:id/claim 路由
  Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/task-tasks.js','utf8');if(!c.includes(\"/:id/claim\")||!c.includes('Task already claimed'))process.exit(1);console.log('OK')"`

- [x] [BEHAVIOR] 同时 claim 同一 task 两次 → 第二次返回 409（单测覆盖）
  Test: tests/task-tasks-claim.test.js

- [x] [BEHAVIOR] dispatcher tick 调 claim 失败（已被 claim）→ 跳过此任务不重复 in_progress；失败/revert 路径释放 claim
  Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('already_claimed')||!c.includes('claimed_by = NULL'))process.exit(1);console.log('OK')"`

- [x] [ARTIFACT] `packages/brain/src/selfcheck.js` 的 EXPECTED_SCHEMA_VERSION 升到 234
  Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/src/selfcheck.js','utf8');if(!c.includes(\"EXPECTED_SCHEMA_VERSION = '234'\"))process.exit(1);console.log('OK')"`
