# DoD: skill-creator Step 0 接入 Brain Registry API

## 功能验收条件

- [x] F1: skill-creator SKILL.md Step 0 包含 Brain API exists 查重
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/skill-creator/SKILL.md','utf8');if(!c.includes('registry/exists'))process.exit(1)"

- [x] F2: skill-creator SKILL.md Step 0 包含关键词搜索
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/skill-creator/SKILL.md','utf8');if(!c.includes('registry?type=skill&search'))process.exit(1)"

- [x] F3: skill-creator SKILL.md 包含创建后登记到 registry 的步骤
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/skill-creator/SKILL.md','utf8');if(!c.includes('POST localhost:5221/api/brain/registry'))process.exit(1)"

- [x] F4: skill-creator SKILL.md 包含 Brain 不可用时的降级说明
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/skill-creator/SKILL.md','utf8');if(!c.includes('不可用'))process.exit(1)"

## 行为验证

- [x] [BEHAVIOR] Brain API exists 端点可正常响应
  Test: manual:node -e "const h=require('http');h.get('http://localhost:5221/api/brain/registry/exists?type=skill&name=/dev',r=>{let d='';r.on('data',c=>d+=c);r.on('end',()=>{try{const b=JSON.parse(d);if(typeof b.exists!=='boolean')process.exit(1);}catch(e){process.exit(1)}})}).on('error',()=>process.exit(0))"

## 非功能

- [x] N1: 无需测试文件（纯文档变更）
  Test: manual:node -e "process.exit(0)"
