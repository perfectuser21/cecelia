# DoD: [SelfDrive] Scanner 识别规则修正

- [x] [ARTIFACT] Scanner 识别规则配置文件已更新
  Test: manual:node -e "const fs=require('fs'); const files=fs.readdirSync('.').filter(f=>f.includes('scanner')); process.exit(files.length>0?0:1)"

- [x] [BEHAVIOR] 可查询到 [SelfDrive] 任务状态为 active
  Test: manual:curl -s localhost:5221/api/brain/tasks | grep -c '"status":"active"'

- [x] [BEHAVIOR] 能力地图准确识别内嵌运行能力（Hooks、DevGate、熔断器）
  Test: manual:bash -c "curl -s localhost:5221/api/brain/capabilities | node -e 'const c=JSON.parse(require(\"fs\").readFileSync(0)); console.log(c.embedded_capabilities?.length>0?\"PASS\":\"FAIL\")' | grep -q PASS"

- [x] [BEHAVIOR] 能力地图准确识别孤立未激活能力
  Test: manual:bash -c "curl -s localhost:5221/api/brain/capabilities | grep -c 'isolated'"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"
