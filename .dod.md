# DoD: 棒1 回执线——execution-callback 回执保 stage/metrics + internal token 鉴权（链 bf5088a3）

- [x] [BEHAVIOR] 终态回执 `result.{stage,stage_status,metrics,evidence,probes}` 经 finishRun 合进 task_runs.result；evidence/probes 只留引用形态不落大 blob；artifacts/pr_url 原样；非终态只 startRun
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/callback-stage-receipt-auth.test.js')"
- [x] [BEHAVIOR] CECELIA_INTERNAL_TOKEN 配置后 POST /api/brain/execution-callback 缺头/错 token → 401 UNAUTHORIZED；带 Bearer → 200 进入处理
  Test: manual:bash packages/brain/scripts/smoke/callback-stage-receipt-smoke.sh
- [x] [ARTIFACT] 内部调用方带 Bearer / 透传 token：cecelia-run.sh、flush-callback-queue.sh、executor.js（codex fetch×2 + 本地 codex curl + docker env）、cecelia-bridge.js、verify-billing-pause-e2e.js
  Test: manual:node -e "const fs=require('fs');for(const f of ['packages/brain/scripts/cecelia-run.sh','packages/brain/scripts/flush-callback-queue.sh','packages/brain/src/executor.js','packages/brain/scripts/cecelia-bridge.js','packages/brain/scripts/verify-billing-pause-e2e.js']){if(!fs.readFileSync(f,'utf8').includes('CECELIA_INTERNAL_TOKEN'))process.exit(1)}"
