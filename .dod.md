## DoD - fix: L2 concurrency group 去掉 run_number

### 验收标准

- [ ] concurrency group 不含 run_number：`! grep -q 'run_number' .github/workflows/ci-l2-consistency.yml`
- [ ] cancel-in-progress: true 保留：`grep -q 'cancel-in-progress: true' .github/workflows/ci-l2-consistency.yml`
- [ ] group 格式正确为 `l2-${{ github.ref }}`：`grep -q 'group: l2-' .github/workflows/ci-l2-consistency.yml`
