# Task Card: Janitor 孤儿进程精准识别修复

## 成功标准

1. `packages/workflows/skills/janitor/janitor.sh` 存在且被 git 管理
2. `~/bin/janitor.sh` 是软链接指向 repo 文件
3. frequent 模式精准识别孤儿：祖先 zsh PPID=1 + 运行 > 10 分钟 → kill
4. 合法进程绝不误杀：祖先链有活着的 claude 进程 → 不动
5. crontab 频率为 15 分钟

## DoD

### 1. janitor.sh 纳入 git
- [x] `packages/workflows/skills/janitor/janitor.sh` 存在且可执行
  Test: manual: node -e "require('fs').accessSync('packages/workflows/skills/janitor/janitor.sh',require('fs').constants.X_OK)"
- [x] `~/bin/janitor.sh` 是软链接指向 repo 文件（SKILL.md 有记录）
  Test: manual: node -e "const c=require('fs').readFileSync('packages/workflows/skills/janitor/SKILL.md','utf8');if(!c.includes('bin/janitor.sh ->'))process.exit(1)"

### 2. 孤儿检测逻辑正确
- [x] 新增 `find_shell_ancestor()` 函数：向上遍历进程树找到祖先 zsh/bash
  Test: manual: node -e "const c=require('fs').readFileSync('packages/workflows/skills/janitor/janitor.sh','utf8');if(!c.includes('find_shell_ancestor'))process.exit(1)"
- [x] 新增 `has_live_claude_ancestor()` 函数：祖先链中有 claude 进程则返回真
  Test: manual: node -e "const c=require('fs').readFileSync('packages/workflows/skills/janitor/janitor.sh','utf8');if(!c.includes('has_live_claude_ancestor'))process.exit(1)"
- [x] vitest 扫描循环：必须同时满足孤儿且运行超 600s 才 kill
  Test: manual: node -e "const c=require('fs').readFileSync('packages/workflows/skills/janitor/janitor.sh','utf8');if(!c.includes('is_orphan')&&!c.includes('PPID')||!c.includes('THRESHOLD_SEC'))process.exit(1)"

### 3. 阈值改为 600s
- [x] `THRESHOLD_SEC=600`（原 7200）
  Test: manual: node -e "const c=require('fs').readFileSync('packages/workflows/skills/janitor/janitor.sh','utf8');if(!c.includes('THRESHOLD_SEC=600'))process.exit(1)"

### 4. crontab 频率改为 15 分钟
- [x] crontab 频率为 15 分钟（SKILL.md 有记录）
  Test: manual: node -e "const c=require('fs').readFileSync('packages/workflows/skills/janitor/SKILL.md','utf8');if(!c.includes('*/15'))process.exit(1)"

### 5. [BEHAVIOR] 孤儿检测端到端验证
- [x] [BEHAVIOR] 运行 test suite 验证 janitor 孤儿检测逻辑正确（不误杀、不漏杀）
  Test: manual: bash packages/workflows/skills/janitor/__tests__/janitor_orphan.test.sh
