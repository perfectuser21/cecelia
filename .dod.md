# DoD: 修复 MiniMax Coding Plan API 调用方式

## 验收清单

### P0: API 调用修复
- [ ] D1: orchestrator-chat.js — API URL 从 `/v1/text/chatcompletion_v2` 改为 `/v1/chat/completions`
- [ ] D2: orchestrator-chat.js — 模型名从 `MiniMax-Text-01` 改为 `MiniMax-M2.5`
- [ ] D3: orchestrator-chat.js — 新增 `stripThinking(content)` 函数去掉 `<think>...</think>` 块

### P1: 容器凭据挂载
- [ ] D4: docker-compose.yml — 添加 `/home/xx/.credentials:/home/cecelia/.credentials:ro` 卷挂载

### 版本
- [ ] D5: 版本号 v1.63.0 → v1.63.1（fix: patch bump）

## Test 映射

| DoD | Test 文件 | Test 名称 |
|-----|-----------|-----------|
| D1 | orchestrator-chat.test.js | callMiniMax sends request to correct URL |
| D2 | orchestrator-chat.test.js | callMiniMax uses MiniMax-M2.5 model |
| D3 | orchestrator-chat.test.js | strips thinking block from reply |
| D3 | orchestrator-chat.test.js | handles reply without thinking block |
| D3 | orchestrator-chat.test.js | handles empty thinking block |
