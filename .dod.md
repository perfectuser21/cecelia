# DoD - 审计修复：stale refs

QA: 无需独立 QA（修复配置默认值 + 文档版本号 + 路径）

## 验收清单

| # | DoD 条目 | Test 映射 | 验证方式 |
|---|---------|-----------|----------|
| 1 | pool.py DB defaults 与 db-config.js 一致 | `cd /home/xx/perfect21/cecelia/core && PYTHONPATH=. pytest tests/` | Python 测试通过 |
| 2 | DEFINITION.md 正文无 1.9.5 版本号 | manual:grep -c '1.9.5' DEFINITION.md 返回 0 | 文档修复 |
| 3 | regression-contract.yaml 使用正确路径 | manual:grep 'perfect21' docs/regression-contract.yaml | 路径修复 |
| 4 | Brain 测试不回归 | `cd brain && npx vitest run` | >= 639 pass |
| 5 | DevGate 全绿 | `node scripts/facts-check.mjs && bash scripts/check-version-sync.sh` | 退出码 0 |

## 测试验收

- [ ] `cd brain && npx vitest run` 无回归 (>= 639 pass)
- [ ] `cd /home/xx/perfect21/cecelia/core && PYTHONPATH=. pytest tests/` 通过
- [ ] `node scripts/facts-check.mjs` 全绿
- [ ] `bash scripts/check-version-sync.sh` 全绿
