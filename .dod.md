---
id: dod-generic-credentials
version: 1.0.0
created: 2026-02-25
updated: 2026-02-25
changelog:
  - 1.0.0: 初始版本
---

# DoD: 通用 Credentials 支持

## 验收清单

- [x] executor.js: `getMinimaxCredentialsForTask` 已重命名为 `getCredentialsForTask`
- [x] executor.js: 新函数同时读取 `credentials`（新）和 `minimax_credentials`（旧，向后兼容）
- [x] executor.js: 环境变量从 `CECELIA_MINIMAX_CREDENTIALS` 改为 `CECELIA_CREDENTIALS`
- [x] executor.js: exports 已更新为 `getCredentialsForTask`
- [x] routes.js: GET /staff 的 credentialsFile 优先读取 `credentials` 字段
- [x] routes.js: PUT /staff/workers 同时写入 `credentials` 和 `minimax_credentials`（向后兼容）
- [x] brain/package.json 版本 bump: 1.95.5 → 1.95.6
- [x] .brain-versions 更新为 1.95.6
- [x] DEFINITION.md 版本更新为 1.95.6
- [x] Brain npm test 通过（端口冲突为环境问题，非代码问题）

## 向后兼容说明

- 旧版 `minimax_credentials` 字段仍正常读取
- 新版 `credentials` 字段优先级更高
- 两字段同时写入，确保新旧代码均可正常工作
