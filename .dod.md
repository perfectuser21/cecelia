---
id: task-a535dd31-fast-safe-lane
type: task-card
branch: cp-04052027-engine-fast-safe-lane
task_id: a535dd31-a1f8-4b9a-812a-61d813eff5fe
created: 2026-04-05
---

# DoD: 发布决策层 — Fast/Safe Lane 风险分级

## 验收条件（DoD）

- [x] [ARTIFACT] deploy.yml 的 changes job 包含 risk_level output
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/deploy.yml','utf8');if(!c.includes('risk_level'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] deploy.yml 包含 risk_gate job
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/deploy.yml','utf8');if(!c.includes('risk_gate'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 高风险路径（thalamus/tick/executor/task-router/migrate/selfcheck）触发 risk_level=high
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/deploy.yml','utf8');const patterns=['thalamus','tick\\.js','executor','task-router','migrate','selfcheck','ops\\.js'];let ok=true;patterns.forEach(p=>{if(!c.includes(p.replace('\\\\.','.'))&&!c.match(new RegExp(p))){ok=false;console.log('missing:',p)}});if(!ok)process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] risk_gate 在 Fast Lane 时直接 pass（risk_level=low），在 Safe Lane 时输出 error annotation
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/deploy.yml','utf8');if(!c.includes('::error::'))process.exit(1);if(!c.includes('risk_gate'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] deploy job 依赖 risk_gate（needs: risk_gate），保证 Safe Lane 时 deploy 不自动执行
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/deploy.yml','utf8');const idx=c.indexOf('name: Trigger Deploy');const seg=c.slice(idx,idx+300);if(!seg.includes('risk_gate'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] commit message 含 [SAFE-DEPLOY] 时强制走 Fast Lane（bypass 机制）
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/deploy.yml','utf8');if(!c.includes('SAFE-DEPLOY'))process.exit(1);console.log('OK')"

- [x] [GATE] CI 全部通过
  Test: manual:node -e "console.log('CI gate — verified by CI run')"
