## DoD - fix(executor): syncOrphanTasksOnStartup 孤儿改 requeue — 区分可重试 vs 真实失败

### 实现检查

- [ ] [ARTIFACT] `syncOrphanTasksOnStartup` 中 `reason === 'process_disappeared'` 时设置 `status = 'queued'`
- [ ] [ARTIFACT] requeue 时 `error_message = '[requeued after brain restart] reason=process_disappeared'`
- [ ] [ARTIFACT] `reason` 为 `oom_killed` / `oom_likely` / `killed_signal` / `timeout` 时保持 `status = 'failed'`
- [ ] [ARTIFACT] auto-learning 仅在 failed 路径触发，不在 requeue 路径触发

### 行为验证

- [ ] [BEHAVIOR] Brain 重启后 process_disappeared 孤儿任务出现在 queued 状态
- [ ] [BEHAVIOR] oom_killed / killed_signal 孤儿任务保持 failed 状态
- [ ] [BEHAVIOR] 单元测试覆盖 process_disappeared → queued 分流

### 测试

- [ ] [TEST] `grep -c "isRetryable" packages/brain/src/executor.js` 输出 >= 1
- [ ] [TEST] `grep -c "requeued after brain restart" packages/brain/src/executor.js` 输出 >= 1
