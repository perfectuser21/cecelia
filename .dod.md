## DoD - CeceliaPage 前后端数据对齐

### 验收清单

- [ ] **1. status/full 返回 today stats** — 增加 task_stats, tick_stats, token_stats
  - Test: `curl -s localhost:5221/api/brain/status/full | jq '.task_stats.completed_today'` 返回数字

- [ ] **2. WebSocket tick:executed 广播修复** — actionsToday 变量引用修复
  - Test: Brain 日志不再出现 `actionsToday is not defined`

- [ ] **3. briefing 路由前端可达** — 前端 useBriefing 能正确获取数据
  - Test: `curl -s localhost:5221/api/brain/briefing | jq '.greeting'` 返回问候语

- [ ] **4. Brain 版本更新** — patch bump
  - Test: `bash scripts/check-version-sync.sh` 通过

- [ ] **5. npm test 通过** — Brain 测试无新增失败
  - Test: `npx vitest run` 无失败
