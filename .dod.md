# DoD — cp-0426001002-d17c-plugin1

## Artifact

- [x] [ARTIFACT] 4 plugin 文件存在（dept-heartbeat 已存在新增 tick；其他 3 个新建）
      Test: manual:node -e "['dept-heartbeat','kr-progress-sync-plugin','heartbeat-plugin','goal-eval-plugin'].forEach(m=>require('fs').accessSync(\`packages/brain/src/\${m}.js\`))"

## Behavior

- [x] [BEHAVIOR] 4 plugin 文件均 export tick 函数
      Test: manual:node -e "(async()=>{for(const m of ['dept-heartbeat','kr-progress-sync-plugin','heartbeat-plugin','goal-eval-plugin']){const x=await import(\`./packages/brain/src/\${m}.js\`);if(typeof x.tick!=='function')process.exit(1)}})()"

- [x] [BEHAVIOR] tick-runner.js 4 处 scheduled job 改为 plugin 调用（namespace import 全在）
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick-runner.js','utf8');const ms=['deptHeartbeatPlugin','krProgressSyncPlugin','heartbeatPlugin','goalEvalPlugin'];for(const m of ms){if(!c.includes(m))process.exit(1)}"

- [x] [BEHAVIOR] 4 个新 plugin 单测文件存在
      Test: manual:node -e "['dept-heartbeat-plugin','kr-progress-sync-plugin','heartbeat-plugin','goal-eval-plugin'].forEach(m=>require('fs').accessSync(\`packages/brain/src/__tests__/\${m}.test.js\`))"

- [x] [BEHAVIOR] tick-runner.js 自省测试存在（验证 4 个 plugin 全 wire 上）
      Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/tick-runner.test.js')"

- [x] [BEHAVIOR] tick-runner.js 旧 inline 调用全删（triggerDeptHeartbeats / runHeartbeatInspection / evaluateGoalOuterLoop / runAllVerifiers / syncAllKrProgress）
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick-runner.js','utf8');for(const p of ['= await triggerDeptHeartbeats(','= await runHeartbeatInspection(','= await evaluateGoalOuterLoop(','= await runAllVerifiers(','= await syncAllKrProgress(']){if(c.includes(p)){console.error('still has '+p);process.exit(1)}}"
