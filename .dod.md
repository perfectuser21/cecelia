# DoD: 棒3a 判定——run.finished → 比对探针 → 写回执 → cell 翻色（任务 33aa2bc4）

- [x] [BEHAVIOR] finishRun 补终态成功后单点发 run.finished（payload 含 runId/taskId/status/合并 result），已终态/running 不发，emit 抛错不影响返回
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/lib/__tests__/task-run-finish-event.test.js')"
- [x] [BEHAVIOR] event-bus 进程内 on/off：emit 落库后派发订阅者，订阅者抛错隔离、落库失败照样派发
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/event-bus.test.js','utf8'); if(!c.includes('on/off'))process.exit(1)"
- [x] [BEHAVIOR] judge 比对矩阵：>= == <= not_null_all、expect.value/expect.ref→metrics、缺 observed→probe_missing、error→probe_error、op 非法→op_unsupported；PASS→green / FAIL&error→red / FAIL&warn→pending；handleRunFinished 写回执+UPDATE cell_status，无锚/无探针跳过，pool 抛错 fail-open
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/lib/__tests__/business-probe-judge.test.js')"
- [x] [BEHAVIOR] persistBusinessProbeReceipt 按占位约定写 journey_assertion_receipts（business_probe_runner / probe:<key> / ["probe",key] / sha,machine NULL / exit 0|1），非法输入抛错不写库
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/impact-contract/__tests__/business-probe-receipt.test.js')"
- [x] [BEHAVIOR] 两处 resolver 对 business_probe_runner 回执只看 verdict（PASS→green / FAIL→red）不比 sha；brain_assertion_runner 行为不变；合并闸 SQL 仍只认 brain_assertion_runner
  Test: manual:node -e "const fs=require('fs'); for (const f of ['packages/brain/src/lib/__tests__/map-state-resolver.test.js','packages/brain/src/map/__tests__/state-resolver.test.js','packages/brain/src/impact-contract/__tests__/harness-gates.test.js']) if(!fs.readFileSync(f,'utf8').includes('business_probe_runner'))process.exit(1)"
- [x] [BEHAVIOR] F1 step4 交付有回执：judge 组装的证据被真 persistBusinessProbeReceipt 原样接住（守卫在边上，不 mock 被改模块）
  Test: manual:node -e "require('fs').accessSync('tests/gp/f1/step4-business-probe-receipt.test.js')"
- [x] [BEHAVIOR] 真 Postgres 端到端：finishRun 终态 → 真 event-bus → 回执行落库（business_probe_runner 字段约定）→ cell_status green/pending；重复判定幂等；已终态不重判
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/integration/business-probe-judge.pg.integration.test.js')"
- [x] [ARTIFACT] 迁移 475 放宽 executor_kind 两值 + verdict_chk 按 executor_kind 分支 + 回滚脚本
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/migration-475-business-probe-receipts.test.js')"
- [x] [ARTIFACT] smoke 脚本 business-probe-judge-smoke.sh 登记 allowlist 且本地 PASS
  Test: manual:bash packages/brain/scripts/smoke/business-probe-judge-smoke.sh
