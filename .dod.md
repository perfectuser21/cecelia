# DoD: 修复 memory/search — 纳入 capabilities 向量搜索

## 验收清单

- [ ] `similarity.js` `vectorSearch()` 查询 capabilities 表
- [ ] capability 结果格式：`{ level: 'capability', id, title, description, score, current_stage }`
- [ ] `POST /api/brain/memory/search` 查询"熔断"返回 capability 结果（score > 0.5）
- [ ] 现有 task 搜索逻辑不变
- [ ] 测试：`memory-capabilities-search.test.js` 覆盖 capability 搜索路径

## 测试映射

| DoD 项 | 测试 |
|--------|------|
| vectorSearch 查 capabilities | memory-capabilities-search.test.js > 返回 capability 结果 |
| 结果 score > 0.5 | memory-capabilities-search.test.js > score 阈值 |
| 无 embedding 跳过 | memory-capabilities-search.test.js > 跳过无 embedding |
| task 搜索不受影响 | memory-capabilities-search.test.js > task 路径不变 |
