# DoD: harness 内部线 staging→promote 接缝对齐 dashboard（A 方案）

- [x] [BEHAVIOR] deployStaging 内部线(line=internal)走 deploy-local.sh --changed=apps/dashboard，返回 stagingPort 5223；非内部线沿用 staging-deploy.sh（5222）
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/staging-e2e-runner-dashboard-seam.test.js')"
- [x] [BEHAVIOR] defaultPromoteExec 跑 promote-dashboard.sh 时注入 CECELIA_SKIP_BRAIN_PROMOTE=1（防 promote 重启执行它自己的 Brain）
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/staging-promote-skip-brain.test.js')"
- [x] [ARTIFACT] handlePromote 的 promote/readProductionInfo 用 process.env.REPO_ROOT||getRepoRoot()（修裸 getRepoRoot 容器内=/ 找不到脚本）
  Test: manual:node -e "const s=require('fs').readFileSync('packages/brain/src/staging-e2e-runner.js','utf8'); if(!s.includes('process.env.REPO_ROOT || getRepoRoot()'))process.exit(1)"
- [x] [ARTIFACT] defaultPromoteExec 源码含 CECELIA_SKIP_BRAIN_PROMOTE
  Test: manual:node -e "const s=require('fs').readFileSync('packages/brain/src/staging-promote.js','utf8'); if(!s.includes('CECELIA_SKIP_BRAIN_PROMOTE'))process.exit(1)"
