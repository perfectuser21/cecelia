## DoD - 前端聊天接入 Brain Intent 管道

### 验收清单

- [ ] **1. chat-action-dispatcher 升级** — 关键词不匹配时回退到 intent/execute 管道
  - Test: Brain 重启正常，`curl -X POST localhost:5221/api/orchestrator/chat -d '{"message":"创建一个任务：优化首页性能"}' -H 'Content-Type: application/json'` 返回包含任务创建确认

- [ ] **2. 纯聊天不触发动作** — 日常对话不创建任何资源
  - Test: `curl -X POST localhost:5221/api/orchestrator/chat -d '{"message":"你好"}' -H 'Content-Type: application/json'` 返回普通回复，无副作用

- [ ] **3. 现有关键词快速通道保留** — "帮我记任务：xxx" 仍走关键词匹配
  - Test: 关键词匹配路径代码未被修改

- [ ] **4. Brain 版本更新** — patch bump
  - Test: `bash scripts/check-version-sync.sh` 通过

- [ ] **5. npm run build 成功** — 整体构建无新增错误
  - Test: `cd apps/dashboard && npm run build` 返回 0
