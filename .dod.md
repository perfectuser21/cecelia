# DoD: Docker Compose 生产默认化

QA: 无需独立 QA（纯文件重命名 + 引用更新）

## 验收清单

| # | DoD 条目 | Test 映射 | 验证方式 |
|---|---------|-----------|----------|
| 1 | `docker-compose.yml` 包含 `read_only: true` | grep 验证 | `grep 'read_only' docker-compose.yml` |
| 2 | `docker-compose.dev.yml` 包含 bind mount `./brain:/app` | grep 验证 | `grep './brain:/app' docker-compose.dev.yml` |
| 3 | 无脚本/文档仍引用 `docker-compose.prod.yml` | grep 验证 | `grep -r 'docker-compose.prod.yml' scripts/ docs/` 返回 0 |
| 4 | 现有生产容器不受影响 | docker ps | `docker ps` 显示 `cecelia-brain:1.8.1` healthy |
| 5 | MEMORY.md 中 prod compose 引用已更新 | 目视检查 | Compose 字段引用 `docker-compose.yml` |

## 测试验收

- [ ] `npx vitest run` 全量通过（不引入回归）
