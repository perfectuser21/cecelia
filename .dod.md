# DoD: Brain Linux->macOS 全面平台适配

## 验收清单

- [x] 1. platform-utils.js 创建，导出 IS_DARWIN, IS_LINUX 和平台无关的工具函数
  Test: manual:node -e "import('./packages/brain/src/platform-utils.js').then(m => { if(!('IS_DARWIN' in m)) process.exit(1) })"
- [x] 2. slot-allocator.js detectUserSessions() macOS 兼容
  Test: manual:node -e "const fs=require('fs'); if(!fs.readFileSync('packages/brain/src/slot-allocator.js','utf8').includes('listProcessesWithElapsed')) process.exit(1)"
- [x] 3. executor.js sampleCpuUsage() macOS 兼容（os.loadavg 代理）
  Test: manual:node -e "const fs=require('fs'); if(!fs.readFileSync('packages/brain/src/executor.js','utf8').includes('platformSampleCpuUsage')) process.exit(1)"
- [x] 4. executor.js checkServerResources() swap 检测 macOS 兼容
  Test: manual:node -e "const fs=require('fs'); if(!fs.readFileSync('packages/brain/src/executor.js','utf8').includes('getSwapUsedPct')) process.exit(1)"
- [x] 5. executor.js pgrep -xc claude macOS 兼容
  Test: manual:node -e "const fs=require('fs'); if(!fs.readFileSync('packages/brain/src/executor.js','utf8').includes('countClaudeProcesses')) process.exit(1)"
- [x] 6. executor.js PHYSICAL_CAPACITY 加 SYSTEM_RESERVED_MB=5000 和 cap MAX_PHYSICAL_CAP=10
  Test: manual:node -e "const fs=require('fs'); if(!fs.readFileSync('packages/brain/src/executor.js','utf8').includes('calculatePhysicalCapacity')) process.exit(1)"
- [x] 7. routes/cluster.js /proc 依赖 macOS 兼容
  Test: manual:node -e "const fs=require('fs'); if(!fs.readFileSync('packages/brain/src/routes/cluster.js','utf8').includes('processExists')) process.exit(1)"
- [x] 8. zombie-sweep.js ps 格式 macOS 兼容
  Test: manual:node -e "const fs=require('fs'); if(!fs.readFileSync('packages/brain/src/zombie-sweep.js','utf8').includes('listProcessesWithPpid')) process.exit(1)"
- [x] 9. routes/vps-monitor.js top/netstat macOS 兼容
  Test: manual:node -e "const fs=require('fs'); if(!fs.readFileSync('packages/brain/src/routes/vps-monitor.js','utf8').includes('getTopCpuUsage')) process.exit(1)"
- [x] 10. executor.js dmesg macOS 降级（直接返回 null）
  Test: manual:node -e "const fs=require('fs'); if(!fs.readFileSync('packages/brain/src/executor.js','utf8').includes('platformGetDmesgInfo')) process.exit(1)"
- [x] 11. platform-utils.test.js 基本测试覆盖（24 tests）
  Test: manual:npx vitest run packages/brain/src/__tests__/platform-utils.test.js --reporter=json 2>/dev/null | node -e "process.stdin.on('data',d=>{const r=JSON.parse(d);console.log(r.numPassedTests+' passed')})"
- [x] 12. 现有测试不 break（vitest run 通过）
  Test: manual:npx vitest run packages/brain/src/__tests__/slot-allocator.test.js --reporter=json 2>/dev/null | node -e "process.stdin.on('data',d=>{const r=JSON.parse(d);console.log(r.numPassedTests+' passed')})"
