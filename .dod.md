# DoD: Pipeline Rerun 幂等检查修复

branch: cp-03311024-pipeline-rerun-fix

## 验收条件

- [ ] [ARTIFACT] orchestrator 幂等检查 SQL 只含 queued/in_progress，不含 completed
  Test: manual:node -e "const fs=require('fs'); const src=fs.readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8'); const start=src.indexOf('async function _startOnePipeline'); const end=src.indexOf('async function',start+1); const fn=src.slice(start,end); if(fn.match(/status IN[^)]*'completed'/))throw new Error('FAIL: 幂等检查仍含 completed'); console.log('PASS');"

- [ ] [ARTIFACT] 修改后 SQL 精确含 status IN ('queued', 'in_progress')
  Test: manual:node -e "const fs=require('fs'); const src=fs.readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8'); if(!src.includes(\"status IN ('queued', 'in_progress')\"))throw new Error('FAIL: SQL 不含预期字符串'); console.log('PASS');"

- [ ] [BEHAVIOR] _startOnePipeline 函数体内不出现 'completed' 状态字符串
  Test: manual:node -e "const fs=require('fs'); const src=fs.readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8'); const start=src.indexOf('async function _startOnePipeline'); const end=src.indexOf('async function',start+1); const fn=src.slice(start,end); if(fn.includes(\"'completed'\"))throw new Error('FAIL: 函数体含 completed'); console.log('PASS');"

- [ ] [GATE] 模块可正常加载，无语法错误
  Test: manual:node -e "const src=require('fs').readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8'); try{new Function(src)}catch(e){if(e instanceof SyntaxError)throw e}; console.log('PASS: no syntax error');"
