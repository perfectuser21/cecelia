## DoD - resource-monitor.js：封装 os.loadavg + memoryUsage，暴露 getResourcePressure()

### 实现检查

- [x] [ARTIFACT] `packages/brain/src/resource-monitor.js` 已创建
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/resource-monitor.js','utf8');if(!c.includes('getResourcePressure'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] `getResourcePressure()` 同步函数已导出
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/resource-monitor.js','utf8');if(!c.includes('export function getResourcePressure'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] 返回结构包含 cpu_load_1m、memory_pct、cpu_throttle、memory_throttle、any_throttle
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/resource-monitor.js','utf8');['cpu_load_1m','memory_pct','cpu_throttle','memory_throttle','any_throttle'].forEach(k=>{if(!c.includes(k))process.exit(1)});console.log('OK')"

- [x] [ARTIFACT] cpu_load_1m > 2.0 时 cpu_throttle=true
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/resource-monitor.js','utf8');if(!c.includes('CPU_THRESHOLD = 2.0'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] memory_pct > 0.85 时 memory_throttle=true
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/resource-monitor.js','utf8');if(!c.includes('MEM_THRESHOLD = 0.85'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] any_throttle = cpu_throttle || memory_throttle
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/resource-monitor.js','utf8');if(!c.includes('any_throttle'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] `resetThresholds(cpu, mem)` 已导出（测试用）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/resource-monitor.js','utf8');if(!c.includes('export function resetThresholds'))process.exit(1);console.log('OK')"

### 行为验证

- [x] [BEHAVIOR] mock os.loadavg 为 [3.0] 时 cpu_throttle=true
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/resource-monitor.test.js','utf8');if(!c.includes('3.0'))process.exit(1);if(!c.includes('cpu_throttle'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] mock heapUsed/heapTotal 为 0.9 时 memory_throttle=true
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/resource-monitor.test.js','utf8');if(!c.includes('heapUsed'))process.exit(1);if(!c.includes('memory_throttle'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 正常值时 any_throttle=false
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/resource-monitor.test.js','utf8');if(!c.includes('any_throttle'))process.exit(1);if(!c.includes('false'))process.exit(1);console.log('OK')"
