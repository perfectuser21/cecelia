---
id: task-cp-03310932-pipeline-rerun
type: task-card
branch: cp-03310932-pipeline-rerun
created: 2026-03-31
---

# Task Card: content-pipeline /run 支持重新生成已完成的 pipeline

## 验收条件（DoD）

- [x] [ARTIFACT] content-pipeline.js /run 端点中不存在同时检查 completed 状态并返回 400 的代码行
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');const bad=c.split('\n').filter(l=>l.includes('completed')&&l.includes('400'));if(bad.length>0){console.error('FAIL:',bad);process.exit(1);}console.log('OK')"

- [x] [BEHAVIOR] completed 状态的 pipeline 重置为 queued 后继续执行，不再拒绝
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes(\"status = 'queued'\"))process.exit(1);console.log('OK: resets to queued')"

- [x] [GATE] /run 端点在 completed 状态时调用 reset SQL 而非直接拒绝
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes('completed_at = NULL'))process.exit(1);if(!c.includes('started_at = NULL'))process.exit(1);console.log('OK: reset query present')"
