# DoD - 修复 tick.js 中 getDispatchRate 未定义错误

## 验收标准

### 功能验收
- [ ] tick.js Line 1213 修复完成：getDispatchRate() → getDispatchRateEnhanced()
      Test: manual:代码diff检查
- [ ] 手动触发 tick 成功执行，不再报错 "getDispatchRate is not defined"
      Test: manual:curl测试
- [ ] 相关测试（alertness 系统测试）通过
      Test: brain/src/__tests__/alertness/index.test.js

### 测试验收
- [ ] Brain 测试套件全部通过（npm test）
      Test: contract:brain-tests
- [ ] CI 通过（Version Check, Facts Consistency, Brain tests）
      Test: contract:ci-passed

## Evidence（手动验证证据）

### 代码修复验证
```bash
# 验证修复
grep -n "getDispatchRateEnhanced" brain/src/tick.js | grep "1213"
```

### Tick 执行验证
```bash
# 手动触发 tick 验证
curl -s -X POST localhost:5221/api/brain/tick | jq .

# 预期结果：{"success": true, ...}
# 不应该返回：{"success": false, "error": "getDispatchRate is not defined"}
```

### 测试覆盖验证
```bash
# 运行 alertness 测试
cd brain && npm test -- alertness
```
