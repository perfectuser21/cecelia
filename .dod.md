## DoD - 公众号图文混排：HTML 内嵌图片自动上传

### 实现检查

- [ ] `uploadInlineImages(html, accessToken, contentDir?)` 函数已实现
- [ ] 解析 HTML 中所有 `<img src="...">` 标签
- [ ] 本地路径识别：非 http/https 开头的 src 视为本地路径
- [ ] 相对路径：相对 `contentDir` 解析为绝对路径
- [ ] 上传使用 `media/uploadimg` 接口，返回微信 CDN URL
- [ ] 替换 HTML 中本地 src 为微信 URL
- [ ] 上传失败时 console.warn，不 throw，继续处理其他图片
- [ ] 主流程在创建草稿前调用 `uploadInlineImages` 处理 content
- [ ] `createDraft` 接受并传递 `thumbMediaId` 参数（修复 40007）
- [ ] 封面使用 `material/add_material` 接口上传，获取永久 media_id
- [ ] 外链图片（http/https）不动

### 向后兼容

- [ ] 无 `<img>` 标签的 HTML 行为不变
- [ ] 无封面时草稿不传 thumbMediaId

### 导出

- [ ] `uploadInlineImages` 加入 `module.exports` 供测试
