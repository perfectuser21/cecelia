---
id: strategy-session-impl
version: 1.0.0
created: 2026-03-07
updated: 2026-03-07
changelog:
  - 1.0.0: 初始版本
---

# DoD: strategy_session task_type 实现

## 背景

`strategy_session` task_type 已注册到 Brain，skill 也已创建，但 execution-callback 缺少解析 KR 并写入 goals 的逻辑。

## DoD 清单

### D1: skillMap 路由 ✅
- [x] `getSkillForTaskType('strategy_session')` 返回 `'/strategy-session'`
- Test: tests/../executor-strategy-session.test.js

### D2/D3: model-registry agent ✅
- [x] `model-registry.js` 包含 `strategy_session` agent
- Test: tests/../executor-strategy-session.test.js
- [x] `allowed_models` 包含 `claude-opus-4-6` 和 `claude-sonnet-4-6`
- Test: tests/../executor-strategy-session.test.js
- [x] `recommended_model` 为 `claude-opus-4-6`
- Test: tests/../executor-strategy-session.test.js
- [x] `layer` 为 `executor`
- Test: tests/../executor-strategy-session.test.js

### D4: execution-callback 解析 KR JSON
- [x] 从 task output 中提取 `krs` 数组
- Test: tests/../executor-strategy-session.test.js
- [x] 支持 ` ```json ... ``` ` 块格式
- Test: tests/../strategy-session-parser.test.js
- [x] 支持纯 JSON 字符串格式
- Test: tests/../strategy-session-parser.test.js

### D5: goals 表写入
- [x] 每个 KR 写入 `goals` 表
- Test: tests/../executor-strategy-session.test.js
- [x] 字段映射：
  - `title` → KR title
  - `domain` → KR domain (product/tech/ops/finance/growth)
  - `owner_role` → KR owner_role (转为小写: cto/cfo/coo/ceo)
  - `priority` → KR priority (P0/P1/P2，无效值默认 P1)
  - `status` → 'pending'
  - `progress` → 0
  - `type` → 'area_okr'
- Test: tests/../executor-strategy-session.test.js
- [x] 关联 `project_id`（从 task payload 中获取）
- Test: tests/../executor-strategy-session.test.js

### D6: task payload 写入
- [x] `meeting_summary` 写入 task payload
- Test: tests/../executor-strategy-session.test.js
- [x] `key_tensions` 写入 task payload
- Test: tests/../executor-strategy-session.test.js

### D7: 错误处理
- [x] JSON 解析失败时不抛出，记录警告日志
- Test: tests/../strategy-session-parser.test.js
- [x] krs 为空数组时不执行写入
- Test: tests/../strategy-session-parser.test.js
- [x] 缺少 domain/owner_role 时使用 null
- Test: tests/../strategy-session-parser.test.js

## 验收标准

1. 创建 `strategy_session` 任务并执行后，goals 表新增对应 KR 记录
2. KR 包含正确的 domain 和 owner_role
3. task payload 包含 meeting_summary
4. JSON 解析异常不导致 callback 失败

## 实现位置

- `packages/brain/src/routes.js` - execution-callback 端点
- `packages/brain/src/strategy-session-parser.js` - 已存在，直接复用
