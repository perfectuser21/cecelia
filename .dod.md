## DoD - feat(executor): MAX_SEATS 重启状态日志 + 并发天花板评估报告

### 实现检查

- [x] [ARTIFACT] server.js 启动时打印 MAX_SEATS 和 INTERACTIVE_RESERVE 配置值
- [x] [ARTIFACT] syncOrphanTasksOnStartup 完成后日志包含 requeued/rebuilt/failed 汇总
- [x] [ARTIFACT] GET /api/brain/capacity 端点存在并返回 max_seats 字段

### 测试验证

- [x] [TEST] capacity 端点测试：`max_seats` 字段为数字类型

### 行为验证

- [x] [BEHAVIOR] Brain 启动日志包含 `[Server] Capacity config: MAX_SEATS=`
- [x] [BEHAVIOR] syncOrphanTasksOnStartup 完成后打印包含 requeued/rebuilt/failed

### DoD 验证命令

```bash
# 验证 server.js 有 MAX_SEATS 启动日志
grep -c 'Capacity config: MAX_SEATS' packages/brain/server.js

# 验证 server.js syncOrphanTasksOnStartup 日志更新
grep -c 'requeued' packages/brain/server.js

# 验证 routes.js 有 /capacity 端点
grep -c "get.*'\/capacity'" packages/brain/src/routes.js

# 验证 routes.js capacity 返回 max_seats
grep -c 'max_seats' packages/brain/src/routes.js

# 验证测试文件存在
ls packages/brain/src/__tests__/capacity-endpoint.test.js
```
