# DoD: 修复 brain-unit CI OOM

## 完成定义

- [x] [ARTIFACT] `packages/brain/vitest.config.js` 中 `maxForks` 值为 1
  Test: `manual:node -e "const c=require('fs').readFileSync('packages/brain/vitest.config.js','utf8');if(!c.includes('maxForks: 1'))process.exit(1);console.log('OK')"`

- [x] [ARTIFACT] `.github/workflows/ci.yml` 中 brain-unit job 不含 `continue-on-error`
  Test: `manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(c.includes('continue-on-error: true'))process.exit(1);console.log('OK')"`

- [x] [ARTIFACT] `.github/workflows/ci.yml` brain-unit job 的 NODE_OPTIONS 为 `--max-old-space-size=4096`
  Test: `manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(!c.includes('max-old-space-size=4096'))process.exit(1);console.log('OK')"`

- [x] [BEHAVIOR] continue-on-error 已彻底移除，CI gate 有效
  Test: `manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(c.includes('continue-on-error'))process.exit(1);console.log('continue-on-error 已移除，CI gate 有效')"`
