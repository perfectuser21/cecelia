# DoD: codex-bridge 回调 Brain 补内部鉴权 Bearer 头（任务 446aa294，链 bf5088a3 棒1 补棒）

- [x] [BEHAVIOR] 配置 `CECELIA_INTERNAL_TOKEN` 后 `callbackBrain()` 打 `POST /api/brain/execution-callback` 的请求头含 `Authorization: Bearer <token>`；未配置时不带该头（先红后绿，回归测试永久保留）
  Test: manual:bash -c "cd packages/brain && npx vitest run src/__tests__/codex-bridge-callback-auth.test.js"
- [x] [ARTIFACT] `codex-bridge.cjs` 经 `scripts/lib/brain-auth-headers.cjs` 取头（token 只从 env / `~/.credentials/cecelia-internal.env` 读，不写死）；`kernel-attempt-handler.cjs` 走 kernel 签发 callback_token 未动
  Test: manual:node -e "const fs=require('fs');const s=fs.readFileSync('packages/brain/scripts/codex-bridge/codex-bridge.cjs','utf8');if(!s.includes('brain-auth-headers.cjs')||!s.includes('...brainAuthHeaders()'))process.exit(1)"
- [x] [ARTIFACT] `node --check` 桥文件通过；DevGate 三件通过
  Test: manual:node --check packages/brain/scripts/codex-bridge/codex-bridge.cjs
