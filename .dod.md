# DoD: 步级断言认「探针」形状——step_probes 注册表 + probe:<key> + 仓库 YAML 哈希漂移（链 bf5088a3 棒2）

- [x] [BEHAVIOR] classify 认 `probe:<k1>[,<k2>]` → kind=probe/executor_kind=business_probe_runner；canonical 命令/argv 对 probe 抛 ASSERTION_PROBE_NOT_RUNNABLE；非法 key 拒收；journey cell 分类认 `probe` 态
  Test: manual:bash packages/brain/scripts/smoke/step-probes-smoke.sh
- [x] [BEHAVIOR] map/radius 按前缀排除 probe 格子且不判 unsafe；既有 vitest/pytest/smoke 形状不退化
  Test: manual:bash -c "cd packages/brain && npx vitest run src/map/radius.test.js src/lib/__tests__/gp-assertion-command.test.js src/lib/__tests__/journey-cell-assertion.test.js"
- [x] [BEHAVIOR] POST /api/brain/step-probes 按 probe_key upsert：同 spec 再 POST 回 unchanged（spec_hash=sha256(canonical JSON)），spec 变了回 updated，任一条非法整批 400 不写库；drift-check 分类 missing/extra/changed
  Test: manual:bash -c "cd packages/brain && npx vitest run src/routes/step-probes.test.js src/lib/__tests__/step-probe-spec.test.js"
- [x] [BEHAVIOR] sync 脚本：真 YAML → upsert 带 journey_step_link_id → 每格 PATCH assertion_ref=probe:<keys>；找不到格子报错不写；--check 只比对不写
  Test: manual:bash -c "cd packages/brain && npx vitest run src/__tests__/sync-step-probes.test.js"
- [x] [ARTIFACT] 迁移 474 step_probes（probe_key UNIQUE / spec jsonb / spec_hash CHECK hex64 / severity CHECK warn|error / FK journey_step_links SET NULL）+ 回滚脚本
  Test: manual:bash -c "cd packages/brain && npx vitest run src/__tests__/migration-474-step-probes.test.js"
- [x] [ARTIFACT] smoke：packages/brain/scripts/smoke/step-probes-smoke.sh 登记 allowlist；server.js 挂路由
  Test: manual:node -e "const fs=require('fs');if(!fs.readFileSync('packages/quality/smoke-allowlist.txt','utf8').includes('step-probes-smoke.sh')||!fs.readFileSync('packages/brain/server.js','utf8').includes('stepProbesRouter'))process.exit(1)"
