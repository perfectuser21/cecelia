# DoD: /dev 工作流步骤命名统一 + Codex 协作流程对齐

## 成功标准

1. [ARTIFACT] devloop-check.sh 不再包含旧步骤名
2. [BEHAVIOR] cleanup_done: true 终止路径正常触发
3. [BEHAVIOR] SKILL.md 包含完整 Codex 协作流程
4. [GATE] 所有测试文件同步更新

## 验收条件（DoD）

- [x] [ARTIFACT] devloop-check.sh 中不再包含 step_10_learning 或 step_11_cleanup
  Test: manual:bash -c "! grep -q 'step_10_learning' packages/engine/lib/devloop-check.sh && ! grep -q 'step_11_cleanup' packages/engine/lib/devloop-check.sh"

- [x] [ARTIFACT] cleanup.sh STEP_PATTERNS 使用新 6 步模型
  Test: manual:bash -c "grep -q 'step_0_worktree' packages/engine/skills/dev/scripts/cleanup.sh && grep -q 'step_5_clean' packages/engine/skills/dev/scripts/cleanup.sh"

- [x] [BEHAVIOR] 05-clean.md 完成后写入 cleanup_done: true（终止条件）
  Test: manual:bash -c "grep -q 'cleanup_done: true' packages/engine/skills/dev/steps/05-clean.md"

- [x] [BEHAVIOR] SKILL.md 完成条件包含 Codex 协作流程
  Test: manual:bash -c "grep -q 'cto_review' packages/engine/skills/dev/SKILL.md && grep -q 'code_quality' packages/engine/skills/dev/SKILL.md && grep -q 'prd_coverage_audit' packages/engine/skills/dev/SKILL.md"

- [x] [GATE] 测试文件同步更新步骤命名
  Test: manual:bash -c "! grep -q 'step_10_learning' packages/engine/tests/dev/checklist.test.ts && ! grep -q 'step_11_cleanup' packages/engine/tests/dev/checklist.test.ts"
