# DoD: Pipeline Rerun 幂等检查修复

branch: cp-03311024-pipeline-rerun-fix

## 验收条件

- [x] [ARTIFACT] orchestrator 幂等检查 SQL 只含 queued/in_progress，不含 completed
  Test: manual:node -e "const fs=require('fs'); const src=fs.readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8'); const start=src.indexOf('async function _startOnePipeline'); const end=src.indexOf('async function',start+1); const fn=src.slice(start,end); if(fn.includes(\"AND status IN ('queued', 'in_progress', 'completed')\"))throw new Error('FAIL: 幂等检查仍含 completed'); console.log('PASS');"

- [x] [ARTIFACT] 修改后 SQL 精确含 status IN ('queued', 'in_progress')
  Test: manual:node -e "const fs=require('fs'); const src=fs.readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8'); if(!src.includes(\"status IN ('queued', 'in_progress')\"))throw new Error('FAIL: SQL 不含预期字符串'); console.log('PASS');"

- [x] [BEHAVIOR] _startOnePipeline 函数体内不出现 'completed' 状态字符串
  Test: manual:node -e "const fs=require('fs'); const src=fs.readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8'); const start=src.indexOf('async function _startOnePipeline'); const end=src.indexOf('async function',start+1); const fn=src.slice(start,end); if(fn.includes(\"'completed'\"))throw new Error('FAIL: 函数体含 completed'); console.log('PASS');"

- [x] [GATE] 文件结构完整，包含必要的模块导入
  Test: manual:node -e "const fs=require('fs'); const src=fs.readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8'); if(!src.includes('import pool from'))throw new Error('FAIL: 模块导入缺失'); console.log('PASS');"

- [x] [BEHAVIOR] detect-review-issues.js 恢复 DeepSeek 🔴 **xxx** 格式检测（与 main 一致）
  Test: manual:node -e "const c=require('fs').readFileSync('scripts/devgate/detect-review-issues.js','utf8'); c.includes('未发现')&&c.includes('noIssuesDeclared')?console.log('PASS'):process.exit(1)"
