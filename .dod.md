# Task Card: Codex 独立审查加固

## 需求（What & Why）
**功能描述**: spec_review / code_review_gate 走独立 triggerLocalCodexExec，不占用 cecelia-bridge 10-slot 池。
**不做什么**: 不改常规编码任务的路由

## 成功标准
1. [BEHAVIOR] triggerLocalCodexExec 函数存在于 executor.js
2. [BEHAVIOR] cecelia-run.sh MAX_CONCURRENT = 10
3. [GATE] 所有现有测试通过

## 验收条件（DoD）
- [ ] [BEHAVIOR] triggerLocalCodexExec 在 executor.js 中定义
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('triggerLocalCodexExec'))process.exit(1)"
- [ ] [BEHAVIOR] cecelia-run.sh MAX_CONCURRENT = 10
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-run.sh','utf8');if(!c.includes(':-10}'))process.exit(1)"
- [ ] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"
