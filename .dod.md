# DoD: Cortex persist await + Capacity 硬顶

## 验收清单
- [x] cortex.js: _persistReflectionEntry 从 fire-and-forget 改为 async/await
- [x] cortex.js: _checkReflectionBreaker 中 await persist 调用
- [x] cortex-dedup-persist.test.js: 移除所有 setTimeout(200ms) 等待
- [x] capacity.js: getMaxStreams() 增加 Math.min(..., MAX_PHYSICAL_CAP) 硬顶
- [x] 现有 Brain 测试全部通过
