# DoD: step_probes 对齐 workspace 探针最终形状——http filter/reduce/minus 保留 + source_sha256 文件级哈希（链 bf5088a3 棒2 后续）

- [x] [BEHAVIOR] http 探针 `{url, filter, reduce, minus?}` 原样进 spec；缺 filter/reduce、reduce 非 count|field:<列>、未知键 → STEP_PROBE_TARGET_INVALID；sourceSha256 同文本同哈希改一字节即变
  Test: manual:bash -c "cd packages/brain && npx vitest run src/lib/__tests__/step-probe-spec.test.js"
- [x] [BEHAVIOR] POST /step-probes 收 source_sha256（非 hex64 → 400，缺省传 null COALESCE 保留）；drift-check 回 source_match（多版本并存=false 且 drift=true）
  Test: manual:bash -c "cd packages/brain && npx vitest run src/routes/step-probes.test.js"
- [x] [BEHAVIOR] sync 脚本：loadProbesYaml 附 source_sha256（原文 sha256）；upsert 与 --check 都带上；真 YAML（含 http filter/reduce）走通全链
  Test: manual:bash -c "cd packages/brain && npx vitest run src/__tests__/sync-step-probes.test.js"
- [x] [ARTIFACT] 迁移 476 幂等加列 source_sha256 + hex64 CHECK + 回滚
  Test: manual:bash -c "cd packages/brain && npx vitest run src/__tests__/migration-476-step-probes-source-sha256.test.js"
- [x] [ARTIFACT] smoke step-probes-smoke.sh 覆盖 http filter/reduce/minus 与 source_sha256
  Test: manual:bash packages/brain/scripts/smoke/step-probes-smoke.sh
