# DoD: Tick Loop 诊断集成测试

branch: cp-04011104-integration-tick

- [x] [ARTIFACT] tests/integration/tick.integration.test.ts 存在
  Test: manual:node -e "require('fs').accessSync('tests/integration/tick.integration.test.ts')"

- [x] [BEHAVIOR] 测试文件包含 ≥2 个 it() 测试用例
  Test: manual:node -e "const c=require('fs').readFileSync('tests/integration/tick.integration.test.ts','utf8');if((c.match(/\bit\(/g)||[]).length<2)process.exit(1)"

- [x] [BEHAVIOR] 不包含 vi.mock（真实集成测试）
  Test: manual:node -e "const c=require('fs').readFileSync('tests/integration/tick.integration.test.ts','utf8');if(c.includes('vi.mock'))process.exit(1)"

- [x] [ARTIFACT] test-registry.yaml 已注册 tick-integration
  Test: manual:node -e "const c=require('fs').readFileSync('test-registry.yaml','utf8');if(!c.includes('tick-integration'))process.exit(1)"
