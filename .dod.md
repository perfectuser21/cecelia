# DoD — cp-0426171442-cleanup-c-c8a-observer-e2e

## Goal

补 2 个 brain integration E2E 自动化原本手动 smoke：
- C8a harness-initiative graph checkpoint resume 链路
- observer-runner 真后台 setInterval 通道

## Artifact

- [x] [ARTIFACT] C8a checkpoint resume integration test 文件存在
      Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/integration/c8a-checkpoint-resume.integration.test.js')"

- [x] [ARTIFACT] observer-runner-flow integration test 文件存在
      Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/integration/observer-runner-flow.integration.test.js')"

## Behavior

- [x] [BEHAVIOR] C8a integration test 含 checkpointer.put 调用 >=5 + getState resume 验证
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/c8a-checkpoint-resume.integration.test.js','utf8');if(!c.includes('toBeGreaterThanOrEqual(5)'))process.exit(1);if(!c.includes('getState'))process.exit(1)"

- [x] [BEHAVIOR] observer-runner integration test 真启 setInterval + 验 run_count >= 2
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/observer-runner-flow.integration.test.js','utf8');if(!c.includes('initObserverRunner'))process.exit(1);if(!c.includes('toBeGreaterThanOrEqual(2)'))process.exit(1)"

- [x] [BEHAVIOR] C8a integration test 验幂等门：第二次 invoke spawn not called
      Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/integration/c8a-checkpoint-resume.integration.test.js','utf8');if(!c.includes('mockSpawn).not.toHaveBeenCalled'))process.exit(1)"

## Constraints

- 不动 src/ 代码（仅加 tests/）
- mock 模板：pg-checkpointer mock 用 MemorySaver 单例；spawn 用 vi.fn().mockResolvedValue
