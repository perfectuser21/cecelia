# DoD - 修复 tsconfig 路径配置和缺失 workers.config.json

QA: docs/QA-DECISION.md

## 验收标准

### 功能验收
- [ ] tsconfig.json 的 @features/core/* 路径指向 src/features/core/*，与 vite.config.ts 一致
      Test: manual:检查 tsconfig.json paths 配置
- [ ] workers.config.json 存在于 frontend/src/features/core/execution/config/ 目录
      Test: manual:git ls-files 确认文件已跟踪
- [ ] workers.config.ts 的 import 路径指向同目录的 workers.config.json
      Test: manual:检查 import 语句
- [ ] JSON 数据结构的 teams 字段改为 departments，与 WorkersConfig 类型定义匹配
      Test: manual:对比 JSON 键名和 TypeScript 接口
- [ ] npm run build 在 frontend 目录下不因这些文件报错
      Test: manual:npm run build
