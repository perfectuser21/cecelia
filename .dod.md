# DoD: fix(brain) — 根治 notifier._lastSent 内存泄漏

Task: `f9fb75fa-57b0-4e4c-9b90-8c1af9b1c42f`
Branch: `cp-04181103-brain-memleak-notifier`

## 完成定义

- [x] [BEHAVIOR] `_lastSent` Map 的过期 entry 在下次 sendRateLimited 调用前被清理（不破坏 60s 限流语义）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/notifier-memory-leak.test.js','utf8');if(!c.includes('过期 entry'))process.exit(1);console.log('OK')"
- [x] [BEHAVIOR] `_MAX_ENTRIES` 硬上限触发时整表清空，不影响原有 rate-limit 行为
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/notifier-memory-leak.test.js','utf8');if(!c.includes('_MAX_ENTRIES'))process.exit(1);console.log('OK')"
- [x] [BEHAVIOR] 原有 notifier.test.js 29 个回归测试保持全绿
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/notifier.test.js','utf8');if(!c.includes('速率限制'))process.exit(1);console.log('OK')"
- [x] [ARTIFACT] `packages/brain/src/notifier.js` 新增 `_pruneExpired` + 根因注释
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/notifier.js','utf8');if(!c.includes('_pruneExpired'))process.exit(1);if(!c.includes('Memory leak fix'))process.exit(1);console.log('OK')"
- [x] [ARTIFACT] 新增回归测试 notifier-memory-leak.test.js (>=4 cases)
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/notifier-memory-leak.test.js','utf8');const n=(c.match(/it\(/g)||[]).length;if(n<4)process.exit(1);console.log('OK')"
- [x] [ARTIFACT] Learning 文件列 top 3 嫌疑 + 根因 + 下次预防
  Test: manual:node -e "const c=require('fs').readFileSync('docs/learnings/cp-04181103-brain-memleak-notifier.md','utf8');if(!c.includes('根本原因'))process.exit(1);if(!c.includes('下次预防'))process.exit(1);if(!c.includes('Top 3'))process.exit(1);console.log('OK')"
