# DoD: Learning 系统 Task 1: DB schema 扩展（migration）

## 功能验收

- [ ] migration 文件 150_learnings_type.sql 存在
  Test: manual:bash -c "ls packages/brain/migrations/150_learnings_type.sql"

- [ ] migration 包含 learning_type 字段（5 个合法值）
  Test: manual:bash -c "grep -c 'learning_type' packages/brain/migrations/150_learnings_type.sql"

- [ ] migration 包含 source_branch、source_pr、repo 字段
  Test: manual:bash -c "grep -c 'source_branch' packages/brain/migrations/150_learnings_type.sql"

- [ ] selfcheck.js EXPECTED_SCHEMA_VERSION 已更新为 '150'
  Test: manual:bash -c "grep -c \"'150'\" packages/brain/src/selfcheck.js"

- [ ] selfcheck.test.js 断言已更新为 '150'
  Test: manual:bash -c "grep -c \"'150'\" packages/brain/src/__tests__/selfcheck.test.js"

## 质量验收

- [ ] CI 全部通过
  Test: manual:bash -c "echo 'CI via GitHub Actions'"
