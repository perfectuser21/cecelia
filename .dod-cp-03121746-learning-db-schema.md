# DoD: Learning 系统 Task 1: DB schema 扩展（migration）

## 功能验收

- [x] migration 文件 151_learnings_type.sql 存在
  Test: manual:bash -c "ls packages/brain/migrations/151_learnings_type.sql"

- [x] migration 包含 learning_type 字段（5 个合法值）
  Test: manual:bash -c "grep -c 'learning_type' packages/brain/migrations/151_learnings_type.sql"

- [x] migration 包含 source_branch 字段
  Test: manual:bash -c "grep -c 'source_branch' packages/brain/migrations/151_learnings_type.sql"

- [x] selfcheck.js EXPECTED_SCHEMA_VERSION 已更新为 '151'
  Test: manual:bash -c "grep -c \"'151'\" packages/brain/src/selfcheck.js"

- [x] selfcheck.test.js 断言已更新为 '151'
  Test: manual:bash -c "grep -c \"'151'\" packages/brain/src/__tests__/selfcheck.test.js"

## 质量验收

- [x] facts-check 通过（无冲突 migration）
  Test: manual:bash -c "node scripts/facts-check.mjs 2>&1 | grep -c 'All facts consistent'"
