# DoD: 物理容量计算加安全边际
- [x] [BEHAVIOR] SAFETY_MARGIN 从 0.85 降为 0.80
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('SAFETY_MARGIN = 0.80'))process.exit(1)"
- [x] [BEHAVIOR] calculatePhysicalCapacity 默认 memPerTaskMb 改为 500
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/platform-utils.js','utf8');if(!c.includes('memPerTaskMb = 500'))process.exit(1)"
- [x] [GATE] 所有测试通过
  Test: manual:bash -c "cd packages/brain && npx vitest run src/__tests__/platform-utils.test.js 2>&1 | tail -3"
