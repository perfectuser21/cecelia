# DoD: 物理容量计算加安全边际防止内存峰值 OOM

## 验收标准

- [x] `platform-utils.js` calculatePhysicalCapacity 默认 memPerTaskMb 改为 500
- [x] `executor.js` SAFETY_MARGIN 改为 0.80
- [x] platform-utils.test.js 测试通过
- [x] slot-allocator.test.js 测试通过

## Test

- manual: node -e "const c=require('fs').readFileSync('packages/brain/src/platform-utils.js','utf8');if(!c.includes('memPerTaskMb = 500'))process.exit(1)"
- manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('SAFETY_MARGIN = 0.80'))process.exit(1)"
