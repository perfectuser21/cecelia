---
id: task-cp-03171239-remove-old-check-dod-mapping
type: task-card
branch: cp-03171239-remove-old-check-dod-mapping
created: 2026-03-17
---

# Task Card: 删除根目录旧版 check-dod-mapping.cjs，统一用 engine 版

## 需求（What & Why）

**功能描述**: 删除 `scripts/devgate/check-dod-mapping.cjs`（根目录旧版，3.9KB，仅做基础 Test 字段检查），将所有运行时引用指向 `packages/engine/scripts/devgate/check-dod-mapping.cjs`（新版，26KB，含 isInlineCommand/detectFakeTest 等完整检查）。

**背景**: Step 1 skill 文件运行 `node scripts/devgate/check-dod-mapping.cjs` 时，从仓库根目录执行会找到旧版，而 CI 用的是 engine 版新版，导致本地自检通过但 CI 可能失败（行为不一致）。另外 `.agent-knowledge/engine.md` 和 `.claude/CLAUDE.md` 的文档命令也指向旧版。

**不做什么**: 不修改 `.github/workflows/ci-l1-process.yml`（其中路径已是正确的 `packages/engine` 版本）、不修改 `packages/quality` 中的引用（其 SCRIPT_DIR 指向 packages/quality，不是根目录版）、不修改历史存档文档。

## 成功标准

1. [ARTIFACT] `scripts/devgate/check-dod-mapping.cjs`（根目录旧版）文件不存在
2. [ARTIFACT] `.agent-knowledge/engine.md` 中的 DevGate 命令更新为 `packages/engine/scripts/devgate/check-dod-mapping.cjs`
3. [ARTIFACT] `.claude/CLAUDE.md` 中 DevGate 命令更新为 `packages/engine/scripts/devgate/check-dod-mapping.cjs`
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] 根目录旧版文件已删除
  Test: manual:bash -c "! ls scripts/devgate/check-dod-mapping.cjs 2>/dev/null"

- [x] [ARTIFACT] `.agent-knowledge/engine.md` 命令路径已更新
  Test: manual:bash -c "grep -q 'packages/engine/scripts/devgate/check-dod-mapping' .agent-knowledge/engine.md"

- [x] [ARTIFACT] `.claude/CLAUDE.md` 命令路径已更新
  Test: manual:bash -c "grep -q 'packages/engine/scripts/devgate/check-dod-mapping' .claude/CLAUDE.md"

- [x] [BEHAVIOR] engine 版脚本语法正确且可被 node 解析
  Test: manual:bash -c "node --check packages/engine/scripts/devgate/check-dod-mapping.cjs && grep -q 'detectFakeTest' packages/engine/scripts/devgate/check-dod-mapping.cjs"

- [x] [GATE] engine 测试文件存在且测试覆盖 DoD 映射功能
  Test: manual:bash -c "grep -q 'valid-contract.dod.md' packages/engine/tests/hooks/pr-gate-phase1.test.ts"

## 实现方案

**要改的文件**:
1. `scripts/devgate/check-dod-mapping.cjs` — 删除
2. `.agent-knowledge/engine.md` — 更新 node 命令路径
3. `.claude/CLAUDE.md` — 更新 node 命令路径

**Scope 锚定**: 仅删除和更新路径引用，不修改任何逻辑代码。
