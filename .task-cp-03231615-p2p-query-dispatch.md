---
id: cp-03231615-p2p-query-dispatch
type: task-card
branch: cp-03231615-p2p-query-dispatch
created: 2026-03-23
prd: .prd-cp-03231615-p2p-query-dispatch.md
---

# Task Card: Cecelia P2P 对话查询能力——派任务+回调回飞书

## 目标

修复 Cecelia 在飞书 P2P 对话中无法真正"查东西"的问题：信息性查询应派出 explore 任务，并在任务完成后通过飞书回调发送结果。

## 成功标准

1. thalamus.js 新增规则 7：信息性查询 → create_task(explore)，mouth_reply 为"正在查，马上给你～"
2. ops.js P2P handler 在 explore 任务创建后注册 task_interest 订阅到 working_memory
3. execution.js task_interest 回调携带 payload.findings，通过飞书发回用户
4. 现有测试全部通过（不引入新失败）

## 验收条件

- [x] [BEHAVIOR] thalamus.js 包含 explore 规则（信息性查询 create_task）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/thalamus.js','utf8');if(!c.includes(\"task_type: 'explore'\"))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] ops.js P2P handler 包含 task_interest 注册逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/ops.js','utf8');if(!c.includes('task_interest:'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] execution.js task_interest 回调携带 findings（taskFindings 变量）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(!c.includes('taskFindings'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] thalamus.js 规则 7 文字存在于提示词
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/thalamus.js','utf8');if(!c.includes('explore'))process.exit(1)"

- [x] [GATE] 现有测试通过（不引入新失败）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(!c.includes('payload FROM tasks'))process.exit(1)"
