# PRD: projects 表 current_phase 字段 + migration

## 背景
任务来自 Brain 自动调度，任务类型：dev

## 功能描述
1. 新建 migration 169：修正 projects_phase_check 约束（去掉 plan，只允许 now/next/later/null）
2. 根据 sequence_order 回填现有 active projects 的 current_phase
3. SelfDrive update_roadmap action 写入此字段（已实现，无需改动）

## 成功标准
- migration 169 文件存在
- CHECK 约束只含 now/next/later/null
- schema_version 包含 169
- CI 全部通过
