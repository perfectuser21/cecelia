# PRD: refactor(brain) — 重构 analyzeDeep 提取子函数（复杂度 55 → ~10）

## 背景

`cortex.analyzeDeep` 是 Brain 皮层的核心分析函数，当前实现为 216 行单函数，
圈复杂度 55，极难维护和测试。

## 目标

将函数拆解为职责单一的子函数，降低复杂度，提升可读性，行为完全不变。

## 成功标准

- analyzeDeep 主体 ≤ 40 行
- 圈复杂度降至 ~10
- 提取 ≥ 6 个子函数
- 所有现有 cortex 测试通过（零回归）
