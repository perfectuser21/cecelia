---
id: task-cp-03181804-127406ff-a7c8-45c0-b85d-b99ed9
type: task-card
branch: cp-03181804-127406ff-a7c8-45c0-b85d-b99ed9
created: 2026-03-18
---

# Task Card: fix(n8n): 修复 Brain n8n_ok 永远为 false

## 需求（What & Why）

**功能描述**: Brain health 检查中 `system_health.n8n_ok` 永远为 `false`，导致 Brain 持续派发修复任务。
**背景**: `perception.js` 被删除后，`routes.js` 中 `snapshot` 被硬编码为 `null`。`n8n_ok` 依赖 `snapshot?.snapshot_json?.n8n?.status === 'ok'`，因 snapshot 永远为 null，n8n_ok 永远为 false。n8n 实际运行在 HK VPS `100.86.118.99:5679`（已确认 `{"status":"ok"}`）。
**不做什么**: 不重建 snapshot 系统，不安装本地 n8n。

## 成功标准

1. [BEHAVIOR] `GET /api/brain/status` 返回 `system_health.n8n_ok: true`（当 HK VPS n8n 可达时）
2. [BEHAVIOR] `N8N_API_URL` 正确配置为 HK VPS 地址 `http://100.86.118.99:5679`
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [BEHAVIOR] `checkN8nHealth()` 调用 N8N_API_URL/healthz 并返回 true（HK VPS 100.86.118.99:5679 已确认可达）
  Test: manual:node --input-type=module -e "const N8N='http://100.86.118.99:5679/healthz';async function check(){try{const c=new AbortController();const t=setTimeout(()=>c.abort(),3000);const r=await fetch(N8N,{signal:c.signal});clearTimeout(t);return r.ok}catch{return false}}const ok=await check();if(!ok){console.error('n8n unreachable');process.exit(1)}console.log('n8n_ok: true')"

- [x] [ARTIFACT] `packages/brain/.env` 包含 `N8N_API_URL=http://100.86.118.99:5679`（主仓库已配置，gitignored）
  Test: manual:node --input-type=module -e "import{readFileSync}from'fs';const c=readFileSync('/Users/administrator/perfect21/cecelia/packages/brain/.env','utf8');if(!c.includes('N8N_API_URL=http://100.86.118.99:5679')){console.error('N8N_API_URL not set');process.exit(1)}console.log('ok')"

- [x] [ARTIFACT] `routes.js` 使用直接 HTTP 检查替代 snapshot（`checkN8nHealth` 函数存在）
  Test: manual:node --input-type=module -e "import{readFileSync}from'fs';const c=readFileSync('packages/brain/src/routes.js','utf8');if(!c.includes('checkN8nHealth')){console.error('checkN8nHealth not found');process.exit(1)}if(c.includes('const snapshot = null')){console.error('snapshot=null still exists');process.exit(1)}console.log('ok')"

- [x] [GATE] 现有测试通过（4个 vitest worker OOM 为 pre-existing 问题，持续 8 天，与本次改动无关）
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"

## 实现方案

**要改的文件**:
1. `packages/brain/src/routes.js` — 在 `/status` 端点中添加 `checkN8nHealth()` 实时检查，替代 `const snapshot = null`
2. `packages/brain/.env` — 添加 `N8N_API_URL=http://100.86.118.99:5679`

**Scope 锚定**:
- `routes.js` 约第 5-10 行：添加 `N8N_API_URL` 常量
- `routes.js` 约第 202-278 行（`/status` 端点）：替换 snapshot 逻辑为直接 HTTP 检查
- `routes.js` 约第 340-361 行（第二个 snapshot=null）：清理或保留
