---
id: task-cp-03220504-40920991-9049-4eb5-9c9b-527a45
type: task-card
branch: cp-03220504-40920991-9049-4eb5-9c9b-527a45
created: 2026-03-22
---

# Task Card: 重构 advanceContentPipeline（CC 33 → 10）

## 需求（What & Why）
**功能描述**: 重构 `packages/brain/src/content-pipeline-orchestrator.js` 中的 `advanceContentPipeline` 函数，将圈复杂度从 33 降至 10 以下。
**背景**: 代码复杂度扫描发现该函数圈复杂度严重超标（阈值 10，当前 33），导致可读性差、难以维护。
**不做什么**: 不改变函数的外部行为，不新增功能，不改变测试用例。

## 成功标准

1. [ARTIFACT] `advanceContentPipeline` 函数圈复杂度降至 10 以下（通过提取子函数实现）
2. [PRESERVE] 所有现有 `content-pipeline-orchestrator.test.js` 测试用例通过
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] advanceContentPipeline 现有接口签名不变（4 个参数：taskId, taskStatus, findings, dbPool）
  Test: manual:node -e "import('./packages/brain/src/content-pipeline-orchestrator.js').then(m => { if (typeof m.advanceContentPipeline !== 'function') process.exit(1); console.log('OK'); })"

- [x] [ARTIFACT] advanceContentPipeline 主体通过提取子函数降低 CC（行数 ≤ 30 行）
  Test: manual:node -e "const fs=require('fs');const src=fs.readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8');const m=src.match(/export async function advanceContentPipeline[\s\S]*?\n\}/);if(!m)process.exit(1);const lines=m[0].split('\n').length;if(lines>35){console.error('lines='+lines+' >35');process.exit(1);}console.log('OK lines='+lines);"

- [x] [BEHAVIOR] content-research → content-generate 状态转换正确
  Test: manual:npm test

- [x] [BEHAVIOR] content-review 失败重试逻辑正确（retry_count 递增，超限标 failed）
  Test: manual:npm test

- [x] [GATE] 所有现有测试通过
  Test: manual:npm test

## 实现方案（Stage 2 探索后填写）
**要改的文件**: `packages/brain/src/content-pipeline-orchestrator.js`
**Scope 锚定**: 仅重构 `advanceContentPipeline` 函数（第 200-328 行），提取私有辅助函数：
  - `_loadContext(dbPool, taskId)` — DB 查询 + 前置校验
  - `_resolveContentConfig(pipeline, task)` — 解析 keyword/content_type
  - `_handleResearch` / `_handleGenerate` / `_handleReview` / `_handleExport` — 各阶段处理器
  - `ADVANCE_HANDLERS` dispatch map — 替换 4 个 if 块
