---
id: dod-fix-tick-auto-requeue
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD: 超时任务自动 requeue

## 验收清单

- [x] D1: autoFailTimedOutTasks 的非隔离分支改为 requeue（status=queued）
      Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/tick.js','utf8'); process.exit(c.includes('auto-requeue-timeout') ? 0 : 1)"

- [x] D2: requeue 时清空 started_at（防止下次立即被再次判为超时）
      Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/tick.js','utf8'); process.exit(c.includes(\"started_at = NULL\") ? 0 : 1)"

- [x] D3: 新增单元测试 tick-timeout-requeue.test.js — 非隔离走 requeue
      Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/__tests__/tick-timeout-requeue.test.js','utf8'); process.exit(c.includes('should requeue task when not quarantined') ? 0 : 1)"

- [x] D4: 新增单元测试 — 隔离时走 quarantine 不 requeue
      Test: manual:node -e "const fs=require('fs'); const c=fs.readFileSync('packages/brain/src/__tests__/tick-timeout-requeue.test.js','utf8'); process.exit(c.includes('should quarantine task when quarantine threshold reached') ? 0 : 1)"

- [x] D5: facts-check.mjs 通过
      Test: manual:node scripts/facts-check.mjs 2>&1 | grep "All facts consistent"
