# PRD: 修复 content-pipeline 成功率崩溃（当前24%）

## 背景
所有 content-pipeline 任务失败，导致 Brain 近 1 小时成功率跌至 24%。
根本原因：代码 Bug（`_parsePipelineParams` 使用错误字段名 `keyword`，实际为 `pipeline_keyword`）。

## 成功标准

- `_parsePipelineParams` 字段名修复为优先读取 `pipeline_keyword`
- `executeGenerate` 生成内容满足 review 质量门禁（copy≥300，article≥1000 字符）
- CI 全部通过
