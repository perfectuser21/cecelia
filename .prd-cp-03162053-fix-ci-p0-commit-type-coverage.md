# PRD: CI P0 修复 — fix commit 不跳 L3 + coverage-delta 接上基线对比

## 背景
CTO 架构审查发现两个 P0 级 CI 漏洞。

## 目标
1. fix:/refactor:/chore: commit 必须跑 L3（只有 docs:/style: 可跳过）
2. coverage-delta job 接上基线 artifact 下载，真正展示覆盖率 delta

## 范围
仅修改 .github/workflows/ci-l3-code.yml
