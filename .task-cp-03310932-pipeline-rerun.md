# Task Card: content-pipeline /run 支持重新生成

## 任务信息
- 分支：cp-03310932-pipeline-rerun
- 类型：fix
- 优先级：P1

## 改动范围
### `packages/brain/src/routes/content-pipeline.js`
- 移除对 completed 状态的 400 拦截
- 改为重置 status='queued' 后继续执行

## DoD 验收

- [x] [ARTIFACT] `packages/brain/src/routes/content-pipeline.js` 包含 completed→queued 重置逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes('completed') || !c.includes('queued'))process.exit(1)"

- [x] [BEHAVIOR] content-pipeline.js 对 completed pipeline 执行 UPDATE SET status='queued' 重置
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes(\"status = 'queued'\"))process.exit(1)"

- [x] [GATE] CI 全部通过，Brain 路由文件可正常 require
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(c.length===0)process.exit(1)"

## 成功标准
- 重新生成按钮不再触发 400 失败，返回 202
- completed 状态的 pipeline 被重置为 queued 并重新触发执行
