# Task Card: content-pipeline /run 支持重新生成

## 任务信息
- 分支：cp-03310932-pipeline-rerun
- 类型：fix
- 优先级：P1

## 改动范围
### `packages/brain/src/routes/content-pipeline.js`
- 移除对 completed 状态的 400 拦截
- 改为重置 status='queued' 后继续执行

## DoD 验收

- [x] [ARTIFACT] `packages/brain/src/routes/content-pipeline.js` 不再对 completed 状态返回 400
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(c.includes(\"status === 'completed'\") && c.includes('return res.status(400)'))process.exit(1)"

- [x] [BEHAVIOR] content-pipeline.js 对 completed pipeline 执行 status='queued' 重置逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes(\"status = 'queued'\") && !c.includes(\"status='queued'\"))process.exit(1)"

- [x] [GATE] CI 全部通过，Brain 代码无语法错误
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c)process.exit(1)"

## 成功标准
重新生成按钮不再触发 400 失败
