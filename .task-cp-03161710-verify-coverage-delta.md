---
id: task-cp-03161710-verify-coverage-delta
type: task-card
branch: cp-03161710-verify-coverage-delta
created: 2026-03-16
task_id: 049a3868-8c1c-4985-a2fe-efd3424717bc
---

# Task Card: 验证 coverage-delta CI job 行为

## 需求（What & Why）
**功能描述**: 新增 `packages/brain/src/utils/format-duration.js` 工具函数及其测试文件，作为最小化 feat Brain PR，触发 coverage-delta CI job 并观察其行为。
**背景**: CI L3 刚新增了 coverage-delta job，需要验证其是否能正常工作，以及是否存在基线缺失等问题。
**不做什么**: 不修改 Engine 版本，不修改 CI 配置，纯 Brain 功能代码改动。

## 成功标准
> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [ARTIFACT] `packages/brain/src/utils/format-duration.js` 文件存在并导出 `formatDuration` 函数
2. [ARTIFACT] `packages/brain/src/utils/format-duration.test.js` 测试文件存在，含4个测试用例
3. [BEHAVIOR] `formatDuration` 对 500ms/2500ms/90000ms/120000ms 分别返回 "500ms"/"2.5s"/"1m30s"/"2m"
4. [GATE] CI 全部通过，并记录 coverage-delta job 的具体输出

## 验收条件（DoD）

- [x] [ARTIFACT] format-duration.js 工具函数文件存在
  Test: manual:node -e "require('fs').existsSync('packages/brain/src/utils/format-duration.js')&&process.exit(0)||process.exit(1)"

- [x] [ARTIFACT] format-duration.test.js 测试文件存在
  Test: manual:node -e "require('fs').existsSync('packages/brain/src/utils/format-duration.test.js')&&process.exit(0)||process.exit(1)"

- [x] [BEHAVIOR] formatDuration 函数毫秒级返回正确格式
  Test: manual:node -e "const {formatDuration}=require('./packages/brain/src/utils/format-duration.js');console.log(formatDuration(500)==='500ms'?'PASS':'FAIL')"

- [x] [BEHAVIOR] formatDuration 函数秒级返回正确格式
  Test: manual:node -e "const {formatDuration}=require('./packages/brain/src/utils/format-duration.js');console.log(formatDuration(2500)==='2.5s'?'PASS':'FAIL')"

- [x] [BEHAVIOR] formatDuration 函数分钟级（有秒）返回正确格式
  Test: manual:node -e "const {formatDuration}=require('./packages/brain/src/utils/format-duration.js');console.log(formatDuration(90000)==='1m30s'?'PASS':'FAIL')"

- [x] [BEHAVIOR] formatDuration 函数整分钟返回正确格式
  Test: manual:node -e "const {formatDuration}=require('./packages/brain/src/utils/format-duration.js');console.log(formatDuration(120000)==='2m'?'PASS':'FAIL')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -10"

## 实现方案
**要改的文件**:
- 新增 `packages/brain/src/utils/format-duration.js`
- 新增 `packages/brain/src/utils/format-duration.test.js`

**Scope 锚定**: 仅在 packages/brain/src/utils/ 下新增2个文件，零改动现有文件
