---
id: task-cp-04032355-853d9f28-e411-4165-9dff-725207
type: task-card
branch: cp-04032355-853d9f28-e411-4165-9dff-725207
created: 2026-04-04
---

# Task Card: 重构 gatherFailureContext（复杂度 25 → 10）

## 需求（What & Why）
**功能描述**: 将 `monitor-loop.js` 中的 `gatherFailureContext` 函数圈复杂度从 25 降至 10 以下
**背景**: 复杂度扫描触发 Brain 自动调度。函数内 7 个 if 条件 + try/catch + 约 17 个 `||` 操作符共同贡献复杂度
**不做什么**: 不改变函数的对外语义和返回值结构；不改动其他函数

## 成功标准

- gatherFailureContext 重构后可通过 npm test
- 三个 DB 查询提取为独立子函数，主函数逻辑扁平化

## 验收条件（DoD）

- [x] [ARTIFACT] `_fetchTaskMeta`、`_fetchRunPayload`、`_fetchSimilarFailures` 三个子函数存在于 monitor-loop.js
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/monitor-loop.js','utf8');['_fetchTaskMeta','_fetchRunPayload','_fetchSimilarFailures'].forEach(n=>{if(!c.includes(n))throw new Error('missing: '+n)});console.log('OK')"

- [x] [BEHAVIOR] gatherFailureContext 函数行数 ≤ 30（复杂度已降低）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/monitor-loop.js','utf8');const m=c.match(/async function gatherFailureContext[\s\S]*?\n\}/);if(!m)throw new Error('not found');const lines=m[0].split('\n').length;if(lines>30)throw new Error('too long: '+lines+' lines');console.log('OK lines='+lines)"

## 实现方案
**要改的文件**: `packages/brain/src/monitor-loop.js`（第 237-309 行）
**受影响函数**: `gatherFailureContext`（重构），新增 `_fetchTaskMeta`、`_fetchRunPayload`、`_fetchSimilarFailures`
**不改什么**: 函数签名、返回值结构、调用方 `callCortexForRca`
