---
id: dod-brain-agent-config-ux
version: 1.0.0
created: 2026-03-03
updated: 2026-03-03
changelog:
  - 1.0.0: 初始版本
---

# DoD：Brain Agent 配置中心体验改善

## 验收清单

- [x] **动态扫描**：`BrainLayerConfig.tsx` 的 `layers` 数组改为从 `agents` API 动态生成（filter `layer === 'brain' && id !== 'mouth'`）
  Test: manual:bash -c "grep -q 'agents.filter' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **不再硬编码 agent 列表**：删除原来的手写 `const layers: LayerDef[] = [{ id: 'thalamus', ... }, ...]`
  Test: manual:bash -c "! grep -q \"id: 'thalamus'\" apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **Toast 组件**：添加 Toast 通知，切换成功后显示绿色确认，失败显示红色错误
  Test: manual:bash -c "grep -q 'Toast' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **切换成功 toast**：显示 `「${agentName} 已切换」` 格式的绿色 toast，3 秒消失
  Test: manual:bash -c "grep -q 'showToast' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **现有功能不退化**：thalamus/cortex/reflection/memory/rumination 5 个 agent 仍正常显示，MouthModeSelector 保持不变
  Test: manual:bash -c "grep -q 'MouthModeSelector' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **Workspace CI 通过**
  Test: manual:bash -c "gh run list --repo perfectuser21/cecelia --branch cp-03031402-brain-agent-config-ux --limit 1 --json conclusion | grep -q success"
