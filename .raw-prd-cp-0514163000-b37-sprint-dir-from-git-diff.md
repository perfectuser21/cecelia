# B37 — sprint_dir 从 git diff 确定性获取

## 问题

B35/B36 的 LLM 输出 regex 在 planner verdict 包含历史 sprint 目录引用时失效。W47 实证：proposer 持续收到 HARNESS_SPRINT_DIR=sprints/w19-playground-sum。

## 修复

parsePrdNode 在 B35/B36 regex 之后，用 `git diff origin/main HEAD -- sprints/` 找 planner 实际新建的 sprint 目录，结果覆盖 regex。

## DoD

- [x] [BEHAVIOR] git diff 找到新 sprint 目录时覆盖 plannerOutput regex 结果
  Test: `tests/packages/brain/src/workflows/__tests__/harness-initiative-b37.test.js`
- [x] [ARTIFACT] `docs/learnings/cp-0514163000-b37-sprint-dir-from-git-diff.md` 存在
  Test: `node -e "require('fs').accessSync('docs/learnings/cp-0514163000-b37-sprint-dir-from-git-diff.md')"`
