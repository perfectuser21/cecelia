---
id: task-cp-04041225-fix-engine-quality
type: task-card
branch: cp-04041225-fix-engine-quality
created: 2026-04-04
---

# Task Card: Engine 质量修复 — 5 处已确认问题

## 需求（What & Why）
**功能描述**: 修复深度审查发现的 5 个有代码证据的真实问题
**背景**: check-dod-mapping 正则破损影响 DevGate 日常使用；版本同步 CI 漏检 hooks/VERSION；branch-protect 保留已废弃的旧格式回退；devloop-check 和 hook-utils 有重复/死代码
**不做什么**: 不改逻辑架构，不动 devloop-check 的条件判断流程

## 成功标准

- check-dod-mapping.cjs 能正确验证 `manual:node -e "..."` 格式
- check-version-sync.sh 检查 hooks/VERSION
- branch-protect.sh 不再回退到无后缀 .dev-mode
- devloop-check.sh step_4_status 提取为函数消除重复
- hook-utils.sh 删除 run_with_timeout 和 get_project_root

## 验收条件（DoD）

- [x] [BEHAVIOR] check-dod-mapping.cjs 正则匹配带空格的 manual 命令
  Test: manual:node -e "const r=/^\s*Test:\s*(tests\/\S+|contract:\S+|manual:[^\r\n]+)\s*$/;if(!r.test('  Test: manual:node -e \"x\"'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] check-version-sync.sh 输出包含 hooks/VERSION 检查
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/ci/scripts/check-version-sync.sh','utf8');if(!c.includes('hooks/VERSION'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] branch-protect.sh 不含 .dev-mode 无后缀回退
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/branch-protect.sh','utf8');if(c.includes('DEV_MODE_FILE=\"\$PROJECT_ROOT/.dev-mode\"'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] devloop-check.sh 包含 _get_step4_status 辅助函数
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('_get_step4_status'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] hook-utils.sh 不含 run_with_timeout 函数
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/hook-utils.sh','utf8');if(c.includes('run_with_timeout'))process.exit(1);console.log('OK')"

## 实现方案
**要改的文件**:
1. `packages/quality/scripts/devgate/check-dod-mapping.cjs:63` — 正则 `manual:[^\s]+` 改为 `manual:[^\r\n]+`
2. `packages/engine/ci/scripts/check-version-sync.sh` — 末尾追加 hooks/VERSION 检查块
3. `packages/engine/hooks/branch-protect.sh:148-151` — 删 else 回退分支
4. `packages/engine/lib/devloop-check.sh` — 提取 `_get_step4_status()` 函数替代两处重复
5. `packages/engine/lib/hook-utils.sh` — 删 `run_with_timeout` (L20-33) 和 `get_project_root` (L36-39)
