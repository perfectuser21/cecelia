# DoD - worktree-manage.sh 新增数量上限检查（MAX_WORKTREES=8）

## 验收标准

### 功能验收

- [x] [ARTIFACT] worktree-manage.sh cmd_create() 包含 MAX_WORKTREES=8 常量
  Test: manual:bash -c "grep -c 'MAX_WORKTREES=8' packages/engine/skills/dev/scripts/worktree-manage.sh"
- [x] [ARTIFACT] cmd_create() 包含 existing_count 数量检查逻辑（tail -n +2 跳过主仓库行）
  Test: manual:bash -c "grep -c 'existing_count' packages/engine/skills/dev/scripts/worktree-manage.sh"
- [x] [ARTIFACT] 数量超限时输出包含 worktree-gc.sh 提示的错误信息
  Test: manual:bash -c "grep -c 'worktree-gc.sh' packages/engine/skills/dev/scripts/worktree-manage.sh"
- [x] [ARTIFACT] 数量超限时 exit 1 退出
  Test: manual:bash -c "grep -c 'exit 1' packages/engine/skills/dev/scripts/worktree-manage.sh"

### 测试验收

- [x] [GATE] Engine 版本文件同步（6 个文件版本一致）
  Test: manual:bash -c "grep -c '12.61.0' packages/engine/VERSION"
