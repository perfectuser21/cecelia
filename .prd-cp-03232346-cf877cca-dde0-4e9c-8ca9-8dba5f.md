# PRD: MiniMax 路由清理 + content-* EXECUTOR_AFFINITY 补全

## 问题

`token-budget-planner.js` 中 explore/research/talk/data/dept_heartbeat 还在用 `primary: minimax`，但 task-router.js 已路由到西安。需要同步改为 `primary: codex`。

同时 content-pipeline/content-research/content-generate/content-review/content-export 在 EXECUTOR_AFFINITY 中完全缺失定义。

## 修改

文件：`packages/brain/src/token-budget-planner.js`
1. 5 个任务类型 minimax → codex
2. 补充 5 个 content-* 类型，指向 codex

## 成功标准

- token-budget-planner.js 不再有 minimax 引用
- content-* 类型在 EXECUTOR_AFFINITY 中有定义
