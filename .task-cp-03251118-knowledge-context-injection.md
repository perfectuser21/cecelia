# Task Card: 知识感知闭环

**Branch**: cp-03251118-knowledge-context-injection
**Brain Task ID**: b11b0328-eada-452d-8a15-e35bbe7574ef
**Priority**: P1
**Type**: feat

## 功能描述
打通 Claude ↔ Cecelia 知识感知断层。新增4个能力：
1. `GET /api/brain/context` — 汇总接口（OKR + PR + 任务 + 决策 + summary_text）
2. `GET /api/brain/okr/current` — OKR 进度树形接口
3. `.claude/CLAUDE.md` — Brain API 工具地图（让 Claude 知道能查什么）
4. Stop Hook + `POST /api/brain/conversation-summary` — 对话结束自动写 summary

## 成功标准
- `GET /api/brain/context` 返回包含 okr/recent_prs/active_tasks/summary_text 的 JSON
- `GET /api/brain/okr/current` 返回 OKR 树形结构
- `.claude/CLAUDE.md` 包含 `Brain 知识查询工具` 章节
- Stop Hook 末尾有 conversation-summary 触发

## DoD

- [x] [ARTIFACT] `packages/brain/src/routes/context.js` 文件存在
    Test: manual: node -e "require('fs').accessSync('packages/brain/src/routes/context.js')"
- [x] [ARTIFACT] `server.js` 注册 `/api/brain/context` 路由
    Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/server.js','utf8');if(!c.includes('/api/brain/context'))process.exit(1)"
- [x] [BEHAVIOR] context 接口包含 summary_text 字段
    Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/context.js','utf8');if(!c.includes('summary_text'))process.exit(1)"
- [x] [ARTIFACT] `/api/brain/okr/current` 端点存在
    Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/okr-hierarchy.js','utf8');if(!c.includes('/current'))process.exit(1)"
- [x] [BEHAVIOR] `.claude/CLAUDE.md` 包含 Brain 知识查询工具章节
    Test: manual: node -e "const c=require('fs').readFileSync('.claude/CLAUDE.md','utf8');if(!c.includes('Brain 知识查询工具'))process.exit(1)"
- [x] [ARTIFACT] `stop.sh` 包含 conversation-summary 调用
    Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop.sh','utf8');if(!c.includes('conversation-summary'))process.exit(1)"
- [x] [BEHAVIOR] `POST /api/brain/conversation-summary` 端点存在
    Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/server.js','utf8');if(!c.includes('conversation-summary'))process.exit(1)"
