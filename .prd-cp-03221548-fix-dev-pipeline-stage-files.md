# PRD: 修复 /dev pipeline Stage 步骤文件 P0/P1/P2 问题

**Task ID**: 27581a85-d52b-4493-ad01-37b9d38bc498
**Branch**: cp-03221548-fix-dev-pipeline-stage-files
**Created**: 2026-03-22

## 背景

/dev pipeline 的 Stage 步骤文件中存在多处 P0/P1/P2 级别的问题，需要修复以确保开发工作流的正确性和可靠性。

## 问题清单

### P0 问题
1. 02-code.md: 步骤顺序错误（垃圾清理在 npm test 之前），改为：npm test → 垃圾清理 → CI镜像 → DoD Test → Task Card hash
2. 02-code.md: 编号重复（两个 2.3.5），重新编号为 2.3.1→2.3.6
3. verify-step.sh: 只跑 Engine npm test → 根据 changed files 跑所有相关 package 的 test
4. verify-step.sh: 只验证 [BEHAVIOR] 条目 → 同时验证 [ARTIFACT] 和 [GATE] 条目

### P1 问题
5. 01-spec.md: 无限重试无上限 → retry>20 时输出警告并创建 P1 任务到 Brain（但仍继续重试，不降级）
6. 02-code.md: codex-test-gen 悬空引用 → 删除该特殊分支，统一为"修复代码后重新调用 subagent"
7. 04-ship.md: 读 task_id 时兼容 brain_task_id 和 task_id 两种字段名
8. 04-ship.md: 清理 TaskList 时加 owner/metadata 过滤，只清理当前 /dev 创建的 Task
9. 00-worktree-auto.md: 脚本路径改为优先 repo 内路径，再 fallback ~/.claude/
10. verify-step.sh: pipe 操作从禁止列表移除

### P2 问题
11. SKILL.md: 澄清"不要停顿"vs"等 stop hook 放行"的区别
12. 03-integrate.md: 澄清"禁止 check-version-sync"是指不在 PR 中改版本号，但验证脚本可以跑
13. 03-integrate.md: commit 示例补充 Task ID 字段

## 不做什么

- 不改 devloop-check.sh
- 不改 stop hooks
- 不加 timing 功能

## 成功标准

所有 P0/P1/P2 问题修复，CI 全部通过。
