# DoD: Learning 表同步修复

## 验收条件

- [x] distill-learnings.js 在 knowledge INSERT 后增加 learnings INSERT
- [x] 使用 content_hash (SHA-256 前 64 位) 做幂等去重
- [x] 字段映射: name→title, content→content, sub_area→metadata
- [x] learnings.category='process_improvement', learning_type='best_practice'
- [x] source_branch 从 content JSON source 字段提取分支名

## Test

- manual: node -e "const c=require('fs').readFileSync('packages/brain/scripts/distill-learnings.js','utf8');if(!c.includes('INSERT INTO learnings'))process.exit(1)"
- manual: node -e "const c=require('fs').readFileSync('packages/brain/scripts/distill-learnings.js','utf8');if(!c.includes('content_hash'))process.exit(1)"
- manual: node -e "const c=require('fs').readFileSync('packages/brain/scripts/distill-learnings.js','utf8');if(!c.includes('best_practice'))process.exit(1)"
