# DoD - Brain 测试加固 Phase 2: Integration + Mock 瘦身 + 覆盖率

## 验收标准

### A. Integration Test (1 file)
- [x] tick executeTick 链路集成测试：planNextTask + selectNextDispatchableTask 用真实 DB，外部 API mock
      Test: manual:cd packages/brain && npx vitest run src/__tests__/integration/tick-dispatch.integration.test.js --reporter=verbose 2>&1 | grep -q "passed"

### B. Routes 覆盖率提升 (2 files)
- [x] dev-logs.js 路由测试：POST / + GET /stats + GET /:task_id，pool mock
      Test: manual:cd packages/brain && npx vitest run src/__tests__/routes/dev-logs.test.js --reporter=verbose 2>&1 | grep -q "passed"
- [x] notion-sync.js 路由测试：503 disabled 路由 + GET /status + GET /full-status
      Test: manual:cd packages/brain && npx vitest run src/__tests__/routes/notion-sync.test.js --reporter=verbose 2>&1 | grep -q "passed"

### C. 低覆盖 Routes 补测试 (3 files)
- [x] task-goals.js 路由测试：GET / + GET /:id + PATCH /:id
      Test: manual:cd packages/brain && npx vitest run src/__tests__/routes/task-goals.test.js --reporter=verbose 2>&1 | grep -q "passed"
- [x] task-projects.js 路由测试：GET / + GET /:id + PATCH /:id
      Test: manual:cd packages/brain && npx vitest run src/__tests__/routes/task-projects.test.js --reporter=verbose 2>&1 | grep -q "passed"
- [x] task-tasks.js 路由测试：GET / + GET /:id + PATCH /:id
      Test: manual:cd packages/brain && npx vitest run src/__tests__/routes/task-tasks.test.js --reporter=verbose 2>&1 | grep -q "passed"

### D. 覆盖率阈值
- [x] vitest.config.js 覆盖率阈值提升到 statements: 40, branches: 25, functions: 20, lines: 20
      Test: manual:node -e "const f=require('fs'); const c=f.readFileSync('packages/brain/vitest.config.js','utf8'); process.exit(c.includes('statements: 40') && c.includes('branches: 25') ? 0 : 1)"

### E. 测试验收
- [x] npm test 在 packages/brain 下全部通过
      Test: manual:cd packages/brain && npm test 2>&1 | grep -q "passed"
