---
id: task-cp-03271114-content-pipeline-ui
type: task-card
branch: cp-03271114-content-pipeline-ui
created: 2026-03-27
---

# Task Card: Dashboard 内容制作页面

## 需求（What & Why）

**功能描述**：在 Dashboard 新增"内容制作"页面，让 ZenithJoy 员工可以通过 UI 发起内容 pipeline、查看进度。

**背景**：Brain content pipeline 后端 6 步流程已就绪（PR #1608），但只有 API 入口，没有前端 UI。员工需要一个可视化界面。

**不做什么**：不改 Brain API、不改 pipeline 逻辑

## 成功标准

1. [ARTIFACT] content feature manifest 注册到 coreFeatures
2. [ARTIFACT] ContentPipelinePage 组件存在且可渲染
3. [BEHAVIOR] 点击发起制作调用 POST /api/brain/pipelines 创建任务
4. [BEHAVIOR] 页面轮询显示 pipeline 列表
5. [BEHAVIOR] 点击 pipeline 查看 6 步进度
6. [PRESERVE] 现有 Dashboard 页面不受影响
7. [GATE] 所有现有测试通过

## 验收条件（DoD）

- [x] [PRESERVE] 现有 coreFeatures 6 个 feature 不受影响
  Test: manual:node -e "const c=require('fs').readFileSync('frontend/src/features/core/index.ts','utf8');if(!c.includes('dashboard')||!c.includes('brain'))process.exit(1)"

- [x] [ARTIFACT] content feature manifest 已注册
  Test: manual:node -e "const c=require('fs').readFileSync('frontend/src/features/core/index.ts','utf8');if(!c.includes(\"'content'\"))process.exit(1)"

- [x] [ARTIFACT] ContentPipelinePage 组件文件存在
  Test: manual:node -e "require('fs').accessSync('frontend/src/features/core/content/pages/ContentPipelinePage.tsx')"

- [x] [BEHAVIOR] 页面调用 pipelines API
  Test: manual:node -e "const c=require('fs').readFileSync('frontend/src/features/core/content/pages/ContentPipelinePage.tsx','utf8');if(!c.includes('/api/brain/pipelines'))process.exit(1)"

- [x] [BEHAVIOR] 页面包含 6 步进度展示
  Test: manual:node -e "const c=require('fs').readFileSync('frontend/src/features/core/content/pages/ContentPipelinePage.tsx','utf8');if(!c.includes('content-copywriting')&&!c.includes('copywriting'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "require('fs').accessSync('frontend/src/features/core/content/index.ts')"

## 实现方案

**要改的文件**：
1. `frontend/src/features/core/content/index.ts` — feature manifest（新建）
2. `frontend/src/features/core/content/pages/ContentPipelinePage.tsx` — 主页面（重写占位）
3. `frontend/src/features/core/index.ts` — 注册 content feature

**Scope 锚定**：仅改 frontend/src/features/core/ 下 3 个文件
