# Task: 修复 M1 Tailscale IP（executor.js CODEX_BRIDGES 默认值）

## PRD
西安 M1 重新登录 Tailscale 后 IP 变更：
- 旧 IP：100.103.88.66（zenithjoy21xx@ 已替换）
- 新 IP：100.88.166.55（当前有效）

需要更新 executor.js 中的 CODEX_BRIDGES 硬编码默认值，使 Brain 在未设置环境变量时默认路由到正确的 M1 bridge。

## 成功标准
- executor.js CODEX_BRIDGES 默认值包含新 M1 IP 100.88.166.55
- M1 bridge `/health` 返回 `ok:true`（已验证）

## DoD

### 验收条件（DoD）

- [ ] [ARTIFACT] executor.js CODEX_BRIDGES 默认值包含新 IP 100.88.166.55
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('100.88.166.55'))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] M1 bridge health check 返回 ok:true
  Test: manual:curl -s --connect-timeout 5 http://100.88.166.55:3458/health

- [ ] [GATE] CI L1-L4 全部通过
  Test: manual:gh run list --repo perfectuser21/cecelia --branch cp-03221111-fix-m1-bridge-ip
