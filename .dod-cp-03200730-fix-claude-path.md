---
id: task-cp-03200730-fix-claude-path
type: task-card
branch: cp-03200730-fix-claude-path
created: 2026-03-20
---
# Task Card: 修复 Bridge claude CLI 路径

## 需求（What & Why）
**功能描述**: Bridge 硬编码 `/Users/administrator/.local/bin/claude` 但实际路径是 `/opt/homebrew/bin/claude`，导致一晚上所有 LLM 调用失败。

## 成功标准
1. [BEHAVIOR] Bridge 用正确路径或环境变量
2. [GATE] 所有现有测试通过

## 验收条件（DoD）

- [x] [BEHAVIOR] cecelia-bridge.cjs 用 /opt/homebrew/bin/claude 或 CLAUDE_BIN 环境变量
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.cjs','utf8');if(!c.includes('homebrew'))process.exit(1)"

- [x] [BEHAVIOR] 路径可通过环境变量覆盖
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-bridge.cjs','utf8');if(!c.includes('CLAUDE_BIN'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"
