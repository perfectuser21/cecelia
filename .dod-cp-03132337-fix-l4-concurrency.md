---
id: dod-cp-03132337-fix-l4-concurrency
version: 1.0.0
created: 2026-03-13
updated: 2026-03-13
changelog:
  - 1.0.0: 初始版本
---

# DoD：修复 L4 并发限制

## 验收清单

- [x] 去掉 ci-l4-runtime.yml 的全局 concurrency mutex
  Test: manual:bash -c "grep -c 'l4-runtime-mac' .github/workflows/ci-l4-runtime.yml | grep -q '^0$'"

- [x] 端口改为动态计算（基于 GITHUB_RUN_ID，不再硬编码 5432/5433/5434）
  Test: manual:bash -c "grep -c 'GITHUB_RUN_ID' .github/workflows/ci-l4-runtime.yml"

- [x] pgdata 目录含 run_id 隔离（不同 run 不共用 /tmp/pgdata）
  Test: manual:bash -c "grep -c 'pgdata-' .github/workflows/ci-l4-runtime.yml"

- [x] matrix 里 db_port / pg_data 字段移除硬编码值
  Test: manual:bash -c "grep -c 'db_port: 5432' .github/workflows/ci-l4-runtime.yml | grep -q '^0$'"

- [x] Mac mini M1 新增 runner-5、runner-6（SSH 安装并注册）
  Test: manual:bash -c "gh api repos/perfectuser21/cecelia/actions/runners | jq '[.runners[] | select(.labels[].name == \"xian-mac-m1\")] | length' | grep -qE '^[6-9]|^[1-9][0-9]'"
