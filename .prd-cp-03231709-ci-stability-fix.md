# PRD: CI 稳定性加固 — 修复预存测试失败 + L4 Smoke Check

## 背景
Brain 单元测试有 2 个预存失败（`actions-initiative-orchestration.test.js`），
`brain-test-baseline.txt` 容忍值为 2，导致 CI 允许新 PR 引入额外失败而不报警。
L4 没有任何 API smoke check，Brain API 的破坏性变更无法被 CI 检测。

## 目标
1. 修复 `createInitiative` 的 `status` bug（`actions.js:175` 硬编码 `'pending'`，应为 `'active'`）
2. 将 `packages/brain/ci/brain-test-baseline.txt` 降为 `0`
3. 在 `ci-l4-runtime.yml` 的 `brain-integration` job 中添加 API smoke check

## 成功标准
## 成功标准
- Brain 单元测试 0 failures
- `brain-test-baseline.txt` = `0`
- L4 包含 `/health` 和 `/api/brain/tasks` 的 smoke check
