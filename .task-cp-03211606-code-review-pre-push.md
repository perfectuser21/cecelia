# Task Card: Pipeline 审查前置

delivery_type: code-only
confidence: 9

## 成功标准

- Stage 2 完成后派发 code_review_gate 给 Codex
- devloop-check.sh 在 step_2_code done 后检查 code_review
- Stage 3 不再派发 code_review_gate
- SKILL.md 流程描述与实际一致

## DoD

- [x] [BEHAVIOR] 02-code.md Stage 2 末尾包含 code_review_gate 派发逻辑
  Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('code_review_gate'))process.exit(1)"

- [x] [BEHAVIOR] devloop-check.sh 在 step_2_code done 后检查 code_review_gate
  Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');const lines=c.split('\n');const idx2=lines.findIndex(l=>l.includes('step_2_code'));const idxCR=lines.findIndex(l=>l.includes('code_review_gate_task_id'));if(idxCR<idx2||idxCR===-1)process.exit(1)"

- [x] [BEHAVIOR] devloop-check.sh 条件 5（原 CI 后 code_review）已删除
  Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');const lines=c.split('\n');let found=false;for(let i=0;i<lines.length;i++){if(lines[i].includes('条件 5')&&lines[i].includes('code_review'))found=true;}if(found)process.exit(1)"

- [x] [BEHAVIOR] 03-integrate.md 不再包含 code_review_gate 派发代码块
  Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(c.includes('curl.*code_review_gate'))process.exit(1)"

- [x] [BEHAVIOR] SKILL.md 流程描述更新为 Stage 2 包含 code_review_gate
  Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(!c.includes('Stage 2')|| !c.includes('code_review_gate'))process.exit(1)"
