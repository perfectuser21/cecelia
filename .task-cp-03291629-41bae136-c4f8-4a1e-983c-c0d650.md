---
id: task-cp-03291629-41bae136
type: task-card
branch: cp-03291629-41bae136-c4f8-4a1e-983c-c0d650
created: 2026-03-29
---

# Task Card: 从 /dev Stage 3 移除 Playwright Evaluator

## 需求（What & Why）

**功能描述**: /dev Stage 3 流水线中不再包含 Playwright Evaluator 步骤，devloop-check 不再对 evaluator seal 文件做任何检查或拦截。

**背景**: Playwright Evaluator（条件 4.5）在 CI 通过后强制运行 Playwright 验证 DoD [BEHAVIOR] 条目，带来额外依赖和复杂度。决定将其从主流程中移除，简化 Stage 3 执行路径。

**不做什么**: 不删除 playwright-evaluator.sh 脚本本身（若存在）；不修改 Stage 1/2/4 的任何逻辑；不调整 CI workflow 配置。

---

## 成功标准

> [ARTIFACT] = 产出物（文件/API/配置）
> [BEHAVIOR] = 运行时行为（系统如何响应输入）
> [GATE] = 门禁（CI/测试/代码质量）

1. [ARTIFACT] `packages/engine/skills/dev/steps/03-integrate.md` 中不含 "3.3 Playwright Evaluator" 章节，原 3.4/3.5 顺延回 3.3/3.4
2. [ARTIFACT] `packages/engine/lib/devloop-check.sh` 中不含条件 4.5 的 playwright_evaluator 逻辑块
3. [BEHAVIOR] devloop-check 在 CI 通过后直接放行进入条件 5（PR 合并检测），不因 evaluator seal 文件缺失而 blocked
4. [GATE] CI L1-L4 全部通过，Engine 版本 bump 正确同步到 5 个文件

---

## 验收条件（DoD）

- [x] [ARTIFACT] 03-integrate.md 不再含有 "Playwright Evaluator" 字样
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(c.includes('Playwright Evaluator'))process.exit(1)"

- [x] [ARTIFACT] devloop-check.sh 不再含有 "条件 4.5" 字样
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(c.includes('条件 4.5'))process.exit(1)"

- [x] [ARTIFACT] devloop-check.sh 不再含有 "playwright_evaluator" 字样
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(c.includes('playwright_evaluator'))process.exit(1)"

- [x] [PRESERVE] devloop-check.sh 条件 5（PR 合并检测）仍然存在，未被误删
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('条件 5'))process.exit(1)"

- [x] [PRESERVE] 03-integrate.md 仍然包含 CI 等待逻辑（3.2 等待 CI）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('3.2'))process.exit(1)"

- [x] [BEHAVIOR] Engine 版本已 bump，5 个版本文件内容一致
  Test: manual:node -e "const fs=require('fs');const v=fs.readFileSync('packages/engine/VERSION','utf8').trim();const p=JSON.parse(fs.readFileSync('packages/engine/package.json','utf8')).version;if(v!==p)process.exit(1)"

- [x] [GATE] check-dod-mapping.cjs 可正常访问（DevGate 脚本完整）
  Test: manual:node -e "const fs=require('fs');try{fs.accessSync('packages/engine/scripts/devgate/check-dod-mapping.cjs')}catch(e){process.exit(1)}"

---

## 实现方案（Stage 2 探索后填写）

**要改的文件**:
- `packages/engine/skills/dev/steps/03-integrate.md` — 删除 3.3 Playwright Evaluator 章节，3.4→3.3，3.5→3.4，版本 1.3.0→1.4.0
- `packages/engine/lib/devloop-check.sh` — 移除条件 4.5 逻辑块（38行）及相关注释
- `packages/engine/package.json` / `package-lock.json` / `VERSION` / `.hook-core-version` / `regression-contract.yaml` — 版本 13.59.0→13.60.0
- `packages/engine/features/feature-registry.yml` — 新增 13.60.0 changelog 条目
- `packages/engine/docs/paths/` — generate-path-views.sh 重新生成

**Scope 锚定**: packages/engine/（仅工作流引擎，不涉及 Brain/apps）
