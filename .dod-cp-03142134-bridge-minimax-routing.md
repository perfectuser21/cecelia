---
id: dod-cp-03142134-bridge-minimax-routing
version: 1.1.0
created: 2026-03-14
updated: 2026-03-14
changelog:
  - 1.0.0: 初始版本
  - 1.1.0: 修复 Test 命令路径（绝对→相对）
---

# DoD: cecelia-run bridge 自动启动修复

## 验收清单

- [x] server.js 在 `server.listen` 回调中调用 `startCeceliaBridge()`
  Test: manual:bash -c "grep -c 'startCeceliaBridge' packages/brain/server.js"

- [x] `startCeceliaBridge()` 先检测 `/health`，若 200 则跳过（幂等）
  Test: manual:bash -c "grep -c 'health' packages/brain/server.js"

- [x] bridge 进程以 `spawn` 异步启动，stdout/stderr 写入 `/tmp/cecelia-bridge.log`
  Test: manual:bash -c "grep -c 'cecelia-bridge.log' packages/brain/server.js"

- [x] `checkCeceliaRunAvailable()` 改用 `/health` 端点，200 表示可用
  Test: manual:bash -c "grep -c '/health' packages/brain/src/executor.js"

- [x] `checkCeceliaRunAvailable()` 改用 `response.ok` 判断健康状态
  Test: manual:bash -c "grep -c 'response.ok' packages/brain/src/executor.js"
