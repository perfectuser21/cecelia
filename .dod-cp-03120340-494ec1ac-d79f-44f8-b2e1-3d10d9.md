---
id: dod-cp-03120340-cortex-insight-autoclose
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

## DoD - Cortex insight → action 自动闭合机制

### 验收清单

- [x] `cortex.js` 的 `recordLearnings` 函数在新增 learning 后自动分析内容
  Test: manual:bash -c "grep -c 'maybeCreateInsightTask' packages/brain/src/cortex.js"

- [x] `hasCodeFixSignal` 函数检测代码修复信号关键词
  Test: manual:bash -c "grep -c 'hasCodeFixSignal' packages/brain/src/cortex.js"

- [x] `maybeCreateInsightTask` 在检测到代码修复信号时创建 dev task
  Test: manual:bash -c "grep -c 'createTask' packages/brain/src/cortex.js"

- [x] 以 `insight_learning_id` 为去重 key，已有对应 task 时不重复创建
  Test: manual:bash -c "grep -c 'insight_learning_id' packages/brain/src/cortex.js"

- [x] insight 触发 task 后标记 `applied=true`
  Test: manual:bash -c "grep -c 'applied.*true\|applied = true' packages/brain/src/cortex.js"

- [x] integration test 验证 insight→task 链路（mock pool）
  Test: manual:bash -c "grep -c 'maybeCreateInsightTask' packages/brain/src/__tests__/cortex-insight-autoclose.test.js"

- [x] 测试覆盖：有信号创建 task、无信号跳过、重复去重三种场景
  Test: manual:bash -c "grep -c 'describe\\|it(' packages/brain/src/__tests__/cortex-insight-autoclose.test.js"
