---
id: dod-mouth-thalamus-connect
version: 1.0.0
created: 2026-03-01
updated: 2026-03-01
changelog:
  - 1.0.0: 初始版本
---

# DoD: 嘴巴与丘脑连通

## 验收清单

- [ ] 1. `orchestrator-chat.js` 的 `callWithHistory()` 支持解析 JSON 输出（有 reply 字段时用 reply，否则全文当 reply）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/orchestrator-chat.js','utf8');process.exit(c.includes('thalamus_signal')&&c.includes('JSON.parse')?0:1)"

- [ ] 2. Mouth system prompt 新增行动能力说明（thalamus_signal JSON 格式）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/orchestrator-chat.js','utf8');process.exit(c.includes('thalamus_signal')&&c.includes('create_task')?0:1)"

- [ ] 3. `thalamus.js` 导出 `observeChat(signal, context)` 函数
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/thalamus.js','utf8');process.exit(c.includes('observeChat')&&c.includes('export')?0:1)"

- [ ] 4. `orchestrator-chat.js` 在检测到 thalamus_signal 时异步调用 `observeChat`（不阻塞 reply）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/orchestrator-chat.js','utf8');process.exit(c.includes('observeChat')&&c.includes('Promise.resolve')?0:1)"

- [ ] 5. `observeChat` 支持 create_task / cancel_task / save_note / update_user_profile 四种类型
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/thalamus.js','utf8');process.exit(['create_task','cancel_task','save_note','update_user_profile'].every(t=>c.includes(t))?0:1)"

- [ ] 6. Mouth system prompt 注入最近任务状态（让嘴巴能自然回答"开始了吗"）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/orchestrator-chat.js','utf8');process.exit(c.includes('buildRecentTasksBlock')||c.includes('recent_tasks')?0:1)"

- [ ] 7. Brain 测试通过
  Test: manual:cd packages/brain && npx vitest run --exclude=src/__tests__/blocks.test.js --reporter=verbose 2>&1 | grep -E "Test Files"

- [ ] 8. 版本 patch bump v1.141.15 → v1.141.16
  Test: manual:node -e "const p=require('./packages/brain/package.json');process.exit(p.version==='1.141.16'?0:1)"
