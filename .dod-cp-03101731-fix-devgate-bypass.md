---
id: dod-fix-devgate-bypass
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD：A+ 强制开发证据方案

## P0：核心修复

- [x] `.gitignore` 移除 `.prd-*.md` 和 `.dod-*.md` 两行（使这两类文件可被 git 追踪）
  Test: manual:grep -c "\.prd-\*\.md\|\.dod-\*\.md" .gitignore || echo "已移除"

- [x] `check-prd.sh`：PRD 文件不存在时 exit 1（不是 exit 0）
  Test: tests/quality-system/test-check-prd.sh

- [x] `check-dod-mapping.cjs`：CI 环境 DoD 不存在时 exit 1（删除 "CI 模式跳过" 逻辑）
  Test: tests/quality-system/test-check-dod-mapping.sh

- [x] `check-learning.sh`：LEARNINGS.md 不存在 → exit 1；PR 未修改 LEARNINGS.md → exit 1（除非 PR title 含 `[SKIP-LEARNING]`）
  Test: tests/quality-system/test-check-learning.sh

- [x] `ci-l1-process.yml` `l1-passed` gate：cleanup-check / check-prd / check-learning / quality-meta-tests / ci-config-audit 全部改用 `!= 'success'` 判断（消除 skipped 漏洞）
  Test: manual:grep -c "!= 'success'" .github/workflows/ci-l1-process.yml

- [x] `ci-l1-process.yml` engine-l1：删除"Block PRD/DoD in PR to main"步骤（该步骤与 A+ 方案冲突）
  Test: manual:grep -c "Block PRD/DoD" .github/workflows/ci-l1-process.yml || echo "已删除"

## P1：Learning 规则明确化 + 测试更新

- [x] `tests/quality-system/test-check-prd.sh`：场景4 期望改为 exit 1（PRD 不存在 → 失败）
  Test: tests/quality-system/test-check-prd.sh

- [x] 新增 `tests/quality-system/test-check-learning.sh`：覆盖 Learning 的强制场景
  Test: tests/quality-system/test-check-learning.sh

- [x] `tests/quality-system/run-all.sh`：注册新增的 test-check-learning.sh
  Test: manual:grep "test-check-learning" tests/quality-system/run-all.sh

## P2：基于路径触发强制 dev 检查

- [x] 新建 `packages/engine/config/required-dev-paths.yml`：定义高风险路径列表
  Test: manual:test -f packages/engine/config/required-dev-paths.yml && echo "存在"

- [x] `ci-l1-process.yml`：新增 `required-paths-check` 步骤，改动高风险路径时强制要求 PRD/DoD 存在
  Test: manual:grep -c "required-paths-check\|required-dev-paths" .github/workflows/ci-l1-process.yml
