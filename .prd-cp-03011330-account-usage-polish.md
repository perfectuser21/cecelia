# PRD: Account Usage UI 优化 + 调度负载均衡

## 背景
PR #211 实现了 AccUsageRings 精简 + AccountUsagePage 详情页后，有三处需要改进：
1. 圆环内已显示 pct%，下方文字重复显示 → 移除
2. 账号标识用 account1/2/3，认不出是谁 → 换成邮箱缩写
3. account3 从未被使用（seven_day_pct=0%），因为 5h 同为 0% 时 account2 永远排前

## 目标

### 1. AccUsageRings 去掉重复 pct（frontend）
- 圆环 value={`${pct}%`} 已显示，下方只显示 `↺倒计时`，不再重复 pct

### 2. 账号显示用邮箱缩写（frontend）
- account1 (alexperfectapi01@gmail.com) → **AP01**
- account2 (chalexlch@gmail.com) → **LCH**
- account3 (zenithjoy21xx@gmail.com) → **ZJ**
- 适用：AccUsageRings 的 label + AccountUsagePage 的列标题

### 3. selectBestAccount 加 7d 次级排序（brain）
- 5h ePct 相同时，按 seven_day_pct 升序排（用量低的优先）
- 加测试 S5 验证 tie-break 行为

## 技术约束
- Dashboard（apps/dashboard/src/）只改 AccUsageRings 和 AccountUsagePage
- Brain（packages/brain/src/account-usage.js）只改 selectBestAccount 排序逻辑
- 不改 API
