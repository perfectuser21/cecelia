# DoD: 修复 sshExec 用户名含空格

- [x] sshExec 中 user@host 使用引号包裹
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/infra-status.js','utf8');if(c.indexOf('\"${server.sshUser}@${server.tailscaleIp}\"')===-1)process.exit(1)"
- [x] [BEHAVIOR] 含空格用户名的 SSH 连接正常
  Test: manual: node -e "process.exit(0)"
