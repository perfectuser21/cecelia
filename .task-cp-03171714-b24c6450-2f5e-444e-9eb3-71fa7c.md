---
id: task-cp-03171714-b24c6450-2f5e-444e-9eb3-71fa7c
type: task-card
branch: cp-03171714-b24c6450-2f5e-444e-9eb3-71fa7c
created: 2026-03-17
---

# Task Card: MAX_SEATS 重启状态日志 + 并发天花板评估端点

## 需求（What & Why）
**功能描述**: 提升 Brain 并发配置的可观测性，防止 MAX_SEATS 天花板成为静默失败根源。
**背景**: PR1(fix)+PR2(refactor)+PR3(test) 完成后，这是孤儿恢复 initiative 最后一环。
**不做什么**: 不修改并发调度逻辑，不改变 MAX_SEATS 的计算方式。

## 成功标准

1. [ARTIFACT] Brain 启动日志包含 MAX_SEATS 和 INTERACTIVE_RESERVE 配置值
2. [ARTIFACT] syncOrphanTasksOnStartup 完成后日志包含 requeued/rebuilt/failed 汇总
3. [BEHAVIOR] GET /api/brain/capacity 返回包含 max_seats 字段的 JSON
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] server.js 启动时打印 MAX_SEATS 和 INTERACTIVE_RESERVE
  Test: manual:bash -c "grep -c 'MAX_SEATS' packages/brain/server.js"

- [x] [ARTIFACT] server.js syncOrphanTasksOnStartup 日志包含 requeued 和 failed 字段
  Test: manual:bash -c "grep -c 'requeued' packages/brain/server.js"

- [x] [BEHAVIOR] GET /api/brain/capacity 响应体包含 max_seats 字段
  Test: packages/brain/src/__tests__/capacity-endpoint.test.js

- [x] [ARTIFACT] capacity 端点测试覆盖 max_seats 字段存在
  Test: manual:bash -c "grep -c 'max_seats' packages/brain/src/__tests__/capacity-endpoint.test.js"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test -- --reporter=verbose 2>&1 | tail -10"

## 实现方案

**要改的文件**:
- `packages/brain/server.js` — 启动时导入并打印 MAX_SEATS/INTERACTIVE_RESERVE；改善 syncOrphan 日志
- `packages/brain/src/routes.js` — 新增 `GET /capacity` 路由，返回 max_seats + 其他并发配置
- `packages/brain/src/__tests__/capacity-endpoint.test.js` — 新建，测试 /capacity 端点 max_seats 字段

**Scope 锚定**:
- executor.js 已有 `getBudgetCap()` 导出，可直接在 routes.js 中 import 使用
- executor.js 已导出 `MAX_SEATS`、`INTERACTIVE_RESERVE`，server.js 可在 listen 回调中 import 打印
- syncOrphanTasksOnStartup 返回 `{ orphans_found, orphans_fixed, requeued, rebuilt }`；failed = orphans_fixed - requeued - rebuilt
