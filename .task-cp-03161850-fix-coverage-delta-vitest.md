---
id: task-cp-03161850-fix-coverage-delta-vitest
type: task-card
branch: cp-03161850-fix-coverage-delta-vitest
created: 2026-03-16
---

# Task Card: 修复 coverage-delta：换用 davelosert/vitest-coverage-report-action

## 需求（What & Why）

**功能描述**: 替换 `.github/workflows/ci-l3-code.yml` 中的 `coverage-delta` job，从 `anuraag016/Jest-Coverage-Diff` 换成 `davelosert/vitest-coverage-report-action@v2`。同时添加 `coverage-baseline` job 用于在 push 到 main 时更新覆盖率基线。

**背景**: Brain 的 vitest.config.js 已配置 `coverage.reporter: ['json-summary']`，输出到 `./coverage/coverage-summary.json`。当前 `coverage-delta` job 用的 `anuraag016/Jest-Coverage-Diff` 内部跑 `npx jest`，与 vitest 不兼容。vitest-coverage-report-action 是专为 vitest 设计的，能正确读取 json-summary 输出并在 PR 中展示 coverage report。

**不做什么**: 不修改 Brain 的 vitest.config.js 或测试代码；不改动除 `ci-l3-code.yml` 之外的 workflow 文件。

## 成功标准

1. [ARTIFACT] `.github/workflows/ci-l3-code.yml` 中 `coverage-delta` job 使用 `davelosert/vitest-coverage-report-action@v2`，不再引用 `anuraag016/Jest-Coverage-Diff`
2. [ARTIFACT] `ci-l3-code.yml` 新增 `coverage-baseline` job，在 push 到 main 时运行
3. [ARTIFACT] Engine 版本从 12.92.0 bump 到 12.93.0（5 个文件 + feature-registry.yml）
4. [GATE] CI 全部通过（[CONFIG] 标签豁免测试覆盖检查）

## 验收条件（DoD）

- [x] [ARTIFACT] coverage-delta job 不再引用 anuraag016/Jest-Coverage-Diff
  Test: manual:! grep -q 'anuraag016' /Users/administrator/perfect21/cecelia/.claude/worktrees/fix-coverage-delta-vitest/.github/workflows/ci-l3-code.yml

- [x] [ARTIFACT] coverage-delta job 使用 davelosert/vitest-coverage-report-action@v2
  Test: manual:grep -c 'davelosert/vitest-coverage-report-action@v2' /Users/administrator/perfect21/cecelia/.claude/worktrees/fix-coverage-delta-vitest/.github/workflows/ci-l3-code.yml

- [x] [ARTIFACT] coverage-baseline job 存在于 ci-l3-code.yml
  Test: manual:grep -c 'coverage-baseline' /Users/administrator/perfect21/cecelia/.claude/worktrees/fix-coverage-delta-vitest/.github/workflows/ci-l3-code.yml

- [x] [ARTIFACT] Engine VERSION 文件为 12.93.0
  Test: manual:grep -c '12.93.0' /Users/administrator/perfect21/cecelia/.claude/worktrees/fix-coverage-delta-vitest/packages/engine/VERSION

- [x] [ARTIFACT] feature-registry.yml 包含 12.93.0 changelog 条目
  Test: manual:grep -c '12.93.0' /Users/administrator/perfect21/cecelia/.claude/worktrees/fix-coverage-delta-vitest/packages/engine/features/feature-registry.yml

- [x] [GATE] 所有现有测试通过（CI 通过）
  Test: manual:git -C /Users/administrator/perfect21/cecelia/.claude/worktrees/fix-coverage-delta-vitest log --oneline -1

## 实现方案

**要改的文件**:
1. `.github/workflows/ci-l3-code.yml` — 替换 coverage-delta job，添加 coverage-baseline job
2. `packages/engine/VERSION` — 12.92.0 → 12.93.0
3. `packages/engine/package.json` — version bump
4. `packages/engine/package-lock.json` — version bump
5. `packages/engine/CHANGELOG.md` — 添加 12.93.0 条目
6. `packages/engine/features/feature-registry.yml` — 添加 changelog 条目
7. 运行 `bash packages/engine/scripts/generate-path-views.sh`

**Scope 锚定**: 仅修改 CI workflow 文件和 engine 版本文件，不触碰 Brain 代码或测试文件
