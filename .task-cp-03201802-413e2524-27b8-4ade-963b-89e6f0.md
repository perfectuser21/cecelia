---
id: task-cp-03201802-413e2524-27b8-4ade-963b-89e6f0
type: task-card
branch: cp-03201802-413e2524-27b8-4ade-963b-89e6f0
created: 2026-03-20
---

# Task Card: Stage 4 成功回调 Brain + 去掉 30 次重试

## 需求（What & Why）
**功能描述**:
1. PR 合并后回调 Brain execution-callback status=completed
2. stop-dev.sh 去掉 30 次 retry_count 兜底，改为卡住时报告 Brain 让 Patrol 处理

**背景**:
- 当前 Stage 4 完成后没有回调 Brain，需要添加执行状态回调机制
- stop-dev.sh 当前有 30 次重试兜底机制，但这种处理方式不合适，应该报告给 Brain 让 Patrol 模块处理

**不做什么**:
- 不修改其他阶段的回调逻辑
- 不改变现有的 stop-dev.sh 核心循环逻辑

## 成功标准
> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [BEHAVIOR] PR 合并后自动调用 Brain execution-callback API
2. [BEHAVIOR] stop-dev.sh 移除 retry_count 兜底，改为报告 Brain
3. [GATE] CI 全部通过

## 验收条件（DoD）
> Test 字段在 Stage 2 探索后填写，现在写条目即可。

- [x] [ARTIFACT] Stage 4 脚本包含 Brain 回调逻辑
  Test: manual:node -e "const content=require('fs').readFileSync('packages/engine/skills/dev/steps/04-ship.md','utf8');if(!content.includes('execution-callback'))process.exit(1)"

- [x] [BEHAVIOR] PR 合并成功后调用 execution-callback status=completed
  Test: manual:node -e "const fs=require('fs');const content=fs.readFileSync('packages/engine/skills/dev/steps/04-ship.md','utf8');if(!content.includes('\"status\":\"completed\"'))process.exit(1)"

- [x] [BEHAVIOR] stop-dev.sh 移除 30 次重试兜底机制
  Test: manual:node -e "const content=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(content.includes('retry_count') && content.match(/retry_count.*30/))process.exit(1)"

- [x] [BEHAVIOR] stop-dev.sh 在卡住时报告 Brain 让 Patrol 处理
  Test: manual:node -e "const content=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!content.includes('Brain') || !content.includes('Patrol'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:npm test

## 实现方案（Stage 2 探索后填写）
**要改的文件**: 无需修改，功能已存在
**Scope 锚定**:
1. `packages/engine/skills/dev/steps/04-ship.md` 第179-182行已实现 Brain execution-callback API
2. `packages/engine/hooks/stop-dev.sh` v15.4.0已移除30次硬限制，改为 pipeline_rescue 机制

**发现**: 所有要求的功能在现有代码中已经实现，无需额外开发。