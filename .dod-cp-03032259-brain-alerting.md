---
id: dod-cp-03032259-brain-alerting
version: 1.0.0
created: 2026-03-03
updated: 2026-03-03
changelog:
  - 1.0.0: 初始版本
---

## DoD - Brain 分级报警系统（P0/P1/P2/P3）

### 验收清单

- [x] PRD 确认
- [ ] `packages/brain/src/alerting.js` 存在，导出 raise/flushP1/flushP2/flushAlertsIfNeeded/getStatus
- [ ] `packages/brain/src/routes/alerting.js` 存在，注册 GET /status 和 POST /test
- [ ] `packages/brain/server.js` 注册 /api/brain/alerting 路由
- [ ] `circuit-breaker.js` 使用 raise('P0') 替代 notifyCircuitOpen
- [ ] `routes.js` 单次任务失败走 raise('P2')，隔离走 raise('P1')
- [ ] `tick.js` 调用 flushAlertsIfNeeded()
- [ ] `alerting.test.js` 通过测试（raise/flush/status 全覆盖）
- [ ] `npm run test --workspace packages/brain -- --run src/__tests__/alerting.test.js` 通过

### Test 字段

  Test: manual:bash -c "cd packages/brain && npx vitest run src/__tests__/alerting.test.js 2>&1 | grep -E 'PASS|FAIL|Tests'"
  Test: manual:bash -c "node -e \"import('./packages/brain/src/alerting.js').then(m => m.getStatus()).then(s => s.p1_pending === 0 && s.p2_pending === 0 ? console.log('OK') : process.exit(1))\""
