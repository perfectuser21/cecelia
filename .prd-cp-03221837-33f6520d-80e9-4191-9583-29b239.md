# PRD — Pipeline Rescue: 拆分 runPerception 降低圈复杂度

## 背景

Pipeline Patrol 检测到分支 `cp-03221804-0ba02ac2-a1d4-48e0-986c-db18e8` 在 `step_2_code`
阶段停留 23 分钟后 worktree 消失，代码未能 push/merge。

原任务（`0ba02ac2`）已在 Brain 标记为 `completed`，但 `perception.js` 的重构代码从未进入 main。
本次救援将该分支的有效代码 cherry-pick 合并到 main。

## 功能描述

将 `runPerception`（圈复杂度 67）拆分为 14 个独立 async 子函数，
每个函数负责单一信号采集并返回 `observation[]`，
主函数通过 `Promise.all` 并行调用后聚合结果，圈复杂度降至约 2。

## 成功标准

- [ ] `runPerception` 已被拆分为 14 个子采集函数
- [ ] `packages/brain/src/desire/perception.js` 圈复杂度 ≤ 10
- [ ] `npm test` 全部通过
