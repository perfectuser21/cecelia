---
id: task-cp-04060806-d33d49cd-3ac5-471d-8e2e-875f77
type: task-card
branch: cp-04060806-d33d49cd-3ac5-471d-8e2e-875f77
created: 2026-04-06
task_id: d33d49cd-3ac5-471d-8e2e-875f776dd160
---

# Task Card: 数据采集v1 - 多平台集成与验证

## 需求（What & Why）

**功能描述**：完成微博/小红书/抖音等主要平台的数据采集接口集成，验证每日数据流入正常率≥95%。
**背景**：Brain 自动调度，为智能周报和效果分析提供基础数据。
**不做什么**：不实现平台爬虫本身（已有 daily-scrape-scheduler + social-media-sync），不修改采集脚本，不做实时数据采集。

## 成功标准

> [ARTIFACT] Brain API 新增 collection-stats 端点
> [ARTIFACT] Dashboard 新增采集仪表盘页面
> [BEHAVIOR] API 返回各平台每日数据量、健康率指标

## 验收条件（DoD）

- [x] [ARTIFACT] `GET /api/brain/analytics/collection-stats` 端点存在
  Test: manual:node -e "const f=require('fs');const c=f.readFileSync('packages/brain/src/routes/analytics.js','utf8');if(!c.includes('collection-stats'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] collection-stats API 返回 platforms 数组含 daily_volumes 字段
  Test: manual:curl -s localhost:5221/api/brain/analytics/collection-stats | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));if(!d.platforms)process.exit(1);console.log('OK',d.platforms.length,'platforms')"

- [x] [ARTIFACT] CollectionDashboardPage.tsx 文件存在
  Test: manual:node -e "require('fs').accessSync('apps/dashboard/src/pages/collection-dashboard/CollectionDashboardPage.tsx');console.log('OK')"

- [x] [BEHAVIOR] /collection-dashboard 路由已注册到 system-hub/index.ts
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/system-hub/index.ts','utf8');if(!c.includes('collection-dashboard'))process.exit(1);console.log('OK')"

## 实现方案

**要改的文件**：
1. `packages/brain/src/routes/analytics.js` — 新增 `GET /analytics/collection-stats` 端点
2. `apps/dashboard/src/pages/collection-dashboard/CollectionDashboardPage.tsx` — 新建页面
3. `apps/api/features/system-hub/index.ts` — 注册路由 + 组件 + 导航子项

**关键逻辑**：
- collection-stats API 从 `content_analytics` 查近 7 天每平台每日数据量
- 从 `tasks` 查 `platform_scraper` 任务成功率
- 前端展示平台卡片 + 健康率指示器 + 手动触发按钮

**不改什么**：不改 social-media-sync.js，不改 daily-scrape-scheduler.js，不改爬虫
