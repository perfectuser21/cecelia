---
id: task-cp-03231119-fix-dev-skill-docs
type: task-card
branch: cp-03231119-fix-dev-skill-docs
created: 2026-03-23
---

# Task Card: fix(dev-skill): /dev skill .md 文档 P0/P1/P2 bug 修复

## 需求（What & Why）

修复 /dev pipeline skill 文档（SKILL.md + steps/00~04.md）中的多处 bug，包括
macOS sed 不兼容、develop 分支假设、seal key 描述错误、章节编号错误、grep -c 多文件输出问题等。

**不做什么**: 不改逻辑，只修文档描述和 bash 兼容性问题

## 成功标准

1. [ARTIFACT] 所有 sed -i 调用改为跨平台兼容写法（macOS + Linux）
2. [BEHAVIOR] /dev 文档流程描述与实际实现一致（seal key、章节号、merge 位置）
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] SKILL.md 4-Stage Pipeline 流程结构不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(!c.includes('Stage 1'))process.exit(1)"

- [x] [ARTIFACT] SKILL.md 删除 develop 分支动态检测描述，改为 main
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(c.includes('有 develop 用 develop'))process.exit(1)"

- [x] [BEHAVIOR] 02-code.md sed -i 改为跨平台写法
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(c.includes('sed -i \"s/') && !c.includes('sed -i \x27\x27'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/04-ship.md','utf8');if(!c.includes('Learning'))process.exit(1)"
