---
id: dod-cp-03132137-fix-codex-runner
version: 1.0.0
created: 2026-03-13
updated: 2026-03-13
changelog:
  - 1.0.0: 初始版本
---

# DoD - 修复 runner.sh — 对齐 Claude Code 单次完整执行模式

## 验收清单

- [x] 修复 `--sandbox full-access` → `danger-full-access`
  Test: manual:bash -c "grep -c 'danger-full-access' packages/engine/runners/codex/runner.sh"

- [x] 移除不支持的 `--cwd` 参数，改为 `cd "$PROJECT_ROOT"` 执行
  Test: manual:bash -c "grep -c 'cwd' packages/engine/runners/codex/runner.sh | grep -q '^0$' && echo 1 || echo 0"

- [x] 添加 `CODEX_HOME` 环境变量支持（默认 `~/.codex`）
  Test: manual:bash -c "grep -c 'CODEX_HOME' packages/engine/runners/codex/runner.sh"

- [x] 第一次调用发送完整 /dev 工作流 prompt（含 task-id、分支名、项目路径、完整步骤指令）
  Test: manual:bash -c "grep -c 'build_comprehensive_prompt\|comprehensive' packages/engine/runners/codex/runner.sh"

- [x] 后续重试发送带完整上下文的恢复 prompt（含当前状态和未完成原因）
  Test: manual:bash -c "grep -c 'resume\|恢复\|current_reason' packages/engine/runners/codex/runner.sh"

- [x] 有测试文件 `packages/engine/tests/codex-runner.test.ts`
  Test: manual:bash -c "ls packages/engine/tests/codex-runner.test.ts && echo 1"

- [x] 测试覆盖 dry-run 模式（不实际调用 codex-bin）
  Test: manual:bash -c "grep -c 'dry.run\|DRY_RUN' packages/engine/tests/codex-runner.test.ts"
