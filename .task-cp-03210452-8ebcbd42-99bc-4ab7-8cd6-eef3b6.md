# Task Card: fix(config): 验证 #1286 三处改动已恢复

## PRD

### 背景
PR #1286 提交后 merge 过程中覆盖了三处关键改动。本任务验证这三处改动已通过 #1294 和 #1296 恢复：
1. devloop-check.sh：merge 失败 return 2（blocked）而非 return 1（error）
2. devloop-check.sh：`_check_codex_review` 加 15 分钟超时降级
3. 01-spec.md / 02-code.md：派发 review 时写入 `registered_at` 时间戳

### 验证结果
- ✅ merge return 2：`devloop-check.sh:448` 已有 `return 2`（status: blocked）
- ✅ 15 分钟超时：`devloop-check.sh:126-148` 已有超时降级逻辑（elapsed_secs > 900）
- ✅ registered_at：`01-spec.md:260` + `02-code.md:201` 均已写入
- ✅ stop-dev.sh fallback：`stop-dev.sh:871` 已修复为 exit 2 + registered_at 超时
- ✅ #1294 + #1296 已在 main 中，无需额外代码修改

## 成功标准

- [x] [BEHAVIOR] devloop-check.sh 合并失败使用 return 2
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('return 2'))process.exit(1);console.log('ok')"
- [x] [BEHAVIOR] devloop-check.sh 有 15 分钟超时逻辑（elapsed_secs）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('elapsed_secs'))process.exit(1);console.log('ok')"
- [x] [BEHAVIOR] 01-spec.md 派发时写入 registered_at
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('registered_at'))process.exit(1);console.log('ok')"

## DoD

- [x] [BEHAVIOR] devloop-check.sh 合并失败返回 return 2（status: blocked）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('return 2'))process.exit(1);console.log('ok')"
- [x] [BEHAVIOR] devloop-check.sh _check_codex_review 有 15 分钟超时降级
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('elapsed_secs'))process.exit(1);console.log('ok')"
- [x] [BEHAVIOR] 01-spec.md 派发 spec_review 时写入 registered_at 时间戳
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('registered_at'))process.exit(1);console.log('ok')"
