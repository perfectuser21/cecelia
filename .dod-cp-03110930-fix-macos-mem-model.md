---
id: dod-fix-macos-mem-model
version: 1.0.0
created: 2026-03-11
updated: 2026-03-11
changelog:
  - 1.0.0: 初始版本
---

## DoD - macOS 内存管理模型全面重构

### 验收清单

- [x] getMacOSMemoryPressure() 新函数已实现
  Test: manual:bash -c "grep -c 'getMacOSMemoryPressure' packages/brain/src/platform-utils.js"

- [x] getMacOSMemoryPressure() 使用 sysctl vm.memory_pressure，返回 0/1/2/3，失败返回 -1
  Test: manual:bash -c "grep -c 'sysctl vm.memory_pressure' packages/brain/src/platform-utils.js"

- [x] getAvailableMemoryMB() 在 macOS 上改用 used_ratio=(active+wired)/total 而非 free+inactive
  Test: manual:bash -c "grep -c 'used_ratio' packages/brain/src/platform-utils.js"

- [x] checkServerResources() 在 Darwin 上使用 getMacOSMemoryPressure() 作为 rawMemPressure 主信号
  Test: manual:bash -c "grep -c 'getMacOSMemoryPressure' packages/brain/src/executor.js"

- [x] planner.js dedup SQL 加入 'quarantined' 排除状态
  Test: manual:bash -c "grep -c 'quarantined' packages/brain/src/planner.js"

- [x] IS_DARWIN 从 platform-utils 导入并用于 executor.js
  Test: manual:bash -c "grep -c 'IS_DARWIN' packages/brain/src/executor.js"

- [x] metrics 输出包含 mem_pressure_signal 字段（用于调试）
  Test: manual:bash -c "grep -c 'mem_pressure_signal' packages/brain/src/executor.js"

## 成功标准

1. macOS 上 vm.memory_pressure=0 时，Brain effectiveSlots > 0
2. planner.js 不再为已 quarantined 的任务重建新任务
3. getAvailableMemoryMB() 不再依赖 free+inactive（已改为 used_ratio）
4. getMacOSMemoryPressure() 导出可被测试调用
