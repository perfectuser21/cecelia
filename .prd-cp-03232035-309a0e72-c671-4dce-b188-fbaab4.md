# PRD: OKR 写操作迁移 PR9 — goals/projects 写路径切换到新 OKR 表

## 背景

PR1-PR8 完成了所有 SELECT 读操作的迁移，但 INSERT/UPDATE 写操作仍然写旧表（goals/projects）。

## 目标

将以下文件的写操作从旧表迁移到新 OKR 表：

- actions.js — INSERT INTO goals/projects → 新表
- planner.js — INSERT INTO goals/projects → 新表
- self-drive.js — UPDATE goals/projects → 新表
- decision-executor.js — INSERT/UPDATE goals → 新表
- chat-action-dispatcher.js — INSERT INTO goals → 新表
- intent.js — INSERT INTO projects → 新表
- project-activator.js — UPDATE projects → 新表
- progress-reviewer.js — UPDATE projects → 新表
- routes/execution.js — UPDATE projects → 新表

## 成功标准

- 上述所有文件不再有 INSERT INTO goals/projects 或 UPDATE goals/projects 的 SQL
- 新建 OKR/Initiative 时数据写入新 OKR 表
- 所有单元测试通过
- Brain 功能正常（创建/更新 goals 走新表）
