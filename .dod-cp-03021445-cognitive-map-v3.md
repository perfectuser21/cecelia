---
id: dod-cp-03021445-cognitive-map-v3
version: 1.0.0
created: 2026-03-02
updated: 2026-03-02
changelog:
  - 1.0.0: 初始版本
---

# DoD：认知地图 v3 — 全脑意识架构可视化

## 验收清单

- [ ] **brain-manifest.js 创建**
  Test: manual:node -e "import('./packages/brain/src/brain-manifest.js').then(m => { const b = m.BRAIN_MANIFEST; console.assert(b.blocks.length === 5, 'need 5 blocks'); console.assert(b.blockConnections.some(c => c.type === 'feedback'), 'need feedback arcs'); console.log('OK'); })"
  - BRAIN_MANIFEST 包含 5 个块（interface/perception/core/action/evolution）
  - blockConnections 包含2条 type='feedback' 的反馈弧
  - 每个块有 nodeIds 数组，映射到 cognitive-map 的已知节点

- [ ] **GET /api/brain/manifest 端点**
  Test: manual:bash -c "curl -s http://localhost:5221/api/brain/manifest | python3 -c \"import json,sys; d=json.load(sys.stdin); print(len(d['blocks']),' blocks'); print(d['version'])\""
  - 返回 JSON { version, blocks, blockConnections }
  - blocks 数组有5个元素，每个含 id/label/color/nodeIds/modules
  - blockConnections 有6条连接，包含2条 type='feedback'

- [ ] **SuperBrain.tsx Level 1 概览视图**
  Test: manual:bash -c "cd apps/dashboard && npm run build 2>&1 | tail -5"
  - 默认显示5个块（外界接口/感知层/意识核心/行动层/自我演化）
  - 4个主块在顶行（左→右），自我演化在底部
  - 两条反馈弧可见（行动→自我演化，自我演化→感知）
  - 每个块显示 active/idle/dormant 模块数

- [ ] **SuperBrain.tsx Level 2 详情视图**
  Test: manual:bash -c "cd apps/dashboard && npm run build 2>&1 | tail -5"
  - 点击块可切换到现有节点流图（5列布局）
  - 节点按所属块着色（color-coded by block）
  - 有返回概览的按钮

- [ ] **自动扫描：manifest 新增模块自动出现**
  Test: manual:bash -c "grep -c 'self_report' packages/brain/src/brain-manifest.js && echo 'self_report registered'"
  - brain-manifest.js 中 evolution 块包含 self_report 模块
  - 前端从 /api/brain/manifest 拉取，无需改前端代码

- [ ] **所有现有测试通过**
  Test: manual:npm run test --workspace packages/brain -- --run
