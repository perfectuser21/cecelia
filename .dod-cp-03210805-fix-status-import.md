# DoD: 修复 Brain status.js 缺失导入

## 验收条件

- [x] status.js 导入包含 IDEMPOTENCY_TTL
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/status.js','utf8');if(!c.includes('IDEMPOTENCY_TTL'))process.exit(1)"
- [x] status.js 导入包含 ALLOWED_ACTIONS
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/status.js','utf8');if(!c.includes('ALLOWED_ACTIONS'))process.exit(1)"
- [x] [BEHAVIOR] status.js import 行同时包含 IDEMPOTENCY_TTL 和 ALLOWED_ACTIONS 从 shared.js 导入
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/status.js','utf8');const m=c.match(/import\s*\{[^}]*\}\s*from\s*'\.\/shared\.js'/);if(!m||!m[0].includes('IDEMPOTENCY_TTL')||!m[0].includes('ALLOWED_ACTIONS'))process.exit(1)"

## 影响范围
- `packages/brain/src/routes/status.js` — 添加 2 个缺失导入
