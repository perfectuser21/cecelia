# DoD: 修复 Brain API 图片扫描 — 改为前缀匹配

- [x] [PRESERVE] GET /:id/output 返回结构不变（含 image_urls 数组和 images 兼容字段）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes('image_urls'))process.exit(1);if(!c.includes(\"type: 'cover'\"))process.exit(1);if(!c.includes(\"type: 'card'\"))process.exit(1);console.log('PRESERVE OK')"

- [x] [ARTIFACT] 删除原有精确匹配循环（existsSync 逐个检查），改为 readdirSync + filter 前缀扫描
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes('readdirSync(IMAGES_DIR)'))process.exit(1);if(c.includes('padStart(2,'))process.exit(1);console.log('ARTIFACT OK')"

- [x] [BEHAVIOR] 封面文件名含 -cover. 时被正确识别为 type: cover
  Test: manual:node -e "const files=['dankoe-cover.png','dankoe-01-failures.png'];const cover=files.find(f=>f.includes('-cover.'));if(cover!=='dankoe-cover.png')process.exit(1);console.log('cover OK')"

- [x] [BEHAVIOR] 不含 -cover. 的文件被识别为 type: card，按文件名排序分配 index
  Test: manual:node -e "const files=['dankoe-02-success.png','dankoe-01-failures.png'].sort();if(files[0]!=='dankoe-01-failures.png')process.exit(1);console.log('card sort OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes('readdirSync'))process.exit(1);console.log('gate OK')"
