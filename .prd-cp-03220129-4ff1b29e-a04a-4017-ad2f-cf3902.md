# PRD: 重构 actions.createTask（圈复杂度 23 → 10）

## 背景

代码复杂度扫描发现 `packages/brain/src/actions.js` 中的 `createTask` 函数（第43行）圈复杂度为 23，超过阈值 10。

## 目标

通过提取子函数 `buildCommonParams` 和 `buildInsertStatement`，将 `createTask` 圈复杂度降至 10 以下，保持外部接口不变。

## 验收标准

- `createTask` 函数提取两个子函数降低复杂度
- 现有测试全部通过（npm test）
- 公共接口（入参/返回值）与重构前完全一致
