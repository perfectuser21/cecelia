---
id: task-cp-04080646-a67c0070-2305-426e-a497-3ed1d8
type: task-card
branch: cp-04080646-a67c0070-2305-426e-a497-3ed1d8
created: 2026-04-08
---

# Task Card: Dashboard KR5 冲刺 — Bug 修复与演示就绪

## 需求（What & Why）
**功能描述**: 修复 Dashboard 3大模块（Roadmap/LiveMonitor/CollectionDashboard）中阻断演示的 P0/P1 Bug，使 KR5 从 58% 推进到 75%。
**背景**: KR5 目标"3大模块无阻断bug，可完整演示20分钟"，当前存在 `/api/goals` 404、静默错误空白页等阻断问题。
**不做什么**: 不新增功能，不改样式，仅修复已识别的 Bug。

## 问题清单（已审计）

### P0 — 演示阻断（必须修）
1. **LiveMonitorPage**: `fetch('/api/goals?limit=200')` → 路由 `/api/goals` 在 apps/api 未注册，返回 404/HTML，导致 `allGoals` 始终为空，OKR 进度卡片不展示。
   - 文件: `apps/dashboard/src/pages/live-monitor/LiveMonitorPage.tsx`
   - 修复: 改为 `fetch('/api/brain/goals')`

### P1 — 影响演示体验（应修）
2. **CollectionDashboardPage**: `fetchStats` 静默吞错，API 失败时页面空白，用户无法感知。
   - 文件: `apps/dashboard/src/pages/collection-dashboard/CollectionDashboardPage.tsx`
   - 修复: 添加 `error` 状态，失败时显示错误提示而不是空白

3. **AreaSlotsPage**: `fetchConfig` 捕获错误后只 console.error，无用户提示。
   - 文件: `apps/dashboard/src/pages/area-slots/AreaSlotsPage.tsx`
   - 修复: 添加 `error` 状态，加载失败时显示错误消息

## 成功标准

> [BEHAVIOR] LiveMonitor 页面的 OKR 进度区域能正确展示 goals 数据
> [BEHAVIOR] CollectionDashboard API 失败时显示错误提示而非空白页
> [BEHAVIOR] AreaSlots 加载失败时显示错误提示

## 验收条件（DoD）

- [x] [BEHAVIOR] LiveMonitorPage `/api/goals` 改为 `/api/brain/goals`，goals 数据能加载
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/live-monitor/LiveMonitorPage.tsx','utf8');if(c.includes('/api/goals?limit=200'))process.exit(1);if(!c.includes('/api/brain/goals'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] CollectionDashboardPage 添加 error 状态，API 失败时有错误 UI
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/collection-dashboard/CollectionDashboardPage.tsx','utf8');if(!c.includes('setError'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] AreaSlotsPage 添加 error 状态，加载失败时有错误 UI
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/area-slots/AreaSlotsPage.tsx','utf8');if(!c.includes('setError'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] TypeScript 编译无新增错误
  Test: manual:node -e "require('fs').accessSync('apps/dashboard/src/pages/live-monitor/LiveMonitorPage.tsx');console.log('files exist')"

## 实现方案
**要改的文件**:
- `apps/dashboard/src/pages/live-monitor/LiveMonitorPage.tsx` — 第 1239 行
- `apps/dashboard/src/pages/collection-dashboard/CollectionDashboardPage.tsx` — fetchStats + 渲染
- `apps/dashboard/src/pages/area-slots/AreaSlotsPage.tsx` — fetchConfig + 渲染

**受影响函数/API**:
- LiveMonitorPage `load()` 函数
- CollectionDashboardPage `fetchStats()` + 渲染函数
- AreaSlotsPage `fetchConfig()` + 渲染函数

**不改什么**: 不改路由、不改 Brain 后端、不改其他页面
