# DoD: 修复 initiative_plan 任务 skill 字段缺失

## 验收清单

### 1. 代码修改

- [ ] `packages/brain/src/decomposition-checker.js:97` - INSERT 语句添加 `skill` 字段，值为 `'/decomp'`
  - Test: 读取修改后的代码，确认 INSERT 包含 `skill`
- [ ] `packages/brain/src/routes.js:3418` - execution-callback 中的 INSERT 语句添加 `skill` 字段
  - Test: 读取修改后的代码，确认两处创建 initiative_plan 任务的地方都设置了 `skill`

### 2. 单元测试（如果有）

- [ ] 运行 `npm test` 验证没有破坏现有测试
  - Test: `npm test` 返回 0

### 3. 手动验证

- [ ] 创建测试 Initiative，触发 initiative_plan 任务创建
- [ ] 查询数据库确认 `skill='/decomp'`
  ```sql
  SELECT id, title, task_type, skill FROM tasks WHERE task_type='initiative_plan' ORDER BY created_at DESC LIMIT 1;
  ```
  - Test: skill 列不为 null，值为 '/decomp'

### 4. Pre-flight check 验证

- [ ] 下一个 tick 时 initiative_plan 任务能成功 dispatch（不被 pre-flight check 拦截）
  - Test: 查看 Brain 日志，initiative_plan 任务被成功派发

### 5. CI 通过

- [ ] Brain CI 全部通过
  - Test: GitHub Actions 绿色

### 6. 文档更新（可选）

- [ ] 如果有相关文档，更新说明 initiative_plan 任务必须有 `skill='/decomp'`
