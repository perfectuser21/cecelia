# DoD: 修复 initiative_plan 任务 skill 字段缺失

## 验收清单

### 1. 代码修改

- [x] `packages/brain/src/decomposition-checker.js:97` - INSERT 语句添加 `skill` 字段，值为 `'/decomp'`
  - Test: manual:grep -A 3 "INSERT INTO tasks.*initiative_plan" packages/brain/src/decomposition-checker.js | grep "skill"
- [x] `packages/brain/src/routes.js:3418` - execution-callback 中的 INSERT 语句添加 `skill` 字段
  - Test: manual:grep -A 3 "INSERT INTO tasks.*initiative_plan" packages/brain/src/routes.js | grep "skill"

### 2. 单元测试

- [x] 运行 `npm test` 验证没有破坏现有测试
  - Test: manual:cd packages/brain && npm test 2>&1 | grep "Test Files"

### 3. 手动验证

- [ ] 创建测试 Initiative，触发 initiative_plan 任务创建
  - Test: manual:curl -s http://localhost:5221/api/brain/tasks?task_type=initiative_plan | jq '.[0] | {id, title, skill}'
- [ ] 查询数据库确认 `skill='/decomp'`
  - Test: manual:docker exec -e PGPASSWORD=cecelia2024 cecelia-postgres psql -U cecelia -d cecelia -c "SELECT id, title, task_type, skill FROM tasks WHERE task_type='initiative_plan' ORDER BY created_at DESC LIMIT 1;"

### 4. Pre-flight check 验证

- [ ] 下一个 tick 时 initiative_plan 任务能成功 dispatch（不被 pre-flight check 拦截）
  - Test: manual:curl -s http://localhost:5221/api/brain/status/full | jq '.dispatch_stats.events[-5:]'

### 5. CI 通过

- [x] Brain CI 全部通过
  - Test: contract:RCI_BRAIN_CI_REQUIRED

### 6. 文档更新（可选）

- [ ] 如果有相关文档，更新说明 initiative_plan 任务必须有 `skill='/decomp'`
  - Test: manual:grep -r "initiative_plan.*skill" docs/
