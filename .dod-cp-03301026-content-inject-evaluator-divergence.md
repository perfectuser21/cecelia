# DoD: 内容注入 + Evaluator divergence 下限

- [x] [PRESERVE] 02-code.md 现有 Generator subagent 调用结构（2.2 节）保持完整，仅在 prompt 注释和伪码处新增内容注入说明
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('Generator subagent'))process.exit(1);console.log('OK')"

- [x] [PRESERVE] spec-review SKILL.md 现有输出格式（JSON 含 independent_test_plans + negotiation_result）保持不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('independent_test_plans')||!c.includes('negotiation_result'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] 02-code.md Generator subagent prompt 模板中，Sprint Contract 节说明内容必须是实际文件内容（非路径）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('直接嵌入'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] 02-code.md 主 agent 伪码中，spec_review subagent spawn 说明包含 Task Card 内容直接嵌入 prompt
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');const idx=c.indexOf('code_review_gate');if(idx<0)process.exit(1);const section=c.slice(0,idx);if(!section.includes('Task Card') && !section.includes('task card'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] spec-review SKILL.md 包含 divergence_count = 0 时 exit 2 / 要求重跑的逻辑描述
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('divergence_count') || !c.includes('exit 2'))process.exit(1);console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "const fs=require('fs');try{fs.accessSync('packages/engine/skills/dev/steps/02-code.md');fs.accessSync('packages/workflows/skills/spec-review/SKILL.md')}catch(e){process.exit(1)}"
