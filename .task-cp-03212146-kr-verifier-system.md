# Task Card: KR Verification 闭环

## 需求（What & Why）
**功能描述**: KR 进度不可伪造验证系统。每个 KR 绑定 SQL 查询自动采集指标，progress 只由公式算，堵死直写。
**不做什么**: 不改 OKR 结构，不改 decomp

## 成功标准
1. [ARTIFACT] kr_verifiers 表和 migration
2. [BEHAVIOR] kr-verifier.js 自动采集指标并计算 progress
3. [BEHAVIOR] decision-executor.js 的 update_okr_progress 改为只触发验证
4. [GATE] 所有现有测试通过

## 验收条件（DoD）
- [x] [ARTIFACT] kr_verifiers migration 存在
  Test: manual:node -e "require('fs').accessSync('packages/brain/migrations/170_kr_verifiers.sql')"
- [x] [BEHAVIOR] kr-verifier.js 存在且包含 runAllVerifiers
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/kr-verifier.js','utf8');if(!c.includes('runAllVerifiers'))process.exit(1)"
- [x] [BEHAVIOR] decision-executor.js 的 update_okr_progress 不直接写 progress
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/decision-executor.js','utf8');if(c.includes('UPDATE goals SET progress = $1'))process.exit(1)"
- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"
