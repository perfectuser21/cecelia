---
id: dod-projects-compare-page
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD: feat(dashboard): 实现 /projects/compare 项目并排对比页面

## 验收清单

- [x] 新建 `apps/api/features/planning/pages/ProjectCompare.tsx`，包含多选下拉和对比卡片
  Test: manual:bash -c "grep -c 'ProjectCompare' apps/api/features/planning/pages/ProjectCompare.tsx"

- [x] `apps/api/features/planning/index.ts` 注册 `/projects/compare` 路由和 `ProjectCompare` 组件
  Test: manual:bash -c "grep -c 'projects/compare' apps/api/features/planning/index.ts"

- [x] `ProjectsDashboard.tsx` 添加「对比项目」入口按钮，点击跳转 `/projects/compare`
  Test: manual:bash -c "grep -c 'compare' apps/api/features/planning/pages/ProjectsDashboard.tsx"

- [x] TypeScript 类型检查通过（无新增 type error）
  Test: manual:bash -c "cd apps/api && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c 'error TS' || true"
