# DoD: Brain 意图扩展 + CTO Review 两阶段前置审查系统

## 验收条件

- [x] [ARTIFACT] task-router.js 新增 `intent_expand` 和 `cto_review` 到 VALID_TASK_TYPES
  Test: manual:node -e "import('./packages/brain/src/task-router.js').then(m => { const types = m.VALID_TASK_TYPES; if (!types.includes('intent_expand') || !types.includes('cto_review')) process.exit(1); console.log('PASS'); })"

- [x] [ARTIFACT] task-router.js 的 SKILL_WHITELIST 映射正确
  Test: manual:node -e "import('./packages/brain/src/task-router.js').then(m => { const sw = m.SKILL_WHITELIST; if (sw['intent_expand'] !== '/intent-expand' || sw['cto_review'] !== '/cto-review') process.exit(1); console.log('PASS'); })"

- [x] [ARTIFACT] task-router.js 的 LOCATION_MAP 映射正确（intent_expand→us，cto_review→xian）
  Test: manual:node -e "import('./packages/brain/src/task-router.js').then(m => { const lm = m.LOCATION_MAP; if (lm['intent_expand'] !== 'us' || lm['cto_review'] !== 'xian') process.exit(1); console.log('PASS'); })"

- [x] [ARTIFACT] model-registry.js AGENTS 包含 intent_expand 和 cto_review 条目
  Test: manual:node -e "import('./packages/brain/src/model-registry.js').then(m => { const ids = m.AGENTS.map(a => a.id); if (!ids.includes('intent_expand') || !ids.includes('cto_review')) process.exit(1); console.log('PASS'); })"

- [x] [BEHAVIOR] Brain API POST /tasks/:id/request-intent-expand 路由注册完成
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/tasks.js','utf8');if(!c.includes('request-intent-expand'))process.exit(1);console.log('PASS')"

- [x] [BEHAVIOR] Brain API POST /tasks/:id/request-cto-review 路由注册完成
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/tasks.js','utf8');if(!c.includes('request-cto-review'))process.exit(1);console.log('PASS')"

- [x] [ARTIFACT] DEFINITION.md task_types 表格包含 intent_expand 和 cto_review
  Test: manual:node -e "const c=require('fs').readFileSync('DEFINITION.md','utf8');if(!c.includes('intent_expand')||!c.includes('cto_review'))process.exit(1);console.log('PASS')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -10"
