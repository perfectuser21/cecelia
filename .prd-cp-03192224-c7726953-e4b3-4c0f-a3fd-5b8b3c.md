# PRD: Learning 表同步修复 — distill-learnings.js 写入 learnings 表

## 背景
distill-learnings.js 写 knowledge 表，learning-retriever.js 读 learnings 表，两张表不通。100+ 篇高质量 Learning 永远不会被注入到下次 /dev 任务。

## 功能描述
修改 `packages/brain/scripts/distill-learnings.js`，在写入 knowledge 表的同时，也写入 learnings 表（title, learning_type, content, metadata, source_branch）。

字段映射：name→title, learning_rule→best_practice（learning_type）, sub_area→metadata.source_file，filename（去.md）→source_branch。

## 成功标准
- [ ] distill-learnings.js 同时写入两张表（knowledge + learnings）
- [ ] --dry-run 模式显示 learnings 写入预览
- [ ] 幂等保证：重复运行不报错
- [ ] CI 全部通过
