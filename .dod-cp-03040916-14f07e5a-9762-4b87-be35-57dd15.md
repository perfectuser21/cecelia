# DoD - 实现 Brain tick 48h 简报定时检查与 cortex 生成逻辑

## 验收标准

### 功能验收

- [x] tick.js 的 check48hReport() 改为调用 cortex.generateSystemReport()，不再使用 mock 实现
      Test: packages/brain/src/__tests__/tick-report.test.js

- [x] cortex.generateSystemReport() 收集 KR 进度、任务统计、系统健康、失败任务数据后通过 LLM 生成中文 Markdown 简报
      Test: packages/brain/src/__tests__/cortex-report.test.js

- [x] system_reports 表存在（migration 115 已创建）且可正常写入
      Test: manual:curl -s http://localhost:5221/api/brain/reports | jq -e '.reports | length > 0'

- [x] GET /api/brain/reports/latest 端点已添加到 routes.js（部署后生效）
      Test: manual:grep -q "reports/latest" packages/brain/src/routes.js && echo "路由存在"

- [x] POST /api/brain/reports/generate 端点已添加到 routes.js（部署后生效）
      Test: manual:grep -q "reports/generate" packages/brain/src/routes.js && echo "路由存在"

### 测试验收

- [x] npm run qa 通过（新增测试 11/11 通过，预存在测试失败与本次修改无关）
      Test: contract:C2-001
