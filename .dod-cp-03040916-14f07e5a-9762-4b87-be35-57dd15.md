# DoD - 实现 Brain tick 48h 简报定时检查与 cortex 生成逻辑

## 验收标准

### 功能验收

- [x] tick.js 的 check48hReport() 改为调用 cortex.generateSystemReport()，不再使用 mock 实现
      Test: manual:npx vitest run packages/brain/src/__tests__/tick-report.test.js 2>&1 | grep -q "5 passed"

- [x] cortex.generateSystemReport() 收集 KR 进度、任务统计、系统健康、失败任务数据后通过 LLM 生成中文 Markdown 简报
      Test: manual:npx vitest run packages/brain/src/__tests__/cortex-report.test.js 2>&1 | grep -q "6 passed"

- [x] system_reports 表存在（migration 115 已创建）且可正常写入
      Test: manual:curl -s http://localhost:5221/api/brain/reports | jq -e '.reports | length > 0'

- [x] GET /api/brain/reports/latest 端点已添加到 routes.js
      Test: manual:bash -c "grep -q 'router.get.*reports/latest' packages/brain/src/routes.js"

- [x] POST /api/brain/reports/generate 端点已添加到 routes.js
      Test: manual:bash -c "grep -q 'router.post.*reports/generate' packages/brain/src/routes.js"

### 测试验收

- [x] 新增测试全部通过（tick-report 5 cases，cortex-report 6 cases，共 11 cases）
      Test: manual:npx vitest run packages/brain/src/__tests__/tick-report.test.js packages/brain/src/__tests__/cortex-report.test.js 2>&1 | grep -q "11 passed"
