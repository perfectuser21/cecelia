---
id: dod-fix-engine-macos-compat
version: 1.2.0
created: 2026-03-14
updated: 2026-03-14
---

# DoD: Fix Engine macOS Compatibility

## 验收标准

- [x] branch-protect.sh 行 268 `$TODAY，` → `${TODAY}，` 修复 bash 3.2 UTF-8 unbound variable
  Test: manual:bash -c "grep -c '{TODAY}' packages/engine/hooks/branch-protect.sh"

- [x] runner.sh `$task_id）` → `${task_id}）` 修复同类 bash 3.2 bug
  Test: manual:bash -c "grep -c '{task_id}' packages/engine/runners/codex/runner.sh"

- [x] hook-contracts.test.ts `git checkout master` → 动态检测 main/master
  Test: manual:bash -c "grep -c 'branch.*list' packages/engine/tests/integration/hook-contracts.test.ts"

- [x] cleanup-prd-dod.test.ts 兼容 macOS 默认 main 分支
  Test: manual:bash -c "grep -c 'rev-parse.*abbrev-ref' packages/engine/tests/scripts/cleanup-prd-dod.test.ts"

- [x] pr-gate-phase1.test.ts 期望值修正（toBe(2) → toBeGreaterThan(0)）
  Test: manual:bash -c "grep -c 'toBeGreaterThan' packages/engine/tests/hooks/pr-gate-phase1.test.ts"

- [x] macOS 本地 npm test 零失败（54 test files，613 passed，0 failed）
  Test: manual:node -e "const d=require('./packages/engine/package.json');console.log(d.version);process.exit(0)"

- [x] 版本号 bump 到 12.66.1（6 个文件同步）
  Test: manual:node -e "const v=require('./packages/engine/package.json').version;process.exit(v==='12.66.1'?0:1)"
