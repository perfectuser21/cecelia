---
id: task-cp-0414115133-chinese-punct-scan
type: task-card
branch: cp-0414115133-chinese-punct-scan
created: 2026-04-14
---

# Task Card: 全代码库中文全角标点炸弹扫描 + 修复 + CI 防线

## 需求（What & Why）
**功能描述**: 新建 check-chinese-punctuation-bombs.sh 扫描脚本 + 修复代码库已知的 28 处 bomb + CI 接入
**背景**: `$var，`（变量紧跟中文标点）在 `set -u` 语境下触发 unbound variable，PR #2332 三次踩同类坑
**不做什么**: 不改 bash 引号语法、不扫描 markdown、不改测试 fixture 里故意的样例

## 成功标准
- 扫描脚本能检测 `$var[中文标点]` 模式并按 `file:line:content` 输出
- 现有代码库 28 处命中全部修复（`$var` → `${var}`）
- 测试覆盖：命中 / 无命中 / 纯中文注释 3 个场景
- CI 工作流接入扫描步骤，失败即挂

## 验收条件（DoD）

- [ ] [BEHAVIOR] 扫描脚本存在且可执行
  Test: manual:node -e "require('fs').accessSync('packages/engine/ci/scripts/check-chinese-punctuation-bombs.sh', require('fs').constants.X_OK);console.log('EXEC OK')"

- [ ] [BEHAVIOR] 扫描脚本对命中文件 exit 1
  Test: manual:bash -c "D=$(mktemp -d);printf '#!/bin/bash\necho \$x，\n' > $D/bad.sh;! bash packages/engine/ci/scripts/check-chinese-punctuation-bombs.sh $D/bad.sh >/dev/null 2>&1 && echo EXIT1_OK;rm -rf $D"

- [ ] [BEHAVIOR] 扫描脚本对无命中 exit 0
  Test: manual:bash -c "D=$(mktemp -d);printf '#!/bin/bash\necho \${x}，\n' > $D/good.sh;bash packages/engine/ci/scripts/check-chinese-punctuation-bombs.sh $D/good.sh >/dev/null 2>&1 && echo EXIT0_OK;rm -rf $D"

- [ ] [BEHAVIOR] 对当前代码库运行脚本应 exit 0（所有 28 处修复完成）
  Test: manual:bash packages/engine/ci/scripts/check-chinese-punctuation-bombs.sh >/dev/null 2>&1 && echo CLEAN_OK

- [ ] [BEHAVIOR] 测试文件存在且覆盖 3 场景
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/tests/scripts/chinese-punct-check.test.ts','utf8');['命中','无命中','纯中文注释'].forEach(k=>{if(!c.includes(k))process.exit(1)});console.log('TEST_OK')"

- [ ] [BEHAVIOR] CI workflow 接入扫描步骤
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(!c.includes('check-chinese-punctuation-bombs.sh'))process.exit(1);console.log('CI_OK')"

- [ ] [ARTIFACT] 修复后 scan-hit 数降为 0
  Test: manual:bash -c "N=$(bash packages/engine/ci/scripts/check-chinese-punctuation-bombs.sh 2>/dev/null | wc -l | tr -d ' ');if [ \"$N\" -ne 0 ];then exit 1;fi;echo ZERO_HITS_OK"

## 实现方案
**要改的文件**:
- `packages/engine/ci/scripts/check-chinese-punctuation-bombs.sh`（新建）
- `packages/engine/tests/scripts/chinese-punct-check.test.ts`（新建）
- `.github/workflows/ci.yml`（加扫描步骤）
- 28 处 shell 文件修复：
  - packages/engine/ci/scripts/check-dynamic-tests.sh
  - packages/engine/hooks/stop-dev.sh
  - packages/engine/lib/devloop-check.sh
  - packages/engine/runners/codex/playwright-runner.sh
  - packages/engine/skills/dev/scripts/{worktree-manage,fetch-task-prd,check,cleanup}.sh
  - scripts/{setup-staging-env,update-rna-kr-progress,pre-push-check}.sh

**不改什么**: 不动 vitest 配置、不碰测试 fixture、不动 brain 包
