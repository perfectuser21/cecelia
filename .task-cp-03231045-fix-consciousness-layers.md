---
id: task-cp-03231045-fix-consciousness-layers
type: task-card
branch: cp-03231045-fix-consciousness-layers
created: 2026-03-23
brain_task_id: f1faa96c-0475-4c56-8431-b7441da03c5a
---

# Task Card: 修复意识层断链（emotion + narrative + model fallback）

## 需求（What & Why）

**功能描述**: 修复 Cecelia 嘴巴说"废话"的根本原因——意识层数据断链。

**根因诊断**:
1. thalamus 用 gpt-5.4-mini（OpenAI），OpenAI 返回空内容 → runEmotionLayer LLM 调用失败 → working_memory.emotion_state 13小时未更新
2. 所有3个 Anthropic 账号返回 429 → narrative 失败 → 日记不写了
3. 嘴巴没有 fallback → 触发 tier3 降级 → 模板回复

**不做什么**: 不换整体 model profile，不修改账号配置。

## 成功标准

1. [BEHAVIOR] emotion-layer.js thalamus 失败时 fallback 到 mouth 模型
2. [BEHAVIOR] narrative 写入频率提升至每6小时1次
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [ ] [BEHAVIOR] emotion-layer.js 包含 fallback 到 mouth 的逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/emotion-layer.js','utf8');if(!c.includes('fallback'))process.exit(1);console.log('ok')"

- [ ] [BEHAVIOR] narrative 写入间隔从24h改为6h
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cognitive-core.js','utf8');if(!c.includes('6 * 60 * 60 * 1000'))process.exit(1);console.log('ok')"

- [ ] [GATE] 两个核心文件均已修改
  Test: manual:node -e "const fs=require('fs');['packages/brain/src/emotion-layer.js','packages/brain/src/cognitive-core.js'].forEach(f=>{if(!fs.existsSync(f))process.exit(1)});console.log('ok')"
