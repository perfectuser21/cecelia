---
id: task-cp-03171841-c3b2c8d4-e444-4c55-bbb7-41b9f6
type: task-card
branch: cp-03171841-c3b2c8d4-e444-4c55-bbb7-41b9f6
created: 2026-03-17
---

# Task Card: 修复 migration 023 索引创建不幂等

## 需求（What & Why）
**功能描述**: 修复 `packages/brain/migrations/023_add_run_events_observability_v1.1.sql` 中所有 `CREATE INDEX` 语句，添加 `IF NOT EXISTS` 使其幂等。
**背景**: smoke.test.js 在 CI 中因 migration 023 创建 `idx_run_events_task_id` 等索引时没有 `IF NOT EXISTS`，当 CI DB 中已存在这些索引时，migrate 报错退出，导致 server 启动失败。
**不做什么**: 不改动表结构、视图、函数；不改变索引定义本身（只加 IF NOT EXISTS）。

## 成功标准

1. [ARTIFACT] `023_add_run_events_observability_v1.1.sql` 中所有 `CREATE INDEX` 和 `CREATE UNIQUE INDEX` 均含 `IF NOT EXISTS`
2. [BEHAVIOR] 在已存在索引的 DB 重跑 migration，不报错退出
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] migration 023 所有 CREATE INDEX 加了 IF NOT EXISTS
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/023_add_run_events_observability_v1.1.sql','utf8');const bad=c.split('\n').filter(l=>l.startsWith('CREATE INDEX')||l.startsWith('CREATE UNIQUE INDEX')).filter(l=>!l.includes('IF NOT EXISTS'));if(bad.length>0){console.error('缺少 IF NOT EXISTS:',bad);process.exit(1)}console.log('OK')"

- [x] [BEHAVIOR] migration 023 重复执行不报 ERROR（幂等性验证）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/023_add_run_events_observability_v1.1.sql','utf8');if(!c.includes('IF NOT EXISTS')){process.exit(1)}console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"

## 实现方案（Step 2 探索后填写）
**要改的文件**: `packages/brain/migrations/023_add_run_events_observability_v1.1.sql`
**Scope 锚定**: 仅修改 CREATE INDEX / CREATE UNIQUE INDEX 行，在 INDEX 后加 IF NOT EXISTS
