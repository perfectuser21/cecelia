# DoD: Step 3 Push 后自动注册 3 个审查任务

## 验收标准

- [ ] [BEHAVIOR] 03-prci.md 在 push（8.5 节）和 PR 创建（8.6 节）之间包含向 Brain API 注册 cto_review 任务的 bash 代码块
  - Test: `node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-prci.md','utf8');const pushIdx=c.indexOf('8.5 推送');const prIdx=c.indexOf('8.6 创建 PR');const regIdx=c.indexOf('cto_review');if(pushIdx<0||prIdx<0||regIdx<0||regIdx<pushIdx||regIdx>prIdx)process.exit(1)"`

- [ ] [BEHAVIOR] 03-prci.md 包含向 Brain API 注册 code_quality_review 任务的逻辑，并将 code_quality_task_id 写入 .dev-mode 文件
  - Test: `node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-prci.md','utf8');if(!c.includes('code_quality_review')||!c.includes('code_quality_task_id'))process.exit(1)"`

- [ ] [BEHAVIOR] 03-prci.md 包含向 Brain API 注册 prd_coverage_audit 任务的逻辑，并将 prd_audit_task_id 写入 .dev-mode 文件
  - Test: `node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-prci.md','utf8');if(!c.includes('prd_coverage_audit')||!c.includes('prd_audit_task_id'))process.exit(1)"`

- [ ] [BEHAVIOR] Brain 不可用时打印警告但不阻塞流程（curl 超时 5 秒）
  - Test: `node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-prci.md','utf8');if(!c.includes('max-time 5')||!c.includes('Brain 不可用'))process.exit(1)"`

- [ ] [GATE] 存在测试文件验证 03-prci.md 包含审查注册逻辑
  - Test: `node -e "const c=require('fs').readFileSync('packages/engine/tests/skills/step03-review-register.test.ts','utf8');if(!c.includes('cto_review')&&!c.includes('code_quality'))process.exit(1)"`
