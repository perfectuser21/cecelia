# DoD: Step 3 Push 后自动注册 3 个审查任务

## 验收标准

- [x] [BEHAVIOR] 03-prci.md 在 push 和 PR 创建之间包含向 Brain API 注册 cto_review 任务的代码块
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-prci.md','utf8');if(c.indexOf('cto_review')<0)process.exit(1)"

- [x] [BEHAVIOR] 03-prci.md 包含向 Brain API 注册 code_quality_review 任务的逻辑并将 task_id 写入 dev-mode
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-prci.md','utf8');if(c.indexOf('code_quality_review')<0||c.indexOf('code_quality_task_id')<0)process.exit(1)"

- [x] [BEHAVIOR] 03-prci.md 包含向 Brain API 注册 prd_coverage_audit 任务的逻辑并将 task_id 写入 dev-mode
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-prci.md','utf8');if(c.indexOf('prd_coverage_audit')<0||c.indexOf('prd_audit_task_id')<0)process.exit(1)"

- [x] [BEHAVIOR] Brain 不可用时打印警告但不阻塞流程
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-prci.md','utf8');if(c.indexOf('max-time 5')<0)process.exit(1)"

- [x] [GATE] 存在测试文件验证 03-prci.md 包含审查注册逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/tests/skills/step03-review-register.test.ts','utf8');if(c.indexOf('cto_review')<0)process.exit(1)"
# retrigger
