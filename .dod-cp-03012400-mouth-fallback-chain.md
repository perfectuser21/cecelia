---
id: dod-cp-03012400-mouth-fallback-chain
version: 1.0.0
created: 2026-03-01
updated: 2026-03-01
changelog:
  - 1.0.0: 初始版本
---

# DoD: 嘴巴降级链

- [x] 1. account-usage.js 新增 selectBestAccountForHaiku()（只过滤 five_hour_pct，不过滤 sonnet 配额）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/account-usage.js','utf8');process.exit(c.includes('selectBestAccountForHaiku')?0:1)"

- [x] 2. callLLM() 读取 agentConfig.fallbacks，主模型失败时依次尝试降级
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/llm-caller.js','utf8');process.exit(c.includes('fallbacks')&&c.includes('attempted_fallback')?0:1)"

- [x] 3. model_profiles DB 中 mouth 配置更新，包含 fallbacks 数组
  Test: manual:docker exec cecelia-postgres psql -U cecelia -d cecelia -t -c "SELECT config->'mouth'->'fallbacks' FROM model_profiles WHERE is_active=true;" | grep -q "haiku"

- [x] 4. 版本 bump v1.143.0 → v1.143.1
  Test: manual:node -e "const p=require('./packages/brain/package.json');process.exit(p.version==='1.143.1'?0:1)"
