# DoD: Brain 设置 CECELIA_BUDGET_SLOTS=7

## 验收标准

- [x] [BEHAVIOR] docker-compose.yml brain 服务 environment 块包含 `CECELIA_BUDGET_SLOTS=7`
- [x] [BEHAVIOR] `GET /api/brain/capacity` 在 Brain 重启后返回 `budget_cap=7, max_seats=7, physical_capacity=10`
- [x] `CECELIA_MAX_SEATS=10` 未被修改（硬上限保持不变）

## 测试

- manual: `curl -s http://localhost:5221/api/brain/capacity | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));if(d.budget_cap!==7||d.max_seats!==7||d.physical_capacity!==10)process.exit(1)"`
