# DoD: Brain 设置 CECELIA_BUDGET_SLOTS=7

## 验收标准

- [x] [BEHAVIOR] docker-compose.yml brain 服务 environment 块包含 `CECELIA_BUDGET_SLOTS=7`
  Test: manual:node -e "const c=require('fs').readFileSync('docker-compose.yml','utf8');if(!c.includes('CECELIA_BUDGET_SLOTS=7'))process.exit(1)"
- [x] [BEHAVIOR] executor.js 读取 CECELIA_BUDGET_SLOTS 环境变量并限制 getEffectiveMaxSeats() 返回值
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('CECELIA_BUDGET_SLOTS')||!c.includes('getEffectiveMaxSeats'))process.exit(1)"
- [x] `CECELIA_MAX_SEATS=10` 未被修改（硬上限保持不变）
  Test: manual:node -e "const c=require('fs').readFileSync('docker-compose.yml','utf8');if(!c.includes('CECELIA_MAX_SEATS=10'))process.exit(1)"
