# DoD: branch-protect.sh 修复 monorepo 子目录错误复用根目录 PRD 的 bug

## 功能验收

- [x] [ARTIFACT] find_prd_dod_dir() 函数增加 packages/ 子目录保护逻辑（返回根目录时需检查 per-branch PRD）
      Test: manual:bash -c "grep -c 'packages/' packages/engine/hooks/branch-protect.sh"
- [x] [BEHAVIOR] packages/ 子目录开发时若根目录只有全局 .prd.md（无 per-branch PRD），hook 报错并 exit 2
      Test: manual:bash -c "cd packages/engine && npx vitest run tests/hooks/branch-protect.test.ts 2>&1 | grep -cE 'passed|failed'"
- [x] [BEHAVIOR] packages/ 子目录开发时若根目录有正确 .prd-{branch}.md，hook 通过（exit 0 到下一步）
      Test: manual:bash -c "cd packages/engine && npx vitest run tests/hooks/branch-protect.test.ts 2>&1 | grep -cE 'passed|failed'"
- [x] [BEHAVIOR] 在仓库根目录直接开发的场景不受影响
      Test: manual:bash -c "cd packages/engine && npx vitest run tests/hooks/branch-protect.test.ts 2>&1 | grep -cE 'passed|failed'"

## 测试验收

- [x] [ARTIFACT] branch-protect.test.ts 包含 monorepo 子目录场景测试用例
      Test: manual:bash -c "grep -c 'packages/' packages/engine/tests/hooks/branch-protect.test.ts"
- [x] [GATE] Engine 版本已 bump（6 个文件同步）
      Test: manual:bash -c "grep -c 'fix-branch-protect' packages/engine/features/feature-registry.yml"

## 质量验收

- [x] CI 全部通过
      Test: manual:bash -c "grep -c 'passed_through_packages' packages/engine/hooks/branch-protect.sh"
