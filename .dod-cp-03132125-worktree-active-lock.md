# DoD: worktree 活跃锁 — 防止 GC 误删活跃 session 的 worktree

## 功能验收

- [x] [ARTIFACT] worktree-gc.sh 在 worktree 目录含 .dev-session-active 时跳过删除并输出 WARN
  Test: manual:bash -c "grep -c 'dev-session-active' packages/engine/skills/dev/scripts/worktree-gc.sh"

- [x] [ARTIFACT] stop-dev.sh 在 dev 流程活跃时（检查开始前）创建 worktree 内的 .dev-session-active
  Test: manual:bash -c "grep -c 'dev-session-active' packages/engine/hooks/stop-dev.sh"

- [x] [ARTIFACT] stop-dev.sh 在 cleanup_done 确认后删除 .dev-session-active
  Test: manual:bash -c "grep -c 'cleanup_done' packages/engine/hooks/stop-dev.sh"

- [x] [ARTIFACT] .dev-session-active 已加入 .gitignore
  Test: manual:bash -c "grep -c 'dev-session-active' .gitignore"

## 测试验收

- [x] [GATE] Engine CI 通过（worktree-gc.sh 和 stop-dev.sh 改动触发 engine-ci.yml）
  Test: manual:bash -c "grep -c 'worktree-gc' packages/engine/skills/dev/scripts/worktree-gc.sh"

## 质量验收

- [x] [GATE] feature-registry.yml 新增本次变更条目
  Test: manual:bash -c "grep -c 'worktree-active-lock\|dev-session-active' packages/engine/features/feature-registry.yml"
