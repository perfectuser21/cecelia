# PRD: 修复 devloop-check.sh + stop hooks runtime bug

**Task ID**: 84c967fd-43b9-4c6b-88a8-9ceb720d7f94
**Branch**: cp-03221547-fix-devloop-stop-hooks
**Priority**: P0
**Created**: 2026-03-22

## 背景

/dev pipeline runtime 层存在 4 个 P0/P1 问题，导致工作流在特定场景下卡死或搜索到错误的状态文件。

## 需求

修复以下 bug：

1. devloop-check.sh:305 cleanup 失败后无 retry → PR 合并后工作流卡死
2. stop-dev.sh `_collect_search_dirs` 在 worktree 内调用时搜索顺序不对 → 主仓库 .dev-lock 找不到
3. stop-dev.sh:38 垃圾注释（无头模式不再旁路）没有对应代码
4. stop-dev.sh:106 TTY 匹配条件判断需要改为检查非空
5. stop-dev.sh:193 LEGACY 兼容代码无删除日期，永远不会被清理
6. devloop-check.sh changelog「删除Codex」表述不准确 → 应改为「改为Agent subagent同步调用」

## 不做什么

- 不改 step 文件（01-spec.md / 02-code.md 等）
- 不改 verify-step.sh
- 不加新功能

## 成功标准

1. [ARTIFACT] devloop-check.sh cleanup 失败后有 return 2 触发重试
2. [ARTIFACT] stop-dev.sh `_collect_search_dirs` 先搜主仓库再搜 worktree
3. [ARTIFACT] 垃圾注释已删除
4. [ARTIFACT] LEGACY 代码注释加删除日期
5. [ARTIFACT] changelog 更新为「改为Agent subagent同步调用」
6. [GATE] CI 全部通过
