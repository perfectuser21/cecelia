---
id: task-cp-03241529-okr-pr10-old-table-cleanup
type: task-card
branch: cp-03241529-okr-pr10-old-table-cleanup
created: 2026-03-24
---

# Task Card: OKR PR10 — 旧表引用收尾迁移

## 需求（What & Why）

**功能描述**：清除 Brain 业务代码中对旧表（goals/projects）的业务读写残留，删除无必要的 fallback 写入分支。

**背景**：PR1-PR9 完成了主路径迁移，但多个文件仍有旧表读写。

**前置条件**：migration 179 已完成全量数据迁移，key_results/objectives/okr_projects/okr_initiatives 数据完整。

**不做什么**：
- 不删旧表（DB migration 单独 PR）
- 不改 notion-full-sync.js（有意保留 goals 为 Notion 枢纽）
- 不改 planner.js:1194（已有双查设计，intentional compatibility）
- 不改 routes/execution.js:2645 的 repo_path 联表查询（复杂，单独 PR）
- 不改 memory-service.js / project-compare.js（复杂 UNION，单独 PR）

**domain 字段策略**：okr_initiatives 无独立 domain 列，用 `metadata->>'domain'`（目前均为 NULL，fallback 'coding'，行为等同旧表）

## 成功标准

1. [BEHAVIOR] `planner.js` 的 `generateArchitectureDesignTask` 从 `okr_initiatives JOIN okr_scopes` 读取 initiative，domain 用 `metadata->>'domain'`
2. [BEHAVIOR] `decomposition-checker.js` 的 `checkReadyKRInitiatives` 从 `okr_initiatives` 读取，tasks 用 `okr_initiative_id` 关联
3. [ARTIFACT] 目标 9 个文件中无旧表（goals/projects）读写活跃代码
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [BEHAVIOR] planner.js generateArchitectureDesignTask 查 okr_initiatives JOIN okr_scopes
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/planner.js','utf8');const lines=c.split('\n');if(!lines.some(l=>l.includes('okr_initiatives i')&&!l.trim().startsWith('//')))process.exit(1);if(!lines.some(l=>l.includes('scope_id')&&l.includes('okr_scopes')&&!l.trim().startsWith('//')))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] decomposition-checker.js checkReadyKRInitiatives 查 okr_initiatives，用 okr_initiative_id 关联 tasks
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/decomposition-checker.js','utf8');const lines=c.split('\n');if(!lines.some(l=>l.includes('okr_initiative_id')&&!l.trim().startsWith('//')))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] planner.js generateArchitectureDesignTask 无 FROM projects 活跃代码
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/planner.js','utf8');const fn=c.slice(c.indexOf('async function generateArchitectureDesignTask'));const body=fn.slice(0,fn.indexOf('\nasync function',10)||fn.length);const bad=body.split('\n').filter(l=>!l.trim().startsWith('//') && !l.trim().startsWith('*') && l.includes('FROM projects i'));if(bad.length>0){console.error('FAIL',bad);process.exit(1);}console.log('ok')"

- [x] [ARTIFACT] decomposition-checker.js checkReadyKRInitiatives 无 FROM projects 活跃代码
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/decomposition-checker.js','utf8');const bad=c.split('\n').filter(l=>!l.trim().startsWith('//') && !l.trim().startsWith('*') && l.includes('FROM projects') && l.includes('type'));if(bad.length>0){console.error('FAIL',bad);process.exit(1);}console.log('ok')"

- [x] [ARTIFACT] self-drive.js 无 UPDATE goals/UPDATE projects 活跃代码
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/self-drive.js','utf8');const bad=c.split('\n').filter(l=>!l.trim().startsWith('//') && !l.trim().startsWith('*') && (l.includes('UPDATE goals') || l.includes('UPDATE projects')));if(bad.length>0){console.error('FAIL',bad);process.exit(1);}console.log('ok')"

- [x] [ARTIFACT] project-activator.js 无 UPDATE projects 活跃代码
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/project-activator.js','utf8');const bad=c.split('\n').filter(l=>!l.trim().startsWith('//') && !l.trim().startsWith('*') && l.includes('UPDATE projects'));if(bad.length>0){console.error('FAIL',bad);process.exit(1);}console.log('ok')"

- [x] [ARTIFACT] actions.js 无 INSERT INTO goals 活跃代码
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/actions.js','utf8');const bad=c.split('\n').filter(l=>!l.trim().startsWith('//') && !l.trim().startsWith('*') && l.includes('INSERT INTO goals'));if(bad.length>0){console.error('FAIL',bad);process.exit(1);}console.log('ok')"

- [x] [ARTIFACT] progress-reviewer.js 无 UPDATE projects 活跃代码
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/progress-reviewer.js','utf8');const bad=c.split('\n').filter(l=>!l.trim().startsWith('//') && !l.trim().startsWith('*') && l.includes('UPDATE projects'));if(bad.length>0){console.error('FAIL',bad);process.exit(1);}console.log('ok')"

- [x] [ARTIFACT] decision-executor.js 无 INSERT INTO goals / UPDATE goals 活跃代码
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/decision-executor.js','utf8');const bad=c.split('\n').filter(l=>!l.trim().startsWith('//') && !l.trim().startsWith('*') && (l.includes('INSERT INTO goals') || l.includes('UPDATE goals')));if(bad.length>0){console.error('FAIL',bad);process.exit(1);}console.log('ok')"

- [x] [ARTIFACT] routes/execution.js:1508 无 UPDATE projects fallback（okr_initiatives 主查已有）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/routes/execution.js','utf8');const lines=c.split('\n');const bad=lines.filter((l,i)=>i>1500&&i<1520&&!l.trim().startsWith('//') && l.includes('UPDATE projects'));if(bad.length>0){console.error('FAIL',bad);process.exit(1);}console.log('ok')"

- [x] [ARTIFACT] query-okr-status.mjs 无 FROM goals 活跃代码
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/query-okr-status.mjs','utf8');const bad=c.split('\n').filter(l=>!l.trim().startsWith('//') && !l.trim().startsWith('*') && l.includes('FROM goals'));if(bad.length>0){console.error('FAIL',bad);process.exit(1);}console.log('ok')"

- [x] [ARTIFACT] decision-executor.js 中 INSERT INTO goals 替换为 INSERT INTO key_results
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/decision-executor.js','utf8');if(!c.includes('INSERT INTO key_results'))process.exit(1);console.log('ok')"

- [x] [GATE] notion-full-sync.js 保持不变（Notion 枢纽）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/notion-full-sync.js','utf8');if(!c.includes('INSERT INTO goals'))process.exit(1);console.log('ok: notion-full-sync.js still uses goals as Notion hub')"

## 实现方案

[已完成 - 见代码改动]
