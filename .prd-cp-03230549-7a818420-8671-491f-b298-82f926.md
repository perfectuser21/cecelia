# PRD: OKR体系统一 PR3（续）——剩余 routes 层切新表

## 背景

PR #1462 已完成 task-goals.js + task-projects.js 迁移到新 OKR 表。
本 PR 继续完成 packages/brain/src/routes/ 下剩余 OKR 相关查询的切换。

## 功能描述

将以下文件中 OKR 相关查询从旧 goals/projects 表切换到新 OKR 表
（objectives/key_results/okr_projects/okr_scopes/okr_initiatives）：

1. `status.js` — GET /goals, POST /goals/:id/approve, GET /goals/:id/okr-context
2. `intent-match.js` — goals/projects 搜索
3. `shared.js` — getActiveExecutionPaths()
4. `task-router-diagnose.js` — KR诊断 + Initiative查询
5. `ops.js` — project stats + KR lookup + planner/initiatives-without-tasks
6. `notion-sync.js` — 表计数更新
7. `execution.js` — 进度 rollup、strategy_session KR 写入、decomp trigger
8. `actions.js` — KR上下文查询、redecomp trigger

## 成功标准

## 成功标准

- [ ] 任务完成
