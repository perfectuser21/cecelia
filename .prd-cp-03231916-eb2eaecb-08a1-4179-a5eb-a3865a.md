# PRD: 重构 notion-memory-sync.contactFieldsToNotionProps（CC 27 → 10）

## 背景
代码复杂度扫描发现 packages/brain/src/notion-memory-sync.js 中的 contactFieldsToNotionProps 函数圈复杂度为 27，超出阈值 10。

## 功能描述
重构 contactFieldsToNotionProps 函数，将圈复杂度从 27 降至 10 以下，同时保持对外接口和行为完全不变。

## 成功标准
- contactFieldsToNotionProps 函数主体 if/else 分支数 ≤ 4
- 提取独立的 applyKnownField 或等效查找表函数（KNOWN_FIELD_HANDLERS）
- 对外接口不变（签名：contactFieldsToNotionProps(fields, sourceName, updatedAt)）
- npm test 全部通过
