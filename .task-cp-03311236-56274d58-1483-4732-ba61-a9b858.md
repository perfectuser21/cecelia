# Task Card: KR 进度跟踪系统修复 — 度量可观测性

## PRD 摘要

**任务来源**：Brain 自动调度（SelfDrive）
**类型**：fix
**影响范围**：`packages/brain/migrations/`（1 文件）

### 根本原因诊断

KR 进度全 0% 的根因是 **4 个 KR verifier 的 SQL 查询指向了错误或空的数据表**，而非数据流断链：

| KR | 旧查询表 | 问题 | 新查询表/逻辑 |
|----|---------|------|--------------|
| 自动发布 | `publish_results`（0行） | 表存在但从未写入 | `content_publish_jobs`（2行） |
| 内容生成 | `content_topics`（0行） | 表存在但从未写入 | `content_publish_jobs`（2行） |
| 系统稳定 | `self_healing_log`（0行） | 表存在但从未写入 | `alertness_metrics` 稳定率（96%） |
| 算力全开 | `tasks` in-progress snapshot（2%） | 公式错误度量瞬态 | `tasks` 30天完成率（79%） |

**管家闭环 40% 正确**的原因：`daily_logs` 有真实数据（12/30 条）。
**数据闭环 0%、Dashboard可用 0%** 属实，不修改。

### 修复范围

创建 `migrations/208_fix_kr_verifier_queries.sql`：
- 更新 4 个 KR verifier 的查询 SQL 指向正确数据表
- 重置 `last_checked = NULL` 使 verifier 在下次 tick（5秒内）立即重算

---

## DoD

- [x] **[ARTIFACT]** `packages/brain/migrations/208_fix_kr_verifier_queries.sql` 存在
  Test: manual:node -e "require('fs').accessSync('packages/brain/migrations/208_fix_kr_verifier_queries.sql');console.log('ok')"

- [x] **[BEHAVIOR]** 迁移文件包含 4 个 UPDATE 语句，覆盖 `content_publish_jobs`、`alertness_metrics`、30天完成率、`last_checked = NULL`
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/208_fix_kr_verifier_queries.sql','utf8');const keys=['content_publish_jobs','alertness_metrics','last_checked = NULL','30 days'];keys.forEach(k=>{if(!c.includes(k))throw new Error('missing: '+k)});console.log('ok')"

- [x] **[GATE]** 迁移文件编号为 208（延续序列），SQL 语法完整（含 4 个 UPDATE，每个有 WHERE kr_id 过滤）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/208_fix_kr_verifier_queries.sql','utf8');const updates=c.match(/UPDATE kr_verifiers/g)||[];if(updates.length<4)throw new Error('需要 4 个 UPDATE，实际: '+updates.length);const whereKr=c.match(/WHERE kr_id/g)||[];if(whereKr.length<4)throw new Error('需要 4 个 WHERE kr_id，实际: '+whereKr.length);console.log('ok')"

---

## 成功标准

- `系统稳定` KR: progress ≥ 90%（基于 alertness_metrics 稳定率 96%）
- `算力全开` KR: progress ≥ 70%（基于 30 天任务完成率 79%）
- `自动发布` / `内容生成` KR: 从 `content_publish_jobs` 读取真实数据
- 无误报：数据闭环 / Dashboard可用 仍显示 0%（属实，不修改）
