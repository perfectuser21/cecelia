# PRD: 写 write-current-state.sh + 接入 /dev Stage 4

Brain 任务 ID: 34db1ec7-ff45-44ae-84b0-aff27e7d024c

## 问题

`.agent-knowledge/CURRENT_STATE.md` 存在，但生成脚本 `write-current-state.sh` 不存在。文件头声明 `source: write-current-state.sh` 但脚本从未创建。`/dev` 完成后没有任何机制自动更新这个文件。

## 要做的事

1. 创建 `scripts/write-current-state.sh`：
   - 查询 Brain API（health、alertness、in_progress tasks、probes/status）
   - 生成带时间戳的 `.agent-knowledge/CURRENT_STATE.md`
   - Brain 离线时静默跳过（exit 0）

2. 接入 `/dev` Stage 4（packages/engine/skills/dev/steps/04-ship.md）：
   - 在 cleanup.sh 调用后添加 `bash scripts/write-current-state.sh`
   - 这样每次 PR 合并后自动刷新状态快照

## 成功标准

- `scripts/write-current-state.sh` 创建，chmod +x
- 执行后生成正确格式的 CURRENT_STATE.md
- Brain 离线时 exit 0 不报错
- Stage 4 文件包含对脚本的调用
