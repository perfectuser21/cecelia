---
id: task-harness-planner-fix
type: task-card
branch: cp-04041226-harness-planner-fix
created: 2026-04-04
---

# Task Card: Harness v2.0 完整流程接通 — Planner→Sprint 断链修复

## 需求（What & Why）
**功能描述**: 修复 Harness v2.0 中 Planner→Generator→Evaluator 断链的 3 个 Bug，使全流程贯通
**背景**: /architect Mode 2 已能注册 dev+harness_mode 任务，但 Brain 断链有 3 处错误导致多 Sprint 流程无法正常运转
**不做什么**: 不改 cecelia-run / executor.js / Evaluator skill

## 成功标准
> [BEHAVIOR] dev+harness_mode 任务完成后，生成的 sprint_evaluate.payload.dev_task_id = 该 dev task 自身 ID
> [BEHAVIOR] sprint_evaluate PASS 后，下一个 blocked dev+harness_mode task 被 unblock 到 queued（而非创建新 sprint_generate）
> [ARTIFACT] /architect SKILL.md Phase 5 示例包含 sprint_dir 字段

## 验收条件（DoD）

- [x] [BEHAVIOR] dev+harness_mode task 触发 sprint_evaluate 时，payload.dev_task_id = task_id 自身
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(!c.includes('harnessPayload.dev_task_id || task_id'))process.exit(1)"

- [x] [BEHAVIOR] sprint_evaluate PASS 后不再创建 sprint_generate，而是 unblock 下一个 dev+harness_mode task
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(c.includes('sprint_generate 创建 for next sprint'))process.exit(1);if(!c.includes('unblocked next dev'))process.exit(1)"

- [x] [ARTIFACT] /architect SKILL.md Phase 5 包含 sprint_dir 示例
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/architect/SKILL.md','utf8');if(!c.includes('sprint_dir'))process.exit(1)"

## 实现方案
**要改的文件**:
- `packages/brain/src/routes/execution.js` (两处)
- `packages/workflows/skills/architect/SKILL.md` (Phase 5 curl 示例)

**受影响函数**: execution-callback harness 断链 (5c-harness section)
**不改什么**: executor.js, cecelia-run, sprint-evaluator SKILL.md
