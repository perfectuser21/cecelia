---
id: task-cp-03201000-publisher-brain-integration
type: task-card
branch: cp-03201000-publisher-brain-integration
created: 2026-03-20
brain-task-id: dacf972a-7092-4008-a640-8a0ccca6ed14
---

# Task Card: feat(publisher): 多平台发布调度器 v1

## 需求（What & Why）

**功能描述**: 将 Brain 与 zenithjoy 现有的 8 个平台发布脚本打通，实现 Brain 可以派发 `content_publish` 类型任务，自动路由到对应平台发布者（Claude Code skill），并通过 `content_publish_jobs` 表追踪发布任务状态。

**背景**: zenithjoy 已有 8 个平台发布脚本（抖音/快手/小红书/头条/微博/视频号/知乎/公众号），多个已测试通过，但与 Cecelia Brain 完全孤立——没有任务队列、没有状态追踪、无法实现每日自动发布。本 PR 是「发布流程集成（端到端）」Initiative 的第一块基石。

**不做什么**:
- 不实际运行发布脚本（那是 publisher skill 的职责）
- 不修改 zenithjoy 的发布脚本
- 不实现发布任务的自动触发调度（只做 Brain 的接入层）
- 不修改 `publish_results` 表（已存在，保留）

## 成功标准

1. [ARTIFACT] `content_publish_jobs` 表存在于 PostgreSQL（migration 163 执行成功）
2. [BEHAVIOR] `task_type=content_publish` + `payload.platform=douyin` 在 `getSkillForTaskType` 中返回 `/douyin-publisher`
3. [BEHAVIOR] `GET /api/brain/publish-jobs` 返回 HTTP 200 和 `{ success: true, jobs: [] }`
4. [BEHAVIOR] `POST /api/brain/publish-jobs` 创建 job 记录并返回 201
5. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] `content_publish_jobs` 表存在，含正确字段
  Test: manual:node -e "const {Pool}=require('pg');const p=new Pool({connectionString:'postgresql://cecelia:cecelia@localhost:5432/cecelia'});p.query('SELECT column_name FROM information_schema.columns WHERE table_name=\\'content_publish_jobs\\' ORDER BY column_name').then(r=>{const cols=r.rows.map(x=>x.column_name);const required=['id','platform','content_type','payload','status','task_id','error_message','created_at'];const missing=required.filter(c=>!cols.includes(c));if(missing.length)process.exit(1);console.log('OK');p.end()}).catch(()=>process.exit(1))"

- [x] [BEHAVIOR] `getSkillForTaskType('content_publish', {platform:'douyin'})` 返回 `/douyin-publisher`
  Test: manual:node -e "import('/Users/administrator/perfect21/cecelia/packages/brain/src/executor.js').then(m=>{const r=m.getSkillForTaskType?.('content_publish',{platform:'douyin'});if(r!=='/douyin-publisher'){console.error('got:',r);process.exit(1)}console.log('OK')})"

- [x] [BEHAVIOR] `GET /api/brain/publish-jobs` 返回 200
  Test: manual:curl -sf http://localhost:5221/api/brain/publish-jobs | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));if(!d.success||!Array.isArray(d.jobs))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] `POST /api/brain/publish-jobs` 创建 job 并返回 201
  Test: manual:node -e "const r=require('https');const http=require('http');const b=JSON.stringify({platform:'douyin',content_type:'video',payload:{title:'test'}});const req=http.request({hostname:'localhost',port:5221,path:'/api/brain/publish-jobs',method:'POST',headers:{'Content-Type':'application/json','Content-Length':b.length}},res=>{let d='';res.on('data',c=>d+=c);res.on('end',()=>{const j=JSON.parse(d);if(res.statusCode!==201||!j.id)process.exit(1);console.log('OK')})});req.write(b);req.end()"

- [x] [BEHAVIOR] `POST /api/brain/publish-jobs/retry/:id` 对失败 job 重置为 pending
  Test: manual:node -e "const http=require('http');const b=JSON.stringify({platform:'douyin',content_type:'video',payload:{},status:'failed'});const req=http.request({hostname:'localhost',port:5221,path:'/api/brain/publish-jobs',method:'POST',headers:{'Content-Type':'application/json','Content-Length':b.length}},res=>{let d='';res.on('data',c=>d+=c);res.on('end',()=>{const {id}=JSON.parse(d);const r2=http.request({hostname:'localhost',port:5221,path:'/api/brain/publish-jobs/retry/'+id,method:'POST',headers:{'Content-Length':0}},r=>{let d2='';r.on('data',c=>d2+=c);r.on('end',()=>{const j=JSON.parse(d2);if(!j.success)process.exit(1);console.log('OK')})});r2.end()})});req.write(b);req.end()"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd /Users/administrator/perfect21/cecelia && npm test --workspace=packages/brain 2>&1 | tail -5"

## 实现方案

**要改的文件**:
1. `packages/brain/migrations/163_add_content_publish_jobs.sql`（新建）
2. `packages/brain/src/executor.js`（`getSkillForTaskType` 加 content_publish 平台路由）
3. `packages/brain/src/routes/publish-jobs.js`（新建 GET/POST/retry 端点）
4. `packages/brain/src/routes.js`（import + 注册 publishJobsRouter）

**Scope 锚定**: 仅 Brain 包，zenithjoy 不改动。`publish_results` 表保留不动。
