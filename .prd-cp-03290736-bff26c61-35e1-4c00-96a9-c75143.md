# PRD: Playwright Evaluator 层 — 端到端行为验证

## 背景
在 /dev Stage 3（CI 之后）增加 Playwright Evaluator subagent，对照 DoD [BEHAVIOR] 条目逐条验证运行中的 Brain API 和 Dashboard。Evaluator 读取 Sprint Contract，动态生成 Playwright 测试脚本，逐条执行，失败则返回详细反馈给主 agent 修复。

## 成功标准
- `packages/engine/scripts/devgate/playwright-evaluator.cjs` 存在且完整性验证通过（包含核心函数且文件非空）
- 脚本解析 [BEHAVIOR] 条目并执行 `Test:` 命令，逐条 pass/fail 报告
- playwright-evaluator.cjs 包含 Brain API `/api/brain/health` 基线检查
- playwright-evaluator.cjs --dry-run 输出 DRY RUN 报告（不需要 Brain 运行）
- `packages/engine/skills/dev/steps/03-integrate.md` 包含 Playwright Evaluator 调用段落
- `regression-contract.yaml` 包含 PE-001 RCI 条目，PRESERVE check-new-files-need-rci 检查逻辑不变

## 任务 ID
bff26c61-35e1-4c00-96a9-c75143497b4d
