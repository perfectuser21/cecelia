# PRD: Playwright Evaluator 层 — 端到端行为验证

## 背景
在 /dev Stage 3（CI 之后）增加 Playwright Evaluator subagent，对照 DoD [BEHAVIOR] 条目逐条验证运行中的 Brain API 和 Dashboard。Evaluator 读取 Sprint Contract，动态生成 Playwright 测试脚本，逐条执行，失败则返回详细反馈给主 agent 修复。

## 成功标准
- `packages/engine/scripts/devgate/playwright-evaluator.cjs` 存在且可执行
- 脚本解析 `.task-cp-*.md` 中的 [BEHAVIOR] 条目，逐条执行 `Test:` 命令
- 始终包含 Brain API `/api/brain/health` 基础端点检测
- 输出逐条 pass/fail 报告，失败时附带命令输出
- `packages/engine/skills/dev/steps/03-integrate.md` 在 3.3 节调用 evaluator
- `regression-contract.yaml` 新增 PE-001 RCI 条目

## 任务 ID
bff26c61-35e1-4c00-96a9-c75143497b4d
