# Task Card: 重构 cecelia-routes 降低复杂度

## 背景
ComplexityScanner 检测到 cecelia-routes.js 两个函数超阈值（阈值 10）：
- inferStepProgress: 复杂度 15
- buildCheckpoints: 复杂度 13

（mapStatus 的 26 是误报，已由 PR #1353 修复）

## 目标
提取辅助函数降低复杂度，保持行为完全不变。

## DoD

- [x] [ARTIFACT] 提取 parseStepIndex(stepStr, total) 辅助函数，inferStepProgress 复杂度 ≤ 9
  Test: manual:node -e "import('./packages/brain/src/task-generators/complexity-scanner.js').then(async m => { const s = new m.default(); const c = require('fs').readFileSync('./packages/brain/src/cecelia-routes.js','utf-8'); const r = s.analyzeComplexity(c); const f = r.find(x => x.name === 'inferStepProgress'); if (!f || f.cyclomatic > 9) { process.exit(1); } console.log('PASS inferStepProgress:', f.cyclomatic); })"

- [x] [ARTIFACT] 提取 getCheckpointStatus(status, stepNum, currentStepIdx) 辅助函数，buildCheckpoints 复杂度 ≤ 9
  Test: manual:node -e "import('./packages/brain/src/task-generators/complexity-scanner.js').then(async m => { const s = new m.default(); const c = require('fs').readFileSync('./packages/brain/src/cecelia-routes.js','utf-8'); const r = s.analyzeComplexity(c); const f = r.find(x => x.name === 'buildCheckpoints'); if (!f || f.cyclomatic > 9) { process.exit(1); } console.log('PASS buildCheckpoints:', f.cyclomatic); })"

- [x] [BEHAVIOR] 重构后函数行为不变：复杂度达标且 cecelia-routes 模块可正常加载解析
  Test: manual:node -e "import('./packages/brain/src/task-generators/complexity-scanner.js').then(async m => { const s = new m.default(); const c = require('fs').readFileSync('./packages/brain/src/cecelia-routes.js','utf-8'); const r = s.analyzeComplexity(c); const inf = r.find(x => x.name === 'inferStepProgress'); const bld = r.find(x => x.name === 'buildCheckpoints'); if (!inf || inf.cyclomatic > 9 || !bld || bld.cyclomatic > 9) { console.error('FAIL', inf && inf.cyclomatic, bld && bld.cyclomatic); process.exit(1); } console.log('PASS: inferStepProgress=' + inf.cyclomatic + ' buildCheckpoints=' + bld.cyclomatic); })"

## 成功标准

- inferStepProgress 圈复杂度降至 9（阈值 10 以下）
- buildCheckpoints 圈复杂度降至 5（阈值 10 以下）
- mapStatus 保持不变（复杂度 2）
- packages/brain 测试全部通过
