# Task Card: 重构 cecelia-routes 降低复杂度

## 背景
mapStatus 误报已由 PR #1353 修复（扫描器边界检测 bug）。
实际仍有两个函数超阈值：
- inferStepProgress: 复杂度 15（阈值 10）
- buildCheckpoints: 复杂度 13（阈值 10）

## 目标
提取辅助函数，将 inferStepProgress 和 buildCheckpoints 复杂度降至 10 以下。

## 验收清单

- [ ] inferStepProgress 圈复杂度 ≤ 9
- [ ] buildCheckpoints 圈复杂度 ≤ 9
- [ ] mapStatus 保持不变（复杂度 2）
- [ ] packages/brain 测试全部通过

## DoD

### 1. inferStepProgress 复杂度 ≤ 9
提取 parseStepIndex/inferProgressForFailed/inferProgressForRunning 辅助函数。
  Test: node -e "const c=require('fs').readFileSync('packages/brain/src/cecelia-routes.js','utf8');const m=c.match(/function inferStepProgress[\s\S]*?\n\}/);if(!m){console.error('FAIL: inferStepProgress not found');process.exit(1)}const branches=(m[0].match(/\bif\b|\bwhile\b|\bfor\b|\bcase\b|\bcatch\b|\&\&|\|\|/g)||[]).length;const cc=branches+1;if(cc>9){console.error('FAIL: inferStepProgress complexity='+cc+' >9');process.exit(1)}console.log('PASS: inferStepProgress complexity='+cc)"

### 2. buildCheckpoints 复杂度 ≤ 9
提取 getCheckpointStatus(status, stepNum, currentStepIdx) 辅助函数。
  Test: node -e "const c=require('fs').readFileSync('packages/brain/src/cecelia-routes.js','utf8');const m=c.match(/function buildCheckpoints[\s\S]*?\n\}/);if(!m){console.error('FAIL: buildCheckpoints not found');process.exit(1)}const branches=(m[0].match(/\bif\b|\bwhile\b|\bfor\b|\bcase\b|\bcatch\b|\&\&|\|\|/g)||[]).length;const cc=branches+1;if(cc>9){console.error('FAIL: buildCheckpoints complexity='+cc+' >9');process.exit(1)}console.log('PASS: buildCheckpoints complexity='+cc)"

### 3. 测试全部通过
  Test: tests: packages/brain

## 成功标准

- inferStepProgress 和 buildCheckpoints 圈复杂度均降至 9 以下
- packages/brain 测试全部通过
