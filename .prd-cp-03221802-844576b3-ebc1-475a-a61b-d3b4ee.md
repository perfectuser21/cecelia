# PRD: 重构 cortex.analyzeDeep（圈复杂度 55 → 10 以下）

## 背景
`packages/brain/src/cortex.js` 中的 `analyzeDeep` 函数圈复杂度为 55，远超阈值 10。需要通过提取子函数降低复杂度，行为保持完全一致。

## 目标
将 `analyzeDeep` 圈复杂度降至 10 以下，不改变任何业务逻辑。

## 成功标准
- analyzeDeep 函数本身圈复杂度 ≤ 10
- npm test 全部通过
- 不引入新的公开 export
