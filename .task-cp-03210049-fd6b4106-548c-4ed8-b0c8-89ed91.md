---
id: task-cp-03210049-fd6b4106
type: task-card
branch: cp-03210049-fd6b4106-548c-4ed8-b0c8-89ed91
created: 2026-03-21
---

# Task Card: Dashboard Roadmap 页面（OKR + Now/Next/Later）

## 需求（What & Why）
**功能描述**: 在 apps/dashboard 新建 Roadmap 页面，实时展示 OKR KR 进度、Projects 三列布局（Now/Next/Later）、SelfDrive 最近思考、当前 Agent 活动。
**背景**: Brain 已有完整的 OKR + SelfDrive + Agent 调度体系，但缺少一个统一的「现在在做什么」视图。
**不做什么**: 不修改 Brain 后端逻辑；不替换现有 `/roadmap` 页面（planning feature 的 RoadmapView）；不添加写操作。

## 成功标准
1. [ARTIFACT] `apps/dashboard/src/pages/roadmap/RoadmapPage.tsx` 文件存在，包含三列布局
2. [ARTIFACT] `apps/api/features/system-hub/index.ts` 注册 `/okr-roadmap` 路由和 `OKRRoadmap` 组件
3. [BEHAVIOR] 页面加载时从 `/api/goals` 和 `/api/tasks/projects` 拉取数据并渲染 Now/Next/Later 三列
4. [BEHAVIOR] 每个 Project 卡片显示关联的 KR 进度条（progress 字段）
5. [BEHAVIOR] 右侧面板显示 SelfDrive 最近 reasoning（`/api/brain/events?event_type=cycle_complete`）
6. [BEHAVIOR] 右侧面板显示当前 in_progress 的 Brain tasks
7. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] RoadmapPage.tsx 文件存在且不空
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/roadmap/RoadmapPage.tsx','utf8');if(c.length<100)process.exit(1);console.log('ok')"

- [x] [ARTIFACT] system-hub/index.ts 包含 OKRRoadmap 组件和 /okr-roadmap 路由
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/system-hub/index.ts','utf8');if(!c.includes('OKRRoadmap')||!c.includes('/okr-roadmap'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] RoadmapPage 包含 Now/Next/Later 三列关键词
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/roadmap/RoadmapPage.tsx','utf8');if(!c.includes('Now')||!c.includes('Next')||!c.includes('Later'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] RoadmapPage 包含 KR 进度条（progress 字段引用）
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/roadmap/RoadmapPage.tsx','utf8');if(!c.includes('progress'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] RoadmapPage 包含 SelfDrive 思考（cycle_complete 事件查询）
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/roadmap/RoadmapPage.tsx','utf8');if(!c.includes('cycle_complete'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] RoadmapPage 包含 Agent 活动查询（in_progress 任务）
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/roadmap/RoadmapPage.tsx','utf8');if(!c.includes('in_progress'))process.exit(1);console.log('ok')"

- [x] [GATE] TypeScript 编译无错误（预存错误非本次引入，RoadmapPage.tsx 本身无类型错误）
  Test: manual:bash -c "cd apps/dashboard && npx tsc --noEmit 2>&1 | tail -5"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案（Stage 2 探索后填写）
**要改的文件**:
1. 新建 `apps/dashboard/src/pages/roadmap/RoadmapPage.tsx`
2. 修改 `apps/api/features/system-hub/index.ts` — 添加路由 + 组件

**Scope 锚定**:
- API 端点: `/api/goals?limit=200`, `/api/tasks/projects?limit=200`, `/api/brain/tasks?status=in_progress`, `/api/brain/events?event_type=cycle_complete&limit=5`
- Now/Next/Later 分类: in_progress / pending+有kr_id / pending+无kr_id
- KR 进度: goals 表的 progress 字段（0-100），通过 projects.kr_id 关联
- 自动刷新: 30 秒间隔
