# DoD - 修复 initiative_plan 任务超时和进程丢失

## 验收标准

### 功能验收

#### 1. 超时时间动态配置
- [x] initiative_plan 任务的超时时间从 60 分钟增加到 120 分钟
      Test: packages/brain/src/__tests__/tick-timeout-config.test.js
- [x] 其他 task_type 仍然使用默认 60 分钟超时
      Test: packages/brain/src/__tests__/tick-timeout-config.test.js
- [x] 超时配置通过环境变量支持（`TASK_TIMEOUT_OVERRIDES`）
      Test: packages/brain/src/__tests__/tick-timeout-config.test.js

#### 2. Orphan 任务重试机制
- [x] Brain 重启时，initiative_plan 类型的 orphan 任务自动重新入队（status=queued）
      Test: packages/brain/src/__tests__/executor-orphan-retry.test.js
- [x] 非幂等任务类型（如 dev）的 orphan 任务仍然标记为 failed
      Test: packages/brain/src/__tests__/executor-orphan-retry.test.js
- [x] 重试次数有上限（最多 2 次重试），超过后标记为 failed
      Test: packages/brain/src/__tests__/executor-orphan-retry.test.js

#### 3. 日志增强
- [x] initiative_plan 任务启动时记录关键参数（Initiative ID、KR ID）
      Test: manual:grep -q "\[initiative_plan\] start:" /tmp/brain.log
- [x] 任务超时时记录详细的超时原因和已执行时间
      Test: manual:grep -q "\[tick\] auto-fail:" /tmp/brain.log
- [x] orphan 检测时记录是否触发重试
      Test: manual:grep -q "\[executor\] orphan retry:" /tmp/brain.log

#### 4. API 查询支持
- [x] GET /api/brain/tasks?status=failed 正确返回所有失败任务
      Test: manual:curl -s http://localhost:5221/api/brain/tasks?status=failed | jq 'length > 0'
- [x] 失败任务的 payload.error_details 包含 type、message、elapsed_minutes 字段
      Test: manual:curl -s http://localhost:5221/api/brain/tasks?status=failed | jq '.[0].payload.error_details | has("type") and has("message")'

### 测试验收
- [x] 所有新增单元测试通过
      Test: packages/brain/src/__tests__/tick-timeout-config.test.js
- [x] npm run qa 通过（整体回归）
      Test: manual:cd packages/brain && npm test
- [x] 现有 initiative_plan 相关测试仍然通过
      Test: manual:cd packages/brain && npm test

### 兼容性验证
- [x] 修改不影响其他 task_type 的超时行为
      Test: packages/brain/src/__tests__/tick-timeout-config.test.js
- [x] 修改不影响正常任务的失败处理流程
      Test: manual:cd packages/brain && npm test

## 回滚计划

如果部署后发现问题：

1. **回滚 commit**：`git revert <commit-hash>`
2. **环境变量清空**：`unset TASK_TIMEOUT_OVERRIDES`
3. **重启 Brain**：`pm2 restart brain`

## 验证步骤（手动测试）

### 验证 1：超时配置生效

```bash
# 1. 启动 Brain（设置环境变量）
export TASK_TIMEOUT_OVERRIDES='{"initiative_plan":120}'
pm2 restart brain

# 2. 查看日志确认配置生效
tail -f /tmp/brain.log | grep -i timeout
# 应该看到：[tick] Timeout config: initiative_plan=120min, default=60min

# 3. 创建 initiative_plan 任务并等待 70 分钟
curl -X POST http://localhost:5221/api/brain/tasks \
  -H "Content-Type: application/json" \
  -d '{
    "title": "测试超时配置",
    "task_type": "initiative_plan",
    "description": "测试任务",
    "priority": "P1",
    "project_id": "test-project-id",
    "goal_id": "test-goal-id"
  }'

# 4. 等待 70 分钟后检查任务状态（应该仍在 in_progress，未超时）
curl -s http://localhost:5221/api/brain/tasks/<task_id> | jq '.status'
# 预期：in_progress（而非 failed）
```

### 验证 2：Orphan 重试机制

```bash
# 1. 创建 initiative_plan 任务并派发
curl -X POST http://localhost:5221/api/brain/tasks/dispatch/<task_id>

# 2. 等待任务开始执行（status=in_progress）
curl -s http://localhost:5221/api/brain/tasks/<task_id> | jq '.status'

# 3. 强制重启 Brain（模拟崩溃）
pm2 restart brain

# 4. 等待 Brain 启动完成，检查任务状态
curl -s http://localhost:5221/api/brain/tasks/<task_id> | jq '.status'
# 预期：queued（被重新入队）

# 5. 检查日志
tail -f /tmp/brain.log | grep -i "orphan retry"
# 应该看到：[executor] orphan retry: task_id=xxx, type=initiative_plan
```

### 验证 3：日志增强

```bash
# 1. 创建并派发 initiative_plan 任务
curl -X POST http://localhost:5221/api/brain/tasks/dispatch/<task_id>

# 2. 实时查看日志
tail -f /tmp/brain.log

# 3. 验证启动日志
# 应该看到：[initiative_plan] start: initiative_id=xxx, kr_id=xxx

# 4. 如果任务超时，验证超时日志
# 应该看到：[initiative_plan] timeout: elapsed=XXm, limit=120m
```

## 依赖/前置条件

- Brain 版本 >= 1.171.0
- Node.js >= 18.0.0
- PostgreSQL 数据库已初始化（包含 tasks 表）
