# DoD: dev-workflow-consistency-fix

## 成功标准

- [x] [ARTIFACT] devloop-check.sh 中不再包含 step_10_learning 或 step_11_cleanup
  Test: manual:bash -c "! grep -q 'step_10_learning\|step_11_cleanup' packages/engine/lib/devloop-check.sh"

- [x] [ARTIFACT] cleanup.sh STEP_PATTERNS 使用新 6 步模型（step_0 到 step_5）
  Test: manual:bash -c "grep -q 'step_0_worktree' packages/engine/skills/dev/scripts/cleanup.sh && grep -q 'step_5_clean' packages/engine/skills/dev/scripts/cleanup.sh"

- [x] [BEHAVIOR] 05-clean.md 完成后写入 cleanup_done: true（终止条件）
  Test: manual:bash -c "grep -q 'cleanup_done: true' packages/engine/skills/dev/steps/05-clean.md"

- [x] [BEHAVIOR] SKILL.md 完成条件包含 Codex 协作流程（cto_review / code_quality / prd_audit）
  Test: manual:bash -c "grep -q 'cto_review' packages/engine/skills/dev/SKILL.md && grep -q 'code_quality' packages/engine/skills/dev/SKILL.md"

- [x] [GATE] 所有测试文件同步更新步骤命名
  Test: manual:bash -c "! grep -q 'step_10_learning\|step_11_cleanup' packages/engine/tests/dev/checklist.test.ts"
