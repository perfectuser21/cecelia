# PRD: 重构 reflection.runReflection（复杂度 12 → ≤ 9）

## 背景
PR #1432 已将 runReflection 圈复杂度从 39 降至 12，仍高于阈值 10。
需进一步提取子函数，将主函数决策点降至 9。

## 目标
提取 `_callLLMForReflection` 和 `_persistInsightAndReset` 两个子函数，
使 `runReflection` 圈复杂度降至 9（阈值 10 以下）。

## 文件
- packages/brain/src/desire/reflection.js

## 成功标准
- runReflection 圈复杂度 ≤ 9
- 所有现有 reflection 测试通过
- CI 全部通过
