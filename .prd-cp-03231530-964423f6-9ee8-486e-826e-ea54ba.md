# PRD: 重构 reflection.runReflection（复杂度 12 → ≤ 9）

## 背景
PR #1432 已将 runReflection 圈复杂度从 39 降至 12，仍高于阈值 10。
需进一步提取子函数，将主函数决策点降至 9。

## 目标
提取 `_callLLMForReflection` 和 `_persistInsightAndReset` 两个子函数，
使 `runReflection` 圈复杂度降至 9（阈值 10 以下）。

## 文件
- packages/brain/src/desire/reflection.js

## 成功标准
- [PRESERVE] `runReflection` 公共接口和返回格式不变
- [ARTIFACT] 提取 `_callLLMForReflection(memories, accumulator)` 子函数
- [ARTIFACT] 提取 `_persistInsightAndReset(pool, insight, accumulator)` 子函数（包含写入洞察 + 重置 accumulator + 各自的错误处理）
- [BEHAVIOR] runReflection 函数圈复杂度降至 10 以下（目标 ≤ 9）
- [GATE] 所有现有测试通过
