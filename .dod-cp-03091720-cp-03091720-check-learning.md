---
id: check-learning-gate-dod
version: 1.0.0
created: 2026-03-09
updated: 2026-03-09
changelog:
  - 1.0.0: 初始版本
---
# DoD: check-learning HARD GATE

## 验收清单

### 实现
- [x] 新建 `packages/engine/scripts/devgate/check-learning.sh` 脚本，无新增内容时 exit 0，缺少"根本原因"或"下次预防"或 checklist 时 exit 1
      Test: manual:bash -n packages/engine/scripts/devgate/check-learning.sh
- [x] 修改 `.github/workflows/devgate.yml` 新增 `check-learning` job，纳入 `ci-passed` 依赖
      Test: manual:node -e "const f=require('fs');const c=f.readFileSync('.github/workflows/devgate.yml','utf8');process.exit(c.includes('check-learning') && c.includes('check-learning.sh') ? 0 : 1)"
- [x] `check-learning` job 的 `needs` 包含在 `ci-passed` 的依赖列表中
      Test: manual:node -e "const f=require('fs');const c=f.readFileSync('.github/workflows/devgate.yml','utf8');process.exit(c.includes('check-learning') ? 0 : 1)"
