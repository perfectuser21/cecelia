---
id: task-cp-03271509-test-step-api
type: task-card
branch: cp-03271509-test-step-api
created: 2026-03-27
---

# Task Card: Brain test-step API

## 需求（What & Why）
**功能描述**：新增 POST /api/brain/content-types/test-step API，让前端工作流编辑器可以测试单个 pipeline 节点。
**不做什么**：不改现有路由逻辑

## 成功标准
1. [ARTIFACT] POST /api/brain/content-types/test-step 路由存在
2. [BEHAVIOR] API 接收 step/prompt/model/input 参数，调 LLM 返回结果
3. [PRESERVE] 现有 content-pipeline 路由不受影响
4. [GATE] 测试通过

## 验收条件（DoD）

- [x] [PRESERVE] 现有 pipelines API 正常
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes('GET /'))process.exit(1)"

- [x] [ARTIFACT] test-step 路由存在
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes('test-step'))process.exit(1)"

- [x] [BEHAVIOR] 调用 callLLM 函数
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/content-pipeline.js','utf8');if(!c.includes('callLLM'))process.exit(1)"

- [x] [GATE] 文件语法正确
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/routes/content-pipeline.js')"

## 实现方案
在 packages/brain/src/routes/content-pipeline.js 添加 POST /content-types/test-step 路由
