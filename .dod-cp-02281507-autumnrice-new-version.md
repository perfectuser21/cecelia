---
id: dod-autumnrice-new-version
version: 1.0.0
created: 2026-02-28
updated: 2026-02-28
changelog:
  - 1.0.0: 初始版本
---

# DoD：秋米直接写入新版本到左侧 Tab

## 验收清单

- [ ] **后端**：`POST /autumnrice/chat` 检测"新版本"意图关键词
  - Test: message 含 `写一个新版本` 时，后端调用 LLM 获取 JSON 格式 initiatives
- [ ] **后端**：解析 LLM JSON 响应，创建新 `pending_action` 记录（same action_type, same kr_id, new initiatives）
  - Test: DB 中出现新 pending_action 记录，context.kr_id 与原记录相同
- [ ] **后端**：返回 `version_created: true`、`new_version_id`、简短 `reply`
  - Test: API 响应包含 version_created 字段
- [ ] **前端**：`handleSend` 收到 `version_created` 后，自动刷新版本列表并切换到新 Tab
  - Test: 点击发送后左侧 Tab 出现新版本并自动激活
- [ ] **前端**：聊天里只显示秋米的简短确认消息，不显示完整 Initiative 列表
  - Test: 聊天消息 ≤ 50 字
- [ ] Brain CI 通过（`brain-ci.yml`）
- [ ] Workspace CI 通过（`workspace-ci.yml`）
