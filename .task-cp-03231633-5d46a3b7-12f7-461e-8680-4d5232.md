---
id: task-cp-03231633-5d46a3b7-12f7-461e-8680-4d5232
type: task-card
branch: cp-03231633-5d46a3b7-12f7-461e-8680-4d5232
created: 2026-03-23
---

# Task Card: 重构 cortex.analyzeDeep（降低圈复杂度 55 → 10）

## 需求（What & Why）

**功能描述**: 重构 `packages/brain/src/cortex.js` 中的 `analyzeDeep` 函数，将其圈复杂度从 55 降低到 10 以下。

**背景**: 代码复杂度扫描发现该函数集中承担了太多职责——上下文构建、多个 DB 查询、LLM 调用、结果处理、错误处理等全部混合在一个函数里，导致圈复杂度达到 55（阈值 10）。

**不做什么**:
- 不改变函数的外部行为（输入/输出接口不变）
- 不修改其他函数
- 不改变数据库查询逻辑本身

## 成功标准

- `analyzeDeep` 函数圈复杂度降至 10 以下（提取子函数后每个函数也须≤10）
- `npm test` 全部通过，现有行为不变
- CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] analyzeDeep 公开行为不变：输入 (event, thalamusDecision) 返回相同格式 CortexDecision
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');if(!c.includes('async function analyzeDeep'))process.exit(1)"

- [x] [ARTIFACT] 提取独立子函数：`_buildBaseContext`、`_enrichContextWithHistory`、`_enrichContextWithSystemStatus`、`_enrichContextWithLearnings`、`_enrichContextWithHistoricalAnalyses`、`_enrichContextWithSelfModel`、`_enrichContextWithCrossTaskPatterns`
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');['_buildBaseContext','_enrichContextWithHistory','_enrichContextWithSystemStatus','_enrichContextWithLearnings','_enrichContextWithHistoricalAnalyses','_enrichContextWithSelfModel','_enrichContextWithCrossTaskPatterns'].forEach(fn=>{if(!c.includes('async function '+fn)&&!c.includes('function '+fn))throw new Error('missing:'+fn)})"

- [x] [ARTIFACT] 提取 `_callLLMAndProcess` 函数处理 LLM 调用与结果处理
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');if(!c.includes('_callLLMAndProcess'))process.exit(1)"

- [x] [BEHAVIOR] analyzeDeep 主体≤30行（复杂度≤10）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/cortex.js','utf8');const m=c.match(/async function analyzeDeep[\s\S]*?\n\}/);if(!m)process.exit(1);const lines=m[0].split('\n').length;console.log('analyzeDeep lines:',lines);if(lines>50)process.exit(1)"

- [x] [GATE] 测试文件已更新以匹配 console.info 重构（cortex-reflection-breaker D4 测试）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/cortex-reflection-breaker.test.js','utf8');if(!c.includes(\"console, 'info'\"))process.exit(1)"

## 实现方案

**要改的文件**: `packages/brain/src/cortex.js`（仅此一个文件）

**重构策略**:
1. 提取 `_buildBaseContext(event, thalamusDecision)` — 构建基础上下文对象
2. 提取 `_enrichContextWithHistory(context)` — 获取最近决策历史
3. 提取 `_enrichContextWithSystemStatus(context)` — 获取系统状态
4. 提取 `_enrichContextWithLearnings(context, event)` — 注入历史经验
5. 提取 `_enrichContextWithHistoricalAnalyses(context, event)` — 注入历史分析
6. 提取 `_enrichContextWithSelfModel(context)` — 注入 self-model
7. 提取 `_enrichContextWithCrossTaskPatterns(context)` — 注入跨任务失败模式
8. 提取 `_callLLMAndProcess(event, context)` — LLM 调用 + 结果处理（含去重、记录）
9. analyzeDeep 主体仅剩：熔断检查 → 构建上下文 → 串联 enrich → 调用 LLM
