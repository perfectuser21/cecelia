# Task Card: /dev pipeline 7项BUG修复

## 任务目标

修复 /dev 4-Stage pipeline skill 文件中7个已确认的 bug，全部在 `packages/engine/skills/dev/steps/` 目录。

## 成功标准

- 所有 7 个已确认 bug 修复完毕：CI镜像路径正确、[PRESERVE]模板存在、2.3.2 exit 1、过时注释删除、git add 修正、分数 X/20、BRANCH_NAME 变量
- /dev 全流程自检通过：verify-step.sh Gate 2 全绿（8/8 DoD PASS），npm test 717 passed

## 实现方案

**要改的文件**:
- `packages/engine/skills/dev/steps/01-spec.md`
- `packages/engine/skills/dev/steps/02-code.md`
- `packages/engine/skills/dev/steps/03-integrate.md`
- `packages/engine/skills/dev/steps/04-ship.md`
- Engine 版本 6 文件（packages/engine/package.json, package-lock.json, VERSION, .hook-core-version, regression-contract.yaml, features/feature-registry.yml）

**Scope 锚定**: packages/engine/skills/dev/steps/ — 纯 skill 文档修复，无代码层改动

## 验收条件（DoD）

- [x] [PRESERVE] 01-spec.md Stage 1 自检逻辑（格式验证 exit 1）未被破坏
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('exit 1') || !c.includes('Stage 1 自检失败'))process.exit(1);console.log('OK')"

- [x] [PRESERVE] 03-integrate.md Engine版本6文件自检（3.1.3 exit 1）未被破坏
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');if(!c.includes('Engine 版本文件不完整') || !c.includes('exit 1'))process.exit(1);console.log('OK')"

- [x] [PRESERVE] 04-ship.md cleanup_done: true 写入逻辑未被破坏
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/04-ship.md','utf8');if(!c.includes('cleanup_done: true'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 01-spec.md CI镜像路径已修正为正确路径
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(c.includes('node scripts/devgate/check-dod-mapping.cjs'))process.exit(1);if(!c.includes('packages/engine/scripts/devgate/check-dod-mapping.cjs'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 01-spec.md Task Card模板包含[PRESERVE]占位条目
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('[PRESERVE]'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 02-code.md 2.3.2 build/precheck/version-sync 均有 exit 1
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');const s=c.slice(c.indexOf('### 2.3.2'),c.indexOf('### 2.3.3'));if(!s.includes('exit 1'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 03-integrate.md 3.2 注释不再包含"派发 code_review"过时内容
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');const s=c.slice(c.indexOf('## 3.2 CI'),c.indexOf('### 3.2.1'));if(s.includes('派发 code_review'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 03-integrate.md 3.1.5 提交步骤显式 add task card 和 prd 文件
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/03-integrate.md','utf8');const s=c.slice(c.indexOf('### 3.1.5'),c.indexOf('### 3.1.6'));if(!s.includes('.task-cp-') || !s.includes('git add'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 04-ship.md 执行质量评分总分显示为 X/20
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/04-ship.md','utf8');if(c.includes('X/10') || !c.includes('X/20'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 04-ship.md 4.6归档使用 BRANCH_NAME 变量且确认 .history/ 存在
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/04-ship.md','utf8');if(c.includes('mv .task-\${BRANCH}.md') || !c.includes('mkdir -p .history'))process.exit(1);console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/engine && npm test"
