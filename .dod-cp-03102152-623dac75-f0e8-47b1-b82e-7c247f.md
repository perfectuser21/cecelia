# DoD - feat(brain): 低峰期动态 DAILY_BUDGET

## 验收清单

- [x] 新增 `getDailyBudget()` 函数，读取上海时间 (UTC+8) 小时数
  Test: manual:bash -c "grep -c 'getDailyBudget' packages/brain/src/rumination.js"

- [x] 00:00-06:00 UTC+8 返回 40，其他时间返回 20
  Test: manual:bash -c "grep -c 'off_peak\|offPeak\|low.*peak\|00.*06\|peak.*budget\|2.*40\|40\|peakHour\|offPeakHour\|isOffPeak\|OFF_PEAK' packages/brain/src/rumination.js"

- [x] `hasBudget()` 使用 `getDailyBudget()` 替代硬编码 `DAILY_BUDGET`
  Test: manual:bash -c "grep -c 'getDailyBudget()' packages/brain/src/rumination.js"

- [x] `getRuminationStatus()` 的 `daily_budget` 字段反映动态值
  Test: manual:bash -c "grep -A2 'daily_budget:' packages/brain/src/rumination.js | grep -c 'getDailyBudget'"

- [x] `runRumination()` 的 `remaining` 计算使用 `getDailyBudget()`
  Test: manual:bash -c "grep -c 'getDailyBudget' packages/brain/src/rumination.js"

- [x] 单元测试覆盖低峰期 (00:00-06:00 UTC+8) 返回 40
  Test: manual:bash -c "grep -c 'getDailyBudget\|off.peak\|offPeak\|40\|low.peak' packages/brain/src/__tests__/rumination.test.js"

- [x] 单元测试覆盖正常时段返回 20
  Test: manual:bash -c "grep -c 'getDailyBudget\|normal.*hour\|返回.*20' packages/brain/src/__tests__/rumination.test.js"

- [x] `DAILY_BUDGET` 常量（20）保留导出，向后兼容现有测试
  Test: manual:bash -c "grep -c 'export const DAILY_BUDGET' packages/brain/src/rumination.js"

- [x] `getDailyBudget` 函数导出，可被测试直接调用
  Test: manual:bash -c "grep -c 'export function getDailyBudget\|export.*getDailyBudget' packages/brain/src/rumination.js"
