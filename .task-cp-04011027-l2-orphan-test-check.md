# Task Card: L2 孤儿测试检测 Gate

**Branch**: cp-04011027-l2-orphan-test-check
**Brain Task**: ae1fa297-5144-4b1c-af4c-d580bc2a35bd
**Commit type**: chore:
**Created**: 2026-04-01

---

## 背景

PR #1774 建立了 `test-registry.yaml`（测试文件 SSOT）和测试分类 ADR。
但光有注册表不够——需要 CI 强制执行：任何未注册的 `*.test.*` 文件出现在 PR 里，必须阻断合并。

---

## 目标

在 `ci-l2-consistency.yml` 加入 `orphan-test-check` job：
1. 扫描 repo 中所有 `*.test.*` / `*.spec.*` 文件
2. 对比 `test-registry.yaml`（已登记的路径）
3. 发现未注册文件 → 输出清单 → 失败，PR 无法合并

---

## 成功标准

1. L2 新增 `orphan-test-check` job
2. 已注册的 25 个文件不触发报错
3. 新增未注册测试文件时 CI 会失败

---

## DoD

- [x] **[ARTIFACT]** `ci-l2-consistency.yml` 包含 `orphan-test-check` job
  - Test: `manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l2-consistency.yml','utf8');if(!c.includes('orphan-test-check'))process.exit(1)"`

- [x] **[BEHAVIOR]** orphan-test-check 读取 test-registry.yaml 并扫描测试文件
  - Test: `manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l2-consistency.yml','utf8');if(!c.includes('test-registry.yaml'))process.exit(1)"`

- [x] **[BEHAVIOR]** node_modules 和 .git 目录被排除在扫描范围外
  - Test: `manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l2-consistency.yml','utf8');if(!c.includes('node_modules'))process.exit(1)"`
