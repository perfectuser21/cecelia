# Task: TaskTypeConfigPage 三层下钻架构

**Task ID**: a676faa7-960a-4db8-9488-ef95051edd19
**Branch**: cp-03222102-task-type-drilldown
**Type**: fix

## 目标

重设计 TaskTypeConfigPage 为三层下钻架构，替代现有扁平表格：
- 第一层：4 个 ABCD 类别大卡片，点击进入
- 第二层：该类任务列表，每行可点击
- 第三层：右侧面板显示任务详情 + 编辑表单（C类动态任务可编辑）
- 面包屑导航支持逐层返回

## 成功标准

- [x] [ARTIFACT] D1: 页面包含面包屑导航组件（Breadcrumb）
  Test: manual: node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx','utf8');if(!c.includes('Breadcrumb')||!c.includes('breadcrumb'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] D2: 存在 CategoryCard 或 category card 组件，展示 ABCD 四类
  Test: manual: node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx','utf8');if(!c.includes('CategoryCard')&&!c.includes('category-card'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] D3: 存在 DetailPanel 或 detail panel，显示任务详情
  Test: manual: node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/task-type-configs/TaskTypeConfigPage.tsx','utf8');if(!c.includes('DetailPanel')&&!c.includes('detailPanel')&&!c.includes('detail-panel'))process.exit(1);console.log('ok')"
