# DoD: content-pipeline Brain 内部编排

- [x] [ARTIFACT] content-pipeline-orchestrator.js 存在，导出 orchestrateContentPipelines + advanceContentPipeline
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-orchestrator.js','utf8');if(!c.includes('orchestrateContentPipelines')||!c.includes('advanceContentPipeline'))process.exit(1);console.log('OK exports present')"

- [x] [ARTIFACT] tick.js 引用 content-pipeline-orchestrator.js
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('content-pipeline-orchestrator'))process.exit(1);console.log('OK tick imports orchestrator')"

- [x] [ARTIFACT] execution.js 引用 content-pipeline-orchestrator.js
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/execution.js','utf8');if(!c.includes('content-pipeline-orchestrator'))process.exit(1);console.log('OK execution imports orchestrator')"

- [x] [BEHAVIOR] PIPELINE_STAGES 数组按正确顺序定义四个阶段
  Test: manual:node -e "import('./packages/brain/src/content-pipeline-orchestrator.js').then(m=>{const s=m.PIPELINE_STAGES;if(!s||s[0]!=='content-research'||s[3]!=='content-export')process.exit(1);console.log('OK stages ordered')})"

- [x] [BEHAVIOR] MAX_REVIEW_RETRY 常量存在且值为3（防止无限重试）
  Test: manual:node -e "import('./packages/brain/src/content-pipeline-orchestrator.js').then(m=>{if(m.MAX_REVIEW_RETRY!==3)process.exit(1);console.log('OK MAX_REVIEW_RETRY=3')})"

- [x] [BEHAVIOR] 单元测试覆盖主流程（含 retry 上限边界）
  Test: manual:bash -c "cd packages/brain && npm test -- --reporter=verbose 2>&1 | grep -E 'content-pipeline-orchestrator' | head -5"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | grep -E '[0-9]+ passed' | tail -1"
