---
id: task-cp-03212358-tick-watchdog-test
type: task-card
branch: cp-03212358-tick-watchdog-test
created: 2026-03-21
---

# Task Card: Tick Watchdog 测试覆盖

## 需求（What & Why）
**功能描述**: 为 tick watchdog 自动恢复机制补充测试覆盖。PR #1267 实现了 drain 自杀修复 + tick watchdog，但 startTickWatchdog() 的核心逻辑（非 manual disable 时自动 enable、manual disable 时保持）没有任何测试。
**背景**: 本任务是 Brain 重新派发的 [P0] drain 自杀修复任务。代码改动（TICK_AUTO_RECOVER_MINUTES=5、drain 改用 postDrainCooldown、startTickWatchdog）已在 PR #1267 合并。本次补充缺失的 tick watchdog 行为测试。
**不做什么**: 不重复实现已有改动，不修改 tick.js 代码。

## 成功标准

1. [ARTIFACT] 新建 `tick-watchdog.test.js` 覆盖 watchdog 核心行为
2. [BEHAVIOR] 测试验证：drain/alertness source 时 watchdog 自动 enable tick
3. [BEHAVIOR] 测试验证：manual source 时 watchdog 不自动 enable tick
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] `packages/brain/src/__tests__/tick-watchdog.test.js` 文件存在
  Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/brain/src/__tests__/tick-watchdog.test.js'))process.exit(1)"

- [x] [BEHAVIOR] watchdog 检测到 drain source disabled 时自动恢复 tick
  Test: manual:bash -c "cd packages/brain && npm test -- --reporter=verbose tick-watchdog 2>&1 | grep -E 'PASS|pass|✓' | head -5"

- [x] [BEHAVIOR] watchdog 检测到 manual source disabled 时不自动恢复
  Test: manual:bash -c "cd packages/brain && npm test -- --reporter=verbose tick-watchdog 2>&1 | grep -E 'PASS|pass|✓' | head -5"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -10"

## 实现方案
**要改的文件**: 新建 `packages/brain/src/__tests__/tick-watchdog.test.js`
**Scope 锚定**: 仅测试 startTickWatchdog/stopTickWatchdog/disableTick/enableTick 交互
