# PRD - Step3 push前强制执行DoD Test命令

## 背景
在 /dev Step 3 push 之前，逐条提取 Task Card 里的 manual: 命令并真实执行，全部 exit 0 才允许 push。
当前 Step 7.2 被删除后没有替代，DoD Test 命令从未被本地执行过。

## 功能描述
在 `packages/engine/skills/dev/steps/03-prci.md` 的 8.4（提交）和 8.5（推送）之间插入 8.4.5 DoD Test 门禁节，
逐条执行所有 `Test: manual:` 命令，任一失败则 exit 1 阻止 push。

## 成功标准
- [ ] `### 8.4.5 ⛔ DoD Test 门禁` 节存在于 03-prci.md 中，位于 8.4 和 8.5 之间
- [ ] Engine 版本 bump 到 12.95.0（5 个文件同步）
- [ ] feature-registry.yml 新增 changelog 条目
- [ ] CI 全部通过
