---
id: task-cp-03230753-57fb49ec-a804-4fc7-951a-d294d2
type: task-card
branch: cp-03230753-57fb49ec-a804-4fc7-951a-d294d2
created: 2026-03-23
---

# Task Card: arch_review 每4小时定时执行 + Codex 路由

## 需求（What & Why）

**功能描述**: 为 arch_review 任务添加定时调度逻辑（每4小时自动派发），并将其路由从 US 本机改为 西安 Codex（xian）。

**背景**: arch_review 目前无定时触发，只能手动调用，架构漂移无人发现。需要在 tick 循环中加入每4小时一次的自动触发，并且 arch_review 属于纯分析/审查类型，应路由到西安 Codex 而非美国本机（/dev 才是美国本机专属）。

**不做什么**: 不修改 /arch-review skill 本身的逻辑；不改变 initiative_verify 的路由。

## 成功标准

1. [ARTIFACT] `daily-review-scheduler.js` 包含 `triggerArchReview` 函数，每4小时触发（UTC 0/4/8/12/16/20），guard: 上次 review 后至少1个 dev 任务完成
2. [ARTIFACT] `task-router.js` 中 `arch_review` 路由改为 `'xian'`
3. [ARTIFACT] `tick.js` 调用 `triggerArchReview`
4. [BEHAVIOR] 测试覆盖窗口判断、dedup、guard 逻辑
5. [GATE] Brain L3 测试全部通过

## 验收条件（DoD）

- [ ] thalamus.js 或 tick.js 含 arch_review 定时逻辑
  Test: manual:node -e "const fs=require('fs');const files=['packages/brain/src/thalamus.js','packages/brain/src/tick.js'];const found=files.some(f=>fs.readFileSync(f,'utf8').includes('arch_review'));if(!found)process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] task-router.js arch_review 路由存在
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/task-router.js','utf8');if(!c.includes('arch_review'))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] task-router.js arch_review 路由到 xian
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/task-router.js','utf8');if(!c.includes(\"'arch_review': 'xian'\"))process.exit(1);console.log('OK')"

- [ ] [GATE] Brain L3 通过
  Test: manual:node -e "console.log('manual verify: cd packages/brain && npm test')"

## 实现方案

**要改的文件**:
1. `packages/brain/src/daily-review-scheduler.js` — 添加 `isInArchReviewWindow`、`hasRecentArchReview`、`hasCompletedDevTaskSinceLastArchReview`、`triggerArchReview` 四个函数
2. `packages/brain/src/tick.js` — import `triggerArchReview` 并在 tick 末尾调用（编号 10.X）
3. `packages/brain/src/task-router.js` — 将 `'arch_review': 'us'` 改为 `'arch_review': 'xian'`
4. `packages/brain/src/__tests__/daily-review-scheduler.test.js` — 新增 arch_review 调度测试
5. `packages/brain/src/__tests__/task-router-core.test.js` — 更新 arch_review 路由期望值

**核心逻辑**:
- 触发窗口：UTC 小时 % 4 === 0，minute < 5（即 0:00/4:00/8:00/12:00/16:00/20:00 UTC）
- Guard: 查询 tasks 表，上次 arch_review 之后是否有 completed 的 dev 任务
- Dedup: 4小时内已有 arch_review task 则跳过
