---
id: task-cp-0416104736-c2-tasks-schema-normalize
type: task-card
branch: cp-0416104736-c2-tasks-schema-normalize
task_id: 16adaf35-c357-4f36-929a-2d59588bdf1b
created: 2026-04-16
---

# Task Card: C2 — POST /tasks schema 收敛 + priority normalize

## 需求
PR #2370 在 pre-flight 层做了 payload.prd_summary fallback + priority normalize（防线）。
C2 在入口层（POST /api/brain/tasks handler）做相同 normalize，使 pre-flight 变 double-check 而非唯一防线。

## 实现
- `packages/brain/src/routes/task-tasks.js` POST handler INSERT 前加 normalize
- `const` 解构改 `let`（允许 description/priority 被重写）
- 9 个单测覆盖 fallback + normalize + reject

## DoD
- [x] [BEHAVIOR] POST 传 payload.prd_summary + description 空 → 返回的 description 非空
- [x] [BEHAVIOR] POST 传 priority=normal → 返回 P2
- [x] [BEHAVIOR] POST 传 priority=foo → 400
- [x] [ARTIFACT] task-tasks.js 含 PRIORITY_NORMALIZE_MAP + prd_summary fallback
- [x] [ARTIFACT] 测试文件 tasks-schema-normalize.test.js 存在
