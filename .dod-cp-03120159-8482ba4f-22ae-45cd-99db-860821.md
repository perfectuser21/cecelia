---
id: dod-liveness-requeue
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

## DoD - liveness 失败改 requeue

### 验收清单

- [x] executor.js probeTaskLiveness DEAD 路径改为调用 requeueTask() 而非 updateTaskStatus(failed)
  Test: manual:bash -c "grep -c 'requeueTask(task.id' packages/brain/src/executor.js"

- [x] requeue 时 reason 传 'liveness_dead'，evidence 包含 diagnostic_info
  Test: manual:bash -c "grep -c 'liveness_dead' packages/brain/src/executor.js"

- [x] action 改为 'liveness_auto_requeue'（quarantined 时保留 'liveness_auto_fail'）
  Test: manual:bash -c "grep -c 'liveness_auto_requeue' packages/brain/src/executor.js"

- [x] 超过 FAILURE_THRESHOLD 后走隔离逻辑（requeueTask 内部已有）
  Test: manual:bash -c "grep -c 'QUARANTINE_AFTER_KILLS' packages/brain/src/executor.js"

- [x] 单测：DEAD 探针 → 验证任务被 requeue（action = liveness_auto_requeue）
  Test: manual:bash -c "grep -c 'liveness_auto_requeue' packages/brain/src/__tests__/liveness-probe.test.js"

- [x] 单测：decomp 任务过 grace period → 同样走 requeue 而非 fail
  Test: manual:bash -c "grep -c 'liveness_auto_requeue' packages/brain/src/__tests__/liveness-probe.test.js"
