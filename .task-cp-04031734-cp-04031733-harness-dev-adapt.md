---
id: task-cp-04031734-cp-04031733-harness-dev-adapt
type: task-card
branch: cp-04031734-cp-04031733-harness-dev-adapt
created: 2026-04-03
---

# Task Card: /dev 适配 Harness v2.0 模式

## 需求（What & Why）
**功能描述**: 在 harness_mode 下简化 /dev 流程 — 不自写 Spec/DoD（Sprint Contract 已由 Generator 写好），不自验 DoD（Evaluator 来验），Stop Hook 只检查"代码写完 + PR 已创建"。
**背景**: Harness v2.0 Sprint 循环（PR #1810）已合并，需要 /dev 适配新模式。
**不做什么**: 不改 Brain 断链逻辑（PR1 已做），不改 Generator/Evaluator Skill（PR2 已做）。

## 成功标准
> [BEHAVIOR] harness_mode 下 /dev 跳过 Spec 自写 + DoD 自验
> [BEHAVIOR] devloop-check 在 harness_mode 下只检查代码完成+PR创建
> [ARTIFACT] 01-spec.md/02-code.md/SKILL.md 包含 harness 模式判断

## 验收条件（DoD）

- [x] [BEHAVIOR] 01-spec.md 包含 harness_mode 判断：读 sprint-contract.md 而非自写 Task Card
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('harness_mode'))process.exit(1)"

- [x] [BEHAVIOR] 02-code.md 包含 harness_mode 判断：不逐条验证 DoD
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('harness_mode'))process.exit(1)"

- [x] [BEHAVIOR] devloop-check.sh 在 harness_mode 下跳过 DoD/CI/Learning 检查，只检查代码完成+PR创建
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('harness_mode'))process.exit(1)"

- [x] [BEHAVIOR] stop-dev.sh 在 harness_mode 下识别 harness 模式
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('harness'))process.exit(1)"

- [x] [ARTIFACT] SKILL.md 包含 harness 模式说明
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(!c.includes('harness'))process.exit(1)"

## 实现方案（必填）
**要改的文件**:
- `packages/engine/skills/dev/steps/01-spec.md`: 加 harness_mode 判断
- `packages/engine/skills/dev/steps/02-code.md`: 加 harness_mode 判断
- `packages/engine/skills/dev/SKILL.md`: 加 harness 模式说明
- `packages/engine/lib/devloop-check.sh`: 加 harness_mode 快速通道
- `packages/engine/hooks/stop-dev.sh`: 加 harness_mode 检测
- 版本 bump: package.json/VERSION/.hook-core-version/feature.yaml

**不改什么**: Brain 断链、Generator/Evaluator Skill、03-integrate.md、04-ship.md
