# DoD: cross-ref-registry 检查器实现 + CI L2 接入

## 成功标准
1. [ARTIFACT] scripts/check-cross-ref.mjs 存在且能执行
2. [BEHAVIOR] check-cross-ref.mjs 在全一致时 exit 0
3. [BEHAVIOR] CI L2 新增 cross-ref-check job
4. [GATE] CI 全通过

## 验收条件（DoD）

- [x] [ARTIFACT] check-cross-ref.mjs 存在
  Test: manual:node -e "require('fs').accessSync('scripts/check-cross-ref.mjs');process.exit(0)"

- [x] [BEHAVIOR] check-cross-ref.mjs 执行 exit 0（当前仓库一致）
  Test: manual:node -e "const{execSync}=require('child_process');try{execSync('node scripts/check-cross-ref.mjs',{stdio:'pipe'});process.exit(0)}catch{process.exit(1)}"

- [x] [BEHAVIOR] CI L2 workflow 包含 cross-ref-check job
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l2-consistency.yml','utf8');if(!c.includes('cross-ref'))process.exit(1)"

- [x] [GATE] check-cross-ref.mjs 语法正确
  Test: manual:bash -c "node --check scripts/check-cross-ref.mjs"
