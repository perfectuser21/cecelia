---
branch: cp-03171706-fix-brain-l3-tests
created: 2026-03-17
---

# PRD: 修复 Brain L3 测试失败

## 背景

CI L3 每次 push main 都失败。`coverage-baseline` job 运行 `npm run test:coverage` 时有 20 个测试失败（17 + 3）。

## 问题

1. `startup-sync.test.js`（3 个）：PR #1025 改了孤儿恢复逻辑（requeue-first），测试未同步
2. `blocks.test.js`（17 个）：集成测试无 skip guard，coverage job 里没有排除

## 目标

修复这 20 个测试失败，使 CI L3 全绿。

## 成功标准

## 成功标准

1. `startup-sync.test.js` 3 个失败测试修复
2. `blocks.test.js` 加 skip guard（CI 中跳过）
3. `npm run test:coverage` 在 CI 环境通过
