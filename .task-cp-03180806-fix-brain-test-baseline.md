# Task Card: fix-brain-test-baseline

## 需求

`packages/brain/ci/brain-test-baseline.txt` 当前值为 `3`（允许 3 个 Brain 测试失败），但 #1039 已修复 startup-sync + blocks 测试，实际失败数为 0。基线过松，需改为 `0`。

同时关闭 #1042（该 PR 是 #1039 的孪生僵尸 PR，核心测试修复已在 main）。

## 成功标准

- `packages/brain/ci/brain-test-baseline.txt` 内容为 `0`
- PR 合并后 CI Brain L3 通过，baseline 不再允许虚假失败

## DoD

### [ARTIFACT] baseline 文件值
- Test: `node -e "const v=require('fs').readFileSync('packages/brain/ci/brain-test-baseline.txt','utf8').trim();if(v!=='0')process.exit(1);"`
- Expected: exit 0

### [BEHAVIOR] CI 不因旧 baseline 放行假失败
- Test: `manual: node -e "const v=require('fs').readFileSync('packages/brain/ci/brain-test-baseline.txt','utf8').trim();console.log('baseline='+v);if(parseInt(v)>0)process.exit(1);"`
- Expected: baseline=0，exit 0

## 验收清单

- [ ] brain-test-baseline.txt 值为 0
- [ ] PR CI 全通过
- [ ] #1042 已关闭
