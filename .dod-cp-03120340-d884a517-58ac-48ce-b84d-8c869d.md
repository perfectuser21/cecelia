---
id: dod-tick-gradient-requeue
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

# DoD - tick.js step 6.5 梯度 requeue

## 验收清单

- [x] 在 tick.js 常量区新增 `MAX_REQUEUE_PER_TICK = 2`，与 UNBLOCK_BATCH_LIMIT / QUARANTINE_RELEASE_LIMIT 同区
  Test: manual:bash -c "grep -c 'MAX_REQUEUE_PER_TICK' packages/brain/src/tick.js"

- [x] step 6.5 UPDATE 改为子查询写法：`WHERE id IN (SELECT id FROM tasks WHERE status='quota_exhausted' ORDER BY priority ASC, created_at ASC LIMIT $1)`
  Test: manual:bash -c "grep -c 'ORDER BY priority ASC' packages/brain/src/tick.js"

- [x] log 中报告本次 requeue 数量和剩余数量（remaining）
  Test: manual:bash -c "grep -c 'remaining' packages/brain/src/tick.js"

- [x] 新增测试文件 `tick-gradient-requeue.test.js`：6 个 quota_exhausted 任务，第一 tick 只 requeue 2 个
  Test: manual:bash -c "ls packages/brain/src/__tests__/tick-gradient-requeue.test.js"
