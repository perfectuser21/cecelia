# PRD: fix executor routing by location

## 背景
executor.js 的 DEV_ONLY_TYPES 逻辑盲目把非 dev 任务路由到西安 Codex Bridge，但 cto_review/dod_verify/prd_audit 的 location='us' 应该走本机 cecelia-bridge。

## 成功标准
- executor 按 task-router.js 的 location 路由（us→本机，xian→Codex Bridge，hk→MiniMax）
- 删除 DEV_ONLY_TYPES 硬编码
- executor.js 语法正确
