---
id: task-cp-04021500-engine-e2e-test
type: task-card
branch: cp-04021500-engine-e2e-test
created: 2026-04-02
---

# Task Card: 补 Engine E2E 测试 — /dev Stage 0→4 全流程

## 需求（What & Why）
**功能描述**: 扩展 packages/engine/tests/e2e/dev-workflow-e2e.test.ts，补充 devloop-check.sh 的 Stage 0→4 全流程 E2E 测试覆盖。当前已有 13 个测试覆盖 worktree-manage、部分 devloop-check stage 检测和 stop hook 退出码，但缺少 Stage 3（PR 条件）、Stage 4（合并后行为）、DoD 完整性检查（条件 2.6）、以及完整的状态推进序列验证。

**背景**: Engine E2E 测试是后续 Engine 重构（删 Stage 系统、瘦 stop-dev.sh）的前置安全网。没有完整的 E2E 覆盖，重构可能引入隐蔽回归。

**不做什么**:
- 不改 devloop-check.sh 本身的逻辑
- 不改 stop-dev.sh / branch-protect.sh
- 不需要真实的 GitHub PR（mock gh CLI）
- 不涉及 Brain API 交互

## 成功标准
- E2E 测试覆盖 devloop-check.sh 全部 7 个判断条件（cleanup_done / step_1 / step_2 / DoD完整性 / PR创建 / CI状态 / PR合并+Ship）
- 新增测试全部通过（npx vitest run packages/engine/tests/e2e/）
- 现有 13 个测试不受影响（无回归）

## 验收条件（DoD）

- [x] [PRESERVE] 现有 13 个 E2E 测试全部通过
  Test: manual:node -e "const {execSync}=require('child_process');const out=execSync('npx vitest run packages/engine/tests/e2e/dev-workflow-e2e.test.ts --reporter=verbose 2>&1',{encoding:'utf8',cwd:'/Users/administrator/worktrees/cecelia/engine-e2e-test'});if(!out.includes('13 passed'))process.exit(1)"

- [x] [BEHAVIOR] devloop-check 在 step_2_code=done + 无 PR 时 exit 2 + 输出 PR 未创建
  Test: tests/e2e/dev-workflow-e2e.test.ts

- [x] [BEHAVIOR] devloop-check DoD 完整性检查：Task Card 含未勾选条目时 exit 2
  Test: tests/e2e/dev-workflow-e2e.test.ts

- [x] [BEHAVIOR] devloop-check 完整 Stage 推进序列：从 step_1=pending 到 cleanup_done=true，每步 exit code 正确
  Test: tests/e2e/dev-workflow-e2e.test.ts

- [x] [ARTIFACT] 新增测试数量 >= 5
  Test: manual:node -e "const {execSync}=require('child_process');const out=execSync('npx vitest run packages/engine/tests/e2e/ --reporter=verbose 2>&1',{encoding:'utf8',cwd:'/Users/administrator/worktrees/cecelia/engine-e2e-test'});const m=out.match(/(\\d+) passed/);if(!m||parseInt(m[1])<18)process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:npm test
