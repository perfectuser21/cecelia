---
branch: cp-03241135-okr-write-migration-pr9
created: 2026-03-24
task_id: 309a0e72-c671-4dce-b188-fbaab4cbe75c
---

# PRD: OKR Write Migration PR9 — 所有 WRITE 操作迁移到新 OKR 表

## 背景

PR1-PR8 已完成所有 SELECT（读）操作从 `goals`/`projects` 迁移到新 OKR 表。
本 PR9 迁移所有 WRITE 操作（INSERT/UPDATE），彻底完成 OKR 表结构迁移。

新表层级：`visions → objectives → key_results → okr_projects → okr_scopes → okr_initiatives`

## 目标

将以下文件中的 INSERT/UPDATE 操作从旧表迁移到新 OKR 表：
- `packages/brain/src/actions.js`（createInitiative/createScope/createProject/createGoal/updateGoal）
- `packages/brain/src/planner.js`（handlePlanInput）
- `packages/brain/src/decision-executor.js`（create_okr/okr_decomp_review）
- `packages/brain/src/self-drive.js`（executeAdjustmentAction）
- `packages/brain/src/progress-reviewer.js`（executePlanAdjustment）
- `packages/brain/src/project-activator.js`（manageProjectActivation）
- `packages/brain/src/chat-action-dispatcher.js`（handleLlmCreateGoal/pipelineCreateGoal）
- `packages/brain/src/intent.js`（createInitiative）
- `packages/brain/src/routes/execution.js`（initiative complete/verify）

## 成功标准

1. 所有9个源文件的 INSERT/UPDATE 操作迁移到新 OKR 表
2. UPDATE 操作有 fallback（先试新表，rowCount=0 则回退旧表）
3. metadata JSONB 存储 domain/repo_path/sequence_order/current_phase 等无对应新列的字段
4. 所有测试通过（npm test）
