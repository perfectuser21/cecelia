# DoD - fix(executor): syncOrphanTasksOnStartup 孤儿改 requeue

## 完成标准

- [ ] `syncOrphanTasksOnStartup` 中 `reason=process_disappeared` 的孤儿更新为 `status=queued`
- [ ] `reason=oom_killed / oom_likely / killed_signal / timeout` 的孤儿仍保持 `status=failed`
- [ ] requeue 时 `error_message = [requeued after brain restart] reason=process_disappeared`
- [ ] 单元测试覆盖：process_disappeared → queued；oom_killed → failed

## 测试命令

```bash
grep -c 'isRetryable' packages/brain/src/executor.js
grep -c 'requeued after brain restart' packages/brain/src/executor.js
grep -c 'syncOrphanTasksOnStartup' packages/brain/tests/executor.test.js
```
