# PRD: 重启恢复集成测试套件 + 回归契约

## 背景
PR1 修复了 syncOrphanTasksOnStartup 孤儿改 requeue 逻辑；PR2 统一了重复代码路径。为这两个变更建立测试覆盖和回归契约。

## 目标
为 Brain 重启恢复路径建立完整集成测试套件，并在 regression-contract.yaml 登记回归契约。

## 成功标准
- [ ] executor-startup-sync.test.js 新增 OOM/kill 区分测试 + 并发重启安全测试
- [ ] packages/quality/regression-contract.yaml 新增 C-STARTUP-REQUEUE-001 条目（含 expires）
- [ ] CI 测试通过（brain-ci.yml L1+L3）
