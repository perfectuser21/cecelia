---
id: dod-cp-03122118-bridge-timeout-fix
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

# DoD - fix(bridge): 移除 Math.min timeout cap

## 验收清单

- [x] cecelia-bridge.cjs line 74 改为 `Math.min(timeout || BRIDGE_TIMEOUT_MS, 600000)`
  Test: manual:bash -c "grep -c 'Math.min(timeout || BRIDGE_TIMEOUT_MS, 600000)' packages/brain/scripts/cecelia-bridge.cjs"

- [x] 启动日志增加 BRIDGE_TIMEOUT_MS 显示
  Test: manual:bash -c "grep -c 'BRIDGE_TIMEOUT_MS' packages/brain/scripts/cecelia-bridge.cjs"

- [x] 单元测试验证 300s 超时正确传递（不被截断）
  Test: manual:bash -c "grep -c '300000' packages/brain/src/__tests__/bridge-llm-timeout.test.js"
