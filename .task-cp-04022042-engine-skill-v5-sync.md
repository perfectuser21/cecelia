---
id: task-engine-skill-v5-sync
type: task-card
---
# Task Card: 引擎内嵌 skill 同步到 v5.0.0

## 需求
引擎内嵌 skill（packages/engine/skills/dev/）是 v4.0.0 旧版，含大量已删功能引用。
用户目录是 symlink 指向引擎目录，所以必须走 PR 更新。

## 验收条件（DoD）

- [ ] [ARTIFACT] 18 个多余脚本已删除
  Test: manual:node -e "const fs=require('fs');['anti-cheat-dod.sh','anti-cheat-prd.sh','branch-gc.sh','extract-learnings.sh','generate-feedback-report.sh','generate-feedback-report-v2.sh','generate-report.sh','multi-feature.sh','record-step.sh','scan-change-level.sh','track.sh','update-capability.sh','update-task-status.sh','upload-feedback.sh','validate-dod.py','validate-prd.py','wait-for-merge.sh','worktree-gc.sh'].forEach(f=>{try{fs.accessSync('packages/engine/skills/dev/scripts/'+f);process.exit(1)}catch(e){}});console.log('OK')"

- [ ] [ARTIFACT] lib/planner-prompt.md 和 lib/step-expectations.json 已删除
  Test: manual:node -e "try{require('fs').accessSync('packages/engine/skills/dev/lib');process.exit(1)}catch(e){console.log('OK')}"

- [ ] [BEHAVIOR] SKILL.md 版本为 5.0.0 且无已删功能引用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/SKILL.md','utf8');if(!c.includes('version: 5.0.0'))process.exit(1);if(c.includes('Planner subagent')||c.includes('Generator subagent')||c.includes('Sprint Contract'))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] Engine 版本 6 文件同步
  Test: manual:node -e "const {execSync}=require('child_process');execSync('bash ci/scripts/check-version-sync.sh',{cwd:'packages/engine',stdio:'inherit'})"

- [ ] [GATE] CI 全部通过
  Test: manual:npm test

## 实现方案
**要改的文件**: packages/engine/skills/dev/ 下所有文件
**不改什么**: hooks、CI、devgate 脚本
