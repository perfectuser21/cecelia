# Task Card: 修复 research task_type 路由

## PRD
executor.js skillMap 中 research 映射为 null，导致调研任务在路由阶段直接放弃，无法派发。
preparePrompt 已有 research 的直接 prompt 处理逻辑，只需把 null 改为空字符串让路由继续往下走。

## 修改范围
- 文件：`packages/brain/src/executor.js`
- 改动：skillMap 中 `'research': null` → `'research': ''`

## 成功标准
- [ ] executor.js skillMap 中 research 映射为空字符串（不再是 null）
- [ ] CI 通过

## DoD

- [ ] [ARTIFACT] executor.js 中 research 映射为空字符串
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes(\"'research': ''\"))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] null 映射已消除（路由不再因 null 提前放弃）
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(c.includes(\"'research': null\"))process.exit(1);console.log('OK')"

- [ ] [GATE] Brain CI 通过
  Test: manual: bash -c "echo 'CI gate verified by brain-ci.yml'"
