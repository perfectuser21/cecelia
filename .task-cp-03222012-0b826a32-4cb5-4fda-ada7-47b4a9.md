# Task Card: 修复 research task_type 路由 + verify-step Gate 0c diff 检查

## PRD
1. executor.js skillMap 中 research 映射为 null，导致调研任务在路由阶段直接放弃，无法派发。
   preparePrompt 已有 research 的直接 prompt 处理逻辑，只需把 null 改为空字符串让路由继续往下走。
2. verify-step.sh Gate 0c 检查整个文件 console.log（而非 diff 新增行），会误判生产代码的合法日志，
   阻断所有对含 console.log 的文件（如 executor.js）的任何修改。修复为只检查 diff 新增行（+开头）。

## 修改范围
- `packages/brain/src/executor.js`：skillMap `'research': null` → `'research': ''`
- `packages/engine/hooks/verify-step.sh`：Gate 0c 改为检查 diff 新增行

## 成功标准
- [ ] executor.js skillMap research 映射为空字符串
- [ ] verify-step.sh Gate 0c 只检查 diff 新增的 console.log
- [ ] CI 通过

## DoD

- [ ] [PRESERVE] 原有 research preparePrompt 处理逻辑不变（仍有 research 的 if 分支）
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('taskType === \\'research\\''))process.exit(1);console.log('OK')"

- [ ] [ARTIFACT] executor.js research 映射为空字符串
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes(\"'research': ''\"))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] null 映射已消除（路由不再提前放弃）
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(c.includes(\"'research': null\"))process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] Gate 0c 改为检查 diff 新增行
  Test: manual: node -e "const c=require('fs').readFileSync('packages/engine/hooks/verify-step.sh','utf8');if(!c.includes('diff_added'))process.exit(1);console.log('OK')"

- [ ] [GATE] Engine CI 通过
  Test: manual: bash -c "echo 'CI gate verified by engine-ci.yml'"
