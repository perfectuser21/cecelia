---
id: dod-fix-executor-error-message
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD - fix(brain): executor 孤儿检测路径补写 error_message 列

## 验收清单

- [x] syncOrphanTasksOnStartup：UPDATE tasks 同时写 error_message
  Test: manual:bash -c "grep -c 'error_message' packages/brain/src/executor.js"

- [x] liveness probe 超时路径：通过 updateTaskStatus 写入 error_message
  Test: manual:bash -c "grep -c 'error_message.*liveness' packages/brain/src/executor.js"

- [x] watchdog quarantine 路径：UPDATE tasks 同时写 error_message
  Test: manual:bash -c "grep -c 'error_message.*watchdog\\|watchdog.*error_message' packages/brain/src/executor.js"

- [x] task-updater.js ALLOWED_COLUMNS 包含 error_message
  Test: manual:bash -c "grep -c 'error_message' packages/brain/src/task-updater.js"

- [x] 单测文件存在，验证 error_message 被写入
  Test: manual:bash -c "ls packages/brain/src/__tests__/executor-error-message.test.js"
