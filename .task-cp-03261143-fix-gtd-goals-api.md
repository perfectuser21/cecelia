# Task: 修复 GTD 页面空数据 — 接通 Brain 真实表

## 背景
GTD 系列页面（/gtd/okr, /gtd/projects, /gtd/area）调用 apps/api 的 `/api/tasks/goals`、`/api/tasks/projects`、`/api/tasks/areas`，但这些路由查的本地表（goals/projects）不存在，导致前端永远显示空数据。

真实数据在同一个 cecelia DB 的 objectives/key_results/okr_projects/okr_initiatives/areas 表中。

## 改动范围
- `apps/api/src/task-system/areas.js`（新建）— GET / 查 areas 表，支持 archived 过滤
- `apps/api/src/task-system/routes.js` — 挂载 /areas

## 成功标准
- `GET /api/tasks/areas` 返回 areas 表数据
- /gtd/area 前端不再显示 0 条

## DoD

- [x] areas.js 已创建且包含 areas 表查询
  Test: manual: node -e "const c=require('fs').readFileSync('apps/api/src/task-system/areas.js','utf8');if(!c.includes('FROM areas'))process.exit(1)"
- [x] routes.js 已挂载 /areas 路由
  Test: manual: node -e "const r=require('fs').readFileSync('apps/api/src/task-system/routes.js','utf8');if(!r.includes('areasRouter'))process.exit(1)"
- [x] [BEHAVIOR] areas.js 返回 areas 表中的 id/name/domain 字段
  Test: manual: node -e "const c=require('fs').readFileSync('apps/api/src/task-system/areas.js','utf8');if(!c.includes('id'))process.exit(1);if(!c.includes('name'))process.exit(1);if(!c.includes('domain'))process.exit(1)"
