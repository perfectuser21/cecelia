# DoD: 修复 rumination 链路 — NotebookLM 响应清洗

## 验收条件

- [x] [ARTIFACT] `cleanNotebookResponse()` 函数已导出并存在于 `packages/brain/src/rumination.js`
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/rumination.js','utf8');if(!c.includes('export function cleanNotebookResponse'))process.exit(1)"

- [x] [BEHAVIOR] `cleanNotebookResponse()` 正确提取 Answer 内容，剥离 "Different notebook specified" 前缀噪音
  Test: manual:node --input-type=module -e "import{cleanNotebookResponse}from'./packages/brain/src/rumination.js';const r=cleanNotebookResponse('Different notebook specified, starting new conversation...\nContinuing conversation abc..\nAnswer:\n[反刍洞察] 测试洞察');if(!r.includes('测试洞察')||r.includes('Different notebook'))process.exit(1)"

- [x] [BEHAVIOR] `digestLearnings()` 在获得 NotebookLM 响应后调用 `cleanNotebookResponse()` 清洗，清洗后 length<=50 则 fallback 到 callLLM
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/rumination.js','utf8');if(!c.includes('cleanNotebookResponse(nbResult.text.trim())'))process.exit(1)"

- [x] [GATE] cecelia_events INSERT 使用 `$1::jsonb` 显式类型转换
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/rumination.js','utf8');if(!c.includes('$1::jsonb'))process.exit(1)"
