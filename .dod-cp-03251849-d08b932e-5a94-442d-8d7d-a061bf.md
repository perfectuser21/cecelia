# DoD: 修复 rumination 链路 — NotebookLM 响应清洗

## 验收条件

- [ ] `cleanNotebookResponse()` 函数存在于 `packages/brain/src/rumination.js`
- [ ] `digestLearnings()` 在 NotebookLM 响应后调用 `cleanNotebookResponse()` 清洗
- [ ] 清洗后 insight 长度 < 50 时 fallback 到 callLLM
- [ ] cecelia_events INSERT 使用对象参数（非 JSON.stringify）
- [ ] 所有现有 rumination 测试通过
