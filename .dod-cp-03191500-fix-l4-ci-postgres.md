# DoD: 修复 L4 CI PostgreSQL 连接失败

## 验收条件

- [ ] [BEHAVIOR] `ci-l4-runtime.yml` 在 migrate 步骤前有 `pg_isready` 循环等待步骤
  - Test: `manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes('pg_isready'))process.exit(1)"`

- [ ] [BEHAVIOR] migrate 步骤显式设置 `DB_PORT` 环境变量
  - Test: `manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');const m=c.indexOf('Run migrations');const s=c.substring(m-200,m);if(!s.includes('DB_PORT'))process.exit(1)"`

- [ ] [BEHAVIOR] DB_PORT 为空时有防御性重新计算逻辑
  - Test: `manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes('Recalculating from GITHUB_RUN_ID'))process.exit(1)"`

- [ ] [BEHAVIOR] 测试步骤显式传递 DB_PORT
  - Test: `manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');const t=c.indexOf('Run Brain tests');const s=c.substring(t-200,t+50);if(!s.includes('DB_PORT'))process.exit(1)"`

- [ ] ipcs 清理脚本兼容 macOS/Linux
  - Test: `manual: node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes('ipcs') || !c.includes('darwin'))process.exit(1)"`
