# DoD: 修复 L4 CI PostgreSQL 连接失败

- [x] [BEHAVIOR] ci-l4-runtime.yml 在 migrate 步骤前有 pg_isready 循环等待
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes('pg_isready'))process.exit(1)"
- [x] [BEHAVIOR] migrate 步骤显式设置 DB_PORT 环境变量
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8');if(!c.includes('DB_PORT'))process.exit(1)"
- [x] [GATE] YAML 语法正确
  Test: manual:node -e "require('fs').readFileSync('.github/workflows/ci-l4-runtime.yml','utf8')"
