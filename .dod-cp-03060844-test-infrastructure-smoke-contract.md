# DoD - Brain 测试加固 Phase 1: Smoke Test + Contract Test

## 验收标准

### Smoke Test (1 file)
- [x] Brain 启动后 GET / 返回 200 且 service=cecelia-brain
      Test: manual:cd packages/brain && npx vitest run src/__tests__/smoke.test.js --reporter=verbose 2>&1 | grep -q "Brain Smoke Test"
- [x] Brain 启动后 GET /api/brain/health 返回 200 且有 status 字段
      Test: manual:node -e "const f=require('fs'); const c=f.readFileSync('packages/brain/src/__tests__/smoke.test.js','utf8'); process.exit(c.includes('api/brain/health') ? 0 : 1)"
- [x] Migration 在测试 DB 上成功执行（不 crash）
      Test: manual:node -e "const f=require('fs'); const c=f.readFileSync('packages/brain/src/__tests__/smoke.test.js','utf8'); process.exit(c.includes('migration succeeded') ? 0 : 1)"

### Contract Tests (6 files)
- [x] planner.js 契约：applyContentAwareScore 返回数组，selectTopAreas 返回数组
      Test: manual:cd packages/brain && npx vitest run src/__tests__/contracts/planner-contract.test.js --reporter=verbose 2>&1 | grep -q "passed"
- [x] executor.js 契约：getSkillForTaskType 返回字符串，generateRunId 返回 UUID
      Test: manual:cd packages/brain && npx vitest run src/__tests__/contracts/executor-contract.test.js --reporter=verbose 2>&1 | grep -q "passed"
- [x] task-router.js 契约：identifyWorkType 返回字符串，getTaskLocation 返回 'us'|'hk'
      Test: manual:cd packages/brain && npx vitest run src/__tests__/contracts/task-router-contract.test.js --reporter=verbose 2>&1 | grep -q "passed"
- [x] decision.js 契约：splitActionsBySafety 返回 {safeActions, unsafeActions}，SAFE_ACTIONS 是 Set
      Test: manual:cd packages/brain && npx vitest run src/__tests__/contracts/decision-contract.test.js --reporter=verbose 2>&1 | grep -q "passed"
- [x] initiative-closer.js 契约：getMaxActiveInitiatives 返回数字，MAX_ACTIVE_INITIATIVES 是数字
      Test: manual:cd packages/brain && npx vitest run src/__tests__/contracts/initiative-closer-contract.test.js --reporter=verbose 2>&1 | grep -q "passed"
- [x] memory-retriever.js 契约：classifyQueryIntent 返回意图字符串，computeTopicDepth 返回 0-2
      Test: manual:cd packages/brain && npx vitest run src/__tests__/contracts/memory-retriever-contract.test.js --reporter=verbose 2>&1 | grep -q "passed"

### 裸奔模块最小测试 (16 files)
- [x] 16 个零测试模块各有测试文件，验证 import 成功 + 主导出函数存在，全部通过
      Test: manual:cd packages/brain && npx vitest run src/__tests__/bare-modules/ --reporter=verbose 2>&1 | grep -q "16 passed"

### 覆盖率阈值
- [x] vitest.config.js 覆盖率阈值提升到 statements: 20, branches: 10
      Test: manual:grep -q "statements: 20" packages/brain/vitest.config.js && grep -q "branches: 10" packages/brain/vitest.config.js

### 测试验收
- [x] npm test 在 packages/brain 下全部通过（含新测试）
      Test: manual:cd packages/brain && npm test 2>&1 | grep -q "passed"

<!-- CI trigger -->
