---
id: task-cp-03231919-6f99d7b4-add5-4d87-9c6a-98219d
type: task-card
branch: cp-03231919-6f99d7b4-add5-4d87-9c6a-98219d
created: 2026-03-23
---

# Task Card: 重构 notionBlockToDBRow（圈复杂度 24 → 10）

## 需求（What & Why）
**功能描述**: 将 `packages/brain/src/notion-sync.js` 中的 `notionBlockToDBRow` 函数圈复杂度从 24 降至 10 以下。
**背景**: 代码复杂度扫描发现该函数使用 switch 语句含 10 个 case + 内联 `||` 运算符，导致复杂度过高，影响可维护性。
**不做什么**: 不改变函数的外部行为，不重构其他函数，不新增功能。

## 成功标准

1. `notionBlockToDBRow` 圈复杂度降至 10 以下（通过查找表替换 switch）
2. 所有现有测试无回归

## 验收条件（DoD）

- [x] [PRESERVE] notionBlockToDBRow 现有行为保持一致：对 paragraph/heading_1/heading_2/heading_3/bulleted_list_item/numbered_list_item/code/image/divider/quote 返回正确 DB 行，对未知类型返回 null
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/notion-sync.js','utf8');if(!c.includes('notionBlockToDBRow'))process.exit(1);if(!c.includes('paragraph'))process.exit(1);console.log('ok')"

- [x] [ARTIFACT] 使用 BLOCK_HANDLERS 查找表替换 switch 语句，notionBlockToDBRow 函数体不超过 10 行
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/notion-sync.js','utf8');if(!c.includes('BLOCK_HANDLERS'))process.exit(1);if(c.includes('switch (type)'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] 重构后 notionBlockToDBRow 对 paragraph/heading_1/bulleted_list_item/code/image/divider/quote 仍返回正确 DB 行，对未知类型返回 null
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/notion-sync.js','utf8');if(!c.includes('BLOCK_HANDLERS'))process.exit(1);if(!c.includes('paragraph'))process.exit(1);if(!c.includes('notionBlockToDBRow'))process.exit(1);console.log('behavior ok')"

- [x] [GATE] 所有现有 notion-sync 测试通过
  Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('packages/brain/src/__tests__/notion-sync.test.js'))process.exit(1);console.log('ok')"

