---
id: task-cp-03171709-librarian-p0-batch1
type: task-card
branch: cp-03171709-cd37641f-b9e4-45c8-bc7b-22ab48
created: 2026-03-17
---

# Task Card: 知识库 BACKLOG.yaml 全量同步

## 需求（What & Why）
**功能描述**: 同步 BACKLOG.yaml 状态，将所有已生成 HTML 知识页的条目从 todo/in_progress 更新为 done，更新 meta.done 计数和 output_url。
**背景**: 首批5个P0知识单元（brain-quarantine, engine-dev-step0/1/2/3）的 HTML 文件已由前几次运行生成，BACKLOG.yaml 已标记为 done。同时，其他代理并行生成了大量额外 HTML 页（~60多个），BACKLOG.yaml 状态未同步。
**不做什么**: 不重新生成已存在的 HTML 文件；不修改 HTML 内容；不处理 claude-output/ 以外的文件。

## 成功标准
> [ARTIFACT] 产出物 / [BEHAVIOR] 运行时行为 / [GATE] 门禁

1. [ARTIFACT] BACKLOG.yaml 中所有已有 HTML 文件的条目 status 更新为 done
2. [ARTIFACT] meta.done 计数反映实际完成数量
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] brain 模块所有有 HTML 文件的条目 status=done（watchdog/self-healing/database等）
  Test: manual:node -e "const y=require('js-yaml');const d=y.load(require('fs').readFileSync('docs/knowledge/BACKLOG.yaml','utf8'));const done=d.brain.filter(i=>i.status==='done').length;console.log('brain done:',done);if(done<6)process.exit(1)"

- [x] [ARTIFACT] engine 模块所有有 HTML 文件的条目 status=done（dev-step4/hooks/devgate/ci-l1-4）
  Test: manual:node -e "const y=require('js-yaml');const d=y.load(require('fs').readFileSync('docs/knowledge/BACKLOG.yaml','utf8'));const done=d.engine.filter(i=>i.status==='done').length;console.log('engine done:',done);if(done<5)process.exit(1)"

- [x] [ARTIFACT] workflows/skills 模块所有有 HTML 文件的条目 status=done
  Test: manual:node -e "const y=require('js-yaml');const d=y.load(require('fs').readFileSync('docs/knowledge/BACKLOG.yaml','utf8'));const done=d.workflows.filter(i=>i.status==='done').length;console.log('workflows done:',done);if(done<3)process.exit(1)"

- [x] [ARTIFACT] meta.done 大于等于 70（大量条目已完成）
  Test: manual:node -e "const y=require('js-yaml');const d=y.load(require('fs').readFileSync('docs/knowledge/BACKLOG.yaml','utf8'));console.log('meta.done:',d.meta.done);if(d.meta.done<70)process.exit(1)"

- [x] [BEHAVIOR] BACKLOG.yaml 可被 YAML 解析器正确读取（格式无错误）
  Test: manual:node -e "const yaml=require('js-yaml');yaml.load(require('fs').readFileSync('docs/knowledge/BACKLOG.yaml','utf8'));console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案（Step 2 探索后填写）
**要改的文件**: docs/knowledge/BACKLOG.yaml
**Scope 锚定**: 只更新 status/output_url/completed 字段，不修改 id/title/desc/source_files
