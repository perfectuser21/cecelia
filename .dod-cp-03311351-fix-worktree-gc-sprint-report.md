# DoD: 修复 worktree GC 误删 + stop-dev 过早删锁 + Sprint Report 误导输出

**branch**: cp-03311351-fix-worktree-gc-sprint-report

## 验收条件

- [x] [ARTIFACT] worktree-gc.sh 增加 `.dev-mode` 文件守卫，存在且不含 `cleanup_done: true` 时跳过删除
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/scripts/worktree-gc.sh','utf8');if(!c.includes('.dev-mode'))process.exit(1);if(!c.includes('cleanup_done: true'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] stop-dev.sh 所有删除 `.dev-session-active` 的路径被守卫条件包裹，仅在 `.dev-mode` 含 `cleanup_done: true` 时执行
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('cleanup_done: true'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] generate-sprint-report.sh seal 缺失分支输出「⚠️ 无法检测（Seal 文件缺失）」且评分为 0
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/scripts/devgate/generate-sprint-report.sh','utf8');if(!c.includes('无法检测'))process.exit(1);if(!c.includes('Seal 文件缺失'))process.exit(1);if(!c.includes('SCORE_PLANNER=0'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] GC 执行后，含有未完成 `.dev-mode` 的 worktree 不被删除（即使 `.dev-session-active` 缺失）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/scripts/worktree-gc.sh','utf8');if(!c.includes('.dev-mode'))process.exit(1);if(!c.includes('cleanup_done: true'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] context 超限後 `.dev-session-active` 保持存在，直到 Stage 4 完成（cleanup_done: true）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('cleanup_done: true'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] Sprint Report seal 缺失时 Planner 隔离项显示「⚠️ 无法检测（Seal 文件缺失）」且得分为 0
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/scripts/devgate/generate-sprint-report.sh','utf8');if(!c.includes('无法检测'))process.exit(1);if(!c.includes('SCORE_PLANNER=0'))process.exit(1);console.log('OK')"

- [x] [GATE] 所有现有测试通过，Engine 版本 bump 同步到 5 个文件
  Test: manual:node -e "const fs=require('fs');const ver=fs.readFileSync('packages/engine/VERSION','utf8').trim();const pkg=JSON.parse(fs.readFileSync('packages/engine/package.json','utf8'));const hook=fs.readFileSync('packages/engine/.hook-core-version','utf8').trim();const lock=JSON.parse(fs.readFileSync('packages/engine/package-lock.json','utf8'));const rc=fs.readFileSync('packages/engine/regression-contract.yaml','utf8');if(pkg.version!==ver||hook!==ver||lock.version!==ver||!rc.includes(ver))process.exit(1);console.log('OK:',ver)"
