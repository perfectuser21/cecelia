## DoD - fix(selfcheck): 版本检查改为 >= 防止 migration 崩溃循环

- [x] selfcheck.js 版本比较改为 >= (parseInt)
  Test: manual:bash -c "grep -q 'parseInt(maxVer' packages/brain/src/selfcheck.js"

- [x] EXPECTED_SCHEMA_VERSION 保持与最高迁移文件号一致（141）
  Test: manual:bash -c "grep -q \"EXPECTED_SCHEMA_VERSION = '141'\" packages/brain/src/selfcheck.js"

- [x] selfcheck.test.js 新增 DB 版本高于预期时仍 PASS 的测试
  Test: manual:bash -c "grep -q 'should pass when schema version is ahead' packages/brain/src/__tests__/selfcheck.test.js"

- [x] DEFINITION.md 描述更新为 >= 语义
  Test: manual:bash -c "grep -q 'DB 版本 >=' DEFINITION.md"
