# PRD: 启动每日AI内容生成调度引擎

## 背景
接通每日AI内容生成调度引擎最后一公里：注册 content-pipeline API 路由、创建 topic_selection_log 数据库迁移、补全选题手动触发接口。

## 功能描述
1. 注册 content-pipeline 路由到 routes.js（/api/brain/pipelines + /api/brain/content-types）
2. 创建 migration 203：topic_selection_log 表（7日选题历史去重）
3. 新增 POST /api/brain/pipelines/trigger-topics 手动触发端点
4. 更新 selfcheck.js EXPECTED_SCHEMA_VERSION 到 '203'

## 成功标准
- `GET /api/brain/content-types` 不再 404
- `GET /api/brain/pipelines` 不再 404
- `POST /api/brain/pipelines/trigger-topics` 可手动触发选题
- migration 203 执行后 topic_selection_log 表存在
- CI 全部通过
