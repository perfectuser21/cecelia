---
id: prd-update-schema-version-031
version: 1.0.0
created: 2026-02-14
---

# PRD: 更新 EXPECTED_SCHEMA_VERSION 到 031

## 需求来源

Migration 031 已应用到数据库（给 capabilities 表添加 vector embedding 支持），但代码中的 EXPECTED_SCHEMA_VERSION 还是 '030'，导致版本不匹配。

## 功能描述

更新 `brain/src/selfcheck.js` 中的 EXPECTED_SCHEMA_VERSION 从 '030' 改为 '031'，使其与数据库实际的 schema_version 一致。

## 涉及文件

- `brain/src/selfcheck.js` - 修改第 18 行

## 成功标准

- [ ] EXPECTED_SCHEMA_VERSION = '031'
- [ ] selfcheck 不报错（数据库版本 = 代码版本）
- [ ] CI 通过

## 非目标

- 不生成 embeddings（等 OpenAI 配额恢复后再处理）
- 不修改 Migration 031 文件（已经应用）
