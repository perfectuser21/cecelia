---
id: task-cp-04062246-fix-eslint-hard-gate
type: task-card
branch: cp-04062246-fix-eslint-hard-gate
created: 2026-04-06
---

# Task Card: 修复 ESLint 硬检查

## 需求
Brain 的 `eslint.config.mjs` 手动声明了 `console`/`process` globals，但 `js.configs.recommended` 里已将它们视为内置全局，导致 `no-redeclare` 72个 error。CI 里 ESLint 步骤有 `continue-on-error: true`，等于摆设。

## 成功标准
ESLint 对 Brain src/ 0 errors，然后去掉 `continue-on-error`，CI ESLint 硬卡。

## 验收条件（DoD）

- [x] [BEHAVIOR] Brain src/ ESLint errors 为 0
  Test: manual:bash -c "cd packages/brain && npx eslint src/ 2>&1 | tail -1 | grep -q '0 errors' && echo ok"

- [x] [ARTIFACT] ci.yml ESLint 步骤无 continue-on-error
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(c.includes('continue-on-error'))process.exit(1);console.log('ok')"

## 实现方案
1. `packages/brain/eslint.config.mjs`: 用 `globals` 包替换手动声明，消灭 no-redeclare
2. `no-case-declarations` 降为 `warn`（10个真实代码问题，不阻断）
3. `.github/workflows/ci.yml`: 删除两处 `continue-on-error: true`
