# PRD: 重构 reflection.runReflection（圈复杂度 39 → <10）

## 背景

代码复杂度扫描发现 `packages/brain/src/desire/reflection.js` 中 `runReflection` 函数圈复杂度过高（39），超过阈值 10，需重构降低复杂度。

## 功能描述

通过提取安全包装函数，将 `runReflection` 内的 try/catch 分支移出到独立函数，使主函数只保留业务逻辑判断（5 个 if 分支），圈复杂度降至 6。

## 成功标准

- `runReflection` 函数体内 if/else/catch/for/while 关键字 < 15 个
- 导出函数签名 `export async function runReflection(pool)` 保持不变
- Brain 单元测试全部通过
