## DoD - Hook Gates 真锁实现

### 验收清单

- [x] bash-guard.sh 新增 git push 拦截规则：检测 `git push` 命令 → 运行 local-precheck.sh → 失败时 exit 2 阻止
  Test: manual:bash -c "grep -c 'local-precheck' hooks/bash-guard.sh"

- [x] bash-guard.sh 新增 git commit 消息格式验证：检测 `-m` 参数 → 验证 Conventional Commits 格式 → 不符合时 exit 2
  Test: manual:bash -c "grep -c 'VALID_PREFIXES' hooks/bash-guard.sh"

- [x] stop-dev.sh 在 STEP_10 flag=done 时额外运行 check-learning.sh 验证内容
  Test: manual:bash -c "grep -c 'check-learning' hooks/stop-dev.sh"

- [x] branch-protect.sh Write .prd-*.md 时验证内容包含 ## 成功标准 章节
  Test: manual:bash -c "grep -c 'PRD_CONTENT_VALID' hooks/branch-protect.sh"

- [x] branch-protect.sh Write .dod-*.md 时验证内容包含 - [ ] checkbox 格式
  Test: manual:bash -c "grep -c 'DOD_CONTENT_VALID' hooks/branch-protect.sh"

- [x] 所有新规则遇到脚本缺失时降级放行（不阻止正常开发流程）
  Test: manual:bash -c "grep -c 'PRECHECK_SCRIPT' hooks/bash-guard.sh"

- [x] Hook VERSION 文件版本号 bump（记录本次变更）
  Test: manual:bash -c "cat hooks/VERSION"
