# DoD: stop-dev.sh worktree 修复 + 垃圾清理

## 成功标准
1. [BEHAVIOR] stop-dev.sh 扫描 worktree 目录
2. [ARTIFACT] 垃圾文件已删除
3. [GATE] CI 全通过

## 验收条件（DoD）

- [x] [BEHAVIOR] stop-dev.sh 包含 worktree 扫描函数
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('worktree list --porcelain'))process.exit(1)"

- [x] [BEHAVIOR] stop-dev.sh 会话预检查扫描 worktree
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('_find_all_dev_locks'))process.exit(1)"

- [x] [ARTIFACT] 历史残留 .prd-*/.dod-*/.task-* 文件已删除
  Test: manual:node -e "const fs=require('fs');const files=fs.readdirSync('.').filter(f=>f.startsWith('.prd-cp-03')||f.startsWith('.dod-cp-03')||f.startsWith('.task-cp-03'));if(files.length>5)process.exit(1)"

- [x] [GATE] stop-dev.sh 语法正确
  Test: manual:bash -c "bash -n packages/engine/hooks/stop-dev.sh"
