## DoD - Dashboard 48h 系统简报展示

### 验收清单

- [ ] **Brain API 端点实现** | Test: `curl -s localhost:5221/api/brain/reports` 返回 JSON 数组（空或有数据）
- [ ] **Brain API 详情端点** | Test: `curl -s localhost:5221/api/brain/reports/<id>` 返回 JSON 对象或 404
- [ ] **Routes 路由文件创建** | Test: `packages/brain/src/routes/reports.js` 文件存在且语法正确
- [ ] **Server.js 路由注册** | Test: server.js 中有 `/api/brain/reports` 路由挂载
- [ ] **Dashboard 简报列表页** | Test: `apps/dashboard/src/pages/reports/ReportsListPage.tsx` 存在
- [ ] **Dashboard 简报详情页** | Test: `apps/dashboard/src/pages/reports/ReportDetailPage.tsx` 存在
- [ ] **Feature Manifest 路由配置** | Test: system-hub 的 manifest 包含 `/reports` 和 `/reports/:id` 路由
- [ ] **Feature Manifest pageComponents** | Test: manifest.pageComponents 包含 `ReportsListPage` 和 `ReportDetailPage` 组件映射
- [ ] **端到端测试** | Test: 手动触发 tick，验证 system_reports 表有记录（或说明表已存在但 tick 还未实现写入逻辑）
- [ ] **CI 通过** | Test: GitHub Actions 全绿

### 实现计划

1. `packages/brain/src/routes/reports.js` - 新建路由文件
2. `packages/brain/server.js` - 注册路由
3. `apps/dashboard/src/pages/reports/ReportsListPage.tsx` - 简报列表页
4. `apps/dashboard/src/pages/reports/ReportDetailPage.tsx` - 简报详情页
5. `apps/api/features/system-hub/index.ts` - 添加路由和页面组件
