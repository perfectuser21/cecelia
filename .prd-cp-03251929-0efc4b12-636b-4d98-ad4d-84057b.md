# PRD: OKR 全树视图

## 功能描述
新增 GET /api/brain/okr/tree API，返回 objectives→key_results→projects→scopes→initiatives→tasks 完整嵌套结构。前端新增 /okr/tree 页面，可展开/收起每层节点，点击跳转详情。复用现有表，不改 schema。

## 成功标准
- GET /api/brain/okr/tree 返回嵌套至 tasks 层的完整结构
- /okr/tree 页面可访问，显示完整层级树，各层节点可展开/收起
- 点击 Project 节点跳转 /projects/:id，点击 Initiative 节点跳转 /initiatives/:id
- CI 全部通过

## Brain Task ID
0efc4b12-636b-4d98-ad4d-84057bad7aa7
