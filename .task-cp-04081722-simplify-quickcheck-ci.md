---
id: task-cp-04081722-simplify-quickcheck-ci
type: task-card
branch: cp-04081722-simplify-quickcheck-ci
created: 2026-04-08
---

# Task Card: 简化 quickcheck.sh + 补全 CI apps 测试

## 需求
**功能描述**:
1. quickcheck.sh 300行 scope 逻辑 → 20行，覆盖全部4个包
2. ci.yml 补全 apps/api + apps/dashboard 的 vitest

**背景**: quickcheck 只覆盖 engine/brain，apps/ 两层全盲。CI 也一样。
**不做什么**: 不改 CI 其他 job，不改 deploy 逻辑

## 验收条件（DoD）

- [x] [ARTIFACT] quickcheck.sh 行数 < 80
  Test: manual:bash -c "wc -l < scripts/quickcheck.sh | xargs -I{} [ {} -lt 80 ] && echo OK"

- [x] [BEHAVIOR] quickcheck.sh 改动 apps/api 文件时触发 apps/api 测试
  Test: manual:node -e "const c=require('fs').readFileSync('scripts/quickcheck.sh','utf8');if(!c.includes('apps/api'))process.exit(1)"

- [x] [BEHAVIOR] quickcheck.sh 改动 apps/dashboard 文件时触发 apps/dashboard 测试
  Test: manual:node -e "const c=require('fs').readFileSync('scripts/quickcheck.sh','utf8');if(!c.includes('apps/dashboard'))process.exit(1)"

- [x] [ARTIFACT] ci.yml 包含 apps/api npm test
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(!c.includes('apps/api') || !c.includes('npm test'))process.exit(1)"

- [x] [ARTIFACT] ci.yml 包含 apps/dashboard vitest
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci.yml','utf8');if(!c.includes('apps/dashboard') || !c.includes('vitest'))process.exit(1)"

## 实现方案
**文件**: scripts/quickcheck.sh, .github/workflows/ci.yml
