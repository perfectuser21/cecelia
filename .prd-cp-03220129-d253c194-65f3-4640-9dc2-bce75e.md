# PRD: 重构 auto-learning.extractTaskSummary（圈复杂度 21 → 10）

## 背景

代码复杂度扫描发现 `packages/brain/src/auto-learning.js` 中的 `extractTaskSummary` 函数圈复杂度为 21，超过阈值 10，需要重构降低复杂度。

## 目标

将 `extractTaskSummary` 函数圈复杂度从 21 降至 10 以下，同时保持所有现有行为不变。

## 方案

提取子函数：
- `_extractRunResult` — 处理 cecelia-run 合成结果（exit_code/stderr_tail/failure_class）
- `_extractObjectSummary` — 处理通用对象摘要

## 成功标准

- extractTaskSummary 圈复杂度 < 10
- npm test 全部通过
