---
id: task-cp-03252044-gtdokr-fix
type: task-card
branch: cp-03252044-37e2f5a5-adbf-4223-9dfb-211c65
created: 2026-03-26
---

# Task Card: 修复 GTDOkr 等组件空数据

## 需求（What & Why）
**功能描述**: GTDOkr/GTDProjects/GTDArea 四个 GTD 组件因本地 DB 缺表而无法加载数据，需代理到 Brain API。
**背景**: apps/api task-system 中 goals/projects 表不存在，areas 表缺 sort_order 列，且 routes.js 未注册 /areas 路由。Brain DB（5221）已有真实数据。
**不做什么**: 不修改 GTD 前端组件；不修改 Brain 的 routes；不迁移 DB schema。

## 成功标准

1. [BEHAVIOR] GET /api/tasks/goals 返回 200 + goals 数组（来自 Brain），`area_kr` type 映射为 `kr`
2. [BEHAVIOR] GET /api/tasks/projects 返回 200 + projects 数组（来自 Brain），含 name/type/status/priority 字段
3. [BEHAVIOR] GET /api/tasks/areas 返回 200 + areas 数组（来自本地 DB，无 sort_order 错误）
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [ ] [BEHAVIOR] /api/tasks/goals 返回非空数组，type 字段中 area_kr 已映射为 kr
  Test: manual:node -e "fetch('http://localhost:5211/api/tasks/goals').then(r=>r.json()).then(d=>{if(!Array.isArray(d)||d.length===0)process.exit(1);const bad=d.find(g=>g.type==='area_kr');if(bad)process.exit(1)}).catch(()=>process.exit(1))"

- [ ] [BEHAVIOR] /api/tasks/projects 返回非空数组，每项含 name 字段
  Test: manual:node -e "fetch('http://localhost:5211/api/tasks/projects').then(r=>r.json()).then(d=>{if(!Array.isArray(d)||d.length===0)process.exit(1);if(!d[0].name)process.exit(1)}).catch(()=>process.exit(1))"

- [ ] [BEHAVIOR] /api/tasks/areas 返回 200 JSON 数组（不再返回 HTML）
  Test: manual:node -e "fetch('http://localhost:5211/api/tasks/areas').then(r=>{if(!r.ok)process.exit(1);return r.json()}).then(d=>{if(!Array.isArray(d))process.exit(1)}).catch(()=>process.exit(1))"

- [ ] [ARTIFACT] goals.js GET / 改为代理 Brain /api/brain/goals
  Test: node -e "const c=require('fs').readFileSync('apps/api/src/task-system/goals.js','utf8');if(!c.includes('brain/goals'))process.exit(1)"

- [ ] [ARTIFACT] projects.js GET / 改为代理 Brain /api/brain/projects
  Test: node -e "const c=require('fs').readFileSync('apps/api/src/task-system/projects.js','utf8');if(!c.includes('brain/projects'))process.exit(1)"

- [ ] [ARTIFACT] routes.js 注册 /areas 路由
  Test: node -e "const c=require('fs').readFileSync('apps/api/src/task-system/routes.js','utf8');if(!c.includes(\"'/areas'\"))process.exit(1)"

- [ ] [ARTIFACT] areas.js GET / 不再使用 sort_order
  Test: node -e "const c=require('fs').readFileSync('apps/api/src/task-system/areas.js','utf8');if(c.includes('sort_order'))process.exit(1)"

- [ ] [GATE] 所有现有测试通过
  Test: manual:npm test --workspace=apps/api

## 实现方案

**要改的文件**:
- `apps/api/src/task-system/goals.js` — GET / 改为 fetch Brain /api/brain/goals
- `apps/api/src/task-system/projects.js` — GET / 改为 fetch Brain /api/brain/projects
- `apps/api/src/task-system/areas.js` — 删除 ORDER BY sort_order，改为 ORDER BY created_at
- `apps/api/src/task-system/routes.js` — import areasRouter + router.use('/areas', areasRouter)

**数据映射**:
- goals: type `area_kr` → `kr`; 补充 priority='P2', weight=1.0（Brain 不返回这些字段）
- projects: title → name; type='initiative'; 补充 priority='P2', parent_id=null, execution_mode=null（Brain 有 progress 字段）
