---
id: dod-dev-step10-learning-db
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

## DoD - /dev Step 10 重写：分类写入 learnings DB

### 验收清单

- [x] Migration 151 已包含 learning_type、source_branch、source_pr、repo 字段
  Test: manual:bash -c "grep -c 'learning_type' packages/brain/migrations/151_learnings_type.sql"

- [ ] learnings-received 端点在 INSERT 时写入 learning_type（关键词分类）
  Test: manual:bash -c "grep -c 'learning_type' packages/brain/src/routes.js"

- [ ] learnings-received 端点在 INSERT 时写入 source_branch（来自 branch_name 参数）
  Test: manual:bash -c "grep -c 'source_branch' packages/brain/src/routes.js"

- [ ] learnings-received 端点在 INSERT 时写入 source_pr（来自 pr_number 参数）
  Test: manual:bash -c "grep -c 'source_pr' packages/brain/src/routes.js"

- [ ] classifyLearningType() 函数实现 5 种分类（trap/architecture_decision/process_improvement/failure_pattern/best_practice）
  Test: manual:bash -c "grep -c 'classifyLearningType' packages/brain/src/routes.js"

- [ ] 调用 learnings-received API 后，DB 中有包含 learning_type 的记录（集成验证）
  Test: manual:bash -c "curl -s -X POST http://localhost:5221/api/brain/learnings-received -H 'Content-Type: application/json' -d '{\"next_steps_suggested\":[\"避免陷阱：mock existsSync 顺序\"],\"branch_name\":\"test-branch\",\"pr_number\":\"999\"}' | grep -c 'success.*true'"

- [ ] 现有 docs/learnings/<branch>.md 文件仍然生成（MD 文件写入逻辑不变）
  Test: manual:bash -c "grep -c 'docs/learnings' packages/engine/skills/dev/steps/10-learning.md"

- [ ] Brain API 不可达时降级（只写 MD），不中断 Step 10
  Test: manual:bash -c "grep -c 'true' packages/engine/skills/dev/scripts/fire-learnings-event.sh"

- [ ] 10-learning.md skill 文件更新：说明 DB 写入路径（learning_type 自动分类）
  Test: manual:bash -c "grep -c 'learning_type' packages/engine/skills/dev/steps/10-learning.md"
