# DoD: fix-brain-test-baseline

## 验收条目

- [ ] [ARTIFACT] brain-test-baseline.txt 值为 0
  - Test: `node -e "const v=require('fs').readFileSync('packages/brain/ci/brain-test-baseline.txt','utf8').trim();if(v!=='0')process.exit(1);"`
  - Expected: exit 0

- [ ] [BEHAVIOR] baseline 收紧后 CI 不容忍假失败
  - Test: `manual: node -e "const v=require('fs').readFileSync('packages/brain/ci/brain-test-baseline.txt','utf8').trim();if(parseInt(v)>0){console.error('baseline过松:'+v);process.exit(1);}console.log('baseline='+v);"`
  - Expected: 输出 baseline=0，exit 0
