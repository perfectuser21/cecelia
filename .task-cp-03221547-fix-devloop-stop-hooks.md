---
id: task-cp-03221547-fix-devloop-stop-hooks
type: task-card
branch: cp-03221547-fix-devloop-stop-hooks
created: 2026-03-22
brain_task_id: 84c967fd-43b9-4c6b-88a8-9ceb720d7f94
---

# Task Card: 修复 devloop-check.sh + stop hooks runtime bug

## 需求（What & Why）

**功能描述**: 修复 /dev pipeline runtime 层的 4 个 P0/P1 问题，保证 devloop-check.sh 和 stop-dev.sh 在所有场景下正确工作。

**背景**:
- devloop-check.sh:305 cleanup 失败后无 retry → 直接 return 2 而不触发重试，导致 PR 合并后工作流卡死
- stop-dev.sh:28 `_collect_search_dirs` 在 worktree 内调用时先搜 worktree，主仓库的 .dev-lock 在主仓库根目录，顺序应先主仓库再 worktree
- stop-dev.sh:38 有一段垃圾注释（"无头模式不再旁路"），后面没有对应代码，注释孤立
- stop-dev.sh:106 TTY 匹配条件判断 `_pre_lock_tty` 是否非空，应改为检查非空而非用正则
- stop-dev.sh:193 LEGACY 兼容代码（旧字段映射）没有删除日期注释，永远不会被清理
- devloop-check.sh 顶部 changelog 的「删除Codex」表述不准确，应改为「改为Agent subagent同步调用」

**不做什么**:
- 不改 step 文件（01-spec.md / 02-code.md 等）
- 不改 verify-step.sh
- 不加新功能

## 成功标准

1. [ARTIFACT] devloop-check.sh:305 cleanup 失败后返回 `return 2` 前有 retry 机制（exit 2 触发重新调度）
2. [ARTIFACT] stop-dev.sh `_collect_search_dirs` 先搜主仓库目录，再搜 worktree 目录
3. [ARTIFACT] stop-dev.sh:38 垃圾注释已删除
4. [ARTIFACT] stop-dev.sh:106 TTY 匹配改为检查非空（去掉不必要条件或改写逻辑）
5. [ARTIFACT] stop-dev.sh:193 LEGACY 代码注释加上删除日期（如 `# LEGACY: 可在 2026-07-01 后删除`）
6. [ARTIFACT] devloop-check.sh 顶部 changelog 已更新为「改为Agent subagent同步调用」
7. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] devloop-check.sh cleanup 失败路径：`(cd ... && bash cleanup.sh ...) || true` 后面加 `return 2` 确保强制重试
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('cleanup 失败，exit 2 重试'))process.exit(1)"

- [x] [ARTIFACT] stop-dev.sh `_collect_search_dirs` 先获取主仓库根目录再搜 worktree
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('_main_root'))process.exit(1)"

- [x] [ARTIFACT] stop-dev.sh:38 垃圾注释（无头模式：不再旁路）已删除
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(c.includes('不再旁路'))process.exit(1)"

- [x] [ARTIFACT] stop-dev.sh LEGACY 兼容代码注释含删除日期
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');if(!c.includes('2026-'))process.exit(1)"

- [x] [ARTIFACT] devloop-check.sh changelog 包含「改为 Agent subagent 同步调用」
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('改为 Agent subagent 同步调用'))process.exit(1)"

- [x] [BEHAVIOR] stop-dev.sh `_collect_search_dirs` 函数内先获取主仓库路径（_main_root 变量名）再搜 worktree，确保主仓库 .dev-lock 不被跳过
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/stop-dev.sh','utf8');const fn=c.match(/(_collect_search_dirs\(\)[^\n]*\n)([\s\S]*?)^\}/m)?.[0]||c;if(!fn.includes('_main_root')||!fn.includes('_main_root'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/engine && npm test 2>&1; [ $? -eq 0 ]"

## 实现方案

**要改的文件**:
1. `packages/engine/lib/devloop-check.sh` — cleanup 失败 retry + changelog 更新
2. `packages/engine/hooks/stop-dev.sh` — _collect_search_dirs 顺序 + 删垃圾注释 + TTY 逻辑 + LEGACY 日期

**Scope 锚定**:
- devloop-check.sh: 约 305-325 行（cleanup 失败路径）+ 顶部 changelog（约 17-19 行）
- stop-dev.sh: 约 26-36 行（_collect_search_dirs）+ 38-42 行（垃圾注释）+ 106 行（TTY）+ 217 行（LEGACY）
