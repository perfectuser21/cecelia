---
id: task-cp-03311742-fc04ee9d-cddf-4b51-8605-1d0a94
type: task-card
branch: cp-03311742-fc04ee9d-cddf-4b51-8605-1d0a94
created: 2026-03-31
---

# Task Card: fix(escalation): cancel_pending 排除 content pipeline 子任务

## 需求（What & Why）
**功能描述**: 在 `cancelPendingTasks` 的 `task_type NOT IN` 排除列表中加入所有 content pipeline 子类型，防止 escalation 误取消 content pipeline 正在进行的子任务。

**背景**: `cancelPendingTasks` 会取消所有 queued/pending 任务，但排除列表只含 `research` 和 `suggestion_plan`，缺少 content pipeline 相关子类型（content-research/content-copywriting/content-copy-review/content-generate/content-image-review/content-export/content-pipeline），导致 content pipeline 子任务被 escalation 误取消，pipeline 永久卡在 in_progress 状态。

**不做什么**: 不改动其他动作实现，不调整 escalation 级别逻辑。

## 成功标准

1. [ARTIFACT] `cancelPendingTasks` SQL 排除列表包含全部 7 个 content pipeline 类型
2. [BEHAVIOR] content pipeline 相关任务在 escalation emergency_brake 时不被取消
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] escalation.js cancelPendingTasks 的 NOT IN 列表包含 content-research/content-copywriting/content-copy-review/content-generate/content-image-review/content-export/content-pipeline
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/alertness/escalation.js','utf8');['content-research','content-copywriting','content-copy-review','content-generate','content-image-review','content-export','content-pipeline'].forEach(t=>{if(!c.includes(t))throw new Error('missing: '+t)});console.log('OK')"

- [x] [PRESERVE] research 和 suggestion_plan 仍在排除列表中
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/alertness/escalation.js','utf8');['research','suggestion_plan'].forEach(t=>{if(!c.includes(t))throw new Error('missing: '+t)});console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/alertness/escalation.js');console.log('file exists')"

## 实现方案
**要改的文件**: `packages/brain/src/alertness/escalation.js`
**Scope 锚定**: `cancelPendingTasks` 函数，第 344 行 `task_type NOT IN` 子句
