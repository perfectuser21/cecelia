# DoD - Brain tick 48h 简报定时检查逻辑

## 验收标准

### 功能验收
- [x] system_reports 表在 PostgreSQL 中创建成功（包含 id, report_type, content, generated_at, metadata 字段）
      Test: manual:psql $DATABASE_URL -c "SELECT column_name FROM information_schema.columns WHERE table_name='system_reports'" | grep -q 'generated_at'
- [x] tick.js 每次 executeTick() 执行时调用 checkSystemReport(now) 函数
      Test: packages/brain/src/tests/tick-48h-report.test.js
- [x] 首次运行（system_reports 表为空）时触发简报生成
      Test: packages/brain/src/tests/tick-48h-report.test.js
- [x] 距离上次生成超过 48h 时触发简报生成
      Test: packages/brain/src/tests/tick-48h-report.test.js
- [x] 距离上次生成不足 48h 时跳过简报生成
      Test: packages/brain/src/tests/tick-48h-report.test.js
- [x] cortex.generateSystemReport() 调用 LLM 并返回简报内容
      Test: packages/brain/src/tests/tick-48h-report.test.js
- [x] 生成的简报被写入 system_reports 表
      Test: manual:psql $DATABASE_URL -c "SELECT count(*) FROM system_reports WHERE report_type='system_48h'" | grep -qv ' 0'
- [x] Brain 重启后能从数据库读取上次简报时间（不重复生成）
      Test: packages/brain/src/tests/tick-48h-report.test.js
- [x] LLM 调用失败时降级处理（记录错误，不影响 tick 循环）
      Test: packages/brain/src/tests/tick-48h-report.test.js
- [x] 日志包含 [tick:48h-report] 前缀，便于追踪
      Test: packages/brain/src/tests/tick-48h-report.test.js

### 测试验收
- [x] npm test 通过
      Test: contract:C2-001
