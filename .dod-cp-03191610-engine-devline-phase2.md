# DoD: Engine Dev 线 Phase 2

- [x] [BEHAVIOR] task-router.js LOCATION_MAP 中 cto_review/code_quality_review/prd_coverage_audit 路由到 us
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/task-router.js','utf8');const m=c.match(/cto_review.*?'(us|xian|hk)'/s);if(!m||m[1]!=='us')process.exit(1);const q=c.match(/code_quality_review.*?'(us|xian|hk)'/s);if(!q||q[1]!=='us')process.exit(1);const p=c.match(/prd_coverage_audit.*?'(us|xian|hk)'/s);if(!p||p[1]!=='us')process.exit(1)"

- [x] [BEHAVIOR] devloop-check.sh 中审查条件（2.5/2.6/2.7）在 PR 创建条件之前
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');const rev=c.indexOf('条件 2.5');const pr=c.indexOf('条件 1: PR');if(rev<0||pr<0||rev>pr)process.exit(1)"

- [x] [ARTIFACT] dev SKILL.md 02-code.md 包含 /simplify 集成说明
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('simplify'))process.exit(1)"

- [x] [ARTIFACT] engine/skills 下 audit/qa/assurance 已移入 .archive
  Test: manual:node -e "const fs=require('fs');if(fs.existsSync('packages/engine/skills/audit/SKILL.md'))process.exit(1);if(fs.existsSync('packages/engine/skills/qa/SKILL.md'))process.exit(1);if(fs.existsSync('packages/engine/skills/assurance/SKILL.md'))process.exit(1)"

- [x] [GATE] Brain 测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -3"

- [x] [BEHAVIOR] DEFINITION.md 中 cto_review/code_quality_review/prd_coverage_audit 标记为 US 而非西安
  Test: manual:node -e "const c=require('fs').readFileSync('DEFINITION.md','utf8');if(c.includes('cto_review | 西安'))process.exit(1)"
