---
id: dod-cp-03142149-ci-task-format
version: 1.1.0
created: 2026-03-14
updated: 2026-03-14
changelog:
  - 1.1.0: 修复 DoD 条目状态为已验证，修复 L26 假测试
  - 1.0.0: 初始版本
---

# DoD: ci-l1-process.yml 支持 Task Card 格式

## 验收清单

- [x] cleanup-check job 新增 `.task-${GITHUB_HEAD_REF}.md` 查找逻辑
  Test: manual:bash -c "grep -c '.task-\${GITHUB_HEAD_REF}.md' .github/workflows/ci-l1-process.yml"

- [x] cleanup-check job 保留原有 `.dod-${GITHUB_HEAD_REF}.md` 和 `.dod.md` 向后兼容
  Test: manual:bash -c "grep -c '.dod-\${GITHUB_HEAD_REF}.md' .github/workflows/ci-l1-process.yml"

- [x] required-paths-check job 新增 `.task-${HEAD_REF}.md` 查找逻辑
  Test: manual:bash -c "grep -c '.task-\${HEAD_REF}.md' .github/workflows/ci-l1-process.yml"

- [x] required-paths-check job 保留原有 `.prd-${HEAD_REF}.md` 向后兼容
  Test: manual:bash -c "grep -c '.prd-\${HEAD_REF}.md' .github/workflows/ci-l1-process.yml"

- [x] CI 全部通过
  Test: manual:bash -c "grep -c 'GITHUB_HEAD_REF' .github/workflows/ci-l1-process.yml"
