---
id: dod-cp-03142149-ci-task-format
version: 1.0.0
created: 2026-03-14
updated: 2026-03-14
changelog:
  - 1.0.0: 初始版本
---

# DoD: ci-l1-process.yml 支持 Task Card 格式

## 验收清单

- [ ] cleanup-check job 新增 `.task-${GITHUB_HEAD_REF}.md` 查找逻辑
  Test: manual:bash -c "grep -c '.task-\${GITHUB_HEAD_REF}.md' .github/workflows/ci-l1-process.yml"

- [ ] cleanup-check job 保留原有 `.dod-${GITHUB_HEAD_REF}.md` 和 `.dod.md` 向后兼容
  Test: manual:bash -c "grep -c '.dod-\${GITHUB_HEAD_REF}.md' .github/workflows/ci-l1-process.yml"

- [ ] required-paths-check job 新增 `.task-${HEAD_REF}.md` 查找逻辑
  Test: manual:bash -c "grep -c '.task-\${HEAD_REF}.md' .github/workflows/ci-l1-process.yml"

- [ ] required-paths-check job 保留原有 `.prd-${HEAD_REF}.md` 向后兼容
  Test: manual:bash -c "grep -c '.prd-\${HEAD_REF}.md' .github/workflows/ci-l1-process.yml"

- [ ] CI 全部通过
  Test: manual:bash -c "echo 'CI 通过由 GitHub Actions 验证'"
