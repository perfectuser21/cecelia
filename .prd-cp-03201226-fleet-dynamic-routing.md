# PRD: 动态 Codex 并发上限 + task_type 路由硬分

## 需求
1. slot-allocator: MAX_CODEX_CONCURRENT 基于 fleet cache 动态计算
2. executor: dev→Claude Code，其他所有→Codex（选最空闲机器的 bridge）
3. task-router: 新增 isDevOnly() 判断

## 成功标准
- code_review 任务被派到西安 Codex Bridge
- dev 任务仍然走美国 Claude Code
- slot budget 的 codex.max 动态变化
