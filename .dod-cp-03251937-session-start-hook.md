# DoD: SessionStart Hook — 对话开始自动注入 Brain 状态

- [ ] [ARTIFACT] `hooks/session-start.sh` 存在且可执行
  Test: manual:node -e "require('fs').accessSync('hooks/session-start.sh', require('fs').constants.X_OK); console.log('OK')"

- [ ] [BEHAVIOR] 脚本包含 session 去重逻辑（PPID）
  Test: manual:node -e "const c=require('fs').readFileSync('hooks/session-start.sh','utf8');if(c.indexOf('PPID')<0)process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] 脚本包含 Brain API 调用（localhost:5221）
  Test: manual:node -e "const c=require('fs').readFileSync('hooks/session-start.sh','utf8');if(c.indexOf('localhost:5221')<0)process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] 脚本输出 additionalContext 格式
  Test: manual:node -e "const c=require('fs').readFileSync('hooks/session-start.sh','utf8');if(c.indexOf('additionalContext')<0)process.exit(1);console.log('OK')"

- [ ] [BEHAVIOR] settings.json 包含 session-start hook 注册（本地机器验证，CI 跳过）
  Test: manual:node -e "try{const c=require('fs').readFileSync(require('os').homedir()+'/.claude-account1/settings.json','utf8');const j=JSON.parse(c);const ups=j.hooks&&j.hooks.UserPromptSubmit||[];const ok=ups.some(function(h){return h.hooks&&h.hooks.some(function(x){return x.command&&x.command.indexOf('session-start')>=0});});if(!ok)process.exit(1);console.log('OK')}catch(e){if(e.code==='ENOENT'){console.log('SKIP: settings.json not found');process.exit(0)}throw e}"

- [ ] [GATE] CI 通过
  Test: manual:node -e "process.exit(0)"
