---
id: task-cp-0411212141-a2ef9e4d-f16d-4c10-94bc-586677
type: task-card
branch: cp-0411212141-a2ef9e4d-f16d-4c10-94bc-586677
created: 2026-04-12
---

# Task Card: KR5 Dashboard — Harness Pipeline 3 大阻断 Bug 清零

## 需求（What & Why）
**功能描述**: 修复 Harness Pipeline 模块 3 个阻断演示的 bug，使详情页可完整访问和导航。
**背景**: KR5 Dashboard 进度 58%，目标是 3 大模块无阻断 bug 可演示 20 分钟。扫描发现 Harness Pipeline 模块存在以下阻断 bug：
1. 详情页返回按钮导航至 `/pipeline`（不存在），用户无法返回列表
2. `HarnessPipeline` API 类型缺少 `planner_task_id` 字段
3. 列表页无"查看详情"入口，详情页路由 `/harness-pipeline/:id` 无法从 UI 到达

**不做什么**: 不改 Brain API，不改 CI 配置，不重构 TypeScript 类型架构

## 成功标准

- [x] [ARTIFACT] `HarnessPipelineDetailPage.tsx` 返回按钮路由改为 `/harness-pipeline`
  Test: manual:node -e "const c=require('fs').readFileSync('apps/dashboard/src/pages/harness-pipeline/HarnessPipelineDetailPage.tsx','utf8');if(!c.includes(\"navigate('/harness-pipeline')\"))process.exit(1)"

- [x] [ARTIFACT] `harness-pipeline.api.ts` 中 `HarnessPipeline` 接口新增 `planner_task_id` 字段
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/execution/api/harness-pipeline.api.ts','utf8');if(!c.includes('planner_task_id'))process.exit(1)"

- [x] [BEHAVIOR] Pipeline 列表页每条 pipeline 卡片有"查看详情"入口，点击导航至 `/harness-pipeline/:id`
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/execution/pages/HarnessPipelinePage.tsx','utf8');if(!c.includes('/harness-pipeline/'))process.exit(1)"

## 实现方案
**要改的文件**:
- `apps/dashboard/src/pages/harness-pipeline/HarnessPipelineDetailPage.tsx:306` — 修改 navigate 参数
- `apps/api/features/execution/api/harness-pipeline.api.ts:28` — 新增 `planner_task_id: string` 字段
- `apps/api/features/execution/pages/HarnessPipelinePage.tsx` — 在 PipelineCard 标题区加"查看详情"按钮

**受影响函数**: `HarnessPipelineDetailPage`（返回导航），`PipelineCard`（添加详情链接）
**不改什么**: Brain API、其他 Dashboard 页面、测试文件
