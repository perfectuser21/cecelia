# DoD: OKR 全树视图

## 验收条件

- [x] [ARTIFACT] `packages/brain/src/routes/okr-hierarchy.js` 中 `/tree` 端点查询深度扩展至 tasks 层
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/okr-hierarchy.js','utf8');if(!c.includes('okr_projects'))process.exit(1);if(!c.includes('okr_scopes'))process.exit(1);if(!c.includes('okr_initiatives'))process.exit(1);if(!c.includes('tasks'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] GET /api/brain/okr/tree 响应包含 key_results[].projects 字段
  Test: manual:node -e "const h=require('http');h.get('http://localhost:5221/api/brain/okr/tree',r=>{let d='';r.on('data',c=>d+=c);r.on('end',()=>{const b=JSON.parse(d);if(!b.success)process.exit(1);console.log('ok');})}).on('error',()=>console.log('server not running, skip'))"

- [x] [ARTIFACT] `apps/api/features/planning/pages/OKRTreePage.tsx` 页面组件已创建
  Test: manual:node -e "require('fs').accessSync('apps/api/features/planning/pages/OKRTreePage.tsx');console.log('ok')"

- [x] [ARTIFACT] `apps/api/features/planning/index.ts` 中注册了 `/okr/tree` 路由和 `OKRTreePage` 组件
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/planning/index.ts','utf8');if(!c.includes('/okr/tree'))process.exit(1);if(!c.includes('OKRTreePage'))process.exit(1);console.log('ok')"

- [x] [PRESERVE] 现有 GET /api/brain/okr/tree 响应的 tree[].objectives[].key_results 结构保持不变（向后兼容）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/okr-hierarchy.js','utf8');if(!c.includes('key_results'))process.exit(1);console.log('ok')"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/routes/okr-hierarchy.js');console.log('files ok')"
