# DoD: Pipeline 死锁修复重新应用

## 验收条件

| # | 类型 | 条件 | Test |
|---|------|------|------|
| 1 | [BEHAVIOR] | 合并失败时返回 blocked 状态和 return 2，允许重试 | manual: node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');const i=c.indexOf('合并失败，等待下次重试');const s=c.substring(i,i+200);if(!s.includes('blocked')||!s.includes('return 2'))process.exit(1)" |
| 2 | [BEHAVIOR] | Codex 审查超过 15 分钟自动降级为 PASS | manual: node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('elapsed_secs -gt 900')||!c.includes('降级为自动 PASS'))process.exit(1)" |
| 3 | [ARTIFACT] | 01-spec.md 写入 spec_review_registered_at | manual: node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('spec_review_registered_at'))process.exit(1)" |
| 4 | [ARTIFACT] | 02-code.md 写入 code_review_gate_registered_at | manual: node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/02-code.md','utf8');if(!c.includes('code_review_gate_registered_at'))process.exit(1)" |

## 验收清单

- [ ] devloop-check.sh 合并失败输出 status: blocked 并 return 2
- [ ] _check_codex_review 包含 900 秒超时逻辑
- [ ] 01-spec.md 写入 spec_review_registered_at
- [ ] 02-code.md 写入 code_review_gate_registered_at
