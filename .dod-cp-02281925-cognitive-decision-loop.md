---
id: dod-cp-02281925-cognitive-decision-loop
version: 1.1.0
created: 2026-02-28
updated: 2026-02-28
changelog:
  - 1.0.0: 初始版本
  - 1.1.0: 补全 Test 字段 + 标记已验证项
---

# DoD: Cecelia 认知-决策双闭环

## 验收清单

### 方向1：情绪 → 调度

- [x] 情绪评估：从 DB 获取 queueDepth（queued 任务数）和 successRate（最近成功率）
  Test: manual:bash -c "grep 'queued' packages/brain/src/tick.js | grep 'FROM tasks' | wc -l | grep -qv '^0$'"
- [x] `effectiveDispatchMax` 乘以 `emotion.dispatch_rate_modifier`
  Test: manual:bash -c "grep 'emotionDispatchModifier' packages/brain/src/tick.js | wc -l | grep -qv '^0$'"
- [x] `overloaded` 状态：log "情绪过载，跳过本轮派发" 并跳过
  Test: manual:bash -c "grep 'emotion_overloaded' packages/brain/src/tick.js | wc -l | grep -qv '^0$'"
- [x] `cognitive-core.js` importance 0.6 → 6（修复叙事写入）
  Test: manual:bash -c "grep 'importance, 6' packages/brain/src/cognitive-core.js | wc -l | grep -q 1"

### 方向3：反刍 → 计划

- [x] `planner.js` 新增 `buildInsightAdjustments(krIds)` 函数
  Test: manual:bash -c "grep 'buildInsightAdjustments' packages/brain/src/planner.js | wc -l | grep -qv '^0$'"
- [x] `scoreKRs()` 注入 insight 调整量
  Test: manual:bash -c "grep 'insightAdjustments' packages/brain/src/planner.js | wc -l | grep -qv '^0$'"

### 质量

- [x] 新增测试：emotion-dispatch.test.js（情绪门禁 + 修正系数）
  Test: packages/brain/src/__tests__/emotion-dispatch.test.js
- [x] 新增测试：planner-insight.test.js（buildInsightAdjustments 得分调整）
  Test: packages/brain/src/__tests__/planner-insight.test.js
- [x] Brain CI 通过（facts-check + version-sync）
  Test: manual:bash -c "node scripts/facts-check.mjs && bash scripts/check-version-sync.sh"
