# Task Card: 重构 capability-scanner.scanCapabilities（复杂度 42 → 10 以下）

## PRD

**背景**：代码复杂度扫描发现 `packages/brain/src/capability-scanner.js` 第 69 行 `scanCapabilities` 函数的圈复杂度为 42，超过阈值 10。需要重构以降低复杂度。

**目标**：
- `scanCapabilities` 函数圈复杂度降至 10 以下
- 提取多个命名子函数，每个函数职责单一
- 保持所有现有测试通过，功能行为不变

## 成功标准

- [ ] `scanCapabilities` 函数圈复杂度降至 10 以下
- [ ] 提取至少 3 个命名子函数
- [ ] 所有现有测试通过
- [ ] 功能行为不变（不能破坏任何业务逻辑）

## DoD

### DoD-1: [ARTIFACT] scanCapabilities 导出存在
  Test: manual: node -e "const src = require('fs').readFileSync('packages/brain/src/capability-scanner.js','utf8'); if(!src.includes('export async function scanCapabilities')) { process.exit(1); } console.log('export ok')"

### DoD-2: [ARTIFACT] 提取了至少 3 个私有子函数
  Test: manual: node -e "const src = require('fs').readFileSync('packages/brain/src/capability-scanner.js','utf8'); const fns = src.match(/^(async )?function [a-zA-Z]/gm)||[]; if(fns.length < 3) { console.error('需要至少 3 个提取函数，当前:' + fns.length); process.exit(1); } console.log('提取函数数量:', fns.length)"

### DoD-3: [BEHAVIOR] 扫描器各子函数存在且 scanCapabilities 为异步函数
  Test: manual: node -e "const src = require('fs').readFileSync('packages/brain/src/capability-scanner.js','utf8'); const required = ['determineStatus','checkSkillUsage','checkTableEvidence','evaluateCapability','buildSummary']; const missing = required.filter(f => !src.includes('function ' + f)); if(missing.length > 0) { console.error('缺少子函数:', missing); process.exit(1); } if(!src.includes('export async function scanCapabilities')) { process.exit(1); } console.log('all functions ok')"
