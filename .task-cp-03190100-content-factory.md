---
id: task-cp-03190100-content-factory
type: task-card
branch: cp-03190100-content-factory
task_id: dfe6a01e-2427-4c0e-abbb-da8d49735f24
created: 2026-03-19
---

# Task Card: Dashboard 内容工厂触发页

## 需求（What & Why）
**功能描述**: 在 Dashboard 新增「内容工厂」页面，路由 /content-factory。
**背景**: Brain Content Pipeline API 已就绪，但 Dashboard 没有 UI 入口，用户无法触发 Pipeline。
**不做什么**: 不做图片预览，不做 Human Approve 交互，不做发布按钮。

## 成功标准

1. [ARTIFACT] ContentFactory.tsx 存在于 apps/api/features/content/pages/
2. [ARTIFACT] knowledge/index.ts 注册了 /content-factory 路由
3. [BEHAVIOR] 页面展示内容类型下拉（从 /api/brain/content-types 读取）
4. [BEHAVIOR] 表单提交后调用 POST /api/brain/pipelines 并刷新列表
5. [BEHAVIOR] 页面展示 Pipeline 列表（title/status/created_at）
6. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] ContentFactory.tsx 文件存在
  Test: manual:node --input-type=commonjs -e "const fs=require('fs');if(!fs.existsSync('apps/api/features/content/pages/ContentFactory.tsx'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] knowledge/index.ts 包含 /content-factory 路由
  Test: manual:node --input-type=commonjs -e "const c=require('fs').readFileSync('apps/api/features/knowledge/index.ts','utf8');if(!c.includes('content-factory'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] ContentFactory.tsx 包含 GET /api/brain/content-types 调用
  Test: manual:node --input-type=commonjs -e "const c=require('fs').readFileSync('apps/api/features/content/pages/ContentFactory.tsx','utf8');if(!c.includes('content-types'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] ContentFactory.tsx 包含 POST /api/brain/pipelines 调用
  Test: manual:node --input-type=commonjs -e "const c=require('fs').readFileSync('apps/api/features/content/pages/ContentFactory.tsx','utf8');if(!c.includes('POST'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] ContentFactory.tsx 展示 Pipeline 列表
  Test: manual:node --input-type=commonjs -e "const c=require('fs').readFileSync('apps/api/features/content/pages/ContentFactory.tsx','utf8');if(!c.includes('pipelines'))process.exit(1);console.log('OK')"

## 实现方案
**要改的文件**:
- 新增 apps/api/features/content/pages/ContentFactory.tsx
- 修改 apps/api/features/knowledge/index.ts：添加路由 + 组件注册 + navItem
