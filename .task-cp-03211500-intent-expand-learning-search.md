---
id: task-cp-03211500-intent-expand-learning-search
type: task-card
branch: cp-03211500-intent-expand-learning-search
created: 2026-03-21
---

# Task Card: intent-expand 和 Learning 检索集成到 Pipeline Stage 1

## 需求（What & Why）
**功能描述**: 在 Stage 1 Spec 流程中新增两个步骤：(1) intent-expand 从 Brain 读取 KR/Initiative/Project 上下文补充到 PRD；(2) Task Card 生成后检索 docs/learnings/ 推荐相关历史 Learning。
**背景**: Agent 执行任务时缺少"更大目标"的上下文，也不知道历史踩坑记录，导致重复犯错。
**不做什么**: 不改变 Stage 1 的核心流程（Task Card 格式、自检、spec_review 派发等）

## 成功标准
1. [ARTIFACT] 01-spec.md 包含 "## 1.1.5 上下文补充（intent-expand）" 章节，含 Brain API 调用逻辑
2. [ARTIFACT] 01-spec.md 包含 "## 1.3.5 相关 Learning 检索" 章节，含 docs/learnings/ 搜索逻辑
3. [BEHAVIOR] intent-expand 章节位于 1.2 节（生成 Task Card）之前
4. [BEHAVIOR] Learning 检索章节位于 1.3 节（写入 .dev-mode）之前（即 Task Card 生成之后）
5. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] 01-spec.md 包含 intent-expand 章节
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('## 1.1.5'))process.exit(1)"

- [x] [ARTIFACT] 01-spec.md 包含 Learning 检索章节
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('## 1.3.5'))process.exit(1)"

- [x] [BEHAVIOR] intent-expand 在 1.2 之前（行号更小）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');const a=c.indexOf('## 1.1.5');const b=c.indexOf('## 1.2');if(a<0||b<0||a>=b)process.exit(1)"

- [x] [BEHAVIOR] Learning 检索在 写入 .dev-mode 之前
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');const a=c.indexOf('## 1.3.5');const b=c.indexOf('## 1.3 写入');if(a<0||b<0||a>=b)process.exit(1)"

- [x] [GATE] CI 全部通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案
**要改的文件**: packages/engine/skills/dev/steps/01-spec.md
**Scope 锚定**: 仅修改 01-spec.md，新增两个章节（1.1.5 和 1.3.5），不改动现有章节
