---
branch: cp-03171930-fix-migrations-bulk
created: 2026-03-17
---

# PRD: 批量修复 migration 013 和 112 不幂等

## 背景

CI L3 smoke.test.js 在修复 023 和 088 后仍然失败，112 报：
```
[FAIL] 112_notion_unique_index.sql: relation "goals_notion_id_unique" already exists
```

## 问题

- **013**: `CREATE INDEX` 无 `IF NOT EXISTS`（4 条）
- **112**: `ALTER TABLE ... ADD CONSTRAINT ... UNIQUE` 前没有 `DROP CONSTRAINT IF EXISTS`（3 条）

## 目标

修复这两个文件，使其幂等，彻底解决 CI DB 积累状态导致的 smoke.test.js 失败。

## 成功标准

1. [BEHAVIOR] migration 013 和 112 可重复执行不报错
2. [ARTIFACT] 013 的 CREATE INDEX 含 IF NOT EXISTS；112 的 ADD CONSTRAINT 前有 DROP CONSTRAINT IF EXISTS
3. [GATE] CI L3 smoke.test.js 通过
