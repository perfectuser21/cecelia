---
id: task-cp-03272221-1f74988f-51d8-4cdc-a52b-533c6d
type: task-card
branch: cp-03272221-1f74988f-51d8-4cdc-a52b-533c6d
created: 2026-03-28
---

# Task Card: /dev Stage4 自动写 CURRENT_STATE.md

## 需求（What & Why）
**功能描述**: 在 /dev Stage 4 末尾加自动写 CURRENT_STATE.md 步骤，汇入 capability-probe 结果。
**背景**: Claude 每次对话开始时通过 session-start.sh 注入 Brain 当前任务状态，但缺少系统健康/链路探针状态。加入后 Claude 能感知哪些能力链路正常、哪些故障。
**不做什么**: 不修改 Brain 核心调度逻辑；不改变 probe 的触发机制（仍由 Brain tick 调度）。

## 成功标准
1. [ARTIFACT] `scripts/write-current-state.sh` 存在且可执行
2. [ARTIFACT] `.agent-knowledge/CURRENT_STATE.md` 存在（初始版本）
3. [BEHAVIOR] `bash scripts/write-current-state.sh` 执行后 CURRENT_STATE.md 包含 probe 状态信息
4. [BEHAVIOR] `packages/engine/skills/dev/steps/04-ship.md` 包含 `write-current-state` 调用步骤
5. [BEHAVIOR] `packages/engine/hooks/session-start.sh` 包含 CURRENT_STATE.md 注入逻辑
6. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] session-start.sh 原有 Brain 任务注入逻辑不受影响（仅追加，不替换）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/session-start.sh','utf8');if(!c.includes('Brain 当前状态'))process.exit(1)"

- [x] [ARTIFACT] scripts/write-current-state.sh 存在
  Test: manual:node -e "require('fs').accessSync('scripts/write-current-state.sh')"

- [x] [ARTIFACT] .agent-knowledge/CURRENT_STATE.md 存在
  Test: manual:node -e "require('fs').accessSync('.agent-knowledge/CURRENT_STATE.md')"

- [x] [BEHAVIOR] 04-ship.md 包含 write-current-state 步骤调用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/04-ship.md','utf8');if(!c.includes('write-current-state'))process.exit(1)"

- [x] [BEHAVIOR] session-start.sh 包含 CURRENT_STATE 注入逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/hooks/session-start.sh','utf8');if(!c.includes('CURRENT_STATE'))process.exit(1)"

- [x] [BEHAVIOR] write-current-state.sh 内含 probe 数据读取逻辑（capability_probe）
  Test: manual:node -e "const c=require('fs').readFileSync('scripts/write-current-state.sh','utf8');if(!c.includes('capability_probe'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "require('fs').accessSync('packages/engine/VERSION')"

## 实现方案

**要改的文件**:
1. `scripts/write-current-state.sh` (NEW) - 从 Brain API + DB 读取状态，写 CURRENT_STATE.md
2. `packages/engine/skills/dev/steps/04-ship.md` (MODIFY) - 在 Learning 后加 4.1.5 步骤
3. `packages/engine/hooks/session-start.sh` (MODIFY) - 追加 CURRENT_STATE.md 注入
4. `.agent-knowledge/CURRENT_STATE.md` (NEW) - 初始占位文件
5. Engine 版本文件 × 5 (MODIFY) - 13.48.0 → 13.49.0
6. `packages/engine/feature-registry.yml` (MODIFY) - 新增 changelog 条目

**Scope 锚定**: packages/engine/ 内的 skills + hooks，scripts/ 的新脚本，.agent-knowledge/ 的新文件
