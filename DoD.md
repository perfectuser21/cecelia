contract_branch: cp-harness-contract-ad3cd28b
workstream_index: 2
sprint_dir: sprints/harness-v7-docker-sandbox

- [x] [ARTIFACT] docker/harness-runner/Dockerfile 存在，包含实际 claude CLI 安装指令（RUN/COPY，非注释占位）和 Node.js 运行时
- [x] [BEHAVIOR] HARNESS_DOCKER_ENABLED=true 时，cecelia-run.sh 使用 docker run --rm --memory=Xm --cpus=Y 启动任务（非注释代码中存在条件分支 + docker run + --memory + setsid 回退）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-run.sh','utf8');const lines=c.split('\n').filter(l=>!l.trim().startsWith('#'));const code=lines.join('\n');if(!/HARNESS_DOCKER_ENABLED/.test(code)){console.log('FAIL: 无 HARNESS_DOCKER_ENABLED');process.exit(1);}if(!/docker run/.test(code)){console.log('FAIL: 无 docker run');process.exit(1);}if(!/--memory/.test(code)){console.log('FAIL: 无 --memory');process.exit(1);}if(!/setsid bash/.test(code)){console.log('FAIL: 无 setsid 回退');process.exit(1);}console.log('PASS');"
- [x] [BEHAVIOR] HARNESS_DOCKER_ENABLED=false 或未设置时，仍使用 setsid bash -c 启动（零回归）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-run.sh','utf8');const lines=c.split('\n').filter(l=>!l.trim().startsWith('#'));const code=lines.join('\n');if(!/setsid bash -c/.test(code)){console.log('FAIL: setsid bash 回退不存在');process.exit(1);}console.log('PASS');"
- [x] [BEHAVIOR] executor.js 中 CONTAINER_SIZES 常量存在（非注释代码），light>=256、normal>=512、heavy>=1024 MB
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');const lines=c.split('\n').filter(l=>!l.trim().startsWith('//'));const code=lines.join('\n');const m=code.match(/CONTAINER_SIZES\s*=\s*\{[\s\S]*?\}/);if(!m){console.log('FAIL: 无 CONTAINER_SIZES');process.exit(1);}const obj=m[0];const l=obj.match(/light[^,}]*?(\d{3,})/);const n=obj.match(/normal[^,}]*?(\d{3,})/);const h=obj.match(/heavy[^,}]*?(\d{3,})/);if(!l||parseInt(l[1])<256){console.log('FAIL: light<256');process.exit(1);}if(!n||parseInt(n[1])<512){console.log('FAIL: normal<512');process.exit(1);}if(!h||parseInt(h[1])<1024){console.log('FAIL: heavy<1024');process.exit(1);}console.log('PASS: light='+l[1]+' normal='+n[1]+' heavy='+h[1]);"
- [x] [BEHAVIOR] Docker daemon 不可用时 cecelia-run.sh 检测并回退到 non-docker 模式（非注释代码中存在 docker info/version 检测 + 失败后的 setsid 回退逻辑）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-run.sh','utf8');const lines=c.split('\n').filter(l=>!l.trim().startsWith('#'));const code=lines.join('\n');if(!/docker info|docker version/.test(code)){console.log('FAIL: 无 docker 可用性检测');process.exit(1);}if(!/setsid/.test(code)){console.log('FAIL: 无 setsid 回退');process.exit(1);}console.log('PASS');"
