contract_branch: cp-06120358-ws-52441a6e-ws1
sprint_dir: sprints/06120010-e2e-exec-judge-r2

---
skeleton: false
journey_type: autonomous
---
# Contract DoD — Sprint: evaluate 职责分离（代码执行 E2E + 轻量 LLM 裁读）

**范围**: `evaluateContractNode` 节点重构 — 代码层执行 BEHAVIOR 命令 + 结构化执行记录落盘 + LLM 裁读记录 + Brain 代码覆盖校验
**大小**: M

## ARTIFACT 条目

- [x] [ARTIFACT] `harness-final-e2e.js` 导出三个新函数（executeContractCommands / validateCoverageTable / isLegacyMode）
  Test: node -e "const src=require('fs').readFileSync('packages/brain/src/harness-final-e2e.js','utf8');if(!src.includes('executeContractCommands'))process.exit(1);if(!src.includes('validateCoverageTable'))process.exit(1);if(!src.includes('isLegacyMode'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] `harness-initiative-evaluate-exec-judge.test.js` 回归测试文件存在且含覆盖校验用例
  Test: node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/harness-initiative-evaluate-exec-judge.test.js','utf8');if(!c.includes('executeContractCommands'))process.exit(1);if(!c.includes('validateCoverageTable'))process.exit(1);if(!c.includes('EVALUATOR_LEGACY'))process.exit(1);console.log('OK')"

## BEHAVIOR 条目（内嵌 manual:node 命令，autonomous — 测 Brain 代码层）

- [x] [BEHAVIOR] `executeContractCommands` 函数已从 `harness-final-e2e.js` 导出，通过型 fixture 执行记录 schema 正确（exit_code / stdout / stderr / duration_ms 字段完整）
  Test: manual:node -e "import('./packages/brain/src/harness-final-e2e.js').then(async({executeContractCommands})=>{const fs=require('fs'),dir='/tmp/dod1-'+process.pid;fs.mkdirSync(dir,{recursive:true});try{const r=await executeContractCommands([{cmd:'echo beh1-fixture'}],{sprintDir:dir,runId:'b1'});if(!r.recordPath)throw new Error('no recordPath');const c=JSON.parse(fs.readFileSync(r.recordPath,'utf8')).commands[0];if(typeof c.exit_code!=='number')throw new Error('no exit_code');if(typeof c.duration_ms!=='number')throw new Error('no duration_ms');if(!c.stdout.includes('beh1-fixture'))throw new Error('stdout missing');console.log('OK')}finally{fs.rmSync(dir,{recursive:true,force:true})}}).catch(e=>{console.error(e.message);process.exit(1)})"

- [x] [BEHAVIOR] 失败型命令 — `exit 42` 的 `exit_code` 非零且失败 stderr 原文保留于执行记录（LLM 不许自行发明错误原因）
  Test: manual:node -e "import('./packages/brain/src/harness-final-e2e.js').then(async({executeContractCommands})=>{const fs=require('fs'),dir='/tmp/dod2-'+process.pid;fs.mkdirSync(dir,{recursive:true});try{const r=await executeContractCommands([{cmd:'echo FAIL-ORIGIN >&2 && exit 42'}],{sprintDir:dir,runId:'b2'});const c=JSON.parse(fs.readFileSync(r.recordPath,'utf8')).commands[0];if(c.exit_code===0)throw new Error('exit_code should be nonzero');const combined=(c.stdout||'')+' '+(c.stderr||'');if(!combined.includes('FAIL-ORIGIN'))throw new Error('FAIL-ORIGIN not preserved');console.log('OK')}finally{fs.rmSync(dir,{recursive:true,force:true})}}).catch(e=>{console.error(e.message);process.exit(1)})"

- [x] [BEHAVIOR] `validateCoverageTable(coverage, gpSteps)` — 缺少 GP 步骤时返回 `{ok: false, missing: [...]}` 强制 FAIL，不信任 LLM 自称 PASS
  Test: manual:node -e "import('./packages/brain/src/harness-final-e2e.js').then(({validateCoverageTable})=>{const steps=['Step 1','Step 2','Step 3'];const r1=validateCoverageTable({steps:[{step:'Step 1',passed:true}]},steps);if(r1.ok!==false)throw new Error('should be false: '+JSON.stringify(r1));if(!r1.missing.includes('Step 2'))throw new Error('Step 2 not in missing');const r2=validateCoverageTable({steps:steps.map(s=>({step:s,passed:true}))},steps);if(r2.ok!==true)throw new Error('full coverage should be true');console.log('OK')}).catch(e=>{console.error(e.message);process.exit(1)})"

- [x] [BEHAVIOR] error path — `validateCoverageTable` 收到空 steps 数组时返回 `{ok: false, missing: [...全部 GP 步骤...]}`
  Test: manual:node -e "import('./packages/brain/src/harness-final-e2e.js').then(({validateCoverageTable})=>{const r=validateCoverageTable({steps:[]},['A','B','C']);if(r.ok!==false)throw new Error('should be false');if(!Array.isArray(r.missing)||r.missing.length!==3)throw new Error('missing should have 3 items');console.log('OK')}).catch(e=>{console.error(e.message);process.exit(1)})"

- [x] [BEHAVIOR] 回退开关 — `EVALUATOR_LEGACY=1` 时 `isLegacyMode()` 返回 `true`；未设时返回 `false`
  Test: manual:node -e "import('./packages/brain/src/harness-final-e2e.js').then(({isLegacyMode})=>{if(isLegacyMode()!==false)throw new Error('should be false without env');process.env.EVALUATOR_LEGACY='1';if(isLegacyMode()!==true)throw new Error('should be true with EVALUATOR_LEGACY=1');delete process.env.EVALUATOR_LEGACY;console.log('OK')}).catch(e=>{console.error(e.message);process.exit(1)})"

- [x] [BEHAVIOR] 取证唯一命名 — 同 sprintDir 下两次 executeContractCommands（不同 runId）产生不同 recordPath
  Test: manual:node -e "import('./packages/brain/src/harness-final-e2e.js').then(async({executeContractCommands})=>{const fs=require('fs'),dir='/tmp/dod6-'+process.pid;fs.mkdirSync(dir,{recursive:true});try{const r1=await executeContractCommands([{cmd:'echo a'}],{sprintDir:dir,runId:'run-001'});const r2=await executeContractCommands([{cmd:'echo b'}],{sprintDir:dir,runId:'run-002'});if(r1.recordPath===r2.recordPath)throw new Error('paths should differ: '+r1.recordPath);console.log('OK')}finally{fs.rmSync(dir,{recursive:true,force:true})}}).catch(e=>{console.error(e.message);process.exit(1)})"

- [x] [BEHAVIOR] evaluateContractNode（新路径）节点终态 — dispatch 语义（capturedCmds.length>0）+ evaluate_coverage + .brain-result.json 含 verdict + coverage
  Test: manual:node -e "Promise.all([import('./packages/brain/src/workflows/harness-task.graph.js'),import('./packages/brain/src/harness-final-e2e.js')]).then(async([g,e])=>{const fs=require('fs'),path=require('path'),dir='/tmp/dod7-'+process.pid,brp=path.join(dir,'.brain-result.json');fs.mkdirSync(dir,{recursive:true});try{fs.writeFileSync(path.join(dir,'contract-dod.md'),'skeleton: false\njourney_type: autonomous\n\n## BEHAVIOR\n\n- [ ] [BEHAVIOR] b7\n  Test: manual:node -e \"console.log(42)\"\n  期望: OK\n','utf8');const steps=['Step 1','Step 2','Step 3'],caps=[];const r=await g.evaluateContractNode({task:{id:'b7',payload:{sprint_dir:dir,journey_type:'autonomous'}},evaluate_verdict:null,pr_url:null,worktreePath:null,prdContent:steps.map(s=>'### '+s).join('\n'),githubToken:'x',brainResultPath:brp},{checkPrMerged:async()=>false,resolveToken:async()=>'x',verifyArtifacts:async()=>({ran:true,ok:true}),runContractGate:async()=>({ok:true}),executeContractCommands:async(c,o)=>{caps.push(...c);return e.executeContractCommands(c,o)},judgeWithLlm:async(_,__,ss)=>({verdict:'PASS',coverage:{steps:ss.map(s=>({step:s,passed:true}))}})});if(!caps.length)throw new Error('dispatch failed: capturedCmds=0');if(!r.evaluate_coverage)throw new Error('no evaluate_coverage');const br=JSON.parse(fs.readFileSync(brp,'utf8'));if(!br.verdict||!br.coverage||!Array.isArray(br.coverage.steps))throw new Error('bad brain-result: '+JSON.stringify(br).slice(0,200));console.log('OK caps='+caps.length)}finally{fs.rmSync(dir,{recursive:true,force:true})}}).catch(e=>{console.error(e.message);process.exit(1)})"

- [x] [BEHAVIOR] evaluateContractNode 失败型终态 — judgeWithLlm 返回 verdict=FAIL + failed_step 时，.brain-result.json 含 verdict=FAIL + failed_step 字段
  Test: manual:node -e "Promise.all([import('./packages/brain/src/workflows/harness-task.graph.js'),import('./packages/brain/src/harness-final-e2e.js')]).then(async([g,e])=>{const fs=require('fs'),path=require('path'),dir='/tmp/dod8-'+process.pid,brp=path.join(dir,'.brain-result.json');fs.mkdirSync(dir,{recursive:true});try{fs.writeFileSync(path.join(dir,'contract-dod.md'),'skeleton: false\njourney_type: autonomous\n\n## BEHAVIOR\n\n- [ ] [BEHAVIOR] b8\n  Test: manual:node -e \"console.log(42)\"\n  期望: OK\n','utf8');const steps=['Step 1','Step 2','Step 3'];await g.evaluateContractNode({task:{id:'b8',payload:{sprint_dir:dir,journey_type:'autonomous'}},evaluate_verdict:null,pr_url:null,worktreePath:null,prdContent:steps.map(s=>'### '+s).join('\n'),githubToken:'x',brainResultPath:brp},{checkPrMerged:async()=>false,resolveToken:async()=>'x',verifyArtifacts:async()=>({ran:true,ok:true}),runContractGate:async()=>({ok:true}),executeContractCommands:async(c,o)=>e.executeContractCommands(c,o),judgeWithLlm:async()=>({verdict:'FAIL',failed_step:'Step 2',coverage:{steps:[]}})});const br=JSON.parse(fs.readFileSync(brp,'utf8'));if(br.verdict!=='FAIL')throw new Error('verdict should be FAIL got '+br.verdict);if(!br.failed_step)throw new Error('no failed_step');if(br.failed_step!=='Step 2')throw new Error('wrong failed_step: '+br.failed_step);console.log('OK')}finally{fs.rmSync(dir,{recursive:true,force:true})}}).catch(e=>{console.error(e.message);process.exit(1)})"

- [x] [BEHAVIOR] 大输出截断 — stdout/stderr 超 4000 字节时保留头部（TRNK_HEAD）+ 尾部（TRNK_TAIL），exit_code 仍正确捕获
  Test: manual:node -e "import('./packages/brain/src/harness-final-e2e.js').then(async({executeContractCommands})=>{const fs=require('fs'),dir='/tmp/dod9-'+process.pid;fs.mkdirSync(dir,{recursive:true});try{const r=await executeContractCommands([{cmd:'python3 -c \'import sys; sys.stdout.write(\"TRNK_HEAD\"+\"A\"*5000+\"TRNK_TAIL\"); sys.exit(9)\''}],{sprintDir:dir,runId:'trunc-9'});const c=JSON.parse(fs.readFileSync(r.recordPath,'utf8')).commands[0];if(c.exit_code!==9)throw new Error('exit_code should be 9 got '+c.exit_code);const stdout=c.stdout||'';if(stdout.length>4000)throw new Error('stdout too long: '+stdout.length);if(!stdout.includes('TRNK_HEAD'))throw new Error('TRNK_HEAD not preserved');if(!stdout.includes('TRNK_TAIL'))throw new Error('TRNK_TAIL not preserved');console.log('OK: length='+stdout.length+' exit_code='+c.exit_code)}finally{fs.rmSync(dir,{recursive:true,force:true})}}).catch(e=>{console.error(e.message);process.exit(1)})"
