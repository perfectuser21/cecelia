---
id: dod-fix-sw-white-screen
version: 1.0.0
created: 2026-03-11
updated: 2026-03-11
changelog:
  - 1.0.0: 初始版本
---

# DoD: 修复 SW 更新白屏

## 验收清单

- [x] `apps/dashboard/src/main.tsx` 添加了 `controllerchange` 事件监听，触发时调用 `window.location.reload()`
  Test: manual:bash -c "grep -c 'controllerchange' apps/dashboard/src/main.tsx"

- [x] `clearStaleCache()` 返回 `boolean`，`reload()` 后返回 `true`，`.then()` 检查返回值再决定是否挂载 React
  Test: manual:bash -c "grep -c 'reloading' apps/dashboard/src/main.tsx"

- [x] `APP_VERSION` 更新为 `2026-03-11-v1`，强制触发一次缓存清理
  Test: manual:bash -c "grep -c '2026-03-11-v1' apps/dashboard/src/main.tsx"

- [x] dist 已重建，新 bundle 包含修改后的 main.tsx 逻辑
  Test: manual:bash -c "ls apps/dashboard/dist/assets/index-*.js | wc -l"
