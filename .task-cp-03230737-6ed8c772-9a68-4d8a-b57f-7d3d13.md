---
id: task-cp-03230737-6ed8c772-9a68-4d8a-b57f-7d3d13
type: task-card
branch: cp-03230737-6ed8c772-9a68-4d8a-b57f-7d3d13
created: 2026-03-23
---

# Task Card: fix(engine) Stage3 drift detection + spec_review 测试层检查

## 需求（What & Why）

**功能描述**：
1. devloop-check.sh Stage 3 增加 drift check：实际改动文件超出 DoD 声明范围时输出 warning（非阻断）
2. spec_review subagent prompt 增加测试层检查指令：每个 DoD 条目必须对应合适的测试类型（unit/integration/e2e）

**背景**：
- Stage 3 push 前没有检测代码改动是否偏离 Stage 1 Spec 范围（scope drift）
- spec_review 只看逻辑，不检查 DoD Test 是否覆盖对应测试层

**不做什么**：
- drift check 不阻断流程（仅 warning）
- 不新增独立的 drift-check 文件，直接改 devloop-check.sh

## 成功标准

- devloop-check.sh Stage 3 阶段含 drift check 逻辑，实际改动文件超出 DoD 声明范围时输出 warning
- spec_review subagent prompt 增加测试层检查指令，要求每个 DoD 条目对应合适的测试类型
- Engine L3 测试全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] packages/engine/lib/devloop-check.sh 含 drift check 逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/lib/devloop-check.sh','utf8');if(!c.includes('drift'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] spec_review prompt 含测试层检查关键词
  Test: manual:node -e "const fs=require('fs');const dir='packages/engine/skills/dev/steps';const files=fs.readdirSync(dir).filter(f=>fs.statSync(dir+'/'+f).isFile());const found=files.some(f=>{const c=fs.readFileSync(dir+'/'+f,'utf8');return c.includes('测试层')||c.includes('test layer');});if(!found)process.exit(1);console.log('OK')"

- [x] [GATE] Engine L3 通过（devloop-check.sh 语法正确）
  Test: manual:bash -c 'bash -n packages/engine/lib/devloop-check.sh'

## 实现方案

**要改的文件**：
- `packages/engine/lib/devloop-check.sh` — 在条件 2.6 之后（CI/PR 之前）添加 drift check warning 逻辑
- `packages/engine/skills/dev/steps/01-spec.md` — 在 spec_review prompt 描述中增加测试层检查维度说明
- `packages/workflows/skills/spec-review/SKILL.md` — 新增维度 F：测试层合理性检查

**Scope 锚定**：
- 修改 engine 包工具文件（devloop-check.sh、skills/dev/steps/01-spec.md）
- 修改 workflows 包 spec-review SKILL.md
- 不涉及 brain/API/数据库
