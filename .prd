# PRD: 修复 tick_history 表缺失 started_at 列

## 背景
Brain 启动时报错 `column "started_at" does not exist`，因为 migration 164 只添加了 `completed_at` 列，遗漏了 `started_at` 列。`metrics.js` 的 `getRecentTickTime()` 同时依赖这两列。

## 成功标准
- migration 164 同时添加 `started_at` 和 `completed_at` 两列
- 使用 `ADD COLUMN IF NOT EXISTS` 确保幂等性
- Brain 启动不再报 `column "started_at" does not exist` 错误
