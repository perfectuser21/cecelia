# PRD: Brain v2 Phase D1.7b — executeTick 抽到 tick-runner.js

## 背景

D1.7a（PR #2623，已合）抽出 `tick-state.js` 收口 19 个模块级状态字段（14 个 `lastXxxTime` + 5 个 loop 控制态），为后续拆分准备共享容器。

`packages/brain/src/tick.js` 当前仍 2427 行，其中 `executeTick` 函数体（L662-L2287，共 1626 行）占绝大部分。它是 brain 的"自驱核心循环"——里面调度 30+ 个 scheduled job、跑 alertness/思考/任务派发/认知评估/感知层巡检全流程。

D1.7b 把 executeTick 函数体整体抽到新文件 `tick-runner.js`，**纯重构，零行为变化**。tick.js 通过 `import { executeTick } from './tick-runner.js'` re-export 兼容老 caller（19 个 `vi.mock('../tick.js')` 测试 + server.js + routes/）。

D1.7c 才拆 scheduled job 为独立 plugin。本 PR 不动 30+ 调度调用，只搬家函数体。

## 上游 PRD

- 设计：`docs/design/brain-v2-c8-d-e-handoff.md` §5 D1.7b
- 团队：`brain-v2-d-team` Task #2（builder-2 完成）

## 成功标准

- 新建 `packages/brain/src/tick-runner.js`（~1840 行：80 行 imports + 60 行常量/helpers + 1626 行 executeTick body）
- `tick.js` 删除 L662-L2287 executeTick 函数体；顶部新增 `import { executeTick } from './tick-runner.js'` 兼容；底部 export block 保留 `executeTick`（re-export 自动生效）
- `tick.js` 行数 < 1500（实际：< 850）
- tick-runner.js 与 tick.js **不形成循环 import**（关键设计）：
  - tick.js → tick-runner.js（单向导入 executeTick）
  - tick-runner.js 不导入 tick.js；常量/helpers（tickLog/isStale/logTickDecision/incrementActionsToday/MINIMAL_MODE/各 INTERVAL_MS）在 tick-runner.js 本地重新定义，与 tick.js 同名等价（process.env 派生 + 字面量）
  - 实测教训：vite/vitest 对真循环 import 触发 TDZ ReferenceError，致 mock-tick 全套测试假阴性。本设计绕开 vite 模块图坑
- 所有 tick 相关 vitest 测试（tick-cleanup / tick-throttle / tick.test / tick-workflow-runtime / tick-state）全 pass
- 19 个 `vi.mock('../tick.js')` 测试不需要改 mock 路径（兼容由 re-export 维持）
- 不动 executeTick 函数体的语义（仅搬家）；不动 30+ scheduled job 调用（D1.7c 才做）
