# Task Card: 背压阈值 5→20

## 需求（What & Why）
**功能描述**: 背压阈值从 5 调到 20。7 个正常 KR 拆解任务就触发背压导致系统完全停转。
**不做什么**: 不改背压机制本身

## 成功标准
1. [BEHAVIOR] 背压阈值改为 20
2. [BEHAVIOR] 旧阈值 5 不再存在
3. [GATE] 所有现有测试通过

## 验收条件（DoD）
- [x] [BEHAVIOR] BACKPRESSURE_THRESHOLD = 20
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/slot-allocator.js','utf8');if(!c.includes('BACKPRESSURE_THRESHOLD = 20'))process.exit(1)"
- [x] [BEHAVIOR] 旧阈值 5 已移除（7个任务不会卡死系统）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/slot-allocator.js','utf8');if(c.includes('BACKPRESSURE_THRESHOLD = 5'))process.exit(1)"
- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"
