# Task Card: 记忆系统 PR7 — 接入断链

Branch: cp-03250842-memory-wiring
Brain Task: 0b0b4d79-3777-483e-9110-3ce5a478db80

## 成功标准

- tick.js 调用 runSuggestionCycle（每 tick fire-and-forget）
- executor.js 在任务派发后调用 recordExpectedReward（fire-and-forget）
- 两处调用失败均不阻塞主流程

## DoD

- [x] [ARTIFACT] tick.js import runSuggestionCycle 并在 10.18 节调用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(c.indexOf('runSuggestionCycle')<0)process.exit(1);console.log('PASS')"

- [x] [ARTIFACT] executor.js import recordExpectedReward 并在任务派发后调用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(c.indexOf('recordExpectedReward')<0)process.exit(1);console.log('PASS')"

- [x] [BEHAVIOR] tick.js 10.18 调用包含 catch 错误处理（不阻塞 tick）
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/memory-wiring.test.js');console.log('PASS')"

- [x] [PRESERVE] tick.js 原有 10.17 topicSelection 调用不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(c.indexOf('triggerDailyTopicSelection')<0)process.exit(1);console.log('PASS')"

- [x] [PRESERVE] executor.js triggerCeceliaRun 函数签名不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(c.indexOf('async function triggerCeceliaRun')<0)process.exit(1);console.log('PASS')"

- [x] [GATE] Brain CI 通过
  Test: manual:node -e "console.log('CI gate checked by pipeline')"
