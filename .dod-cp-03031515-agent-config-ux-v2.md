---
id: dod-agent-config-ux-v2
version: 1.0.0
created: 2026-03-03
updated: 2026-03-03
changelog:
  - 1.0.0: 初始版本
---

# DoD：Brain Agent 配置 UI 重设计 v2

## 验收清单

- [x] **折叠态**：每个 agent 默认折叠，显示 agent 名称 + 当前选中组合（provider · model · 调用方式）+ 展开箭头
  Test: manual:bash -c "grep -q 'isOpen\|expanded\|collapsed' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **展开态**：点击展开，显示按 provider 分组的所有可选组合
  Test: manual:bash -c "grep -q 'setIsOpen\|toggleOpen' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **Anthropic 分组**：显示 API 和无头两种调用方式 × allowed claude 模型
  Test: manual:bash -c "grep -q 'anthropic-api\|anthropic' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **MiniMax 分组**：显示 MiniMax API 和 MiniMax 无头两种调用方式 × allowed MiniMax 模型
  Test: manual:bash -c "grep -q 'minimax-headless' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **只显示 allowed_models 里有的模型**：每个 agent 只显示其 allowed_models 匹配的选项
  Test: manual:bash -c "grep -q 'allowed_models' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **嘴巴不变**：MouthModeSelector 保持现有 2×2 网格
  Test: manual:bash -c "grep -q 'MouthModeSelector' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **Toast 保留**：切换成功/失败 Toast 通知仍然有效
  Test: manual:bash -c "grep -q 'showToast' apps/api/features/system/components/BrainLayerConfig.tsx"

- [x] **TypeScript 编译通过**：0 错误
  Test: manual:bash -c "cd apps/api && npx tsc --noEmit --skipLibCheck 2>&1 | grep -c 'error TS' | grep -q '^0$'"

- [x] **Workspace CI 通过**
  Test: manual:bash -c "gh run list --repo perfectuser21/cecelia --branch cp-03031515-agent-config-ux-v2 --limit 1 --json conclusion | grep -q success"
