---
id: dod-dashboard-projects-compare
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

## DoD - feat(dashboard): 实现 /projects/compare 项目并排对比页面

### 验收清单

- [x] 新建 `apps/api/features/planning/pages/ProjectCompare.tsx`
  Test: manual:bash -c "grep -c 'ProjectCompare' apps/api/features/planning/pages/ProjectCompare.tsx"

- [x] `index.ts` 注册路由 `/projects/compare` 和组件 `ProjectCompare`
  Test: manual:bash -c "grep -c 'projects/compare' apps/api/features/planning/index.ts"

- [x] `ProjectsDashboard.tsx` 添加「对比项目」入口按钮，点击跳转 `/projects/compare`
  Test: manual:bash -c "grep -c 'compare' apps/api/features/planning/pages/ProjectsDashboard.tsx"

- [x] `ProjectCompare.tsx` 实现多选下拉（2-4 个项目），使用 GET /api/tasks/projects 加载项目列表
  Test: manual:bash -c "grep -c 'api/tasks/projects' apps/api/features/planning/pages/ProjectCompare.tsx"

- [x] `ProjectCompare.tsx` 选择项目后调用 POST /api/brain/projects/compare/report 获取对比数据
  Test: manual:bash -c "grep -c 'api/brain/projects/compare/report' apps/api/features/planning/pages/ProjectCompare.tsx"

- [x] 并排对比卡片展示：评分、进度条、任务统计、优劣势
  Test: manual:bash -c "grep -c 'strengths\|weaknesses\|progress\|score' apps/api/features/planning/pages/ProjectCompare.tsx"

- [x] 深色主题一致（使用 slate/gray 调色板，与其他页面风格一致）
  Test: manual:bash -c "grep -c 'slate-\|gray-' apps/api/features/planning/pages/ProjectCompare.tsx"
