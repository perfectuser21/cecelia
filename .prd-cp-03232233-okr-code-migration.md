# PRD: OKR 业务代码迁移（批次1）

branch: cp-03232233-okr-code-migration
task_id: 326260da-3493-4a59-87d3-6bbffb347587

## 背景

Cecelia 系统已在 Migration 177-182 中建立了新的 OKR 层级表（visions/objectives/key_results/okr_projects/okr_scopes/okr_initiatives），数据已迁移（UUID 保留），双向同步触发器已就位。

当前问题：大量业务代码仍在查询旧的 `goals` 和 `projects` 表，导致新表与旧表并存，造成混乱。需要将业务代码迁移到新表。

## 目标

将 3 个核心业务文件从旧表迁移到新 OKR 表，不新增 DB migration，不影响现有测试。

## 范围

**目标文件**：
- `packages/brain/src/kr-verifier.js` — KR 进度验证，goals → key_results
- `packages/brain/src/orchestrator-realtime.js` — 实时状态查询，goals → UNION 新表
- `packages/brain/src/heartbeat-inspector.js` — 心跳检查活跃 OKR，goals → key_results

**不做什么**：
- 不修改 initiative-closer.js、kr-completion.js、okr-tick.js、kr-progress.js
- 不新增 DB migration
- 不修改 selfcheck.js（schema version 保持 182）

## 成功标准

## 成功标准

1. kr-verifier.js 中不再引用 `FROM goals` / `UPDATE goals`，改为 `key_results`
2. orchestrator-realtime.js 状态查询改为 UNION 全部新 OKR 表
3. heartbeat-inspector.js active_okrs 查询改为 `key_results`
4. CI L1/L2/L3 全通过
