---
id: task-cp-03191714-selfdrive-whitelist
type: task-card
branch: cp-03191714-selfdrive-whitelist
created: 2026-03-19
---

# Task Card: Self-Drive 加入 systemSources 白名单

## 需求（What & Why）
**功能描述**: self_drive 作为 trigger_source 加入 isSystemTask 白名单，允许 Self-Drive 引擎创建无 goal_id 的系统任务。
**背景**: Self-Drive 第一次运行时建议了 3 个任务但全部创建失败，因为 createTask 要求 goal_id。

## 成功标准
1. [BEHAVIOR] self_drive 在 systemSources 白名单中
2. [GATE] 所有现有测试通过

## 验收条件（DoD）

- [x] [BEHAVIOR] actions.js 的 isSystemTask 函数 systemSources 包含 self_drive
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/actions.js','utf8');if(!c.includes('self_drive'))process.exit(1)"

- [x] [BEHAVIOR] auto_fix 也在 systemSources 白名单中（Probe 故障修复同理）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/actions.js','utf8');if(!c.includes('auto_fix'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -5"
