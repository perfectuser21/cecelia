# DoD: fix(brain): executeResearch 加入 NotebookLM web search 步骤

## 完成条件

- [x] [ARTIFACT] content-pipeline-executors.js 中 executeResearch 包含 `notebooklm source add-research` 调用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes('source add-research'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] content-pipeline-executors.js 中 executeResearch 包含 `notebooklm research wait` 调用
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes('research wait'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] content-pipeline-executors.js 中 executeResearch ask 后有清空 sources 逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');if(!c.includes('deleteSource'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] executeResearch 函数在 ask 之前执行 source add-research 和 research wait 步骤
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/content-pipeline-executors.js','utf8');const addIdx=c.indexOf('source add-research');const waitIdx=c.indexOf('research wait');const askIdx=c.indexOf('notebooklm ask');if(addIdx<0||waitIdx<0||askIdx<0)process.exit(1);if(addIdx>askIdx||waitIdx>askIdx)process.exit(1);console.log('OK: add-research('+addIdx+') wait('+waitIdx+') ask('+askIdx+')')"

## 成功标准

executeResearch 在 ask 之前先添加并等待 web research sources，ask 后清空 notebook sources 以便复用。
