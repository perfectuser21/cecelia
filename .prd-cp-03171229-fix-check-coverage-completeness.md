# PRD: 补 check-coverage-completeness.mjs CI 接入与测试

**Branch**: cp-03171229-fix-check-coverage-completeness
**Task ID**: 68966a37-6a20-408b-96d7-0121cfd139c1
**Priority**: P1
**Created**: 2026-03-17

## 背景

PR #1004 关闭时遗漏了 `check-coverage-completeness.mjs`。虽然后来通过 commit 69800a629 将脚本本身合并进了 `packages/engine/scripts/devgate/`，但：
1. 没有对应的测试文件
2. 没有接入任何 CI

## 目标

1. 为 `check-coverage-completeness.mjs` 编写测试文件
2. 将其接入 `ci-l2-consistency.yml` 的 `engine-l2` job
3. bump engine 版本（feat → minor）

## 成功标准

- `packages/engine/tests/devgate/check-coverage-completeness.test.ts` 存在且测试通过
- `ci-l2-consistency.yml` 的 engine-l2 job 中新增调用步骤
- engine 版本从 12.94.0 升至 12.95.0
- CI 全部通过

## 不做什么

- 不修改 `check-coverage-completeness.mjs` 脚本内容
- 不接入 brain-l2（该脚本属于 engine，应放 engine-l2）
