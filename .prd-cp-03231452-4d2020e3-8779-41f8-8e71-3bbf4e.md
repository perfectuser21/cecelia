# PRD: 重构 capability-scanner.scanCapabilities（复杂度 42 → 10）

## 背景
代码复杂度扫描发现 `packages/brain/src/capability-scanner.js` 中的 `scanCapabilities` 函数圈复杂度为 42（阈值 10），需要重构。

## 目标
重构 `scanCapabilities` 函数，将圈复杂度降至 10 以下，提高可维护性。

## 范围
- 文件：`packages/brain/src/capability-scanner.js`
- 函数：`scanCapabilities`（第 69 行）
- 纯结构重构，不改变行为

## 方案
提取辅助函数：`evaluateCapability`、`determineStatus`、`collectSkillActivity`、`collectTableData`、`checkBrainEmbeddedSources`、`loadEmbeddedSourcesActive`

## 验收标准
- scanCapabilities 函数体 ≤ 60 行
- npm test 通过
