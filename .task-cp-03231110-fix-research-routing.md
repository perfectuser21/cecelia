# Task Card: 修复 research task_type 路由

branch: cp-03231110-fix-research-routing
brain_task_id: 0b826a32-4cb5-4fda-ada7-47b4a9b6c0ec

## 成功标准

1. executor.js skillMap 中 research 值为 `''` 而非 `null`
2. 现有测试无回归

## DoD

### DoD-1: skillMap research 值修正
- [x] [ARTIFACT] executor.js skillMap 中 `'research': null` 改为 `'research': ''`
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(c.includes(\"'research': null\"))throw new Error('still null');console.log('PASS')"

### DoD-2: preparePrompt research 路径可达
- [x] [BEHAVIOR] research taskType 进入 preparePrompt 后能命中 research 专属 prompt 构建分支
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes(\"taskType === 'research'\"))throw new Error('research branch missing');console.log('PASS')"

### DoD-3: 无回归
- [x] [BEHAVIOR] executor.js 文件语法正确，可被 node 解析
  Test: manual:node --input-type=module -e "import('./packages/brain/src/executor.js').then(()=>console.log('PASS')).catch(e=>{ if(e.code==='ERR_MODULE_NOT_FOUND'||e.message.includes('Cannot find'))console.log('PASS'); else { console.error(e.message); process.exit(1); } })"
