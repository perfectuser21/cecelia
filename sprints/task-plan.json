{
  "initiative_id": "pending",
  "journey_type": "autonomous",
  "journey_type_reason": "仅涉及 packages/brain/ 内部路由，无 UI / 无外部 agent 协议，evaluator 用 curl localhost:5221 验证",
  "tasks": [
    {
      "task_id": "ws1",
      "title": "新增 GET /api/brain/version 路由",
      "scope": "在 packages/brain/src/routes/status.js 注册 GET /version 路由。handler 内部使用独立 try-catch + readFileSync（new URL('../../package.json', import.meta.url)）读取 package.json version，从 selfcheck.js import EXPECTED_SCHEMA_VERSION，返回 {version, schema_version}。package.json 不可读时返回 HTTP 500 + {error:'version read failed'}。多余 query 参数忽略。",
      "dod": [
        "[ARTIFACT] packages/brain/src/routes/status.js 含 /version 路由注册",
        "[ARTIFACT] 路由内使用 EXPECTED_SCHEMA_VERSION 从 selfcheck.js 导入",
        "[ARTIFACT] selfcheck.js 含 export const EXPECTED_SCHEMA_VERSION",
        "[ARTIFACT] /version handler 含 try-catch + HTTP 500",
        "[BEHAVIOR] GET /api/brain/version 返回 HTTP 200，version 字段类型为 string",
        "[BEHAVIOR] GET /api/brain/version 返回 schema_version 字段类型为 string",
        "[BEHAVIOR] 响应 keys 完全等于 [schema_version,version]，不多不少",
        "[BEHAVIOR] 禁用字段 ver/v/pkg_version/db_version/build/tag/release 均不出现",
        "[BEHAVIOR] package.json chmod 000 时返回 HTTP 500 + error 字段",
        "[BEHAVIOR] 多余 query 参数被忽略，仍返回 HTTP 200",
        "[BEHAVIOR] version 值与 package.json 实际值一致（值来源 oracle）",
        "[BEHAVIOR] version 符合 semver 格式 x.y.z",
        "[BEHAVIOR] schema_version 值与 selfcheck.js EXPECTED_SCHEMA_VERSION 一致"
      ],
      "files": [
        "packages/brain/src/routes/status.js"
      ],
      "depends_on": [],
      "complexity": "S",
      "estimated_minutes": 25
    }
  ]
}
