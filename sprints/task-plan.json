{
  "initiative_id": "pending",
  "journey_type": "autonomous",
  "journey_type_reason": "仅动 playground/server.js + tests + README，无 UI / 无 Brain / 无外部 agent 协议；target_environment=playground，evaluator 用 localhost:3001 验证",
  "tasks": [
    {
      "task_id": "ws1",
      "title": "playground/server.js 新增 GET /ping 路由 + tests + README 段（smoke fire）",
      "scope": "在 playground/server.js 注册 GET /ping，无视 query 参数，返回 HTTP 200 + {pong:true}（pong 为布尔字面 true，禁用字段 ok/status/alive/message/data，顶层 keys 仅 [\"pong\"]）；在 playground/tests/server.test.js 新增 describe('GET /ping') 块（200+pong:true / keys 完整性 / 禁用字段反向 / query 忽略边界 / POST→404 负向）；playground/README.md 新增 /ping 段",
      "dod": [
        "[ARTIFACT] playground/server.js 含 app.get('/ping' 路由注册",
        "[ARTIFACT] playground/server.js /ping 响应含字面 pong 字段（禁用 ok/status/alive/message/data）",
        "[ARTIFACT] playground/tests/server.test.js 含 describe('GET /ping') 块",
        "[ARTIFACT] playground/README.md 含 /ping 段",
        "[BEHAVIOR] GET /ping → HTTP 200 + {pong:true}（schema 字段值，pong 布尔）",
        "[BEHAVIOR] GET /ping → 顶层 keys 完全等于 [\"pong\"]（schema 完整性）",
        "[BEHAVIOR] GET /ping → 禁用字段 ok/status/alive 均不存在（反向验证）",
        "[BEHAVIOR] GET /ping?x=1&foo=bar → 仍 200 + {pong:true}（query 忽略边界）",
        "[BEHAVIOR] POST /ping → 404（仅 GET 注册的负向验证）"
      ],
      "files": [
        "playground/server.js",
        "playground/tests/server.test.js",
        "playground/README.md"
      ],
      "depends_on": [],
      "complexity": "S",
      "estimated_minutes": 30
    }
  ]
}
