{
  "initiative_id": "pending",
  "journey_type": "dev_pipeline",
  "journey_type_reason": "本次 sprint 核心目标是验证 B42 propose_branch mismatch warn+fallback 修复在完整三阶段 harness pipeline（Phase A→B→C）中正确生效，playground /abs 作为运行载体",
  "tasks": [
    {
      "task_id": "ws1",
      "title": "playground/server.js 新增 GET /abs 端点",
      "scope": "在 playground/server.js 新增 /abs 路由：query 参数 n（严格数字，使用 STRICT_NUMBER 正则），成功返回 {result: Math.abs(n), operation: 'abs'}，非法输入（非数字、缺少 n）返 HTTP 400 + {error: '...'}",
      "dod": [
        "[BEHAVIOR] GET /abs?n=-5 返回 {result:5, operation:'abs'}，result 类型为 number",
        "[BEHAVIOR] GET /abs?n=0 返回 {result:0, operation:'abs'}",
        "[BEHAVIOR] GET /abs?n=3 返回 {result:3, operation:'abs'}",
        "[BEHAVIOR] schema keys 恰好为 ['operation','result']，无多余字段",
        "[BEHAVIOR] 禁用字段 value/answer/data 不存在于 response",
        "[BEHAVIOR] GET /abs?n=foo → HTTP 400 + body has('error') + body has('message')|not",
        "[BEHAVIOR] GET /abs（无 n 参数）→ HTTP 400 + body has('error') + body has('message')|not",
        "[ARTIFACT] playground/server.js 含 /abs 路由，query 参数名字面量 n"
      ],
      "files": ["playground/server.js"],
      "depends_on": [],
      "complexity": "S",
      "estimated_minutes": 20
    }
  ]
}
