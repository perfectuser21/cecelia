{
  "initiative_id": "9d693319-2577-487c-9420-79a7420624be",
  "journey_type": "user_facing",
  "journey_type_reason": "入口是用户在 Dashboard 浏览器打开 /harness/:id 页面，核心价值在前端 EventSource 实时日志区自动追加节点推进——用户直接感知",
  "tasks": [
    {
      "task_id": "ws1",
      "title": "DB Migration — initiative_run_events 表",
      "scope": "新建 initiative_run_events 表 migration SQL 文件，含 initiative_id/node/status/attempt/verdict/ts(BIGINT) 字段（无 label 列）和 (initiative_id, ts) 复合索引",
      "dod": [
        "[ARTIFACT] packages/brain/migrations/ 含 initiative_run_events migration 文件，包含 CREATE TABLE 和 CREATE INDEX",
        "[BEHAVIOR] psql 可查到表结构（含 node/status/attempt/ts 列，无 label 列），INSERT node_update 行（含 ts BIGINT）成功返回 id"
      ],
      "files": [
        "packages/brain/migrations/YYYYMMDD_initiative_run_events.sql"
      ],
      "depends_on": [],
      "complexity": "S",
      "estimated_minutes": 20
    },
    {
      "task_id": "ws2",
      "title": "Brain SSE 端点 GET /api/brain/harness/stream",
      "scope": "packages/brain/src/routes/harness.js 新增 GET /stream SSE 端点（initiative_id 参数校验，2s 轮询 initiative_run_events，30s keepalive `: keepalive`）；packages/brain/src/events/initiativeRunEvents.js 提供 writeInitiativeRunEvent helper",
      "dod": [
        "[ARTIFACT] packages/brain/src/routes/harness.js 含 /stream 路由，text/event-stream Content-Type",
        "[ARTIFACT] packages/brain/src/events/initiativeRunEvents.js 导出 writeInitiativeRunEvent 函数",
        "[BEHAVIOR] GET /stream?initiative_id={id} 推 event: node_update，data keys 恰好为 [attempt,node,status,ts]，ts 为 number",
        "[BEHAVIOR] 禁用字段 name/step/timestamp/label 不存在于 node_update data",
        "[BEHAVIOR] 未知 initiative_id → 404 {error: string}（禁用 message/msg/reason）",
        "[BEHAVIOR] 缺 initiative_id → 400 {error: string}",
        "[BEHAVIOR] status=run_completed 行 → event: run_completed，data 含 initiative_run_id/verdict/ts（无 status 字段）"
      ],
      "files": [
        "packages/brain/src/routes/harness.js",
        "packages/brain/src/events/initiativeRunEvents.js"
      ],
      "depends_on": ["ws1"],
      "complexity": "M",
      "estimated_minutes": 45
    },
    {
      "task_id": "ws3",
      "title": "executor 写入 initiative_run_events",
      "scope": "harness executor 相关文件新增 writeInitiativeRunEvent 调用，节点入口（status=running）/出口（status=done/failed）写入 initiative_run_events（无 label 字段）",
      "dod": [
        "[ARTIFACT] executor 文件 import writeInitiativeRunEvent 且函数体内含调用",
        "[BEHAVIOR] writeInitiativeRunEvent({initiativeId, node, status, attempt}) 调用后 initiative_run_events 有对应行，ts 字段为 BIGINT（≥ 1000000000）"
      ],
      "files": [
        "packages/brain/src/executor.js"
      ],
      "depends_on": ["ws1", "ws2"],
      "complexity": "S",
      "estimated_minutes": 25
    },
    {
      "task_id": "ws4",
      "title": "Dashboard HarnessDetailPage /harness/:id",
      "scope": "apps/dashboard/src/pages/harness/HarnessDetailPage.tsx 新建页面（含 EventSource 连接到 /api/brain/harness/stream?initiative_id=:id，实时日志区 data-testid=realtime-log，节点行 data-testid=log-entry，监听 run_completed 事件显示完成状态）+ router 注册 /harness/:id",
      "dod": [
        "[ARTIFACT] HarnessDetailPage.tsx 含 EventSource，URL 使用 initiative_id 参数（禁用 taskId/id/task_id）",
        "[ARTIFACT] HarnessDetailPage.tsx 含 data-testid=realtime-log 和 data-testid=log-entry",
        "[ARTIFACT] router 含 /harness/:id → HarnessDetailPage 路由",
        "[BEHAVIOR] /harness/:id 页面渲染，data-testid=realtime-log 可见",
        "[BEHAVIOR] 收到 node_update 后日志区追加 log-entry 节点行",
        "[BEHAVIOR] run_completed 事件（非 done）后显示 Pipeline 已完成 / Pipeline 失败 状态文字"
      ],
      "files": [
        "apps/dashboard/src/pages/harness/HarnessDetailPage.tsx",
        "apps/dashboard/src/config/routes.tsx"
      ],
      "depends_on": ["ws2"],
      "complexity": "M",
      "estimated_minutes": 50
    }
  ]
}
