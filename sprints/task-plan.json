{
  "initiative_id": "pending",
  "journey_type": "user_facing",
  "journey_type_reason": "主入口是用户在 Dashboard 浏览器访问 /pipeline/:id 详情页，核心价值在前端实时日志区自动推进——不是 Brain 内部 tick，是用户主动发起的可视化会话",
  "tasks": [
    {
      "task_id": "ws1",
      "title": "Brain SSE 端点: 在 harness.js 新增 GET /stream",
      "scope": "在 packages/brain/src/routes/harness.js 新增 GET /stream SSE 端点：从 task_events 表每 2s 轮询 graph_node_update 行，映射字段 (nodeName→node, attemptN→attempt, created_at→ts) + 中文标签 label；task completed/failed 发 event: done；每 30s 发 keepalive comment；缺参数 400，未知 ID 404",
      "dod": [
        "[BEHAVIOR] GET /stream?planner_task_id={id} 推送 event: node_update，data JSON 含 node/label/attempt/ts 字段，keys 恰好为 [\"attempt\",\"label\",\"node\",\"ts\"]",
        "[BEHAVIOR] 禁用字段 nodeName/timestamp/name/type/payload/result 不出现在 data 中",
        "[BEHAVIOR] task completed/failed 后发送 event: done，data.status 为 completed 或 failed",
        "[BEHAVIOR] 缺 planner_task_id → 400 + error key（非 message）；未知 UUID → 404 + error key",
        "[ARTIFACT] packages/brain/src/routes/harness.js 含 router.get('/stream' 路由及 graph_node_update 过滤"
      ],
      "files": [
        "packages/brain/src/routes/harness.js"
      ],
      "depends_on": [],
      "complexity": "M",
      "estimated_minutes": 40
    },
    {
      "task_id": "ws2",
      "title": "Dashboard 实时日志区: HarnessPipelineDetailPage 新增 EventSource hook",
      "scope": "在 apps/dashboard/src/pages/harness-pipeline/HarnessPipelineDetailPage.tsx 新增实时日志区：useEffect 内建立 EventSource 连接到 GET /api/brain/harness/stream?planner_task_id={id}，监听 node_update 事件追加日志条目（label + ts），监听 done 事件显示完成状态，组件卸载时 close()",
      "dod": [
        "[BEHAVIOR] EventSource URL 使用 planner_task_id query param（非禁用名 task_id/taskId/pipeline_id）",
        "[BEHAVIOR] 组件读取 data.node / data.label / data.attempt / data.ts 字段（不用 nodeName/timestamp）",
        "[BEHAVIOR] done 事件触发后显示"Pipeline 已完成 ✅"或"Pipeline 失败 ❌"文案",
        "[BEHAVIOR] useEffect cleanup 调用 eventSource.close()（无内存泄漏）",
        "[ARTIFACT] apps/dashboard/src/pages/harness-pipeline/HarnessPipelineDetailPage.tsx 含 new EventSource( 构造及实时日志区渲染"
      ],
      "files": [
        "apps/dashboard/src/pages/harness-pipeline/HarnessPipelineDetailPage.tsx"
      ],
      "depends_on": ["ws1"],
      "complexity": "M",
      "estimated_minutes": 45
    }
  ]
}
