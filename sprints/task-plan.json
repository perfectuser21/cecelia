{
  "initiative_id": "pending",
  "journey_type": "dev_pipeline",
  "journey_type_reason": "验证 packages/brain/src/workflows/harness-initiative.graph.js runSubTaskNode → extractTargetEnv → executeOnHost pipeline 机制，无 UI 交互，无新代码变更",
  "tasks": [
    {
      "task_id": "ws1",
      "title": "执行 mac_web pipeline E2E 验证（Slice4 透传 gap 修复确认）",
      "scope": "运行 final-e2e 脚本：（1）验证 Slice4 fix 代码就位（源码检查）；（2）运行 harness 路由回归单测；（3）POST harness_task（target_environment=mac_web）到 Brain 5221；（4）等待 /tmp/cecelia-host-prompts/ 出现 .host.prompt 文件（executeOnHost 调用证明）；（5）等待任务在 120s 内离开 running 状态；（6）写入 sprints/e2e-verify-report.json（status=PASS）",
      "dod": [
        "[BEHAVIOR] Slice4 fix 代码就位：runSubTaskNode 源码含 target_environment 透传（node -e 静态验证 exit 0）",
        "[BEHAVIOR] harness-task-evaluator-host-routing.test.js 回归单测全 PASS",
        "[BEHAVIOR] POST harness_task（target_environment=mac_web）→ Brain 5221 → HTTP 200 + task_id（接缝）",
        "[BEHAVIOR] /tmp/cecelia-host-prompts/ 30s 内出现 ${TASK_ID}.host.prompt（executeOnHost 真实调用证明，接缝）",
        "[BEHAVIOR] tasks 表 status 在 120s 内变为 completed 或 failed（不卡死，接缝）",
        "[ARTIFACT] sprints/e2e-verify-report.json 存在且 status=PASS"
      ],
      "files": [
        "sprints/e2e-verify-report.json"
      ],
      "depends_on": [],
      "complexity": "S",
      "estimated_minutes": 30
    }
  ]
}
