{
  "initiative_id": "pending",
  "journey_type": "autonomous",
  "journey_type_reason": "纯后端 harness 验证流程，无 UI 交互，evaluator 自动从 origin/main 同步 playground/ 并 curl 校验 /echo 端点",
  "tasks": [
    {
      "task_id": "ws1",
      "title": "修复 playground/tests/echo.test.js 验证正确 schema（{msg} 而非 {echo}）",
      "scope": "playground/tests/echo.test.js：(1) response 期望从 {echo:'hello'} 改为 {msg:'hello'}；(2) keys assertion 从 ['echo'] 改为 ['msg']；(3) 移除 msg 出禁用 key 列表",
      "dod": [
        "[BEHAVIOR] GET /echo?msg=hello 返回 HTTP 200 + {msg:'hello'}，jq -e '.msg == \"hello\"' 通过",
        "[BEHAVIOR] response keys 完整性恰好 ['msg']，jq -e 'keys == [\"msg\"]' 通过",
        "[BEHAVIOR] 禁用字段 echo 不存在，jq -e 'has(\"echo\") | not' 通过",
        "[BEHAVIOR] GET /echo?msg= 返回 {msg:''}（空字符串边界）",
        "[BEHAVIOR] GET /echo（无 msg 参数）返回 HTTP 400 + error 字段",
        "[ARTIFACT] playground/tests/echo.test.js 不含 { echo: 'hello' } 或 { echo: \"hello\" } 作为 response 期望"
      ],
      "files": [
        "playground/tests/echo.test.js"
      ],
      "depends_on": [],
      "complexity": "S",
      "estimated_minutes": 20
    }
  ]
}
