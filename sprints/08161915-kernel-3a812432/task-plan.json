{
  "initiative_id": "e64c335a-63a0-457e-bd58-02b43ed2ad83",
  "journey_type": "agent_remote",
  "journey_type_reason": "改动核心是 fleet-worker 远端 agent 执行传输（容器化 runner），非 UI/纯后端 CRUD",
  "tasks": [
    {
      "task_id": "ws1",
      "title": "Fleet Runner run 级双容器：工作容器常驻（Planner/Proposer/Reviewer/Generator 共用、候选不丢）+ 干净评估容器（Evaluator 从候选 SHA 干净 clone）",
      "scope": "新增 run-container.cjs（run 级工作容器命名 cecelia-fleet-run-<run8> + eval 容器 cecelia-fleet-eval-<attempt8> + role→容器路由 + FLEET_RUN_SCOPED_CONTAINER fallback）；新增 candidate-quarantine.cjs（候选 git bundle 落 quarantine + eval 干净 clone 防污染）；capacity.js 增 computeRunContainerCapacity（per-run 2GB/2cpu 计量，5GB VM ≥2）；attempt-runner/fleet-worker 接线 run 级建/复用/reconcile/销毁 + container_id 记 initiative_runs & harness_attempts；migration 加 container_id 列；capacity-gate/task-router autonomous_singleton 与 xian 选机改 per-run。不改合同/闸语义、不去 Docker、不派 Claude 到 xian。",
      "dod": [
        "[BEHAVIOR] resolveContainerTarget 同 run 复用、不同 run 隔离、evaluator 干净 attempt 级、fallback off 回退（tests/run-scoped-container.test.mjs + eval-clean-clone.test.mjs 全绿）",
        "[BEHAVIOR] computeRunContainerCapacity(5120MB,8core) ≥ 2",
        "[BEHAVIOR] 候选 bundle 落 quarantine + eval 从 bundle clone 后 HEAD==候选 SHA 且无工作容器污染文件",
        "[BEHAVIOR] 同 run attempt 共享 container_id（psql harness_attempts，Final E2E）、一条 run 稳定 ≤2 容器（docker ps）",
        "[BEHAVIOR] 信任回归全绿：非 root/零 cap/push 拒绝/token 独立（attempt-runner + credential-envelope 现有单测）",
        "[ARTIFACT] initiative_runs.container_id migration + Brain semver bump + DevGate 三闸"
      ],
      "files": [
        "packages/brain/scripts/fleet-worker/run-container.cjs",
        "packages/brain/scripts/fleet-worker/candidate-quarantine.cjs",
        "packages/brain/scripts/fleet-worker/attempt-runner.cjs",
        "packages/brain/scripts/fleet-worker/fleet-worker.cjs",
        "packages/brain/scripts/fleet-worker/workspace-manager.cjs",
        "packages/brain/src/capacity.js",
        "packages/brain/src/capacity-gate.js",
        "packages/brain/src/task-router.js",
        "packages/brain/src/dispatcher.js",
        "packages/brain/migrations/"
      ],
      "depends_on": [],
      "complexity": "L",
      "estimated_minutes": 120
    }
  ]
}
