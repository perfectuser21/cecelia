# Reviewer Round 1 Feedback — REVISION（2 条必修）

事实核验全部通过（executor=claude / host=skill-relay-claude-headed / phase=gan / tui.log 缺失兜底验源码留痕 / e2e-verify.sh 不存在构成合法 Red）。以下 2 条 spec 漏洞必须修，修完达成共识即 APPROVED。

## 问题1（Scope 匹配 PRD，6→7）：合同缺 DoD.md 覆盖
PRD「范围限定」与「预期受影响文件」都要求"更新 DoD.md 记录本 sprint 的 claude-headed relay DoD"，但 contract-draft.md 的 Golden Path / E2E 验收 与 contract-dod.md 全部条目均未提及 DoD.md（grep 无命中）。
**修法**：
- contract-draft.md Golden Path 补一步（DoD.md 记录本 sprint claude-headed relay DoD）。
- contract-dod.md 增加一条 `[ARTIFACT]` 条目，带 manual:bash 验证命令：grep 确认 DoD.md 已记录本 sprint（如含 `skill-relay-claude-headed` 或本 sprint dir 关键字）。

## 问题2（Verification Oracle 完整性，5→7）：CI seed 一致性是弱 oracle
当前合同/DoD/测试三处对 ci.yml 的 Step4 验证只有 4 条 `grep -F` 检查字符串"是否存在于 ci.yml 任意位置"。这是弱 oracle：generator 只需在文件任意处（含注释、死分支）堆出 4 个目标字符串即可让 DoD 全绿，而"claude-headed 任务被误 seed 成 codex 身份"这个真实回归 bug 可能原封不动。ci.yml 现存 962-1034 行的宽松 OR 条件会把任何 headed 任务无条件 seed 成 codex 身份。
**修法（二选一，建议 A）**：
- A. **顺序性静态断言**：用行号确认 claude-headed 精确匹配分支的判定**优先于**原通用 codex OR 分支。例如断言含 `skill-relay-claude-headed` seed 的分支行号 < 原 codex 通用兜底分支行号；或断言 seed 逻辑用 executor 值精确分流（`executor=claude` → claude host，`executor=codex` → codex host）而非无条件 codex。
- B. **运行时行为验证**：模拟两种 task-card（claude-headed / codex-headed）灌入 CI 检测逻辑，断言各自路由到正确的 orchestrator_host seed。
- 同步更新 contract-dod.md 对应 [BEHAVIOR] 条目 + tests/headed-smoke-contract.test.ts 的对应断言，使测试真能捕获"误 seed 成 codex"的回归。

## 其余维度（无需改动，已达标）
- dod_machineability=8, test_is_red=8, internal_consistency=8, risk_registered=8, ci_workflow_alignment=10。
- 保持 codex-headed 既有分支不回归（合同已覆盖，勿删）。
