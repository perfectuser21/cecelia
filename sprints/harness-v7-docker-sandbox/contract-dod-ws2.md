# Contract DoD — Workstream 2: Docker 容器化执行（Round 2）

- [ ] [ARTIFACT] docker/harness-runner/Dockerfile 存在且包含实际的 claude CLI 安装指令 + Node.js 运行时
  Test: manual:node -e "const c=require('fs').readFileSync('docker/harness-runner/Dockerfile','utf8');if(!c.includes('FROM'))process.exit(1);if(!/RUN.*install.*claude|COPY.*claude|claude.*--version|npm.*install.*-g.*@anthropic/.test(c)){console.log('FAIL: 未实际安装 claude CLI');process.exit(1)}if(!/node|NODE_VERSION|nvm|FROM.*node/.test(c)){console.log('FAIL: 缺少 Node.js');process.exit(1)}console.log('PASS')"
- [ ] [BEHAVIOR] cecelia-run.sh 含 HARNESS_DOCKER_ENABLED 条件分支 + docker run --memory + setsid 回退（非注释代码）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-run.sh','utf8');const lines=c.split('\n').filter(l=>!l.trim().startsWith('#'));const code=lines.join('\n');if(!/HARNESS_DOCKER_ENABLED/.test(code)){console.log('FAIL: 无条件分支');process.exit(1)}if(!/docker run/.test(code)){console.log('FAIL: 无 docker run');process.exit(1)}if(!/--memory/.test(code)){console.log('FAIL: 无 --memory');process.exit(1)}if(!/setsid/.test(code)){console.log('FAIL: 无 setsid 回退');process.exit(1)}console.log('PASS')"
- [ ] [BEHAVIOR] CONTAINER_SIZES 常量值合理：light >= 256 MB、normal >= 512 MB、heavy >= 1024 MB（非注释代码）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');const lines=c.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*'));const code=lines.join('\n');const m=code.match(/CONTAINER_SIZES\s*=\s*\{[\s\S]*?\}/);if(!m){console.log('FAIL: 无 CONTAINER_SIZES');process.exit(1)}const obj=m[0];const l=obj.match(/light[^,}]*?(\d{3,})/);const n=obj.match(/normal[^,}]*?(\d{3,})/);const h=obj.match(/heavy[^,}]*?(\d{3,})/);if(!l||parseInt(l[1])<256){console.log('FAIL: light<256');process.exit(1)}if(!n||parseInt(n[1])<512){console.log('FAIL: normal<512');process.exit(1)}if(!h||parseInt(h[1])<1024){console.log('FAIL: heavy<1024');process.exit(1)}console.log('PASS: light='+l[1]+' normal='+n[1]+' heavy='+h[1])"
- [ ] [BEHAVIOR] Docker daemon 不可用时 cecelia-run.sh 检测（docker info/version）并回退到 setsid（非注释代码，控制流正确）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/scripts/cecelia-run.sh','utf8');const lines=c.split('\n').filter(l=>!l.trim().startsWith('#'));const code=lines.join('\n');if(!/docker info|docker version/.test(code)){console.log('FAIL: 无 docker 可用性检测');process.exit(1)}const di=code.indexOf('docker info')!==-1?code.indexOf('docker info'):code.indexOf('docker version');const si=code.indexOf('setsid bash');if(di<0||si<0||si<di){console.log('FAIL: 控制流不正确');process.exit(1)}console.log('PASS')"
