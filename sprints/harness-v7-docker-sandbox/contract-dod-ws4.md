# Contract DoD — Workstream 4: 监控端点 + Dashboard 页面（Round 2）

- [ ] [BEHAVIOR] GET /api/brain/harness/pipeline-health 返回 200 + JSON，pipelines 数组每个元素含 pipeline_id/pipeline_stuck/last_activity 字段
  Test: manual:curl -sf localhost:5221/api/brain/harness/pipeline-health | node -e "const d=JSON.parse(require('fs').readFileSync('/dev/stdin','utf8'));if(!Array.isArray(d.pipelines)){console.log('FAIL: 缺 pipelines');process.exit(1)}if(!('failure_rate' in d)){console.log('FAIL: 缺 failure_rate');process.exit(1)}d.pipelines.forEach((p,i)=>{if(!p.pipeline_id||!('pipeline_stuck' in p)||!p.last_activity){console.log('FAIL: pipeline['+i+'] 缺必要字段');process.exit(1)}});console.log('PASS: '+d.pipelines.length+' pipelines')"
- [ ] [BEHAVIOR] ops.js 非注释代码中含 pipeline_stuck 实现 + 6小时阈值 + DB 查询
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/ops.js','utf8');const lines=c.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*'));const code=lines.join('\n');if(!/pipeline_stuck/.test(code)){console.log('FAIL: 无 pipeline_stuck');process.exit(1)}if(!/6\s*\*\s*60|360|21600|hours.*6|6.*hour/i.test(code)){console.log('FAIL: 无6小时阈值');process.exit(1)}if(!/SELECT|query|FROM.*harness|pool\.query|db\.query/.test(code)){console.log('FAIL: 无 DB 查询');process.exit(1)}console.log('PASS')"
- [ ] [ARTIFACT] server.js 非注释代码中注册了 pipeline-health 路由
  Test: node -e "const c=require('fs').readFileSync('packages/brain/src/server.js','utf8');const lines=c.split('\n').filter(l=>!l.trim().startsWith('//')&&!l.trim().startsWith('*'));if(!lines.join('\n').includes('pipeline-health'))process.exit(1);console.log('OK')"
- [ ] [BEHAVIOR] Dashboard 监控组件含实际 API 调用 + 空状态条件渲染 + stuck 视觉区分
  Test: manual:node -e "const fs=require('fs');const files=fs.readdirSync('apps/dashboard/src',{recursive:true}).filter(f=>/harness|pipeline/i.test(f)&&/\.(tsx|jsx)$/.test(f));if(files.length===0){console.log('FAIL: 无组件');process.exit(1)}const c=files.map(f=>fs.readFileSync('apps/dashboard/src/'+f,'utf8')).join('\n');if(!/fetch\(|useSWR|useQuery|axios/.test(c)){console.log('FAIL: 无 API 调用');process.exit(1)}if(!/\.length\s*===\s*0|!.*\.length|\?.*empty|暂无|no.*pipeline/i.test(c)){console.log('FAIL: 无空状态');process.exit(1)}if(!/stuck|warning|error|red|danger/i.test(c)){console.log('FAIL: 无 stuck 视觉区分');process.exit(1)}console.log('PASS: '+files.join(','))"
- [ ] [BEHAVIOR] Dashboard 监控组件含 loading + error 状态处理（失败路径）
  Test: manual:node -e "const fs=require('fs');const files=fs.readdirSync('apps/dashboard/src',{recursive:true}).filter(f=>/harness|pipeline/i.test(f)&&/\.(tsx|jsx)$/.test(f));const c=files.map(f=>fs.readFileSync('apps/dashboard/src/'+f,'utf8')).join('');if(!/loading|isLoading|Loading|Spinner|skeleton/i.test(c)){console.log('FAIL: 无 loading');process.exit(1)}if(!/error|Error|catch|onError/i.test(c)){console.log('FAIL: 无 error');process.exit(1)}console.log('PASS')"
