# PRD: fix(engine): verify-step.sh 强制校验 Planner seal 文件存在

**Branch**: `cp-03311755-fix-verify-step-planner-seal`
**Brain Task**: `ee40b5b8-acd1-45da-b888-16fe286c53bc`
**类型**: fix

## 背景

`verify_step1()` 在 `step_1_spec: done` 写入 `.dev-mode` 时被调用，但只校验 Task Card 格式，未检查 Planner seal 文件（`.dev-gate-planner.{branch}`）是否物理存在。这导致 Claude 可在未运行 Planner subagent 的情况下直接进入 Stage 2 写代码，Sprint Contract 形同虚设。

## 变更范围

- `packages/engine/hooks/verify-step.sh`：`verify_step1()` 末尾添加 Gate Planner 检查
- `packages/engine/tests/verify-step-planner-seal.test.ts`：新增测试
- Engine 版本 bump：13.74.1 → 13.74.2

## 成功标准

- `verify_step1()` 在 Planner seal 缺失时返回 exit 1，输出明确错误
- `verify_step1()` 在 Planner seal 存在时正常通过
- 原有 Task Card 格式检查逻辑不受影响
- CI Gate 门禁检查通过，Engine 版本 bump 同步到 5 个文件（13.74.2）
