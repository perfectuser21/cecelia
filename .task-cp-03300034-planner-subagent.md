---
id: task-cp-03300034-planner-subagent
type: task-card
branch: cp-03300034-planner-subagent
created: 2026-03-30
---

# Task Card: Planner 独立 subagent — Stage 1 Task Card 生成拆分

## 需求（What & Why）
**功能描述**: 将 /dev Stage 1 的 Task Card + DoD 生成从主 agent 内部拆成独立 Planner subagent，遵循 Generator subagent 同样的隔离模式。
**背景**: Generator subagent（Stage 2）已成功拆出，主 agent 变成编排者。Stage 1 的 Task Card 生成也应该拆出，让 Planner 只关注需求分析（WHAT），不接触实现细节（HOW）。
**不做什么**: 不修改 spec_review 逻辑；不修改 Stage 2/3/4；不修改 Brain API 交互。

## 成功标准

1. [ARTIFACT] 01-spec.md 包含 Planner subagent 调用流程和隔离规则
2. [BEHAVIOR] Planner subagent prompt 模板只含任务描述 + SYSTEM_MAP，不含 CLAUDE.md 或 brain context
3. [BEHAVIOR] Planner 输出的 Task Card 格式与现有 Task Card 格式兼容（含 DoD 条目、成功标准）
4. [GATE] Sprint Contract Gate（spec_review）仍然正常工作
5. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [PRESERVE] 01-spec.md 的 spec_review Gate 逻辑（Sprint Contract Gate）保持不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('Sprint Contract Gate'))process.exit(1)"

- [x] [PRESERVE] 01-spec.md 的 .dev-mode 写入逻辑保持不变
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('.dev-mode'))process.exit(1)"

- [x] [ARTIFACT] 01-spec.md 包含 Planner subagent 调用节（含 prompt 模板和隔离规则）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('Planner subagent'))process.exit(1)"

- [x] [BEHAVIOR] Planner subagent 隔离规则表明确标注：允许传入（任务描述 + SYSTEM_MAP）和禁止传入（CLAUDE.md + brain context + 代码库细节）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('SYSTEM_MAP'))process.exit(1);if(!c.includes('禁止传入'))process.exit(1);if(!c.includes('允许传入'))process.exit(1)"

- [x] [BEHAVIOR] Planner prompt 模板指示只说 WHAT 不说 HOW（聚焦行为描述，不含实现路径）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/skills/dev/steps/01-spec.md','utf8');if(!c.includes('WHAT'))process.exit(1);if(!c.includes('不说 HOW'))process.exit(1)"

- [x] [ARTIFACT] packages/workflows/skills/dev/steps/02-code.md 同步更新（如有引用 Stage 1 的地方）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/dev/steps/02-code.md','utf8');if(!c.includes('Planner subagent'))process.exit(1)"

- [x] [ARTIFACT] packages/workflows/skills/dev/SKILL.md 同步更新
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/dev/SKILL.md','utf8');if(!c.includes('Planner subagent'))process.exit(1)"

- [x] [GATE] Engine 测试全部通过
  Test: manual:bash -c "cd packages/engine && npm test"

## 实现方案
**要改的文件**:
1. `packages/engine/skills/dev/steps/01-spec.md` — 主要改动：在 1.1.5 和 1.2 之间插入 1.1.8 Planner subagent 调用节
2. `packages/engine/skills/dev/SKILL.md` — 更新流程图：Stage 1 描述加入 Planner subagent
3. `packages/workflows/skills/dev/steps/02-code.md` — 同步更新 planner 引用
4. `packages/workflows/skills/dev/SKILL.md` — 同步更新流程图

**Scope 锚定**: 只改 Stage 1 的 Task Card 生成方式，不动 spec_review Gate、Stage 2-4、Brain API 交互
