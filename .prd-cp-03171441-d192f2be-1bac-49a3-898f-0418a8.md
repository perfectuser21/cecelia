# PRD - refactor(startup): 统一 startup-recovery.js 与 executor.js 孤儿处理逻辑

## 背景
删除 startup-recovery.js 中的 DB 孤儿恢复段，统一由 executor.js::syncOrphanTasksOnStartup 负责完整恢复逻辑。

## 目标
- 删除 runStartupRecovery 中的 DB orphan task recovery 段
- runStartupRecovery 只保留三个环境清理步骤，不接受 pool 参数
- server.js 显式调用 syncOrphanTasksOnStartup

## 成功标准
- startup-recovery.js 中无 UPDATE tasks 语句
- server.js 显式调用 syncOrphanTasksOnStartup
- startup-recovery.test.js 中无重置为 queued 测试
- Brain L4 集成测试通过

## DoD
- grep -c 'UPDATE tasks' packages/brain/src/startup-recovery.js || echo 0
- grep -c 'syncOrphanTasksOnStartup' packages/brain/server.js
- ! grep -q '重置为 queued' packages/brain/src/__tests__/startup-recovery.test.js && echo PASS
