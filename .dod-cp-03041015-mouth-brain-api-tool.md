## DoD - Cecelia 嘴巴添加 call_brain_api 工具 + brain-manifest 注入

### 验收清单

- [x] 新增 `callBrainApi(path, method, body)` 函数，调用 localhost:5221 Brain API
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js

- [x] `callBrainApi` 调用失败时返回 `{ success: false, error: "..." }`（不抛异常）
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js

- [x] 新增 `buildManifestBlock()` 函数，从 `brain-manifest.generated.json` 读取 allActions + allSkills
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js

- [x] `buildManifestBlock` 文件不存在时静默降级（返回空字符串）
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js

- [x] `buildUnifiedSystemPrompt` 调用 `buildManifestBlock`，将结果注入 prompt
  Test: manual:bash -c "grep -q 'buildManifestBlock' packages/brain/src/orchestrator-chat.js"

- [x] `MOUTH_SYSTEM_PROMPT` 添加 `call_brain_api` 到支持的委托类型
  Test: manual:bash -c "grep -q 'call_brain_api' packages/brain/src/orchestrator-chat.js"

- [x] `handleChat` 实现 tool-use 循环：LLM 返回 `call_brain_api` 信号时，同步执行 API 调用，将结果注入后重新调用 LLM，最多 3 轮
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js

- [x] 新增测试：`callBrainApi` 成功、HTTP 失败、网络超时
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js

- [x] 新增测试：`buildManifestBlock` 正常返回、文件缺失降级
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js

- [x] 新增测试：`handleChat` 中 call_brain_api 工具循环
  Test: manual:npm run test --workspace packages/brain -- --run src/__tests__/orchestrator-chat.test.js

- [x] Brain CI 通过
  Test: manual:curl -s http://localhost:5221/api/brain/health | python3 -c "import sys,json; d=json.load(sys.stdin); sys.exit(0 if d.get('status')=='healthy' else 1)"
