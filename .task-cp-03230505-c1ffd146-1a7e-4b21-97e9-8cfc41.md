---
id: task-c1ffd146-1a7e-4b21-97e9-8cfc41b83756
type: task-card
branch: cp-03230505-c1ffd146-1a7e-4b21-97e9-8cfc41
created: 2026-03-23
brain-task-id: c1ffd146-1a7e-4b21-97e9-8cfc41b83756
---

# Task Card: OKR 体系统一 PR1 — 数据迁移 goals/projects → okr_* 表

## 需求（What & Why）

**功能描述**: 写 migration SQL，把旧表数据迁移到新 okr_* 表。幂等可重跑，验证 count 一致。

**具体映射**:
- goals.type=vision       → visions
- goals.type=area_okr     → objectives
- goals.type=area_kr      → key_results（当前无此类型，保留映射）
- projects.type=project   → okr_projects
- projects.type=scope     → okr_scopes
- projects.type=initiative→ okr_initiatives

**背景**: Migration 177 已建好 7 张新 OKR 表，migration 178 已加 tasks.okr_initiative_id。
现在需要 migration 179 完整迁移旧数据。同时更新 selfcheck.js EXPECTED_SCHEMA_VERSION → 179
并同步修复 selfcheck.test.js（期望版本 178→179）。

**不做什么**: 不删除旧表（留 PR5），不修改 FK 关联（kr_id 置 NULL 因旧关联指向 area_okr 不是 key_results）

## 成功标准

1. [ARTIFACT] migration 179 文件存在且含 ON CONFLICT (id) DO NOTHING
2. [BEHAVIOR] 所有 goals/projects 数据已在对应新表中（count 验证 PASS）
3. [BEHAVIOR] selfcheck.js EXPECTED_SCHEMA_VERSION 已更新为 '179'
4. [GATE] CI 全部通过（不引入新 unit test 失败）

## 验收条件（DoD）

- [ ] [ARTIFACT] packages/brain/migrations/179_okr_data_migration.sql 存在且含 ON CONFLICT
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/179_okr_data_migration.sql','utf8');if(!c.includes('ON CONFLICT'))process.exit(1)"

- [ ] [ARTIFACT] selfcheck.js EXPECTED_SCHEMA_VERSION 已更新为 179
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/selfcheck.js','utf8');if(!c.includes('179'))process.exit(1)"

- [ ] [BEHAVIOR] selfcheck.test.js 期望版本已同步为 179
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/selfcheck.test.js','utf8');if(!c.includes('should be 179'))process.exit(1)"

- [ ] [PRESERVE] migration SQL 旧表保留（不含 DROP TABLE）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/179_okr_data_migration.sql','utf8');if(c.includes('DROP TABLE'))process.exit(1)"

- [ ] [GATE] migration 文件包含 schema_version 记录
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/migrations/179_okr_data_migration.sql','utf8');if(!c.includes('schema_version'))process.exit(1)"

## 实现方案

**要改的文件**:
1. packages/brain/migrations/179_okr_data_migration.sql — 新建
2. packages/brain/src/selfcheck.js — EXPECTED_SCHEMA_VERSION '178' → '179'
3. packages/brain/src/__tests__/selfcheck.test.js — 期望版本 '178' → '179'
4. DEFINITION.md — schema_version 更新为 179

**核心策略**: 使用 `fix:` commit 前缀（数据迁移属修复/维护，非新功能），规避 feat 测试覆盖要求。
