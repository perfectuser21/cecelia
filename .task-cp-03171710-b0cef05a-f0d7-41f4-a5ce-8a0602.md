# Task Card — fix(ci): 验证 CI 审计 5 个问题均已修复

## PRD

### 背景
Brain 自动调度的 CI 审计任务，验证 5 个 CI 配置问题已修复。

### 问题清单

**P0**
1. ci-l3-code.yml — coverage-delta 条件依赖 brain-unit.result，brain-unit skip 时 coverage-delta 也 skip

**P1**
2. ci-l3-code.yml — TEST_PATTERNS 缺 .mts 扩展名
3. ci-l3-code.yml — [CONFIG] 豁免路径 ^packages/engine/ 太宽

**P2**
4. ci-l2-consistency.yml — engine-l2 version check 豁免列表缺 release: 前缀
5. ci-l1-process.yml — changes job 失败时 engine output 为空导致误报

## 成功标准

所有 5 个 CI 审计问题在 main 分支中已修复，本次验证确认：

- coverage-delta needs 不含 brain-unit
- TEST_PATTERNS 含 .mts 支持
- [CONFIG] 豁免路径精确匹配 engine 子目录
- engine-l2 version check 含 release: 豁免
- L1 gate 先验证 changes.result 再用 engine output

## DoD

- [x] [BEHAVIOR] coverage-delta job needs 不含 brain-unit（Fix P0）
      Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l3-code.yml','utf8'); const match=c.match(/coverage-delta:[\s\S]*?needs:\s*\[([^\]]+)\]/); if(!match)process.exit(1); process.exit(match[1].includes('brain-unit')?1:0)"

- [x] [BEHAVIOR] TEST_PATTERNS 已包含 .mts 扩展名（Fix P1-2）
      Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l3-code.yml','utf8'); process.exit(c.includes('test\\\\.mts')?0:1)"

- [x] [BEHAVIOR] [CONFIG] 豁免路径精确匹配 engine 子目录（Fix P1-3）
      Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l3-code.yml','utf8'); process.exit(c.includes('packages/engine/(hooks|skills|scripts|config')?0:1)"

- [x] [BEHAVIOR] engine-l2 version check 含 release: 豁免（Fix P2-4）
      Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l2-consistency.yml','utf8'); process.exit(c.includes(\"'release:'\") ? 0:1)"

- [x] [BEHAVIOR] L1 gate 先验证 changes.result 再读 engine output（Fix P2-5）
      Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l1-process.yml','utf8'); process.exit(c.includes('cannot determine engine scope')?0:1)"
