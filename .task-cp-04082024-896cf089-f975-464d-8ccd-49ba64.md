---
id: task-cp-04082024-896cf089-f975-464d-8ccd-49ba64
type: task-card
branch: cp-04082024-896cf089-f975-464d-8ccd-49ba64
created: 2026-04-09
task_id: 896cf089-f975-464d-8ccd-49ba648a481b
---

# Task Card: KR4 geo SEO网站 — 诊断阻断点 + 修复进度追踪

## 需求（What & Why）

**功能描述**: 诊断 KR4（geo SEO网站上线）卡在 25% 的根本原因，修复进度追踪逻辑，并添加 geo 网站健康探针。

**背景**:
- KR4 要求：网站可访问 + ≥1篇内容通过网站首发
- 当前状态：zenithjoyai.com 已部署、3篇 blog 文章可访问、posts 页有内容（PR #2115 已修复 fallback）
- KR 卡在 25% 的原因：`okr_projects` 中 P1=active(50分), P2=inactive(0分) → 平均 25/100
- Cecelia 探针系统无 geo 网站健康检查，无法自动感知网站是否在线

**诊断结论**（部署阻断点）:
1. ✅ 域名/SSL：zenithjoyai.com HTTPS 正常
2. ✅ 内容：3篇 blog + posts 静态 fallback 均可访问（200 OK）
3. ✅ SEO：sitemap-index.xml、robots.txt 配置完整，AI crawler 开放
4. ❌ KR 追踪：P1 仍为 active（实际已上线应为 completed），P2 为 inactive（未启动）
5. ❌ 监控盲区：Cecelia 探针无 geo_website 检查，网站宕机不会自动告警

**不做什么**: 不修改 zenithjoy 仓库代码，不重新构建部署网站，不改 KR verifier SQL。

## 成功标准

> [BEHAVIOR] capability-probe.js 包含 geo_website 探针，检测网站 + blog + posts 三项健康
> [BEHAVIOR] okr_projects P1 = completed，P2 = active，KR4 verifier 下次运行后 progress ≥ 75%

## 验收条件（DoD）

- [x] [ARTIFACT] capability-probe.js 新增 geo_website 探针函数（probeGeoWebsite）
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/capability-probe.js','utf8');if(!c.includes('probeGeoWebsite'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] geo_website 探针注册到 PROBES 数组
  Test: manual:node -e "const fs=require('fs');const c=fs.readFileSync('packages/brain/src/capability-probe.js','utf8');if(!c.includes(\"name: 'geo_website'\"))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] okr_projects P1 状态已更新为 completed
  Test: manual:node -e "const {Pool}=require('pg');const p=new Pool({database:'cecelia'});p.query(\"SELECT status FROM okr_projects WHERE id='f3daa2d1-819b-41a3-b8de-7c3fbed5f7b5'\").then(r=>{if(r.rows[0]?.status!=='completed')process.exit(1);console.log('ok');p.end()})"

- [x] [BEHAVIOR] okr_projects P2 状态已更新为 active（启动 SEO+首发流程）
  Test: manual:node -e "const {Pool}=require('pg');const p=new Pool({database:'cecelia'});p.query(\"SELECT status FROM okr_projects WHERE id='de0580d9-7b95-4293-ae6d-e29a3b13e43d'\").then(r=>{if(r.rows[0]?.status!=='active')process.exit(1);console.log('ok');p.end()})"

## 实现方案

**要改的文件**:
- `packages/brain/src/capability-probe.js`：新增 `probeGeoWebsite` 函数 + 注册到 PROBES 数组

**受影响函数**:
- `PROBES` 数组：新增一项 `{ name: 'geo_website', ... }`
- 新函数 `probeGeoWebsite()`：fetch zenithjoyai.com/zh/（200 + 有标题内容）

**数据变更**（直接 SQL，不改代码）:
- P1 `f3daa2d1-...` status: active → completed
- P2 `de0580d9-...` status: inactive → active

**不改什么**: 不动 KR verifier SQL，不改 zenithjoy 仓库，不改 Brain tick 频率。

## 剩余工程里程碑（KR4完成路径）

1. **M1（本次 PR）**: 添加 geo_website 探针 + 更新 P1/P2 状态 → KR4 进度升至 75%
2. **M2**: P2 完成（SEO配置优化 + 首发内容流程自动化）→ KR4 进度升至 100%
3. **M3**: 首发内容验证（≥1篇内容 through 网站发布，Analytics 可追踪）→ KR4 验收通过
