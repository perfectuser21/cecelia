---
id: dod-fix-selfcheck-version-gte
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD: selfcheck >= 版本检查

## 验收清单

- [ ] D1: EXPECTED_SCHEMA_VERSION 更新为 '142'
      Test: manual:node -e "import('./packages/brain/src/selfcheck.js').then(m => { if (m.EXPECTED_SCHEMA_VERSION !== '142') throw new Error('expected 142'); console.log('OK') })"

- [ ] D2: DB版本高于预期时 PASS（>= 检查验证）
      Test: manual:cd packages/brain && npx vitest run src/__tests__/selfcheck.test.js --reporter=verbose 2>&1 | grep "should pass when DB schema version is ahead"

- [ ] D3: DB版本低于预期时 FAIL（严格向下检查保持）
      Test: manual:cd packages/brain && npx vitest run src/__tests__/selfcheck.test.js --reporter=verbose 2>&1 | grep "should fail when schema version is behind"

- [ ] D4: 所有 selfcheck 单元测试通过（12/12）
      Test: manual:cd packages/brain && npx vitest run src/__tests__/selfcheck.test.js 2>&1 | grep "12 passed"

- [ ] D5: facts-check.mjs 通过
      Test: manual:node scripts/facts-check.mjs 2>&1 | grep "All facts consistent"
