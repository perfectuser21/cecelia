---
id: dod-fix-selfcheck-version-gte
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD: selfcheck >= 版本检查

## 验收清单

- [ ] D1: EXPECTED_SCHEMA_VERSION 更新为 '142' — manual: `node -e "import('./packages/brain/src/selfcheck.js').then(m => { console.assert(m.EXPECTED_SCHEMA_VERSION === '142'); console.log('OK') })"`
- [ ] D2: DB版本高于预期时 PASS — manual: `cd packages/brain && npx vitest run src/__tests__/selfcheck.test.js 2>&1 | grep -E 'pass|fail'`
- [ ] D3: DB版本低于预期时 FAIL（严格向下检查保持）— manual: `cd packages/brain && npx vitest run src/__tests__/selfcheck.test.js 2>&1 | grep -E 'pass|fail'`
- [ ] D4: 所有 selfcheck 单元测试通过 — manual: `cd packages/brain && npx vitest run src/__tests__/selfcheck.test.js --reporter=verbose`
- [ ] D5: facts-check.mjs 通过 — manual: `node scripts/facts-check.mjs`
