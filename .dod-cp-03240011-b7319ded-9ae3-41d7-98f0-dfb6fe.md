# DoD: fix(brain): actions.js catch-all 路由顺序修复

## 验收清单

- [ ] catch-all 路由（`/action/:actionName`）在所有具体路由之后注册
- [ ] `create-project`、`create-scope`、`create-initiative` 三个专用路由仍然存在
- [ ] `actions.js` 语法正确，无 parse 错误

## 验收测试

### 1. catch-all 在具体路由之后
- Test: `node -e "const c=require('fs').readFileSync('packages/brain/src/routes/actions.js','utf8');const catchAllIdx=c.indexOf(\"router.post('/action/:actionName'\");const lastSpecificIdx=Math.max(c.indexOf(\"router.post('/action/create-project'\"),c.indexOf(\"router.post('/action/create-scope'\"),c.indexOf(\"router.post('/action/create-initiative'\")); if(catchAllIdx<lastSpecificIdx){process.exit(1);}console.log('OK: catch-all is after specific routes');"`

### 2. 具体路由均存在
- Test: `node -e "const c=require('fs').readFileSync('packages/brain/src/routes/actions.js','utf8');['create-project','create-scope','create-initiative'].forEach(r=>{if(!c.includes(\"'/action/\"+r+\"'\"))throw new Error('missing: '+r);});console.log('OK: all specific routes present');"`

### 3. 语法检查
- Test: `node --input-type=module --check < packages/brain/src/routes/actions.js 2>&1 && echo "OK: syntax valid"`
