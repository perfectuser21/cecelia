# DoD: fix(brain): actions.js catch-all 路由顺序修复

## 验收清单

- [x] catch-all 路由（`/action/:actionName`）在所有具体路由之后注册
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/actions.js','utf8');const ci=c.indexOf(\"router.post('/action/:actionName'\");const si=Math.max(c.indexOf(\"router.post('/action/create-project'\"),c.indexOf(\"router.post('/action/create-scope'\"),c.indexOf(\"router.post('/action/create-initiative'\")); if(ci<si){process.exit(1);}console.log('OK');"

- [x] `create-project`、`create-scope`、`create-initiative` 三个专用路由仍然存在
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/actions.js','utf8');['create-project','create-scope','create-initiative'].forEach(r=>{if(!c.includes(\"'/action/\"+r+\"'\"))throw new Error('missing: '+r);});console.log('OK');"

- [x] `actions.js` 语法正确，无 parse 错误
  Test: manual:node --input-type=module --check < packages/brain/src/routes/actions.js

- [x] [BEHAVIOR] 调用 `POST /action/create-project` 时由专用处理器处理，而非 catch-all 通用路由
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/actions.js','utf8');const ci=c.indexOf(\"router.post('/action/:actionName'\");const pi=c.indexOf(\"router.post('/action/create-project'\");if(pi>ci)throw new Error('catch-all before create-project');console.log('OK: create-project handler registered before catch-all');"
