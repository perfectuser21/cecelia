# PRD: OKR PR10 — 旧表引用收尾迁移

## 背景

PR1-PR9 完成了主路径迁移，以下文件仍有旧表读写残留。目标：清除所有业务代码对旧表的读取，删除已无必要的 fallback 写入路径。

## 不做什么

- 不改 notion-full-sync.js（goals 表作为 Notion 同步枢纽，有意设计）
- 不删旧表（本 PR 只改代码，删表通过独立 DB migration PR）
- 不改 memory-service.js / project-compare.js（复杂 UNION 查询，单独 PR）

## 成功标准

1. 无业务代码通过 `FROM goals` 或 `FROM projects` 读取 OKR 数据（Notion sync 除外）
2. 无业务代码通过 `UPDATE goals` 或 `UPDATE projects` 写入（Notion sync 除外）
3. 所有现有测试通过（CI L1-L4）
