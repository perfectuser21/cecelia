---
id: dod-codex-runner-prd-inject
version: 1.0.0
created: 2026-03-14
updated: 2026-03-14
changelog:
  - 1.0.0: 初始版本
---

# DoD: Codex runner.sh PRD 预注入

## 验收清单

- [x] runner.sh 版本升级到 v2.2.0
  Test: manual:bash -c "grep -c 'v2.2.0' packages/engine/runners/codex/runner.sh"

- [x] 新增 `BRAIN_API_URL` 环境变量配置（默认 http://localhost:5221）
  Test: manual:bash -c "grep -c 'BRAIN_API_URL' packages/engine/runners/codex/runner.sh"

- [x] `fetch_task_prd()` 函数从 Brain API 预拉 PRD 内容
  Test: manual:bash -c "grep -c 'fetch_task_prd' packages/engine/runners/codex/runner.sh"

- [x] `build_comprehensive_prompt()` 接受 prd_title + prd_content 参数并嵌入 prompt
  Test: manual:bash -c "grep -c 'prd_content' packages/engine/runners/codex/runner.sh"

- [x] `build_resume_prompt()` 同样嵌入 PRD 内容（重试时也带 PRD）
  Test: manual:bash -c "grep -c 'PRD 开始' packages/engine/runners/codex/runner.sh"

- [x] 失败回调改用 `$BRAIN_API_URL`（不再 hardcoded localhost:5221）
  Test: manual:bash -c "grep -c 'BRAIN_API_URL.*api/brain/tasks' packages/engine/runners/codex/runner.sh"

- [x] PRD 预拉失败时优雅降级，日志明确标注
  Test: manual:bash -c "grep -c '降级到 --task-id' packages/engine/runners/codex/runner.sh"

- [x] Engine 版本 bump 到 12.63.0（6 个文件同步）
  Test: manual:bash -c "grep -c '12.63.0' packages/engine/package.json"

- [x] feature-registry.yml 新增 3.45.0 changelog 条目
  Test: manual:bash -c "grep -c '3.45.0' packages/engine/features/feature-registry.yml"

- [x] LEARNINGS.md 新增 PRD 预注入经验
  Test: manual:bash -c "grep -c 'PRD 预注入' docs/LEARNINGS.md"
