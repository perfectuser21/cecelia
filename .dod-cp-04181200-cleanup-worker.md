# DoD: R4 cleanup-worker

- [x] [BEHAVIOR] 脚本能识别 merged PR 的 worktree 并清理
  Test: tests/cleanup-worker (packages/brain/src/__tests__/cleanup-worker.test.js)

- [x] [BEHAVIOR] 有 uncommitted changes 的 worktree 不清理（Guard A，脚本内实现）
  Test: manual:bash -c "grep -q 'Guard A' packages/brain/scripts/cleanup-merged-worktrees.sh && grep -q 'has uncommitted changes' packages/brain/scripts/cleanup-merged-worktrees.sh"

- [x] [BEHAVIOR] PR 未 merged 的 worktree 不清理（Guard: pr_state != MERGED，脚本内实现）
  Test: manual:bash -c "grep -q 'not merged' packages/brain/scripts/cleanup-merged-worktrees.sh"

- [x] [BEHAVIOR] main/master/develop branch 不清理（Guard D，脚本内实现）
  Test: manual:bash -c "grep -qE 'main\\|master\\|develop' packages/brain/scripts/cleanup-merged-worktrees.sh"

- [x] [ARTIFACT] packages/brain/scripts/cleanup-merged-worktrees.sh 存在 + executable + 含安全守卫 A-E
  Test: manual:bash -c "[[ -x packages/brain/scripts/cleanup-merged-worktrees.sh ]] && for g in uncommitted unpushed merged_at human_hold; do grep -qi \"\$g\" packages/brain/scripts/cleanup-merged-worktrees.sh || exit 1; done"

- [x] [ARTIFACT] packages/brain/src/cleanup-worker.js export runCleanupWorker + 集成到 tick.js
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('runCleanupWorker')&&!c.includes('cleanup-worker'))process.exit(1)"

- [x] [ARTIFACT] Engine 14.17.3 六文件同步
  Test: manual:bash -c "v=\$(cat packages/engine/VERSION);[[ \$v == 14.17.3 ]]"

- [x] [ARTIFACT] feature-registry 14.17.3 changelog 含 orphan 和 cleanup
  Test: manual:node -e "const c=require('fs').readFileSync('packages/engine/feature-registry.yml','utf8');if(!c.includes('14.17.3')||!c.includes('orphan')||!c.includes('cleanup'))process.exit(1)"
