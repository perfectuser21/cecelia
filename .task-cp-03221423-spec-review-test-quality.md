# Task: spec-review SKILL.md 新增维度 D — DoD Test 字段可执行性验证

## 需求描述

当前 spec-review 审查 DoD 质量，但不检查 Test 字段是否是真实的行为验证命令。需要在 SKILL.md 中新增「维度 D：Test 字段可执行性验证（强制 blocker）」审查维度，并将原有维度 D「测试命令可执行性」重命名为维度 E，同时更新版本号和 FAIL 条件。

## 变更范围

- 文件：`packages/workflows/skills/spec-review/SKILL.md`
- 类型：SKILL 文件修改（不涉及 Brain 代码）

## 成功标准

- SKILL.md 中原维度 D「测试命令可执行性」重命名为维度 E
- 新增维度 D「Test 字段可执行性验证（强制 blocker）」，包含完整的检查表和示例
- FAIL 条件中新增「Test 字段质量不达标（维度 D 任一 blocker）」
- version 从 1.0.0 改为 1.1.0
- changelog 新增条目

## DoD

- [x] 原维度 D「测试命令可执行性」重命名为维度 E
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('### 维度 E：测试命令可执行性'))process.exit(1)"

- [x] 新增维度 D「Test 字段可执行性验证（强制 blocker）」章节存在
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('### 维度 D：Test 字段可执行性验证'))process.exit(1)"

- [x] FAIL 条件包含「Test 字段质量不达标（维度 D 任一 blocker）」
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('Test 字段质量不达标'))process.exit(1)"

- [x] version 已更新为 1.1.0
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('version: 1.1.0'))process.exit(1)"

- [x] changelog 包含 1.1.0 条目
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('1.1.0: '))process.exit(1)"

- [x] [BEHAVIOR] SKILL.md 同时包含维度 D 和维度 E 两个独立章节
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/spec-review/SKILL.md','utf8');if(!c.includes('### 维度 D：Test 字段可执行性验证')||!c.includes('### 维度 E：测试命令可执行性'))process.exit(1)"
