---
id: task-cp-03241913-knowledge-doc-system
type: task-card
branch: cp-03241913-knowledge-doc-system
created: 2026-03-24
brain-task-id: af0f5475-676a-440d-b46b-a5f73acdc3de
---

# Task Card: Cecelia Knowledge & Documentation System

## 需求（What & Why）

**功能描述**：在现有 Dashboard (apps/dashboard) 中构建统一文档系统，包含5个子模块：Dev Log、Decision Registry、Design Vault、Daily Diary、Knowledge Map。

**背景**：Cecelia 系统缺乏统一知识沉淀层。决策、PR记录、学习、设计方案散落在数据库/对话/文件中，每次新对话都从零开始认知。

**不做什么**：
- 不做历史 PR 数据回填（dev_records 从实施后开始积累）
- 不引入新的 UI 组件库（用已有 Tailwind + lucide-react）
- 不做完整 Markdown 编辑器（只读+备注）

## 成功标准

- 用户打开 `/knowledge/dev-log` 能看到历史 PR 完整记录页面正常渲染
- 用户打开 `/knowledge/diary` 能看到每日日报时间线页面正常渲染
- `dev_records`、`design_docs`、`user_annotations` 三张表已创建
- PR 合并后回调自动写入 dev_records（pr-callback-handler.js 扩展）
- 用户添加的标注（user_annotations）持久保存
- Brain API 3组新端点响应正常：`/api/brain/dev-records`、`/api/brain/design-docs`、`/api/brain/user-annotations`
- 每日 15:00 UTC（23:00 上海）tick 自动生成日报到 design_docs
- Brain CI + Workspace CI 全部通过

## 验收条件（DoD）

- [ ] [ARTIFACT] migration 187_dev_records.sql 已创建并可执行
  Test: manual:node -e "require('fs').accessSync('packages/brain/migrations/187_dev_records.sql')"

- [ ] [ARTIFACT] migration 188_design_docs.sql 已创建并可执行
  Test: manual:node -e "require('fs').accessSync('packages/brain/migrations/188_design_docs.sql')"

- [ ] [ARTIFACT] migration 189_user_annotations.sql 已创建并可执行
  Test: manual:node -e "require('fs').accessSync('packages/brain/migrations/189_user_annotations.sql')"

- [ ] [ARTIFACT] Brain API /api/brain/dev-records 路由文件已注册 GET 端点
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/dev-records.js','utf8');if(!c.includes('router.get'))process.exit(1)"

- [ ] [ARTIFACT] Brain API /api/brain/design-docs 路由文件已注册 GET 端点
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/design-docs.js','utf8');if(!c.includes('router.get'))process.exit(1)"

- [ ] [ARTIFACT] Brain API /api/brain/user-annotations 路由文件已注册 GET 端点
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/user-annotations.js','utf8');if(!c.includes('router.get'))process.exit(1)"

- [ ] [ARTIFACT] selfcheck.js EXPECTED_SCHEMA_VERSION 已更新为 '189'
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/selfcheck.js','utf8');if(!c.includes(\"'189'\"))process.exit(1)"

- [ ] [ARTIFACT] apps/api/features/knowledge/index.ts 包含5条新路由
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/index.ts','utf8');['dev-log','decisions','designs','diary','map'].forEach(r=>{if(!c.includes(r))process.exit(1)})"

- [ ] [ARTIFACT] 5个前端页面文件已创建
  Test: manual:node -e "['DevLog','DecisionRegistry','DesignVault','DailyDiary','KnowledgeMap'].forEach(p=>{require('fs').accessSync('apps/api/features/knowledge/pages/'+p+'.tsx')})"

- [ ] [PRESERVE] 现有 /knowledge/* 路由（KnowledgeHome、ContentStudio、SuperBrain 等）仍正常
  Test: manual:node -e "const c=require('fs').readFileSync('apps/api/features/knowledge/index.ts','utf8');['KnowledgeHome','ContentStudio','SuperBrain','KnowledgeDigestion','InstructionBook'].forEach(r=>{if(!c.includes(r))process.exit(1)})"

- [ ] [GATE] Brain CI 全部通过（vitest 配置已就绪）
  Test: manual:node -e "require('fs').accessSync('packages/brain/vitest.config.js')"

- [ ] [BEHAVIOR] POST /api/brain/user-annotations 创建标注后可立即查询到
  Test: tests/packages/brain/src/__tests__/pr-callback-handler.test.js

- [ ] [ARTIFACT] pr-callback-handler.js 已扩展写入 dev_records 逻辑
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/pr-callback-handler.js','utf8');if(!c.includes('dev_records'))process.exit(1)"

- [ ] [ARTIFACT] tick.js 已调用日报生成函数（generateDailyDiaryIfNeeded）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('generateDailyDiary'))process.exit(1)"

- [ ] [GATE] Brain CI 全部通过（L3/L4 CI 门禁验证）
  Test: manual:node -e "require('fs').accessSync('packages/brain/vitest.config.js')"

- [ ] [GATE] Workspace CI 全部通过（L3 workspace build 验证）
  Test: manual:node -e "require('fs').accessSync('apps/api/package.json')"

- [ ] [GATE] 所有迁移语法正确（psql 可执行）
  Test: manual:node -e "require('fs').accessSync('packages/brain/migrations/187_dev_records.sql');require('fs').accessSync('packages/brain/migrations/188_design_docs.sql');require('fs').accessSync('packages/brain/migrations/189_user_annotations.sql')"

## 实现方案

**要改的文件**：
- 新建：`packages/brain/migrations/187_dev_records.sql`
- 新建：`packages/brain/migrations/188_design_docs.sql`
- 新建：`packages/brain/migrations/189_user_annotations.sql`
- 修改：`packages/brain/src/selfcheck.js`（schema version → '189'）
- 新建：`packages/brain/src/routes/dev-records.js`
- 新建：`packages/brain/src/routes/design-docs.js`
- 新建：`packages/brain/src/routes/user-annotations.js`
- 修改：`packages/brain/server.js`（注册3个新路由）
- 修改：`packages/brain/src/pr-callback-handler.js`（写入 dev_records）
- 修改：`packages/brain/src/daily-review-scheduler.js`（加日报 tick）
- 修改：`packages/brain/src/tick.js`（调用日报生成）
- 修改：`apps/api/features/knowledge/index.ts`（5条新路由+组件）
- 新建：`apps/api/features/knowledge/pages/DevLog.tsx`
- 新建：`apps/api/features/knowledge/pages/DecisionRegistry.tsx`
- 新建：`apps/api/features/knowledge/pages/DesignVault.tsx`
- 新建：`apps/api/features/knowledge/pages/DailyDiary.tsx`
- 新建：`apps/api/features/knowledge/pages/KnowledgeMap.tsx`

**Scope 锚定**：Brain 后端（packages/brain）+ Workspace 前端（apps/api/features/knowledge）
