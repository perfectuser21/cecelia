# PRD: Knowledge System 补齐 — 导航 + 新增入口 + Markdown 渲染

## 目标

补齐 Knowledge & Documentation System 的核心缺口，让系统真正可用。

## 需要做的

### 1. 导航入口
修改 `apps/api/features/knowledge/index.ts`，在 navGroups 里加入知识库导航组。

### 2. Strategic Decisions 专属 API
新建 `packages/brain/src/routes/strategic-decisions.js`（GET/POST/PUT），
服务 decisions 表的战略决策（category/topic/decision/reason/status 字段）。
注册为 /api/brain/strategic-decisions。

### 3. DecisionRegistry 修复 + 新增入口
更新 DecisionRegistry.tsx 使用 /api/brain/strategic-decisions，
加「记录决策」按钮和表单。

### 4. DesignVault Markdown 渲染 + 新建入口
内联简单 Markdown 渲染函数，加「新建文档」按钮。

### 5. DevLog 评分录入
展开后加 self_score 输入（1-10），PUT 更新。

## 成功标准

- 侧边栏能看到知识库入口
- 能在前端新建 design_doc 和 decision
- Design Vault 内容 Markdown 格式化显示
- Dev Log 支持评分录入
