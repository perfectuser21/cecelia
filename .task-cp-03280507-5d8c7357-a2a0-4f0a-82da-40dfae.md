---
id: task-cp-03280507-5d8c7357
type: task-card
branch: cp-03280507-5d8c7357-a2a0-4f0a-82da-40dfae
created: 2026-03-28
---

# Task Card: CI L2 扩展 — Brain 新文件强制登记知识文档

## 需求（What & Why）
**功能描述**：修复 `brain-new-files-check` 独立 job，使其接受 `.agent-knowledge/brain.md` **或** `DEFINITION.md` 作为合法的知识文档更新（与 `brain-l2` 内嵌步骤保持一致）。

**背景**：PRs #1649/#1650 已合并，添加了 `brain-new-files-check` job，但该 job 目前只检查 `brain.md`，不接受 `DEFINITION.md`。`brain-l2` 内嵌步骤已正确支持两个文档，存在不一致。

**不做什么**：不修改 `brain-l2` 内嵌步骤（已正确）；不引入新的检查逻辑。

## 成功标准

1. `brain-new-files-check` job 接受 `brain.md` 或 `DEFINITION.md` 作为合法更新
2. 独立 job 与 `brain-l2` 内嵌步骤行为一致
3. CI L2 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] `.github/workflows/ci-l2-consistency.yml` 中 `brain-new-files-check` job 更新：接受 `brain.md` OR `DEFINITION.md`
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l2-consistency.yml','utf8');if(!c.includes('DEFINITION.md') || !c.includes('brain-new-files-check'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] 仅更新 `DEFINITION.md` 时 `brain-new-files-check` 通过
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l2-consistency.yml','utf8');if(!c.includes('DEFINITION\\.md') && c.includes('brain-new-files-check'))process.exit(1);console.log('OK')"

- [x] [PRESERVE] 现有 `brain-l2` 内嵌步骤逻辑不变
  Test: manual:node -e "const c=require('fs').readFileSync('.github/workflows/ci-l2-consistency.yml','utf8');if(!c.includes('BRAIN_MD_UPDATED'))process.exit(1);console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:node -e "require('fs').accessSync('.github/workflows/ci-l2-consistency.yml');console.log('file exists')"

## 实现方案
**要改的文件**：`.github/workflows/ci-l2-consistency.yml`（`brain-new-files-check` job，约 543-565 行）
**Scope 锚定**：brain-new-files-check job 的检测逻辑，增加 DEFINITION.md 支持
