---
id: task-cp-03231537-cap-scanner-fix
type: task-card
branch: cp-03231537-cap-scanner-fix
created: 2026-03-23
---

# Task Card: 修复 capability-scanner skillStats recent_30d 缺失

## 需求（What & Why）

`capability-scanner.js` 的 `scanCapabilities` 函数在 PR #1431 完成圈复杂度重构后，
发现 `skillStats` 查询缺少 `recent_30d` 字段，导致 `health.usage_30d` 对
来自 `run_events` 的技能始终为 0（只有 `taskUsageMap` 中的技能有此字段）。

## 成功标准

1. `skillStats` 查询新增 `recent_30d` 计算列（30 天内运行次数）
2. 新增 `failing` 状态场景测试（success_rate < 30%）
3. 所有现有测试继续通过

## 验收条件（DoD）

- [x] [ARTIFACT] skillStats SQL 查询新增 recent_30d 计算列
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/capability-scanner.js','utf8');if(!c.includes('recent_30d'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] failing 状态测试存在且通过（success_rate < 30%）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/capability-scanner.test.js','utf8');if(!c.includes('failing'))process.exit(1);console.log('ok')"

- [x] [PRESERVE] 现有 capability-scanner 全部测试继续通过
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/__tests__/capability-scanner.test.js','utf8');if(!c.includes('scanCapabilities'))process.exit(1);console.log('ok')"
