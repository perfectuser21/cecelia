# PRD: 修复 worktree GC 误删 + stop-dev 过早删锁 + Sprint Report 误导输出

## 目标

修复三个影响 /dev 工作流可靠性的 bug，确保 worktree 隔离机制在 context 超限场景下仍然正确工作，并修正 Sprint Report 的误导性输出。

## 成功标准

- worktree-gc.sh 对含有未完成 `.dev-mode` 的 worktree 不执行删除（即使 `.dev-session-active` 不存在）
- stop-dev.sh 只在 `.dev-mode` 含 `cleanup_done: true` 时才删除 `.dev-session-active`
- Sprint Report seal 文件缺失时输出「⚠️ 无法检测（Seal 文件缺失）」并评 0 分（不再是硬编码的「Planner 预填了 Test」和虚假的 5 分）
- 所有功能产出物（artifact）的运行时行为（behavior）均经过验证，结果符合预期
- Engine CI Gate 门禁检查通过，版本 bump 同步到 5 个文件（13.73.2）

## 范围

- packages/engine/skills/dev/scripts/worktree-gc.sh（修改）
- packages/engine/hooks/stop-dev.sh（修改）
- packages/engine/scripts/devgate/generate-sprint-report.sh（修改）
- Engine 版本 bump

## 任务 ID
aa1a59bb-a335-4bd6-80fa-8369586857e8
