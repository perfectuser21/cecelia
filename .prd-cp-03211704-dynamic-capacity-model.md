# Task Card: 动态产能模型

## 需求（What & Why）
**功能描述**: Brain 新增 capacity-budget API，基于历史实际完成数据动态计算产能（pr_per_slot_per_day），供 /decomp 和 /decomp-check 使用。冷启动用理论值，随数据积累自校准。

**背景**: 当前 /decomp SKILL.md 中的产能模型是写死的（12 slots、287 PR/day），但实际有 3 台 Mac Mini（26 slots）。校准表的 PR 数量也是固定值，导致拆解粒度不准。需要改为动态查询，让拆解精度随运行数据自动提升。

**不做什么**:
- 不改 slot-allocator.js 的调度逻辑
- 不改 account-usage.js 的 5h/7d 限制
- 不做产能仪表盘（那是另一个任务）

## 成功标准

1. [ARTIFACT] Brain 新增 `GET /api/brain/capacity-budget` API 端点
2. [BEHAVIOR] API 返回 `pr_per_slot_per_day`，冷启动返回理论值 25，有历史数据后返回实测值
3. [BEHAVIOR] API 返回 `confidence` 字段（theoretical/low/medium/high），基于 sample_size 动态变化
4. [ARTIFACT] /decomp SKILL.md 产能模型章节改为动态查询说明，删除写死数字
5. [ARTIFACT] /decomp-check SKILL.md 新增 PR 数量校验检查项
6. [GATE] 所有现有测试通过

## 验收条件（DoD）

- [x] [ARTIFACT] `packages/brain/src/routes/` 下新增 capacity-budget 路由文件
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/capacity-budget.js','utf8');if(!c.includes('capacity-budget'))process.exit(1)"

- [x] [BEHAVIOR] 代码中定义冷启动理论值 pr_per_slot_per_day=25 和 confidence=theoretical
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/capacity-budget.js','utf8');if(!c.includes('THEORETICAL_PR_PER_SLOT_PER_DAY = 25')||!c.includes('theoretical'))process.exit(1)"

- [x] [BEHAVIOR] 代码中定义 area 级别的 slot 分配（至少包含 cecelia 和 zenithjoy）
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/routes/capacity-budget.js','utf8');if(!c.includes('cecelia')||!c.includes('zenithjoy'))process.exit(1)"

- [x] [ARTIFACT] /decomp SKILL.md 产能模型章节引用 capacity-budget API，不含写死的日产能/月产能数字
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/decomp/SKILL.md','utf8');if(c.includes('~287 PR/day')||c.includes('~8,600 PR/month'))process.exit(1)"

- [x] [ARTIFACT] /decomp-check SKILL.md 包含 PR 数量校验检查项
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/decomp-check/SKILL.md','utf8');if(!c.includes('PR 数量'))process.exit(1)"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "npm test 2>&1 | tail -5"

## 实现方案（Stage 2 探索后填写）
**要改的文件**: （探索后填写）
**Scope 锚定**: （探索后填写）
