---
id: task-cp-03210607-8333fca2-c9d8-4da1-9d1b-1b5e23
type: task-card
branch: cp-03210607-8333fca2-c9d8-4da1-9d1b-1b5e23
created: 2026-03-21
---

# Task Card: 新增 codex_security_scan 任务类型 + SKILL.md

## 需求（What & Why）
**功能描述**: 新增 `codex_security_scan` task_type，注册到 Brain 各器官（task-router、executor、tick、slot-allocator、token-budget-planner、role-registry）。同时创建对应 SKILL.md（packages/workflows/skills/codex-security-scan/SKILL.md），让 Codex 在西安 M4 自动扫描代码安全问题（OWASP Top 10），输出安全报告。

**背景**: 免疫系统 KR 推进——继 codex_test_gen（自动测试生成）之后，新增安全扫描能力。

**不做什么**: 不实现具体扫描逻辑代码，只做注册和 SKILL.md。

## 成功标准

1. [ARTIFACT] task-router.js 包含 `codex_security_scan` 路由到 xian + skill `/codex-security-scan`
2. [ARTIFACT] executor.js、tick.js、slot-allocator.js、token-budget-planner.js、role-registry.js 均包含 `codex_security_scan`
3. [ARTIFACT] `packages/workflows/skills/codex-security-scan/SKILL.md` 已创建
4. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] [ARTIFACT] task-router.js VALID_TASK_TYPES、SKILL_WHITELIST、LOCATION_MAP 均包含 codex_security_scan
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/task-router.js','utf8');if(!c.includes('codex_security_scan'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] executor.js skillMap + getModelForTask 包含 codex_security_scan
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');if(!c.includes('codex_security_scan'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] tick.js isCodexNativeTask 包含 codex_security_scan
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/tick.js','utf8');if(!c.includes('codex_security_scan'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] slot-allocator.js countCodexInProgress SQL 包含 codex_security_scan
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/slot-allocator.js','utf8');if(!c.includes('codex_security_scan'))process.exit(1);console.log('OK')"

- [x] [ARTIFACT] token-budget-planner.js + role-registry.js 包含 codex_security_scan
  Test: manual:node -e "['packages/brain/src/token-budget-planner.js','packages/brain/src/role-registry.js'].forEach(f=>{const c=require('fs').readFileSync(f,'utf8');if(!c.includes('codex_security_scan')){console.error('MISSING:',f);process.exit(1)}});console.log('OK')"

- [x] [ARTIFACT] SKILL.md 已创建且包含 OWASP 扫描流程
  Test: manual:node -e "const c=require('fs').readFileSync('packages/workflows/skills/codex-security-scan/SKILL.md','utf8');if(!c.includes('OWASP'))process.exit(1);console.log('OK')"

- [x] [BEHAVIOR] Brain 可解析 codex_security_scan 任务类型不报错
  Test: manual:node -e "const r=require('./packages/brain/src/task-router.js');const vt=r.VALID_TASK_TYPES||[];if(!vt.includes('codex_security_scan')){console.error('NOT IN VALID_TASK_TYPES');process.exit(1)}console.log('OK')"

- [x] [GATE] 所有现有测试通过
  Test: manual:bash -c "cd packages/brain && npm test 2>&1 | tail -10"

## 实现方案

**要改的文件**:
- `packages/brain/src/task-router.js` — 3 处：VALID_TASK_TYPES、SKILL_WHITELIST、LOCATION_MAP
- `packages/brain/src/executor.js` — 2 处：skillMap、getModelForTask null check
- `packages/brain/src/tick.js` — 1 处：isCodexNativeTask
- `packages/brain/src/slot-allocator.js` — 2 处：注释 + SQL IN 子句
- `packages/brain/src/token-budget-planner.js` — 1 处：codex 预算类型
- `packages/brain/src/role-registry.js` — 1 处：CTO task_types
- `packages/workflows/skills/codex-security-scan/SKILL.md` — 新建

**Scope 锚定**: 仅注册配置 + 新建 SKILL.md，不修改业务逻辑
