---
id: dod-cp-03120415-inject-learning-on-dispatch
version: 1.0.0
created: 2026-03-12
updated: 2026-03-12
changelog:
  - 1.0.0: 初始版本
---

## DoD - Brain 派发任务时注入相关 Learning 上下文

### 验收清单

- [x] 新建 `packages/brain/src/learning-retriever.js`，导出 `buildLearningContext(task)` 函数
  Test: manual:bash -c "grep -c 'export async function buildLearningContext' packages/brain/src/learning-retriever.js"

- [x] `buildLearningContext` 查询 learnings 表，按 task_type/domain/title 关键词打分
  Test: manual:bash -c "grep -c 'normalizedScore' packages/brain/src/learning-retriever.js"

- [x] 仅注入 normalizedScore > 0.3 的 learning，最多 3 条
  Test: manual:bash -c "grep -c 'SCORE_THRESHOLD\|MAX_INJECT' packages/brain/src/learning-retriever.js"

- [x] 按 learning_type 分组展示
  Test: manual:bash -c "grep -c 'learning_type\|byType' packages/brain/src/learning-retriever.js"

- [x] 查询失败时降级（return ''，不影响 dispatch）
  Test: manual:bash -c "grep -c 'catch.*err\|return.*empty\|\\'\\'' packages/brain/src/learning-retriever.js"

- [x] `executor.js` 的 `preparePrompt` 注入 learningCtx
  Test: manual:bash -c "grep -c 'buildLearningContext\|learningCtx' packages/brain/src/executor.js"

- [x] 新增单元测试 `packages/brain/src/__tests__/learning-retriever.test.js`
  Test: manual:bash -c "grep -c 'describe\|it(' packages/brain/src/__tests__/learning-retriever.test.js"
