---
id: dod-cp-03100801-bridge-timeout-fix
version: 1.0.0
created: 2026-03-10
updated: 2026-03-10
changelog:
  - 1.0.0: 初始版本
---

# DoD: Bridge 超时修复

## 验收清单

- [x] cecelia-bridge.js 增加 `BRIDGE_TIMEOUT_MS` 常量，从 `CECELIA_BRIDGE_TIMEOUT_MS` 读取，默认 120000
  Test: manual:bash -c "grep -q 'CECELIA_BRIDGE_TIMEOUT_MS' packages/brain/scripts/cecelia-bridge.js"
- [x] cecelia-bridge.cjs 同步修改（内容与 .js 相同）
  Test: manual:bash -c "grep -q 'CECELIA_BRIDGE_TIMEOUT_MS' packages/brain/scripts/cecelia-bridge.cjs"
- [x] bridge 超时时返回 HTTP 200 + degraded 响应（非 HTTP 500）
  Test: manual:bash -c "grep -q 'degraded.*true' packages/brain/scripts/cecelia-bridge.js"
- [x] llm-caller.js 识别 `degraded: true` 响应并抛出带 `degraded` 属性的错误
  Test: manual:bash -c "grep -q 'degraded.*true' packages/brain/src/llm-caller.js"
- [x] cortex.js 超时时调用 `recordFailure`，并记录 error_message 到任务
  Test: manual:bash -c "grep -q 'recordFailure' packages/brain/src/cortex.js"
- [x] 新增 bridge-timeout.test.js 测试文件
  Test: manual:bash -c "grep -q 'describe' packages/brain/src/__tests__/bridge-timeout.test.js"
