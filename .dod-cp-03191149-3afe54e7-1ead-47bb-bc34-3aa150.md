# DoD: fix(brain): status.js 缺失 IDEMPOTENCY_TTL/ALLOWED_ACTIONS 导入

## 验收条件

- [ ] `packages/brain/src/routes/status.js` import 语句包含 `IDEMPOTENCY_TTL` 和 `ALLOWED_ACTIONS`
- [ ] `/api/brain/status` 返回完整 JSON，`action_constraints.idempotency_ttl_seconds` = 300
- [ ] `action_constraints.allowed_actions` 列表非空

## Tests

- manual: `node -e "const s=require('fs').readFileSync('packages/brain/src/routes/status.js','utf8');if(!s.includes('IDEMPOTENCY_TTL')||!s.includes('ALLOWED_ACTIONS'))process.exit(1);console.log('OK')"`
