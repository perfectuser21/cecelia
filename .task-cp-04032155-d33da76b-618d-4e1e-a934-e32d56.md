---
id: task-cp-04032155-d33da76b-618d-4e1e-a934-e32d56
type: task-card
branch: cp-04032155-d33da76b-618d-4e1e-a934-e32d56
created: 2026-04-03
---

# Task Card: 重构 executor.preparePrompt（CC 107 → 10）

## 需求（What & Why）
**功能描述**: 将 `packages/brain/src/executor.js` 中的 `preparePrompt` 函数从单一巨型函数拆分为多个命名子函数，并通过 dispatch map 路由。
**背景**: 圈复杂度 107（阈值 10），每个 if/else 分支处理一种 taskType，难以维护。
**不做什么**: 不改变任何业务逻辑，不修改 API 接口，不改其他函数。

## 成功标准

- [x] [BEHAVIOR] preparePrompt 路由逻辑拆分为 10+ 命名子函数，主函数 CC < 10
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');const m=c.match(/^(?:async function|function) build\\w+Prompt/mg);if(!m||m.length<8)process.exit(1);console.log('子函数数量:',m.length)"

- [x] [BEHAVIOR] 所有现有 preparePrompt 测试通过
  Test: manual:node -e "require('fs').accessSync('packages/brain/src/__tests__/executor-sprint-prompt.test.js')"

- [x] [ARTIFACT] executor.js 中 preparePrompt 主体行数 < 50 行
  Test: manual:node -e "const c=require('fs').readFileSync('packages/brain/src/executor.js','utf8');const m=c.match(/async function preparePrompt[\s\S]*?^}/m);if(!m)process.exit(1);const lines=m[0].split('\n').length;if(lines>50){console.error('行数:',lines);process.exit(1);}console.log('OK行数:',lines)"

## 实现方案

**要改的文件**: `packages/brain/src/executor.js`

**重构策略**:
1. 提取 18 个命名子函数（每种 taskType 对应一个）：
   - `buildDecompositionPrompt(task)` — decomposition 分支
   - `buildInitiativePlanPrompt(task)` — initiative_plan
   - `buildScopePlanPrompt(task)` — scope_plan
   - `buildProjectPlanPrompt(task)` — project_plan
   - `buildSprintPrompt(task)` — sprint_generate/sprint_fix/harness
   - `buildSprintEvaluatePrompt(task)` — sprint_evaluate
   - `buildInitiativeVerifyPrompt(task)` — initiative_verify
   - `buildArchitectureDesignPrompt(task)` — architecture_design
   - `buildDecompReviewPrompt(task)` — decomp_review
   - `buildPrdReviewPrompt(task)` — prd_review
   - `buildSpecReviewPrompt(task)` — spec_review
   - `buildCodeReviewGatePrompt(task)` — code_review_gate
   - `buildInitiativeReviewPrompt(task)` — initiative_review
   - `buildTalkPrompt(task)` — talk
   - `buildReviewPrompt(task)` — review/qa/audit
   - `buildResearchPrompt(task)` — research
   - `buildCodeReviewPrompt(task)` — code_review
   - `buildDefaultPrompt(task, skill)` — fallback

2. `preparePrompt` 主体变为：先处理 decomposition（异步），再通过 taskType dispatch map 调用对应子函数

**不改什么**: 所有子函数内部逻辑与原代码完全相同，只是搬运。
