# DoD: War Room 横向多列布局

## 完成标准

- [x] GTDWarRoom.tsx 使用 `h-full flex-col` 撑满容器高度
- [x] 下方内容区使用 `grid-cols-1 md:grid-cols-3` 横向三列布局
- [x] 每列容器包含 `overflow-y-auto` 实现独立滚动
- [x] Vision 横幅保持在顶部固定区域（不参与列布局）
- [x] 无 TypeScript 编译错误

## Tests

### [BEHAVIOR] War Room 页面使用 h-full flex-col 布局
manual: node -e "const c=require('fs').readFileSync('apps/api/features/gtd/pages/GTDWarRoom.tsx','utf8');if(!c.includes('h-full'))process.exit(1);console.log('OK')"

### [BEHAVIOR] 页面包含三列 grid 布局
manual: node -e "const c=require('fs').readFileSync('apps/api/features/gtd/pages/GTDWarRoom.tsx','utf8');if(!c.includes('grid-cols-3'))process.exit(1);console.log('OK')"

### [BEHAVIOR] 每列有独立 overflow-y-auto 滚动
manual: node -e "const c=require('fs').readFileSync('apps/api/features/gtd/pages/GTDWarRoom.tsx','utf8');if(!c.includes('overflow-y-auto'))process.exit(1);console.log('OK')"
