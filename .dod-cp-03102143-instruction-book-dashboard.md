---
id: dod-instruction-book-dashboard
version: 1.0.0
created: 2026-03-10
---

# DoD：Instruction Book Dashboard 页面

## 验收清单

- [x] D1: Brain API 端点存在，能返回 instruction-book 数据
  Test: manual:bash -c "curl -s http://localhost:5221/api/brain/docs/instruction-book | node -e \"const d=require('fs').readFileSync('/dev/stdin','utf8'); const j=JSON.parse(d); process.exit(j.skills && j.features ? 0 : 1)\""

- [x] D2: API 返回包含 skills 数组且非空
  Test: manual:bash -c "curl -s http://localhost:5221/api/brain/docs/instruction-book | node -e \"const d=require('fs').readFileSync('/dev/stdin','utf8'); const j=JSON.parse(d); process.exit(Array.isArray(j.skills) && j.skills.length > 0 ? 0 : 1)\""

- [x] D3: API 返回包含 features 数组且非空
  Test: manual:bash -c "curl -s http://localhost:5221/api/brain/docs/instruction-book | node -e \"const d=require('fs').readFileSync('/dev/stdin','utf8'); const j=JSON.parse(d); process.exit(Array.isArray(j.features) && j.features.length > 0 ? 0 : 1)\""

- [x] D4: InstructionBook.tsx 页面文件存在
  Test: manual:bash -c "ls frontend/src/features/core/knowledge/pages/InstructionBook.tsx"

- [x] D5: knowledge manifest 中注册了 /knowledge/instruction-book 路由
  Test: manual:bash -c "grep -q 'instruction-book' frontend/src/features/core/knowledge/index.ts"

- [x] D6: KnowledgeHome 有 instruction-book 入口卡片
  Test: manual:bash -c "grep -q 'instruction-book\|InstructionBook\|Instruction Book' frontend/src/features/core/knowledge/pages/KnowledgeHome.tsx"
