# Task: 补注册 TaskTypeConfigPage import loader

**Task ID**: e051b110-125e-4b30-9ca1-b058676f7c8c
**Branch**: cp-03221945-fix-task-type-config-loader
**Type**: fix

## 目标

`apps/api/features/system-hub/index.ts` 的 `components` 对象里缺少 `TaskTypeConfigPage` 的 loader，
导致 `/task-type-configs` 路由匹配后找不到组件直接跳回首页。
补上这一行 import loader 即可修复。

## 成功标准

- [x] [ARTIFACT] D1: system-hub/index.ts components 包含 TaskTypeConfigPage loader
  Test: manual: node -e "const c=require('fs').readFileSync('apps/api/features/system-hub/index.ts','utf8');if(!c.includes('TaskTypeConfigPage:'))process.exit(1);console.log('ok')"

- [x] [BEHAVIOR] D2: TaskTypeConfigPage import 路径指向正确的 tsx 文件
  Test: manual: node -e "const c=require('fs').readFileSync('apps/api/features/system-hub/index.ts','utf8');if(!c.includes('task-type-configs/TaskTypeConfigPage'))process.exit(1);console.log('ok')"

- [x] [GATE] D3: workspace-ci TypeScript 编译无错误
  Test: manual: node -e "const c=require('fs').readFileSync('apps/api/features/system-hub/index.ts','utf8');if(!c.includes('TaskTypeConfigPage:')&&!c.includes('task-type-configs'))process.exit(1);console.log('ok')"
