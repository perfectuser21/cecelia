# Task Card: 建立 test-registry.yaml + 测试分类 ADR

**Branch**: cp-04011011-test-registry-adr
**Brain Task**: 07acd416-4552-4b4a-820e-5188f008268d
**Commit type**: chore:
**Created**: 2026-04-01

---

## 背景

Cecelia 系统已有完善的 registry 生态（skills-registry.json、workflow-registry.json、cross-ref-registry.yaml），但唯独测试文件没有注册机制。当前 `tests/` 根目录下存在 25 个孤儿测试文件游离在 CI 之外，无人发现、无人负责。

架构审查（2026-04-01）将此列为 🔴 High 问题：测试体系存在结构性空缺。

---

## 目标

1. 建立 `test-registry.yaml`，定义测试分类标准（unit/integration/e2e）
2. 补录所有 25 个孤儿测试文件，明确每个文件的类型和应跑的 CI 层级
3. 写 ADR 文档，记录测试分类标准决策

---

## 成功标准

1. `test-registry.yaml` 存在于 repo 根目录
2. 注册了全部 25 个 `tests/` 目录下的孤儿测试文件
3. 每个条目有 `type`（unit/integration/e2e）和 `ci`（L3/L4）字段
4. `docs/adr/` 下有对应的 ADR 文档

---

## DoD

- [x] **[ARTIFACT]** `test-registry.yaml` 存在于根目录
  - Test: `manual:node -e "require('fs').accessSync('test-registry.yaml')"`

- [x] **[ARTIFACT]** `docs/adr/` 下 ADR 文档存在
  - Test: `manual:node -e "const fs=require('fs');const files=fs.readdirSync('docs/adr/');if(!files.some(f=>f.includes('test-taxonomy')))process.exit(1)"`

- [x] **[BEHAVIOR]** test-registry.yaml 包含 unit/integration/e2e 三种分类
  - Test: `manual:node -e "const c=require('fs').readFileSync('test-registry.yaml','utf8');if(!c.includes('unit')||!c.includes('integration')||!c.includes('e2e'))process.exit(1)"`

- [x] **[BEHAVIOR]** test-registry.yaml 覆盖 tests/ 目录所有孤儿文件（≥25条）
  - Test: `manual:node -e "const c=require('fs').readFileSync('test-registry.yaml','utf8');const matches=c.match(/- id:/g);if(!matches||matches.length<25)process.exit(1)"`
