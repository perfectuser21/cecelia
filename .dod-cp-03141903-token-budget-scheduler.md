---
id: dod-token-budget-scheduler
version: 1.0.0
created: 2026-03-14
updated: 2026-03-14
changelog:
  - 1.0.0: 初始版本
---

# DoD：动态 Token 预算调度器

## 验收清单

- [x] 新建 token-budget-planner.js，导出 calculateBudgetState()
  Test: manual:bash -c "grep -c 'calculateBudgetState' packages/brain/src/token-budget-planner.js"

- [x] calculateBudgetState() 返回 state: abundant|moderate|tight|critical
  Test: manual:bash -c "grep -c 'abundant\|moderate\|tight\|critical' packages/brain/src/token-budget-planner.js"

- [x] 30% 用户预留逻辑：USER_RESERVE_PCT = 0.30
  Test: manual:bash -c "grep -c 'USER_RESERVE_PCT' packages/brain/src/token-budget-planner.js"

- [x] getExecutorAffinity() 返回 dev 任务的降级路径（fallback: codex）
  Test: manual:bash -c "grep -c 'fallback.*codex' packages/brain/src/token-budget-planner.js"

- [x] architecture_design / strategy_session / initiative_plan 标记 no_downgrade: true
  Test: manual:bash -c "grep -c 'no_downgrade.*true' packages/brain/src/token-budget-planner.js"

- [x] slot-allocator.js 集成 budget_state，Pool C 按 state 动态缩放
  Test: manual:bash -c "grep -c 'budget_state\|calculateBudgetState' packages/brain/src/slot-allocator.js"

- [x] tick.js 在 dispatch 前检查降级并设置 provider=codex
  Test: manual:bash -c "grep -c 'shouldDowngrade\|provider.*codex' packages/brain/src/tick.js"

- [x] 单元测试覆盖 calculateBudgetState 四种状态
  Test: manual:bash -c "grep -c 'abundant\|moderate\|tight\|critical' packages/brain/src/__tests__/token-budget-planner.test.js"

- [x] Brain 版本 bump（feat:）
  Test: manual:bash -c "grep -c '\"version\"' packages/brain/package.json"
