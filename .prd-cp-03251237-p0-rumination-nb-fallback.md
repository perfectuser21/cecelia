# PRD: P0 修复 — Rumination NotebookLM fallback 丢上下文

**Branch**: cp-03251237-p0-rumination-nb-fallback
**Task ID**: 2083997e-ba96-456a-913a-fb7dfb5deb6d

## 问题

`rumination.js:digestLearnings` 在 NotebookLM 不可用时 fallback 到 `callLLM`，但传入空 `notebookContext`（`buildRuminationPrompt(learnings, memoryBlock, "")`）。LLM 完全看不到历史反刍洞察，只能基于当次 learnings 输出，产生重复/浅层洞察。

## 成功标准

1. [PRESERVE] 现有 NotebookLM 主路不变（`queryNotebook` 成功时行为不变）
2. [ARTIFACT] `digestLearnings` fallback 路径：从 `synthesis_archive` 查最近 7 天 daily 历史洞察，拼接为 `fallbackContext`
3. [ARTIFACT] `buildRuminationPrompt` 在 fallback 时传入 `fallbackContext`（非空时注入 `## 历史反刍上下文` 区块）
4. [BEHAVIOR] NotebookLM 主路失败 → fallback 仍注入历史上下文（有 synthesis_archive 记录时）
5. [GATE] CI 全部通过
