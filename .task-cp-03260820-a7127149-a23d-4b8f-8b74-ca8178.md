---
id: task-cp-03260820-a7127149-a23d-4b8f-8b74-ca8178
type: task-card
branch: cp-03260820-a7127149-a23d-4b8f-8b74-ca8178
brain_task_id: a7127149-a23d-4b8f-8b74-ca8178f6f2d0
created: 2026-03-26
---

# Task Card: Content Pipeline Audit — 全链路审计报告

## 需求（What & Why）

**功能描述**：
对 ZenithJoy 内容 pipeline 进行全链路审计，产出结构化审计报告，记录：
1. 现有 pipeline 架构图（采集 → 生产 → 发布 → 回流）
2. 各节点连通状态（✅ 通 / ❌ 断 / ⚠️ 部分）
3. P0/P1 问题清单及修复建议

**背景**：内容 pipeline 由多个子系统组成，近期完成了多个子 PR（scraper work_id、pipeline executor、publisher skill），需要一次全局审计确认整体连通状态。

**不做什么**：
- 不修复发现的问题（只审计记录，修复由后续任务执行）
- 不改 Brain 核心代码
- 不做内容生成或发布

## 成功标准

1. [ARTIFACT] 审计报告文件存在于 `docs/audits/`，报告包含 P0 问题标注和 social_media_raw 数据库 P0 问题
2. [ARTIFACT] 报告包含发布系统分析（publisher 覆盖情况）
3. [BEHAVIOR] audit-content-pipeline.sh 脚本语法正确（可执行），健康检查涵盖 4 层

## 验收条件（DoD）

- [x] [ARTIFACT] 审计报告文件存在
  Test: manual:node -e "const fs=require('fs');if(!fs.existsSync('docs/audits/content-pipeline-audit-20260326.md'))process.exit(1)"

- [x] [ARTIFACT] 报告包含 P0 问题标注
  Test: manual:node -e "const c=require('fs').readFileSync('docs/audits/content-pipeline-audit-20260326.md','utf8');if(!c.includes('P0'))process.exit(1)"

- [x] [ARTIFACT] 报告包含 social_media_raw 数据库 P0 问题
  Test: manual:node -e "const c=require('fs').readFileSync('docs/audits/content-pipeline-audit-20260326.md','utf8');if(!c.includes('social_media_raw'))process.exit(1)"

- [x] [ARTIFACT] 报告包含发布系统分析
  Test: manual:node -e "const c=require('fs').readFileSync('docs/audits/content-pipeline-audit-20260326.md','utf8');if(!c.includes('publisher'))process.exit(1)"

- [x] [BEHAVIOR] audit-content-pipeline.sh 脚本语法正确（可执行）
  Test: manual:bash -n scripts/audit-content-pipeline.sh

## 实现方案

**要改的文件**:
- `docs/audits/content-pipeline-audit-20260326.md`（新建，审计报告）
- `scripts/audit-content-pipeline.sh`（新建，健康检查脚本）

**Scope 锚定**: 只新增文档和脚本文件，不改现有代码
