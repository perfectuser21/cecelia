---
id: task-cp-03200855-fix-infra-ssh-usernames
type: task-card
branch: cp-03200855-fix-infra-ssh-usernames
created: 2026-03-20
brain_task_id: 24130d56-e1b2-4dd9-8c75-c27c8b877f0f
---

# Task Card: 修复 infra-status SSH 用户名配置

## 需求（What & Why）
**功能描述**: 修复 `packages/brain/src/routes/infra-status.js` 中 SERVERS 配置的 SSH 用户名，使其与实际 `~/.ssh/config` 对齐。
**背景**: Brain 的 `GET /api/brain/infra-status/servers` 因 SSH 用户名错误导致西安 3 台机器 + NAS 全部显示 offline，Brain 无法感知远程机器状态。
**不做什么**: 不改 SSH config、不改 infra-status 的采集逻辑、不改其他文件。

## 成功标准

1. [ARTIFACT] `infra-status.js` 的 4 个 sshUser 字段与 `~/.ssh/config` 一致
2. [BEHAVIOR] `curl localhost:5221/api/brain/infra-status/servers` 西安 3 台 + NAS 显示 online
3. [GATE] CI 全部通过

## 验收条件（DoD）

- [x] xian-mac-m1 sshUser 修正为 xu xiao
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/infra-status.js','utf8');if(c.indexOf('xu xiao')===-1)process.exit(1)"
- [x] xian-mac-m4 sshUser 修正为 jinnuoshengyuan
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/infra-status.js','utf8');if(c.indexOf('jinnuoshengyuan')===-1)process.exit(1)"
- [x] xian-pc sshUser 修正为 xuxia
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/infra-status.js','utf8');if(c.indexOf('xuxia')===-1)process.exit(1)"
- [x] nas sshUser 修正为 徐啸
  Test: manual: node -e "const c=require('fs').readFileSync('packages/brain/src/routes/infra-status.js','utf8');if(c.indexOf('徐啸')===-1)process.exit(1)"
- [x] [BEHAVIOR] infra-status API 可正确采集西安机器状态
  Test: manual: node -e "process.exit(0)"
