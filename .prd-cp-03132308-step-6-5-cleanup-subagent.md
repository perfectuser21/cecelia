---
id: prd-cp-03132308-step-6-5-cleanup-subagent
version: 1.0.0
created: 2026-03-13
updated: 2026-03-13
changelog:
  - 1.0.0: 初始版本
---

## PRD - Step 6.5 Cleanup Sub-Agent（自动清理改动文件中的垃圾代码）

**需求来源**: Step 6 的"必须清理的4类问题"当前是软规则（只是 .md 文字，AI 可无视）。需要把它变成真锁：Step 6 完成后自动召唤 sub-agent 扫描改动文件，找到并删除4类垃圾代码。

**功能描述**:
1. 修改 `packages/engine/skills/dev/steps/06-code.md`：将现有 Step 6.5 内联文字规则升级为真正调用 Agent 工具（subagent_type=general-purpose）的执行节点
2. devloop-check.sh 已足够完整，不需要修改（step_6_5 是 step 内部执行逻辑，不是 devloop 状态机的条件）
3. Engine 版本 bump 到 12.58.0（feature 级别）+ feature-registry.yml 新增条目
4. PR title 含 [CONFIG]

**涉及文件**:
- `packages/engine/skills/dev/steps/06-code.md`（核心修改：Step 6.5 升级为真正 sub-agent 调用）
- `packages/engine/package.json`（版本 bump）
- `packages/engine/package-lock.json`（版本 bump）
- 根 `package-lock.json`（engine 条目版本 bump）
- `packages/engine/VERSION`（版本 bump）
- `packages/engine/.hook-core-version`（版本 bump）
- `packages/engine/regression-contract.yaml`（版本 bump）
- `packages/engine/features/feature-registry.yml`（新增条目）

## 成功标准

- 06-code.md 的 Step 6.5 节包含明确的 Agent 工具调用指令（subagent_type=general-purpose），而不是仅文字描述
- sub-agent prompt 包含4类垃圾代码的扫描指令
- sub-agent 完成后有明确的"确认 diff → 误删恢复 → 继续 Step 7"流程
- Engine 版本已 bump 到 12.58.0（6个文件同步）
- feature-registry.yml 有新条目记录此改动
- PR title 含 [CONFIG]

**非目标**:
- 不修改 devloop-check.sh（step_6_5 不是 devloop 状态机条件）
- 不修改 .dev-mode 格式（step_6_5_cleanup flag 方案过于复杂，软约束已够）
- 不写 stop hook 兜底（依赖 AI 执行，不加 flag 检查）

### 异常处理 & 报警规范

N/A - 无 IO 操作，仅修改文档和版本文件
